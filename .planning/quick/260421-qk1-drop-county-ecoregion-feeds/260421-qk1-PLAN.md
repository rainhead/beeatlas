---
phase: quick
plan: 260421-qk1
type: execute
wave: 1
depends_on: []
files_modified:
  - data/feeds.py
  - data/tests/test_feeds.py
  - frontend/public/data/feeds/county-*.xml   # 39 files deleted
  - frontend/public/data/feeds/ecoregion-*.xml  # 66 files deleted
autonomous: true
requirements: [QK1]
must_haves:
  truths:
    - "No county-*.xml or ecoregion-*.xml files exist under frontend/public/data/feeds/"
    - "feeds.py contains no _COUNTY_QUERY, _ECOREGION_QUERY, or county/ecoregion entries in any dict or loop"
    - "All remaining feeds tests pass (collector, genus, determinations, slugify, index)"
  artifacts:
    - path: data/feeds.py
      provides: "Feeds generator — collector and genus variants only"
    - path: data/tests/test_feeds.py
      provides: "Tests for remaining feed types"
  key_links:
    - from: "write_all_variants() _ENUM_QUERIES dict"
      to: "_VARIANT_QUERIES dict"
      via: "_VARIANT_QUERIES[variant_type]"
      pattern: "collector.*genus"
---

<objective>
Remove county and ecoregion Atom feed generation from feeds.py and delete the 105 pre-generated XML files.

Purpose: Reduce pipeline complexity and static asset footprint — county/ecoregion feeds are unused.
Output: Leaner feeds.py (collector + genus only), cleaned test suite, 105 fewer committed XML files.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@./CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Strip county/ecoregion from feeds.py</name>
  <files>data/feeds.py</files>
  <action>
Make the following targeted edits to data/feeds.py:

1. **Module docstring** (lines 1-16): Remove the two bullet lines for county and ecoregion feeds:
   - Delete:  `  frontend/public/data/feeds/county-{slug}.xml      (one per WA county)`
   - Delete:  `  frontend/public/data/feeds/ecoregion-{slug}.xml   (one per WA ecoregion)`

2. **`_TITLE_TEMPLATES` dict** (lines 155-160): Remove the `'county'` and `'ecoregion'` entries. Result should have only `'collector'` and `'genus'` keys.

3. **`_COUNTY_QUERY` constant** (lines 198-223): Delete the entire constant.

4. **`_ECOREGION_QUERY` constant** (lines 225-254): Delete the entire constant.

5. **`_VARIANT_QUERIES` dict** (lines 256-261): Remove the `'county': _COUNTY_QUERY` and `'ecoregion': _ECOREGION_QUERY` entries. Result should have only `'collector'` and `'genus'` keys.

6. **`write_all_variants()` docstring** (lines 337-340): Remove the sentence about counties and ecoregions being enumerated from the geographies tables and the D-01 parenthetical. Replace with: `"Enumerates all filter values per variant type (collector, genus) and writes one feed file each."`

7. **`_ENUM_QUERIES` local dict inside `write_all_variants()`** (lines 341-360): Remove the `'county'` and `'ecoregion'` entries.

8. **`for variant_type in (...)` loop** (line 364): Change to `for variant_type in ('collector', 'genus'):`.

Do NOT touch write_variant_feed(), _slugify(), write_determinations_feed(), write_index_json(), or main(). Those functions are unchanged.
  </action>
  <verify>
    <automated>cd /Users/rainhead/dev/beeatlas/data && python -c "import feeds; print('ok')" && ! grep -n 'county\|ecoregion\|_COUNTY_QUERY\|_ECOREGION_QUERY' feeds.py</automated>
  </verify>
  <done>feeds.py imports cleanly; grep finds no county or ecoregion references anywhere in the file.</done>
</task>

<task type="auto">
  <name>Task 2: Clean test_feeds.py and delete XML files</name>
  <files>data/tests/test_feeds.py</files>
  <action>
**Edit data/tests/test_feeds.py:**

1. Delete the entire `test_county_variant` function (lines 273-295).

2. Delete the entire `test_ecoregion_variant` function (lines 298-320).

3. Delete the entire `test_empty_variant_feed` function (lines 323-411). This test exists solely to verify county and ecoregion empty-feed behavior (D-01/D-02/D-03 decisions for those feed types). With those variants gone, the test has no purpose. Do NOT attempt to repurpose it — just delete it.

4. In the import line at the top (line 14):
   ```python
   from feeds import _slugify, write_variant_feed, write_all_variants, write_index_json
   ```
   This import is still valid — all four names remain exported from feeds.py. No change needed.

5. `test_index_json` (lines 414-448) makes no county/ecoregion-specific assertions. Leave it unchanged.

**Delete the 105 generated XML files:**

Run these shell commands:
```bash
cd /Users/rainhead/dev/beeatlas/frontend/public/data/feeds
rm county-*.xml
rm ecoregion-*.xml
```

Do NOT delete index.json, determinations.xml, collector-*.xml, or genus-*.xml files. Do NOT modify index.json by hand — it will be regenerated by the next pipeline run with the correct reduced entry list.
  </action>
  <verify>
    <automated>cd /Users/rainhead/dev/beeatlas && ls frontend/public/data/feeds/county-*.xml 2>&1 | grep -c 'No such' | grep -q 1 && ls frontend/public/data/feeds/ecoregion-*.xml 2>&1 | grep -c 'No such' | grep -q 1 && cd data && uv run pytest tests/test_feeds.py -v 2>&1 | tail -20</automated>
  </verify>
  <done>
  - No county-*.xml or ecoregion-*.xml files exist in frontend/public/data/feeds/
  - All remaining test_feeds.py tests pass (test_time_window_and_sort, test_entry_fields, test_feed_metadata, test_output_file, test_blank_fields_excluded, test_run_py_integration, test_empty_window, test_slugify, test_collector_variant, test_genus_variant, test_index_json)
  - test_county_variant, test_ecoregion_variant, test_empty_variant_feed are gone
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| pipeline → filesystem | feeds.py writes XML to frontend/public/data/ — pure deletion, no new attack surface |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-qk1-01 | Tampering | XML deletion via rm glob | accept | Glob is scoped to county-*.xml and ecoregion-*.xml; no other files match pattern |
</threat_model>

<verification>
After both tasks:

```bash
# No county/ecoregion references in source
grep -rn 'county\|ecoregion' /Users/rainhead/dev/beeatlas/data/feeds.py
# (should return nothing)

# XML files gone
ls /Users/rainhead/dev/beeatlas/frontend/public/data/feeds/county-*.xml 2>&1
ls /Users/rainhead/dev/beeatlas/frontend/public/data/feeds/ecoregion-*.xml 2>&1
# (should both say: No such file or directory)

# Tests pass
cd /Users/rainhead/dev/beeatlas/data && uv run pytest tests/test_feeds.py -v
```
</verification>

<success_criteria>
- feeds.py: `_COUNTY_QUERY`, `_ECOREGION_QUERY` deleted; `_TITLE_TEMPLATES`, `_VARIANT_QUERIES`, `_ENUM_QUERIES`, and the variant loop all reference only `collector` and `genus`
- test_feeds.py: `test_county_variant`, `test_ecoregion_variant`, `test_empty_variant_feed` deleted; all remaining tests pass
- frontend/public/data/feeds/: 105 XML files deleted (39 county + 66 ecoregion); no other files touched
- `uv run pytest tests/test_feeds.py` exits 0
</success_criteria>

<output>
After completion, create `.planning/quick/260421-qk1-drop-county-ecoregion-feeds/260421-qk1-SUMMARY.md`
</output>
