---
id: 260514-f7c
title: dlt pipeline state housekeeping — audit
mode: quick
date: 2026-05-14
source: .planning/todos/pending/dlt-pipeline-state-housekeeping.md
---

# Quick Task: `_dlt_*` table growth audit

## Goal

Confirm whether `_dlt_pipeline_state` / `_dlt_loads` growth in `data/beeatlas.duckdb` warrants uniform housekeeping across all dlt pipelines, OR document "no action needed" so the next person doesn't re-investigate.

## Tasks

### T1 — Inventory `_dlt_*` tables and row counts (read-only)

- Enumerate every `_dlt_*` table across all schemas.
- Capture row counts for `_dlt_pipeline_state` per `pipeline_name` and `_dlt_loads` per schema.
- Map each `data/*_pipeline.py` to its `(pipeline_name, dataset_name)` tuple to understand the shared-name picture.
- Compute observed growth rate from `_dlt_loads.inserted_at` time range.

### T2 — Make the call

Either:
- **Action**: extract a uniform DELETE/truncate helper into `data/config.py` and apply to all 5 dlt pipelines; or
- **No action**: write the audit into SUMMARY.md and close the todo.

Decision driven by T1 evidence.

## must_haves

1. Decision (action vs no action) recorded in SUMMARY.md with row-count evidence.
2. If action: shared helper exists and is wired into every dlt pipeline.
3. Todo retired (pending → done) regardless of which path.
