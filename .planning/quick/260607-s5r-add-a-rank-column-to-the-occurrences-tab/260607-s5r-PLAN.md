---
phase: 260607-s5r
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/filter.ts
  - src/bee-table.ts
  - src/tests/filter-join-execution.test.ts
  - src/tests/filter.test.ts
  - src/tests/bee-table.test.ts
autonomous: true
requirements: [table-rank-column]
must_haves:
  truths:
    - "The occurrences table view shows a Rank column for each row"
    - "Rank value (e.g. species, genus) is sourced from taxa.rank via the existing taxa JOIN"
    - "Undetermined rows (taxon_id IS NULL) show a null label in the Rank column, never a blank-by-omission"
    - "All five OccurrenceRow-producing query functions resolve display_rank"
    - "The existing Vitest suite still passes"
  artifacts:
    - path: "src/filter.ts"
      provides: "display_rank resolution in all 5 selectCols builders + OccurrenceRow.display_rank field"
      contains: "t.rank AS display_rank"
    - path: "src/bee-table.ts"
      provides: "Rank column def in OCCURRENCE_COLUMN_DEFS sourced from display_rank"
      contains: "display_rank"
  key_links:
    - from: "src/filter.ts selectCols (x5)"
      to: "taxa.rank"
      via: "t.rank AS display_rank in SELECT, existing LEFT JOIN taxa t"
      pattern: "t\\.rank AS display_rank"
    - from: "src/bee-table.ts OCCURRENCE_COLUMN_DEFS"
      to: "OccurrenceRow.display_rank"
      via: "ColumnDef dataField: 'display_rank'"
      pattern: "dataField: 'display_rank'"
---

<objective>
Add a Rank column to the occurrences table view, sourced from `taxa.rank` (e.g. "species", "genus"), mirroring the existing Species (`display_name`) column added in Phase 131.

Purpose: Closes the `table-rank-column` todo raised at the Phase 131 human-verify checkpoint — the user wants rank shown alongside species name in the table.
Output: `display_rank` resolved in all 5 filter.ts query functions, a `display_rank` field on `OccurrenceRow`, a new Rank column def in bee-table.ts, and updated/extended tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@./CLAUDE.md

<interfaces>
<!-- Verified from the codebase. Use these directly — no exploration needed. -->

src/filter.ts — the `display_rank` source is exactly parallel to `display_name`:
- All 5 query functions build selectCols with the SAME literal expression:
  `OCCURRENCE_COLUMNS.map(c => `o.${c}`).join(', ') + ', t.name AS display_name'`
  at lines ~154 (queryAllFiltered), ~191 (queryTablePage), ~398 (queryListPage),
  ~429 (queryOccurrencesByBounds), ~451 (getOccurrences).
  Each is followed by `FROM occurrences o LEFT JOIN taxa t ON t.taxon_id = o.taxon_id`.
  The `taxa` table already carries a `rank` column — no new JOIN needed.
- `OccurrenceRow` (line ~40) declares `display_name: string | null;` at line ~74.
- `OCCURRENCE_COLUMNS` (line ~77) is the mart column list; it does NOT include
  display_name/display_rank (those are JOIN-resolved, not mart columns) — do NOT
  add display_rank to OCCURRENCE_COLUMNS.

src/bee-table.ts:
- `ColumnDef` interface (line ~6) supports `valueFn?: (row) => string | null` and
  `nullLabel?: string`. Null/empty cells render with the `cell-null` italic class when
  `nullLabel` is set (render logic at lines ~382-386).
- `OCCURRENCE_COLUMN_DEFS` (line ~41). The Species column to mirror:
  `{ key: 'species', label: 'Species', dataField: 'display_name', minWidth: '180px', nullLabel: 'No Determination' }`

taxa.rank values are lowercase (e.g. 'species', 'genus', 'family'). Title-case for
display via a small `valueFn` (capitalize first letter); keep it simple.
</interfaces>

<test_anchors>
- src/tests/filter-join-execution.test.ts seeds a real node:sqlite `taxa` table that
  ALREADY has a `rank` column (lines ~72-83): taxa 100 = rank 'genus', taxa 101 =
  rank 'species'. So display_rank is directly assertable with NO fixture change.
  Existing tests assert `rows[0]!.display_name === 'Bombus vosnesenskii'`.
- src/tests/filter.test.ts:337 asserts the generated SQL contains 'display_name'
  (string-level test against the mocked sqlite3).
- src/tests/bee-table.test.ts TABLE-01 (lines ~45-62) hard-asserts
  `headers.length).toBe(10)` and checks each label. Adding Rank makes it 11 —
  this assertion MUST be updated or the suite goes red.
</test_anchors>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Resolve display_rank in filter.ts and add the OccurrenceRow field</name>
  <files>src/filter.ts</files>
  <action>
In each of the 5 selectCols builder lines (queryAllFiltered ~154, queryTablePage ~191,
queryListPage ~398, queryOccurrencesByBounds ~429, getOccurrences ~451), append
`, t.rank AS display_rank` to the string so it reads
`... + ', t.name AS display_name, t.rank AS display_rank'`. The existing
`LEFT JOIN taxa t ON t.taxon_id = o.taxon_id` already provides `t.rank`; do NOT add a
new join. Do NOT touch the OCCURRENCE_COLUMNS array — display_rank is JOIN-resolved,
not a mart column, exactly like display_name.

In the `OccurrenceRow` interface, add `display_rank: string | null;` immediately after
the existing `display_name: string | null;` (line ~74). Mirror the trailing comment
style: it is JOIN-resolved from taxa.rank, null when taxon_id IS NULL.
  </action>
  <verify>
    <automated>grep -c "t.rank AS display_rank" src/filter.ts | grep -qx 5 && grep -q "display_rank: string | null;" src/filter.ts && echo OK</automated>
  </verify>
  <done>All 5 SELECT builders include `t.rank AS display_rank`; OccurrenceRow has `display_rank: string | null;`; `npx tsc --noEmit` passes.</done>
</task>

<task type="auto">
  <name>Task 2: Add the Rank column def in bee-table.ts and update/extend tests</name>
  <files>src/bee-table.ts, src/tests/filter-join-execution.test.ts, src/tests/filter.test.ts, src/tests/bee-table.test.ts</files>
  <action>
In src/bee-table.ts, insert a new ColumnDef into OCCURRENCE_COLUMN_DEFS immediately
AFTER the Species column (line ~43):
`{ key: 'rank', label: 'Rank', dataField: 'display_rank', minWidth: '100px', nullLabel: 'No Determination', valueFn: (row) => row.display_rank ? row.display_rank.charAt(0).toUpperCase() + row.display_rank.slice(1) : null }`.
The valueFn Title-cases the lowercase taxa.rank value (e.g. 'species' -> 'Species');
returning null when display_rank is null lets the existing render path apply nullLabel
and the cell-null italic style. Use the same 'No Determination' nullLabel as Species
for consistency with the existing null-determination styling.

Update src/tests/bee-table.test.ts TABLE-01 (lines ~45-62): change
`expect(headers.length).toBe(10)` to `toBe(11)` and add
`expect(labels.some(l => l.includes('Rank'))).toBe(true);`. Verify no other test
hard-codes the column count or column ordering (the Species cell index check at
bee-table.test.ts line ~186 uses `cells[1]` for the date=0/species=1 layout — Rank is
inserted at index 2, so the Species index 1 is unaffected; leave it).

Extend src/tests/filter-join-execution.test.ts: in the existing
'queryTablePage ... resolves display_name' test (or a new sibling test in the same
describe block), assert `rows[0]!.display_rank` is `'species'` for the identified
taxon 101 (the seeded taxa table already has rank='species' for 101). Add an assertion
in the null-taxon test (~line 147) that the undetermined row's `display_rank` is null.

Extend src/tests/filter.test.ts:337 SQL string test: add
`expect(dataSql).toContain('display_rank');`.
  </action>
  <verify>
    <automated>npx vitest run src/tests/bee-table.test.ts src/tests/filter-join-execution.test.ts src/tests/filter.test.ts</automated>
  </verify>
  <done>OCCURRENCE_COLUMN_DEFS has a Rank column (dataField display_rank) after Species; TABLE-01 expects 11 headers including Rank; filter-join-execution asserts display_rank resolves to 'species' for taxon 101 and null for the undetermined row; filter.test.ts asserts the SQL contains display_rank; all three test files pass.</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` passes (display_rank typed on OccurrenceRow).
- `npx vitest run` passes the full suite (no regressions; TABLE-01 count updated to 11).
- `grep -c "t.rank AS display_rank" src/filter.ts` returns 5.
</verification>

<success_criteria>
- A Rank column appears in OCCURRENCE_COLUMN_DEFS, sourced from `display_rank`, placed immediately after Species.
- All 5 filter.ts SELECTs include `t.rank AS display_rank`; OccurrenceRow includes `display_rank: string | null`.
- Rank values display Title-cased; undetermined rows show the null label.
- `npx vitest run` passes.
- No Python/dbt/data-layer changes (frontend-only).
</success_criteria>

<output>
Create `.planning/quick/260607-s5r-add-a-rank-column-to-the-occurrences-tab/260607-s5r-01-SUMMARY.md` when done.
</output>
