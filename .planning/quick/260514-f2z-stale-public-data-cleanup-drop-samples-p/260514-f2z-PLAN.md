---
id: 260514-f2z
title: Stale public/data artifact cleanup
mode: quick
date: 2026-05-14
source: .planning/todos/pending/stale-public-data-cleanup.md
---

# Quick Task: Stale `public/data/` cleanup

## Goal

S3 `s3://${BUCKET}/data/` and local `public/data/` contain only the canonical artifact set produced by `data/nightly.sh`. `scripts/fetch-data.sh` matches.

## Canonical artifact set (per `data/nightly.sh:47–51`)

- `occurrences.parquet`
- `counties.geojson`
- `ecoregions.geojson`
- `species.json`
- `seasonality.json`
- `feeds/*` (recursive)

`species.parquet` is a local-only intermediate (used by `data/species_maps.py` for SVG generation) — never uploaded to S3.

## Inventory (verified)

**S3 stale items:** `ecdysis.parquet` (973KB, 2026-04-21), `samples.parquet` (258KB, 2026-04-21).
**Local stale items:** `public/data/ecdysis.parquet`, `public/data/samples.parquet`.
**Source-code stale refs:** `scripts/fetch-data.sh` (downloads stale set), `BENCHMARK.md` (data file list).
**Frontend (`src/`):** zero references to either stale parquet.

## Tasks

### T1 — Update `scripts/fetch-data.sh` to canonical artifact set

- **Files:** `scripts/fetch-data.sh`
- **Action:** Replace `for f in ecdysis.parquet samples.parquet counties.geojson ecoregions.geojson` loop with the canonical 5-file set; add a recursive `feeds/` sync.
- **Verify:** `bash scripts/fetch-data.sh --help`-style dry inspection; file list matches `data/nightly.sh:47–51`.
- **Done:** Script downloads only canonical artifacts.

### T2 — Update `BENCHMARK.md` data file list

- **Files:** `BENCHMARK.md`
- **Action:** Line 27 — replace stale parquet enumeration with canonical set.
- **Verify:** No remaining `ecdysis.parquet`/`samples.parquet` refs outside `.planning/` and `infra/cdk.out/`.
- **Done:** Doc reflects post-v3.4 artifact reality.

### T3 — Delete local stale files

- **Files:** `public/data/ecdysis.parquet`, `public/data/samples.parquet`
- **Action:** `rm` both.
- **Verify:** `ls public/data/*.parquet` shows only `occurrences.parquet` and `species.parquet`.
- **Done:** Local data dir matches canonical set (+ local-only `species.parquet`).

### T4 — Delete S3 stale objects (destructive — user confirmation gate)

- **Files:** `s3://${BUCKET}/data/ecdysis.parquet`, `s3://${BUCKET}/data/samples.parquet`
- **Action:** `aws s3 rm` both, then `cloudfront create-invalidation` for those two paths so CDN doesn't keep serving them.
- **Verify:** `aws s3 ls s3://${BUCKET}/data/` lists only canonical artifacts.
- **Done:** S3 + CDN purged of stale parquets.

## Out of scope

- `species.parquet` cleanup (not stale — local intermediate)
- Hash-versioned URL strategy (separate pending todo: `hash-versioned-parquet-urls.md`)
- AWS_PROFILE default in fetch-data.sh (user-workstation concern, not artifact cleanup)

## must_haves

1. `scripts/fetch-data.sh` downloads only canonical artifacts.
2. S3 `data/` contains only canonical artifacts.
3. Local `public/data/` contains only canonical artifacts (+ `species.parquet` local intermediate).
4. `BENCHMARK.md` reflects post-v3.4 reality.
