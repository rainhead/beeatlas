---
milestone: v1.3
name: Specimen-Sample Linkage
audited: 2026-03-12
status: tech_debt
scores:
  requirements: 8/8
  phases: 2/2
  integration: 8/8
  flows: 3/3
gaps:
  requirements: []
  integration: []
  flows: []
tech_debt:
  - phase: 11-links-pipeline
    items:
      - "VALIDATION.md exists but nyquist_compliant: false and wave_0_complete: false — validation strategy created but never completed during execution"
      - "No VERIFICATION.md — verifier was disabled in config; requirements confirmed via integration checker + SUMMARY frontmatter"
  - phase: 12-s3-cache-and-build-integration
    items:
      - "No VALIDATION.md — phase was planned without research, Nyquist strategy never created"
      - "No VERIFICATION.md — verifier was disabled in config"
      - "links.parquet not yet copied to frontend assets in build-data.sh — intentionally deferred to v1.4 (frontend display)"
  - cross-phase:
    items:
      - "fetch.py uses relative paths (Path('links.parquet'), Path('raw/ecdysis_cache')) — CWD-sensitive, safe only when invoked via npm run fetch-links or with CWD=data/. Future invocation paths must enforce this."
nyquist:
  compliant_phases: []
  partial_phases: [11]
  missing_phases: [12]
  overall: PARTIAL
---

# Milestone v1.3 Audit — Specimen-Sample Linkage

**Audited:** 2026-03-12
**Status:** tech_debt — all requirements satisfied, no critical blockers, Nyquist validation incomplete

---

## Requirements Coverage (8/8)

| Requirement | Description | Phase | SUMMARY | Traceability | Integration | Final Status |
|-------------|-------------|-------|---------|--------------|-------------|--------------|
| LINK-01 | Fetch Ecdysis pages at ≤20 req/sec, cache HTML | 11 | ✓ | [x] Complete | WIRED | **satisfied** |
| LINK-02 | Two-level skip (links.parquet then disk cache) | 11 | ✓ | [x] Complete | WIRED | **satisfied** |
| LINK-03 | Extract iNat ID from `#association-div a[target="_blank"]` | 11 | ✓ | [x] Complete | WIRED | **satisfied** |
| LINK-04 | links.parquet: occurrenceID (StringDtype) + inat_observation_id (Int64, nullable) | 11 | ✓ | [x] Complete | WIRED | **satisfied** |
| LCACHE-01 | Restore links.parquet + HTML cache from S3 (graceful miss) | 12 | ✓ | [x] Complete | WIRED | **satisfied** |
| LCACHE-02 | Upload links.parquet + sync HTML cache to S3 after run | 12 | ✓ | [x] Complete | WIRED | **satisfied** |
| LCACHE-03 | npm scripts: cache-restore-links, fetch-links, cache-upload-links | 12 | ✓ | [x] Complete | WIRED | **satisfied** |
| PIPE-04 | build-data.sh: cache restore → fetch → cache upload | 12 | ✓ | [x] Complete | WIRED | **satisfied** |

_Note: No VERIFICATION.md files were produced (verifier disabled in config). Requirements confirmed via 3-source cross-reference: SUMMARY.md frontmatter + REQUIREMENTS.md traceability `[x]` + integration checker direct code verification._

---

## Phase Summary (2/2)

| Phase | Plans | VERIFICATION.md | VALIDATION.md | Status |
|-------|-------|-----------------|---------------|--------|
| 11 — links-pipeline | 2/2 | Missing (verifier disabled) | PARTIAL | ✓ Complete |
| 12 — s3-cache-and-build-integration | 2/2 | Missing (verifier disabled) | Missing | ✓ Complete |

---

## E2E Flows (3/3)

### Flow 1: Full CI Build Sequence ✓
`npm run build:data` → `build-data.sh` → `cd $REPO_ROOT` → `npm run cache-restore-links` → `npm run fetch-links` → `npm run cache-upload-links`

All three steps at `build-data.sh` lines 24–31. CWD reset from `data/` to `$REPO_ROOT` before npm calls — correctly implemented.

### Flow 2: Links Pipeline Data Flow ✓
`ecdysis.parquet` (occurrenceID + ecdysis_id) → `run_pipeline()` reads `["ecdysis_id", "occurrenceID"]` → `fetch_page(ecdysis_id)` → `extract_observation_id()` via `#association-div a[target="_blank"]` → `links.parquet` (occurrenceID + inat_observation_id, nullable)

Column contract verified at source: `occurrences.py` includes `occurrenceID` as `pd.StringDtype()` in `to_parquet()`.

### Flow 3: S3 Cache Round-Trip ✓
`cache_restore_links.sh`: S3 → `$DATA_DIR/links.parquet` (graceful miss) + S3 → `$DATA_DIR/raw/ecdysis_cache/*.html` (additive sync)
`fetch-links`: reads `links.parquet` + `raw/ecdysis_cache/` for two-level skip
`cache_upload_links.sh`: `$DATA_DIR/links.parquet` → S3 (fail-fast) + `$DATA_DIR/raw/ecdysis_cache/` → S3 (additive sync)

S3 paths are symmetric across restore and upload scripts. `fetch.py` runtime paths (`data/links.parquet`, `data/raw/ecdysis_cache/`) match script paths exactly.

---

## Cross-Phase Integration

**Connected:** 8 exports properly consumed
**Orphaned:** 0
**Missing:** 0 (links.parquet not consumed by frontend — intentionally deferred to v1.4)

### Integration Map

| Export | Producer | Consumer | Status |
|--------|----------|----------|--------|
| `ecdysis.parquet` with occurrenceID | Phase 11 (11-01) | `fetch.py` ECDYSIS_PARQUET | ✓ |
| `data/links/` package | Phase 11 (11-01) | Phase 11 (11-02) + npm fetch-links | ✓ |
| `links.parquet` | Phase 11 (11-02) | cache_upload_links.sh, Level 1 skip in run_pipeline | ✓ |
| `data/raw/ecdysis_cache/` | Phase 11 (11-02) | cache_restore_links.sh, cache_upload_links.sh, Level 2 skip | ✓ |
| `cache_restore_links.sh` | Phase 12 (12-01) | npm cache-restore-links | ✓ |
| `cache_upload_links.sh` | Phase 12 (12-01) | npm cache-upload-links | ✓ |
| `npm run cache-restore-links` | Phase 12 (12-02) | build-data.sh line 26 | ✓ |
| `npm run cache-upload-links` | Phase 12 (12-02) | build-data.sh line 31 | ✓ |

---

## Tech Debt

### Phase 11: links-pipeline
- VALIDATION.md exists but `nyquist_compliant: false`, `wave_0_complete: false` — strategy was scaffolded but tests were never formally reported back against it
- No VERIFICATION.md (verifier disabled) — automated goal verification skipped

### Phase 12: s3-cache-and-build-integration
- No VALIDATION.md — phase planned without research, Nyquist strategy never created
- No VERIFICATION.md (verifier disabled)
- `links.parquet` not copied to frontend assets — intentionally deferred to v1.4

### Cross-Phase
- `fetch.py` uses CWD-relative paths (`Path("links.parquet")`, `Path("raw/ecdysis_cache")`). Safe now (npm script enforces `cd data &&`). Any future direct invocation must set CWD to `data/` or refactor to absolute paths.

---

## Nyquist Compliance

| Phase | VALIDATION.md | nyquist_compliant | wave_0_complete | Status |
|-------|---------------|-------------------|-----------------|--------|
| 11 | ✓ exists | false | false | PARTIAL |
| 12 | missing | — | — | MISSING |

To address: `/gsd:validate-phase 11` and `/gsd:validate-phase 12`

---

## Deferred to v1.4

Per REQUIREMENTS.md out-of-scope section and 11-02-SUMMARY:
- **MAP-03**: Sample markers (iNat collection events) as distinct map layer
- **MAP-04**: Clicking a sample marker shows iNat observation details in sidebar
- **LINK-05**: Sidebar shows iNat observation link for a clicked specimen

---

*Milestone v1.3 delivered: links pipeline wired end-to-end with S3 caching and incremental CI builds.*
