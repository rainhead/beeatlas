---
phase: 85-pre-cutover-groundwork
plan: 04
type: execute
wave: 2
depends_on:
  - 85-01
  - 85-02
  - 85-03
files_modified:
  - data/dbt/models/marts/schema.yml
  - data/dbt/models/marts/occurrences.sql
  - src/sqlite.ts
  - data/tests/test_dbt_diff.py
autonomous: true
requirements:
  - CLEAN-02

must_haves:
  truths:
    - "The marts/occurrences dbt contract declares exactly 30 columns (down from 33)"
    - "specimen_inat_login, specimen_inat_genus, specimen_inat_family are absent from marts/occurrences.sql SELECT and marts/schema.yml"
    - "specimen_inat_taxon_name remains present in both the dbt contract and src/sqlite.ts (it is NOT in the drop list)"
    - "src/sqlite.ts CREATE TABLE occurrences declares the matching 30-column schema (3 columns removed)"
    - "data/tests/test_dbt_diff.py docstring reflects 30 cols and the schema assertion logic is unchanged (it compares sandbox vs public dynamically)"
    - "npm test passes after the src/sqlite.ts edit"
    - "dbt build exits 0 with 0 FAIL and 0 ERROR (combining Plan 01, Plan 02, and this plan's changes)"
  artifacts:
    - path: "data/dbt/models/marts/schema.yml"
      provides: "Enforced 30-column contract on the occurrences mart"
      contains: "occurrences"
    - path: "data/dbt/models/marts/occurrences.sql"
      provides: "Mart SELECT projecting exactly the 30 contract columns"
      contains: "specimen_inat_taxon_name"
    - path: "src/sqlite.ts"
      provides: "CREATE TABLE occurrences with 30 column declarations matching the dbt contract"
      contains: "specimen_inat_taxon_name TEXT"
    - path: "data/tests/test_dbt_diff.py"
      provides: "Schema-match test with docstring updated to 30 cols"
      contains: "30 cols"
  key_links:
    - from: "data/dbt/models/marts/occurrences.sql SELECT"
      to: "data/dbt/models/marts/schema.yml contract (30 entries)"
      via: "contract enforcement at materialization"
      pattern: "contract:\\s*\\n\\s*enforced: true"
    - from: "src/sqlite.ts CREATE TABLE occurrences"
      to: "occurrences.parquet column set"
      via: "wa-sqlite parquet load expects matching column names"
      pattern: "CREATE TABLE occurrences"
    - from: "data/tests/test_dbt_diff.py::test_occurrences_schema_matches"
      to: "sandbox vs public parquet schema parity"
      via: "DESCRIBE comparison (sandbox produced by Plan 04; public regenerated via data/export.py)"
      pattern: "test_occurrences_schema_matches"
---

<objective>
Resolve CLEAN-02: drop `specimen_inat_login`, `specimen_inat_genus`, and `specimen_inat_family`
from the `marts/occurrences` contract (33 → 30 columns), the mart SELECT, and the frontend SQLite
loader. Update the diff-harness docstring to reflect the new column count.

Purpose: This is the last Phase 85 deliverable. Combined with Plan 01 (TEST-01) and Plan 02
(TEST-02), it leaves `dbt build` exiting 0 cleanly with the contract narrowed before Phase 86
begins porting remaining transforms.

Output: Four files edited atomically. A full `bash data/dbt/run.sh build` confirms the sandbox
produces a 30-column parquet; `npm test` confirms the frontend loader is consistent;
`test_dbt_diff.py` schema assertion's logic is unchanged (it compares sandbox vs public
dynamically), so it remains correct — the docstring is updated to reflect the new column count.

Scope LANDMINES (per locked decision D-04):
1. DO NOT drop these columns from `int_specimen_obs_base` or `int_combined`. Those intermediate
   models get rewritten in Phase 86. Phase 85 only changes the mart projection + contract + loader
   + diff harness docstring.
2. DO NOT drop `specimen_inat_taxon_name` from src/sqlite.ts. It sits between two of the dropped
   columns at lines 89-91 and IS actively used by the frontend. The drop list is exactly:
   `specimen_inat_login` (line 88), `specimen_inat_genus` (line 90), `specimen_inat_family`
   (line 91). Lines 89 and 92 (`specimen_inat_taxon_name`, `specimen_inat_quality_grade`) STAY.
3. DO NOT modify data/export.py or run it as part of this plan. The diff harness will fail until
   public/data is regenerated by a future cron run; that is expected and explicitly acknowledged
   in REQUIREMENTS.md ("which will need updating to reflect the new 30-column contract — this is
   intentional, not a regression"). The plan's verification gate is: dbt build exits 0 on the
   sandbox side AND npm test passes; the diff-harness schema assertion is verified separately
   (after public/data is regenerated, which is out of Phase 85 scope).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/85-pre-cutover-groundwork/85-RESEARCH.md
@.planning/phases/85-pre-cutover-groundwork/85-PATTERNS.md
@.planning/phases/85-pre-cutover-groundwork/85-01-SUMMARY.md
@.planning/phases/85-pre-cutover-groundwork/85-02-SUMMARY.md
@.planning/phases/85-pre-cutover-groundwork/85-03-SUMMARY.md
@CLAUDE.md

@data/dbt/models/marts/schema.yml
@data/dbt/models/marts/occurrences.sql
@src/sqlite.ts
@data/tests/test_dbt_diff.py

<interfaces>
<!-- Verified facts from RESEARCH.md §CLEAN-02 (HIGH confidence, grep verified):

Drop list (exactly 3 columns):
  - specimen_inat_login   (mart line 76; contract line 57-58; sqlite.ts line 88)
  - specimen_inat_genus   (mart line 77; contract line 61-62; sqlite.ts line 90)
  - specimen_inat_family  (mart line 77; contract line 63-64; sqlite.ts line 91)

Preserve list (NOT dropped, sit adjacent in source):
  - specimen_inat_taxon_name   (mart line 76; contract line 59-60; sqlite.ts line 89)
  - specimen_inat_quality_grade (mart line 77; contract line 65-66; sqlite.ts line 92)

Reference confirmations (grep -rn against src/ returns only sqlite.ts — no frontend code reads
the 3 dropped columns by name; safe to drop).

int_combined.sql carries the 3 columns AND separately produces `genus`/`family` outputs for
provisional ARM 2 rows via `sob.specimen_inat_genus AS genus` / `sob.specimen_inat_family AS family`
— different output columns. Dropping from the mart SELECT does NOT affect those genus/family
outputs (which DO appear in the mart SELECT at line 71). DO NOT touch int_combined.

Sequence (from RESEARCH.md §"Sequence of Changes"):
  1. marts/schema.yml — remove 3 contract entries (lines 57-58, 61-62, 63-64; preserve 59-60 and
     65-66)
  2. marts/occurrences.sql — remove `j.specimen_inat_login,` (line 76) and
     `j.specimen_inat_genus, j.specimen_inat_family,` (line 77; PRESERVE
     `j.specimen_inat_taxon_name` and `j.specimen_inat_quality_grade`)
  3. src/sqlite.ts — remove lines 88, 90, 91 (PRESERVE line 89 and line 92)
  4. data/tests/test_dbt_diff.py — update docstring on line 53 and line 56 (`33 cols` → `30 cols`)
-->

Current mart SELECT (lines 68-80 of marts/occurrences.sql) — after edit, lines 76-77 must read:
```
    j.sample_host,
    j.specimen_inat_taxon_name, j.specimen_inat_quality_grade,
    j.is_provisional,
```
(The 3 dropped j.specimen_inat_login, j.specimen_inat_genus, j.specimen_inat_family are gone;
the surviving two — taxon_name and quality_grade — are collapsed onto one line.)

Current sqlite.ts column block (lines 87-92) — after edit, lines must read:
```
    sample_host TEXT,
    specimen_inat_taxon_name TEXT,
    specimen_inat_quality_grade TEXT,
    is_provisional INTEGER,
```

Diff harness docstring (lines 52-57 of test_dbt_diff.py):
```python
def test_occurrences_schema_matches():
    """Column names AND types from DESCRIBE match exactly between sandbox and public (33 cols).
    ...
    Verified baseline: 33 columns with identical names and types in both files.
    """
```
After edit, both `33 cols` and `33 columns` → `30 cols` / `30 columns`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Drop 3 column entries from marts/schema.yml contract</name>
  <files>data/dbt/models/marts/schema.yml</files>
  <action>
    Edit `data/dbt/models/marts/schema.yml`. Remove exactly three `- name: ...` entries (each is a
    2-line YAML block: a name and a data_type):

    - `- name: specimen_inat_login` + `data_type: varchar` (current lines 57-58)
    - `- name: specimen_inat_genus` + `data_type: varchar` (current lines 61-62)
    - `- name: specimen_inat_family` + `data_type: varchar` (current lines 63-64)

    PRESERVE:
    - `- name: specimen_inat_taxon_name` + `data_type: varchar` (current lines 59-60)
    - `- name: specimen_inat_quality_grade` + `data_type: varchar` (current lines 65-66)
    - All other entries (29 of them).

    After the edit, the file declares exactly 30 columns under `columns:`. The
    `config.contract.enforced: true` block remains unchanged at the top.

    Order after edit (for the specimen_inat_* / is_provisional / canonical_name region):
    ```yaml
          - name: sample_host
            data_type: varchar
          - name: specimen_inat_taxon_name
            data_type: varchar
          - name: specimen_inat_quality_grade
            data_type: varchar
          - name: is_provisional
            data_type: boolean
          - name: canonical_name
            data_type: varchar
    ```
  </action>
  <verify>
    <automated>python3 -c "import yaml; d=yaml.safe_load(open('data/dbt/models/marts/schema.yml')); cols=[c['name'] for c in d['models'][0]['columns']]; assert len(cols)==30, f'Expected 30 cols, got {len(cols)}'; assert 'specimen_inat_login' not in cols and 'specimen_inat_genus' not in cols and 'specimen_inat_family' not in cols, f'Dropped cols still present: {cols}'; assert 'specimen_inat_taxon_name' in cols and 'specimen_inat_quality_grade' in cols, f'Preserved cols missing: {cols}'; print('OK 30 cols, drops correct, preserves correct')"</automated>
  </verify>
  <done>
    marts/schema.yml declares exactly 30 columns; the 3 drop-list columns are absent; the 2
    preserve-list columns (taxon_name, quality_grade) remain; YAML parses cleanly.
  </done>
</task>

<task type="auto">
  <name>Task 2: Drop 3 columns from marts/occurrences.sql SELECT</name>
  <files>data/dbt/models/marts/occurrences.sql</files>
  <action>
    Edit `data/dbt/models/marts/occurrences.sql` lines 76-77. The current text is:

    ```
        j.specimen_inat_login, j.specimen_inat_taxon_name,
        j.specimen_inat_genus, j.specimen_inat_family, j.specimen_inat_quality_grade,
    ```

    Replace with exactly:

    ```
        j.specimen_inat_taxon_name, j.specimen_inat_quality_grade,
    ```

    Do NOT touch any other line in the file. The CTE definitions (lines 1-67), the rest of the
    final SELECT (lines 68-75 and 78-84), and the `j.genus, j.family` projection on line 71
    (which comes from `int_combined`'s separate provisional-ARM derivation, NOT from the dropped
    `specimen_inat_genus`/`specimen_inat_family`) all remain unchanged.

    After the edit, the SELECT projects exactly 30 columns matching the contract from Task 1.

    LANDMINE — `j.specimen_inat_taxon_name` MUST remain present (this is preserved; it's between
    the dropped columns in source order).
  </action>
  <verify>
    <automated>! grep -q "specimen_inat_login\|specimen_inat_genus\|specimen_inat_family" data/dbt/models/marts/occurrences.sql || (echo "Dropped column still present in mart SELECT" && exit 1); grep -q "j.specimen_inat_taxon_name" data/dbt/models/marts/occurrences.sql || (echo "specimen_inat_taxon_name accidentally removed" && exit 1); grep -q "j.specimen_inat_quality_grade" data/dbt/models/marts/occurrences.sql || (echo "specimen_inat_quality_grade accidentally removed" && exit 1)</automated>
  </verify>
  <done>
    marts/occurrences.sql no longer references specimen_inat_login, specimen_inat_genus, or
    specimen_inat_family. specimen_inat_taxon_name and specimen_inat_quality_grade are both still
    projected. Build verification deferred to Task 4.
  </done>
</task>

<task type="auto">
  <name>Task 3: Drop columns from frontend loader and update diff harness docstring</name>
  <files>src/sqlite.ts, data/tests/test_dbt_diff.py</files>
  <action>
    Two small mechanical changes in one task. Both are coupled to the contract narrowing from
    Tasks 1-2 and have no functional interaction beyond shared phase scope.

    PART A — Edit `src/sqlite.ts` inside the `CREATE TABLE occurrences (...)` block (currently
    lines 66-100). Remove three lines from the column declarations:

    - Line 88: `    specimen_inat_login TEXT,`
    - Line 90: `    specimen_inat_genus TEXT,`
    - Line 91: `    specimen_inat_family TEXT,`

    PRESERVE (LANDMINE):
    - Line 89: `    specimen_inat_taxon_name TEXT,`
    - Line 92: `    specimen_inat_quality_grade TEXT,`
    - Line 87: `    sample_host TEXT,` (and its trailing comma)
    - All other lines.

    After the edit, the `CREATE TABLE occurrences` declaration has exactly 30 columns. Verify
    trailing commas are valid SQL:
    - The line BEFORE the removed `specimen_inat_login` (`sample_host TEXT,`) keeps its trailing
      comma.
    - The line AFTER the removed `specimen_inat_family` (`specimen_inat_quality_grade TEXT,`)
      keeps its trailing comma.
    - The last declaration in the table (`ecoregion_l3 TEXT` on the line ending with `)`) must
      NOT have a trailing comma.

    LANDMINE — the column at line 89 (`specimen_inat_taxon_name TEXT,`) sits between two dropped
    columns. Removing the wrong line will break the frontend silently (the parquet loader will
    still work, but a queried column won't exist). Confirm the surviving block reads exactly:
    ```
        sample_host TEXT,
        specimen_inat_taxon_name TEXT,
        specimen_inat_quality_grade TEXT,
        is_provisional INTEGER,
    ```

    PART B — Edit `data/tests/test_dbt_diff.py` lines 52-57 (the `test_occurrences_schema_matches`
    docstring). Replace `(33 cols)` on line 53 with `(30 cols)`. Replace `33 columns with
    identical names and types in both files.` on line 56 with `30 columns with identical names
    and types in both files.`

    Do NOT modify the assertion logic (lines 58-74) — the test dynamically compares
    sandbox vs public schemas via DESCRIBE; no hardcoded numeric literal exists in the body.
    The docstring update is the only edit needed in this file.

    Do NOT modify any other test function in the file. `test_occurrences_row_count`,
    `test_occurrences_ecdysis_key_set_matches`, and the other diff tests remain unchanged.

    Note: This test WILL FAIL until `public/data/occurrences.parquet` is also regenerated with
    30 columns (by a future `data/export.py` run). That is expected and acknowledged in
    REQUIREMENTS.md. Do NOT regenerate public/data as part of this plan — that touches the
    production data path which is explicitly out of Phase 85 scope.
  </action>
  <verify>
    <automated>! grep -E "specimen_inat_login|specimen_inat_genus|specimen_inat_family" src/sqlite.ts || (echo "Dropped column still in sqlite.ts" && exit 1); grep -q "specimen_inat_taxon_name TEXT" src/sqlite.ts || (echo "specimen_inat_taxon_name removed by mistake" && exit 1); grep -q "specimen_inat_quality_grade TEXT" src/sqlite.ts || (echo "specimen_inat_quality_grade removed by mistake" && exit 1); npm test && ! grep -q "33 cols" data/tests/test_dbt_diff.py && ! grep -q "33 columns" data/tests/test_dbt_diff.py && grep -q "30 cols" data/tests/test_dbt_diff.py && grep -q "30 columns" data/tests/test_dbt_diff.py && python3 -c "import ast; ast.parse(open('data/tests/test_dbt_diff.py').read()); print('OK docstring updated + syntax valid')"</automated>
  </verify>
  <done>
    src/sqlite.ts no longer declares the 3 dropped columns; preserve list (taxon_name,
    quality_grade) remains; `npm test` passes (covers the sqlite.ts change end-to-end, which
    subsumes type-checking). test_dbt_diff.py docstring uses `30 cols` / `30 columns` in both
    places; the assertion logic is unchanged; Python parses the file without syntax errors.
  </done>
</task>

<task type="checkpoint:integration" gate="blocking">
  <name>Task 4: Full phase verification — dbt build + npm test + schema diff harness</name>
  <what-built>
    All Phase 85 plan edits land cumulatively: Plan 01 (TEST-01) + Plan 02 (TEST-02) +
    Plan 03 (CLEAN-01 docs) + this plan's Tasks 1-3 (CLEAN-02 contract narrowing). This task is
    the integration gate that confirms the four plans compose correctly.
  </what-built>
  <how-to-verify>
    Run these three commands in sequence from the repo root and confirm all pass:

    1. `bash data/dbt/run.sh build`
       Expected: exit 0, 0 FAIL, 0 ERROR. The sandbox
       `data/dbt/target/sandbox/occurrences.parquet` declares exactly 30 columns; all other
       sandbox outputs (counties.geojson, ecoregions.geojson, etc.) are unchanged.

       Verify column count + drops absent + preserves present:
       `python3 -c "import duckdb; cols=[r[0] for r in duckdb.execute(\"DESCRIBE SELECT * FROM read_parquet('data/dbt/target/sandbox/occurrences.parquet')\").fetchall()]; assert len(cols)==30, f'Expected 30, got {len(cols)}'; assert not any(c in cols for c in ['specimen_inat_login','specimen_inat_genus','specimen_inat_family']); assert all(c in cols for c in ['specimen_inat_taxon_name','specimen_inat_quality_grade']); print('OK 30 cols, drops absent, preserves present')"`

    2. `npm test`
       Expected: all Vitest suites pass; the src/sqlite.ts edit has not regressed any frontend
       behavior. (No test file directly references the 3 dropped columns by name — grep verified
       — so no test edits are needed.)

    3. `uv run --project data pytest data/tests/test_dbt_diff.py -k schema`
       Expected: the schema assertion test `test_occurrences_schema_matches` will FAIL against
       current `public/data/occurrences.parquet` until that file is regenerated. THIS IS
       EXPECTED per REQUIREMENTS.md and is NOT a Phase 85 regression. Record the failure in the
       Phase 85 SUMMARY explicitly. If the test passes (because someone regenerated public/data
       out of band), record that too. The remaining diff harness tests (row count, key set,
       geojson byte-comparability) MUST pass.

    If `dbt build` does NOT exit 0 cleanly: do NOT proceed. Report the failure, identify which
    of the four files is misaligned (most likely candidate: a contract-vs-SQL mismatch — the
    contract enforces what the SELECT must project). The standard fix path is to re-read
    schema.yml + occurrences.sql together and confirm the 30-column lists match exactly.
  </how-to-verify>
  <verify>
    <automated>bash data/dbt/run.sh build && python3 -c "import duckdb; cols=[r[0] for r in duckdb.execute(\"DESCRIBE SELECT * FROM read_parquet('data/dbt/target/sandbox/occurrences.parquet')\").fetchall()]; assert len(cols)==30, f'Expected 30, got {len(cols)}'; assert not any(c in cols for c in ['specimen_inat_login','specimen_inat_genus','specimen_inat_family']); assert all(c in cols for c in ['specimen_inat_taxon_name','specimen_inat_quality_grade']); print(f'OK 30 cols, drops absent, preserves present')" && npm test && uv run --project data pytest data/tests/test_dbt_diff.py -k schema; echo "Schema diff harness exit code recorded; failure against current public/data is expected per REQUIREMENTS.md"</automated>
  </verify>
  <resume-signal>Confirm Phase 85 verification complete — type "approved" with the dbt-build status, npm-test status, and the diff-harness schema test outcome (pass or expected-fail).</resume-signal>
  <done>
    `dbt build` exits 0 with 0 FAIL and 0 ERROR. Sandbox occurrences.parquet has exactly 30
    columns with the drop list absent and the preserve list present. `npm test` passes. The
    schema diff harness test outcome is recorded in the Phase 85 SUMMARY (expected failure
    against current public/data is intentional, not a regression). Phase 85 ships the sandbox-side
    and frontend-side changes; the public regeneration happens in a future cron run (or
    explicitly in Phase 88 cutover).
  </done>
</task>

</tasks>

<verification>
After all four tasks land:

1. `bash data/dbt/run.sh build` exits 0 with PASS counts incremented (TEST-01 → PASS,
   ecdysis_id singular test → PASS, no ERROR on ecdysis_id, no FAIL on iNat not_null).
2. `data/dbt/target/sandbox/occurrences.parquet` has exactly 30 columns; the drop list is
   absent; the preserve list is present.
3. `npm test` passes (covers the src/sqlite.ts edit end-to-end including type-checking).
4. `python3 -m py_compile data/tests/test_dbt_diff.py` succeeds (the docstring change is
   syntactically valid).

Combined with Plan 01, Plan 02, and Plan 03, this closes Phase 85: the dbt project builds
cleanly with 0 FAIL / 0 ERROR, the contract is 30 columns, and the macro is documented.
</verification>

<success_criteria>
- marts/schema.yml declares 30 columns (verified by YAML parse + count).
- marts/occurrences.sql projects 30 columns; specimen_inat_taxon_name and
  specimen_inat_quality_grade preserved.
- src/sqlite.ts CREATE TABLE occurrences declares 30 columns; preserve list present;
  `npm test` clean.
- data/tests/test_dbt_diff.py docstring says `30 cols`.
- `dbt build` exits 0; `npm test` passes.
- Phase 85 ROADMAP success criteria 1, 3, 4, 5 are met (SC#2 is reinterpreted per locked
  decision D-03).
</success_criteria>

<output>
After completion, create `.planning/phases/85-pre-cutover-groundwork/85-04-SUMMARY.md` capturing:
- The exact lines changed in each of the 4 files (schema.yml, occurrences.sql, sqlite.ts,
  test_dbt_diff.py).
- The post-build column list (30 names) from DESCRIBE.
- Confirmation that `dbt build` exits 0 with 0 FAIL / 0 ERROR, and `npm test` passes.
- Explicit note that `test_dbt_diff.py::test_occurrences_schema_matches` will fail against
  current `public/data/occurrences.parquet` until that file is regenerated; this is expected
  per REQUIREMENTS.md and is NOT a Phase 85 regression. The public regeneration is deferred
  to a cron run or explicit step in Phase 88 cutover.
- Roll-up of Phase 85 outcomes: TEST-01 closed (Plan 01), TEST-02 closed (Plan 02), CLEAN-01
  documented (Plan 03), CLEAN-02 contract narrowed (Plan 04). Ready for Phase 86.
</output>
