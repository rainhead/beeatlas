---
phase: 153-occurrences-near-me
plan: 02
type: execute
wave: 2
depends_on: ["153-01"]
files_modified:
  - src/bee-map.ts
  - src/bee-pane.ts
  - src/bee-atlas.ts
  - src/tests/filter-join-execution.test.ts
  - src/tests/bee-atlas-legacy-taxon.test.ts
  - src/tests/bee-pane.test.ts
  - src/tests/bee-map.test.ts
autonomous: true

requirements: [NEAR-01, NEAR-02, NEAR-03]

must_haves:
  truths:
    - "A standalone 'Near me' affordance renders on its own row in the filter pane, not inside the Where section, labeled `Near me · 10 km` and reusing the existing .chip/.chip-remove classes (D-06); tapping it emits near-me-changed(true) and renders a removable chip that goes active/pending immediately (the map/list holds until the first GPS fix, then filters) (D-01)"
    - "Toggling the chip off then on re-captures the current GPS position — re-tap is the only refresh affordance, with no coupling to the recenter button (D-05)"
    - "When the proximity filter matches zero occurrences, the existing empty state is reused with no near-me-specific copy (D-08)"
    - "Tapping 'Near me' commands <bee-map> to .trigger() the GeolocateControl exactly once, surfacing the blue dot + accuracy ring (D-03)"
    - "The first GPS fix after activation freezes the center into _nearMeCenter, clears the one-shot _nearMePending flag, and runs ONE guarded filter query; subsequent drifting fixes do not re-query (D-04)"
    - "On denied/unavailable location the existing Phase 152 toast appears and the chip does not strand in pending (D-02)"
    - "Restoring ?near=1 re-activates geolocation and defers the query until a fix arrives (D-07); removing the chip clears nearMe and drops ?near=1"
    - "_nearMeCenter is never on FilterState and never serialized — only the boolean ?near=1 round-trips"
  artifacts:
    - path: "src/bee-map.ts"
      provides: "_geolocate instance field + public triggerGeolocate() method"
      contains: "triggerGeolocate"
    - path: "src/bee-pane.ts"
      provides: "_nearMe @state mirror, _renderNearMe() standalone row, near-me-changed emit, sync from filterState prop"
      contains: "near-me-changed"
    - path: "src/bee-atlas.ts"
      provides: "_nearMeCenter + _nearMePending fields, _onNearMeToggle handler, @query('bee-map') ref, one-shot deferred-query block in _onUserLocationChanged, nearMeCenter threaded through every query call site, nearMe:false backfilled in every FilterState literal"
      contains: "_nearMePending"
  key_links:
    - from: "src/bee-pane.ts near-me chip"
      to: "src/bee-atlas.ts _onNearMeToggle"
      via: "near-me-changed CustomEvent (bubbles+composed) wired in render()"
      pattern: "@near-me-changed"
    - from: "src/bee-atlas.ts _onNearMeToggle"
      to: "src/bee-map.ts triggerGeolocate()"
      via: "@query('bee-map') element ref"
      pattern: "triggerGeolocate"
    - from: "src/bee-atlas.ts _onUserLocationChanged"
      to: "src/bee-atlas.ts _runFilterQuery (via _filterGuard)"
      via: "one-shot _nearMePending block capturing _nearMeCenter"
      pattern: "_nearMePending"
    - from: "src/bee-atlas.ts _runFilterQuery"
      to: "queryVisibleGeoJSON(filterState, _nearMeCenter)"
      via: "second argument"
      pattern: "this\\._nearMeCenter"
---

<objective>
Wire the "Near me" filter end to end on top of the Wave-1 model (153-01): a standalone removable chip in `<bee-pane>`, a `near-me-changed` event, a `triggerGeolocate()` command method on `<bee-map>`, and the activation/freeze/deferral state machine on `<bee-atlas>` that captures a frozen GPS center, fires exactly one guarded query, threads the ephemeral center through every query call site, and round-trips `?near=1` (boolean only).

Purpose: This is the user-facing half of the phase. It connects already-proven SQL/URL functions to geolocation and the UI, honoring the locked decisions: single-tap filter + blue-dot coupling (D-03), freeze-at-activation (D-04), re-tap to refresh (D-05), standalone chip (D-06), boolean-only URL (D-07), and reuse of the Phase 152 denial toast (D-02). It also backfills `nearMe: false` into every component-side `FilterState` literal so `npm run build` (tsc) passes across the whole codebase.

Output: `<bee-map>`, `<bee-pane>`, `<bee-atlas>` extended; component/source-analysis tests added; full `npm test && npm run build` green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/153-occurrences-near-me/153-CONTEXT.md
@.planning/phases/153-occurrences-near-me/153-RESEARCH.md
@.planning/phases/153-occurrences-near-me/153-PATTERNS.md
@.planning/phases/153-occurrences-near-me/153-01-SUMMARY.md
@CLAUDE.md
</context>

<artifacts_this_phase_produces>
Symbols introduced by THIS plan (Wave 2):

- `<bee-map>._geolocate?: mapboxgl.GeolocateControl` — the control lifted from a local `const` (bee-map.ts:396) to an instance field.
- `<bee-map>.triggerGeolocate()` — public method that calls `this._geolocate?.trigger()`, tolerating the not-yet-`_setup` case. Imperative command called downward by `<bee-atlas>` — pure-presenter preserved (command in, no state stored).
- `near-me-changed` — `CustomEvent<boolean>` (`bubbles: true, composed: true`) emitted by `<bee-pane>` when the user taps the chip on (`true`) or removes it (`false`).
- `<bee-pane>._nearMe: boolean` — `@state` mirror of `filterState.nearMe`, synced in `updated()`.
- `<bee-pane>._renderNearMe()` — new standalone render method (own row, sibling to `_renderWho`/`_renderWhere`/`_renderWhen`).
- `<bee-atlas>._nearMeCenter: { lat: number; lon: number } | null` — private (NOT `@state`, NOT on FilterState) FROZEN center snapshot (D-04 / D-07).
- `<bee-atlas>._nearMePending: boolean` — private one-shot GPS-fix barrier flag.
- `<bee-atlas>._onNearMeToggle(e: CustomEvent<boolean>)` — activation/deactivation handler.
- `<bee-atlas>` `@query('bee-map')` element ref — to command `triggerGeolocate()`.
- `nearMe: false` backfilled into every component-side `FilterState` literal (bee-map.ts:55; bee-atlas.ts construction :73, init :480, restore :1035-1047, popstate-merge :1046, filter-changed :1289; and the legacy-taxon test literal).

Consumes from 153-01: `FilterState.nearMe`, `buildFilterSQL(f, nearMeCenter?)`, the `nearMeCenter` param on all 5 query functions, `?near=1` serialize/parse.
</artifacts_this_phase_produces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Contracts — triggerGeolocate() on bee-map, near-me-changed event + standalone chip on bee-pane</name>
  <files>src/bee-map.ts, src/bee-pane.ts</files>
  <read_first>
    - src/bee-map.ts:30-33 (@query('#map') decorator usage — the element-ref idiom), :388-427 (the GeolocateControl block — lift the local `const geolocate` at :396 to an instance field; mirror the `.then()` `_setup`-tolerance at :418-426), :165-169 (`_emit` helper)
    - src/bee-pane.ts:1-11 (imports — `nothing`, `FilterChangedEvent`), :58 (`@property filterState`), :96-106 (the `_selectedPlace`/`_elevMin` @state mirror declarations), :505-555 (the `updated()` filterState-sync method — the `selectedPlace` sync at :545-551 is the analog), :569-589 (`_emitFilter` — the CustomEvent emit shape), :977-1062 (`_renderWhere` — the `selectedPlace` removable-chip at :1004-1010 is the chip analog; do NOT nest the new chip here), :303-325 (the `.chip`/`.chip-remove` CSS), :1145-1162 (the render composition where `_renderWho`/`_renderWhere`/`_renderWhen` are placed)
    - .planning/phases/153-occurrences-near-me/153-PATTERNS.md (bee-map.ts + bee-pane.ts sections — exact idioms and line numbers)
    - .planning/phases/153-occurrences-near-me/153-RESEARCH.md (Q3 — dedicated `near-me-changed` event rationale; Pitfall 4 — `.trigger()` not-yet-setup no-op; D-06 standalone chip; D-01 pending/active state)
  </read_first>
  <action>
    bee-map.ts:
    1. Add a private instance field `_geolocate?: mapboxgl.GeolocateControl;` (declared alongside other private fields). Inside `firstUpdated`/`_initMap`, change the local `const geolocate = new mapboxgl.GeolocateControl(...)` (bee-map.ts:396) to assign `this._geolocate = new mapboxgl.GeolocateControl(...)` and use `this._geolocate` for `addControl`, the `.on('geolocate'|'error')` relays, and the existing auto-trigger `.then()` block. Do NOT remove or weaken the existing `user-location-changed` relay (bee-map.ts:406-416) — near-me consumes that relay; it does not open its own getCurrentPosition (D-03).
    2. Add a public method `triggerGeolocate()` that calls `this._geolocate?.trigger()`. Mirror the Phase 152 robustness (Pitfall 4): the control's `_setup` flag is async, so guard against the not-yet-ready case — by the time a user can tap a chip the map is long initialized, so a direct `.trigger()` is normally fine, but optional-chain it and tolerate the no-op (do not throw if `_geolocate` is undefined). This is a pure command — store NO state in `<bee-map>` as a result (pure-presenter invariant: CLAUDE.md state-ownership).
    3. Add `nearMe: false` to the inline default `filterState` literal at bee-map.ts:44-56 (after `selectedPlace: null`).

    bee-pane.ts:
    4. Add `@state() private _nearMe = false;` near the other filter mirrors (around bee-pane.ts:100).
    5. In `updated()` (bee-pane.ts:508-555), after the `selectedPlace` sync block, add a near-me sync: `if (this._nearMe !== f.nearMe) this._nearMe = f.nearMe;` so a URL restore / programmatic filter change reflects in the chip.
    6. Add a new render method `_renderNearMe()` that renders a STANDALONE row (own `<div class="filter-row">` or equivalent, sibling structure to `_renderWhere`, NOT nested inside it — D-06). When `_nearMe` is false, render an inactive trigger affordance (a tappable button/chip labeled `Near me`) whose click calls `this._emitNearMe(true)`. When `_nearMe` is true, render the active/pending removable chip (`Near me · 10 km`, suggested label/spacing per D-06) reusing `.chip`/`.chip-remove` classes verbatim, whose ✕ button calls `this._emitNearMe(false)`. (Pending vs active styling is Claude's discretion per CONTEXT — the chip is "active/pending" immediately on tap; the data simply hasn't filtered until the fix arrives, which `<bee-atlas>` handles. No separate pending visual is required, but you may add one.)
    7. Add a private `_emitNearMe(active: boolean)` that dispatches `new CustomEvent<boolean>('near-me-changed', { bubbles: true, composed: true, detail: active })` (copy the `_emitFilter` emit shape, bee-pane.ts:573). Do NOT thread `nearMe` through `_emitFilter`/`FilterChangedEvent` — a dedicated event keeps the geolocation side effect out of the generic filter path (RESEARCH Q3).
    8. Place `${this._renderNearMe()}` in the filter-panel composition (bee-pane.ts:1156-1162), on its own line as a sibling to `_renderWhere`/`_renderWhen` — pick the position that reads naturally (e.g. directly after `_renderWhere`). Reuse existing `.chip`/`.chip-remove` CSS; no new chip CSS class required.
  </action>
  <verify>
    <automated>npm test -- bee-pane bee-map filter-join-execution</automated>
  </verify>
  <acceptance_criteria>
    - bee-map.ts declares a `_geolocate` instance field and a public `triggerGeolocate()` that calls `this._geolocate?.trigger()`; the `user-location-changed` relay and the granted-only auto-trigger remain intact.
    - bee-map.ts does NOT store any near-me/location state as a result of `triggerGeolocate()` (pure-presenter — verifiable by source: no new `_userLocation`/`_nearMe` field on bee-map).
    - bee-pane.ts declares `_nearMe` @state, a `_renderNearMe()` standalone render method (not inside `_renderWhere`), and `_emitNearMe` dispatching `near-me-changed` with `{bubbles:true, composed:true}`.
    - `_renderNearMe()` is invoked in the filter-panel render as a sibling row.
    - Both component default `filterState` literals (bee-map.ts:55) carry `nearMe: false`.
    - `npm test -- bee-pane bee-map filter-join-execution` green.
  </acceptance_criteria>
  <done>The downward command (`triggerGeolocate`), the upward event (`near-me-changed`), and the standalone chip exist with correct shapes; `<bee-atlas>` can be wired to them in Task 2.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: bee-atlas activation/freeze/deferral state machine + thread frozen center through every query</name>
  <files>src/bee-atlas.ts</files>
  <read_first>
    - src/bee-atlas.ts:3 (the filter.ts import — query functions), :73-... (the construction-time `_filterState` literal), :115-151 (the @state/private field declarations; `_userLocation` at :122; `_filterGuard` at :144), :159-167 (`intendedFilterActive` getter + `userLocation` getter), :335-377 (render — the `<bee-map>` element and its event bindings; `@user-location-changed` at :376; `<bee-pane>` element starts at :378), :480 (init filter literal), :591-597 (`_runFilterQuery` — wraps `queryVisibleGeoJSON(this._filterState)` in `_filterGuard`), :818-836 (`_runTableQuery` — `queryTablePage` call site), :839-867 (`_runListQuery` — `queryListPage` call site), :986-1000 (`_onUserLocationChanged` — add the one-shot deferred block here), :1021-1054 (`_onPopState` restore — the filter literal at :1035-1047), :1274-1313 (`_onFilterChanged` — filter literal at :1278-1290), :1351 (`queryAllFiltered` CSV call site), :1453-1467 (`_onBoundaryModeChanged`)
    - .planning/phases/153-occurrences-near-me/153-PATTERNS.md (bee-atlas.ts section, items 1-5 — exact field declarations, the deferred-query block, the guarded-query change)
    - .planning/phases/153-occurrences-near-me/153-RESEARCH.md (Pattern 3 — one-shot pending flag + fast path; Pitfall 2 — never query before a fix; Pitfall 3 — drifting dot must not re-query; the activation-flow diagram at lines 86-121)
    - .planning/phases/153-occurrences-near-me/153-01-SUMMARY.md (the final parameter order of each threaded query function)
  </read_first>
  <action>
    In `src/bee-atlas.ts`:
    1. Add two NON-reactive private fields (NOT @state, NOT on FilterState — D-07): `private _nearMeCenter: { lat: number; lon: number } | null = null;` (the FROZEN snapshot — D-04) and `private _nearMePending = false;` (one-shot GPS-fix barrier — RESEARCH Pattern 3). Place them near `_filterGuard` (bee-atlas.ts:144).
    2. Add a `@query('bee-map')` element ref (mirror the decorator usage; import `query` from `lit/decorators.js` if not already imported) so `_onNearMeToggle` can command the control.
    3. Add `_onNearMeToggle(e: CustomEvent<boolean>)`:
       - On `true`: set `this._filterState = { ...this._filterState, nearMe: true }`; set `this._nearMePending = true`; call the bee-map ref's `triggerGeolocate()` to surface the blue dot + ring (D-03). FAST PATH (RESEARCH Pattern 3): if `this._userLocation` is already non-null, capture `this._nearMeCenter = { lat, lon }` from it, clear `_nearMePending`, and run `this._runFilterQuery()` immediately (then `_replaceUrlState()`); otherwise leave the pending flag set and let `_onUserLocationChanged` fire the query. Always `_replaceUrlState()` so `?near=1` appears immediately (D-07).
       - On `false` (chip removed): set `this._filterState = { ...this._filterState, nearMe: false }`; clear `this._nearMeCenter = null` and `this._nearMePending = false`; run `this._runFilterQuery()` then `_replaceUrlState()` (drops `?near=1`). Mirror the table/list refresh that `_onFilterChanged` does (`_runTableQuery()` / list re-query if pane is list) so all views update consistently.
       - Wire `@near-me-changed=${this._onNearMeToggle}` on `<bee-map>` OR `<bee-pane>` (whichever hosts the chip — the chip is in `<bee-pane>`, so bind it there in the `<bee-pane>` element at bee-atlas.ts:378+; the event bubbles+composed so either works, but bind where the emitter lives).
    4. In `_onUserLocationChanged` (bee-atlas.ts:986-1000), in the SUCCESS branch only (after `this._userLocation = { lat, lon, accuracy }; this._locationError = false;`), add a one-shot block: `if (this._nearMePending) { this._nearMeCenter = { lat, lon }; this._nearMePending = false; this._runFilterQuery(); this._runTableQuery(); /* + list re-query if pane is list */ this._replaceUrlState(); }`. This is the FROZEN capture (D-04) — subsequent fixes update `_userLocation` (blue dot) but do NOT re-query (Pitfall 3). Do NOT re-query unconditionally. In the ERROR branch, the existing behavior (set `_locationError`, clear `_userLocation`) stays; ADDITIONALLY clear the pending state so the chip does not strand (D-02): set `this._nearMePending = false`, set `this._filterState = { ...this._filterState, nearMe: false }`, `this._nearMeCenter = null`, and `_replaceUrlState()` — the chip goes inactive and the existing toast (already rendered from `_locationError`) communicates the failure. (Confirm: leaving `nearMe:true` with no center would show ALL points via the proximity-clause-omitted path, which is wrong on denial — so clearing nearMe on error is required by D-02's "leave the chip inactive".)
    5. Thread `this._nearMeCenter` into EVERY query call site (use the final param order from 153-01-SUMMARY): `_runFilterQuery` → `queryVisibleGeoJSON(this._filterState, this._nearMeCenter)`; `_runTableQuery` → `queryTablePage(this._filterState, ..., this._nearMeCenter)`; `_runListQuery` → `queryListPage(this._filterState, ..., this._nearMeCenter)`; the CSV export at :1351 → `queryAllFiltered(this._filterState, this._tableSortBy, this._nearMeCenter)`. (If `queryOccurrencesByBounds` is called anywhere in bee-atlas, thread it too.) The deferred near-me query slots into the SAME `_filterGuard` unchanged (CLAUDE.md filter race guard — re-tap/filter-change discards a stale in-flight result).
    6. URL RESTORE (D-07): in `_onPopState` (and any firstUpdated/init path that builds `_filterState` from `parseParams`), add `nearMe: parsed.filter?.nearMe ?? false` to the filter literal. When the restored `nearMe` is true, run the SAME pending flow as a fresh tap: set `_nearMePending = true` and command `triggerGeolocate()` (defer the query until a fix arrives — do NOT query with a null center). Add `nearMe` to the `_onFilterChanged` filter literal too (`nearMe: detail.nearMe ?? this._filterState.nearMe ?? false` — but since `near-me-changed` is a dedicated event, `_onFilterChanged`/`FilterChangedEvent` does NOT carry nearMe; preserve the existing `nearMe` across a generic filter change by spreading: ensure `_onFilterChanged` does not clobber `nearMe` to false — read the current `this._filterState.nearMe` and keep it).
    7. Backfill `nearMe: false` (or the preserved value) into EVERY `FilterState` object literal in bee-atlas.ts so tsc compiles: construction (:73), init (:480), `_onFilterChanged` (:1278-1290 — preserve current value), `_onPopState` (:1035-1047 — from parsed), and the `_onBoundaryModeChanged` spread at :1458 (a spread `{ ...this._filterState, selectedPlace: null }` already preserves nearMe — no change needed there, but verify).
  </action>
  <verify>
    <automated>npm test && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `_nearMeCenter` and `_nearMePending` are private fields, NOT `@state`, NOT on FilterState (source assertion).
    - `_onNearMeToggle` sets `nearMe`, sets `_nearMePending`, calls `triggerGeolocate()` via the `@query('bee-map')` ref, has the fast path when `_userLocation` is non-null, and writes the URL.
    - `_onUserLocationChanged` success branch has a one-shot `if (this._nearMePending)` block that captures `_nearMeCenter`, clears the flag, and runs exactly one guarded query; subsequent fixes do not re-query.
    - `_onUserLocationChanged` error branch clears `_nearMePending` and sets `nearMe:false` so the chip goes inactive on denial (D-02).
    - Every query call site passes `this._nearMeCenter` (queryVisibleGeoJSON, queryTablePage, queryListPage, queryAllFiltered).
    - `_onPopState` restore sets `nearMe` from `parsed.filter?.nearMe`, and when true re-triggers geolocation + defers the query.
    - Every `FilterState` literal in bee-atlas.ts has `nearMe`; `_onFilterChanged` preserves the current `nearMe` rather than clobbering it.
    - `npm test && npm run build` both green (tsc passes across the whole codebase — every FilterState literal has nearMe).
  </acceptance_criteria>
  <done>The activation/freeze/deferral state machine works: one tap triggers geolocation, the first fix freezes the center and fires one guarded query, drift does not re-query, denial deactivates the chip, and `?near=1` restores via the deferred flow — all with the center kept off FilterState.</done>
</task>

<task type="auto">
  <name>Task 3: Source-analysis + behavior tests for the wiring (privacy, freeze, pure-presenter, restore)</name>
  <files>src/tests/filter-join-execution.test.ts, src/tests/bee-atlas-legacy-taxon.test.ts, src/tests/bee-pane.test.ts, src/tests/bee-map.test.ts</files>
  <read_first>
    - src/tests/filter-join-execution.test.ts (the real-engine adapter — extend if a wiring-level SQL assertion helps)
    - src/tests/bee-atlas-legacy-taxon.test.ts:33 (the FilterState literal needing `nearMe: false`)
    - src/tests/bee-pane.test.ts (existing bee-pane test patterns — event-dispatch assertions)
    - src/tests/bee-map.test.ts (existing bee-map test patterns)
    - The LOC-02 source-analysis test style in src/tests/filter-join-execution.test.ts lines 10-30 (readFileSync of a component source, then regex assertions) — this is the established pattern for invariant assertions that jsdom cannot exercise
  </read_first>
  <action>
    Add the following automated tests (favor the established source-analysis pattern — `readFileSync` the component source then regex-assert — for invariants jsdom/Vitest cannot drive, since the live GeolocateControl needs a real device):
    1. Privacy invariant (T-153-01): assert `src/bee-atlas.ts` source declares `_nearMeCenter` as a `private` field and does NOT declare it as `@state`; assert no string `nearMe.*lat|lat.*nearMe` co-locates coordinates onto FilterState (i.e. the FilterState interface in filter.ts has `nearMe` but no lat/lon — re-assert from filter.ts source that the FilterState block contains `nearMe` and not `lat:`/`lon:`).
    2. Pure-presenter invariant (CLAUDE.md): assert `src/bee-map.ts` declares a `triggerGeolocate` method and does NOT store a near-me/location field as a result (no `private _nearMe` / no `@state _userLocation` on bee-map — extend the existing LOC-02 pure-presenter assertions).
    3. Freeze invariant (D-04 / Pitfall 3): assert `src/bee-atlas.ts` `_onUserLocationChanged` re-queries only inside a `_nearMePending` guard (regex: the `_runFilterQuery` call in that handler is reachable only under `if (this._nearMePending)`), i.e. there is no unconditional re-query in `_onUserLocationChanged`.
    4. Dedicated-event invariant (Q3): assert `near-me-changed` appears in `src/bee-pane.ts` and is wired (`@near-me-changed`) in `src/bee-atlas.ts`.
    5. Add `nearMe: false` to the FilterState literal in `src/tests/bee-atlas-legacy-taxon.test.ts:33`.
    6. (Optional, if expressible in jsdom) a bee-pane component test: setting `filterState.nearMe = true` renders a removable chip whose ✕ dispatches `near-me-changed` with `detail: false`; the inactive state renders a trigger that dispatches `near-me-changed` with `detail: true`.
    Do NOT attempt to drive the live GeolocateControl in a test — that path is the manual UAT in plan 153-03.
  </action>
  <verify>
    <automated>npm test</automated>
  </verify>
  <acceptance_criteria>
    - A test asserts `_nearMeCenter` is `private` and not `@state` in bee-atlas.ts (privacy / D-07).
    - A test asserts bee-map.ts exposes `triggerGeolocate` and stores no location/near-me state (pure-presenter).
    - A test asserts `_onUserLocationChanged` re-queries only under the `_nearMePending` guard (freeze / D-04).
    - A test asserts the `near-me-changed` event is emitted by bee-pane and bound in bee-atlas.
    - `src/tests/bee-atlas-legacy-taxon.test.ts` FilterState literal has `nearMe: false`.
    - `npm test` green.
  </acceptance_criteria>
  <done>The load-bearing invariants (privacy, freeze, pure-presenter, dedicated event) are guarded by automated tests, complementing the manual real-device UAT in 153-03.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| GeolocationCoordinates → `<bee-atlas>` state | The browser geolocation fix enters app state via the `user-location-changed` relay; near-me snapshots a frozen copy into `_nearMeCenter`. |
| `<bee-atlas>` state → URL / history | `_replaceUrlState()` serializes `FilterState` to the address bar — a public, shareable surface. |
| `<bee-atlas>` → `<bee-map>` command | `triggerGeolocate()` crosses the component boundary downward; must not invert the state-ownership invariant. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-153-01 | Information Disclosure | `_nearMeCenter` / `_replaceUrlState` | mitigate | **Location privacy (D-07):** the frozen center lives in a `private` field on `<bee-atlas>`, NEVER on `FilterState`, so `buildParams` (which iterates FilterState) cannot serialize it. Only the boolean `?near=1` round-trips. Task 3 adds a source-analysis test asserting `_nearMeCenter` is `private` and not `@state`/not on FilterState; 153-01 already regression-tests that `buildParams` emits no coordinate fragment. |
| T-153-03 | Tampering / Repudiation | denial path | mitigate | On geolocation error the chip is deactivated (D-02), preventing a stranded "active but unfiltered" state that would silently show ALL points (a misleading "near me" result). The existing Phase 152 toast communicates the failure. |
| T-153-04 | Elevation of Privilege (invariant breach) | `<bee-map>` | accept→mitigate | `triggerGeolocate()` is a command-in method; pure-presenter is preserved because no state is stored as a result. Task 3 asserts bee-map stores no location/near-me state. |
| T-153-SC | Tampering | npm/pip/cargo installs | accept | No packages installed this phase (RESEARCH Package Legitimacy Audit: not applicable). No install task; nothing to gate. |
</threat_model>

<verification>
- `npm test` — full suite green (all new wiring tests + all pre-existing suites).
- `npm run build` — `tsc --noEmit` then Eleventy + Vite build pass; every `FilterState` literal across `src/` carries `nearMe`.
- Source-analysis tests enforce the privacy, freeze, and pure-presenter invariants that jsdom cannot exercise.
- The live-GPS behaviors (real OS permission prompt, frozen-set-on-walk, <200 ms timing log) are deferred to the manual UAT plan 153-03 (UI hint: yes).
</verification>

<success_criteria>
- Tapping "Near me" triggers the GeolocateControl, shows the blue dot, and (after a fix) filters map + list/table to within 10 km (D-01, D-03).
- The filtered set freezes at the activation position; the drifting dot does not re-query (D-04); re-tap re-captures (D-05).
- Near-me AND-composes with all existing filters across map and list/table (NEAR-01).
- Denial/unavailable shows the Phase 152 toast and leaves the chip inactive (D-02).
- `?near=1` appears when active and restores via the deferred flow; removing the chip clears it (NEAR-03, D-07).
- `_nearMeCenter` never reaches FilterState or the URL (privacy).
- `npm test && npm run build` green.
</success_criteria>

<output>
Create `.planning/phases/153-occurrences-near-me/153-02-SUMMARY.md` when done. Record: where the near-me chip sits in the pane layout, the exact denial-path behavior chosen, how the URL-restore deferred flow is wired, and any deviation from the recommended designs (e.g. if a pending visual was added).
</output>
