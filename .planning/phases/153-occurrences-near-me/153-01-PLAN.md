---
phase: 153-occurrences-near-me
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/bee-map.ts
  - src/tests/geolocation.test.ts
autonomous: true
requirements: [NEAR-01, NEAR-02, NEAR-03]
must_haves:
  truths:
    - "<bee-atlas> can trigger the Phase 152 GeolocateControl through a public method on <bee-map> (D-06)"
    - "The blue dot + accuracy ring still appear; the control remains the single source of position (D-06)"
  artifacts:
    - path: src/bee-map.ts
      provides: "private this._geolocate field + public requestUserLocation() method delegating to this._geolocate.trigger()"
      contains: "requestUserLocation"
    - path: src/tests/geolocation.test.ts
      provides: "source-analysis gate asserting bee-map exposes a public trigger and stores the control on an instance field"
      contains: "requestUserLocation"
  key_links:
    - from: "src/bee-map.ts requestUserLocation()"
      to: "this._geolocate.trigger()"
      via: "delegation"
      pattern: "_geolocate\\.trigger"
---

<objective>
Expose a public method on `<bee-map>` so `<bee-atlas>` can trigger the Phase 152 `GeolocateControl` on demand (D-06). Today the control is a local `const geolocate` inside `firstUpdated()` — private and unreachable. Near-me needs to ask the map to start a location fix; the control must remain the single source of position (the blue dot + accuracy ring must still appear), so we trigger the EXISTING control rather than calling `navigator.geolocation` directly.

This plan does NOT compute bounds, set `_selectionBounds`, or touch the URL — it only adds the trigger seam that plan 153-03 consumes. `<bee-map>` stays a pure presenter (no new `@state`, no `_userLocation`); it already relays `user-location-changed` upward.

Purpose: give the state-owner a clean way to start geolocation without reaching into bee-map internals (Architecture Invariant: state-owner/pure-presenter).
Output: `this._geolocate` instance field + public `requestUserLocation()` on `<bee-map>`; an extended source-analysis gate in `geolocation.test.ts`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/153-occurrences-near-me/153-CONTEXT.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Promote the GeolocateControl to an instance field and add a public requestUserLocation()</name>
  <files>src/bee-map.ts</files>
  <read_first>
    - src/bee-map.ts:379-431 — `firstUpdated()`: the `const geolocate = new mapboxgl.GeolocateControl(...)`, `this._map.addControl(geolocate, 'top-left')`, the two `geolocate.on(...)` relays, and the granted-only auto-trigger `.then(status => { if (status.state === 'granted') geolocate.trigger(); })`. This is the exact block to refactor — the local `geolocate` becomes a stored field WITHOUT changing any of its options, placement, relay wiring, or the granted-auto path (D-06 keeps the 152 behavior intact).
    - src/bee-map.ts:35-60 — the existing `@property` block, for where private instance fields are declared (e.g. `_map`). Declare `_geolocate` alongside the other private map-related fields, NOT as a `@property` and NOT as `@state` (pure-presenter invariant — see geolocation.test.ts).
    - src/bee-map.ts:268 — `this._emit('selection-drawn', {...})`: confirms `<bee-map>` already emits upward; no new state is introduced here.
    - CLAUDE.md §Architecture Invariants — `<bee-map>` is a pure presenter; it must hold NO reactive location state.
  </read_first>
  <action>
    Declare a private instance field `_geolocate` on the `BeeMap` class typed as the mapbox `GeolocateControl` (`mapboxgl.GeolocateControl`), initialized lazily in `firstUpdated()`. In `firstUpdated()`, change `const geolocate = new mapboxgl.GeolocateControl(...)` to assign to `this._geolocate` instead, and replace every subsequent reference in that block (`addControl`, the two `.on(...)` relays, and the granted-auto `.trigger()` call) to use `this._geolocate`. Do NOT change the control options, the `'top-left'` placement, the `user-location-changed` relay payloads, or the granted-only auto-trigger gate — those are Phase 152 behavior preserved by D-06.

    Add a public method `requestUserLocation()` that calls `this._geolocate?.trigger()`. Guard for the not-yet-constructed case (optional chaining) so a call before `firstUpdated` is a no-op rather than a crash. Add a short doc comment noting this is the seam `<bee-atlas>` uses for near-me (D-06): it starts a fix on the existing control so the blue dot + accuracy ring appear and the relayed `user-location-changed` carries the position to the state owner; it does NOT compute bounds.
  </action>
  <acceptance_criteria>
    - `src/bee-map.ts` declares a private `_geolocate` instance field (not `@property`, not `@state`).
    - `firstUpdated()` assigns the constructed `GeolocateControl` to `this._geolocate` and all in-block references (`addControl`, both `.on(...)`, the granted-auto `.trigger()`) use `this._geolocate` — the control options, `'top-left'` placement, relay payloads, and granted-only auto-trigger are unchanged.
    - A public `requestUserLocation()` method exists that delegates to `this._geolocate?.trigger()` and is a safe no-op before the control is constructed.
    - `grep -v '^\s*//' src/bee-map.ts | grep -c '_geolocate\.trigger'` returns at least 1 (the granted-auto path and/or requestUserLocation; comments excluded).
    - `npx tsc --noEmit` passes.
  </acceptance_criteria>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <done>`<bee-map>` stores the GeolocateControl on `this._geolocate` and exposes a public `requestUserLocation()` that triggers it; 152 behavior unchanged; no new reactive state; tsc clean.</done>
</task>

<task type="auto">
  <name>Task 2: Extend the geolocation source-analysis gate for the public trigger seam</name>
  <files>src/tests/geolocation.test.ts</files>
  <read_first>
    - src/tests/geolocation.test.ts:1-30 — the existing LOC-02 pure-presenter gate: it reads `bee-map.ts`/`bee-atlas.ts` as source strings and asserts regexes (e.g. bee-map does NOT declare `_userLocation` as `@state`, bee-map emits `user-location-changed`). Mirror this style exactly — these are static source-text assertions, NOT a mounted-component test.
    - src/bee-map.ts (after Task 1) — confirm the identifiers the new assertions match: `requestUserLocation`, `_geolocate`.
  </read_first>
  <action>
    Add a new `describe('NEAR-01/D-06: <bee-map> exposes a public GeolocateControl trigger seam', ...)` block to `geolocation.test.ts` reusing the existing `beeMapSrc` source-read. Add source-text assertions that (a) `bee-map.ts` declares a `requestUserLocation` public method, (b) `bee-map.ts` stores the control on an instance field `this._geolocate` (so a coordinator can trigger it), and (c) the pure-presenter invariant still holds — `bee-map.ts` still does NOT declare `_userLocation` as `@state` (keep/duplicate the regex so a regression here is caught in this NEAR block too). Do NOT assert on bounds, `_selectionBounds`, or `near-me-requested` — those belong to plans 153-02 / 153-03.
  </action>
  <acceptance_criteria>
    - `geolocation.test.ts` contains a NEAR-01/D-06 describe block with at least: a `requestUserLocation` assertion, a `this._geolocate` instance-field assertion, and a re-assertion that bee-map declares no `_userLocation` `@state`.
    - `npx vitest run src/tests/geolocation.test.ts` passes against the Task 1 source.
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run src/tests/geolocation.test.ts</automated>
  </verify>
  <done>The source-analysis gate proves `<bee-map>` exposes a public trigger and keeps the pure-presenter invariant; the geolocation test passes.</done>
</task>

</tasks>

<artifacts_this_phase_produces>
This plan contributes:
- **`<bee-map>` public method `requestUserLocation()`** — the seam `<bee-atlas>` calls in plan 153-03 to start a location fix on the existing GeolocateControl (D-06).
- **`this._geolocate` instance field** — the stored GeolocateControl (was a local const).
</artifacts_this_phase_produces>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser geolocation API → `<bee-map>` | The device GPS/permission prompt crosses into the app; `<bee-map>` only relays coordinates, never persists them. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-153-01 | Information disclosure | `<bee-map>` location relay | accept | `<bee-map>` remains a pure presenter — it relays `user-location-changed` and holds NO reactive location state (`@state`/`private _userLocation` forbidden, asserted in geolocation.test.ts). Storage/disclosure decisions live in `<bee-atlas>` and are modeled in 153-03. |
| T-153-02 | Elevation of privilege | public `requestUserLocation()` | accept | The method only calls `this._geolocate.trigger()`, which itself surfaces the browser's permission prompt; it cannot bypass user consent. No new permission surface is created (the 152 control already requested geolocation). |
</threat_model>

<verification>
- `npx tsc --noEmit` clean.
- `npx vitest run src/tests/geolocation.test.ts` passes.
- `<bee-map>` declares no `_userLocation` reactive state (pure-presenter invariant intact).
</verification>

<success_criteria>
- `<bee-atlas>` (in 153-03) will be able to call `beeMapEl.requestUserLocation()` to start a fix on the existing GeolocateControl.
- Phase 152 GeolocateControl behavior (options, top-left placement, granted-auto, relays) is byte-for-byte unchanged in intent.
- No haversine, `?near=1`, `nearMeCenter`, or new query/URL path introduced.
</success_criteria>

<output>
Create `.planning/phases/153-occurrences-near-me/153-01-SUMMARY.md` when done.
</output>
