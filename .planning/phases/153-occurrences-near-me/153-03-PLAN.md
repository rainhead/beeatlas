---
phase: 153-occurrences-near-me
plan: 03
type: execute
wave: 3
depends_on: ["153-02"]
files_modified:
  - src/bee-atlas.ts
  - .planning/phases/153-occurrences-near-me/153-HUMAN-UAT.md
autonomous: false
auto_advance: false
ui_hint: yes

requirements: [NEAR-01, NEAR-02, NEAR-03]

must_haves:
  truths:
    - "An operator confirms on a real device that tapping 'Near me' filters occurrences to within 10 km of their actual GPS position once a fix arrives"
    - "An operator confirms the filtered set freezes at the activation position and does not shift as the blue dot drifts"
    - "An operator confirms the proximity query timing log reports under 200 ms on the full occurrence set (NEAR-02 success criterion)"
    - "An operator confirms denied/unavailable location surfaces the Phase 152 toast and leaves the chip inactive"
    - "An operator confirms ?near=1 restores the deferred geolocation flow and chip-remove drops it"
  artifacts:
    - path: ".planning/phases/153-occurrences-near-me/153-HUMAN-UAT.md"
      provides: "Real-device UAT checklist + recorded operator sign-off"
      contains: "Near me"
    - path: "src/bee-atlas.ts"
      provides: "in-app proximity-query timing log (NEAR-02 measurement surface)"
      contains: "near-me"
  key_links:
    - from: "153-HUMAN-UAT.md scenarios"
      to: "NEAR-01/NEAR-02/NEAR-03 success criteria"
      via: "one scenario per manual-only behavior in 153-VALIDATION.md"
      pattern: "NEAR-0"
    - from: "src/bee-atlas.ts _runFilterQuery"
      to: "console timing log"
      via: "performance.now() bracket gated on _nearMeCenter"
      pattern: "performance\\.now"
---

<objective>
Add the in-app proximity-query timing log (the NEAR-02 measurement surface), then author and run the real-device Human UAT for the "Near me" filter — the behaviors that cannot be exercised headless: the live OS geolocation prompt, a real GPS fix, the frozen-set-on-walk behavior, the in-browser <200 ms timing log, and the denial path on a real permission-denied state. Record operator sign-off.

Purpose: NEAR-02's "<200 ms on the full occurrence set" is an explicit Success Criterion verified by a live timing log against the real worker + full ~97,648-row DB — the Node probe in RESEARCH (12.7 ms) excludes worker-RPC serialization (Assumption A1), so the in-app number must be confirmed on device. The live geolocation prompt and frozen-set behavior likewise require a real device (jsdom/simulators are unreliable for iOS standalone geolocation — STATE.md Phase 152 research flag). Per project convention (`feedback_uat_ui_phases`), this phase must NOT auto-advance past this checkpoint — `auto_advance: false` is in effect.

Output: an in-app timing log in `src/bee-atlas.ts`, and `.planning/phases/153-occurrences-near-me/153-HUMAN-UAT.md` with each scenario recorded Pass/Fail and an operator sign-off line.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/153-occurrences-near-me/153-CONTEXT.md
@.planning/phases/153-occurrences-near-me/153-VALIDATION.md
@.planning/phases/153-occurrences-near-me/153-02-SUMMARY.md
@.planning/phases/152-geolocatecontrol-location-state/152-HUMAN-UAT.md
@CLAUDE.md
</context>

<artifacts_this_phase_produces>
- `src/bee-atlas.ts` — a `console.info` timing log inside `_runFilterQuery`, gated on `_nearMeCenter !== null`, reporting elapsed ms + row count for near-me queries (NEAR-02 measurement surface).
- `.planning/phases/153-occurrences-near-me/153-HUMAN-UAT.md` — the recorded UAT checklist with operator sign-off.

No new exported symbols, events, or FilterState fields are produced — the feature itself ships in plans 01/02; this plan adds only the measurement log and the verification record.
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Add an in-app timing log around the proximity query (NEAR-02 measurement surface)</name>
  <files>src/bee-atlas.ts</files>
  <read_first>
    - src/bee-atlas.ts:591-597 (`_runFilterQuery` — after 153-02 it wraps `queryVisibleGeoJSON(this._filterState, this._nearMeCenter)` in `_filterGuard`; bracket a `performance.now()` measurement around it)
    - .planning/phases/153-occurrences-near-me/153-02-SUMMARY.md (the final `_runFilterQuery` shape after wiring; note whether 153-02 already added a timing log)
    - .planning/phases/153-occurrences-near-me/153-RESEARCH.md (Performance — 12.7 ms Node probe; Assumption A1 — worker-RPC overhead is the unverified term the on-device timing log must confirm)
  </read_first>
  <action>
    In `_runFilterQuery` (bee-atlas.ts:591-597), when `this._nearMeCenter !== null` (a near-me query is running), capture `const t0 = performance.now()` before the guarded `queryVisibleGeoJSON` call and, after it resolves (and is not stale), emit one `console.info` line reporting the elapsed ms and the resulting row count, e.g. `[near-me] proximity query <ms> ms, <rowCount> rows`. Gate the log on `_nearMeCenter` so it fires only for near-me queries — do NOT log on every filter query. Plain `console.info`; no new dependency, no UI. If 153-02 already added an equivalent timing log, leave it and record that in the summary rather than duplicating.
  </action>
  <verify>
    <automated>npm test && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `_runFilterQuery` emits a `console.info` timing line ONLY when `_nearMeCenter` is non-null, reporting elapsed ms and row count.
    - No timing-log output on non-near-me filter queries.
    - `npm test && npm run build` green.
  </acceptance_criteria>
  <done>The app prints a single proximity-query timing line on near-me activation, giving the operator the NEAR-02 <200 ms measurement.</done>
</task>

<task type="auto">
  <name>Task 2: Write the 153-HUMAN-UAT.md checklist</name>
  <files>.planning/phases/153-occurrences-near-me/153-HUMAN-UAT.md</files>
  <read_first>
    - .planning/phases/152-geolocatecontrol-location-state/152-HUMAN-UAT.md (the format to mirror — frontmatter with `ui_hint: yes`/`auto_advance: false`, Prerequisites, numbered Scenarios with Steps/Expected/Result, sign-off line)
    - .planning/phases/153-occurrences-near-me/153-VALIDATION.md ("Manual-Only Verifications" table — the exact behaviors to turn into scenarios)
    - .planning/phases/153-occurrences-near-me/153-CONTEXT.md (D-01 through D-08 — the locked behaviors the operator confirms)
  </read_first>
  <action>
    Create `153-HUMAN-UAT.md` mirroring `152-HUMAN-UAT.md`'s structure. Frontmatter: `phase: 153-occurrences-near-me`, `plan: 03`, `status: pending`, `gates: /gsd-verify-work`, `ui_hint: yes`, `auto_advance: false`, `created: <today>`. Include a Prerequisites section (dev server or deployed `/app`; for the real-GPS scenarios, a deployed or LAN-reachable `/app` on an actual phone with location services on). State that the automated invariants (SQL haversine correctness, URL round-trip, location privacy, freeze guard, pure-presenter) are covered by Vitest in plans 01/02 and that this file covers ONLY the behaviors that cannot run headless. Then author these numbered scenarios, each with Steps / Expected / Result (Pass·Fail·Deferred) / Notes:
    1. **Near-me filters to 10 km on a real fix (NEAR-01, D-01/D-03):** at a known location, open `/app`, tap "Near me", grant location → blue dot + accuracy ring appear AND map + list/table narrow to occurrences within 10 km. The chip shows active/pending immediately on tap; data settles once the fix lands.
    2. **AND-composition (NEAR-01, SC-2):** with "Near me" active, apply a taxon (and/or date) filter → both narrow simultaneously; clearing the taxon keeps "Near me" applied.
    3. **Frozen set on walk (NEAR-02, D-04/D-05):** with "Near me" active, walk ~100+ m → the blue dot moves but the filtered set does NOT shift; re-tapping the chip (off→on) re-captures the new position and re-filters.
    4. **<200 ms timing log (NEAR-02, SC-3):** open the console, activate "Near me", read the `[near-me] proximity query <ms> ms` line → confirm the reported time is under 200 ms on the full occurrence set.
    5. **Denial path (NEAR-01, D-02):** deny (or revoke) location permission, tap "Near me" → the Phase 152 toast appears and the chip does NOT remain active/pending (it returns to inactive); map/filters/table are unaffected.
    6. **URL round-trip + clear (NEAR-03, D-07):** with "Near me" active, confirm `?near=1` is in the address bar and NO coordinate appears in the URL; reload / open the `?near=1` link → geolocation re-activates and the query defers until a fix arrives; remove the chip (✕) → `?near=1` drops from the URL and the filter clears.
    Add a final "Requirements verified" line (NEAR-01/02/03) and an operator sign-off line (date + Pass/Fail). Leave all Result cells unmarked (the operator fills them in Task 3).
  </action>
  <verify>
    <automated>test -f .planning/phases/153-occurrences-near-me/153-HUMAN-UAT.md && grep -q "auto_advance: false" .planning/phases/153-occurrences-near-me/153-HUMAN-UAT.md</automated>
  </verify>
  <acceptance_criteria>
    - `153-HUMAN-UAT.md` exists with frontmatter `ui_hint: yes` and `auto_advance: false`.
    - It contains six numbered scenarios covering the six manual-only behaviors (10 km filter, AND-composition, frozen set, <200 ms timing log, denial path, URL round-trip + clear).
    - Each scenario has Steps, Expected, and an unmarked Result cell.
    - Requirements NEAR-01/02/03 are each referenced by at least one scenario.
  </acceptance_criteria>
  <done>The real-device UAT checklist exists, covering every manual-only behavior, ready for the operator to run.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 3: Operator runs the 153 real-device UAT and signs off (BLOCKING — no auto-advance)</name>
  <files>.planning/phases/153-occurrences-near-me/153-HUMAN-UAT.md</files>
  <action>
    PAUSE for the operator. This is a blocking human-verify checkpoint; per `feedback_uat_ui_phases` the phase must NOT auto-advance past it (`auto_advance: false`). Present the six scenarios from `153-HUMAN-UAT.md` to the operator, collect Pass/Fail/Deferred for each, write the results plus the sign-off line into `153-HUMAN-UAT.md`, and commit it. Do not mark Phase 153 verified until every scenario shows Pass (or Deferred with rationale). Any Fail becomes gap-closure input for `/gsd-plan-phase 153 --gaps`.
  </action>
  <what-built>
    The full "Near me" filter (plans 01-02) plus the in-app proximity-query timing log (Task 1). All automated checks — SQL haversine correctness (real-engine in-radius count), `?near=1` round-trip, location-privacy regression, freeze guard, and pure-presenter invariants — are green in Vitest. What remains are the six behaviors that only a real device with live GPS can confirm.
  </what-built>
  <how-to-verify>
    1. Serve `/app` reachable from a real phone: `npm run dev` for desktop/console checks; a deployed or LAN-reachable build for the on-device GPS scenarios.
    2. Open `.planning/phases/153-occurrences-near-me/153-HUMAN-UAT.md` and run all six scenarios in order, recording Pass/Fail/Deferred in each Result cell:
       - (1) Near-me filters to 10 km on a real fix
       - (2) AND-composition with taxon/date filters
       - (3) Frozen set on a ~100 m walk; re-tap re-captures
       - (4) Console `[near-me] proximity query <ms> ms` reads under 200 ms
       - (5) Denial path shows the Phase 152 toast and leaves the chip inactive
       - (6) `?near=1` round-trip (no coordinates in URL), deferred restore, and chip-remove clears it
    3. Add the operator sign-off line (date + overall Pass/Fail) and commit the filled-in UAT file.
    Any Fail becomes gap-closure input for `/gsd-plan-phase 153 --gaps`. Do NOT mark the phase verified until every scenario shows Pass (or Deferred with rationale).
  </how-to-verify>
  <verify>
    <human-check>All six 153-HUMAN-UAT.md scenarios recorded Pass (or Deferred with rationale); operator sign-off line present; file committed.</human-check>
  </verify>
  <done>The operator has run all six real-device scenarios, recorded results + sign-off in 153-HUMAN-UAT.md, and committed it.</done>
  <resume-signal>Type "approved" once all six scenarios are recorded Pass (or Deferred with rationale) and the filled-in 153-HUMAN-UAT.md is committed; otherwise describe the failures.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Live device GPS → app | The real OS geolocation permission flow and GPS fix — the surface this UAT exercises that headless tests cannot. |
| App state → URL (operator-observed) | Scenario 6 has the operator visually confirm the address bar carries `?near=1` and NO coordinates — the human cross-check of the T-153-01 privacy mitigation. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-153-01 | Information Disclosure | URL on a real device | mitigate | Scenario 6 has the operator confirm by eye that only `?near=1` appears in the address bar and no latitude/longitude is present — a human verification layered on top of the automated `buildParams` coords-never-serialized regression test from plan 01. |
| T-153-05 | Denial of Service (perf) | proximity query on full DB | mitigate | Scenario 4 confirms the live timing log reads <200 ms on the real worker + full ~97,648-row DB, validating Assumption A1 (worker-RPC overhead) that the Node probe could not measure. |
| T-153-SC | Tampering | npm/pip/cargo installs | accept | No packages installed this phase (RESEARCH Package Legitimacy Audit: not applicable). No install task; nothing to gate. |
</threat_model>

<verification>
- `npm test && npm run build` green (the timing-log change does not break the suite or the build).
- `153-HUMAN-UAT.md` exists, has `auto_advance: false`, and covers all six manual-only behaviors.
- The human-verify checkpoint is recorded Pass for all six scenarios (or Deferred with rationale) and committed before `/gsd-verify-work` for Phase 153 can pass.
- This plan is `autonomous: false` — execution PAUSES at the checkpoint and does NOT auto-advance (UI hint: yes; `feedback_uat_ui_phases`).
</verification>

<success_criteria>
- The in-app proximity-query timing log fires for near-me queries only.
- The real-device UAT checklist covers all six manual-only behaviors mapped to NEAR-01/02/03.
- The operator records Pass (or Deferred) for every scenario and signs off; the filled-in UAT file is committed.
- Phase 153 does not auto-advance past this checkpoint.
</success_criteria>

<output>
Create `.planning/phases/153-occurrences-near-me/153-03-SUMMARY.md` when done. Record: the timing-log line format, the on-device measured proximity time, and the operator UAT outcome (per-scenario Pass/Fail and overall sign-off). Note any UAT findings that became gap-closure input.
</output>
