---
phase: 153-occurrences-near-me
plan: 04
type: execute
wave: 3
depends_on: ["153-03"]
files_modified:
  - .planning/phases/153-occurrences-near-me/153-HUMAN-UAT.md
autonomous: false
auto_advance: false
requirements: [NEAR-01, NEAR-02, NEAR-03]
must_haves:
  truths:
    - "A real/simulated GPS fix produces a ~10 km box that filters occurrences and shows the icon chip (NEAR-01)"
    - "The near-me bounds AND-compose with an active taxon/date/region filter (NEAR-01, D-09)"
    - "Copying the URL into a fresh session reproduces the EXACT same occurrence set with no GPS prompt (NEAR-03, D-03) — the key new scenario"
    - "Denial/unavailable surfaces the Phase 152 toast and applies no bounds (NEAR-03, D-08)"
    - "The chip ✕ / Clear filters clears the bounds (NEAR-03, D-05)"
  artifacts:
    - path: .planning/phases/153-occurrences-near-me/153-HUMAN-UAT.md
      provides: "operator UAT checklist — desktop DevTools-Sensors scenarios + the shared-URL reproducibility check + a real-device confirmation"
      contains: "shared URL"
  key_links:
    - from: "operator"
      to: "near-me behavior"
      via: "DevTools Sensors geolocation override + cross-session URL paste"
      pattern: "Sensors"
---

<objective>
Author the operator UAT checklist for near-me and run the blocking human-verify checkpoint. The phase must NOT auto-advance past UAT (UI hint: yes — `autonomous: false` / `auto_advance: false`, per project convention `feedback_uat_ui_phases`).

Most scenarios are now desktop-testable via Chrome DevTools → Sensors → Location override (no field trip needed). The KEY NEW scenario is shared-URL reproducibility: paste the near-me URL into a fresh session and confirm it reproduces the identical occurrence set with no GPS prompt (D-03) — this is the whole point of the bounds-reuse redesign. A brief real-device confirmation (iOS standalone) is retained because Phase 152 established that simulator geolocation is unreliable on iOS, and because the prior toast failed on a real device.

Purpose: prove the user-visible near-me behavior on real chrome + at least one real device before the phase advances.
Output: `153-HUMAN-UAT.md` checklist + a blocking human-verify checkpoint.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/153-occurrences-near-me/153-CONTEXT.md
@.planning/phases/152-geolocatecontrol-location-state/152-HUMAN-UAT.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write 153-HUMAN-UAT.md</name>
  <files>.planning/phases/153-occurrences-near-me/153-HUMAN-UAT.md</files>
  <read_first>
    - .planning/phases/152-geolocatecontrol-location-state/152-HUMAN-UAT.md — the format to mirror (scenario table, exact steps, expected result, pass/fail, real-device section). Reuse its structure and its iOS-standalone real-device guidance.
    - .planning/phases/153-occurrences-near-me/153-CONTEXT.md §decisions — the behaviors to verify (D-02 box, D-03 shared URL, D-05 chip clear, D-07 freeze/re-tap, D-08 toast, D-09 AND-compose).
    - CLAUDE.md §Running Locally — `npm run dev` to serve the app for desktop testing.
  </read_first>
  <action>
    Write `153-HUMAN-UAT.md` as a lean operator checklist. Mark each scenario DESKTOP (DevTools-doable) or DEVICE (needs real GPS). Include at minimum:

    1. DESKTOP — Button + box: `npm run dev`; open the where ("County, ecoregion, or place") input; confirm a geolocate-icon button is right-aligned inside it. In DevTools → Sensors, set a Location override inside Washington with known nearby occurrences; tap the button; expect the blue dot to appear and occurrences to filter to roughly a ~20 km square around the override point, with an icon-only chip in the where input.
    2. DESKTOP — AND-compose (D-09): with near-me active, also apply a taxon (or year) filter; confirm the visible occurrences are the intersection (bounds AND taxon), and the table/list reflects the same set.
    3. DESKTOP — Shared-URL reproducibility (KEY, D-03): copy the URL while near-me is active; open it in a fresh incognito window with Sensors set to a DIFFERENT location (or geolocation blocked); confirm it reproduces the IDENTICAL occurrence set with NO geolocation prompt and the same icon chip — the recipient sees the sender's occurrences.
    4. DESKTOP — Clear (D-05): tap the chip ✕ (and separately "Clear filters" if present); confirm the bounds are removed, occurrences return to unfiltered (or the remaining filter), and the `sel=` param leaves the URL.
    5. DESKTOP — Re-tap recompute (D-07): move the Sensors override to a new point; tap the button again; confirm the box recomputes around the new point (the old box is replaced, not frozen to the prior location).
    6. DESKTOP — Denial toast (D-08): set DevTools Sensors to "Location unavailable" (or block the permission); tap the button; confirm the Phase 152 toast appears, is visible (not hidden behind the map), and NO bounds are applied.
    7. DEVICE — Real GPS on iOS standalone: install/open `/app` on a real iPhone; tap the button outdoors; confirm a real fix produces a box filtering to nearby occurrences, and that denying permission shows the toast on the real device (the 152 UAT toast gap is the reason this is retained).

    For each: exact steps, expected result, and a Pass/Fail line.
  </action>
  <acceptance_criteria>
    - `153-HUMAN-UAT.md` exists with the seven scenarios above (or a superset), each tagged DESKTOP/DEVICE with exact steps + expected result + Pass/Fail.
    - The shared-URL reproducibility scenario (D-03) is present and called out as the key new check.
    - `npm test && npm run build` is referenced as the automated gate that must be green before UAT.
  </acceptance_criteria>
  <verify>
    <automated>test -f .planning/phases/153-occurrences-near-me/153-HUMAN-UAT.md && grep -qi "shared" .planning/phases/153-occurrences-near-me/153-HUMAN-UAT.md</automated>
  </verify>
  <done>The operator UAT checklist exists, is mostly desktop-testable, and includes the shared-URL reproducibility scenario.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Operator runs the near-me UAT</name>
  <action>
    Pause for the operator. Confirm `npm test` and `npm run build` are green, then present `153-HUMAN-UAT.md` and have the operator work through every scenario (DESKTOP via DevTools → Sensors; DEVICE on a real iPhone). Do not advance the phase until the operator types "approved". This is a blocking gate (`auto_advance: false`) per the project's UI-hint convention.
  </action>
  <what-built>
    Near-me end-to-end: a geolocate button in the where input that resolves the user's GPS into a ~10 km bounding box, applied as a `_selectionBounds` filter that reuses the existing shift-drag query + `sel=` URL round-trip; an icon-only removable chip; AND-composition with other filters; the Phase 152 denial toast (now fixed). Build + full test suite are green.
  </what-built>
  <how-to-verify>
    1. Confirm `npm test` and `npm run build` are green.
    2. Run `npm run dev` and work through `.planning/phases/153-occurrences-near-me/153-HUMAN-UAT.md` — the DESKTOP scenarios via DevTools → Sensors (button+box, AND-compose, shared-URL reproducibility, clear, re-tap, denial toast).
    3. Run the DEVICE scenario on a real iPhone (`/app` standalone): a real GPS fix produces a box; denial shows the toast.
    4. Pay special attention to the shared-URL reproducibility check (open the near-me URL in a fresh session — it must reproduce the same occurrences with no GPS prompt) and to the denial toast actually appearing (it failed in the Phase 152 UAT).
  </how-to-verify>
  <verify>
    <human-check>Operator confirms all UAT scenarios pass, with special attention to shared-URL reproducibility (D-03) and the denial toast (D-08).</human-check>
  </verify>
  <done>Operator has worked through every UAT scenario and typed "approved" (or logged failures for gap closure).</done>
  <resume-signal>Type "approved" if all scenarios pass, or describe the failures (which scenario, observed vs. expected).</resume-signal>
</task>

</tasks>

<artifacts_this_phase_produces>
This plan contributes:
- **`153-HUMAN-UAT.md`** — the operator UAT checklist (desktop DevTools-Sensors scenarios + the shared-URL reproducibility check + a real-device confirmation).
</artifacts_this_phase_produces>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator + real device → near-me behavior | Verification only; introduces no new code surface. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-153-UAT | Information disclosure | shared-URL reproducibility check exercises the deliberate ~20 km box-in-URL disclosure (D-03) | accept | UAT confirms the disclosure is the intended coarse ~20 km box (not exact coordinates) and that a recipient reproduces the set without re-geolocating — validating the accepted, deliberate posture from T-153-PRIV (153-03). No new code; verification only. |
</threat_model>

<verification>
- `npm test` and `npm run build` green before UAT.
- Operator works through all UAT scenarios; the phase does not auto-advance (blocking checkpoint, `auto_advance: false`).
</verification>

<success_criteria>
- A real/simulated GPS fix produces a ~10 km box that filters occurrences and shows the icon chip; bounds AND-compose with other filters.
- A shared near-me URL reproduces the exact same occurrence set with no recipient GPS (the key new scenario, D-03).
- Denial fires the toast and applies no bounds (D-08); the chip ✕ / Clear filters clears the bounds (D-05).
- Operator approves before the phase advances.
</success_criteria>

<output>
Create `.planning/phases/153-occurrences-near-me/153-04-SUMMARY.md` when done.
</output>
