---
phase: 126-taxon-ids
plan: 03
type: execute
wave: 3
depends_on: ["126-02"]
files_modified:
  - _data/species.js
  - _pages/species-detail.njk
  - _pages/genus.njk
  - _pages/subgenus.njk
  - _pages/tribe.njk
autonomous: false
requirements: [TID-03]
must_haves:
  truths:
    - "Species pages render a 'View on iNaturalist' link to https://www.inaturalist.org/taxa/{taxon_id} (TID-03, D-05)"
    - "Genus, subgenus, and tribe pages render the same iNat link using each rank's self-row taxon_id (D-06)"
    - "The link is suppressed (no broken href) when taxon_id is null, so checklist-only taxa degrade gracefully"
    - "_data/species.js threads taxon_id onto speciesList and genus/subgenus/tribe list entries"
  artifacts:
    - path: "_pages/species-detail.njk"
      provides: "View on iNaturalist link, sibling to the atlas action link (D-05)"
      contains: "View on iNaturalist"
    - path: "_data/species.js"
      provides: "taxon_id on genusList/subgenusList/tribeList from higher_rank_taxon_ids.json (D-06)"
      contains: "higher_rank_taxon_ids"
  key_links:
    - from: "_data/species.js"
      to: "public/data/higher_rank_taxon_ids.json"
      via: "readFileSync + per-rank taxon_id lookup"
      pattern: "higher_rank_taxon_ids"
    - from: "_pages/species-detail.njk"
      to: "https://www.inaturalist.org/taxa/"
      via: "anchor href guarded by sp.taxon_id"
      pattern: "inaturalist.org/taxa"
---

<objective>
Render the "View on iNaturalist" action link on the species page (TID-03, D-05) and — by explicit D-06 scope expansion — on the genus, subgenus, and tribe pages, each using its own rank's taxon_id. Thread taxon_id through _data/species.js from species.json (species) and higher_rank_taxon_ids.json (higher ranks).

Purpose: TID-03 is the user-visible deliverable of the phase. D-06 extends it to higher-rank pages using the lookup produced in Plan 02. The link uses a null-guard so checklist-only taxa without a taxon_id degrade gracefully.
Output: Updated _data/species.js and four njk templates, plus a human-verify checkpoint confirming the rendered links.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/126-taxon-ids/126-CONTEXT.md
@.planning/phases/126-taxon-ids/126-RESEARCH.md
@.planning/phases/126-taxon-ids/126-PATTERNS.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Thread taxon_id through _data/species.js</name>
  <files>_data/species.js</files>
  <read_first>
    - _data/species.js (the file being modified; JSON-load block lines 13-22, speciesList filter line 97, genusList builder lines 103-153, subgenusList builder lines 159-222, tribeList builder lines 246-258)
    - .planning/phases/126-taxon-ids/126-PATTERNS.md (section "_data/species.js" — exact return-object additions + compound-key note)
    - .planning/phases/126-taxon-ids/126-RESEARCH.md (Pitfall 5 subgenus disambiguation)
  </read_first>
  <action>Add a readFileSync + JSON.parse of public/data/higher_rank_taxon_ids.json parallel to the existing species.json/seasonality.json reads, into const higherRankTaxonIds. speciesList needs NO change — taxon_id flows automatically from species.json through raw (confirm it is present, do not strip it). In the genusList return object add taxon_id from higherRankTaxonIds.genus indexed by genus name, with a null fallback (?? null). In the tribeList return object add taxon_id from higherRankTaxonIds.tribe indexed by tribe name, with ?? null. In the subgenusList return object add taxon_id from higherRankTaxonIds.subgenus indexed by subgenus NAME, with ?? null — the JSON subgenus dict already holds only subgenus-rank ids from Plan 02, so the Bombus genus==subgenus collision is already resolved upstream; do not use the genus dict for subgenus lookups. The ?? null fallback makes the template guard suppress the link when an id is missing.</action>
  <verify>
    <automated>cd /home/peter/dev/beeatlas && node --input-type=module -e "import('./_data/species.js').then(m=>Promise.resolve(m.default())).then(d=>{if(!(d.genusList||[]).some(x=>'taxon_id' in x))throw new Error('genusList missing taxon_id');if(!(d.tribeList||[]).some(x=>'taxon_id' in x))throw new Error('tribeList missing taxon_id');console.log('species.js ok')}).catch(e=>{console.error(e.message);process.exit(1)})"</automated>
  </verify>
  <acceptance_criteria>
    - _data/species.js reads public/data/higher_rank_taxon_ids.json
    - genusList entries carry taxon_id from higherRankTaxonIds.genus
    - tribeList entries carry taxon_id from higherRankTaxonIds.tribe
    - subgenusList entries carry taxon_id from higherRankTaxonIds.subgenus keyed on subgenus name (not the genus dict)
    - speciesList entries still carry taxon_id sourced from species.json (not stripped)
    - the node verify command prints "species.js ok"
  </acceptance_criteria>
  <done>_data/species.js threads taxon_id onto species + genus/subgenus/tribe list entries, with null fallback for missing ids.</done>
</task>

<task type="auto">
  <name>Task 2: Add View on iNaturalist link to all four taxon templates</name>
  <files>_pages/species-detail.njk, _pages/genus.njk, _pages/subgenus.njk, _pages/tribe.njk</files>
  <read_first>
    - _pages/species-detail.njk (the file being modified; existing atlas action link at line 46, the occurrence_count guard block lines 45-47, closing article tag line 48)
    - _pages/genus.njk (the file being modified; template tail lines 36-39, closing article tag + taxon-page script)
    - _pages/subgenus.njk (the file being modified; same tail structure)
    - _pages/tribe.njk (the file being modified; same tail structure)
    - .planning/phases/126-taxon-ids/126-PATTERNS.md (sections for each of the four templates — exact anchor markup + guard)
  </read_first>
  <action>Add a sibling action link to each template using the verbatim label "View on iNaturalist" with a trailing right-arrow (matching the existing atlas link arrow style, D-05) and an href of https://www.inaturalist.org/taxa/ followed by the rank variable's taxon_id. Wrap each in its own conditional guard so the link appears even when occurrence_count is 0 and is suppressed when taxon_id is null. In species-detail.njk guard on sp.taxon_id, render with sp.taxon_id, placed as a sibling immediately after the existing atlas link (line 46) before the closing article tag — a SEPARATE guard from the occurrence_count block so checklist-only species still get the link. In genus.njk guard on and render genus.taxon_id, placed before the closing article tag (after the media-grid closing div). In subgenus.njk use subgenus.taxon_id. In tribe.njk use tribe.taxon_id. Use the identical label in all four. After editing, run the production build (npm run build) to confirm templates compile and the link renders.</action>
  <verify>
    <automated>cd /home/peter/dev/beeatlas && grep -q "inaturalist.org/taxa" _pages/species-detail.njk && grep -q "View on iNaturalist" _pages/species-detail.njk && grep -q "genus.taxon_id" _pages/genus.njk && grep -q "subgenus.taxon_id" _pages/subgenus.njk && grep -q "tribe.taxon_id" _pages/tribe.njk && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - _pages/species-detail.njk contains "View on iNaturalist" and an href to https://www.inaturalist.org/taxa/ using sp.taxon_id, guarded by sp.taxon_id, as a sibling to the atlas link
    - _pages/genus.njk renders the link using genus.taxon_id, guarded
    - _pages/subgenus.njk renders the link using subgenus.taxon_id, guarded
    - _pages/tribe.njk renders the link using tribe.taxon_id, guarded
    - All four use the identical "View on iNaturalist" label (D-05)
    - npm run build exits 0
  </acceptance_criteria>
  <done>All four taxon pages render a guarded "View on iNaturalist" link to the iNat taxon URL; production build succeeds.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify iNaturalist links render on all four taxon pages</name>
  <what-built>The "View on iNaturalist" action link on species, genus, subgenus, and tribe pages, sourced from taxon_id (species) and higher_rank_taxon_ids.json (higher ranks), with a null guard for graceful degradation.</what-built>
  <how-to-verify>
    1. Run `npm run dev` and open a species page (e.g. a Bombus species at /species/Bombus/{epithet}/).
    2. Confirm a "View on iNaturalist" link appears as a sibling to the existing "View N records on the atlas" link, and its href is https://www.inaturalist.org/taxa/{integer}.
    3. Open a genus page (/genus/Bombus/), a subgenus page, and a tribe page (e.g. /tribe/Bombini/). Confirm each shows the same link with the correct rank's taxon_id (genus Bombus -> 52775, subgenus Bombus -> 538903, tribe Bombini -> 538883 per RESEARCH).
    4. Open a checklist-only species (occurrence_count 0) that still has a taxon_id and confirm the link appears (separate guard from occurrence_count). If a taxon lacks taxon_id, confirm NO broken/empty link renders.
  </how-to-verify>
  <resume-signal>Type "approved" or describe what rendered incorrectly.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| public/data/*.json -> Eleventy templates | Pipeline-produced JSON rendered into static HTML at build time |
| static HTML -> user browser | Outbound iNat anchor; no runtime, no server, no user input in the URL |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-126-07 | Tampering | iNat link href built from taxon_id | mitigate | taxon_id is a build-time integer from the contract-enforced parquet (NOT NULL INTEGER); no user-controlled input enters the URL, so no injection surface |
| T-126-08 | Information Disclosure | rendered taxon_id in static HTML | accept | Public iNaturalist identifier; no PII or secret. Static site is read-only |
| T-126-SC | Tampering | npm/pip/cargo installs | accept | No new packages this phase (RESEARCH Package Legitimacy Audit: N/A); no install task exists |
</threat_model>

<verification>
- `node --input-type=module` smoke run confirms genusList/tribeList carry taxon_id.
- All four templates contain the "View on iNaturalist" anchor with the correct rank variable.
- `npm run build` exits 0.
- Human checkpoint confirms the link renders correctly on species + genus/subgenus/tribe pages and degrades gracefully when taxon_id is null.
</verification>

<success_criteria>
- TID-03: species pages link to https://www.inaturalist.org/taxa/{taxon_id} with the "View on iNaturalist" label (D-05).
- D-06: genus, subgenus, and tribe pages carry the same link using each rank's taxon_id.
- Graceful degradation: no broken link when taxon_id is null.
</success_criteria>

<output>
Create `.planning/phases/126-taxon-ids/126-03-SUMMARY.md` when done.
</output>
