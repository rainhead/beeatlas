---
phase: 126-taxon-ids
plan: 02
type: execute
wave: 2
depends_on: ["126-01"]
files_modified:
  - data/species_export.py
  - data/tests/test_species_export.py
  - data/tests/test_dbt_diff.py
  - CLAUDE.md
autonomous: true
requirements: [TID-01, TID-03]
must_haves:
  truths:
    - "public/data/species.json carries a taxon_id field on every species row (TID-01 surfaced to frontend; TID-03 prerequisite)"
    - "public/data/higher_rank_taxon_ids.json provides genus/subgenus/tribe taxon_ids by name (D-06 prerequisite)"
    - "The CLAUDE.md stale 30-column contract note is corrected to 37 (D-08)"
  artifacts:
    - path: "data/species_export.py"
      provides: "taxon_id in SPECIES_COLUMNS + pyarrow int32 schema; higher-rank taxon_id lookup from taxa.csv.gz (D-06)"
      contains: "taxon_id"
    - path: "public/data/higher_rank_taxon_ids.json"
      provides: "genus/subgenus/tribe → taxon_id lookup for D-06 templates"
    - path: "data/tests/test_species_export.py"
      provides: "Wave-0 assertion that species.json carries taxon_id (TID-03)"
      contains: "taxon_id"
  key_links:
    - from: "data/species_export.py"
      to: "data/raw/taxa.csv.gz"
      via: "read_csv for genus/subgenus/tribe self-row taxon_ids"
      pattern: "taxa.csv.gz"
    - from: "data/species_export.py"
      to: "public/data/species.json"
      via: "SPECIES_COLUMNS includes taxon_id"
      pattern: "taxon_id"
---

<objective>
Surface the mart `taxon_id` into the public JSON the frontend consumes: add `taxon_id` to `species_export.py`'s column list and pyarrow schema so it lands in `public/data/species.json`, and build a `higher_rank_taxon_ids.json` lookup (genus/subgenus/tribe → taxon_id) from `taxa.csv.gz` for the D-06 higher-rank links. Correct the stale CLAUDE.md column-count note and the stale test_dbt_diff docstrings.

Purpose: TID-03 needs `taxon_id` in `species.json` to render the species-page link; D-06 needs higher-rank taxon_ids that `taxon_lineage_extended` cannot supply (it drops `rank` after the pivot — RD-02), so they come from `taxa.csv.gz` directly.
Output: Updated species_export.py (column + pyarrow + lookup step), new public/data/higher_rank_taxon_ids.json, a Wave-0 test_species_export assertion, doc-count corrections.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/126-taxon-ids/126-CONTEXT.md
@.planning/phases/126-taxon-ids/126-RESEARCH.md
@.planning/phases/126-taxon-ids/126-PATTERNS.md
@.planning/phases/126-taxon-ids/126-VALIDATION.md
@CLAUDE.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wave-0 species.json taxon_id assertion</name>
  <files>data/tests/test_species_export.py</files>
  <read_first>
    - data/tests/test_species_export.py (the file being modified; study how it loads/asserts against public/data/species.json and any existing skipif guards)
    - .planning/phases/126-taxon-ids/126-VALIDATION.md (Wave 0 Requirements: test_species_export.py::test_taxon_id)
  </read_first>
  <behavior>
    - test_taxon_id: every species entry in public/data/species.json has a `taxon_id` key whose value is a non-null integer (TID-03 prerequisite). Guard with the file-exists skipif pattern already used in the module so the test skips cleanly before export has run.
    - RED before species_export.py adds the column; GREEN after Task 2.
  </behavior>
  <action>Add a test function (e.g. test_taxon_id) to data/tests/test_species_export.py that loads public/data/species.json and asserts every row contains a non-null integer `taxon_id`. Match the module's existing JSON-load + skipif(file missing) convention; do not invent a new fixture style. Keep it RED until Task 2 lands.</action>
  <verify>
    <automated>uv run --project data pytest data/tests/test_species_export.py --collect-only -q 2>&1 | grep -E "test_taxon_id"</automated>
  </verify>
  <acceptance_criteria>
    - data/tests/test_species_export.py defines a test asserting species.json rows carry a non-null integer taxon_id
    - The test uses the module's existing file-exists skipif convention
    - `pytest --collect-only` lists the new test node ID
  </acceptance_criteria>
  <done>A Wave-0 test for species.json taxon_id is collected by pytest.</done>
</task>

<task type="auto">
  <name>Task 2: Add taxon_id column + higher-rank lookup to species_export.py</name>
  <files>data/species_export.py</files>
  <read_first>
    - data/species_export.py (the file being modified; SPECIES_COLUMNS lines 50-56, pyarrow schema lines 151-173, mart_cols slice line 119, post-write print/assert lines 186-188, existing JSON sidecar writes for species.json/seasonality.json)
    - .planning/phases/126-taxon-ids/126-PATTERNS.md (section "data/species_export.py" — exact insertion points + `_build_higher_rank_taxon_ids` helper)
    - .planning/phases/126-taxon-ids/126-RESEARCH.md (RD-02 complication: taxon_lineage_extended drops rank; query taxa.csv.gz directly; Pitfall 5 subgenus disambiguation)
  </read_first>
  <action>Insert `'taxon_id'` into SPECIES_COLUMNS between `'inat_obs_count'` and `'slug'` (yields 22 entries). Add `('taxon_id', pa.int32())` to the pyarrow schema between `('inat_obs_count', ...)` and `('slug', pa.string())`. The `mart_cols = SPECIES_COLUMNS[:-1]` slice naturally picks up taxon_id (it precedes slug) — confirm no slice-logic change is needed. Add `_build_higher_rank_taxon_ids(con)` that queries `data/raw/taxa.csv.gz` via `read_csv(?, delim=chr(9), header=true, compression='gzip')` filtering `rank IN ('genus','subgenus','tribe') AND active = 'true'`, returning `{"genus": {name: int(tid)}, "subgenus": {...}, "tribe": {...}}`; because the query filters by rank, the subgenus dict holds only subgenus-rank ids (resolves the Bombus genus==subgenus name collision, Pitfall 5). Serialize this dict to `public/data/higher_rank_taxon_ids.json` using the same JSON-sidecar write the file already uses for species.json. Add a post-write print + `assert` for the new JSON following the existing `assert total > 0` pattern. Then run the full export (`uv run python run.py` is heavy — instead invoke just the export entrypoint the file exposes, or run `bash data/dbt/run.sh build` was already done in Plan 01; run the species-export step directly).</action>
  <verify>
    <automated>cd /home/peter/dev/beeatlas/data && uv run python species_export.py && uv run python -c "import json; d=json.load(open('../public/data/species.json')); rows=d if isinstance(d,list) else d.get('species',d); assert all(r.get('taxon_id') is not None for r in rows), 'null taxon_id in species.json'; h=json.load(open('../public/data/higher_rank_taxon_ids.json')); assert h['genus'] and h['tribe'], 'empty higher-rank lookup'; print('export ok')"</automated>
  </verify>
  <acceptance_criteria>
    - SPECIES_COLUMNS contains 'taxon_id' between 'inat_obs_count' and 'slug' (22 entries total)
    - pyarrow schema contains ('taxon_id', pa.int32())
    - species_export.py defines _build_higher_rank_taxon_ids reading taxa.csv.gz with rank IN ('genus','subgenus','tribe') AND active='true'
    - public/data/species.json has a non-null taxon_id on every row
    - public/data/higher_rank_taxon_ids.json exists with non-empty genus and tribe maps
    - `uv run --project data pytest data/tests/test_species_export.py -k taxon_id -x` exits 0
  </acceptance_criteria>
  <done>species.json carries taxon_id; higher_rank_taxon_ids.json is written with rank-disambiguated lookups; the Wave-0 export test is GREEN.</done>
</task>

<task type="auto">
  <name>Task 3: Correct stale doc + test docstrings (D-08, column counts)</name>
  <files>data/tests/test_dbt_diff.py, CLAUDE.md</files>
  <read_first>
    - data/tests/test_dbt_diff.py (the file being modified; test_occurrences_schema_matches docstring ~line 53, test_species_parquet_schema_matches docstring ~line 326 — note the test LOGIC `p_cols[:-1] == s_cols` does NOT hardcode counts, so only docstrings change)
    - CLAUDE.md (the file being modified; the "dbt 30-column contract on `marts/occurrences`" note in the Known State section)
    - .planning/phases/126-taxon-ids/126-RESEARCH.md ("Contract Column Count Corrections" + D-08)
  </read_first>
  <action>In test_dbt_diff.py, update the test_occurrences_schema_matches docstring from "36 cols" to "37 cols" and the test_species_parquet_schema_matches docstring to "21-column"/"22-column" (the actual pre-phase values were 20/21). Do NOT change test logic — `p_cols[:-1] == s_cols` is count-agnostic. In CLAUDE.md, correct the stale "30-column contract on `marts/occurrences`" note in the Known State section to read 37 columns (D-08), keeping the sentence's meaning intact (the contract is enforced at every `bash data/dbt/run.sh build`). Run the full test suite to confirm nothing regressed.</action>
  <verify>
    <automated>cd /home/peter/dev/beeatlas && grep -q '37' CLAUDE.md && ! grep -q '30-column contract on `marts/occurrences`' CLAUDE.md && uv run --project data pytest data/tests/ -x</automated>
  </verify>
  <acceptance_criteria>
    - CLAUDE.md no longer contains "30-column contract on `marts/occurrences`"; the corrected note references 37 columns
    - test_dbt_diff.py docstrings reference 37 (occurrences) and 21/22 (species); no test-logic change
    - `uv run --project data pytest data/tests/ -x` exits 0 (full suite green)
  </acceptance_criteria>
  <done>Stale 30-column note corrected to 37; test docstrings accurate; full suite green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `taxa.csv.gz` → higher_rank_taxon_ids.json | External taxonomy file read into a published JSON lookup |
| species_export.py → public/data/*.json | Pipeline writes public static JSON consumed by the browser |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-126-05 | Tampering | higher_rank_taxon_ids.json built from taxa.csv.gz | mitigate | Filter `active = 'true'` and `rank IN ('genus','subgenus','tribe')`; rank filter resolves the genus==subgenus name collision (Pitfall 5) so the wrong-rank id cannot be emitted |
| T-126-06 | Information Disclosure | taxon_id / higher-rank ids in public JSON | accept | All values are public iNaturalist taxon identifiers; no PII or secret |
| T-126-SC | Tampering | npm/pip/cargo installs | accept | No new packages (RESEARCH Package Legitimacy Audit: N/A); no install task exists |
</threat_model>

<verification>
- `uv run python species_export.py` produces species.json with non-null taxon_id and a non-empty higher_rank_taxon_ids.json.
- `uv run --project data pytest data/tests/ -x` is green (includes the new species-export test and unchanged schema-diff tests).
- CLAUDE.md no longer carries the stale 30-column note.
</verification>

<success_criteria>
- TID-03 prerequisite: public/data/species.json carries a non-null taxon_id per species.
- D-06 prerequisite: public/data/higher_rank_taxon_ids.json provides genus/subgenus/tribe taxon_ids by name.
- D-08: CLAUDE.md column-count note corrected to 37.
</success_criteria>

<output>
Create `.planning/phases/126-taxon-ids/126-02-SUMMARY.md` when done.
</output>
