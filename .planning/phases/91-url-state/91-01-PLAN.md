---
phase: 91
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - src/url-state.ts
  - src/tests/url-state.test.ts
autonomous: true
requirements:
  - SEL-06
user_setup: []
tags:
  - url-state
  - typescript
  - litelement
must_haves:
  truths:
    - "D-02: SelectionState supports a third variant: { type: 'bounds'; west; south; east; north } — buildParams signature unchanged"
    - "buildParams emits sel=west,south,east,north with 4 decimal places when selection.type is 'bounds'"
    - "buildParams does NOT emit o= alongside sel= (mutually exclusive per D-01)"
    - "parseParams returns selection={type:'bounds',...} for a well-formed sel= param"
    - "parseParams silently drops malformed sel= (non-finite, out-of-range, south>=north)"
    - "Existing ids and cluster round-trip tests still pass unchanged"
  artifacts:
    - path: "src/url-state.ts"
      provides: "Extended SelectionState union + buildParams/parseParams support for bounds"
      contains: "type: 'bounds'"
    - path: "src/tests/url-state.test.ts"
      provides: "Round-trip and validation tests for bounds variant"
      contains: "type: 'bounds'"
  key_links:
    - from: "buildParams selection.type==='bounds' branch"
      to: "URLSearchParams sel key"
      via: "params.set('sel', [west,south,east,north].map(toFixed(4)).join(','))"
      pattern: "params\\.set\\('sel'"
    - from: "parseParams"
      to: "selection result"
      via: "p.get('sel') -> split(',') -> 4 finite floats -> range checks -> { type: 'bounds', ... }"
      pattern: "p\\.get\\('sel'\\)"
---

<objective>
Extend the URL state contract in `src/url-state.ts` so a rectangle-selection bounding box can be serialized to and parsed from a `sel=west,south,east,north` query param. This is the foundational data-layer change for Phase 91 — `bee-atlas.ts` wiring in Plan 02 depends on the new `SelectionState` variant existing.

Purpose: SEL-06 — encode rectangle bounds in the URL with 4 decimal places so selections are shareable and survive page refresh.

Output:
- `src/url-state.ts` — new `bounds` variant in `SelectionState`; new `buildParams` branch emitting `sel=`; new `parseParams` block reading `sel=` with validation.
- `src/tests/url-state.test.ts` — round-trip + validation tests covering the new variant, mirroring the existing `cluster` test pattern.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/91-url-state/91-CONTEXT.md
@.planning/phases/91-url-state/91-PATTERNS.md
@.planning/phases/91-url-state/91-UI-SPEC.md
@src/url-state.ts
@src/tests/url-state.test.ts

<interfaces>
<!-- Current SelectionState union (src/url-state.ts:24-26) — to be extended -->
```typescript
export type SelectionState =
  | { type: 'ids'; ids: string[] }
  | { type: 'cluster'; lon: number; lat: number; radiusM: number };
```

<!-- New variant to add: -->
```typescript
  | { type: 'bounds'; west: number; south: number; east: number; north: number }
```

<!-- Existing cluster branch in buildParams (src/url-state.ts:61-63) — mirror this pattern: -->
```typescript
} else if (selection.type === 'cluster') {
  params.set('o', `@${selection.lon.toFixed(4)},${selection.lat.toFixed(4)},${Math.ceil(selection.radiusM)}`);
}
```

<!-- Existing cluster parse block (src/url-state.ts:158-170) — analog for sel= validation: -->
```typescript
const oRaw = p.get('o') ?? '';
if (oRaw.startsWith('@')) {
  const parts = oRaw.slice(1).split(',');
  if (parts.length === 3) {
    const lon = parseFloat(parts[0]!);
    const lat = parseFloat(parts[1]!);
    const radiusM = parseInt(parts[2]!, 10);
    if (isFinite(lon) && lon >= -180 && lon <= 180 &&
        isFinite(lat) && lat >= -90  && lat <= 90  &&
        isFinite(radiusM) && radiusM > 0 && radiusM <= 100000) {
      result.selection = { type: 'cluster', lon, lat, radiusM };
    }
  }
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add failing tests for SelectionState 'bounds' round-trip and validation</name>
  <files>src/tests/url-state.test.ts</files>
  <read_first>
    - src/tests/url-state.test.ts (existing test file — see cluster round-trip block at lines 134-167 as the analog to mirror)
    - src/url-state.ts (existing implementation — to confirm SelectionState shape and import path)
    - .planning/phases/91-url-state/91-PATTERNS.md (canonical test pattern reference, lines 309-319)
    - .planning/phases/91-url-state/91-CONTEXT.md §decisions (D-01, D-02; Claude's Discretion parseParams validation rule)
  </read_first>
  <behavior>
    - Test 1: bounds round-trip — `{ type: 'bounds', west: -122.3456, south: 47.1234, east: -122.1234, north: 47.5678 }` -> `params.get('sel')` returns `"-122.3456,47.1234,-122.1234,47.5678"`; `parseParams` returns the same bounds object.
    - Test 2: bounds with positive longitudes — verify `toFixed(4)` is applied (e.g. `120` becomes `"120.0000"`).
    - Test 3: bounds does NOT emit `o=` — `params.has('o')` is false when selection.type === 'bounds'.
    - Test 4: `sel=` does NOT emit when selection.type !== 'bounds' — sanity check `params.has('sel')` is false for `ids` and `cluster` variants.
    - Test 5: malformed sel — `parseParams('sel=not,four,values')` returns `selection: undefined`.
    - Test 6: out-of-range west — `parseParams('sel=999,47,-120,48')` returns `selection: undefined`.
    - Test 7: out-of-range north — `parseParams('sel=-122,47,-121,999')` returns `selection: undefined`.
    - Test 8: south >= north (degenerate / inverted) — `parseParams('sel=-122,48,-121,47')` returns `selection: undefined`.
    - Test 9: non-finite value — `parseParams('sel=NaN,47,-121,48')` returns `selection: undefined`.
    - Test 10: combined params — building with bounds selection + filter (e.g. `taxon=Bombus`) preserves BOTH on round-trip (SEL-06 success criterion 4 — filter and sel coexist).
  </behavior>
  <action>
    Append a new `describe('bounds selection (SEL-06)', () => { ... })` block to `src/tests/url-state.test.ts` after the existing `describe` covering ids/cluster (after line 168). Mirror the structure of the existing `'cluster centroid encodes as @lon,lat,r (D-06)'` test (lines 134-140). Use the existing `defaultView`, `emptyFilter()`, and `defaultUi` helpers already declared at the top of the file. Reference `SelectionState` from the existing import. Run `npx vitest run src/tests/url-state.test.ts` and confirm the new tests fail with errors mentioning the missing `bounds` variant (TypeScript error or runtime assertion failure both acceptable — this is the RED step).
  </action>
  <verify>
    <automated>npx vitest run src/tests/url-state.test.ts 2>&1 | grep -E "(FAIL|bounds|Test Files)" | head -20</automated>
  </verify>
  <acceptance_criteria>
    - src/tests/url-state.test.ts contains `describe('bounds selection`
    - src/tests/url-state.test.ts contains `type: 'bounds'` (test fixture)
    - src/tests/url-state.test.ts contains `params.get('sel')` (encode assertion)
    - src/tests/url-state.test.ts contains `parseParams('sel=` (parse assertion)
    - src/tests/url-state.test.ts contains `sel=-122,48,-121,47` OR equivalent inverted-bounds case
    - Running `npx vitest run src/tests/url-state.test.ts` produces at least one FAIL on a test in the new describe block (RED state confirmed)
    - Existing tests in url-state.test.ts continue to pass (no regression introduced by the new test fixtures)
  </acceptance_criteria>
  <done>
    New describe block with at least 10 tests added. `npx vitest run src/tests/url-state.test.ts` reports failures inside the new block and no failures elsewhere. Commit: `test(91-01): add failing tests for bounds SelectionState variant`.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement bounds variant in SelectionState, buildParams, parseParams (GREEN)</name>
  <files>src/url-state.ts</files>
  <read_first>
    - src/url-state.ts (full file — to extend in three places)
    - src/tests/url-state.test.ts (RED tests from Task 1 — implementation target)
    - .planning/phases/91-url-state/91-PATTERNS.md §`src/url-state.ts` section (extension specs and code excerpts for the three edit sites)
    - .planning/phases/91-url-state/91-CONTEXT.md §decisions (D-01: sel= only on emit, no o= alongside; D-02: keep buildParams signature unchanged; Discretion: validation rule)
  </read_first>
  <action>
    Apply three edits to `src/url-state.ts`:

    1. Extend the `SelectionState` union (currently at lines 24-26) by adding a third variant: `{ type: 'bounds'; west: number; south: number; east: number; north: number }`. Preserve the existing two variants.

    2. In `buildParams` (selection branch at lines 59-63), add a third branch AFTER the `cluster` branch: when `selection.type === 'bounds'`, call `params.set('sel', ...)` with the four values joined by commas, each formatted via `toFixed(4)`. Do NOT call `params.set('o', ...)` in this branch (D-01: sel and o are mutually exclusive). Field order in the joined string is `west,south,east,north`.

    3. In `parseParams`, add a new validation block. Place it before or after the existing `oRaw` selection block (lines 158-177); position is not load-bearing since `sel=` and `o=` are mutually exclusive per D-01. The new block reads `p.get('sel') ?? ''`, splits on `,`, requires exactly 4 parts, parses each via `parseFloat`, and assigns `result.selection = { type: 'bounds', west, south, east, north }` only when ALL of these validation gates pass:
       - all four values pass `isFinite(...)`
       - `west >= -180 && west <= 180`
       - `east >= -180 && east <= 180`
       - `south >= -90 && south <= 90`
       - `north >= -90 && north <= 90`
       - `south < north` (rejects degenerate and inverted bounds)
       Note: per Discretion, `west < east` is NOT required (antimeridian-crossing bounds are theoretically valid; downstream behavior is governed by `queryOccurrencesByBounds`).

    Do not modify the `oRaw` handling, the view/filter/UI handling, or any unrelated code path. Do not introduce a separate helper function — keep the parse block inline to match the existing in-file style.

    Run `npx vitest run src/tests/url-state.test.ts` after each edit until all tests pass. The implementation should also typecheck cleanly (`npm run build` or `tsc --noEmit`).
  </action>
  <verify>
    <automated>npx vitest run src/tests/url-state.test.ts && npx tsc --noEmit -p tsconfig.json</automated>
  </verify>
  <acceptance_criteria>
    - src/url-state.ts contains `| { type: 'bounds'` in the SelectionState union declaration
    - src/url-state.ts contains `selection.type === 'bounds'` (buildParams branch)
    - src/url-state.ts contains `params.set('sel'` (sel= emit)
    - src/url-state.ts contains `selection.west.toFixed(4)` AND `selection.south.toFixed(4)` AND `selection.east.toFixed(4)` AND `selection.north.toFixed(4)`
    - src/url-state.ts contains `p.get('sel')` (parseParams read)
    - src/url-state.ts contains `south < north` (validation gate)
    - src/url-state.ts contains `result.selection = { type: 'bounds'`
    - `npx vitest run src/tests/url-state.test.ts` reports 0 failures (all tests from Task 1 GREEN)
    - `npx tsc --noEmit -p tsconfig.json` exits 0 (no type errors from the new union variant)
    - No new export added beyond what was already in the file; no signature change to `buildParams` or `parseParams`
  </acceptance_criteria>
  <done>
    All RED tests from Task 1 turn GREEN. Existing ids/cluster tests remain passing. `tsc --noEmit` clean. Commit: `feat(91-01): add bounds variant to SelectionState (SEL-06)`.
  </done>
</task>

</tasks>

<verification>
- `npx vitest run src/tests/url-state.test.ts` — all tests pass including new `describe('bounds selection (SEL-06)', ...)` block
- `npx tsc --noEmit -p tsconfig.json` — exits 0
- `grep -n "type: 'bounds'" src/url-state.ts` — at least 2 hits (union variant + result.selection assignment)
- `grep -c "params.set('sel'" src/url-state.ts` — exactly 1
- `grep -c "p.get('sel')" src/url-state.ts` — exactly 1
- No edits outside `src/url-state.ts` and `src/tests/url-state.test.ts`
</verification>

<success_criteria>
1. SelectionState accepts `{ type: 'bounds'; west; south; east; north }` and the project typechecks.
2. `buildParams({type:'bounds',...})` emits exactly one new param `sel=west,south,east,north` with 4 decimal places, and does not emit `o=`.
3. `parseParams('sel=...')` round-trips bounds when valid and returns `selection: undefined` for any of: missing parts, non-finite floats, lon out of [-180,180], lat out of [-90,90], south >= north.
4. Existing ids/cluster round-trip tests continue to pass.
5. Plan 02 can import the new variant from `src/url-state.ts` and use it in `bee-atlas.ts` without further changes to `url-state.ts`.
</success_criteria>

<output>
After completion, create `.planning/phases/91-url-state/91-01-SUMMARY.md` documenting:
- The exact diff applied to `src/url-state.ts` (3 edit sites)
- The new test count added to `url-state.test.ts`
- Confirmation that `tsc --noEmit` is clean and all tests pass
- Any deviations from the patterns in 91-PATTERNS.md (none expected)
</output>
