---
phase: 07-url-sharing
plan: 05
type: execute
wave: 2
depends_on:
  - 07-03
  - 07-04
files_modified: []
autonomous: false
requirements:
  - NAV-01
gap_closure: true

must_haves:
  truths:
    - "Browser back button navigates between settled map positions (Scenario F)"
    - "Pasting a URL with o=ecdysis:... opens that occurrence's detail panel and keeps o= in the URL bar (Scenario G1)"
    - "Clicking a multi-occurrence cluster encodes all occurrence IDs in the URL (Scenario G2)"
    - "All five previously passing scenarios (A-E) continue to pass"
  artifacts:
    - path: "frontend/src/bee-map.ts"
      provides: "Fixed _onPopState and o= param handling"
      contains: "map.once.*moveend"
  key_links:
    - from: "human verifier"
      to: "browser"
      via: "manual test of Scenarios F and G in dev server"
      pattern: "manual"
---

<objective>
Human browser verification of gap-closure fixes: back button (Scenario F) and o= param (Scenario G).

Purpose: Confirm that the targeted fixes in plans 07-03 and 07-04 resolve both failing scenarios without regressing the five passing scenarios (A-E).
Output: Verified PASS/FAIL results for all 7 scenarios; NAV-01 marked complete if all pass.
</objective>

<execution_context>
@/Users/rainhead/.claude/get-shit-done/workflows/execute-plan.md
@/Users/rainhead/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-url-sharing/07-03-SUMMARY.md
@.planning/phases/07-url-sharing/07-04-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build and serve the frontend</name>
  <files></files>
  <action>
Build the frontend and start the dev server for human verification.

```bash
cd /Users/rainhead/dev/beeatlas && npm --prefix frontend run build 2>&1 | tail -10
```

If the build succeeds, start the dev server:
```bash
cd /Users/rainhead/dev/beeatlas/frontend && npm run dev
```

Report the local URL (typically http://localhost:5173).
  </action>
  <verify>
    <automated>cd /Users/rainhead/dev/beeatlas/frontend && npx tsc --noEmit 2>&1; echo "exit: $?"</automated>
  </verify>
  <done>TypeScript compiles and dev server starts successfully. Local URL reported to human verifier.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human verify Scenarios F and G (and regression check A-E)</name>
  <action>Run `cd /Users/rainhead/dev/beeatlas/frontend && npm run dev` and open http://localhost:5173. Follow the verification sequence in how-to-verify below.</action>
  <done>User types "approved" — all 7 scenarios confirmed passing and NAV-01 is complete.</done>
  <what-built>
    Fix F: _onPopState now uses map.once('moveend') to reset _isRestoringFromHistory AFTER OL fires the moveend from the programmatic view change, preventing the spurious pushState that was canceling back navigation.

    Fix G: Three changes:
    1. Initial replaceState now preserves o= from incoming URL (instead of passing null)
    2. Cluster click now encodes ALL occurrence IDs comma-separated in o=
    3. _restoreSelectedOccurrences looks up all IDs and shows them in sidebar; then calls _pushUrlState() to keep o= in URL
  </what-built>
  <how-to-verify>
Open the dev server URL. Test each scenario in order:

**Regression checks (must still pass):**
- A. Fresh load (no params): Map shows Washington State. URL bar shows ?x=...&y=...&z=...
- B. Pan or zoom: URL bar updates in real time without page reload.
- C. Copy URL, open new tab, paste: Map restores to same position.
- D. Select a taxon in the filter dropdown. Copy URL, open new tab: taxon filter restored.
- E. Enter a year in the "From year" field. Copy URL, open new tab: year filter restored.

**Gap F — Back button:**
1. Load the map fresh. Pan to Seattle area. Wait 1 second (debounce settles).
2. Pan to Spokane area. Wait 1 second.
3. Pan to Mt. Rainier area. Wait 1 second.
4. Press browser back. Map should jump to Spokane position.
5. Press back again. Map should jump to Seattle position.
6. Press forward. Map should return to Spokane.
Expected: Back/forward navigate between settled positions.

**Gap G1 — Paste URL with o= param:**
1. Click any specimen point or cluster on the map. The sidebar shows specimen details.
2. Check that the URL bar now contains `o=ecdysis:...` (may be comma-separated for clusters).
3. Copy the full URL from the URL bar.
4. Open a new tab and paste the URL.
5. Expected: The sidebar opens with the same specimen details. The URL bar still shows `o=ecdysis:...` (not stripped).

**Gap G2 — Multi-occurrence cluster encoding:**
1. Click a cluster that shows multiple specimens in the sidebar (more than one occurrence).
2. Check the URL bar — the `o=` param should contain multiple comma-separated IDs, e.g. `o=ecdysis:123,ecdysis:456`.
3. Copy the URL and open in a new tab.
4. Expected: The sidebar shows all the same specimens.
(Note: if you can only find single-specimen points, that is acceptable — encoding is still correct, just with one ID.)
  </how-to-verify>
  <resume-signal>
Type "approved" if all scenarios pass. Or describe which scenarios failed and what happened (e.g. "F still fails — back button still does nothing" or "G1 passes but G2 cluster URL only shows one occurrence").
  </resume-signal>
</task>

</tasks>

<verification>
All 7 scenarios must pass (A through G) before NAV-01 can be marked complete.
</verification>

<success_criteria>
- Scenarios A-E: no regression (all still pass)
- Scenario F: browser back/forward navigates between settled map views
- Scenario G1: pasting URL with o= opens detail panel and keeps o= in URL bar
- Scenario G2: clicking a multi-occurrence cluster encodes all IDs in o= param
- NAV-01 marked complete in ROADMAP.md
</success_criteria>

<output>
After completion, create `.planning/phases/07-url-sharing/07-05-SUMMARY.md` with:
- Final PASS/FAIL table for all 7 scenarios
- NAV-01 status (complete or still partial)
- Any remaining issues for further gap-closure

If all pass, also update:
- .planning/ROADMAP.md: mark Phase 7 complete, NAV-01 satisfied
- .planning/STATE.md: update status to completed
</output>
