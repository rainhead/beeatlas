---
phase: 16-pipeline-spatial-join
plan: "07"
type: execute
wave: 1
depends_on: []
files_modified: []
autonomous: false
gap_closure: true
requirements:
  - PIPE-05
  - PIPE-06

must_haves:
  truths:
    - "The S3 cache holds ecdysis.parquet and samples.parquet with county and ecoregion_l3 columns"
    - "node scripts/validate-schema.mjs exits 0 (schema validation passes after cache restore)"
    - "The deploy CI workflow passes the validate-schema step on the next push"
  artifacts:
    - path: "frontend/src/assets/ecdysis.parquet"
      provides: "Specimen records with county and ecoregion_l3 columns"
      contains: "county, ecoregion_l3 columns present (not missing)"
    - path: "frontend/src/assets/samples.parquet"
      provides: "iNat sample records with county and ecoregion_l3 columns"
      contains: "county, ecoregion_l3 columns present (not missing)"
  key_links:
    - from: ".github/workflows/fetch-data.yml"
      to: "S3 cache (ecdysis.parquet, samples.parquet)"
      via: "npm run cache-upload after pipeline run"
      pattern: "cache-upload"
    - from: "scripts/cache_restore.sh"
      to: "frontend/src/assets/ecdysis.parquet"
      via: "aws s3 cp + cp to assets"
      pattern: "cp.*ecdysis\\.parquet.*assets"
---

<objective>
Trigger the fetch-data GitHub Actions workflow to regenerate ecdysis.parquet and samples.parquet with the new county and ecoregion_l3 columns, then verify that CI schema validation passes after the cache is refreshed.

Purpose: The Phase 16 pipeline code correctly adds county and ecoregion_l3 columns to both parquet files. However, the S3 cache still holds pre-Phase-16 artifacts. The deploy workflow restores from S3 and then runs validate-schema, which currently exits 1. Once the fetch-data workflow runs end-to-end, it will upload fresh parquets to S3, and subsequent deploys will pass schema validation.

Output: S3 cache updated with fresh parquets; deploy CI passes validate-schema step.
</objective>

<execution_context>
@/Users/rainhead/.claude/get-shit-done/workflows/execute-plan.md
@/Users/rainhead/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

<!-- Gap being closed: Parquet files lack county/ecoregion_l3 columns because the
     S3 cache holds pre-Phase-16 artifacts. The pipeline code is correct (occurrences.py
     and inat/download.py both call add_region_columns). The fix is to run the fetch-data
     workflow which regenerates and uploads fresh parquets to S3. -->

<interfaces>
<!-- .github/workflows/fetch-data.yml structure -->
Trigger: workflow_dispatch (manual) or weekly schedule
Key steps:
  1. Download and process Ecdysis data (ecdysis/download.py + ecdysis/occurrences.py)
  2. Restore S3 cache for incremental iNat fetch (if manual dispatch)
  3. Fetch iNaturalist observations (inat/download.py — adds county/ecoregion_l3)
  4. Restore and fetch links cache
  5. Upload all caches to S3 (npm run cache-upload)

After this workflow completes:
  - S3 cache has ecdysis.parquet with county + ecoregion_l3 columns
  - S3 cache has samples.parquet with county + ecoregion_l3 columns
  - Next deploy run restores these, passes validate-schema, builds successfully

<!-- scripts/validate-schema.mjs -->
Validation command: node scripts/validate-schema.mjs
Exits 0 if all parquet files have expected columns, exits 1 with error message otherwise.
Currently fails: "ecdysis.parquet: missing columns: county, ecoregion_l3"
After cache refresh: should pass (pipeline code is correct)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Trigger fetch-data workflow via GitHub CLI</name>
  <files></files>
  <action>
    Trigger the fetch-data GitHub Actions workflow using the GitHub CLI. This is a manual dispatch (workflow_dispatch) that runs the full data pipeline and uploads fresh parquets to S3.

    Command:
      gh workflow run fetch-data.yml --repo rainhead/beeatlas

    If the repo name is different, check with: gh repo view --json nameWithOwner

    After triggering, get the run ID to monitor progress:
      gh run list --workflow=fetch-data.yml --limit=3

    Note: The workflow takes approximately 10-20 minutes to complete (Ecdysis download is the longest step at ~5 min; iNat incremental fetch is fast). The --force_links_refetch input should NOT be passed (default false is correct — we don't need to re-fetch all links).

    If gh CLI is not authenticated: gh auth status will show the issue. If not authenticated, provide the user with the manual dispatch URL instead:
      https://github.com/rainhead/beeatlas/actions/workflows/fetch-data.yml
  </action>
  <verify>
    <automated>gh run list --workflow=fetch-data.yml --limit=1 --json status,conclusion,url | cat</automated>
  </verify>
  <done>A fetch-data workflow run has been triggered and appears in the GitHub Actions run list with status "queued" or "in_progress".</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Checkpoint: Verify fetch-data workflow completed and schema passes</name>
  <files></files>
  <action>
    Wait for the fetch-data workflow run (triggered in Task 1) to complete. Then verify the S3 cache holds fresh parquets by running cache-restore and validate-schema locally.
  </action>
  <verify>
    <automated>npm run cache-restore && node /Users/rainhead/dev/beeatlas/scripts/validate-schema.mjs</automated>
  </verify>
  <how-to-verify>
    1. Check the workflow run status (wait for "completed / success"):
       gh run list --workflow=fetch-data.yml --limit=3

       Or view in browser: https://github.com/rainhead/beeatlas/actions/workflows/fetch-data.yml
       (takes approximately 10-20 minutes)

    2. Once the workflow shows "completed / success", verify schema validation passes:
       cd /Users/rainhead/dev/beeatlas && npm run cache-restore && node scripts/validate-schema.mjs

       Expected output: "All parquet schemas valid." (exits 0)

       If validate-schema still fails after the workflow succeeded:
       - The workflow upload step may have failed — check the "Upload all caches to S3" step in the workflow logs
       - gh run view --log-failed

    3. If the fetch-data workflow itself failed, common causes:
       - AWS credentials / OIDC role expired
       - iNat API rate limit (re-trigger the workflow)
       - Python import error in occurrences.py or download.py (unlikely — tests pass)
  </how-to-verify>
  <done>node scripts/validate-schema.mjs exits 0 after npm run cache-restore. The S3 cache holds fresh parquets with county and ecoregion_l3 columns.</done>
  <resume-signal>Type "approved" when validate-schema passes, or describe any failures encountered.</resume-signal>
</task>

</tasks>

<verification>
After checkpoint approval:
1. node /Users/rainhead/dev/beeatlas/scripts/validate-schema.mjs — must exit 0 after cache-restore
2. gh run list --workflow=fetch-data.yml --limit=1 — shows "completed / success"
3. No code changes needed — this is a CI/data operation only
</verification>

<success_criteria>
- fetch-data workflow run completed successfully in GitHub Actions
- S3 cache holds fresh ecdysis.parquet and samples.parquet with county and ecoregion_l3 columns
- node scripts/validate-schema.mjs exits 0 after running npm run cache-restore
- The next push to any branch triggers a deploy that passes the validate-schema step
</success_criteria>

<output>
After completion, create `.planning/phases/16-pipeline-spatial-join/16-07-SUMMARY.md`
</output>
