---
phase: 112
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/tests/bee-map.test.ts
  - src/tests/bee-pane.test.ts
  - src/tests/url-state.test.ts
  - src/tests/bee-atlas.test.ts
autonomous: true
requirements:
  - MAP-01
  - MAP-02
  - MAP-03
  - MAP-04
must_haves:
  truths:
    - "Test file src/tests/bee-map.test.ts exists and runs under Vitest"
    - "MAP-01 source-text assertions exist in src/tests/bee-pane.test.ts (toggle label + checklist-layer-changed event)"
    - "MAP-02 source-text assertions exist in src/tests/bee-map.test.ts (checklist-county-fill layer ID, showChecklist/checklistTaxon properties, parquetReadObjects, beforeId ordering)"
    - "MAP-03 source-text assertion exists in src/tests/bee-atlas.test.ts (checklistTaxon bound to _filterState.taxonName on bee-map)"
    - "MAP-04 round-trip assertions exist in src/tests/url-state.test.ts (cl=1 encode/decode, absent when default, combines with bm=counties)"
    - "All four test files compile, run, and fail RED (production code not yet written) — confirming the gates are active and discriminate"
  artifacts:
    - path: "src/tests/bee-map.test.ts"
      provides: "MAP-02 source-text gate for checklist county fill layer"
      contains: "checklist-county-fill"
    - path: "src/tests/bee-pane.test.ts"
      provides: "MAP-01 extension (checklist toggle assertions)"
      contains: "checklist-layer-changed"
    - path: "src/tests/url-state.test.ts"
      provides: "MAP-04 extension (cl=1 round-trip)"
      contains: "cl=1"
    - path: "src/tests/bee-atlas.test.ts"
      provides: "MAP-03 extension (checklistTaxon binding)"
      contains: "checklistTaxon"
  key_links:
    - from: "src/tests/bee-map.test.ts"
      to: "src/bee-map.ts (to be modified in Plan 03)"
      via: "readFileSync source-text match"
      pattern: "readFileSync.*bee-map\\.ts"
    - from: "src/tests/url-state.test.ts MAP-04 describe"
      to: "src/url-state.ts (to be modified in Plan 02)"
      via: "buildParams/parseParams call"
      pattern: "buildParams|parseParams"
---

<objective>
Establish Nyquist Wave 0 RED gates for Phase 112. Create a new test file `src/tests/bee-map.test.ts` containing source-text assertions for MAP-02 (checklist-county-fill layer + checklist properties on bee-map). Extend three existing test files — `src/tests/bee-pane.test.ts` (MAP-01 toggle), `src/tests/url-state.test.ts` (MAP-04 cl= param round-trip), and `src/tests/bee-atlas.test.ts` (MAP-03 checklistTaxon binding) — with new `describe` blocks that will fail until Plans 02 and 03 land.

Purpose: Every behavior gate from the validation map must exist and be RED before any production code is written. This plan is the discriminator — if the gates pass before implementation, they are not really gates.
Output: Four committed test files; `npm test` runs all tests but the new gates fail (RED). No production code modified.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/112-checklist-map-layer/112-RESEARCH.md
@.planning/phases/112-checklist-map-layer/112-PATTERNS.md
@.planning/phases/112-checklist-map-layer/112-VALIDATION.md
@.planning/phases/112-checklist-map-layer/112-UI-SPEC.md

<interfaces>
<!-- Existing test infrastructure that the new tests will mirror. -->

From src/tests/url-state.test.ts (line 24):
```typescript
const defaultUi = { boundaryMode: 'off' as const, paneState: 'collapsed' as const };
// defaultUi works unchanged after Plan 02 adds optional checklistVisible to UiState
```

From src/tests/bee-pane.test.ts header (source-text pattern):
```typescript
import { readFileSync } from 'node:fs';
import { resolve, dirname } from 'node:path';
import { fileURLToPath } from 'node:url';
const __dirname = dirname(fileURLToPath(import.meta.url));
const src = readFileSync(resolve(__dirname, '../bee-pane.ts'), 'utf-8');
```

From src/tests/bee-atlas.test.ts BOUNDARY-01 (lines 187-208): describe-block pattern that does `readFileSync(resolve(__dirname, '../bee-map.ts'), 'utf-8')` and uses `expect(src).toMatch(/.../)` — copy this exact pattern for the new `src/tests/bee-map.test.ts`.

From src/filter.ts: `FilterState.taxonName: string | null` and `FilterState.taxonRank: 'family' | 'genus' | 'species' | null` — `checklistTaxon` will be bound to `_filterState.taxonName` from bee-atlas.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create src/tests/bee-map.test.ts with MAP-02 source-text gates</name>
  <files>src/tests/bee-map.test.ts</files>
  <read_first>
    - src/tests/bee-atlas.test.ts (lines 1-65 for imports/header pattern; lines 187-208 for BOUNDARY-01 describe block — the closest analog because it does `readFileSync('../bee-map.ts')` source-text assertions)
    - src/tests/bee-pane.test.ts (header lines 1-30 for the canonical source-text test header)
    - src/bee-map.ts (current state — confirm `checklist-county-fill`, `showChecklist`, `checklistTaxon`, `_checklistGeneration`, and `parquetReadObjects` are NOT yet present; tests must fail RED)
    - .planning/phases/112-checklist-map-layer/112-PATTERNS.md (lines 471-533 for the exact MAP-02 test block to write)
  </read_first>
  <behavior>
    - File `src/tests/bee-map.test.ts` exists, is parseable by Vitest, and contains one describe block `'MAP-02: checklist county fill layer'`.
    - The describe contains 7 tests asserting source-text presence on `src/bee-map.ts`:
      1. addLayer for `'checklist-county-fill'` (matches `/['"]checklist-county-fill['"]/`)
      2. declares `showChecklist` identifier (matches `/showChecklist/`)
      3. declares `checklistTaxon` identifier (matches `/checklistTaxon/`)
      4. beforeId ordering: `'ghost-points'` string appears in the source AFTER the first `'checklist-county-fill'` occurrence (uses indexOf comparison — see PATTERNS.md lines 513-519)
      5. imports/uses `parquetReadObjects` (matches `/parquetReadObjects/`)
      6. has `_checklistGeneration` counter field (matches `/_checklistGeneration/`)
      7. calls `resolveDataUrl('checklist')` (matches `/resolveDataUrl\s*\(\s*['"]checklist['"]/`)
      8. uses `setLayoutProperty` to toggle the checklist layer (matches `/setLayoutProperty[\s\S]{0,100}checklist-county-fill/`)
    - When run against the CURRENT `src/bee-map.ts` (no Phase 112 changes), all 8 tests FAIL — confirming RED state.
  </behavior>
  <action>
    Create file `src/tests/bee-map.test.ts`. Use the exact header pattern from `src/tests/bee-pane.test.ts` (lines 1-30): import `test`, `expect`, `describe` from `vitest`; import `readFileSync` from `'node:fs'`; import `resolve`, `dirname` from `'node:path'`; import `fileURLToPath` from `'node:url'`; derive `__dirname` via `dirname(fileURLToPath(import.meta.url))`; load `const src = readFileSync(resolve(__dirname, '../bee-map.ts'), 'utf-8')` at module top.

    Then write a single `describe('MAP-02: checklist county fill layer', () => { ... })` block containing the 8 source-text assertions enumerated in `<behavior>` above. Use the exact regex literals and string identifiers given there. For the `beforeId` ordering test, follow PATTERNS.md lines 513-519: `const checklistIdx = src.indexOf("'checklist-county-fill'"); const ghostBeforeIdIdx = src.indexOf("'ghost-points'", checklistIdx); expect(ghostBeforeIdIdx).toBeGreaterThan(checklistIdx);`.

    Do NOT import `bee-map.ts` — these are source-text-only assertions, no Lit component mounting and no mocks needed.
  </action>
  <verify>
    <automated>cd /Users/rainhead/dev/beeatlas &amp;&amp; npm test -- src/tests/bee-map.test.ts 2>&amp;1 | grep -E "MAP-02|FAIL|fail|PASS" | head -30</automated>
  </verify>
  <acceptance_criteria>
    - File exists at `src/tests/bee-map.test.ts`.
    - `npm test -- src/tests/bee-map.test.ts` runs (does not error with "Cannot find module" or syntax error).
    - The describe block `'MAP-02: checklist county fill layer'` is collected by Vitest.
    - All 8 tests are RED (fail) — none of the asserted identifiers exist in `src/bee-map.ts` yet. Specifically `grep -c "checklist-county-fill" src/bee-map.ts` returns `0` and `grep -c "parquetReadObjects" src/bee-map.ts` returns `0` (ignoring comments).
    - No production code (`src/bee-map.ts`) was modified.
  </acceptance_criteria>
  <done>New test file committed; running it produces 8 failing assertions; bee-map.ts is unchanged.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Extend src/tests/bee-pane.test.ts (MAP-01) and src/tests/bee-atlas.test.ts (MAP-03)</name>
  <files>src/tests/bee-pane.test.ts, src/tests/bee-atlas.test.ts</files>
  <read_first>
    - src/tests/bee-pane.test.ts (full file — see PANE-05 describe block at lines 148-219 for the source-text describe pattern to mirror)
    - src/tests/bee-atlas.test.ts (lines 187-208 for BOUNDARY-01 describe block; lines 486-530 for PMAP-02/04 place filter wiring describe — closest analog for `_filterState.taxonName` binding assertion)
    - src/bee-pane.ts (current state — confirm `_showChecklist`, `_renderShow`, `checklist-layer-changed` are NOT present)
    - src/bee-atlas.ts (current state — confirm `_checklistVisible`, `checklistTaxon`, `_onChecklistLayerChanged` are NOT present)
    - .planning/phases/112-checklist-map-layer/112-PATTERNS.md (lines 536-624 for the exact MAP-01 and MAP-04 test blocks)
  </read_first>
  <behavior>
    For `src/tests/bee-pane.test.ts`, append one new describe block `'MAP-01: checklist toggle in filter panel'` containing 6 tests asserting on the source-text of `src/bee-pane.ts`:
      1. has `_showChecklist` field (matches `/_showChecklist/`)
      2. defines `_renderShow` method (matches `/_renderShow\s*\(/`)
      3. renders `Checklist records` label (matches `/Checklist records/`)
      4. dispatches `'checklist-layer-changed'` event (matches `/new CustomEvent\(['"]checklist-layer-changed['"]/`)
      5. uses `aria-label="Show checklist county records on map"` on the checkbox (matches `/aria-label=["']Show checklist county records on map["']/`)
      6. `_renderListContent` body contains a call to `this._renderShow()` (use the regex from PATTERNS.md line 562-564 to extract `_renderListContent` body and assert it includes `this._renderShow()`)

    For `src/tests/bee-atlas.test.ts`, append one new describe block `'MAP-03: checklist taxon filter binding'` containing 4 tests asserting on the source-text of `src/bee-atlas.ts`:
      1. has `_checklistVisible` @state field (matches `/_checklistVisible/`)
      2. has `_onChecklistLayerChanged` method (matches `/_onChecklistLayerChanged\s*\(/`)
      3. binds `.checklistTaxon=${this._filterState.taxonName}` on the `<bee-map>` element (matches `/\.checklistTaxon=\$\{this\._filterState\.taxonName\}/`)
      4. registers `@checklist-layer-changed=${this._onChecklistLayerChanged}` on the `<bee-pane>` element (matches `/@checklist-layer-changed=\$\{this\._onChecklistLayerChanged\}/`)

    When run against current (unchanged) source, all 10 new tests FAIL.
  </behavior>
  <action>
    Edit `src/tests/bee-pane.test.ts`: append the new `describe('MAP-01: checklist toggle in filter panel', () => { ... })` block at the end of the file (after the last existing describe). Use the existing module-top `const src = readFileSync(resolve(__dirname, '../bee-pane.ts'), 'utf-8')` — do not re-declare. Use the exact regex literals listed in `<behavior>`.

    Edit `src/tests/bee-atlas.test.ts`: append the new `describe('MAP-03: checklist taxon filter binding', () => { ... })` block at the end of the file. Reuse the existing source-text variable for `bee-atlas.ts` (if present) or read both `bee-atlas.ts` and `bee-map.ts` source as needed. Match the regex literals listed in `<behavior>` exactly — escape the `$` and `.` in the property-binding regex per JS regex rules.

    Do NOT modify production code (`src/bee-pane.ts`, `src/bee-atlas.ts`).
  </action>
  <verify>
    <automated>cd /Users/rainhead/dev/beeatlas &amp;&amp; npm test -- src/tests/bee-pane.test.ts src/tests/bee-atlas.test.ts 2>&amp;1 | grep -E "MAP-01|MAP-03|FAIL|fail" | head -40</automated>
  </verify>
  <acceptance_criteria>
    - `src/tests/bee-pane.test.ts` contains the literal string `'MAP-01: checklist toggle in filter panel'`.
    - `src/tests/bee-atlas.test.ts` contains the literal string `'MAP-03: checklist taxon filter binding'`.
    - Running `npm test -- src/tests/bee-pane.test.ts` shows the MAP-01 describe with 6 FAILING tests.
    - Running `npm test -- src/tests/bee-atlas.test.ts` shows the MAP-03 describe with 4 FAILING tests.
    - Pre-existing tests in both files continue to PASS (no regression from the edits).
    - `grep -v '^[[:space:]]*//' src/bee-pane.ts | grep -c "_showChecklist"` returns `0` (production unchanged).
    - `grep -v '^[[:space:]]*//' src/bee-atlas.ts | grep -c "_checklistVisible"` returns `0` (production unchanged).
  </acceptance_criteria>
  <done>Two test files extended with 10 new failing assertions across MAP-01 and MAP-03; pre-existing tests unaffected; production source unchanged.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Extend src/tests/url-state.test.ts with MAP-04 cl=1 round-trip tests</name>
  <files>src/tests/url-state.test.ts</files>
  <read_first>
    - src/tests/url-state.test.ts (full file — lines 24, 79-101 for `defaultUi`, `boundaryMode=counties`, and `boundaryMode=off (default)` patterns; these are the exact analogs for the new cl= tests)
    - src/url-state.ts (current state — confirm `checklistVisible` and `'cl'` param are NOT present)
    - .planning/phases/112-checklist-map-layer/112-PATTERNS.md (lines 580-624 for the exact MAP-04 test block)
  </read_first>
  <behavior>
    Append one new describe block `'MAP-04: checklist layer URL param (cl=1)'` to `src/tests/url-state.test.ts` containing 6 tests using the existing `defaultUi`, `defaultView`, `defaultSelection`, `emptyFilter()`, `buildParams`, `parseParams` helpers:
      1. `checklistVisible: true` causes `params.get('cl')` to equal `'1'`
      2. `defaultUi` (no checklistVisible) causes `params.has('cl')` to be `false`
      3. Parsing `'cl=1'` yields `result.ui?.checklistVisible === true`
      4. Parsing `'bm=counties'` (no cl) yields `(result.ui?.checklistVisible ?? false) === false`
      5. Combined `cl=1 + bm=counties` round-trips both `boundaryMode === 'counties'` AND `checklistVisible === true`
      6. Parsing `'cl=0'` (any value other than `'1'`) yields `checklistVisible` falsy

    When run against the current `src/url-state.ts` (no `cl` param support), tests 1, 3, 5, 6 either fail outright OR fail TypeScript compilation. Since the test uses inline `as const` ui objects with the new `checklistVisible: true` field, the test file must be written so TypeScript still compiles even before Plan 02 adds the optional `checklistVisible?: boolean` field to `UiState`. Cast literals with `as UiState` after a `// @ts-expect-error` only if necessary — preferred path: write the test exactly per PATTERNS.md (which assumes `checklistVisible?: boolean` will be optional) and accept that the test file may fail to compile cleanly until Plan 02 lands. If TypeScript blocks Vitest from running the file, add a single `// @ts-expect-error — MAP-04 gate, satisfied by Plan 02 url-state.ts extension` comment immediately above each line that constructs a UI object with the new field.
  </behavior>
  <action>
    Append the new describe block to the end of `src/tests/url-state.test.ts`. Reuse the existing imports and helpers — do not duplicate them. The block must contain exactly the 6 tests enumerated in `<behavior>` using the regex/literals from PATTERNS.md lines 595-622.

    If Vitest cannot compile the file because `UiState` does not yet include `checklistVisible`, prepend `// @ts-expect-error — MAP-04 gate, satisfied by Plan 02 UiState extension` to the offending UI literal lines. Do NOT extend `UiState` in this plan — that is Plan 02's job.

    Do NOT modify `src/url-state.ts`.
  </action>
  <verify>
    <automated>cd /Users/rainhead/dev/beeatlas &amp;&amp; npm test -- src/tests/url-state.test.ts 2>&amp;1 | grep -E "MAP-04|cl=1|FAIL|fail|PASS" | head -30</automated>
  </verify>
  <acceptance_criteria>
    - `src/tests/url-state.test.ts` contains the literal string `'MAP-04: checklist layer URL param (cl=1)'`.
    - `npm test -- src/tests/url-state.test.ts` runs without TypeScript collection error (file is loaded by Vitest).
    - At least 4 of the 6 new MAP-04 tests are RED (tests 1, 3, 5, 6 require `cl` param support which does not exist yet).
    - Pre-existing url-state tests continue to PASS — no regression.
    - `grep -c "'cl'" src/url-state.ts` returns `0` (production unchanged); `grep -c "checklistVisible" src/url-state.ts` returns `0`.
  </acceptance_criteria>
  <done>url-state.test.ts extended with the MAP-04 describe block; Vitest collects it; at least 4 tests fail RED; src/url-state.ts is unchanged.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none added in this plan) | This plan only creates test files; no new runtime trust boundaries introduced |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-112-01-SC | Tampering | npm/yarn/pnpm install | accept | No new packages installed in this plan; existing `vitest` and `hyparquet` already audited in prior phases. No package-manager install tasks. |
| T-112-01-01 | Tampering | Test source files | accept | Test files are read-only assertions against source text; cannot mutate production behavior. Tests committed to git provide tamper-evident history. |
</threat_model>

<verification>
- `npm test` runs successfully (test runner does not crash); the four touched files are collected.
- Each new describe block has the expected number of FAILING tests (8 + 6 + 4 + 6 = 24 new failures expected).
- No production source files modified: `git diff --stat src/bee-map.ts src/bee-pane.ts src/bee-atlas.ts src/url-state.ts src/manifest.ts` shows zero changes.
- All pre-existing tests still pass (no regression).
</verification>

<success_criteria>
- 4 test files committed (1 new, 3 extended).
- All 4 phase requirements (MAP-01..04) have at least one RED automated assertion gate.
- `npm test` exits non-zero ONLY because the new gates fail (no syntax/collection errors, no pre-existing test breakage).
- Production source code (src/*.ts excluding tests) is byte-identical to the pre-plan state.
</success_criteria>

<output>
Create `.planning/phases/112-checklist-map-layer/112-01-SUMMARY.md` when done with: files added/modified, new test counts, count of newly RED tests, confirmation that production sources are untouched, and a note that Plans 02 and 03 will turn these gates GREEN.
</output>
