---
phase: 112
plan: 03
type: execute
wave: 3
depends_on:
  - 112-01
  - 112-02
files_modified:
  - src/bee-pane.ts
  - src/bee-atlas.ts
  - src/bee-map.ts
autonomous: false
requirements:
  - MAP-01
  - MAP-02
  - MAP-03
  - MAP-04
must_haves:
  truths:
    - "A 'Checklist records' toggle appears as a row in the bee-pane filter panel below the When row, with a layers SVG icon"
    - "Toggling the checkbox dispatches a 'checklist-layer-changed' CustomEvent (bubbles, composed) with detail.visible matching the new state"
    - "bee-atlas owns _checklistVisible state and updates _replaceUrlState (writing cl=1 / removing cl) when the event fires"
    - "bee-atlas binds .showChecklist=_checklistVisible and .checklistTaxon=_filterState.taxonName on <bee-map>"
    - "bee-map declares showChecklist and checklistTaxon @property fields and reacts in updated() when either changes"
    - "bee-map adds a Mapbox fill layer with id 'checklist-county-fill' on the 'counties' source, with paint fill-color rgba(44,122,44,0.25) and fill-outline-color rgba(44,122,44,0.7), added BEFORE 'ghost-points' via beforeId"
    - "When showChecklist becomes true, bee-map fetches checklist.parquet via resolveDataUrl('checklist') + parquetReadObjects with columns ['county','scientificName','family','genus'], caches rows, and applies a Mapbox setFilter([\"in\", [\"get\", \"NAME\"], [\"literal\", counties]]) on the layer"
    - "Taxon filter changes (year/month/collector filters do NOT) re-filter the cached checklist rows and update the layer filter — locked decision from STATE.md is honored"
    - "A _checklistGeneration counter guards against stale async fetch results overwriting newer ones"
    - "On page load with cl=1 in URL, the toggle is pre-checked AND the checklist layer is visible"
    - "All 18 RED tests from Plan 01 (MAP-01, MAP-02, MAP-03; MAP-04 already greened by Plan 02) turn GREEN"
  artifacts:
    - path: "src/bee-pane.ts"
      provides: "_showChecklist @state, _renderShow() method, _onChecklistChange handler, checklist-layer-changed event"
      contains: "_renderShow"
    - path: "src/bee-atlas.ts"
      provides: "_checklistVisible @state, _onChecklistLayerChanged, .showChecklist/.checklistTaxon property bindings on bee-map, URL state restore on firstUpdated and _onPopState"
      contains: "_checklistVisible"
    - path: "src/bee-map.ts"
      provides: "showChecklist + checklistTaxon @property, addLayer('checklist-county-fill') beforeId 'ghost-points', _loadChecklistData with _checklistAllRows cache + _checklistGeneration guard, _applyChecklistLayer/_applyChecklistVisibility/_applyChecklistFilter"
      contains: "checklist-county-fill"
  key_links:
    - from: "bee-pane.ts checkbox @change"
      to: "bee-atlas.ts _onChecklistLayerChanged"
      via: "CustomEvent('checklist-layer-changed')"
      pattern: "checklist-layer-changed"
    - from: "bee-atlas.ts <bee-map> binding"
      to: "bee-map.ts showChecklist + checklistTaxon @property"
      via: "Lit property binding"
      pattern: "\\.showChecklist=|\\.checklistTaxon="
    - from: "bee-map.ts _loadChecklistData"
      to: "manifest.ts resolveDataUrl('checklist') → checklist.parquet"
      via: "fetch + parquetReadObjects"
      pattern: "parquetReadObjects"
    - from: "bee-map.ts _applyChecklistFilter"
      to: "Mapbox 'checklist-county-fill' layer"
      via: "setFilter(['in', ['get', 'NAME'], ['literal', counties]])"
      pattern: "setFilter[\\s\\S]{0,100}checklist-county-fill|checklist-county-fill[\\s\\S]{0,100}setFilter"
    - from: "URL ?cl=1"
      to: "bee-atlas.ts _checklistVisible"
      via: "parseParams → firstUpdated + _onPopState restore"
      pattern: "parsed\\.ui\\?\\.checklistVisible"
---

<objective>
Land the user-visible behavior: add the "Checklist records" toggle to bee-pane, coordinate state through bee-atlas (including URL restore from cl=1), and add the Mapbox county-fill layer in bee-map with checklist.parquet fetch + taxon filtering. After this plan, all four phase requirements (MAP-01..04) are satisfied end-to-end with the gates from Plan 01 turning GREEN.

Purpose: This is the deliverable phase work — the toggle, the layer, the data, the URL persistence. STATE.md locked decisions are honored: county-fill (not clustered points) on the existing counties GeoJSON source; checklist responds to taxon filter ONLY (not year/month/collector).
Output: Three TypeScript files modified; all MAP-* tests GREEN; manual checkpoint confirms visual appearance in browser.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/112-checklist-map-layer/112-RESEARCH.md
@.planning/phases/112-checklist-map-layer/112-PATTERNS.md
@.planning/phases/112-checklist-map-layer/112-UI-SPEC.md
@.planning/phases/112-checklist-map-layer/112-VALIDATION.md
@.planning/phases/112-checklist-map-layer/112-01-PLAN.md
@.planning/phases/112-checklist-map-layer/112-02-PLAN.md

<interfaces>
<!-- Interfaces this plan consumes from earlier plans / existing code. -->

From src/url-state.ts (extended by Plan 02):
```typescript
export interface UiState {
  boundaryMode: 'off' | 'counties' | 'ecoregions' | 'places';
  paneState: 'list' | 'table' | 'collapsed';
  checklistVisible?: boolean;     // optional; absent === false
}
```

From src/manifest.ts (extended by Plan 02): `resolveDataUrl('checklist')` returns `Promise<string | null>`.

From src/filter.ts (existing):
```typescript
export interface FilterState {
  taxonName: string | null;
  taxonRank: 'family' | 'genus' | 'species' | null;
  // ... other fields
}
```

From src/sqlite.ts (existing — pattern to mirror for parquetReadObjects):
```typescript
import { parquetReadObjects } from 'hyparquet';
const resp = await fetch(url);
const buffer = await resp.arrayBuffer();
const file = { byteLength: buffer.byteLength, slice: (s: number, e: number) => buffer.slice(s, e) };
const rows = await parquetReadObjects({ file });
```

From src/bee-atlas.ts (existing): `_filterState: FilterState` is the live filter, `_replaceUrlState()` writes URL via `buildParams`, `_onBoundaryModeChanged` is the canonical analog for `_onChecklistLayerChanged`, `_buildCurrentParams()` constructs the UiState argument to buildParams, `firstUpdated()` and `_onPopState()` restore state from URL.

From src/bee-map.ts (existing): `counties` Mapbox source is added in the `_map.on('load')` callback with `generateId: true`; features have `NAME` property; `ghost-points` is the FIRST occurrence-layer added — it is the correct `beforeId` to keep checklist fill BELOW all specimen dots. `_applyBoundaryMode()` is the canonical analog for `_applyChecklistVisibility()`.

From .planning/phases/112-checklist-map-layer/112-RESEARCH.md "Data Facts": checklist.parquet has `scientificName` (Title Case) which matches `filterState.taxonName` directly when `taxonRank === 'species'`. For `taxonRank === 'genus'` filter on `genus` column; for `'family'` filter on `family` column.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add checklist toggle UI to src/bee-pane.ts (MAP-01)</name>
  <files>src/bee-pane.ts</files>
  <read_first>
    - src/bee-pane.ts (full file — focus on the @state block around lines 100-110, the `_renderWhen()` method around lines 1034-1063 which is the EXACT template to mirror, `_emitFilter` / `_onToggle` event-dispatch patterns around lines 550-578, and `_renderListContent()` around lines 1065-1105 where _renderShow must be invoked)
    - src/tests/bee-pane.test.ts (the MAP-01 describe block added in Plan 01 — these regexes define the contract this task must satisfy)
    - .planning/phases/112-checklist-map-layer/112-PATTERNS.md (lines 27-109 for the exact code pattern; the `<polygon>` and `<polyline>` SVG layers icon points)
    - .planning/phases/112-checklist-map-layer/112-UI-SPEC.md (Component Inventory and Icon sections — copy verbatim copy "Checklist records", aria-label "Show checklist county records on map", and the 16x16 layers SVG)
  </read_first>
  <behavior>
    - A new private `@state() private _showChecklist = false;` field exists in the same @state block as `_yearThisYear`, `_yearLastYear`, `_yearEarlier`.
    - A new private method `_renderShow()` exists. It returns a Lit html template with the EXACT structure of `_renderWhen()` minus the year checkboxes:
      • Outer `<div class="filter-row">`
      • A 16x16 SVG `<svg class="row-icon" ... fill="none" stroke="currentColor" stroke-width="1.5" aria-hidden="true">` containing a `<polygon points="8,2 14,5.5 8,9 2,5.5"/>` and a `<polyline points="2,8.5 8,12 14,8.5"/>` (the layers icon from UI-SPEC.md)
      • Inner `<div class="year-row">` containing a single `<label class="year-label">` with an `<input type="checkbox" .checked=${this._showChecklist} aria-label="Show checklist county records on map" @change=${this._onChecklistChange} />` and the literal text `Checklist records`.
    - A new private handler `_onChecklistChange(e: Event)` reads `(e.target as HTMLInputElement).checked`, assigns it to `this._showChecklist`, and dispatches `new CustomEvent('checklist-layer-changed', { bubbles: true, composed: true, detail: { visible } })`.
    - `_renderListContent()` invokes `${this._renderShow()}` AFTER `${this._renderWhen()}` and within the same `.filter-panel` div.
    - No new CSS — existing `.filter-row`, `.year-row`, `.year-label`, `.row-icon`, and `input[type="checkbox"] { accent-color: var(--accent) }` rules cover the new element.
    - Year/month checkbox state and event flow are NOT modified — checklist toggle is independent of When-row state.
  </behavior>
  <action>
    Edit `src/bee-pane.ts`:

    1. In the @state declaration block that already contains `_yearThisYear`, add a new line `@state() private _showChecklist = false;`.

    2. Add a new private method `_renderShow()` immediately after `_renderWhen()` in the file. Use the Lit `html` tagged template literal. The template structure is described in `<behavior>` above and shown verbatim in PATTERNS.md lines 63-83. Copy that block. The two SVG primitives MUST have these exact point lists: `<polygon points="8,2 14,5.5 8,9 2,5.5"/>` and `<polyline points="2,8.5 8,12 14,8.5"/>`. The label text is the literal string `Checklist records`. The aria-label is the literal string `Show checklist county records on map`.

    3. Add a new private handler `_onChecklistChange(e: Event)` near the other `_on*` handlers. Its body: `const visible = (e.target as HTMLInputElement).checked; this._showChecklist = visible; this.dispatchEvent(new CustomEvent('checklist-layer-changed', { bubbles: true, composed: true, detail: { visible } }));`. The event name string is the literal `'checklist-layer-changed'`.

    4. In `_renderListContent()` (around line 1065), find the line that calls `${this._renderWhen()}` and add `${this._renderShow()}` on the next line, inside the same parent template block.

    Do NOT modify any existing render method, existing event handler, existing @state field, or any CSS. Do NOT touch year/month/collector logic. Do NOT add new CSS.
  </action>
  <verify>
    <automated>cd /Users/rainhead/dev/beeatlas &amp;&amp; npm test -- src/tests/bee-pane.test.ts 2>&amp;1 | grep -E "MAP-01|PANE-|passed|failed" | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "_showChecklist" src/bee-pane.ts` returns ≥ 3 (declaration + render check + handler assignment).
    - `grep -c "_renderShow" src/bee-pane.ts` returns ≥ 2 (definition + invocation in _renderListContent).
    - `grep -c "Checklist records" src/bee-pane.ts` returns ≥ 1 (the label text).
    - `grep -c "checklist-layer-changed" src/bee-pane.ts` returns ≥ 1 (the CustomEvent name).
    - `grep -c "Show checklist county records on map" src/bee-pane.ts` returns 1 (the aria-label).
    - All 6 MAP-01 tests in `src/tests/bee-pane.test.ts` PASS (GREEN).
    - All pre-existing bee-pane tests (PANE-01 through PANE-05+, others) still PASS — no regression.
    - `npm run typecheck` exits 0.
  </acceptance_criteria>
  <done>Toggle UI implemented per UI-SPEC.md; event dispatched; MAP-01 gates green; no regression.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wire bee-atlas state, event handler, property binding, and URL restore (MAP-03 + MAP-04 wiring)</name>
  <files>src/bee-atlas.ts</files>
  <read_first>
    - src/bee-atlas.ts (full file — focus on @state block at lines 19-58, the `<bee-map>` and `<bee-pane>` render bindings at lines 155-205, `_onBoundaryModeChanged` at lines 957-971 — the canonical analog handler, `_buildCurrentParams()` at lines 521-532, `firstUpdated()` URL restore at lines 221-226, `_onPopState` at lines 581-582)
    - src/tests/bee-atlas.test.ts (the MAP-03 describe block added by Plan 01 — defines the contract; specifically the `\.checklistTaxon=\$\{this\._filterState\.taxonName\}` and `@checklist-layer-changed=\$\{this\._onChecklistLayerChanged\}` regex assertions)
    - .planning/phases/112-checklist-map-layer/112-PATTERNS.md (lines 112-213 for the exact code pattern)
    - src/url-state.ts (post Plan 02 — confirm UiState.checklistVisible? is in place)
  </read_first>
  <behavior>
    - bee-atlas declares `@state() private _checklistVisible = false;` in the same @state block as `_boundaryMode` and `_paneState`.
    - A new private handler `_onChecklistLayerChanged(e: CustomEvent<{ visible: boolean }>)` exists. Body: `this._checklistVisible = e.detail.visible; this._replaceUrlState();`.
    - The `<bee-pane>` element in render() registers `@checklist-layer-changed=${this._onChecklistLayerChanged}`.
    - The `<bee-map>` element in render() binds two new properties: `.showChecklist=${this._checklistVisible}` and `.checklistTaxon=${this._filterState.taxonName}`.
    - `_buildCurrentParams()` passes `checklistVisible: this._checklistVisible` inside the UiState object literal handed to `buildParams`.
    - `firstUpdated()` reads `initialParams.ui?.checklistVisible ?? false` and assigns it to `this._checklistVisible` — placed alongside the existing boundaryMode/paneState restore lines.
    - `_onPopState` similarly reads `parsed.ui?.checklistVisible ?? false` and updates `this._checklistVisible`.
    - When the bee-pane event fires, bee-atlas updates state and immediately calls `_replaceUrlState()` which writes `cl=1` (via the now-extended buildParams). When unchecked, no `cl` param is written.
    - When the user changes the taxon filter, the existing filter event path runs; because Lit re-evaluates `.checklistTaxon=${this._filterState.taxonName}`, bee-map sees a property change and re-filters its layer (handled in Task 3). bee-atlas does NOT need any special hook for taxon-change propagation — Lit's reactive binding handles it automatically.
  </behavior>
  <action>
    Edit `src/bee-atlas.ts`:

    1. Add `@state() private _checklistVisible = false;` to the @state block alongside `_boundaryMode` and `_paneState`.

    2. Add a new private method `_onChecklistLayerChanged(e: CustomEvent<{ visible: boolean }>)` near `_onBoundaryModeChanged`. Body: assigns `e.detail.visible` to `this._checklistVisible` and calls `this._replaceUrlState()`.

    3. In the render() template, locate the `<bee-pane>` element. Add a new attribute `@checklist-layer-changed=${this._onChecklistLayerChanged}` alongside the existing `@filter-changed` / `@pane-expand-list` listeners.

    4. In the same render() template, locate the `<bee-map>` element. Add two new property bindings: `.showChecklist=${this._checklistVisible}` and `.checklistTaxon=${this._filterState.taxonName}`, alongside the existing `.boundaryMode` / `.filterState` / `.visibleIds` bindings.

    5. In `_buildCurrentParams()`, find the UiState object literal currently `{ boundaryMode: this._boundaryMode, paneState: this._paneState }` and extend it to `{ boundaryMode: this._boundaryMode, paneState: this._paneState, checklistVisible: this._checklistVisible }`.

    6. In `firstUpdated()`, alongside `this._boundaryMode = initBoundaryMode; this._paneState = paneState;`, add `this._checklistVisible = initialParams.ui?.checklistVisible ?? false;`.

    7. In `_onPopState`, alongside `this._boundaryMode = parsed.ui?.boundaryMode ?? 'off';`, add `this._checklistVisible = parsed.ui?.checklistVisible ?? false;`.

    Do NOT modify `_filterState`, `_runFilterQuery`, or any existing handler. The checklist toggle is independent of the occurrence filter query and MUST NOT trigger `_runFilterQuery()`.

    Note: The exact regex `\.checklistTaxon=\$\{this\._filterState\.taxonName\}` from the MAP-03 test will only match if the binding is written without intervening whitespace inside the `${...}`. Write the binding compactly as `.checklistTaxon=${this._filterState.taxonName}` to match.
  </action>
  <verify>
    <automated>cd /Users/rainhead/dev/beeatlas &amp;&amp; npm test -- src/tests/bee-atlas.test.ts src/tests/url-state.test.ts 2>&amp;1 | grep -E "MAP-03|MAP-04|BOUNDARY|PANE-|passed|failed" | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "_checklistVisible" src/bee-atlas.ts` returns ≥ 5 (declaration + handler assignment + UiState literal + firstUpdated + _onPopState).
    - `grep -c "_onChecklistLayerChanged" src/bee-atlas.ts` returns ≥ 2 (definition + @ listener).
    - `grep -c "\.checklistTaxon=\${this._filterState.taxonName}" src/bee-atlas.ts` returns 1 (exact substring — note literal `${...}` form).
    - `grep -c "@checklist-layer-changed=\${this._onChecklistLayerChanged}" src/bee-atlas.ts` returns 1.
    - All 4 MAP-03 tests in `src/tests/bee-atlas.test.ts` PASS.
    - MAP-04 round-trip behavior verified end-to-end (toggle event causes URL update — tested by the round-trip tests already green from Plan 02; bee-atlas wiring confirmed by MAP-03 source-text gates).
    - All pre-existing bee-atlas tests PASS — no regression in any describe block (BOUNDARY-01, SIDE-01, VIEW-02, CLICK-01, HALO-01, SEL-*, PMAP-*, SM-01, PANE-01, etc.).
    - `npm run typecheck` exits 0.
  </acceptance_criteria>
  <done>bee-atlas state plumbing complete; URL restore handled; properties passed to bee-map; MAP-03 gates green; no regression.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Add Mapbox layer, checklist fetch, and reactive update logic to src/bee-map.ts (MAP-02)</name>
  <files>src/bee-map.ts</files>
  <read_first>
    - src/bee-map.ts (full file — focus on imports at lines 1-11, @property block at lines 41-60, `updated(changedProperties)` at lines 310-351, the `_map.on('load')` callback containing `counties` source add at line 402 and `ghost-points` layer add around line 566, `_applyBoundaryMode()` at lines 1060-1072 — the canonical analog)
    - src/sqlite.ts (lines 101-104 — the canonical `parquetReadObjects` fetch+slice pattern to mirror)
    - src/tests/bee-map.test.ts (the MAP-02 describe block added by Plan 01 — defines all 8 source-text gates this task must satisfy)
    - .planning/phases/112-checklist-map-layer/112-PATTERNS.md (lines 216-365 for the exact code patterns, especially the addLayer call with beforeId 'ghost-points', _applyChecklistFilter, _loadChecklistData with generation guard, and the parquet column list)
    - .planning/phases/112-checklist-map-layer/112-RESEARCH.md (Data Facts section — confirms `scientificName` (not `canonical_name`) matches `filterState.taxonName`; confirms 39 counties align between checklist.parquet and counties.geojson NAME property)
    - node_modules/hyparquet/types (confirm `parquetReadObjects` accepts `{ file, columns: string[] }` — already used in sqlite.ts)
  </read_first>
  <behavior>
    - Top-of-file imports include `import { parquetReadObjects } from 'hyparquet';` (new import — sqlite.ts already imports from hyparquet, so the package is available).
    - bee-map declares two new `@property({ attribute: false })` fields: `showChecklist = false;` and `checklistTaxon: string | null = null;`. Also declares a `@property({ attribute: false }) checklistTaxonRank: 'family' | 'genus' | 'species' | null = null;` so the rank-aware filter can be applied (the rank is bound from bee-atlas as `.checklistTaxonRank=${this._filterState.taxonRank}` — Task 2 must include this additional binding too; if Task 2 did not add the rank binding, treat all non-null taxon as species and add a TODO note in SUMMARY).
    - bee-map declares three new private fields:
      • `@state() private _checklistCounties: Set<string> = new Set();`
      • `private _checklistAllRows: Array<{ county: string | null; scientificName: string; genus: string; family: string }> = [];`
      • `private _checklistGeneration = 0;`
    - In the `_map.on('load')` callback, immediately BEFORE the existing `addLayer({ id: 'ghost-points', ... })` call, add a new addLayer call with id `'checklist-county-fill'`, type `'fill'`, source `'counties'`, layout `{ visibility: 'none' }`, paint `{ 'fill-color': 'rgba(44, 122, 44, 0.25)', 'fill-outline-color': 'rgba(44, 122, 44, 0.7)' }`, initial filter `['==', 'NAME', '__never__']`, and a second positional argument `'ghost-points'` for beforeId. The string `'ghost-points'` as beforeId must appear AFTER the string `'checklist-county-fill'` in the file (the Plan 01 MAP-02 ordering test relies on this).
    - `updated(changedProperties)` is extended: when either `showChecklist` or `checklistTaxon` (or `checklistTaxonRank`) changes, call `this._applyChecklistLayer()`.
    - `_applyChecklistLayer()` calls `_applyChecklistVisibility()` and, when `showChecklist` is true, calls `void this._loadChecklistData()`.
    - `_applyChecklistVisibility()` guards on `this._map?.getLayer('checklist-county-fill')`, then `setLayoutProperty('checklist-county-fill', 'visibility', this.showChecklist ? 'visible' : 'none')`.
    - `_loadChecklistData()` increments `_checklistGeneration`; if `_checklistAllRows` is empty, fetches `resolveDataUrl('checklist')`, reads the parquet via `parquetReadObjects({ file, columns: ['county', 'scientificName', 'genus', 'family'] })`, and stores into `_checklistAllRows`. After fetch (or if cached), checks `if (generation !== this._checklistGeneration) return` to discard stale results, then filters `_checklistAllRows` by rank:
      • If `checklistTaxonRank === 'species'` → match `r.scientificName === checklistTaxon`
      • If `checklistTaxonRank === 'genus'` → match `r.genus === checklistTaxon`
      • If `checklistTaxonRank === 'family'` → match `r.family === checklistTaxon`
      • If `checklistTaxon` is null or rank is null → no filter (all rows)
      Then builds `this._checklistCounties = new Set(filtered.map(r => r.county).filter(Boolean) as string[])` and calls `_applyChecklistFilter()`.
    - `_applyChecklistFilter()` guards on `getLayer('checklist-county-fill')`, then calls `setFilter('checklist-county-fill', counties.length > 0 ? ['in', ['get', 'NAME'], ['literal', counties]] : ['==', 'NAME', '__never__'])`.
    - Errors (network/parse) are caught and logged via `console.warn('checklist data unavailable:', err)`. The layer stays hidden silently — no toast.
    - `_filterQueryGeneration` is NOT touched by the checklist code path — it uses its own counter.
    - The intentional `speicmenLayer` typo is NOT fixed (CLAUDE.md invariant).
  </behavior>
  <action>
    Edit `src/bee-map.ts`:

    1. Add `import { parquetReadObjects } from 'hyparquet';` to the existing top-of-file import block. Confirm `resolveDataUrl` is already imported from `'./manifest'`; if not, add it.

    2. In the @property block (lines 41-60), add three new property declarations:
       • `@property({ attribute: false }) showChecklist = false;`
       • `@property({ attribute: false }) checklistTaxon: string | null = null;`
       • `@property({ attribute: false }) checklistTaxonRank: 'family' | 'genus' | 'species' | null = null;`

    3. Add three new private fields near other @state declarations:
       • `@state() private _checklistCounties: Set<string> = new Set();`
       • `private _checklistAllRows: Array<{ county: string | null; scientificName: string; genus: string; family: string }> = [];`
       • `private _checklistGeneration = 0;`

    4. In `updated(changedProperties)`, add a new conditional block AFTER the existing `boundaryMode` / `filterState` blocks: `if (changedProperties.has('showChecklist') || changedProperties.has('checklistTaxon') || changedProperties.has('checklistTaxonRank')) { this._applyChecklistLayer(); }`.

    5. In the `_map.on('load')` callback, locate the existing `this._map!.addLayer({ id: 'ghost-points', ... })` call. Immediately BEFORE that call, add a new `addLayer` call with the exact paint, layout, source, type, and filter from `<behavior>` above. Pass `'ghost-points'` as the second positional argument (beforeId). Use the literal RGB values `rgba(44, 122, 44, 0.25)` and `rgba(44, 122, 44, 0.7)` — these are the UI-SPEC.md mandated colors.

    6. Add four new private methods (placement: near `_applyBoundaryMode`):
       • `_applyChecklistLayer()` — calls `_applyChecklistVisibility()` then conditionally `void this._loadChecklistData()` when `this.showChecklist`.
       • `_applyChecklistVisibility()` — getLayer guard + setLayoutProperty.
       • `_applyChecklistFilter()` — getLayer guard + setFilter using the `['in', ['get', 'NAME'], ['literal', counties]]` expression when counties non-empty, else `['==', 'NAME', '__never__']`.
       • `async _loadChecklistData(): Promise<void>` — generation guard pattern from PATTERNS.md lines 322-358; fetch + parquetReadObjects with `columns: ['county', 'scientificName', 'genus', 'family']`; filter by `checklistTaxonRank` per `<behavior>`; sets `_checklistCounties`; calls `_applyChecklistFilter()`. Wrap in try/catch with `console.warn('checklist data unavailable:', err)`.

    7. Task-2 amendment: In `src/bee-atlas.ts`, ALSO add `.checklistTaxonRank=${this._filterState.taxonRank}` to the `<bee-map>` element binding (alongside `.checklistTaxon`). This is a small edit to bee-atlas required by the rank-aware filter. If this conflicts with the Task 2 acceptance criteria, prefer adding the binding here in Task 3 — the binding is one extra line.

    Do NOT fix the `speicmenLayer` typo. Do NOT touch `_filterQueryGeneration`. Do NOT modify any existing layer, source, or event handler.
  </action>
  <verify>
    <automated>cd /Users/rainhead/dev/beeatlas &amp;&amp; npm test 2>&amp;1 | tail -40 &amp;&amp; npm run typecheck 2>&amp;1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "import { parquetReadObjects }" src/bee-map.ts` returns 1.
    - `grep -c "showChecklist" src/bee-map.ts` returns ≥ 3 (property + updated check + visibility apply).
    - `grep -c "checklistTaxon" src/bee-map.ts` returns ≥ 3 (property + updated check + filter use).
    - `grep -c "_checklistGeneration" src/bee-map.ts` returns ≥ 2 (declaration + guard check).
    - `grep -c "'checklist-county-fill'" src/bee-map.ts` returns ≥ 3 (addLayer id + setLayoutProperty + setFilter).
    - `grep -c "resolveDataUrl\\(['\"]checklist['\"]\\)" src/bee-map.ts` returns 1.
    - In the source text, the byte offset of the first `'checklist-county-fill'` literal is LESS THAN the byte offset of the first `'ghost-points'` literal that follows it on the same addLayer call (Plan 01 MAP-02 ordering test enforces this).
    - All 8 MAP-02 tests in `src/tests/bee-map.test.ts` PASS (GREEN).
    - All MAP-01 (bee-pane), MAP-03 (bee-atlas), MAP-04 (url-state) tests still PASS.
    - `npm test` overall: every previously-passing test still passes; the 18 RED gates from Plan 01 (8 MAP-02 + 6 MAP-01 + 4 MAP-03) are all GREEN; MAP-04 gates from Plan 02 stay GREEN.
    - `npm run typecheck` exits 0.
    - `grep -c "speicmenLayer" src/bee-map.ts` is unchanged from pre-plan baseline (typo NOT fixed).
  </acceptance_criteria>
  <done>checklist-county-fill layer added below ghost-points; reactive fetch + filter implemented; generation guard active; all MAP-* gates green; typecheck clean; typo preserved.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking" auto_advance="false">
  <name>Task 4: Browser verification of checklist layer behavior</name>
  <files>(no files modified — manual browser verification only)</files>
  <action>Run `npm run dev` and walk through the 9 verification steps in the `<how-to-verify>` block below. This task does NOT modify code; its purpose is human confirmation that the automated test gates from Tasks 1-3 correspond to the intended visual/interactive behavior. `auto_advance: false` per project memory feedback_uat_ui_phases.md — even in yolo mode, this checkpoint REQUIRES explicit human approval and must not be auto-greened.</action>
  <verify><human-check>9-step browser checklist below; explicit "approved" required.</human-check></verify>
  <done>User responds "approved" after all 9 verification steps pass, OR responds with specific deviation notes that the executor addresses before re-running this checkpoint.</done>
  <what-built>
    - "Checklist records" toggle in the bee-pane filter panel below the When row, with a layers icon
    - Mapbox county-fill layer (semi-transparent green) on the existing counties GeoJSON source, rendered BELOW specimen point dots
    - Toggle off ⇒ layer hidden; toggle on ⇒ layer visible with counties matching the current taxon filter (or all 39 counties when no taxon filter is active)
    - `cl=1` URL param persists across page reloads
    - Taxon filter changes update the visible counties; year/month/collector filter changes do NOT affect the checklist layer
  </what-built>
  <how-to-verify>
    1. Run `npm run dev` and open the printed local URL (typically `http://localhost:5173`).
    2. Open the filter panel (magnifying glass button on the map). Confirm a NEW row appears below the When row with a layers SVG icon and the label "Checklist records" (exact text). The checkbox is unchecked by default.
    3. Check the toggle. Confirm:
       a. A semi-transparent green fill appears on multiple Washington counties.
       b. Existing specimen point dots remain visible on TOP of the green fill (not buried under it).
       c. The URL gains `cl=1` (visible in the address bar).
    4. With the toggle still on, open the What (taxon) filter and select a specific species (e.g. `Andrena aculeata` or any species you know is in the Bartholomew checklist). Confirm:
       a. The green county fill narrows to only the counties listed in the checklist for that species.
       b. If a species has no checklist counties, the fill disappears entirely (no toast, no error).
    5. Clear the taxon filter. Confirm the green fill returns to all checklist counties.
    6. Change the Year or Month filter (When row). Confirm the green county fill is UNCHANGED — checklist responds to taxon only.
    7. Reload the page (full browser reload). Confirm the toggle is pre-checked AND the green fill is visible immediately on load (URL still has `cl=1`).
    8. Uncheck the toggle. Confirm the green fill disappears AND the `cl` param is removed from the URL.
    9. Open DevTools Console. Confirm no JavaScript errors related to checklist, hyparquet, or Mapbox during any of the above steps. (A single `console.warn('checklist data unavailable: ...')` is acceptable only if the parquet legitimately fails to load — should not happen in local dev.)
  </how-to-verify>
  <resume-signal>Type "approved" if all 9 steps behave as described, or describe any deviation (e.g. "step 3b fails — green fill renders on top of specimen dots" or "step 7 fails — toggle not pre-checked on reload"). `auto_advance: false` per project memory feedback_uat_ui_phases.md — this checkpoint requires explicit human confirmation, not yolo-mode auto-approval.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → CloudFront CDN | Existing — checklist.parquet served from same origin as occurrences.parquet (Phase 111). |
| Mapbox filter expression construction | Counties extracted from parquet (server-controlled data); never from user URL input. |
| URL → bee-atlas | Existing — Plan 02 added strict `=== '1'` parsing for `cl`. No new attack surface. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-112-03-SC | Tampering | npm/yarn/pnpm install | accept | No new package installs. `hyparquet` already audited and in production use via sqlite.ts. |
| T-112-03-01 | Tampering | Mapbox setFilter argument | mitigate | County names come from parquet (server-controlled). Mapbox expression syntax `['in', ['get', 'NAME'], ['literal', counties]]` performs strict array membership — non-string input is rejected by Mapbox. No template-string concatenation into expressions. |
| T-112-03-02 | DoS | Repeated taxon filter changes triggering re-fetch | mitigate | `_checklistAllRows` cached on first fetch; subsequent taxon changes filter in JS without re-fetching. `_checklistGeneration` counter discards stale results from rapid changes. |
| T-112-03-03 | Information Disclosure | Console.warn on parquet failure | accept | The warn message is generic (`'checklist data unavailable:'` + err); no credentials or sensitive data. Same posture as existing fetch error paths. |
| T-112-03-04 | Tampering | URL `cl` param | mitigate (inherits Plan 02) | parseParams strict `=== '1'` check; any other value coerces to false. |
</threat_model>

<verification>
- `npm test` exits 0 — full suite green (all 18 new gates plus all pre-existing tests).
- `npm run typecheck` exits 0.
- `npm run build` exits 0 (full Vite + Eleventy production build succeeds — exercises the new code paths at compile time even though runtime is the dev server).
- Browser checkpoint (Task 4) passes all 9 visual/interaction steps.
- `grep -c "speicmenLayer" src/bee-map.ts` is unchanged (typo preserved per CLAUDE.md).
- No file outside `files_modified` is changed.
</verification>

<success_criteria>
- MAP-01: toggle appears in filter panel ✓ (via bee-pane edits + browser verification step 2)
- MAP-02: county-fill layer renders with correct paint, below specimen dots ✓ (via bee-map edits + browser verification step 3)
- MAP-03: taxon filter narrows fill; year/month/collector do not ✓ (via bee-atlas property binding + browser verification steps 4 and 6)
- MAP-04: cl=1 round-trips across reload ✓ (via url-state + bee-atlas restore + browser verification steps 7 and 8)
- All Plan 01 gates GREEN.
- No regression in pre-existing tests (bee-atlas, bee-pane, url-state, others).
- Locked decisions from STATE.md honored: county-fill not points; taxon-only filter.
- CLAUDE.md invariants honored: bee-atlas owns cross-component state; bee-map/bee-pane are presenters; speicmenLayer typo preserved; style cache untouched (checklist is not on the occurrence layer).
</success_criteria>

<output>
Create `.planning/phases/112-checklist-map-layer/112-03-SUMMARY.md` when done with: files modified, diff summary, before/after test counts (RED → GREEN), browser verification outcome (which of the 9 steps passed and any that needed iteration), confirmation of CLAUDE.md invariants, and a note that all four MAP-* requirements are satisfied end-to-end.
</output>
