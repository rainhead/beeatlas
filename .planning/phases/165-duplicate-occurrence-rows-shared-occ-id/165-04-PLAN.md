---
phase: 165-duplicate-occurrence-rows-shared-occ-id
plan: 04
type: execute
wave: 3
depends_on:
  - 165-02
files_modified:
  - docs/domain-model.md
  - CLAUDE.md
autonomous: true
requirements: []
must_haves:
  truths:
    - "docs/domain-model.md exists as a human-first reference (not an ADR) describing the 5 occurrence categories and what real-world thing each represents"
    - "The doc documents the corrected is_provisional definition (project-166376 membership), the occIdFromRow ID-prefix vocabulary + its positional coupling, the 'when are two rows the same occurrence' rule, and the known pipeline-lag state"
    - "CLAUDE.md links to docs/domain-model.md from the Domain Vocabulary section"
  artifacts:
    - path: "docs/domain-model.md"
      provides: "Human-first domain + data-model reference (D-06/D-07)"
      min_lines: 60
      contains: "is_provisional"
    - path: "CLAUDE.md"
      provides: "Link to docs/domain-model.md from Domain Vocabulary"
      contains: "domain-model.md"
  key_links:
    - from: "CLAUDE.md Domain Vocabulary section"
      to: "docs/domain-model.md"
      via: "markdown link"
      pattern: "domain-model\\.md"
---

<objective>
D-06/D-07: write the human-first domain-model reference so the meaning of each
`int_combined` arm and each ID prefix is no longer inferred from SQL. This is the
PRIMARY deliverable of the phase per CONTEXT (the model correction is the supporting
fix; the doc is the durable artifact for human collaborators).

`docs/domain-model.md` (NOT an ADR — `docs/adr/0001-...` is the format precedent only)
covers the FINAL five-category model (CONTEXT table, post-Plan-02 reality):
1. Catalogued specimen (`ecdysis`, FALSE, `ecdysis:N`)
2. iNat-photo specimen pre-Ecdysis (`waba_specimen`, FALSE, `inat_obs:N`) — the 33
3. Provisional sample (`waba_sample`, TRUE, `inat:N`) — project-166376 members
4. Expert observation (`inat_obs`, FALSE, `inat_obs:N`)
5. Checklist (`checklist`, FALSE, `checklist:N`)

Linked from `CLAUDE.md` Domain Vocabulary. Consolidates from CLAUDE.md +
int_combined.sql comments + src/occurrence.ts JSDoc rather than duplicating.

Purpose: durable, human-readable model reference (D-07 — for humans as much as LLMs).
Output: docs/domain-model.md + a CLAUDE.md link.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/165-duplicate-occurrence-rows-shared-occ-id/165-CONTEXT.md
@.planning/phases/165-duplicate-occurrence-rows-shared-occ-id/165-RESEARCH.md
@CLAUDE.md
@src/occurrence.ts
@data/dbt/models/intermediate/int_combined.sql
@docs/adr/0001-mapbox-basemap-cache.md
@.planning/phases/165-duplicate-occurrence-rows-shared-occ-id/165-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write docs/domain-model.md (human-first, 5 categories)</name>
  <files>docs/domain-model.md</files>
  <read_first>
    - CLAUDE.md §3-17 (Domain Vocabulary: Specimen / Sample / Floral host / Observation / Occurrence record / Collection event — the doc must be consistent with and reference these) and §27 (ID format invariant)
    - src/occurrence.ts §1-107 (the AUTHORITATIVE TS definition of the ID-prefix vocabulary, occIdFromRow priority, and the isSpecimenBacked / isSampleOnly / isProvisional predicates — the doc REFERENCES this as the source of truth, does not restate the code)
    - data/dbt/models/intermediate/int_combined.sql header + arm comments (the 5 arms post-Plan-02; what each projects)
    - docs/adr/0001-mapbox-basemap-cache.md (format precedent for a docs/ file — but this is NOT an ADR: no Status/Decision header; reference material)
    - 165-CONTEXT.md three-category table + D-01, D-03, D-05, D-06, D-07, D-10, D-11, D-12 (the canonical model to write down)
    - 165-RESEARCH.md §"Research Question 5" (the exact content checklist for the doc) and §"Open Questions" #1 (mention the pipeline-lag state)
    - 165-02-SUMMARY.md (the AS-BUILT model + the Shape C backlog note — write the doc against what Plan 02 actually shipped, not the pre-fix state)
  </read_first>
  <action>
    Create `docs/domain-model.md` as a standalone human-readable reference (NOT an ADR — no
    Status/Decision/Context ADR headers). Structure it for a human reading top-to-bottom. Cover:
    (1) A short intro tying back to CLAUDE.md Domain Vocabulary (link to it; do not restate the
    six term definitions — reference them).
    (2) The FIVE occurrence categories as a table (mirror the CONTEXT three-category table extended
    to all 5): # | Category | source | is_provisional | occ_id prefix | real-world thing it represents.
    For each, one paragraph: what physical/record reality it is, where it comes from in the pipeline
    (which int_combined arm / which iNat pipeline), and why its occ_id prefix is what it is.
    (3) The CORRECTED `is_provisional` definition (D-03/D-11): membership in the WABA "plant images &
    sample IDs" iNat project (project_id 166376, full URL/slug from CONTEXT D-03) — NOT "WABA
    catalog-field obs not yet matched to Ecdysis" (the old drift, D-04). Explicitly: provisional =
    floral-host / sample observations only; no specimens are provisional (D-11).
    (4) The `occIdFromRow` ID-prefix vocabulary (`ecdysis:` / `inat:` / `inat_obs:` / `checklist:`)
    and its priority order, noting it is POSITIONALLY COUPLED across `src/occurrence.ts`
    (authoritative TS), `src/filter.ts` `OCC_ID_SQL_CASE`, and
    `data/dbt/models/marts/occurrence_places.sql` — change all three together (link to occurrence.ts
    as the authoritative definition; do not re-implement the CASE in prose).
    (5) The "when are two rows the same occurrence?" rule: same occ_id = same occurrence (D-01); after
    the Phase-165 correction this should never collide (Shapes A+B fixed); note the KNOWN, deferred
    "same physical bee, two different occ_ids" case (ecdysis:N vs inat_obs:M for one bee — a separate
    future question per CONTEXT Deferred Ideas / project_taxon_id_milestone) and the KNOWN Shape C
    (OFV fan-out, ecdysis: duplicate) surfaced-but-not-fixed by the D-09 test (backlog).
    (6) The known pipeline-lag state (RESEARCH Open Q1): WABA bee specimens with a catalog # but no
    Ecdysis record yet are category 2 (`waba_specimen`) — first-class specimens awaiting cataloguing,
    a standing ~2-year lag, NOT errors and NOT provisional samples.
    Aim for a concise reference (~60-120 lines); consolidate, link out, do not pad.
  </action>
  <verify>
    <automated>cd /Users/rainhead/dev/beeatlas && test -f docs/domain-model.md && wc -l docs/domain-model.md && grep -Eq "166376" docs/domain-model.md && grep -Eq "waba_specimen" docs/domain-model.md && grep -Eq "is_provisional" docs/domain-model.md && grep -Eq "occIdFromRow|occ_id" docs/domain-model.md && grep -Eq "inat_obs:|ecdysis:" docs/domain-model.md && echo "DOC-OK: all required topics present"</automated>
  </verify>
  <acceptance_criteria>
    - `docs/domain-model.md` exists and is >= 60 lines.
    - Contains the project id `166376`, the `waba_specimen` source, `is_provisional`, the occ_id / occIdFromRow vocabulary, and the `ecdysis:`/`inat_obs:` prefixes.
    - Documents all 5 categories, the corrected is_provisional definition, the positional coupling, the same-occurrence rule, and the pipeline-lag state.
    - Is NOT formatted as an ADR (no Status/Decision header) — it is reference material.
  </acceptance_criteria>
  <done>docs/domain-model.md is a complete, human-first 5-category model reference covering all D-06/D-07 topics.</done>
</task>

<task type="auto">
  <name>Task 2: Link docs/domain-model.md from CLAUDE.md Domain Vocabulary</name>
  <files>CLAUDE.md</files>
  <read_first>
    - CLAUDE.md §3-17 (the Domain Vocabulary section where the link belongs — D-07 specifies linked from here, NOT buried inside the term list)
    - 165-CONTEXT.md D-07 (doc is linked from CLAUDE.md Domain Vocabulary)
  </read_first>
  <action>
    Add a one-line pointer at the end of the CLAUDE.md `## Domain Vocabulary` section (after the
    Collection event definition, before `## Architecture Invariants`) linking to the new doc — e.g.
    "See [docs/domain-model.md](docs/domain-model.md) for the full occurrence data model: the five
    `int_combined` source categories, the `is_provisional` definition, and the synthetic-`occ_id`
    prefix vocabulary." Keep it concise (one sentence) per the user's global instruction to keep
    READMEs/CLAUDE.md focused on principles and link out rather than duplicate. Do NOT duplicate the
    domain-model.md content into CLAUDE.md.
  </action>
  <verify>
    <automated>cd /Users/rainhead/dev/beeatlas && grep -n "domain-model.md" CLAUDE.md && awk '/## Domain Vocabulary/{f=1} /## Architecture Invariants/{f=0} f && /domain-model.md/{print "LINK-IN-VOCAB-SECTION: "$0}' CLAUDE.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep "domain-model.md" CLAUDE.md` matches (the link exists).
    - The link appears WITHIN the `## Domain Vocabulary` section (between that heading and `## Architecture Invariants`), per D-07.
    - No domain-model.md prose is duplicated into CLAUDE.md (link only).
  </acceptance_criteria>
  <done>CLAUDE.md Domain Vocabulary links to docs/domain-model.md with a single concise pointer.</done>
</task>

</tasks>

<artifacts_this_phase_produces>
This plan contributes:
- `docs/domain-model.md` — the human-first 5-category occurrence data-model reference (D-06/D-07).
- A CLAUDE.md Domain Vocabulary link to that doc.
</artifacts_this_phase_produces>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none) | Documentation-only change. No code, no runtime, no input, no network. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-165-06 | (n/a) | docs/domain-model.md + CLAUDE.md | accept | Markdown documentation only; no executable surface, no data flow. No threat. |
| T-165-SC | Tampering | npm/pip/cargo installs | accept | No package installs. No legitimacy gate required. |

No high-severity threats. Reasoning documented per planning-context security note.
</threat_model>

<verification>
- `test -f docs/domain-model.md` and the topic-coverage greps pass.
- `grep domain-model.md CLAUDE.md` matches inside the Domain Vocabulary section.
</verification>

<success_criteria>
- docs/domain-model.md is a complete human-first reference covering the 5 categories, is_provisional, the occ_id vocabulary + coupling, the same-occurrence rule, and the pipeline-lag state.
- CLAUDE.md links to it from Domain Vocabulary.
</success_criteria>

<output>
Create `.planning/phases/165-duplicate-occurrence-rows-shared-occ-id/165-04-SUMMARY.md` when done.
</output>
