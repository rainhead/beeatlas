---
phase: 165-duplicate-occurrence-rows-shared-occ-id
plan: 03
type: execute
wave: 3
depends_on:
  - 165-01
  - 165-02
files_modified:
  - src/url-state.ts
  - src/filter.ts
  - src/bee-pane.ts
  - src/bee-occurrence-detail.ts
  - src/tests/occurrence.test.ts
autonomous: true
requirements: []
must_haves:
  truths:
    - "'waba_specimen' is a valid SourceKey and passes URL src= round-trip validation (VALID_SOURCES) in both url-state.ts and filter.ts"
    - "The source-toggle UI has a fifth entry for waba_specimen; the existing waba_sample toggle copy now reads as 'provisional samples'"
    - "The occurrence-detail card renders a waba_specimen branch reusing the existing source-dispatch pattern"
    - "occIdFromRow / parseOccId / OCC_ID_SQL_CASE remain UNCHANGED (occ_ids unaffected) and stay in sync with occurrence_places.sql"
    - "tsc --noEmit and npm test pass with the new source value"
  artifacts:
    - path: "src/url-state.ts"
      provides: "'waba_specimen' added to SourceKey union + VALID_SOURCES"
      contains: "waba_specimen"
    - path: "src/filter.ts"
      provides: "'waba_specimen' added to OccurrenceRow.source union + the source-filter VALID_SOURCES array"
      contains: "waba_specimen"
    - path: "src/bee-pane.ts"
      provides: "Fifth source-toggle entry (waba_specimen) + all-off count updated 4->5"
      contains: "waba_specimen"
    - path: "src/bee-occurrence-detail.ts"
      provides: "waba_specimen render branch in the source dispatch"
      contains: "waba_specimen"
  key_links:
    - from: "src/bee-pane.ts"
      to: "src/url-state.ts VALID_SOURCES"
      via: "_onSourceToggle('waba_specimen', ...) + hiddenSources.size === 5 all-off check"
      pattern: "waba_specimen"
    - from: "src/filter.ts buildFilterSQL source filter"
      to: "the waba_specimen source value in marts/occurrences"
      via: "VALID_SOURCES array drives the IN (...) source filter; new value flows through the same hardcoded-array path (no user input)"
      pattern: "VALID_SOURCES"
---

<objective>
D-13 frontend change: surface the new `waba_specimen` source (category 2 — the 33
iNat-photo bee specimens) in the SourceKey taxonomy, the source-toggle UI, and the
occurrence-detail card. REUSE existing patterns — introduce NO new UI pattern
(D-13; per the `feedback_no_unrequested_ui_patterns` memory).

Concretely:
- Add `'waba_specimen'` to the `SourceKey` union + `VALID_SOURCES` in `src/url-state.ts`,
  and to the `OccurrenceRow.source` union + the local `VALID_SOURCES` array in `src/filter.ts`.
- Add a fifth source-toggle entry in `src/bee-pane.ts` (mirroring the existing
  waba_sample/inat_obs entries Phase 164 touched); update the existing `waba_sample`
  toggle copy to mean "provisional samples"; bump the all-off count `=== 4` -> `=== 5`.
- Add a `waba_specimen` render branch in `src/bee-occurrence-detail.ts`, reusing the
  existing `row.source === ...` dispatch (mirror `_renderInatObs`/`_renderProvisional`).
- Keep `occIdFromRow` / `parseOccId` / `OCC_ID_SQL_CASE` UNCHANGED — category-2 rows use
  `inat_obs:N`, already handled; they remain positionally coupled to occurrence_places.sql,
  do not desync.

depends_on includes 165-01 because this plan edits `src/tests/occurrence.test.ts`, which
Plan 01 created/extended (file-ownership ordering), and 165-02 because the `waba_specimen`
source value must exist in the data before the frontend can surface it.

Purpose: make the rescued 33 specimens filterable + readable in the UI (D-10/D-13).
Output: the `waba_specimen` source wired end-to-end through the frontend.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/165-duplicate-occurrence-rows-shared-occ-id/165-CONTEXT.md
@.planning/phases/165-duplicate-occurrence-rows-shared-occ-id/165-RESEARCH.md
@.planning/phases/165-duplicate-occurrence-rows-shared-occ-id/165-VALIDATION.md
@CLAUDE.md
@src/url-state.ts
@src/filter.ts
@src/occurrence.ts
@src/bee-pane.ts
@src/bee-occurrence-detail.ts
@.planning/phases/165-duplicate-occurrence-rows-shared-occ-id/165-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add waba_specimen to the SourceKey taxonomy + validation (url-state.ts, filter.ts)</name>
  <files>src/url-state.ts, src/filter.ts</files>
  <read_first>
    - src/url-state.ts §31 (`SourceKey` union) and §33 (`VALID_SOURCES = new Set<SourceKey>([...])`) — also the src= parse path §239-254 that validates against VALID_SOURCES (T-164-IV)
    - src/filter.ts §76 (`OccurrenceRow.source` union) and §393-399 (the source-filter `VALID_SOURCES: SourceKey[]` array used to build the IN(...) clause — T-164-SQL: values come from this hardcoded array, NOT user input)
    - 165-CONTEXT.md D-12, D-13 (new source value waba_specimen, parallel to waba_sample in the waba_* family)
    - 165-RESEARCH.md §"No affected files (frontend)" is SUPERSEDED by D-13 — this IS a frontend change
  </read_first>
  <action>
    In `src/url-state.ts`: add `'waba_specimen'` to the `SourceKey` type union (§31) and to the
    `VALID_SOURCES` Set literal (§33). Place it adjacent to `'waba_sample'` to keep the waba_* family
    grouped. No other change to the parse/build logic is required — the existing src= validation,
    the `size > 0` visible/hidden derivation, and the `src=none` all-hidden sentinel all read
    VALID_SOURCES generically, so they pick up the 5th value automatically.
    In `src/filter.ts`: add `'waba_specimen'` to the `OccurrenceRow.source` union (§76) and to the
    local `VALID_SOURCES: SourceKey[]` array in the source-filter builder (§398). Confirm the value
    flows into the existing parameterized/hardcoded IN(...) path — it must NOT be string-concatenated
    from any user-controlled input (it is a fixed enum member; same path as the 4 existing tokens).
    Do NOT touch `OCC_ID_SQL_CASE` (§108-114), `occIdFromRow`, or `parseOccId` — occ_ids are unaffected.
  </action>
  <verify>
    <automated>cd /Users/rainhead/dev/beeatlas && npx tsc --noEmit 2>&1 | tail -10 && grep -c waba_specimen src/url-state.ts src/filter.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npx tsc --noEmit` exits 0 (the new union member is consistent everywhere SourceKey is used).
    - `grep -c waba_specimen src/url-state.ts` >= 2 (union + VALID_SOURCES). `grep -c waba_specimen src/filter.ts` >= 2 (OccurrenceRow.source + VALID_SOURCES array).
    - `OCC_ID_SQL_CASE`, `occIdFromRow`, `parseOccId` unchanged (no diff in those regions).
  </acceptance_criteria>
  <done>'waba_specimen' is a first-class SourceKey, validates in src= round-trips, and the project type-checks.</done>
</task>

<task type="auto">
  <name>Task 2: Add the waba_specimen source toggle + occurrence-detail branch (D-13, reuse patterns)</name>
  <files>src/bee-pane.ts, src/bee-occurrence-detail.ts</files>
  <read_first>
    - src/bee-pane.ts §1155-1203 (`_renderSources()` — the 4-entry `layers` array; mirror an entry) and §1233 (`this._hiddenSources.size === 4` all-off check — must become 5)
    - src/bee-occurrence-detail.ts §427-449 (the `render()` source dispatch: isProvisional -> _renderProvisional, source==='checklist' -> _renderChecklist, source==='inat_obs' -> _renderInatObs, else _renderSampleOnly) and §351-383 (`_renderInatObs` — the closest analog: bee species + quality badge + iNat link), §327-349 (`_renderProvisional`)
    - 165-CONTEXT.md D-13 (new toggle entry; existing waba_sample toggle copy shifts to "provisional samples"; badge/predicate as needed; reuse existing patterns — NO new UI pattern) + the feedback_no_unrequested_ui_patterns memory
  </read_first>
  <action>
    In `src/bee-pane.ts` `_renderSources()`: add a fifth entry to the `layers` array for
    `waba_specimen` — `label: 'WABA specimens'` (or similar concise copy), a tooltip like
    'WABA bee specimens photographed on iNaturalist, not yet catalogued in Ecdysis',
    `checked: !this._hiddenSources.has('waba_specimen')`, and
    `onChange: (e) => this._onSourceToggle('waba_specimen', (e.target as HTMLInputElement).checked)`.
    Update the EXISTING `waba_sample` entry copy to reflect the corrected meaning: label
    'Provisional samples' and tooltip 'Floral-host / sample observations in the WABA plant-images
    project, awaiting sample metadata' (it no longer means "field collections not yet in Ecdysis").
    Change the all-off guard at §1233 from `this._hiddenSources.size === 4` to `=== 5` so the
    "No sources selected" hint only fires when all FIVE are hidden.
    In `src/bee-occurrence-detail.ts` `render()`: extend the source dispatch so
    `row.source === 'waba_specimen'` routes to a new `_renderWabaSpecimen(row)` method. Implement
    `_renderWabaSpecimen` by reusing the `_renderInatObs` shape (it is the closest analog — bee
    species via taxonCache/display_name + quality badge + iNat obs link): render the bee
    canonical/display name with the existing taxon-filter-link pattern, the quality badge via
    `_renderQualityBadge(row.specimen_inat_quality_grade)`, the date, and a "View on iNaturalist"
    link to `row.obs_url` (populated by Plan 02). Add a short label/badge distinguishing it (e.g.
    an "awaiting catalog" hint) reusing existing `.hint`/badge classes — do NOT introduce a new
    CSS pattern; reuse the classes already in this component. Place the `waba_specimen` branch in
    the dispatch BEFORE the `inat_obs` branch (both are non-provisional, non-checklist; dispatch on
    source explicitly).
  </action>
  <verify>
    <automated>cd /Users/rainhead/dev/beeatlas && npx tsc --noEmit 2>&1 | tail -10 && grep -n "waba_specimen\|size === 5\|Provisional samples" src/bee-pane.ts && grep -n "waba_specimen\|_renderWabaSpecimen" src/bee-occurrence-detail.ts && npm test 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - `npx tsc --noEmit` exits 0.
    - `src/bee-pane.ts`: the `layers` array has 5 entries including `waba_specimen`; the existing waba_sample copy reads "Provisional samples"; the all-off check is `=== 5`.
    - `src/bee-occurrence-detail.ts`: `render()` dispatches `source === 'waba_specimen'` to `_renderWabaSpecimen`, which links to `row.obs_url` and reuses existing CSS classes (no new class/pattern).
    - `npm test` passes (exit 0) — including the occurrence.test.ts cases.
  </acceptance_criteria>
  <done>The fifth source toggle exists, the waba_sample copy is corrected to "provisional samples", the detail card renders waba_specimen via a reused pattern, and tsc + npm test are green.</done>
</task>

</tasks>

<artifacts_this_phase_produces>
This plan contributes:
- `'waba_specimen'` SourceKey value wired through `src/url-state.ts` (SourceKey + VALID_SOURCES), `src/filter.ts` (OccurrenceRow.source + source-filter VALID_SOURCES).
- A fifth source toggle in `src/bee-pane.ts` (+ corrected `waba_sample` copy + 4->5 all-off count).
- A `waba_specimen` occurrence-detail render branch in `src/bee-occurrence-detail.ts`.
(occIdFromRow / OCC_ID_SQL_CASE / occurrence_places.sql remain UNCHANGED and in sync.)
</artifacts_this_phase_produces>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| URL `src=` query param -> SPA filter state | Untrusted URL input crosses into source-filter parsing. (Pre-existing boundary; this plan adds one enum value.) |
| client filter state -> client-side SQL (wa-sqlite) | The source-filter clause is built client-side and run against the in-browser store. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-165-04 | Tampering / Injection | `src=` token -> VALID_SOURCES validation (url-state.ts §239-254) | mitigate | The new `'waba_specimen'` token is validated against the fixed `VALID_SOURCES` Set on parse (T-164-IV: bogus values dropped, never reach SQL). Confirm the new value is added to BOTH VALID_SOURCES (url-state.ts) and the filter.ts source-filter array so an attacker cannot smuggle an unknown source token; unknown tokens still drop. |
| T-165-05 | Injection | filter.ts source-filter IN(...) clause | mitigate | The source value comes from the hardcoded `VALID_SOURCES: SourceKey[]` array (T-164-SQL), NOT user input — it is not string-concatenated from the URL. Verify the new value flows through the same hardcoded-array path as the existing 4 tokens (no new concatenation site). |
| T-165-SC | Tampering | npm/pip/cargo installs | accept | No package installs (RESEARCH §Standard Stack). No legitimacy gate required. |

No high-severity threats: the change adds one member to a fixed enum already validated/escaped via the Phase-164 path. Reasoning documented per planning-context security note.
</threat_model>

<verification>
- `npx tsc --noEmit` exits 0.
- `npm test` exits 0 (occurrence.test.ts + any url-state/filter tests).
- Grep confirms `waba_specimen` present in url-state.ts, filter.ts, bee-pane.ts, bee-occurrence-detail.ts; all-off count is `=== 5`.
- Manual UAT (deferred to verification per VALIDATION "Manual-Only Verifications"): waba_specimen toggle shows/hides the 33 points; the corrected waba_sample toggle controls provisional samples; the waba_specimen badge reads correctly.
</verification>

<success_criteria>
- `waba_specimen` is a validated SourceKey, filterable via src= and the toggle, readable in occurrence detail.
- The existing `waba_sample` toggle copy now correctly means "provisional samples".
- occIdFromRow / OCC_ID_SQL_CASE / occurrence_places.sql remain unchanged and in sync.
- tsc + npm test green.
</success_criteria>

<output>
Create `.planning/phases/165-duplicate-occurrence-rows-shared-occ-id/165-03-SUMMARY.md` when done.
</output>
