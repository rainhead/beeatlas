---
phase: 165-duplicate-occurrence-rows-shared-occ-id
plan: 02
type: execute
wave: 2
depends_on:
  - 165-01
files_modified:
  - data/dbt/models/intermediate/int_waba_link.sql
  - data/dbt/models/intermediate/int_matched_waba_ids.sql
  - data/dbt/models/intermediate/int_ecdysis_base.sql
  - data/dbt/models/intermediate/int_provisional_waba_ids.sql
  - data/dbt/models/intermediate/int_combined.sql
  - data/dbt/models/intermediate/schema.yml
autonomous: true
requirements: []
must_haves:
  truths:
    - "obs 320276469 is matched to its Ecdysis catalog record and resolves to an ecdysis: row (no longer provisional)"
    - "The waba_sample (provisional) arm contains ONLY WABA plant-images/sample-IDs project members (project_id=166376), anti-joined against int_samples_base — ~28 mappable rows, is_provisional=TRUE, source='waba_sample'"
    - "No specimens appear in waba_sample (D-11): every waba_sample row has specimen_observation_id NULL"
    - "The 33 WABA bee-specimen iNat-photo obs (post-MIN-fix) appear as source='waba_specimen', is_provisional=FALSE rows with their bee canonical_name/taxon_id"
    - "After the correction the occ_id uniqueness test returns only the 2 Shape-C rows (Shapes A+B eliminated)"
    - "Removing MIN() does NOT fan out ARM 1 ecdysis rows (int_ecdysis_base.specimen_observation_id stays 1:1 per ecdysis row)"
    - "marts/occurrences still has 36 columns (dbt contract passes) and row-count delta is within test_dbt_diff tolerance"
  artifacts:
    - path: "data/dbt/models/intermediate/int_waba_link.sql"
      provides: "1:N catalog_suffix → all WABA obs (MIN removed) — D-05 catalog-match fix"
    - path: "data/dbt/models/intermediate/int_provisional_waba_ids.sql"
      provides: "Category-3 set: project 166376 members anti-joined int_samples_base (D-03/D-11)"
    - path: "data/dbt/models/intermediate/int_combined.sql"
      provides: "Corrected ARM 2 (waba_sample provisional samples) + NEW ARM (waba_specimen, the 33)"
      contains: "waba_specimen"
  key_links:
    - from: "data/dbt/models/intermediate/int_provisional_waba_ids.sql"
      to: "inaturalist_data.observations__observation_projects"
      via: "JOIN on project_id=166376 + anti-join int_samples_base"
      pattern: "166376"
    - from: "data/dbt/models/intermediate/int_combined.sql"
      to: "int_specimen_obs_base"
      via: "waba_specimen arm: unmatched WABA catalog-field bee obs"
      pattern: "waba_specimen"
    - from: "data/dbt/models/intermediate/int_ecdysis_base.sql"
      to: "int_waba_link"
      via: "LEFT JOIN on catalog_suffix — MUST stay 1:1 after MIN removal (de-dup the link in this consumer)"
      pattern: "int_waba_link"
---

<objective>
The core data-model correction (D-01..D-12). Implements the FINAL three-honest-arms
model from 165-CONTEXT (the table + D-10..D-13), NOT the RESEARCH "accept the
regression" framing (superseded).

Three changes to int_combined's upstream + the UNION ALL itself:
1. **D-05 catalog-match fix** — remove `MIN(waba.id) GROUP BY catalog_suffix` in
   `int_waba_link` so ALL WABA obs sharing a catalog suffix match. Rescues obs 320276469
   → it becomes an `ecdysis:` row. CRITICAL: `int_ecdysis_base` ALSO joins `int_waba_link`
   directly (for `specimen_observation_id`), so the 1:N link MUST be de-duplicated at that
   consumer to avoid fanning out ARM 1 (RESEARCH Pitfall 1 only covered int_matched_waba_ids;
   int_ecdysis_base is a second, real fan-out vector — handle it).
2. **Category 3 — provisional sample (`waba_sample`, is_provisional=TRUE)** — redefine
   `int_provisional_waba_ids` to WABA "plant images & sample IDs" iNat project membership
   (`project_id=166376`), ANTI-JOIN `int_samples_base`. Rewrite ARM 2 in int_combined to
   project plant/sample columns (specimen fields NULL — D-11: no specimens in waba_sample).
3. **Category 2 — `waba_specimen` (NEW source, is_provisional=FALSE)** — add a NEW arm for
   the WABA catalog-field bee obs NOT matched to Ecdysis (the 33, after 320276469 leaves via
   the MIN fix). occ_id stays `inat_obs:<bee_obs_id>`. Carry bee canonical_name/taxon_id and
   an iNat obs link.

Purpose: one corrected model eliminates Shapes A+B at the source (D-02) — fixes list,
detail, map, places, CSV at once — while keeping all real data visible (D-10).
Output: corrected int_* models; the new `waba_specimen` source value materialized.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/165-duplicate-occurrence-rows-shared-occ-id/165-CONTEXT.md
@.planning/phases/165-duplicate-occurrence-rows-shared-occ-id/165-RESEARCH.md
@.planning/phases/165-duplicate-occurrence-rows-shared-occ-id/165-VALIDATION.md
@CLAUDE.md
@data/dbt/models/intermediate/int_combined.sql
@data/dbt/models/intermediate/int_waba_link.sql
@data/dbt/models/intermediate/int_matched_waba_ids.sql
@data/dbt/models/intermediate/int_provisional_waba_ids.sql
@data/dbt/models/intermediate/int_ecdysis_base.sql
@data/dbt/models/intermediate/int_specimen_obs_base.sql
@data/dbt/models/intermediate/int_samples_base.sql
@data/dbt/macros/inat_field_ids.sql
@data/dbt/models/staging/stg_inat__observations.sql
@.planning/phases/165-duplicate-occurrence-rows-shared-occ-id/165-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: D-05 catalog-match fix — remove MIN() and guard the two consumers against fan-out</name>
  <files>data/dbt/models/intermediate/int_waba_link.sql, data/dbt/models/intermediate/int_matched_waba_ids.sql, data/dbt/models/intermediate/int_ecdysis_base.sql</files>
  <read_first>
    - data/dbt/models/intermediate/int_waba_link.sql (the `MIN(waba.id) ... GROUP BY catalog_suffix` to remove)
    - data/dbt/models/intermediate/int_matched_waba_ids.sql (consumer 1 — used as a NOT-IN filter set; verify it stays a filter set, no carried columns)
    - data/dbt/models/intermediate/int_ecdysis_base.sql §31 (consumer 2 — `LEFT JOIN int_waba_link wl ON wl.catalog_suffix = CAST(regexp_extract(o.catalog_number,'[0-9]+$',0) AS BIGINT)`; carries `wl.specimen_observation_id` into ARM 1. THIS is the fan-out vector once the link is 1:N)
    - data/dbt/macros/inat_field_ids.sql (`inat_ofv_catalog_suffix()` = 18116)
    - 165-RESEARCH.md §"Research Question 2" (the verified fix) and §"Pitfall 1" (fan-out)
  </read_first>
  <action>
    In `int_waba_link.sql`: remove `MIN(waba.id)` and `GROUP BY catalog_suffix`; SELECT
    `CAST(ofv.value AS BIGINT) AS catalog_suffix, waba.id AS specimen_observation_id` so the
    model returns ALL WABA obs per catalog_suffix (1:N). Keep the JOIN on field_id =
    `{{ inat_ofv_catalog_suffix() }}` and `value != ''`. Update the header comment from
    "catalog_suffix -> MIN(waba.id)" to "catalog_suffix -> all WABA obs ids (1:N; D-05 fix)".

    `int_matched_waba_ids.sql`: confirm it remains `SELECT wl.specimen_observation_id AS waba_obs_id
    FROM int_waba_link wl JOIN int_ecdysis_catalog_suffixes ...` — now returns the SET of ALL
    matching waba_obs_ids. It is consumed only as a NOT-IN filter (int_provisional_waba_ids), so
    no fan-out; add `SELECT DISTINCT` defensively so duplicate (suffix, obs) pairs collapse.

    `int_ecdysis_base.sql` §31: the `LEFT JOIN int_waba_link wl` now risks fanning out ARM 1
    (one ecdysis row × N WABA obs sharing the suffix → duplicate ecdysis rows = Shape A regression
    in reverse). De-duplicate at this consumer: replace the direct `int_waba_link` join with a join
    to a 1:1-per-suffix subselect that picks ONE specimen_observation_id per catalog_suffix
    (e.g. `SELECT catalog_suffix, MIN(specimen_observation_id) AS specimen_observation_id FROM int_waba_link GROUP BY catalog_suffix`
    inlined as a CTE or subquery), so int_ecdysis_base.specimen_observation_id stays exactly one
    value per ecdysis row. Add a comment explaining that the 1:N link is intentional for matching
    (int_matched_waba_ids) but ARM 1 needs a single representative obs to avoid fan-out.
  </action>
  <verify>
    <automated>cd /Users/rainhead/dev/beeatlas && bash data/dbt/run.sh build --select int_waba_link+ int_ecdysis_base 2>&1 | tail -15 && duckdb data/beeatlas.duckdb -c "SELECT (SELECT COUNT(*) FROM dbt_sandbox.int_matched_waba_ids WHERE waba_obs_id=320276469) AS rescued_320276469, (SELECT COUNT(*) FROM (SELECT ecdysis_id, COUNT(*) c FROM dbt_sandbox.int_ecdysis_base WHERE ecdysis_id IS NOT NULL GROUP BY ecdysis_id HAVING COUNT(*)>1)) AS fanned_out_ecdysis_rows;"</automated>
  </verify>
  <acceptance_criteria>
    - `bash data/dbt/run.sh build --select int_waba_link+ int_ecdysis_base` exits 0.
    - DuckDB: `rescued_320276469` = 1 (obs 320276469 now in int_matched_waba_ids).
    - DuckDB: `fanned_out_ecdysis_rows` = 0 (no ecdysis_id appears more than once in int_ecdysis_base — the MIN-removal did NOT fan out ARM 1).
    - `int_waba_link.sql` no longer contains `MIN(` or `GROUP BY catalog_suffix`.
  </acceptance_criteria>
  <done>obs 320276469 is matched, ARM 1 does not fan out, and the targeted models build clean.</done>
</task>

<task type="auto">
  <name>Task 2: Redefine the provisional arm (category 3) on project membership (D-03/D-11)</name>
  <files>data/dbt/models/intermediate/int_provisional_waba_ids.sql, data/dbt/models/intermediate/int_combined.sql, data/dbt/models/intermediate/schema.yml</files>
  <read_first>
    - data/dbt/models/intermediate/int_provisional_waba_ids.sql (current mis-definition: WABA-field obs NOT IN matched — to be REPLACED)
    - data/dbt/models/intermediate/int_combined.sql §66-150 (ARM 2 — current waba_sample projection from int_specimen_obs_base; to be rewritten to project plant/sample columns from the project-member obs)
    - data/dbt/models/intermediate/int_samples_base.sql (the anti-join target; provides observation_id, host_inat_login, sample_lon/lat, sample_date, specimen_count, sample_id, sample_host)
    - data/dbt/models/staging/stg_inat__observations.sql (wraps inaturalist_data.observations; has id, uuid, user__login, observed_on, longitude, latitude, taxon__name, taxon__iconic_taxon_name)
    - 165-RESEARCH.md §"Research Question 1" (new ARM 2 population = 33 obs, 28 mappable; the project_id=166376, observation_uuid join key; the column projection table) and §"Pitfall 1/2/5"
    - 165-CONTEXT.md D-03, D-08, D-11 (provisional = project members ONLY; no specimens in waba_sample; per-column reconciliation — specimen fields NULL)
  </read_first>
  <action>
    Replace `int_provisional_waba_ids.sql` with the project-membership definition: SELECT the
    observation id of obs that are members of project 166376
    (`inaturalist_data.observations__observation_projects op` joined to `stg_inat__observations obs`
    on `op.observation_uuid = obs.uuid AND op.project_id = 166376`) and that are NOT in
    `int_samples_base` (anti-join: `WHERE obs.id NOT IN (SELECT observation_id FROM int_samples_base)`),
    with `obs.longitude IS NOT NULL AND obs.latitude IS NOT NULL`. Output one column
    `observation_id` (rename from `waba_obs_id` — this is now a plant/sample obs id, not a bee obs id).
    Use the literal `166376` (project id, confirmed in RESEARCH; consider a macro only if one already
    exists — do not invent one). Update the header comment to state D-03 semantics: "provisional =
    WABA plant-images/sample-IDs project (166376) members lacking a specimen-count OFV; floral-host /
    sample observations; never join other sources."

    Rewrite ARM 2 in `int_combined.sql` (the `'waba_sample'` SELECT) to project from
    `int_provisional_waba_ids p JOIN stg_inat__observations obs ON obs.id = p.observation_id`
    (LEFT JOIN int_samples_base is NOT needed — these are anti-joined out of it). Column mapping
    per RESEARCH's projection table and D-11 (NO specimens in waba_sample):
      - ecdysis_id NULL; catalog_number NULL
      - lon = obs.longitude; lat = obs.latitude; date = CAST(obs.observed_on AS VARCHAR); year/month from observed_on
      - observation_id = obs.id  (this gives the `inat:N` occ_id — category 3)
      - specimen_observation_id NULL  (D-11 — no specimen)
      - host_observation_id NULL; host_inat_login = obs.user__login
      - is_provisional TRUE
      - source 'waba_sample'
      - canonical_name / taxon_id: NULL (per D-08 mutually-exclusive/recomputed — plant obs carry no bee species; planner discretion already exercised → leave NULL, matching RESEARCH Pitfall 2's safe path). sample_host = is_plant_taxon('obs') if a host name is desired.
      - all specimen_inat_* fields NULL; image_url/obs_url/user_login/license NULL; checklist/verbatim/locality/collapsed_count NULL
    Preserve the column ORDER and count of the existing ARM 2 SELECT (UNION ALL requires identical
    column lists across arms) — match int_combined's ARM 1/3/4 column sequence exactly.
    Drop the ofv1718 host-observation join and the specimen_inat_taxon_name canonical-name derivation
    that the old bee-specimen ARM 2 used — they do not apply to plant/sample obs.

    Add/update the `int_provisional_waba_ids` and `int_combined` notes in
    `data/dbt/models/intermediate/schema.yml` describing the corrected semantics.
  </action>
  <verify>
    <automated>cd /Users/rainhead/dev/beeatlas && bash data/dbt/run.sh build --select int_provisional_waba_ids+ int_combined 2>&1 | tail -15 && duckdb data/beeatlas.duckdb -c "SELECT COUNT(*) AS waba_sample_rows, SUM(CASE WHEN specimen_observation_id IS NOT NULL THEN 1 ELSE 0 END) AS specimens_in_waba_sample, SUM(CASE WHEN is_provisional THEN 1 ELSE 0 END) AS provisional_true FROM dbt_sandbox.int_combined WHERE source='waba_sample';"</automated>
  </verify>
  <acceptance_criteria>
    - `bash data/dbt/run.sh build --select int_provisional_waba_ids+ int_combined` exits 0.
    - DuckDB: `waba_sample_rows` ≈ 28 (the mappable project-166376 members; tolerate the verified 28 from RESEARCH — flag if wildly off, e.g. >100 or 0).
    - DuckDB: `specimens_in_waba_sample` = 0 (D-11 honored — no specimen_observation_id in waba_sample).
    - DuckDB: `provisional_true` = `waba_sample_rows` (every waba_sample row is is_provisional=TRUE).
    - int_combined ARM 2 SELECT has the same column count/order as the other arms (build would error otherwise).
  </acceptance_criteria>
  <done>waba_sample is exactly the ~28 project-166376 sample/plant members, is_provisional=TRUE, zero specimens (D-11).</done>
</task>

<task type="auto">
  <name>Task 3: Add the waba_specimen arm (category 2 — the 33) and witness Shapes A+B gone</name>
  <files>data/dbt/models/intermediate/int_combined.sql, data/dbt/models/intermediate/schema.yml</files>
  <read_first>
    - data/dbt/models/intermediate/int_combined.sql (after Task 2 — add a NEW UNION ALL arm; the old bee-specimen projection from int_specimen_obs_base is the structural template for the new waba_specimen arm)
    - data/dbt/models/intermediate/int_specimen_obs_base.sql (provides waba_obs_id, specimen_inat_login, specimen_inat_taxon_name, longitude, latitude, observed_on, quality_grade for the bee obs)
    - data/dbt/models/intermediate/int_matched_waba_ids.sql (the now-corrected matched set; the waba_specimen arm = WABA catalog-field bee obs NOT in this set, after the MIN fix dropped 320276469 out of it)
    - 165-CONTEXT.md three-category table + D-10, D-11, D-12 (keep the 33; source='waba_specimen'; is_provisional=FALSE; occ_id inat_obs:N; carry bee species; surface iNat obs link)
    - 165-RESEARCH.md §"Current ARM 2 rows that would be removed" (the 34 → 33 after 320276469 leaves) and §"Research Question 3" (Shape A/B elimination), §"Pitfall 2"
    - data/dbt/tests/test_no_duplicate_occ_ids.sql (the Wave-0 witness — must now return only the 2 Shape-C rows)
  </read_first>
  <action>
    Add a NEW UNION ALL arm to `int_combined.sql` for category 2 (`waba_specimen`). Source it from
    `int_specimen_obs_base sob` (the WABA bee-specimen pipeline) WHERE
    `sob.waba_obs_id NOT IN (SELECT waba_obs_id FROM int_matched_waba_ids)` (the unmatched WABA bee
    obs — these are the 33 after the Task-1 MIN fix moved 320276469 into the matched set) AND
    `sob.longitude IS NOT NULL AND sob.latitude IS NOT NULL`. Reuse the canonical_name derivation +
    taxon_id bridge JOIN pattern from the OLD bee-specimen ARM 2 (the inline lower(trim(CASE ...))
    over specimen_inat_taxon_name, plus the stg_inat__canonical_to_taxon_id / stg_inat__genus_taxon_ids
    joins) — these belong here now, since category 2 carries the bee species (D-10). Preserve the same
    `NOT IN ('cicindela pugetana','cleridae','encopognathus')` non-bee exclusion the old arm had.
    Column mapping (match the arm column order/count exactly):
      - ecdysis_id NULL; catalog_number NULL
      - lon = sob.longitude; lat = sob.latitude; date from sob.observed_on; year/month from observed_on
      - observation_id NULL; host_observation_id NULL  → so occ_id falls to specimen_observation_id = `inat_obs:N` (D-12)
      - specimen_observation_id = sob.waba_obs_id
      - is_provisional FALSE  (D-10 — these are specimens awaiting cataloguing, not provisional samples)
      - source 'waba_specimen'  (NEW value — D-12)
      - canonical_name / taxon_id = the bee species derivation above
      - specimen_inat_login = sob.specimen_inat_login; specimen_inat_taxon_name = sob.specimen_inat_taxon_name; specimen_inat_quality_grade = sob.quality_grade; inat_quality_grade = sob.quality_grade
      - obs_url = 'https://www.inaturalist.org/observations/' || sob.waba_obs_id  (surface the iNat obs link per D-10 "ideally surface obs_url"); image_url/user_login/license NULL
      - sample/host fields NULL; checklist/verbatim/locality/collapsed_count NULL
    Add a comment block on the new arm: "ARM (category 2 / D-10): WABA iNat-photo bee specimens not
    yet in Ecdysis (the ~33). source='waba_specimen', is_provisional=FALSE, occ_id inat_obs:N.
    Verified no inat_obs (ARM 3) overlap except 320276469, which the MIN fix moved to ecdysis: — so
    category 2 collides with nothing."
    Update `data/dbt/models/intermediate/schema.yml` int_combined note to mention the 5 source values
    (ecdysis, waba_sample, waba_specimen, inat_obs, checklist).
  </action>
  <verify>
    <automated>cd /Users/rainhead/dev/beeatlas && bash data/dbt/run.sh build 2>&1 | tail -20 && duckdb data/beeatlas.duckdb -c "SELECT COUNT(*) AS waba_specimen_rows, SUM(CASE WHEN is_provisional THEN 1 ELSE 0 END) AS provisional_true, SUM(CASE WHEN specimen_observation_id IS NOT NULL THEN 1 ELSE 0 END) AS have_spec_obs_id FROM dbt_sandbox.int_combined WHERE source='waba_specimen';" && echo "--- occ_id dup test (expect only 2 Shape-C rows) ---" && duckdb data/beeatlas.duckdb -c "WITH o AS (SELECT CASE WHEN ecdysis_id IS NOT NULL THEN 'ecdysis:'||ecdysis_id WHEN observation_id IS NOT NULL THEN 'inat:'||observation_id WHEN specimen_observation_id IS NOT NULL THEN 'inat_obs:'||specimen_observation_id WHEN checklist_id IS NOT NULL THEN 'checklist:'||checklist_id END AS occ_id FROM dbt_sandbox.int_combined) SELECT occ_id, COUNT(*) FROM o WHERE occ_id IS NOT NULL GROUP BY occ_id HAVING COUNT(*)>1 ORDER BY occ_id;"</automated>
  </verify>
  <acceptance_criteria>
    - Full `bash data/dbt/run.sh build` exits 0 (the 36-col marts/occurrences contract still passes).
    - DuckDB: `waba_specimen_rows` ≈ 33 (tolerate 32-33; flag if 0 or >50). `provisional_true` = 0. `have_spec_obs_id` = `waba_specimen_rows` (all carry specimen_observation_id → inat_obs:N).
    - The occ_id dup query returns ONLY `ecdysis:6317352` and `ecdysis:6317353` (Shape C) — `inat_obs:320276469` and `inat:351027987` (Shapes A+B) are GONE.
    - `bash data/dbt/run.sh test --select test_no_duplicate_occ_ids` WARNs on exactly the 2 Shape-C rows (build not blocked).
  </acceptance_criteria>
  <done>waba_specimen carries the 33 bee specimens (is_provisional=FALSE, inat_obs:N, bee species + obs link); Shapes A+B eliminated; full build green; Shape C remains as a warning only.</done>
</task>

</tasks>

<artifacts_this_phase_produces>
This plan contributes:
- `int_waba_link` 1:N catalog-match (D-05 fix) + fan-out guard in `int_ecdysis_base`.
- Corrected `int_provisional_waba_ids` (project-166376 membership) feeding the `waba_sample` provisional-sample arm (category 3, is_provisional=TRUE, samples only — D-11).
- NEW `waba_specimen` source value in `int_combined` (category 2 — the 33 iNat-photo bee specimens, is_provisional=FALSE, occ_id `inat_obs:N`, bee species + iNat obs link — D-10/D-12).
- Shapes A+B eliminated at the data layer; the D-09 test now warns only on Shape C.
- Backlog note (record in SUMMARY): Shape C (`ecdysis:6317352`/`6317353` OFV fan-out in obs 288589692) is a SEPARATE out-of-scope data-quality bug; recommend a backlog item to fix the duplicate field_id=9963 OFV in int_samples_base, then escalate the D-09 test to severity:error.
</artifacts_this_phase_produces>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | Build-time dbt transforms over already-ingested DuckDB sources. No runtime, no user input, no new network calls. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-165-02 | Tampering | int_combined SQL (new arm + project_id literal) | accept | `project_id=166376` and field-id literals are static, sourced from RESEARCH-verified DuckDB metadata; no user input flows into the SQL. Static-hosted, no server runtime. |
| T-165-03 | Information disclosure | new `waba_specimen` rows expose iNat obs URLs (`obs_url`) | accept | obs_url points to public iNaturalist observation pages already linked elsewhere in the UI (existing inat_obs/provisional arms); no new PII surfaced. |
| T-165-SC | Tampering | npm/pip/cargo installs | accept | No package installs (RESEARCH §Standard Stack: pure data/documentation phase). No legitimacy gate required. |

No high-severity threats. Reasoning documented per planning-context security note.
</threat_model>

<verification>
- `bash data/dbt/run.sh build` exits 0 (36-col contract intact).
- `bash data/dbt/run.sh test --select test_no_duplicate_occ_ids` WARNs only on Shape C.
- `cd data && uv run pytest -x` — test_dbt_diff row-count delta within tolerance (net change ~+28 cat-3, the 33 cat-2 relabeled, 320276469 moved to ecdysis).
- DuckDB row assertions per task acceptance criteria.
</verification>

<success_criteria>
- obs 320276469 resolves to an `ecdysis:` row; ARM 1 does not fan out.
- `waba_sample` = ~28 project-166376 sample members, is_provisional=TRUE, zero specimens (D-11).
- `waba_specimen` = the 33 bee specimens, is_provisional=FALSE, occ_id `inat_obs:N`, bee species + obs link.
- Shapes A+B gone; D-09 test warns only on Shape C; full build + pytest green.
</success_criteria>

<output>
Create `.planning/phases/165-duplicate-occurrence-rows-shared-occ-id/165-02-SUMMARY.md` when done. Include the Shape C backlog recommendation.
</output>
