---
phase: 165-duplicate-occurrence-rows-shared-occ-id
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - data/dbt/tests/test_no_duplicate_occ_ids.sql
  - data/dbt/models/intermediate/schema.yml
  - src/tests/occurrence.test.ts
autonomous: true
requirements: []
must_haves:
  truths:
    - "A dbt singular test exists that fails (returns rows) when any synthetic occ_id appears more than once in int_combined"
    - "The occ_id test is severity:warn so the pre-existing Shape C OFV fan-out duplicate does not block the build"
    - "occurrence.test.ts covers a category-3 provisional row WITH observation_id set, asserting occIdFromRow returns inat:N (the new behavior)"
  artifacts:
    - path: "data/dbt/tests/test_no_duplicate_occ_ids.sql"
      provides: "D-09 occ_id uniqueness regression guard (singular test, severity:warn)"
      contains: "int_combined"
    - path: "src/tests/occurrence.test.ts"
      provides: "Vitest case for provisional row with observation_id (category 3 → inat:N)"
  key_links:
    - from: "data/dbt/tests/test_no_duplicate_occ_ids.sql"
      to: "int_combined"
      via: "ref('int_combined') with occ_id CASE mirroring occurrence_places.sql / occIdFromRow priority"
      pattern: "ref\\('int_combined'\\)"
---

<objective>
Wave 0: create the failing/witness gates BEFORE any model or frontend change, so
the dbt and vitest suites can observe Shapes A+B disappearing (Wave 1) and the new
category-3 provisional behavior (Wave 2).

Two artifacts:
1. The D-09 occ_id uniqueness singular test (`severity: warn`) — today it returns
   4 rows (Shapes A, B, and the two Shape-C OFV-fan-out rows); after Wave 1 it must
   return only the 2 Shape-C rows. `severity: warn` keeps Shape C (out of scope) from
   blocking the build.
2. A new occurrence.test.ts case for a category-3 provisional row that carries
   `observation_id` (the new ARM 2 shape) → `occIdFromRow` returns `inat:N`, distinct
   from the existing `provisionalRow()` fixture (observation_id null → null).

Purpose: Nyquist compliance — every behavior this phase changes gets an automated
witness created before the change.
Output: one new dbt test file, one schema.yml note, one new vitest case.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/165-duplicate-occurrence-rows-shared-occ-id/165-CONTEXT.md
@.planning/phases/165-duplicate-occurrence-rows-shared-occ-id/165-RESEARCH.md
@.planning/phases/165-duplicate-occurrence-rows-shared-occ-id/165-VALIDATION.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create the occ_id uniqueness singular test (D-09)</name>
  <files>data/dbt/tests/test_no_duplicate_occ_ids.sql, data/dbt/models/intermediate/schema.yml</files>
  <read_first>
    - data/dbt/tests/test_no_duplicate_occ_ids.sql (file to create — does not exist yet)
    - data/dbt/tests/test_ecdysis_id_references_source.sql (the analog singular-test pattern: header comment explaining PASS=0-rows, then a SELECT returning violating rows)
    - data/dbt/models/marts/occurrence_places.sql §43-48 (the occ_id CASE expression this test MUST mirror exactly — ecdysis → inat → inat_obs → checklist priority)
    - data/dbt/models/intermediate/int_combined.sql (the model under test; occ_id is NOT a stored column — it is recomputed from ecdysis_id / observation_id / specimen_observation_id / checklist_id)
    - 165-RESEARCH.md §"Research Question 4: Regression Guard (D-09)" (the verified test SQL) and §"Pitfall 3" (Shape C false positive → severity:warn)
    - data/dbt/models/intermediate/schema.yml (add the severity:warn config note for the new test)
  </read_first>
  <action>
    Create `data/dbt/tests/test_no_duplicate_occ_ids.sql` as a dbt singular test: a CTE
    `occ_ids` selects a single `occ_id` column from `{{ ref('int_combined') }}` using a CASE
    expression that mirrors `occurrence_places.sql` priority EXACTLY — `WHEN ecdysis_id IS NOT NULL THEN 'ecdysis:' || ecdysis_id`,
    `WHEN observation_id IS NOT NULL THEN 'inat:' || observation_id`,
    `WHEN specimen_observation_id IS NOT NULL THEN 'inat_obs:' || specimen_observation_id`,
    `WHEN checklist_id IS NOT NULL THEN 'checklist:' || checklist_id`, `END`. The outer
    SELECT groups by occ_id WHERE occ_id IS NOT NULL HAVING COUNT(*) > 1 (returns violating
    rows; 0 rows = PASS). Add a header comment block (matching the
    test_ecdysis_id_references_source.sql style) stating: PASS semantics (0 rows), the CASE
    mirrors occurrence_places.sql + src/occurrence.ts occIdFromRow priority and they must
    move together, and that Shape C (the OFV fan-out producing `ecdysis:6317352`/`ecdysis:6317353`)
    is a known out-of-scope false positive (backlog) — hence severity:warn until Shape C is
    separately fixed.
    Configure severity via `{{ config(severity='warn') }}` at the top of the test SQL.
    Then add a brief note under `int_combined` in `data/dbt/models/intermediate/schema.yml`
    referencing this new singular test (mirroring the existing TEST-02 note style on
    int_ecdysis_base.ecdysis_id) so the test is discoverable from the model doc.
    Do NOT use a generic dbt `unique` test — it cannot target a CASE expression (per RESEARCH "Don't Hand-Roll").
  </action>
  <verify>
    <automated>cd /Users/rainhead/dev/beeatlas && bash data/dbt/run.sh test --select test_no_duplicate_occ_ids 2>&1 | tee /tmp/165-w0-dbt.log; grep -Eq "WARN|warn|PASS=4|Got 4 result" /tmp/165-w0-dbt.log && echo "WITNESS-OK: test runs and warns on current duplicates"</automated>
  </verify>
  <acceptance_criteria>
    - `data/dbt/tests/test_no_duplicate_occ_ids.sql` exists and references `{{ ref('int_combined') }}`.
    - Running the test against the CURRENT (un-fixed) data returns 4 violating rows (`inat_obs:320276469`, `inat:351027987`, `ecdysis:6317352`, `ecdysis:6317353`) and reports as WARN, NOT ERROR (build not blocked).
    - The occ_id CASE in the test is byte-identical in branch order/prefixes to `occurrence_places.sql`.
    - `data/dbt/models/intermediate/schema.yml` mentions the new test under int_combined.
  </acceptance_criteria>
  <done>The singular test exists, runs, and WARNs (does not error) on the 4 current duplicate occ_ids; build still succeeds.</done>
</task>

<task type="auto">
  <name>Task 2: Add the category-3 provisional occIdFromRow vitest case</name>
  <files>src/tests/occurrence.test.ts</files>
  <read_first>
    - src/tests/occurrence.test.ts (the existing suite — the `provisionalRow()` factory sets observation_id:null and the existing test asserts occIdFromRow → null)
    - src/occurrence.ts §23-30 (occIdFromRow priority — UNCHANGED this phase; category-3 rows carry observation_id, so they resolve to `inat:N`)
    - 165-RESEARCH.md §"Wave 0 Gaps" (the exact suggested test case: a provisional row WITH observation_id set returns `inat:N`)
    - 165-CONTEXT.md D-03/D-11 (category 3 = provisional sample, occ_id `inat:N`)
  </read_first>
  <action>
    Add a new test in the `describe('occIdFromRow', ...)` block of `src/tests/occurrence.test.ts`:
    `test('returns inat:N for a provisional sample row that has observation_id (new category-3 ARM 2 shape)', ...)`
    asserting `occIdFromRow({ ...BASE_ROW, observation_id: 351027987, is_provisional: true })` equals
    `'inat:351027987'`. Keep the existing `provisionalRow()` (observation_id null → null) test as-is —
    it remains a valid case. Add a one-line comment noting this codifies the corrected category-3
    behavior (D-03/D-11): provisional sample rows now carry the host/plant observation_id and resolve
    to `inat:N`, whereas the legacy mis-defined provisional arm had observation_id null.
    Do NOT modify `src/occurrence.ts` — occIdFromRow priority is unchanged this phase.
  </action>
  <verify>
    <automated>cd /Users/rainhead/dev/beeatlas && npm test -- src/tests/occurrence.test.ts 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `npm test -- src/tests/occurrence.test.ts` passes (exit 0).
    - A new test named with "provisional sample row that has observation_id" asserts `inat:351027987`.
    - The pre-existing `provisionalRow()` → null test still passes unchanged.
    - `src/occurrence.ts` is NOT modified.
  </acceptance_criteria>
  <done>occurrence.test.ts has the new category-3 case and the whole file passes (exit 0).</done>
</task>

</tasks>

<artifacts_this_phase_produces>
This plan contributes:
- `data/dbt/tests/test_no_duplicate_occ_ids.sql` — D-09 occ_id uniqueness guard (singular test, severity:warn).
- A new vitest case in `src/tests/occurrence.test.ts` codifying category-3 (`inat:N`) behavior.
(Other artifacts — the `waba_specimen` source value, the corrected int_* models, `docs/domain-model.md` — are produced by Plans 02/03/04.)
</artifacts_this_phase_produces>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | This plan adds a build-time dbt test and a unit test. No runtime, no user input, no network. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-165-01 | Tampering | `data/dbt/tests/test_no_duplicate_occ_ids.sql` (SQL run at build time) | accept | Test SQL is static repo content authored here, references only `ref('int_combined')`; no user input, no string interpolation. Static-hosted project, no server runtime. |
| T-165-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this plan (RESEARCH §Standard Stack: "no new npm packages or Python packages"). Existing dbt/vitest toolchain only. No legitimacy gate required. |

No high-severity threats. Reasoning documented per planning-context security note.
</threat_model>

<verification>
- `bash data/dbt/run.sh test --select test_no_duplicate_occ_ids` runs and WARNs (not errors) on current data.
- `npm test -- src/tests/occurrence.test.ts` exits 0.
</verification>

<success_criteria>
- The D-09 test exists, witnesses the 4 current duplicate occ_ids as a warning, and does not block the build.
- occurrence.test.ts has the new category-3 case and passes.
- No production code (`src/occurrence.ts`, any int_* model) is touched in this wave.
</success_criteria>

<output>
Create `.planning/phases/165-duplicate-occurrence-rows-shared-occ-id/165-01-SUMMARY.md` when done.
</output>
