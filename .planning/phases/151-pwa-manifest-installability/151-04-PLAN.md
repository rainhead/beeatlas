---
phase: 151-pwa-manifest-installability
plan: 04
type: execute
wave: 2
depends_on: ["151-02", "151-03"]
files_modified:
  - .planning/phases/151-pwa-manifest-installability/151-HUMAN-UAT.md
autonomous: false
requirements: [PWA-03]
must_haves:
  truths:
    - "Offline cold-start in standalone mode is confirmed on a real Android device and a real iOS device (airplane mode, launched from home screen)"
    - "Chrome installability shows zero manifest validation errors on a production-build preview"
    - "The phase does not auto-advance past UAT (UI hint: yes)"
  artifacts:
    - path: ".planning/phases/151-pwa-manifest-installability/151-HUMAN-UAT.md"
      provides: "Real-device offline cold-start checklist (Android + iOS) + DevTools manifest check"
      contains: "airplane mode"
  key_links:
    - from: "151-HUMAN-UAT.md"
      to: "PWA-03"
      via: "real-device offline cold-start scenarios gating the requirement"
      pattern: "PWA-03"
---

<objective>
Produce the human UAT checklist that confirms PWA-03 (offline cold-start in standalone),
plus the manual Chrome-DevTools manifest-validity check (PWA-01) and the iOS status-bar
appearance check (PWA-02) — the verifications that cannot be automated. This plan creates
the checklist and pauses for the human to run it on real devices; the phase must NOT
auto-advance past this UAT.

Purpose: PWA-03 is inherently a real-device, true-offline, standalone-launch behavior that
no jsdom/CI surface can simulate (D-14). It must be confirmed by a human on a real Android
(Chrome install → airplane mode → launch) and a real iOS device (Share → Add to Home Screen
→ airplane mode → launch), with the map+table rendering from cache. The phase carries
`UI hint: yes` and must stop here for human sign-off (memory feedback_uat_ui_phases).

Output: `.planning/phases/151-pwa-manifest-installability/151-HUMAN-UAT.md` and a blocking
human-verify checkpoint.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@.planning/phases/151-pwa-manifest-installability/151-CONTEXT.md
@.planning/phases/151-pwa-manifest-installability/151-RESEARCH.md
@.planning/phases/151-pwa-manifest-installability/151-VALIDATION.md
</context>

<artifacts_this_phase_produces>
See the full inventory in 151-01-PLAN.md `<artifacts_this_phase_produces>`. This plan (04)
creates: `.planning/phases/151-pwa-manifest-installability/151-HUMAN-UAT.md`.
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Author 151-HUMAN-UAT.md — real-device offline cold-start checklist</name>
  <files>.planning/phases/151-pwa-manifest-installability/151-HUMAN-UAT.md</files>
  <read_first>
    - .planning/phases/151-pwa-manifest-installability/151-CONTEXT.md D-13 (lines 95-100) and D-14 (lines 101-106) — the manifest DevTools check + the real-device UAT requirement
    - .planning/phases/151-pwa-manifest-installability/151-VALIDATION.md §Manual-Only Verifications (lines 62-69) — the three manual scenarios + exact instructions
    - .planning/phases/151-pwa-manifest-installability/151-RESEARCH.md §Verification (lines 468-481) — automatable-vs-human split (so the checklist covers only what is genuinely manual)
  </read_first>
  <action>
    Write `151-HUMAN-UAT.md` as a checkbox checklist with three scenarios drawn verbatim from
    VALIDATION §Manual-Only Verifications:
    (1) PWA-01 — Chrome DevTools manifest validity: run a production-build preview on localhost,
    open DevTools → Application → Manifest, confirm zero validation errors and that an install
    affordance is offered.
    (2) PWA-03 Android — install the app via the in-app Install button in Chrome → enable airplane
    mode → launch from the home screen → confirm it opens in standalone (no browser chrome) and the
    map + table render from cache with no network.
    (3) PWA-03 iOS — Safari Share → Add to Home Screen (verify the Install button's A2HS popover
    steps match the real share-sheet flow) → enable airplane mode → launch from home screen →
    confirm standalone + map + table render; also confirm the status bar is legible against the navy
    theme and note whether `apple-mobile-web-app-status-bar-style` should stay `black-translucent` or
    switch to `black` (PWA-02, the deferred UAT decision).
    Each scenario gets a Pass/Fail checkbox, a "device + OS version" field, and a notes line. Add a
    header noting this gates `/gsd-verify-work` and that the phase has `UI hint: yes` (no
    auto-advance, memory feedback_uat_ui_phases). Mark the file's status as `pending` until the human
    records results.
  </action>
  <verify>
    <automated>test -f .planning/phases/151-pwa-manifest-installability/151-HUMAN-UAT.md && grep -q "PWA-03" .planning/phases/151-pwa-manifest-installability/151-HUMAN-UAT.md && grep -qi "airplane mode" .planning/phases/151-pwa-manifest-installability/151-HUMAN-UAT.md && grep -qi "standalone" .planning/phases/151-pwa-manifest-installability/151-HUMAN-UAT.md && echo "UAT checklist present"</automated>
  </verify>
  <acceptance_criteria>
    - `151-HUMAN-UAT.md` exists with three Pass/Fail scenarios: Chrome DevTools manifest validity (PWA-01), Android offline cold-start in standalone (PWA-03), iOS A2HS + offline cold-start + status-bar legibility (PWA-02/PWA-03).
    - Each scenario contains the exact manual instructions (airplane mode, launch from home screen, standalone confirmation, map+table render).
    - The file notes it gates `/gsd-verify-work` and that the phase must not auto-advance (UI hint: yes).
    - File status is `pending`.
  </acceptance_criteria>
  <done>The real-device offline cold-start UAT checklist exists and is ready for the human to execute.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human UAT — offline cold-start in standalone (real devices)</name>
  <files>.planning/phases/151-pwa-manifest-installability/151-HUMAN-UAT.md</files>
  <action>
    Pause execution and hand off to the human to run `151-HUMAN-UAT.md` on real devices. This is a
    blocking human-verify checkpoint — PWA-03 (offline cold-start in standalone) cannot be automated
    (D-14) and the phase carries `UI hint: yes`, so it must NOT auto-advance. Do not mark the phase
    verified until the human records Pass on all three scenarios in the UAT file. Any Fail becomes
    gap-closure input for `/gsd-plan-phase --gaps`.
  </action>
  <what-built>
    The full Phase 151 PWA install surface: a static `/app/manifest.webmanifest` + committed icons
    (Plan 01), the manifest `<link>` + iOS meta on `/app` (Plan 02), and the in-app Install button +
    iOS A2HS popover driven by `beforeinstallprompt` / iOS-Safari detection (Plan 03). The SW
    offline-cold-start machinery this exercises already shipped in Phases 147–149.
  </what-built>
  <how-to-verify>
    Run `151-HUMAN-UAT.md` end to end and record results in that file:
    1. Production-build preview on localhost → Chrome DevTools → Application → Manifest → confirm
       ZERO validation errors and that an install affordance is offered (PWA-01).
    2. Real Android (Chrome): tap the in-app Install button → install → enable airplane mode →
       launch from the home screen → confirm standalone (no browser chrome) + map + table render
       from cache (PWA-03).
    3. Real iOS (Safari): tap the Install button → confirm the A2HS popover steps match the real
       share sheet → Share → Add to Home Screen → enable airplane mode → launch → confirm standalone
       + render; confirm status-bar legibility against the navy theme and decide
       `black-translucent` vs `black` (PWA-02/PWA-03).
    This is the only verification of PWA-03 — it cannot be automated (D-14). The phase carries
    `UI hint: yes` and must not auto-advance past this UAT.
  </how-to-verify>
  <resume-signal>Type "approved" once both real-device offline cold-starts pass and the DevTools manifest check is clean, or describe any failures (which become gap-closure input).</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Installed PWA ↔ OS home screen | The installed app becomes OS-registered home-screen state on the user's own device (the point of the phase). Verified by human UAT; no repo/server boundary is crossed. |
| (no server runtime) | UAT is documentation + manual device testing; no code, no network service, no input handling introduced. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-151-08 | Tampering | UAT checklist artifact | accept | A planning markdown checklist; no runtime surface, no untrusted input. No security risk. |
| T-151-09 | Information Disclosure | offline-cached data on device | accept | The cached `occurrences.db` + GeoJSON are public bee-atlas data (no PII/secrets); caching them on the user's own installed PWA is the intended behavior. |
| T-151-SC | Tampering | npm/pip/cargo installs | mitigate | No package installs in this plan (docs + manual UAT only). No supply-chain surface. |
</threat_model>

<verification>
- `151-HUMAN-UAT.md` exists and enumerates the three manual scenarios.
- The blocking human-verify checkpoint pauses execution for real-device sign-off; results recorded in the UAT file.
</verification>

<success_criteria>
- Real-device offline cold-start confirmed on Android + iOS (standalone, airplane mode, map+table render).
- Chrome DevTools manifest check clean (no validation errors).
- Phase paused for human sign-off; no auto-advance past UAT.
</success_criteria>

<output>
Create `.planning/phases/151-pwa-manifest-installability/151-04-SUMMARY.md` when done (after the human approves the UAT).
</output>
