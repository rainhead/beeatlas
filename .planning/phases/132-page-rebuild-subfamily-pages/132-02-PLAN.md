---
phase: 132-page-rebuild-subfamily-pages
plan: 02
type: execute
wave: 2
depends_on: ["132-01"]
files_modified:
  - data/species_export.py
  - data/nightly.sh
  - scripts/fetch-data.sh
  - scripts/make-local-manifest.js
  - data/tests/test_species_export.py
autonomous: true
requirements: [PAGE-01, PAGE-03]
must_haves:
  truths:
    - "species-export emits public/data/higher_taxa.json from the dbt rollup parquet (D-01/D-03)"
    - "higher_rank_taxon_ids.json and its Python builder are retired in the same change (D-03)"
    - "A pre-generation slug-collision check enumerates every taxon's public URL across all ranks and HARD-FAILS the build on any genuine collision, with no auto-suffix (D-07, PAGE-03)"
    - "The collision check compares full URL paths so genus Bombus and subgenus Bombus do not false-alarm"
    - "higher_taxa.json is wired into nightly.sh S3 upload + manifest, fetch-data.sh download, and make-local-manifest.js"
  artifacts:
    - path: "data/species_export.py"
      provides: "_build_higher_taxa + _check_slug_collisions; _build_higher_rank_taxon_ids removed"
      contains: "_build_higher_taxa"
    - path: "data/nightly.sh"
      provides: "higher_taxa hashed upload + manifest key (replaces higher_rank_taxon_ids)"
      contains: "higher_taxa"
    - path: "scripts/make-local-manifest.js"
      provides: "higher_taxa manifest entry for local dev"
      contains: "higher_taxa"
  key_links:
    - from: "data/species_export.py"
      to: "target/sandbox/higher_taxa.parquet"
      via: "read_parquet in _build_higher_taxa"
      pattern: "higher_taxa\\.parquet"
    - from: "data/species_export.py"
      to: "public/data/higher_taxa.json"
      via: "json write"
      pattern: "higher_taxa\\.json"
---

<objective>
Rewire the Python export layer onto the new rollup: add `_build_higher_taxa()` that reads
`higher_taxa.parquet` and writes `public/data/higher_taxa.json`; atomically retire
`_build_higher_rank_taxon_ids()` and `higher_rank_taxon_ids.json` (D-03); add the pre-generation
slug-collision HARD-FAIL gate across all ranks (D-07, PAGE-03); and wire the new artifact into
nightly.sh (S3 upload + manifest), fetch-data.sh, and make-local-manifest.js.

Purpose: Make the dbt rollup consumable as a JSON artifact, eliminate the redundant
higher_rank_taxon_ids artifact, and enforce collision-free public URLs at build time.
Output: `public/data/higher_taxa.json`; the retired old artifact; a fail-loud collision gate.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/132-page-rebuild-subfamily-pages/132-CONTEXT.md
@.planning/phases/132-page-rebuild-subfamily-pages/132-RESEARCH.md
@.planning/phases/132-page-rebuild-subfamily-pages/132-PATTERNS.md

<interfaces>
<!-- From PATTERNS.md §species_export.py and §nightly.sh -->
URL scheme per rank (raw capitalized names; only species slugs lowercased via domain.slugify):
  genus     -> /species/{name}/
  subgenus  -> /species/{genus}/{name}/
  tribe     -> /species/tribe/{name}/
  subfamily -> /species/subfamily/{name}/
  species   -> /species/{slug}/   (slug already computed in export_species_parquet)

higher_taxa.json rows: list of dicts with keys taxon_id, rank, name, family, subfamily, tribe,
genus, specimen_count, inat_obs_count, occurrence_count, species_count, member_taxon_ids.

nightly.sh: _upload_hashed "$EXPORT_DIR/<file>" "<key>" emits a hashed name; manifest.json template
maps "<key>": "$<key>_name". Current line uses higher_rank_taxon_ids; replace with higher_taxa.
species-maps/ S3 sync is recursive (no change needed). CloudFront /data/species-maps/* covers SVGs.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wave 0 collision tests + _check_slug_collisions hard-fail gate</name>
  <files>data/tests/test_species_export.py, data/species_export.py</files>
  <read_first>
    - data/species_export.py (export_species_parquet artifact-write block; existing slug post-step using domain.slugify; _build_higher_rank_taxon_ids at lines ~85-105)
    - data/domain.py (slugify — only species slugs are lowercased/normalized)
    - data/tests/test_species_export.py (existing test patterns + sandbox-gated fixtures)
    - .planning/phases/132-page-rebuild-subfamily-pages/132-PATTERNS.md (§species_export.py — _check_slug_collisions full pattern; §Fail-Loud Assert Gate)
    - .planning/phases/132-page-rebuild-subfamily-pages/132-RESEARCH.md (§Pattern 5 URL scheme, Pitfall 5 Bombus false-alarm)
  </read_first>
  <behavior>
    - Test: _check_slug_collisions raises AssertionError on a synthetic collision (two distinct taxa, same URL)
    - Test: _check_slug_collisions does NOT raise for genus Bombus (/species/Bombus/) vs subgenus Bombus (/species/Bombus/Bombus/) — distinct full URLs
    - Test (sandbox-gated): _check_slug_collisions passes on current real data (no collision)
  </behavior>
  <action>
    Add new test cases to `data/tests/test_species_export.py`: a synthetic-collision case asserting
    `pytest.raises(AssertionError)`, the Bombus genus-vs-subgenus non-collision case, and a
    sandbox-gated clean-real-data case. Then add `_check_slug_collisions(higher_taxa_rows, species_rows)`
    to `data/species_export.py` per the PATTERNS.md analog: build a `seen` dict keyed on the FULL URL
    string (using the per-rank URL scheme; raw capitalized names for higher ranks, slugify only for
    species), and raise a descriptive `AssertionError` naming both colliding taxa and the URL, stating
    "no auto-suffix" (D-07). Do not call it yet — wiring happens in Task 2.
  </action>
  <verify>
    <automated>cd data && uv run pytest tests/test_species_export.py -x -k "collision"</automated>
  </verify>
  <acceptance_criteria>
    - Synthetic-collision test asserts AssertionError and passes
    - Bombus genus-vs-subgenus test confirms NO collision (distinct full URLs — Pitfall 5)
    - `_check_slug_collisions` keys on full URL strings, not bare names; raises with both taxa + URL + "no auto-suffix" language
  </acceptance_criteria>
  <done>Collision gate implemented and unit-tested; ready to wire into the export flow.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: _build_higher_taxa, retire higher_rank_taxon_ids, wire collision gate + assertions</name>
  <files>data/species_export.py, data/tests/test_species_export.py</files>
  <read_first>
    - data/species_export.py (export_species_parquet call sequence; _build_higher_rank_taxon_ids call site to remove; ASSETS_DIR/DBT_SANDBOX_DIR constants; post-write verify pattern lines ~207-228)
    - data/run.py (STEPS — confirm species-export runs after dbt-build; no new step needed per RESEARCH §Pattern 6)
    - .planning/phases/132-page-rebuild-subfamily-pages/132-PATTERNS.md (§species_export.py — _build_higher_taxa pattern, artifact-write block, post-write verify 12-subfamily assertion)
    - .planning/phases/132-page-rebuild-subfamily-pages/132-RESEARCH.md (§Pattern 6 atomic retirement sequence; §Runtime State Inventory)
  </read_first>
  <behavior>
    - Test (sandbox-gated): higher_taxa.json is written and non-empty after export; contains exactly 12 subfamily rows; no 'Eumeninae'
    - Test (sandbox-gated): higher_rank_taxon_ids.json is NOT written by export (retirement)
    - Test (sandbox-gated): export run invokes _check_slug_collisions and completes without raising on real data
  </behavior>
  <action>
    Add `_build_higher_taxa(con)` to `data/species_export.py`: read `DBT_SANDBOX_DIR/higher_taxa.parquet`
    (raise FileNotFoundError with a "run dbt build first" message if absent), serialize all rows to
    `ASSETS_DIR/higher_taxa.json` via `json.dumps(..., sort_keys=True, indent=2)`, print row count + bytes,
    assert non-empty, assert exactly 12 subfamily rows (post-write verify pattern). In
    `export_species_parquet`, call `higher_taxa_rows = _build_higher_taxa(con)` then
    `_check_slug_collisions(higher_taxa_rows, species_rows)` (from Task 1) after slug computation.
    Remove the `_build_higher_rank_taxon_ids` function and its call + the `higher_rank_taxon_ids.json`
    write (atomic retirement, D-03). Leave the stale `public/data/higher_rank_taxon_ids.json` file on
    disk for now — Plan 03 confirms _data/species.js no longer reads it before nightly.sh stops uploading
    it; the file removal is non-load-bearing. Extend test_species_export.py with the sandbox-gated
    assertions in <behavior>.
  </action>
  <verify>
    <automated>bash data/dbt/run.sh build --select higher_taxa && cd data && uv run python run.py --only species-export 2>/dev/null || cd /home/peter/dev/beeatlas/data && uv run pytest tests/test_species_export.py -x</automated>
  </verify>
  <acceptance_criteria>
    - After `bash data/dbt/run.sh build` + the species-export step, `public/data/higher_taxa.json` exists, is non-empty, and JSON-parses to a list with exactly 12 rows where rank=='subfamily' and none named 'Eumeninae'
    - `_build_higher_rank_taxon_ids` no longer exists in species_export.py (`grep -v '^#' data/species_export.py | grep -c _build_higher_rank_taxon_ids` returns 0) and higher_rank_taxon_ids.json is not re-emitted by export
    - export raises AssertionError if a slug collision is present; passes clean on real data
    - `cd data && uv run pytest tests/test_species_export.py -x` passes
  </acceptance_criteria>
  <done>higher_taxa.json is produced from the rollup, the old artifact builder is gone, and the collision gate runs as part of export.</done>
</task>

<task type="auto">
  <name>Task 3: Wire higher_taxa.json into nightly.sh, fetch-data.sh, make-local-manifest.js</name>
  <files>data/nightly.sh, scripts/fetch-data.sh, scripts/make-local-manifest.js</files>
  <read_first>
    - data/nightly.sh (_upload_hashed call block; manifest.json template; the higher_rank_taxon_ids line to replace; recursive species-maps/ sync; CloudFront invalidation paths)
    - scripts/fetch-data.sh (download loop list ~line 22)
    - scripts/make-local-manifest.js (manifest object ~lines 11-23)
    - .planning/phases/132-page-rebuild-subfamily-pages/132-PATTERNS.md (§nightly.sh, §fetch-data.sh + make-local-manifest.js — exact line replacements)
    - .planning/phases/132-page-rebuild-subfamily-pages/132-RESEARCH.md (§Pattern 6, Open Question A4)
  </read_first>
  <action>
    In `data/nightly.sh`, replace the `higher_rank_taxon_ids` `_upload_hashed` line with
    `higher_taxa_name=$(_upload_hashed "$EXPORT_DIR/higher_taxa.json" "higher_taxa")` and the manifest
    template entry `"higher_rank_taxon_ids": "$higher_rank_name"` with `"higher_taxa": "$higher_taxa_name"`.
    Do NOT touch the recursive `species-maps/` S3 sync or `/data/species-maps/*` CloudFront path — they
    already cover the new subfamily SVGs. In `scripts/fetch-data.sh`, add `higher_taxa.json` to the
    download list (per A4; higher_rank_taxon_ids.json was never downloaded here). In
    `scripts/make-local-manifest.js`, add `higher_taxa: 'higher_taxa.json'` to the manifest object.
    Verify nightly.sh remains valid bash.
  </action>
  <verify>
    <automated>bash -n data/nightly.sh && node --check scripts/make-local-manifest.js && bash -n scripts/fetch-data.sh && grep -v '^#' data/nightly.sh | grep -c higher_taxa</automated>
  </verify>
  <acceptance_criteria>
    - `bash -n data/nightly.sh` and `bash -n scripts/fetch-data.sh` pass; `node --check scripts/make-local-manifest.js` passes
    - `grep -v '^#' data/nightly.sh | grep -c higher_taxa` is ≥ 2 (upload line + manifest key) and `grep -c higher_rank_taxon_ids data/nightly.sh` is 0
    - `scripts/make-local-manifest.js` contains `higher_taxa: 'higher_taxa.json'`; `scripts/fetch-data.sh` download list includes `higher_taxa.json`
  </acceptance_criteria>
  <done>The new artifact deploys via nightly S3 + manifest and is fetchable for local dev; the retired artifact is removed from the deploy path.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| dbt parquet → JSON artifact → public URLs | The export step is where all final public URLs are first known; a silent slug collision here ships broken/colliding public links |
| nightly.sh → S3/CloudFront | A mis-wired manifest key ships a stale or missing artifact, breaking the site build |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-132-05 | Tampering | two distinct taxa produce the same public URL (PAGE-03) | mitigate | `_check_slug_collisions` hard-fails the build on full-URL collision; unit test on synthetic collision + Bombus non-collision; no auto-suffix (silent URL changes break inbound links) |
| T-132-06 | Denial of service | stale/missing higher_taxa.json after retirement breaks the build | mitigate | atomic D-03 sequence; FileNotFoundError if parquet absent; `assert len > 0`; nightly.sh manifest key swapped, not duplicated |
| T-132-07 | Repudiation | higher_rank_taxon_ids.json still uploaded/referenced post-retirement | mitigate | grep gate: 0 occurrences of higher_rank_taxon_ids in nightly.sh; Plan 03 removes the species.js read before file deletion |
</threat_model>

<verification>
- `cd data && uv run pytest tests/test_species_export.py -x` passes (collision + retirement + 12-subfamily)
- `bash -n data/nightly.sh && bash -n scripts/fetch-data.sh && node --check scripts/make-local-manifest.js`
- After a full `bash data/dbt/run.sh build` + species-export: `public/data/higher_taxa.json` exists and parses to 12 subfamily rows
</verification>

<success_criteria>
- higher_taxa.json produced from the rollup; higher_rank_taxon_ids builder + artifact retired (D-03)
- Slug-collision HARD-FAIL gate runs at export and is clean on real data (PAGE-03, success criterion 3)
- New artifact wired through nightly S3 + manifest + local fetch
</success_criteria>

<output>
Create `.planning/phases/132-page-rebuild-subfamily-pages/132-02-SUMMARY.md` when done.
Record: confirmation collision check is clean on real data, the higher_taxa.json row count, and that
nightly.sh no longer references higher_rank_taxon_ids.
</output>
