---
phase: 10-build-integration-and-verification
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .github/workflows/deploy.yml
autonomous: false
requirements:
  - INAT-03

must_haves:
  truths:
    - "CI build job runs cache-restore, then build, then cache-upload — in that order"
    - "S3_BUCKET_NAME env var is available to all three steps in the build job"
    - "CI deploy job configures AWS credentials before running npm run build"
    - "CI passes green on push to main — both build and deploy jobs complete without error"
  artifacts:
    - path: ".github/workflows/deploy.yml"
      provides: "Wired CI with S3 cache round-trip and correct credential ordering"
      contains: "cache-restore"
  key_links:
    - from: "build job steps"
      to: "cache_restore.sh"
      via: "npm run cache-restore"
      pattern: "cache-restore"
    - from: "cache_restore.sh"
      to: "S3_BUCKET_NAME"
      via: "bash ${VAR:?} expansion"
      pattern: "S3_BUCKET_NAME"
    - from: "deploy job"
      to: "aws-actions/configure-aws-credentials@v4"
      via: "step ordering"
      pattern: "Configure AWS credentials"
---

<objective>
Wire S3 cache scripts into the CI workflow and fix credential ordering in the deploy job, then verify CI passes green on push to main.

Purpose: Phase 9 delivered all pipeline code, but the CI workflow still calls bare `npm run build` without the cache scripts, and `S3_BUCKET_NAME` is never passed to the steps that need it. The deploy job also has AWS credentials configured AFTER `npm run build`, which will cause it to fail when `build-data.sh` calls `cache_restore.sh`.

Output: Updated `.github/workflows/deploy.yml` with correct step ordering; CI green on push to main.
</objective>

<execution_context>
@/Users/rainhead/.claude/get-shit-done/workflows/execute-plan.md
@/Users/rainhead/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/10-build-integration-and-verification/10-RESEARCH.md

<interfaces>
<!-- Current deploy.yml structure — executor must preserve all existing steps, only reorder/add -->

build job (current):
  steps:
    1. actions/checkout@v4
    2. astral-sh/setup-uv@v5
    3. actions/setup-node@v4 (node 22, npm cache)
    4. Install dependencies: npm ci
    5. Configure AWS credentials via OIDC (aws-actions/configure-aws-credentials@v4, role-to-assume: vars.AWS_DEPLOYER_ROLE_ARN, aws-region: us-west-2)
    6. Build: npm run build   ← needs to become 3 steps with S3_BUCKET_NAME

deploy job (current):
  needs: build
  if: github.ref == 'refs/heads/main'
  environment: production
  steps:
    1. actions/checkout@v4
    2. astral-sh/setup-uv@v5
    3. actions/setup-node@v4
    4. Install dependencies: npm ci
    5. Build: npm run build   ← AWS creds NOT configured yet — BUG
    6. Configure AWS credentials via OIDC   ← must move BEFORE step 5
    7. Sync to S3: aws s3 sync frontend/dist/ s3://${{ vars.S3_BUCKET_NAME }} --delete
    8. Invalidate CloudFront cache

Env vars available as GitHub Actions vars (not secrets):
  - vars.S3_BUCKET_NAME
  - vars.AWS_DEPLOYER_ROLE_ARN
  - vars.CF_DISTRIBUTION_ID
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update deploy.yml — wire cache scripts and fix credential ordering</name>
  <files>.github/workflows/deploy.yml</files>
  <action>
Make two sets of changes to `.github/workflows/deploy.yml`:

**build job — add job-level env and split build step:**

Add a job-level `env:` block with `S3_BUCKET_NAME: ${{ vars.S3_BUCKET_NAME }}` to the `build` job. Then replace the single "Build: npm run build" step with three explicit named steps (keeping everything else unchanged):

```yaml
- name: Restore S3 cache
  run: npm run cache-restore

- name: Build
  run: npm run build

- name: Upload S3 cache
  run: npm run cache-upload
```

The job-level env means `S3_BUCKET_NAME` is available to all steps in the build job (including the AWS credentials step, which doesn't use it but that's fine).

**deploy job — fix credential ordering and add cache steps:**

Move the "Configure AWS credentials via OIDC" step to BEFORE "Build" (it's currently after "Build" — this is the bug). Then apply the same three-step split and job-level env as in the build job:

1. actions/checkout@v4
2. astral-sh/setup-uv@v5
3. actions/setup-node@v4
4. Install dependencies: npm ci
5. Configure AWS credentials via OIDC  ← moved here (was after Build)
6. Restore S3 cache: npm run cache-restore
7. Build: npm run build
8. Upload S3 cache: npm run cache-upload
9. Sync to S3
10. Invalidate CloudFront cache

Add job-level `env: S3_BUCKET_NAME: ${{ vars.S3_BUCKET_NAME }}` to the deploy job as well.

Do NOT change any other aspect of the file — preserve permissions blocks, action versions, `needs: build`, `if: github.ref`, environment: production, the sync and invalidate steps exactly as they are.
  </action>
  <verify>
    <automated>cd /Users/rainhead/dev/beeatlas && python3 -c "
import yaml, sys
with open('.github/workflows/deploy.yml') as f:
    wf = yaml.safe_load(f)

build = wf['jobs']['build']
deploy = wf['jobs']['deploy']

# Check build job env
assert build.get('env', {}).get('S3_BUCKET_NAME') == '\${{ vars.S3_BUCKET_NAME }}', 'build job missing S3_BUCKET_NAME env'

# Check build job step names
build_step_names = [s.get('name','') for s in build['steps']]
restore_idx = build_step_names.index('Restore S3 cache')
build_idx = build_step_names.index('Build')
upload_idx = build_step_names.index('Upload S3 cache')
assert restore_idx < build_idx < upload_idx, 'build job step order wrong'

# Check deploy job env
assert deploy.get('env', {}).get('S3_BUCKET_NAME') == '\${{ vars.S3_BUCKET_NAME }}', 'deploy job missing S3_BUCKET_NAME env'

# Check deploy job credential ordering
deploy_step_names = [s.get('name','') for s in deploy['steps']]
creds_idx = next(i for i,s in enumerate(deploy['steps']) if 'configure-aws-credentials' in str(s.get('uses','')))
build_d_idx = deploy_step_names.index('Build')
assert creds_idx < build_d_idx, f'deploy job: AWS creds (step {creds_idx}) must come before Build (step {build_d_idx})'

print('OK: deploy.yml structure correct')
"
    </automated>
  </verify>
  <done>
`deploy.yml` has: build job with S3_BUCKET_NAME env and cache-restore/build/cache-upload steps; deploy job with AWS credentials configured before npm run build, same cache steps, and S3_BUCKET_NAME env. YAML validation passes.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify CI green — S3 cache round-trip and deploy pass</name>
  <what-built>
Updated `.github/workflows/deploy.yml` — S3 cache scripts wired into both build and deploy jobs, credential ordering fixed in deploy job.
  </what-built>
  <how-to-verify>
1. Push the branch to origin: `git push origin HEAD`
2. Open the GitHub Actions tab: https://github.com/rainhead/beeatlas/actions
3. Wait for the workflow run to start on your branch
4. Confirm the `build` job shows three named steps: "Restore S3 cache", "Build", "Upload S3 cache"
5. Confirm "Restore S3 cache" prints "not in cache" or downloads files (no credential error, no S3_BUCKET_NAME error)
6. Confirm "Upload S3 cache" completes without error
7. The `build` job must pass green
8. If pushing to main: confirm the `deploy` job also passes green
  </how-to-verify>
  <resume-signal>Type "approved" if CI passes, or describe which step failed</resume-signal>
</task>

</tasks>

<verification>
- `.github/workflows/deploy.yml` has `S3_BUCKET_NAME` at job level for both build and deploy jobs
- build job: cache-restore → build → cache-upload in order
- deploy job: AWS credentials step before Build step
- CI build job passes green (S3 cache round-trip completes without error)
- CI deploy job passes green on push to main
</verification>

<success_criteria>
1. `npm run build` is preceded by `npm run cache-restore` and followed by `npm run cache-upload` in both CI jobs
2. `S3_BUCKET_NAME` env var is available in both jobs so cache scripts do not fail with "not set"
3. CI passes green on push to main — build and deploy jobs both complete without error
</success_criteria>

<output>
After completion, create `.planning/phases/10-build-integration-and-verification/10-01-SUMMARY.md`
</output>
