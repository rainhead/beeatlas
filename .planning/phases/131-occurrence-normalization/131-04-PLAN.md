---
phase: 131-occurrence-normalization
plan: 04
type: execute
wave: 3
depends_on: ["131-03"]
files_modified:
  - .planning/phases/131-occurrence-normalization/131-VERIFICATION.md
autonomous: false
requirements: [NORM-02]
must_haves:
  truths:
    - "A pre-change baseline (occurrences.db byte size, gzipped transfer weight) is captured and recorded"
    - "The post-change occurrences.db byte size and gzipped transfer weight are recorded and are measurably smaller than the baseline"
    - "The in-browser tablesReady timing is recorded and does not regress from the v4.3 ~250 ms baseline"
    - "A grep audit confirms no remaining live src/ references to the dropped column names (outside the documented out-of-scope checklist reads)"
  artifacts:
    - path: ".planning/phases/131-occurrence-normalization/131-VERIFICATION.md"
      provides: "Before/after size + transfer weight + tablesReady measurement record (NORM-02) and the grep-audit result"
      contains: "occurrences.db"
  key_links:
    - from: "131-VERIFICATION.md"
      to: "occurrences.db"
      via: "recorded before/after byte size + gzip weight"
      pattern: "occurrences.db"
---

<objective>
Record the NORM-02 measurement (D-05, record-only) and run the final phase gate. Capture the
before/after `occurrences.db` byte size and gzipped transfer weight and the in-browser
`tablesReady` timing, prove the size win, run the grep audit, and confirm the full phase gate
(`npm test && npm run typecheck && bash data/dbt/run.sh build`) is green. A human verifies the
size reduction is real and the app still loads/renders names correctly.

Purpose: NORM-02 requires a measurable transfer-weight + DB-size reduction recorded against a
captured pre-change baseline, with `tablesReady` not regressing from the v4.3 ~250 ms baseline.
There is deliberately NO new automated gate (D-05) — the dbt contract already prevents column
re-addition and an absolute size ceiling would fight legitimate data growth.
Output: 131-VERIFICATION.md with the measurement table + grep-audit result; phase gate green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/131-occurrence-normalization/131-CONTEXT.md
@.planning/phases/131-occurrence-normalization/131-RESEARCH.md
@.planning/phases/131-occurrence-normalization/131-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Regenerate occurrences.db, capture before/after size + transfer weight, run grep audit, write 131-VERIFICATION.md</name>
  <files>.planning/phases/131-occurrence-normalization/131-VERIFICATION.md</files>
  <read_first>
    - .planning/phases/131-occurrence-normalization/131-RESEARCH.md (NORM-02 Measurement Procedure — exact commands; size win estimate ~4 MB; tablesReady ~250 ms baseline)
    - .planning/phases/131-occurrence-normalization/131-VALIDATION.md (Manual-Only Verifications table; grep-audit Wave 0 requirement)
    - .planning/phases/131-occurrence-normalization/131-CONTEXT.md (D-05 record-only; D-02 totalSpecimens unaffected)
    - CLAUDE.md "Running Locally" (`cd data && python run.py` / `uv run python run.py` against data/beeatlas.duckdb; `bash data/dbt/run.sh build`)
  </read_first>
  <action>
    Determine the PRE-change baseline: if a baseline `occurrences.db` size + gzip weight was not
    captured before Plan 03, recover it from git (check out the pre-Plan-03 `data/export/`
    artifact or the prior `occurrences.db` from S3/local cache) and record `ls -la` byte size and
    `gzip -c <db> | wc -c`; if a true pre-change baseline cannot be recovered, state that
    explicitly and use the v4.3 documented baseline as the reference, noting the limitation.
    Regenerate the POST-change artifact via the local pipeline (`bash data/dbt/run.sh build` then
    the sqlite export step per CLAUDE.md `data/run.py`), then record the post-change
    `occurrences.db` byte size and `gzip -c data/export/occurrences.db | wc -c`. Run the grep
    audit `grep -rn "scientificName\|specimen_inat_taxon_name" src/ --include=*.ts | grep -v test`
    and record that the only remaining matches are the documented out-of-scope checklist.parquet
    reads in bee-map.ts (and rank-label uses in spa-link.ts/taxa.ts/url-state.ts), with NO live
    occurrences-mart readers. Write `131-VERIFICATION.md` with: a before/after measurement table
    (DB bytes, gzip bytes, delta + percent reduction), a placeholder row for the human-supplied
    `tablesReady` timing (filled in Task 2), the grep-audit result, and the date. Confirm the
    size delta is negative (smaller). Do NOT add any automated size gate (D-05).
  </action>
  <verify>
    <automated>test -f .planning/phases/131-occurrence-normalization/131-VERIFICATION.md && grep -q "occurrences.db" .planning/phases/131-occurrence-normalization/131-VERIFICATION.md && grep -qiE "reduction|smaller|delta|-[0-9]" .planning/phases/131-occurrence-normalization/131-VERIFICATION.md</automated>
  </verify>
  <acceptance_criteria>
    - `131-VERIFICATION.md` exists and contains a before/after table with the post-change `occurrences.db` byte size and `gzip -c … | wc -c` weight
    - The recorded post-change size is smaller than the recorded/referenced baseline (delta is negative); the percent reduction is stated
    - The grep-audit result is recorded showing no remaining live occurrences-mart readers of the 4 dropped column names
    - `bash data/dbt/run.sh build` exits 0 (re-confirmed during regeneration)
    - No new automated size gate was added to nightly.sh or any test (D-05)
  </acceptance_criteria>
  <done>131-VERIFICATION.md records the measurable size + transfer-weight reduction and the clean grep audit; a tablesReady row awaits the human check.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human verifies the size win, tablesReady timing, and correct name rendering</name>
  <files>.planning/phases/131-occurrence-normalization/131-VERIFICATION.md</files>
  <action>Pause for the human to perform the verification steps below. When approved, record the observed tablesReady timing in 131-VERIFICATION.md and mark the phase gate confirmed. This is a verification checkpoint — no source changes.</action>
  <what-built>
    Phase 131 dropped 4 denormalized rank columns from the occurrences mart + geo_blob (7-field
    layout), migrated the table Species column and provisional detail name to a JOIN-resolved
    display_name, deleted the dead summary/count/taxaOptions paths, and recorded the
    occurrences.db size reduction in 131-VERIFICATION.md.
  </what-built>
  <how-to-verify>
    1. Open 131-VERIFICATION.md and confirm the post-change occurrences.db is measurably smaller
       than the baseline (byte size and gzipped transfer weight).
    2. Run `npm run build` then `npm run dev` (or load the production build) and open the app in a
       browser. In the dev console read the `[BENCHMARK] data-loaded: N ms` / tablesReady timing
       and confirm it does NOT regress from the v4.3 ~250 ms baseline. Record the observed value
       in 131-VERIFICATION.md's tablesReady row.
    3. In the table view, confirm the Species column shows real taxon names (e.g. "Bombus
       vosnesenskii") for identified rows and "No Determination" for unidentified (taxon_id NULL)
       rows — never blank.
    4. Open an occurrence detail for a provisional (WABA sample) row and confirm the provisional
       identification shows the taxon name (or "identification pending" when none) — never blank.
    5. Confirm the map still renders points with correct recency styling and source badges (this
       proves the geo_blob source-at-index-6 decode is correct — Pitfall 1).
  </how-to-verify>
  <resume-signal>Type "approved" once the size reduction, tablesReady timing, and name rendering are confirmed, or describe any issue.</resume-signal>
  <verify><human-check>Human confirms size reduction in 131-VERIFICATION.md, tablesReady not regressed from ~250 ms, and Species/provisional names render (never blank) with correct map source/recency badges.</human-check></verify>
  <done>Human approves: size win real, tablesReady within baseline, names render correctly, observed tablesReady recorded in 131-VERIFICATION.md.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | This plan records measurements and runs a human verification — no new runtime surface |

## STRIDE Threat Register

Per RESEARCH.md Security Domain: no new authentication, sessions, or user-controlled inputs in
this phase. This plan only measures artifacts and verifies rendering.

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-131-06 | Tampering | geo_blob positional decode | mitigate | Human step 5 visually confirms correct source/recency badges — the runtime check for the Pitfall-1 silent-wrong-data mode |
| T-131-SC | Tampering | npm/pip/cargo installs | accept | No package installs (RESEARCH.md: "No new packages") — nothing to audit |
</threat_model>

<verification>
- 131-VERIFICATION.md records before/after occurrences.db byte size + gzip weight (delta negative)
  and the human-supplied tablesReady timing (≤ ~250 ms baseline).
- Grep audit recorded: no live occurrences-mart readers of the dropped columns.
- Phase gate green: `npm test && npm run typecheck && bash data/dbt/run.sh build` all exit 0.
</verification>

<success_criteria>
NORM-02 satisfied: a measurable DB-size + transfer-weight reduction is recorded against a captured
baseline; tablesReady does not regress from ~250 ms; the human confirms names render correctly and
the size win is real. Phase gate green.
</success_criteria>

<output>
Create `.planning/phases/131-occurrence-normalization/131-04-SUMMARY.md` when done.
</output>
