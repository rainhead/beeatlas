---
phase: 131-occurrence-normalization
plan: 02
type: execute
wave: 1
depends_on: ["131-01"]
files_modified:
  - src/filter.ts
  - src/bee-table.ts
  - src/bee-occurrence-detail.ts
autonomous: true
requirements: [NORM-03]
must_haves:
  truths:
    - "queryTablePage, queryListPage, and queryAllFiltered LEFT JOIN taxa on taxon_id and select t.name AS display_name with o.-prefixed occurrence columns"
    - "OccurrenceRow has display_name and no longer has scientificName/genus/family/specimen_inat_taxon_name"
    - "OCCURRENCE_COLUMNS no longer lists scientificName/genus/family/specimen_inat_taxon_name (32 entries)"
    - "bee-table Species column reads display_name; null renders 'No Determination', never blank"
    - "bee-occurrence-detail _renderProvisional reads row.display_name; null renders 'identification pending'"
  artifacts:
    - path: "src/filter.ts"
      provides: "JOIN-resolved display_name in the three page queries; slimmed OccurrenceRow/OCCURRENCE_COLUMNS"
      contains: "display_name"
    - path: "src/bee-table.ts"
      provides: "Species column on display_name"
      contains: "display_name"
    - path: "src/bee-occurrence-detail.ts"
      provides: "_renderProvisional on display_name"
      contains: "display_name"
  key_links:
    - from: "src/filter.ts queryTablePage"
      to: "taxa table"
      via: "LEFT JOIN taxa t ON t.taxon_id = o.taxon_id"
      pattern: "LEFT JOIN taxa"
    - from: "src/bee-table.ts Species column"
      to: "OccurrenceRow.display_name"
      via: "dataField"
      pattern: "dataField:\\s*'display_name'"
    - from: "src/bee-occurrence-detail.ts _renderProvisional"
      to: "OccurrenceRow.display_name"
      via: "row.display_name"
      pattern: "row\\.display_name"
---

<objective>
Migrate the NORM-03 query + display layer (D-07, D-08). Resolve taxon display names via a
SQL LEFT JOIN to the `taxa` table inside `queryTablePage` / `queryListPage` / `queryAllFiltered`
(RESEARCH Option A), drop the 4 denormalized columns from `OccurrenceRow` + `OCCURRENCE_COLUMNS`,
and point the two live display consumers (`bee-table` Species column, `bee-occurrence-detail`
`_renderProvisional`) at `display_name`. After this plan the Wave 0 `filter.test.ts` JOIN test
and `bee-table.test.ts` fixtures go GREEN.

Purpose: NORM-03 requires every live display consumer of the dropped columns to be migrated in
the same change. The JOIN is local (taxa is in occurrences.db, indexed on the taxon_id PK), has
no cache-timing dependency, and honors Phase 130 D-07 (taxon_id IS NULL → "No Determination",
never blank).
Output: filter.ts query/type changes + two presenter updates; filter.test.ts + bee-table.test.ts GREEN.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/131-occurrence-normalization/131-CONTEXT.md
@.planning/phases/131-occurrence-normalization/131-RESEARCH.md
@.planning/phases/131-occurrence-normalization/131-PATTERNS.md
@.planning/phases/131-occurrence-normalization/131-01-SUMMARY.md

<interfaces>
<!-- Contracts the executor needs; from RESEARCH.md + PATTERNS.md. Use directly. -->

OccurrenceRow (src/filter.ts) — RETAIN taxon_id; DROP scientificName (L50), genus (L53),
family (L54), specimen_inat_taxon_name (L67); ADD `display_name: string | null` (JOIN alias,
NOT a mart column). Note: `canonical_name` is a mart column already absent from OCCURRENCE_COLUMNS —
this establishes that the column list is intentionally a subset of the mart.

OCCURRENCE_COLUMNS (src/filter.ts L79-87) — DROP 'scientificName' (L81), 'genus' + 'family'
(L82), 'specimen_inat_taxon_name' (L85). Result: 36 → 32 entries. display_name is NOT added
here (it is a JOIN alias, not a mart column).

JOIN restructure (queryTablePage L172-213, queryListPage L401+, queryAllFiltered L150-170):
  selectCols = OCCURRENCE_COLUMNS.map(c => `o.${c}`).join(', ') + ', t.name AS display_name'
  FROM occurrences o LEFT JOIN taxa t ON t.taxon_id = o.taxon_id WHERE ...
  (the `o.` prefix is REQUIRED — taxa has its own taxon_id; bare SELECT is ambiguous. Pitfall 4.)

Integer-interpolation analog (filter.ts L235-241, T-130-01): the existing descendant subquery
embeds `${f.taxonId}` directly — both sides DB-origin integers. The JOIN ON clause follows the
same integer-safe pattern; no user-controlled strings.

bee-table Species ColumnDef (src/bee-table.ts L43): dataField 'scientificName' → 'display_name';
keep `nullLabel: 'No Determination'`. Render loop at L382 is unchanged (null → nullLabel fires).

_renderProvisional (src/bee-occurrence-detail.ts L235-238): `row.specimen_inat_taxon_name` →
`row.display_name`; truthy → `html<em>${row.display_name}</em>`; null → existing
`<span class="hint">identification pending</span>` branch. Null-guard idiom analog:
`_renderCollectorGroup` L189-193.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add the LEFT JOIN + display_name and drop the 4 columns from OccurrenceRow / OCCURRENCE_COLUMNS</name>
  <files>src/filter.ts</files>
  <read_first>
    - src/filter.ts (OccurrenceRow L40-77; OCCURRENCE_COLUMNS L79-87; queryTablePage L172-213; queryAllFiltered L150-170; queryListPage L401-450; the sqlite3.exec callback that maps columnNames→row keys)
    - .planning/phases/131-occurrence-normalization/131-PATTERNS.md §1 (OccurrenceRow, OCCURRENCE_COLUMNS, queryTablePage/queryListPage/queryAllFiltered restructure; integer-interpolation analog) and §"Shared Patterns / sqlite3.exec callback"
    - .planning/phases/131-occurrence-normalization/131-RESEARCH.md (Option A; Pitfall 4 — o. prefix; Pitfall 5 — LEFT JOIN NULL semantics)
    - src/tests/filter.test.ts (the Wave 0 RED assertions this task must turn GREEN)
  </read_first>
  <behavior>
    - queryTablePage data SQL contains `LEFT JOIN taxa` and `t.name AS display_name` (filter.test.ts Task 2 assertion)
    - OCCURRENCE_COLUMNS does NOT contain 'scientificName', 'genus', 'family', 'specimen_inat_taxon_name' (filter.test.ts inverted assertion)
    - A row whose taxon_id is NULL yields display_name === null from the LEFT JOIN (no empty string)
  </behavior>
  <action>
    Remove the `scientificName`, `genus`, `family`, `specimen_inat_taxon_name` fields from the
    `OccurrenceRow` interface and add `display_name: string | null` (documented as a JOIN alias,
    not a mart column). Remove `'scientificName'`, `'genus'`, `'family'`,
    `'specimen_inat_taxon_name'` from `OCCURRENCE_COLUMNS` (36 → 32 entries). In all three page
    query functions — `queryTablePage`, `queryListPage`, `queryAllFiltered` — change the SELECT
    column assembly from `OCCURRENCE_COLUMNS.join(', ')` to
    `OCCURRENCE_COLUMNS.map(c => 'o.' + c).join(', ') + ', t.name AS display_name'` and change the
    FROM to `occurrences o LEFT JOIN taxa t ON t.taxon_id = o.taxon_id`. Ensure any existing WHERE
    column references in those functions that would now be ambiguous (e.g. a bare `taxon_id` from
    the descendant clause) still resolve — the descendant subquery at L235-241 already qualifies
    inside its own subquery; verify the outer WHERE references occurrence columns unambiguously
    (prefix with `o.` only where the same name exists in `taxa`: taxon_id, name, rank). Do NOT add
    `display_name` to OCCURRENCE_COLUMNS. Leave the sqlite3.exec callback unchanged —
    `display_name` arrives in `columnNames` automatically from the alias. Do NOT touch the
    DataSummary interface or queryFilteredCounts in this plan (Plan 03 owns those).
  </action>
  <verify>
    <automated>npx vitest run src/tests/filter.test.ts 2>&1 | tail -20 — JOIN + OCCURRENCE_COLUMNS assertions GREEN</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "LEFT JOIN taxa t ON t.taxon_id = o.taxon_id" src/filter.ts` returns 3 (queryTablePage, queryListPage, queryAllFiltered)
    - `grep -q "display_name: string | null" src/filter.ts` succeeds (OccurrenceRow field added)
    - `grep -nE "'scientificName'|'genus'|'family'|'specimen_inat_taxon_name'" src/filter.ts` returns no matches in OCCURRENCE_COLUMNS (the const has 32 entries)
    - `npx vitest run src/tests/filter.test.ts` exits 0
    - `npm run typecheck` exits 0
  </acceptance_criteria>
  <done>filter.ts resolves display_name via LEFT JOIN in all three page queries; OccurrenceRow + OCCURRENCE_COLUMNS slimmed; filter.test.ts GREEN.</done>
</task>

<task type="auto">
  <name>Task 2: Point bee-table Species column and bee-occurrence-detail _renderProvisional at display_name</name>
  <files>src/bee-table.ts, src/bee-occurrence-detail.ts</files>
  <read_first>
    - src/bee-table.ts (Species ColumnDef L43; render loop L380-386 — confirm dataField + nullLabel are sufficient, no valueFn needed)
    - src/bee-occurrence-detail.ts (_renderProvisional L235-254; _renderCollectorGroup L189-193 — the established null-guard idiom)
    - .planning/phases/131-occurrence-normalization/131-PATTERNS.md §3 (Species column) and §4 (_renderProvisional + null-guard analog)
    - src/tests/bee-table.test.ts (the Wave 0 RED fixtures this task must turn GREEN)
    - .planning/phases/130-map-filter-cutover/130-CONTEXT.md (D-07: taxon_id IS NULL → "No Determination"/"No determination", never blank)
  </read_first>
  <action>
    In `src/bee-table.ts` change the Species column ColumnDef (L43) `dataField` from
    `'scientificName'` to `'display_name'`; leave `nullLabel: 'No Determination'`, `label`,
    `minWidth`, and the L382 render loop unchanged (the existing null → nullLabel path handles
    a null `display_name` correctly). In `src/bee-occurrence-detail.ts` `_renderProvisional`
    (L236-237) change `row.specimen_inat_taxon_name` to `row.display_name` in both the truthy
    test and the `<em>${...}</em>` interpolation; keep the existing
    `<span class="hint">identification pending</span>` null branch unchanged (it fires when
    `display_name` is null per LEFT JOIN NULL semantics — Pitfall 5). Do not add a taxonCache
    lookup here — the name now comes from the JOIN result.
  </action>
  <verify>
    <automated>npx vitest run src/tests/bee-table.test.ts 2>&1 | tail -20 — GREEN</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "dataField: 'display_name'" src/bee-table.ts` succeeds
    - `grep -c "scientificName" src/bee-table.ts` returns 0
    - `grep -q "row.display_name" src/bee-occurrence-detail.ts` succeeds
    - `grep -c "specimen_inat_taxon_name" src/bee-occurrence-detail.ts` returns 0
    - `npx vitest run src/tests/bee-table.test.ts` exits 0
    - `npm run typecheck` exits 0
  </acceptance_criteria>
  <done>Both live display consumers read display_name; null still renders the correct fallback; bee-table.test.ts GREEN.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → wa-sqlite (in-process) | The page queries run against the in-process SQLite DB; the only externally-influenced value reaching SQL in scope here is `f.taxonId` (already a parsed integer, T-130-01) |

## STRIDE Threat Register

Per RESEARCH.md Security Domain: no new authentication, sessions, or user-controlled string
inputs. The JOIN's `ON t.taxon_id = o.taxon_id` is DB-origin integer on both sides.

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-131-02 | Injection (V5) | filter.ts JOIN ON clause | accept | Both sides DB-origin integers; no user-controlled string in the ON clause (RESEARCH §Security Domain) |
| T-131-03 | Injection (V5) | filter.ts WHERE taxonId interpolation | mitigate | `f.taxonId` is a parsed integer interpolated via the existing T-130-01 pattern; no quoting/escaping of strings introduced |
| T-131-SC | Tampering | npm/pip/cargo installs | accept | No package installs (RESEARCH.md: "No new packages") — nothing to audit |
</threat_model>

<verification>
- `npx vitest run src/tests/filter.test.ts src/tests/bee-table.test.ts` exits 0.
- `npm run typecheck` exits 0.
- `grep -rn "scientificName\|specimen_inat_taxon_name" src/ --include=*.ts | grep -v test`
  shows NO matches in filter.ts, bee-table.ts, bee-occurrence-detail.ts (remaining live readers
  in features.ts/bee-atlas.ts are cleared in Plan 03; checklist reads in bee-map.ts are
  out-of-scope and unaffected).
</verification>

<success_criteria>
NORM-03 query + display migration complete: display_name resolved via LEFT JOIN in the three
page queries; the 4 columns gone from OccurrenceRow + OCCURRENCE_COLUMNS; both live display
consumers on display_name with correct null fallback. filter.test.ts + bee-table.test.ts GREEN;
typecheck clean.
</success_criteria>

<output>
Create `.planning/phases/131-occurrence-normalization/131-02-SUMMARY.md` when done.
</output>
