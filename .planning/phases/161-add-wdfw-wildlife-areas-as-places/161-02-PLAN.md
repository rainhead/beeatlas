---
phase: 161-add-wdfw-wildlife-areas-as-places
plan: 02
type: execute
wave: 2
depends_on: ["161-01"]
files_modified:
  - content/places.toml
autonomous: true
requirements: [WLA-DISSOLVE, WLA-WGS84, WLA-VALID, WLA-WEIGHT]
must_haves:
  truths:
    - "content/places.toml contains exactly 33 new WDFW [[places]] entries, each with land_owner = \"Washington Department of Fish & Wildlife\""
    - "places_validation.py passes on the updated places.toml (all 5 checks) — the 16 WDFW↔existing-place overlaps LOAD cleanly without raising (Phase 160 many-to-many model)"
    - "The full data pipeline (data/run.py place steps) completes green and assigns the new WDFW place slugs to occurrences via the existing ST_Within join + occurrence_places bridge"
    - "public/data/places.geojson stays ≤ ~1 MB after the WDFW additions, with the chosen simplification tolerance ratified and before/after sizes reported"
  artifacts:
    - path: "content/places.toml"
      provides: "33 new WDFW wildlife-area [[places]] entries (MultiPolygon, one per area, units dissolved)"
      contains: "Washington Department of Fish & Wildlife"
  key_links:
    - from: "content/places.toml"
      to: "data/places_validation.py validate_places_step()"
      via: "places-validation pipeline step"
      pattern: "validate_places"
    - from: "content/places.toml"
      to: "data/dbt/models/marts/occurrences.sql ST_Within"
      via: "places-load → dbt-build assigns place membership"
      pattern: "ST_Within"
    - from: "content/places.toml"
      to: "public/data/places.geojson"
      via: "places-export step"
      pattern: "places.geojson"
---

<objective>
Run the curation script from Plan 01 to append the 33 web-listed WDFW wildlife areas
to `content/places.toml`, ratify the final simplification tolerance against the D-05
weight budget, and confirm the whole existing pipeline flows green end-to-end with the
new entries — assigning the new place slugs to occurrences and emitting a
`public/data/places.geojson` that stays under the ~1 MB cap.

This is the content-addition plan. The downstream pipeline (validate → load →
dbt-build → export → maps → frontend) is REUSED UNCHANGED — no code edits beyond
appending TOML blocks. The 16 known WDFW↔existing-place partial overlaps must LOAD
cleanly as multi-place membership (Phase 160 removed the `ST_Overlaps` rejection); do
NOT clip, skip, triage, or hard-fail on them.

Purpose: Make bee occurrences collected inside WDFW wildlife areas filterable and
render their boundaries on the map, within the browser weight budget.
Output: updated `content/places.toml` (+33 entries); a regenerated, size-verified
`public/data/places.geojson`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/161-add-wdfw-wildlife-areas-as-places/161-CONTEXT.md
@.planning/phases/161-add-wdfw-wildlife-areas-as-places/161-RESEARCH.md
@.planning/phases/161-add-wdfw-wildlife-areas-as-places/161-VALIDATION.md
@.planning/phases/161-add-wdfw-wildlife-areas-as-places/161-01-SUMMARY.md
@data/add_wdfw_wildlife_areas.py
@data/places_validation.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Ratify tolerance and run the script to append 33 WDFW entries</name>
  <files>content/places.toml</files>
  <read_first>
    - data/add_wdfw_wildlife_areas.py (the Plan 01 script — confirm the TOL constant and main() append behavior)
    - .planning/phases/161-add-wdfw-wildlife-areas-as-places/161-01-SUMMARY.md (the realized function/constant list + slug convention from Plan 01)
    - .planning/phases/161-add-wdfw-wildlife-areas-as-places/161-RESEARCH.md (§ D-05 Weight Analysis — full ~3.0 MB; 0.0002° → ~716 KB; 0.0005° → ~549 KB; baseline places.geojson = 345,580 bytes; threshold total ≤ ~1 MB)
    - content/places.toml (header lines 1–8 + rattlesnake-ledge/tiger-mountain entries 10–30 — the entry shape the appended blocks must match; confirm the 134 existing entries before appending)
  </read_first>
  <action>
    Record the BEFORE size of `public/data/places.geojson` (baseline ~345,580 bytes).

    Ratify the simplification tolerance against the D-05 budget (total places.geojson
    ≤ ~1 MB ≈ 2x the largest existing geojson, counties.geojson at ~512 KB):
    - Default TOL = 0.0002° (~22 m) → WDFW contribution ~716 KB → total ~1.05 MB
      (marginally over the cap).
    - Fallback TOL = 0.0005° (~55 m) → WDFW contribution ~549 KB → total ~0.89 MB
      (under the cap; both tolerances keep all areas valid).
    Per D-05 the user leaned toward the lighter end being acceptable for large
    wildlife-area boundaries. Set the script's TOL so the FINAL total places.geojson
    is ≤ ~1 MB. Use 0.0005° if 0.0002° lands the total over ~1 MB. Record the chosen
    tolerance in the SUMMARY (geometry detail is a publish-time decision).

    Run the curation script:
    `cd data && uv run python add_wdfw_wildlife_areas.py`
    It fetches the WDFW layer, dissolves to 33 MultiPolygons (Jackman Creek excluded),
    and appends 33 [[places]] blocks to content/places.toml.

    Confirm exactly 33 NEW entries were appended (count of [[places]] goes 134 → 167)
    and that every new entry's land_owner is the exact D-02 string
    "Washington Department of Fish & Wildlife".

    DO NOT add, run, or react to any overlap pre-check. If the script (incorrectly)
    contained overlap-handling that aborts, that is a Plan 01 defect — fix the script to
    remove it, not the data. The 16 WDFW↔existing overlaps are expected and must remain.
  </action>
  <verify>
    <automated>cd data && uv run python -c "import tomllib; p=tomllib.load(open('../content/places.toml','rb'))['places']; wdfw=[x for x in p if x.get('land_owner')=='Washington Department of Fish & Wildlife']; assert len(wdfw)==33, f'expected 33 WDFW entries, got {len(wdfw)}'; assert all(x['geometry_wkt'].strip().startswith('MULTIPOLYGON') for x in wdfw), 'all WDFW geoms must be MULTIPOLYGON'; assert all(x['slug'].endswith('-wildlife-area') for x in wdfw), 'slug convention'; assert not any(x.get('name')=='Jackman Creek' for x in p), 'Jackman Creek must be excluded'; print('OK 33 WDFW entries')"</automated>
  </verify>
  <acceptance_criteria>
    - `content/places.toml` contains exactly 33 entries with `land_owner = "Washington Department of Fish & Wildlife"`.
    - Total `[[places]]` count is 167 (134 existing + 33 new).
    - Every new WDFW `geometry_wkt` starts with `MULTIPOLYGON`.
    - Every new WDFW slug ends with `-wildlife-area` and matches `^[a-z0-9-]+$`.
    - No entry named "Jackman Creek" exists (D-01 exclusion honored).
    - The chosen simplification tolerance is recorded for the SUMMARY.
  </acceptance_criteria>
  <done>
    `content/places.toml` has 33 new WDFW MultiPolygon entries with the correct
    land_owner string, Jackman Creek excluded, slugs following the immutable convention,
    and the tolerance ratified against the weight budget.
  </done>
</task>

<task type="auto">
  <name>Task 2: Validate, run the full place pipeline, and confirm places.geojson weight</name>
  <files>content/places.toml</files>
  <read_first>
    - data/places_validation.py (full — the 5 checks; the docstring confirms the ST_Overlaps rejection was REMOVED in Phase 160, so the 16 overlaps must NOT raise)
    - data/run.py (the STEPS list around lines 88–118 — places-validation → places-load → dbt-build → … → places-export → places-maps order)
    - data/tests/test_places_validation.py (full — includes test_overlapping_polygons asserting overlaps LOAD without raising)
    - data/tests/test_places_load.py and data/tests/test_places_export.py (the contract tests to keep green)
    - .planning/phases/161-add-wdfw-wildlife-areas-as-places/161-VALIDATION.md (§ Sampling Rate + § Manual-Only Verifications — the weight-report and frontend-visual expectations)
  </read_first>
  <action>
    First run the direct validation gate against the updated TOML — this is where any
    residual overlap rejection WOULD fire if (incorrectly) still present, and it must
    NOT fire post-Phase-160:
    `cd data && uv run python -c "from places_validation import validate_places_step; validate_places_step()"`
    It must exit 0 with the 16 known WDFW↔existing-place overlaps present in the file.

    Run the three place contract tests (quick tier):
    `cd data && uv run pytest tests/test_places_validation.py tests/test_places_load.py tests/test_places_export.py`
    All must pass.

    Run the full pipeline against the local DuckDB so the new slugs flow end-to-end
    (places-load → dbt-build ST_Within → occurrence_places bridge → places-export →
    places-maps):
    `cd data && uv run python run.py`
    It must complete green. The new WDFW slugs appear in marts/occurrences via the
    existing ST_Within join and in the occurrence_places bridge as multi-place
    membership (an occurrence on shared ground belongs to BOTH the WDFW area and the
    overlapping existing place — by design, double-counted per Phase 160 D-05).

    Record the AFTER size of `public/data/places.geojson`. Confirm it is ≤ ~1 MB
    (~1,048,576 bytes). If it exceeds the cap, the script's TOL was too fine — set TOL to
    0.0005°, revert the just-appended blocks, re-run the script's append, re-run the
    pipeline, and re-measure. Report before/after bytes and the final chosen tolerance
    (D-05 requirement) in the SUMMARY.

    Confirm the 33 new WDFW slugs are present in the exported places.geojson
    (feature `slug` properties) and that the file parses as a FeatureCollection.

    DO NOT auto-advance past the human-visual check (see Manual-Only Verifications) —
    leave it for operator UAT; the automated gates here are the pipeline + size + slug
    presence.
  </action>
  <verify>
    <automated>cd /Users/rainhead/dev/beeatlas/data && uv run python -c "from places_validation import validate_places_step; validate_places_step(); print('validate OK')" && uv run python -c "import json,os; p='../public/data/places.geojson'; sz=os.path.getsize(p); fc=json.load(open(p)); slugs={f['properties'].get('slug') for f in fc['features']}; wdfw=[s for s in slugs if s and s.endswith('-wildlife-area')]; assert fc['type']=='FeatureCollection'; assert len(wdfw)==33, f'expected 33 WDFW slugs in geojson, got {len(wdfw)}'; assert sz <= 1100000, f'places.geojson too large: {sz} bytes (cap ~1MB)'; print(f'OK places.geojson={sz} bytes, {len(wdfw)} WDFW features')"</automated>
  </verify>
  <acceptance_criteria>
    - `validate_places_step()` exits 0 on the updated TOML with the 16 overlaps present (no raise).
    - The three place contract tests pass.
    - `data/run.py` completes green (full pipeline).
    - `public/data/places.geojson` parses as a FeatureCollection, contains 33 WDFW (`-wildlife-area`) slug features, and is ≤ ~1 MB.
    - Before/after places.geojson sizes and the final chosen tolerance are reported.
  </acceptance_criteria>
  <done>
    The updated places.toml passes validation (overlaps load cleanly), the full pipeline
    runs green assigning WDFW slugs to occurrences via ST_Within + the bridge, and
    places.geojson ships under ~1 MB with the chosen tolerance and size delta reported.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| curation script run → WDFW ArcGIS REST endpoint | The Plan 01 script (run in this plan) issues a read-only HTTPS GET to a public WA-state government ArcGIS service. Only external/untrusted input. |
| content/places.toml → pipeline | Appended WKT flows through places_validation.py (validity + WGS84 bounds) before load/dbt; the maintainer reviews the diff. |
| (production runtime) | N/A — the static-hosted site only ships the resulting `places.geojson`/`places.json` data artifacts; no runtime code in this phase. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-161-06 | Tampering | Untrusted WDFW GeoJSON → appended WKT in places.toml | mitigate | `validate_places_step()` runs first (WKT validity via ST_GeomFromText + WGS84 coordinate-range bounds) and the dbt contract gate runs in `data/run.py`; a malformed/out-of-range geometry fails fast before export. The TOML diff is human-reviewed before commit. |
| T-161-07 | Tampering (supply chain) | npm/pip/cargo installs | N/A | No new dependencies (verified: all tooling already in `data/pyproject.toml`). No install task in this plan; legitimacy gate does not apply. |
| T-161-08 | Denial of Service | Oversized browser-shipped places.geojson | mitigate | D-05 weight cap enforced as an automated gate (≤ ~1 MB); simplification tolerance ratified; before/after sizes reported. Prevents shipping a ~3.4 MB artifact to every browser. |
| T-161-09 | Information Disclosure | HTTPS transport / shipped boundary data | accept | Public boundary data only; no secrets/PII; standard TLS. |

No Repudiation, Spoofing-of-identity, or Elevation-of-Privilege surface: no auth, no
user accounts, no privilege transitions. No ASVS category applies beyond standard
transport security (HTTPS), already used.
</threat_model>

<verification>
- `validate_places_step()` exits 0 on the updated places.toml (16 overlaps LOAD, no raise).
- `cd data && uv run pytest tests/test_places_validation.py tests/test_places_load.py tests/test_places_export.py` exits 0.
- `cd data && uv run python run.py` completes green (full pipeline, places steps included).
- `public/data/places.geojson` is a FeatureCollection with 33 `-wildlife-area` slug features and is ≤ ~1 MB; before/after sizes + chosen tolerance reported.
- Manual-only (operator UAT, do not auto-advance): WDFW areas appear in the map Regions/place filter and their boundaries render; `places.geojson` weight confirmed against the cap.
</verification>

<success_criteria>
- `content/places.toml` gains exactly 33 WDFW wildlife-area entries (one MultiPolygon per area, units dissolved), land_owner = "Washington Department of Fish & Wildlife", Jackman Creek excluded.
- Validation passes; the 16 WDFW↔existing overlaps load as multi-place membership (no clip/skip/triage/hard-fail).
- The full pipeline runs green; new WDFW slugs are assigned to occurrences via the existing ST_Within join and the occurrence_places bridge.
- `public/data/places.geojson` stays ≤ ~1 MB; chosen tolerance and before/after byte sizes reported (D-05).
</success_criteria>

<output>
Create `.planning/phases/161-add-wdfw-wildlife-areas-as-places/161-02-SUMMARY.md` when done.
Record: the realized 33 WDFW slugs, the final simplification tolerance, the
before/after `places.geojson` byte sizes, confirmation that the 16 overlaps loaded
without raising, and the full-pipeline-green result.
</output>
