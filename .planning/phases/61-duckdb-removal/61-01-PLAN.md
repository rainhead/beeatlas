---
phase: 61-duckdb-removal
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/duckdb.ts
  - frontend/package.json
  - frontend/src/bee-atlas.ts
  - .planning/PROJECT.md
  - BENCHMARK.md
autonomous: true
requirements: []

must_haves:
  truths:
    - "@duckdb/duckdb-wasm is not listed in frontend/package.json dependencies"
    - "frontend/src/duckdb.ts does not exist"
    - "No file in frontend/src imports or references duckdb"
    - "All 165+ tests pass"
    - "TypeScript compiles with zero errors"
    - "npm run build succeeds"
    - "Bundle size (gzip) is documented in BENCHMARK.md"
  artifacts:
    - path: "frontend/package.json"
      provides: "Clean dependency list without duckdb"
      contains: "wa-sqlite"
    - path: "BENCHMARK.md"
      provides: "Bundle size comparison row"
      contains: "Bundle size"
    - path: ".planning/PROJECT.md"
      provides: "Updated tech stack description"
      contains: "wa-sqlite"
  key_links:
    - from: "frontend/package.json"
      to: "package-lock.json"
      via: "npm install regenerates lockfile"
      pattern: "resolved.*wa-sqlite"
---

<objective>
Remove all DuckDB WASM artifacts from the frontend: delete the orphaned duckdb.ts module, remove the @duckdb/duckdb-wasm dependency, update stale comments in bee-atlas.ts, update PROJECT.md tech stack, and record bundle size delta in BENCHMARK.md.

Purpose: Complete the wa-sqlite migration by removing the old 34 MB DuckDB WASM dependency, reducing bundle size and eliminating dead code.
Output: Clean codebase with no DuckDB references, updated docs, measured bundle size improvement.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/61-duckdb-removal/61-CONTEXT.md
@BENCHMARK.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Record baseline bundle size, remove DuckDB dependency and module</name>
  <files>frontend/package.json, frontend/src/duckdb.ts, package-lock.json, frontend/src/bee-atlas.ts</files>
  <read_first>
    - frontend/package.json (current dependencies including @duckdb/duckdb-wasm)
    - frontend/src/duckdb.ts (confirm it exists and is the file to delete)
    - frontend/src/bee-atlas.ts (lines 243-247, 650-654, 731-735 contain stale DuckDB comments)
  </read_first>
  <action>
1. Record the baseline bundle size BEFORE removal:
   - Run `cd frontend && npm run build` and capture the Vite output showing chunk sizes.
   - Also run `du -sh frontend/dist` for total uncompressed size.
   - Note the gzip total from Vite's output (it prints gzip sizes per chunk).

2. Delete `frontend/src/duckdb.ts`.

3. Edit `frontend/package.json`: remove the line `"@duckdb/duckdb-wasm": "^1.33.1-dev20.0"` from the `dependencies` object.

4. Run `cd frontend && npm install` to regenerate `package-lock.json` without the DuckDB dependency.

5. Update three stale comments in `frontend/src/bee-atlas.ts`:
   - Line 245: change `// so this runs in parallel with DuckDB init and resolves as soon as tables load.` to `// so this runs in parallel with SQLite init and resolves as soon as tables load.`
   - Line 652: change `// bee-map data-loaded hasn't fired yet; load summary from DuckDB` to `// bee-map data-loaded hasn't fired yet; load summary from SQLite`
   - Line 733: change `// If occurrences were restored from URL, fetch their specimen data now that DuckDB is ready` to `// If occurrences were restored from URL, fetch their specimen data now that SQLite is ready`

6. Verify no remaining duckdb references in frontend/src:
   - Run `grep -ri duckdb frontend/src/` and confirm zero matches.

7. Run `cd frontend && npx tsc --noEmit` and confirm zero TypeScript errors.

8. Run `cd frontend && npm test` and confirm all 165+ tests pass.

9. Record the AFTER bundle size:
   - Run `cd frontend && npm run build` and capture Vite output with chunk sizes.
   - Run `du -sh frontend/dist` for total uncompressed size.
  </action>
  <verify>
    <automated>cd /Users/rainhead/dev/beeatlas/frontend && ! test -f src/duckdb.ts && ! grep -riq duckdb src/ && npx tsc --noEmit && npm test</automated>
  </verify>
  <acceptance_criteria>
    - frontend/src/duckdb.ts does not exist (deleted)
    - `grep -ri duckdb frontend/src/` returns zero matches
    - frontend/package.json does NOT contain the string "@duckdb/duckdb-wasm"
    - `npx tsc --noEmit` exits 0
    - `npm test` exits 0 with 165+ tests passing
    - Baseline and post-removal bundle sizes captured in task output for next task
  </acceptance_criteria>
  <done>DuckDB module deleted, dependency removed, lockfile regenerated, comments updated, TypeScript clean, all tests pass, bundle sizes captured.</done>
</task>

<task type="auto">
  <name>Task 2: Update BENCHMARK.md and PROJECT.md</name>
  <files>BENCHMARK.md, .planning/PROJECT.md</files>
  <read_first>
    - BENCHMARK.md (current benchmark table with DuckDB WASM vs wa-sqlite rows)
    - .planning/PROJECT.md (tech stack description mentioning "DuckDB WASM")
  </read_first>
  <action>
1. Edit `BENCHMARK.md`: Add a new row to the Results table:
   ```
   | Bundle size, gzip (KB) | {before_value} | {after_value} |
   ```
   Use the gzip totals captured in Task 1. The "DuckDB WASM" column gets the before value, the "wa-sqlite" column gets the after value. Both values should be in KB.

   Also add a second row if total uncompressed size was captured:
   ```
   | Bundle size, uncompressed (KB) | {before_value} | {after_value} |
   ```

2. Edit `.planning/PROJECT.md` line 5 (the "What This Is" paragraph): Replace the parenthetical `(TypeScript, OpenLayers, Lit, DuckDB WASM)` with `(TypeScript, OpenLayers, Lit, wa-sqlite + hyparquet)`. This is the only occurrence of "DuckDB WASM" in the tech stack description per D-01.

   Also in the same paragraph, the phrase "Four dlt pipelines + a DuckDB-native geographies pipeline write to a local DuckDB store" should remain as-is because it describes the data pipeline (Python side), not the frontend. Only the frontend tech stack parenthetical changes.

3. Verify: Run `cd frontend && npm run build` one final time to confirm the build succeeds cleanly.
  </action>
  <verify>
    <automated>cd /Users/rainhead/dev/beeatlas && grep -q "Bundle size" BENCHMARK.md && grep -q "wa-sqlite" .planning/PROJECT.md && grep -q "wa-sqlite + hyparquet" .planning/PROJECT.md && cd frontend && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - BENCHMARK.md contains a row with "Bundle size" text
    - BENCHMARK.md "Bundle size" row has numeric values in both columns
    - .planning/PROJECT.md contains "(TypeScript, OpenLayers, Lit, wa-sqlite + hyparquet)"
    - .planning/PROJECT.md does NOT contain "(TypeScript, OpenLayers, Lit, DuckDB WASM)"
    - `npm run build` exits 0
  </acceptance_criteria>
  <done>BENCHMARK.md has bundle size comparison row (per D-02). PROJECT.md tech stack updated to wa-sqlite + hyparquet (per D-01). Build succeeds.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

No new trust boundaries introduced. This phase only removes code and updates documentation.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-61-01 | Tampering | package.json | accept | Dependency removal only; no new dependencies introduced. npm install regenerates lockfile deterministically. |
</threat_model>

<verification>
1. `frontend/src/duckdb.ts` does not exist
2. `grep -ri duckdb frontend/src/` returns zero results
3. `grep duckdb frontend/package.json` returns zero results
4. `cd frontend && npx tsc --noEmit` exits 0
5. `cd frontend && npm test` passes 165+ tests
6. `cd frontend && npm run build` succeeds
7. BENCHMARK.md contains "Bundle size" row with both columns populated
8. .planning/PROJECT.md mentions "wa-sqlite + hyparquet" in tech stack
</verification>

<success_criteria>
- All six phase success criteria from ROADMAP.md are met:
  1. @duckdb/duckdb-wasm absent from package.json
  2. frontend/src/duckdb.ts deleted
  3. No remaining DuckDB imports in frontend/src
  4. All 165+ tests pass
  5. TypeScript compiles cleanly
  6. npm run build succeeds and bundle size is measurably smaller
- BENCHMARK.md documents the bundle size reduction (D-02)
- PROJECT.md reflects the current tech stack (D-01)
</success_criteria>

<output>
After completion, create `.planning/phases/61-duckdb-removal/61-01-SUMMARY.md`
</output>
