---
phase: 171.1-collector-data-delivery-rebuild
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/tests/fixtures/collectors.fixture.json
  - src/tests/fixtures/collector_event_pages.fixture.json
  - _data/collectors.js
  - src/tests/data-collectors.test.ts
autonomous: true
requirements: [D-05, D-06]
user_setup: []

must_haves:
  truths:
    - "src/tests/fixtures/collectors.fixture.json carries the Phase 171 extended shape and satisfies every data-collectors.test.ts assertion (D-05)"
    - "src/tests/fixtures/collector_event_pages.fixture.json has at least one page with page_num >= 2 (D-05)"
    - "_data/collectors.js returns [] (does not throw) when public/data/collectors.json is absent"
    - "data-collectors.test.ts reads fixtures (not public/data/*), so npm test passes on a clean checkout with zero S3 access (D-06)"
  artifacts:
    - path: src/tests/fixtures/collectors.fixture.json
      provides: "Committed tiny collectors fixture with full Phase 171 shape"
      contains: "first_page_events"
    - path: src/tests/fixtures/collector_event_pages.fixture.json
      provides: "Committed sub-page fixture (page_num >= 2)"
      contains: "page_num"
    - path: _data/collectors.js
      provides: "existsSync guard returning [] when collectors.json absent"
      contains: "existsSync"
    - path: src/tests/data-collectors.test.ts
      provides: "fixture-pointed beforeAll reads"
      contains: "src/tests/fixtures/collectors.fixture.json"
  key_links:
    - from: "src/tests/data-collectors.test.ts beforeAll"
      to: "src/tests/fixtures/*.fixture.json"
      via: "readFileSync"
      pattern: "fixtures/collectors.fixture.json"
    - from: "_data/collectors.js"
      to: "existsSync(collectorsPath) guard"
      via: "conditional read"
      pattern: "existsSync\\(collectorsPath\\)"
---

<objective>
Make a clean checkout `npm test`-green without the real committed collector data — the prerequisite that lets Plan 04 untrack the files. Three coupled changes: (1) commit tiny synthetic fixtures outside `public/data/` (D-05/D-06), (2) add an `existsSync` guard to `_data/collectors.js` so the unconditional module-load `readFileSync` no longer throws when `public/data/collectors.json` is absent, and (3) repoint `data-collectors.test.ts`'s direct-read `beforeAll` to the fixtures.

This plan touches NO S3 path and has NO dependency on Plan 01 — it runs in parallel (Wave 1). It must land before Plan 04's untrack so the codebase is already test-green on a fileless checkout.

Purpose: Decouple `npm test` green from committed data / S3 access (the whole point of the phase — keep `public/data/*` free of committed data).
Output: Two new committed fixtures; guarded loader; fixture-pointed test.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/171.1-collector-data-delivery-rebuild/171.1-CONTEXT.md
@.planning/phases/171.1-collector-data-delivery-rebuild/171.1-RESEARCH.md
@.planning/phases/171.1-collector-data-delivery-rebuild/171.1-PATTERNS.md
@_data/collectors.js
@src/tests/data-collectors.test.ts
</context>

<artifacts_this_plan_produces>
- New fixture file `src/tests/fixtures/collectors.fixture.json`
- New fixture file `src/tests/fixtures/collector_event_pages.fixture.json`
- `existsSync` guard in `_data/collectors.js` (line 25 read, plus `existsSync` added to the `node:fs` import)
- Repointed `readFileSync` paths in `src/tests/data-collectors.test.ts` `beforeAll`
- Removal of the loader-array `>= 100` floor test (replaced by fixture-direct shape contract)
</artifacts_this_plan_produces>

<tasks>

<task type="auto">
  <name>Task 1: Create the two committed fixtures (Wave 0 — before any repoint)</name>
  <files>src/tests/fixtures/collectors.fixture.json, src/tests/fixtures/collector_event_pages.fixture.json</files>
  <read_first>
    - @src/tests/data-collectors.test.ts lines 80-208 — the Phase 171 describe block whose assertions the fixtures must satisfy (event shape, is_reidentification null-for-Collected/boolean-for-Identified, status split invariant, inat_url mutually-exclusive-with species_slug, page_num >= 2).
    - @.planning/phases/171.1-collector-data-delivery-rebuild/171.1-RESEARCH.md "Fixture Design" — the full annotated JSON templates to copy verbatim (2 collectors; one sub-page).
    - @.planning/phases/171.1-collector-data-delivery-rebuild/171.1-PATTERNS.md "Net-New Fixtures" — the exact assertion checklist.
  </read_first>
  <action>
    Create `src/tests/fixtures/collectors.fixture.json` from the RESEARCH "Fixture Design" template: an array of at least 2 collectors, each with `login`, `display_name`, `recordedBy`, `host_inat_login`, `specimen_count`, `sample_count`, `species_count`, `status_denominator`, `status_identified`, `status_awaiting`, `first_page_events` (array), `total_event_pages`, `total_event_count`. Satisfy every Phase 171 assertion: `status_identified + status_awaiting === status_denominator` per collector; at least one event with `event_type` "Identified" carrying `is_current` boolean, `is_reidentification` boolean, `is_pending` boolean; at least one event with a non-null `inat_url` starting `https://www.inaturalist.org/taxa/` (and NOT `/taxa/search`) whose `species_slug` is null/absent (mutual exclusivity); `catalog_number` as string-or-null and `ecdysis_id` as number-or-null. Use synthetic data only (e.g. alice/bob) — no real PII, no tokens.
    Create `src/tests/fixtures/collector_event_pages.fixture.json` from the RESEARCH template: an array with at least one descriptor having `login`, `page_num`, `total_pages`, `events`, where `page_num >= 2` and `events` is non-empty; each event matches the same item shape; a `Collected` event must carry `is_reidentification` null.
    Do NOT place these under `public/data/` — they live at `src/tests/fixtures/` so `public/data/*` stays free of committed data (D-06).
  </action>
  <verify>
    <automated>node -e "const c=require('./src/tests/fixtures/collectors.fixture.json'); if(!Array.isArray(c)||c.length<1) throw new Error('collectors fixture not a non-empty array'); for(const x of c){ if(x.status_identified+x.status_awaiting!==x.status_denominator) throw new Error('split invariant '+x.login); if(!Array.isArray(x.first_page_events)) throw new Error('first_page_events '+x.login); if(typeof x.total_event_pages!=='number'||typeof x.total_event_count!=='number') throw new Error('pagination counts '+x.login);} const anyInat=c.some(x=>x.first_page_events.some(e=>e.inat_url&&e.inat_url.startsWith('https://www.inaturalist.org/taxa/')&&!e.inat_url.startsWith('https://www.inaturalist.org/taxa/search')&&!e.species_slug)); if(!anyInat) throw new Error('need one inat_url event with null species_slug'); const p=require('./src/tests/fixtures/collector_event_pages.fixture.json'); if(!Array.isArray(p)||!p.some(pg=>pg.page_num>=2&&Array.isArray(pg.events)&&pg.events.length>0)) throw new Error('need one sub-page with page_num>=2'); console.log('fixtures OK');"</automated>
  </verify>
  <acceptance_criteria>
    - Both fixtures are valid JSON arrays.
    - collectors fixture: every entry satisfies the status-split invariant and has `first_page_events`/`total_event_pages`/`total_event_count`; at least one event has a `/taxa/{name}` inat_url with null species_slug.
    - event-pages fixture: at least one entry with `page_num >= 2` and non-empty `events`.
  </acceptance_criteria>
  <done>Both committed fixtures exist outside public/data/ and satisfy the Phase 171 shape assertions.</done>
</task>

<task type="auto">
  <name>Task 2: Add existsSync guard to the loader, repoint the test to fixtures, drop the loader floor</name>
  <files>_data/collectors.js, src/tests/data-collectors.test.ts</files>
  <read_first>
    - @_data/collectors.js — full file; line 18 import, line 25 unconditional `readFileSync` of `public/data/collectors.json` (throws on clean checkout), lines 31-33 the existing `ELEVENTY_RUN_MODE==='build'` guard for the large file (already correct).
    - @src/tests/data-collectors.test.ts — line 10 module-level loader import; lines 19-21 the `>= 100` floor test; lines 84-93 the `beforeAll` direct reads of `public/data/*`.
    - @.planning/phases/171.1-collector-data-delivery-rebuild/171.1-PATTERNS.md "_data/collectors.js — existsSync guard" and "data-collectors.test.ts — repoint readFileSync paths".
  </read_first>
  <action>
    In `_data/collectors.js`: add `existsSync` to the `node:fs` import (line 18). Replace the unconditional line-25 read with a guarded read — compute `collectorsPath = join(repoRoot, 'public/data/collectors.json')`, then set `collectorsArray` to `JSON.parse(readFileSync(collectorsPath,'utf8'))` when `existsSync(collectorsPath)` is true, else `[]` with a single `console.warn` noting the file is absent (fetch from S3 for full data). Leave the lines 31-33 `collectorEventPages` build-mode guard unchanged — vitest never reads that file. Do NOT introduce any `parquet` reference (the Pitfall #8 source-grep test at lines 46-49 / 63-66 asserts its absence).
    In `src/tests/data-collectors.test.ts`: repoint the two `beforeAll` `readFileSync(resolve(ROOT, ...))` paths (lines 86-93) from `public/data/collectors.json` to `src/tests/fixtures/collectors.fixture.json` and from `public/data/collector_event_pages.json` to `src/tests/fixtures/collector_event_pages.fixture.json`.
    REMOVE the loader-array floor test at lines 19-21 (`collectorsArray.length is >= 100`). Do NOT relax it to `>= 1`: this test reads the LOADER's `collectorsArray`, which the new existsSync guard makes `[]` (length 0) on a clean checkout, so any positive floor would fail the clean-checkout gate. The non-empty/shape contract is already enforced by the fixture-direct Phase 171 block (lines 80-208, now reading `collectorsRaw` from fixtures). Leave the loader-contract Array-ness and vacuous-shape tests (lines 15-17, 23-44) intact — they pass on an empty array.
  </action>
  <verify>
    <automated>npm test 2>&1 | tail -15; grep -qF "existsSync(collectorsPath)" _data/collectors.js && grep -qF "src/tests/fixtures/collectors.fixture.json" src/tests/data-collectors.test.ts && grep -qF "src/tests/fixtures/collector_event_pages.fixture.json" src/tests/data-collectors.test.ts && ! grep -q "toBeGreaterThanOrEqual(100)" src/tests/data-collectors.test.ts && ! grep -qi "parquet" _data/collectors.js && echo GUARDS_OK</automated>
  </verify>
  <acceptance_criteria>
    - `npm test` is green (fixtures satisfy the repointed Phase 171 block).
    - `_data/collectors.js` contains `existsSync(collectorsPath)` and no `parquet` reference.
    - The test reads both fixture paths; the `>= 100` loader floor is gone (not relaxed).
    - Clean-checkout-empty-loader path proven structurally: with `public/data/collectors.json` absent the guard returns `[]` and the loader import does not throw (final proof is the phase-level clean-checkout gate in Plan 04).
  </acceptance_criteria>
  <done>npm test passes reading committed fixtures; the loader survives an absent collectors.json by returning []; the brittle loader floor is removed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| committed fixture → test harness | A small JSON fixture enters the repo and is read by Vitest at test time |
| absent data file → Eleventy loader | `_data/collectors.js` runs against a possibly-missing `public/data/collectors.json` |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-171.1-04 | Information disclosure | committed fixture content | mitigate | Fixture is hand-authored synthetic data (alice/bob), no real PII, no tokens/secrets — confirmed by review before commit. |
| T-171.1-05 | Denial of service | loader throwing on absent file | mitigate | existsSync guard returns `[]` (no throw) so a fileless checkout still builds/tests; the large-file read stays behind the existing build-mode guard. |

No package-manager installs in this plan — no `T-171.1-SC` supply-chain row applies.
</threat_model>

<verification>
- `npm test` green with the repointed fixtures.
- Loader contains the existsSync guard and no parquet reference; the `>= 100` floor test is removed.
- Fixtures validate (Task 1 node gate) and live under `src/tests/fixtures/` (not `public/data/`).
</verification>

<success_criteria>
- Contributes to ROADMAP Success Criterion 2: `npm test` passes on a clean checkout with zero S3 access; `_data/collectors.js` reads from `public/data/` unchanged when the file IS present.
- D-05 + D-06 satisfied: tiny fixture committed outside public/data; test repointed to it.
</success_criteria>

<output>
Create `.planning/phases/171.1-collector-data-delivery-rebuild/171.1-02-SUMMARY.md` when done.
</output>
