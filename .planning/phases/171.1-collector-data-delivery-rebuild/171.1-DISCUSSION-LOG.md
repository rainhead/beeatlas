# Phase 171.1: Collector Data Delivery Rebuild - Discussion Log

> **Audit trail only.** Do not use as input to planning, research, or execution agents.
> Decisions are captured in CONTEXT.md — this log preserves the alternatives considered.

**Date:** 2026-06-28
**Phase:** 171.1-collector-data-delivery-rebuild
**Areas discussed:** Git history handling, maderas → GitHub auth, Clean-checkout test data, Cutover sequencing

---

## Git history handling

| Option | Description | Selected |
|--------|-------------|----------|
| Untrack going forward (Recommended) | `git rm --cached` + drop allowlist; old blobs stay in history; no rewrite, non-disruptive | |
| Full purge (filter-repo + force-push) | Rewrite history, reclaim ~150 MB, force-push main; collaborators must re-clone | ✓ |

**User's choice:** Full purge (filter-repo + force-push).
**Notes:** Operator accepts the force-push/re-clone disruption to reclaim the ~150 MB. (Did not take the recommended non-disruptive option.)

---

## maderas → GitHub auth

| Option | Description | Selected |
|--------|-------------|----------|
| Fine-grained PAT (Recommended) | Repo-scoped fine-grained token, minimal permission, stored root-only on maderas, sourced by nightly.sh | ✓ |
| GitHub App | Scoped installation token; better rotation/audit, more setup | |

**User's choice:** Fine-grained PAT.
**Notes:** Simplest path; manual rotation acceptable.

---

## Clean-checkout test data

| Option | Description | Selected |
|--------|-------------|----------|
| Fetch in CI + skip-if-absent locally (Recommended) | Add files to deploy.yml fetch; guard test with existsSync | |
| Commit a tiny fixture | Small representative fixture in git for unit tests; real file only at build | ✓ |

**User's choice:** Commit a tiny fixture.
**Notes:** Captured in CONTEXT D-05/D-06 — fixture lives outside `public/data/` so that dir stays free of committed data (the phase's whole point); `data-collectors.test.ts` repointed to the fixture.

---

## Cutover sequencing

| Option | Description | Selected |
|--------|-------------|----------|
| Data-first, one-time manual nightly (Recommended) | Upload to S3+manifest → ship fetch+fixture → untrack from git | ✓ |
| All in one PR, rely on committed file | Ship everything together; window where S3 lacks files | |

**User's choice:** Data-first, one-time manual nightly.
**Notes:** Mirrors the occurrences-contract release sequence (data before code) so the build always has its data.

## Claude's Discretion

- Fixture path/filename + number of sample collectors.
- Whether to also add `workflow_dispatch` to deploy.yml alongside `repository_dispatch`.
- gzip vs plain for the 29 MB `collector_event_pages.json` on S3.

## Deferred Ideas

- Runtime client-side hydration of collector pages (NOT chosen — pages stay static/pre-aggregated per Phase 169).
- Triggering deploy only when collector data changed (optimization, not needed for correctness).
