---
phase: 171.1-collector-data-delivery-rebuild
plan: "02"
subsystem: test-fixtures / data-loader
tags: [test, fixtures, clean-checkout, existsSync, data-loader]
dependency_graph:
  requires: []
  provides: [clean-checkout-test-green, committed-fixtures-outside-public-data]
  affects: [src/tests/data-collectors.test.ts, _data/collectors.js]
tech_stack:
  added: []
  patterns: [existsSync guard, test fixture JSON outside public/data/]
key_files:
  created:
    - src/tests/fixtures/collectors.fixture.json
    - src/tests/fixtures/collector_event_pages.fixture.json
  modified:
    - _data/collectors.js
    - src/tests/data-collectors.test.ts
decisions:
  - "Removed >= 100 loader floor test entirely (not relaxed to >= 1): loader returns [] on clean checkout so any positive floor fails the clean-checkout gate; shape contract enforced by fixture-direct block"
  - "Used _upload_hashed (plain) for both collector artifacts per RESEARCH gzip analysis (D-08)"
metrics:
  duration: "4 minutes"
  completed: "2026-06-28"
  tasks_completed: 2
  tasks_total: 2
  files_created: 2
  files_modified: 2
---

# Phase 171.1 Plan 02: Test Fixtures and existsSync Guard Summary

**One-liner:** Committed two synthetic JSON fixtures outside `public/data/` and guarded `_data/collectors.js` with `existsSync` so `npm test` passes green on a clean checkout with zero S3 access.

## What Was Done

### Task 1: Create committed fixtures (commit `021cf2a1`)

Created `src/tests/fixtures/` directory with two fixture files:

**`collectors.fixture.json`** — 2 synthetic collectors (alice, bob) with full Phase 171 extended shape:
- `first_page_events`, `total_event_pages`, `total_event_count` on every entry
- Status-split invariant satisfied for each: `status_identified + status_awaiting === status_denominator`
- Covers both event_type branches: "Identified" (with boolean `is_reidentification`/`is_current`) and "Collected" (with null `is_reidentification`)
- At least one event has `inat_url` set to `https://www.inaturalist.org/taxa/Syrphus%20ribesii` with `species_slug: null` (mutual exclusivity satisfied)
- `catalog_number` as string-or-null, `ecdysis_id` as number-or-null per contract

**`collector_event_pages.fixture.json`** — 1 sub-page entry with `page_num: 2` (satisfies `page_num >= 2` assertion) and non-empty `events` array.

Both files live under `src/tests/fixtures/` so `public/data/*` stays fully free of committed data (D-06).

### Task 2: existsSync guard and test repoint (commit `fc47d4dc`)

**`_data/collectors.js`:**
- Added `existsSync` to `node:fs` import
- Replaced unconditional `readFileSync` on line 25 with guarded pattern: returns `[]` with `console.warn` when `public/data/collectors.json` is absent
- `ELEVENTY_RUN_MODE === 'build'` guard for `collector_event_pages.json` left unchanged

**`src/tests/data-collectors.test.ts`:**
- Repointed both `beforeAll` `readFileSync` calls from `public/data/*.json` to `src/tests/fixtures/*.fixture.json`
- Removed the `collectorsArray.length >= 100` loader floor test entirely (loader returns `[]` on clean checkout; any positive floor would fail)
- Added comment explaining D-06 clean-checkout intent

## Verification

- All 891 tests pass after changes (`npm test`: 33 test files, 891 tests, 0 failures)
- Fixture validation gate passed: status-split invariant, `inat_url`/`species_slug` mutual exclusivity, `page_num >= 2`
- Guards confirmed: `existsSync(collectorsPath)` present in loader; both fixture paths in test; `>= 100` floor gone; no `parquet` reference in loader

## Deviations from Plan

None — plan executed exactly as written.

## Known Stubs

None.

## Threat Surface Scan

No new network endpoints, auth paths, or schema changes introduced. Fixtures are synthetic data (alice/bob) with no real PII or tokens — T-171.1-04 mitigated.

## Self-Check: PASSED

- `src/tests/fixtures/collectors.fixture.json` — FOUND
- `src/tests/fixtures/collector_event_pages.fixture.json` — FOUND
- `_data/collectors.js` contains `existsSync(collectorsPath)` — VERIFIED
- `src/tests/data-collectors.test.ts` references both fixture paths — VERIFIED
- Commit `021cf2a1` exists — VERIFIED
- Commit `fc47d4dc` exists — VERIFIED
- `npm test`: 891 passed, 0 failed — VERIFIED
