---
phase: 171.1-collector-data-delivery-rebuild
plan: 04
type: execute
wave: 3
depends_on: ["171.1-02", "171.1-03"]
files_modified:
  - .gitignore
  - public/data/collectors.json
  - public/data/collector_event_pages.json
autonomous: false
requirements: [D-01, D-02, D-07]
user_setup: []

must_haves:
  truths:
    - "public/data/collectors.json + collector_event_pages.json are removed from git tracking; .gitignore no longer allowlists them; the working-tree copies remain on disk (D-02)"
    - "git history contains no revision of either file after filter-repo + force-push; repo .git size drops (D-01)"
    - "A clean checkout still builds (npm run build) and tests (npm test) green — collector data comes from S3 (CI build) / fixtures (tests)"
  artifacts:
    - path: .gitignore
      provides: "blanket /public/data/* ignore with the collector allowlist exceptions removed"
      contains: "/public/data/*"
  key_links:
    - from: ".gitignore /public/data/* blanket ignore"
      to: "collectors.json / collector_event_pages.json now untracked"
      via: "removed allowlist exceptions + git rm --cached in one commit"
      pattern: "/public/data/\\*"
---

<objective>
Final cutover (D-07 step 3): stop tracking both collector artifacts and purge them from history. In ONE commit, remove the `.gitignore` allowlist exceptions (lines 145-148) AND `git rm --cached` both files (Pitfall 2 — doing these in separate commits leaves the files re-tracked or in limbo). `git rm --cached` (NOT `git rm`) preserves the working-tree copies on disk per D-02; with the blanket `/public/data/*` ignore restored, they become untracked-and-ignored. Then the operator runs `git filter-repo --invert-paths` on a fresh `/tmp` clone and force-pushes `main` to reclaim ~150 MB (D-01).

This plan depends on BOTH Plan 02 (clean checkout is test-green via fixtures + existsSync guard) AND Plan 03 (CI build fetches both files from S3). Once untracked, a fileless CI checkout MUST be able to build — which requires Plan 03's deploy.yml fetch to be live and S3 to hold the files (Plan 01). The dependencies place this in Wave 3, last.

Purpose: Restore the `feedback_no_committed_data_artifacts` rule and reclaim the ~150 MB bloat.
Output: Untracking commit; purged history after the operator force-push.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/171.1-collector-data-delivery-rebuild/171.1-CONTEXT.md
@.planning/phases/171.1-collector-data-delivery-rebuild/171.1-RESEARCH.md
@.planning/phases/171.1-collector-data-delivery-rebuild/171.1-PATTERNS.md
@.gitignore
</context>

<artifacts_this_plan_produces>
- `.gitignore` with the 4-line collector allowlist block (lines 145-148) removed; blanket `/public/data/*` ignore retained
- `public/data/collectors.json` + `public/data/collector_event_pages.json` removed from the git index (untracked), retained on disk
- A purged git history (no revision of either path) + force-pushed `main` (operator step)
</artifacts_this_plan_produces>

<tasks>

<task type="auto">
  <name>Task 1: Untrack both files — remove .gitignore exceptions + git rm --cached in ONE commit</name>
  <files>.gitignore, public/data/collectors.json, public/data/collector_event_pages.json</files>
  <read_first>
    - @.gitignore lines 141-150 — the blanket `/public/data/*` ignore (141, stays), the places exceptions (143-144, stay), and the collector allowlist block to delete (145-148: two `# v6.0 ...` comments + `!/public/data/collectors.json` + `!/public/data/collector_event_pages.json`).
    - @.planning/phases/171.1-collector-data-delivery-rebuild/171.1-PATTERNS.md ".gitignore — remove allowlist exception block" — exact lines + the same-commit requirement.
    - @.planning/phases/171.1-collector-data-delivery-rebuild/171.1-RESEARCH.md "Common Pitfalls" Pitfall 2 — the same-commit invariant.
    - Memory `feedback_no_committed_data_artifacts` — "committed/clean git status" is NEVER a PASS for these files; the success state is they are UNTRACKED yet present on disk.
  </read_first>
  <action>
    Delete the 4-line collector allowlist block from `.gitignore` (the two `# v6.0 PAGE-01 / STREAM-03` comments and the two `!/public/data/collectors.json` / `!/public/data/collector_event_pages.json` exception lines). Keep line 141 `/public/data/*` and the places exceptions (143-144) untouched. In the SAME commit, run `git rm --cached public/data/collectors.json public/data/collector_event_pages.json` — `--cached` removes them from the index but KEEPS the working-tree copies on disk (D-02). Do NOT use plain `git rm` (that deletes the disk copies). Do NOT force-add anything. Verify before committing that both files still exist on disk and are now untracked-and-ignored. This must be a single atomic commit (Pitfall 2): separate commits leave the files re-tracked on the next add or in a confusing index/ignore mismatch.
  </action>
  <verify>
    <automated>! grep -q 'public/data/collectors.json' .gitignore && ! grep -q 'public/data/collector_event_pages.json' .gitignore && grep -qF '/public/data/*' .gitignore && test -f public/data/collectors.json && test -f public/data/collector_event_pages.json && ! git ls-files --error-unmatch public/data/collectors.json 2>/dev/null && ! git ls-files --error-unmatch public/data/collector_event_pages.json 2>/dev/null && git check-ignore -q public/data/collectors.json && echo UNTRACKED_AND_IGNORED_OK</automated>
  </verify>
  <acceptance_criteria>
    - `.gitignore` no longer allowlists either collector file; the blanket `/public/data/*` ignore remains.
    - `git ls-files` does NOT list either file (untracked); `git check-ignore` confirms they are ignored.
    - Both files still exist on disk (working-tree copies preserved, D-02).
    - The untracking is a single atomic commit (.gitignore edit + git rm --cached together).
  </acceptance_criteria>
  <done>Both collector files are untracked and ignored yet present on disk; .gitignore no longer allowlists them.</done>
</task>

<task type="auto">
  <name>Task 2: Confirm a clean checkout still builds + tests green without the committed files</name>
  <files>(verification only — no source changes)</files>
  <read_first>
    - @.github/workflows/deploy.yml lines 31-52 — the build job order: Fetch from S3 -> Run tests -> Build site (so a CI build has the files; a local clean checkout uses fixtures for tests and would lack the build files).
    - @.planning/phases/171.1-collector-data-delivery-rebuild/171.1-RESEARCH.md "Sequencing / Risks" — step 2 (verify CI green) before the irreversible filter-repo (step 4).
  </read_first>
  <action>
    Prove the codebase is green on a fileless checkout BEFORE the destructive history purge. Run `npm test` (must be green — reads fixtures, not public/data, per Plan 02). Then simulate a fileless checkout for the build/test surface: temporarily move both untracked files aside (e.g. to /tmp), run `npm test` again (must stay green — the existsSync guard makes the loader return [] and the fixture-backed Phase 171 block is unaffected), then restore the files. Do NOT attempt a full local `npm run build` if it requires the real `collector_event_pages.json` in ELEVENTY_RUN_MODE=build — that path is exercised by CI (which fetches from S3); the operator confirms CI green in the merged state as part of Task 3's gate. Record both `npm test` results in the SUMMARY.
  </action>
  <verify>
    <automated>npm test 2>&1 | tail -15; mkdir -p /tmp/171.1-stash && mv public/data/collectors.json public/data/collector_event_pages.json /tmp/171.1-stash/ && npm test 2>&1 | tail -5; mv /tmp/171.1-stash/collectors.json /tmp/171.1-stash/collector_event_pages.json public/data/ && echo CLEAN_CHECKOUT_TEST_DONE</automated>
  </verify>
  <acceptance_criteria>
    - `npm test` green with the files present.
    - `npm test` green with both files moved aside (existsSync guard returns [], fixtures back the Phase 171 block) — proves the clean-checkout contract (Success-2).
    - Files restored to `public/data/` afterward.
  </acceptance_criteria>
  <done>npm test is green both with and without the committed collector files present, proving the clean-checkout build/test contract before the irreversible purge.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 3: Operator purges history with git filter-repo + force-push</name>
  <files>(operator step: fresh /tmp clone + force-push origin/main — no in-tree file changes)</files>
  <action>Operator runs the RESEARCH "git filter-repo Runbook" on a fresh /tmp clone to purge all revisions of both paths, force-pushes `main`, then realigns the working clone (files preserved on disk). Precondition: Task 1's untracking commit is pushed and CI is GREEN. Follow the how-to-verify steps; confirm the empty `git log` and the live-page freshness before resuming.</action>
  <what-built>Both files are untracked and ignored; the codebase builds/tests green without them (CI fetches from S3, tests use fixtures). This task removes all historical revisions of both paths and reclaims the ~150 MB bloat (D-01) — an irreversible rewrite of public `main` history.</what-built>
  <how-to-verify>
    Follow the RESEARCH "git filter-repo Runbook" exactly:
    1. PRECONDITION: Task 1's untracking commit is pushed to `main` and CI is GREEN on it (the merged state builds from S3 + fixtures). Do not proceed on a red CI.
    2. Fresh clone into /tmp: `git clone https://github.com/rainhead/beeatlas.git /tmp/beeatlas-filterrepo` and cd in. Confirm both paths exist in history (`git log --oneline -- public/data/collectors.json` prints SHAs).
    3. Purge: `git filter-repo --invert-paths --path public/data/collectors.json --path public/data/collector_event_pages.json`. Confirm ONLY these two paths are targeted (no broad globs).
    4. Re-add origin (filter-repo drops it): `git remote add origin https://github.com/rainhead/beeatlas.git`.
    5. Force-push: `git push --force origin main`.
    6. Verify: `git log --oneline -- public/data/collectors.json public/data/collector_event_pages.json` returns empty; `.git` size dropped.
    7. Realign the working clone: in `~/dev/beeatlas` run `git fetch origin && git reset --hard origin/main`. Confirm the two files are STILL on disk afterward (gitignored/untracked, D-02) — `git reset --hard` does not touch ignored untracked files.
    8. Finally, visit rainhead's live collector page after the next deploy and confirm it reflects current data (carries the undetermined-events fix) — Success-5.
  </how-to-verify>
  <verify>
    <automated>cd /tmp/beeatlas-filterrepo 2>/dev/null && [ -z "$(git log --oneline -- public/data/collectors.json public/data/collector_event_pages.json 2>/dev/null)" ] && echo HISTORY_PURGED || echo "operator: run the filter-repo runbook; this gate checks the /tmp clone post-purge"</automated>
  </verify>
  <resume-signal>Type "purged" once `git log -- public/data/collectors.json public/data/collector_event_pages.json` returns empty on the force-pushed main, the working clone is realigned (files still on disk), and the live collector page reflects current data.</resume-signal>
  <done>History purged of both paths (empty git log), repo .git size dropped, working clone realigned with files preserved on disk, and the live collector page reflects current data; operator confirmed "purged".</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator local clone -> GitHub remote | a force-push rewrites public `main` history (irreversible) |
| git index <-> working tree | untracking must drop from the index while preserving on-disk copies |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-171.1-10 | Tampering / availability | force-push of public main history | accept | Operator is effectively sole committer (D-01). Blast radius bounded: run on a fresh /tmp clone, scope `--path` to exactly the two files, confirm CI green on the untracking commit BEFORE purging, then `git reset --hard origin/main` on the working clone. Any other clone must re-clone or hard-reset (accepted disruption). |
| T-171.1-11 | Tampering | untrack split across commits -> files re-tracked | mitigate | Single atomic commit removes the .gitignore exceptions AND runs git rm --cached (Pitfall 2); Task 1 verify asserts untracked+ignored+on-disk together. |
| T-171.1-12 | Denial of service | working-tree copies deleted by `git rm` | mitigate | Use `git rm --cached` (index-only); Task 1 verify asserts both files still exist on disk; Task 3 step 7 reconfirms after reset. |
| T-171.1-13 | Denial of service | purge before CI green -> broken main with no recovery file | mitigate | Task 2 proves clean-checkout green; Task 3 precondition requires CI green on the pushed untracking commit before filter-repo runs. |

No package-manager installs in this plan — no `T-171.1-SC` supply-chain row applies.
</threat_model>

<verification>
- `.gitignore` no longer allowlists either file; blanket `/public/data/*` retained; both files untracked + ignored yet on disk (Task 1).
- `npm test` green with files present AND with files moved aside (Task 2).
- Operator-confirmed: history purged (empty `git log` for both paths), repo size dropped, working clone realigned with files preserved, live page reflects current data (Task 3).
</verification>

<success_criteria>
- ROADMAP Success Criterion 3 satisfied: both files removed from tracking and purged from history; .gitignore no longer allowlists them; repo size drops.
- ROADMAP Success Criterion 5 satisfied: live collector pages reflect current data (the undetermined-events fix rides along).
- D-01 + D-02 satisfied; D-07 final step complete.
</success_criteria>

<output>
Create `.planning/phases/171.1-collector-data-delivery-rebuild/171.1-04-SUMMARY.md` when done. Record the pre/post `.git` size, the empty `git log` proof, and the live-page confirmation.
</output>
