---
phase: 171.1
slug: collector-data-delivery-rebuild
status: draft
nyquist_compliant: false
wave_0_complete: false
created: 2026-06-27
---

# Phase 171.1 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution.

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | Vitest (config in `vite.config.ts` / `package.json`) |
| **Config file** | `vite.config.ts` (existing) |
| **Quick run command** | `npm test` |
| **Full suite command** | `npm test` |
| **Estimated runtime** | ~15–30 seconds |

---

## Sampling Rate

- **After every task commit:** Run `npm test`
- **After every plan wave:** Run `npm test` (clean-checkout green is the contract — zero S3 access)
- **Before `/gsd-verify-work`:** Full suite must be green AND a clean-checkout `npm run build` must succeed
- **Max feedback latency:** ~30 seconds

---

## Per-Task Verification Map

| Behavior | Wave | Source decision | Test Type | Automated Command | File Exists | Status |
|----------|------|-----------------|-----------|-------------------|-------------|--------|
| Fixture carries Phase 171 extended shape (`first_page_events`, `total_event_pages`, `total_event_count`, per-event `catalog_number`/`ecdysis_id`/`inat_url`) | 0 | D-05 | unit | `npm test` | ❌ W0 (`src/tests/fixtures/collectors.fixture.json`) | ⬜ pending |
| `data-collectors.test.ts` reads fixture, not `public/data/*`; passes on clean checkout | 1 | D-06 | unit | `npm test` | ✅ (repointed) | ⬜ pending |
| `_data/collectors.js` loader returns `[]` (not throw) when `public/data/collectors.json` absent | 1 | research finding #2 | unit | `npm test` | ✅ (existsSync guard) | ⬜ pending |
| `manifest.json` gains `collectors` + `collector_event_pages` keys; uploaded via `_upload_hashed` (plain, not gz) | 1 | D-08 | manual/smoke | inspect nightly dry-run / S3 manifest | ✅ (`nightly.sh`) | ⬜ pending |
| `deploy.yml` "Fetch build-time data from S3" pulls both hashed files into `public/data/` | 1 | D-08, Success-2 | smoke | CI run log shows both `aws s3 cp` | ✅ (`deploy.yml`) | ⬜ pending |
| `deploy.yml` accepts `repository_dispatch`; nightly fires it as final step | 1 | D-04 | integration (manual) | observe GitHub Actions run triggered by nightly | ✅ | ⬜ pending |
| Both files untracked, `.gitignore` allowlist removed, history purged | 2 | D-01, D-02 | manual | `git log -- public/data/collectors.json` returns empty | ✅ | ⬜ pending |
| Clean-checkout `npm run build` + `npm test` pass | 2 | Success-2 | smoke | `npm ci && npm run build && npm test` | ✅ | ⬜ pending |

*Status: ⬜ pending · ✅ green · ❌ red · ⚠️ flaky*

---

## Wave 0 Requirements

- [ ] `src/tests/fixtures/collectors.fixture.json` — tiny fixture with Phase 171 extended shape (covers fixture-shape assertions before test paths are repointed)
- [ ] `src/tests/fixtures/collector_event_pages.fixture.json` — at least one event-pages page (covers sub-page assertions)

*Existing Vitest infrastructure covers execution; only the fixtures are net-new.*

---

## Manual-Only Verifications

| Behavior | Source | Why Manual | Test Instructions |
|----------|--------|------------|-------------------|
| nightly → `repository_dispatch` → deploy runs nightly | D-04, Success-4 | Requires PAT stored on maderas + a live nightly cron run; cannot run in CI | After PAT is stored root-only on maderas and `nightly.sh` updated, run `data/nightly.sh` once (or fire the curl dispatch manually) and confirm a GitHub Actions deploy run starts |
| git history purged of both artifacts (~150 MB reclaimed) | D-01 | `git filter-repo` + force-push is a one-time destructive op on a fresh `/tmp` clone | `git log --oneline -- public/data/collectors.json public/data/collector_event_pages.json` returns empty after force-push; repo `.git` size drops |
| Live collector pages show fresh (non-stale) data | Success-5 | Requires an end-to-end nightly+deploy cycle against prod | Visit rainhead's collector page post-deploy; confirm undetermined-events fix is reflected |

---

## Validation Sign-Off

- [ ] All automatable behaviors have an `npm test` or CI-log verify; manual-only items are explicitly enumerated above
- [ ] Sampling continuity: no 3 consecutive code tasks without an automated verify
- [ ] Wave 0 creates both fixtures before any test repoint
- [ ] No watch-mode flags in any verify command
- [ ] Feedback latency < 30s
- [ ] `nyquist_compliant: true` set in frontmatter once plans are finalized

**Approval:** pending
