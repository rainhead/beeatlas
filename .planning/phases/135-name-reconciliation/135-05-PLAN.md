---
phase: 135-name-reconciliation
plan: 05
type: execute
wave: 2
depends_on: ["135-02", "135-03", "135-04"]
files_modified:
  - data/run.py
  - data/checklist_name_resolution_audit.csv
  - data/checklist_fuzzy_review.csv
  - data/dbt/seeds/gbif_checklist_synonyms.csv
  - data/dbt/tests/assert_no_anthophila_homonyms.sql
autonomous: false
requirements: [RCN-02, RCN-03, RCN-07]
user_setup:
  - service: gbif
    why: "One-time GBIF backbone lookup to bake the committed seed (off-nightly, zero nightly calls per RCN-03)"
    env_vars: []
    dashboard_config:
      - task: "No account needed — GBIF species/match is unauthenticated; requires outbound network during the single --refresh-checklist run only"
        location: "api.gbif.org via pygbif"
must_haves:
  truths:
    - "run.py runs resolve-checklist-names (no-op without flag) + a checklist resolution gate; nightly makes zero GBIF calls"
    - "The one-time --refresh-checklist run produced a committed audit CSV, fuzzy-review CSV, and GBIF seed"
    - "Full dbt build is green; the Anthophila homonym guard passes; Anthophila ancestor id is verified"
    - "Curator has reviewed checklist_name_resolution_audit.csv (HUMAN-REVIEW GATE before Phase 136)"
  artifacts:
    - path: "data/run.py"
      provides: "resolve-checklist-names + checklist-resolution-gate STEPS (no-op nightly)"
      contains: "resolve-checklist-names"
    - path: "data/checklist_name_resolution_audit.csv"
      provides: "Committed full audit produced by the one-time refresh"
      contains: "source"
  key_links:
    - from: "data/run.py"
      to: "resolve_checklist_names"
      via: "STEPS entry gated by _REFRESH_CHECKLIST = '--refresh-checklist' in sys.argv"
      pattern: "resolve_checklist_names"
    - from: "data/run.py"
      to: "check_checklist_resolution_gate"
      via: "STEPS gate entry after the checklist resolution step"
      pattern: "checklist-resolution-gate"
---

<objective>
Wire the checklist resolver into the pipeline as a no-op nightly step plus a build-blocking gate, run the one-time `--refresh-checklist` lookup to bake the committed audit/fuzzy/GBIF-seed artifacts, verify the Anthophila ancestor id the homonym guard depends on, prove the full dbt build (including the RCN-07 homonym test) is green, and pause for the curator HUMAN-REVIEW GATE that blocks Phase 136.

Purpose: RCN-02 (committed audit produced + gate) + RCN-03 (zero nightly network calls; one-time committed seed) + RCN-07 (homonym guard green against real data with verified Anthophila ancestor).
Output: modified run.py + committed audit/fuzzy/seed CSVs + verified homonym test + curator sign-off.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/135-name-reconciliation/135-RESEARCH.md
@.planning/phases/135-name-reconciliation/135-PATTERNS.md
@.planning/phases/135-name-reconciliation/135-VALIDATION.md

<interfaces>
From data/run.py (current STEPS, lines 84–109):
  _REFRESH_LINEAGE = "--refresh-lineage" in sys.argv   # line 49 — mirror for checklist
  STEPS = [... ("checklist", load_checklist), ("inat-obs", ...),
           ("resolve-taxon-ids", lambda: resolve_taxon_ids(refresh=_REFRESH_LINEAGE)),
           ("resolution-gate", check_resolution_gate), ...]

From data/resolve_checklist_names.py (Plan 135-02):
  resolve_checklist_names(refresh: bool = False) -> None    # no-op unless refresh
  check_checklist_resolution_gate() -> None                 # sys.exit on source='unresolved'

dbt: bash data/dbt/run.sh build   # full build; runs assert_no_anthophila_homonyms (Plan 135-01)
taxa.csv.gz: data/raw/taxa.csv.gz  # used to verify Anthophila ancestor = 630955 (RESEARCH A3)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire run.py STEPS (no-op resolve + checklist gate) + verify Anthophila ancestor</name>
  <files>data/run.py, data/dbt/tests/assert_no_anthophila_homonyms.sql</files>
  <read_first>
    - data/run.py lines 49 (_REFRESH_LINEAGE) + 84–109 (STEPS) + the import block — mirror the lineage flag + gate pair (135-PATTERNS.md §--refresh-style flag wiring; RESEARCH §RCN-03 run.py wiring places resolve-checklist-names AFTER 'checklist', the gate after it).
    - 135-RESEARCH.md Assumptions A3 — Anthophila ancestor assumed 630955; verify by tracing ancestry of a confirmed bee species in taxa.csv.gz before trusting the homonym test.
  </read_first>
  <action>Add `_REFRESH_CHECKLIST = "--refresh-checklist" in sys.argv` near the existing _REFRESH_LINEAGE line. Import `resolve_checklist_names` and `check_checklist_resolution_gate` from resolve_checklist_names. Insert two STEPS entries after `("checklist", load_checklist)`: `("resolve-checklist-names", lambda: resolve_checklist_names(refresh=_REFRESH_CHECKLIST))` and `("checklist-resolution-gate", check_checklist_resolution_gate)`. Then VERIFY the Anthophila ancestor id: query data/raw/taxa.csv.gz for a confirmed bee species (e.g. agapostemon texanus) and confirm its ancestry contains 630955; if the true Anthophila ancestor differs, correct the literal in assert_no_anthophila_homonyms.sql and note it in the SUMMARY. The resolve-checklist-names step must be a no-op on a normal `uv run python run.py` (no flag) — zero GBIF calls.</action>
  <verify>
    <automated>cd data && python -c "import gzip,csv; r=csv.reader(gzip.open('raw/taxa.csv.gz','rt'),delimiter='\t'); next(r); anc=[row[1] for row in r if row[4].lower()=='agapostemon texanus']; assert anc and '/630955/' in (anc[0]+'/'), anc; print('ANTHOPHILA-630955-OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "resolve-checklist-names" data/run.py` >= 1 and `grep -c "checklist-resolution-gate" data/run.py` >= 1.
    - resolve-checklist-names STEPS entry is gated by `_REFRESH_CHECKLIST` (no-op nightly).
    - The taxa.csv.gz probe confirms `/630955/` in a confirmed bee's ancestry (or the homonym test literal is corrected and noted).
    - `python -c "import run"` exits 0 (imports resolve), and a dry no-flag step list contains both new entries before resolve-taxon-ids.
  </acceptance_criteria>
  <done>run.py runs the no-op resolver + gate; Anthophila ancestor id confirmed (or corrected) for the homonym guard.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: One-time --refresh-checklist run to bake committed seed/audit (RCN-03)</name>
  <files>data/checklist_name_resolution_audit.csv, data/checklist_fuzzy_review.csv, data/dbt/seeds/gbif_checklist_synonyms.csv</files>
  <what-built>
    The resolver (Plan 135-02) and its run.py wiring (Task 1) are in place. The one-time GBIF lookup must run once WITH outbound network to materialize the committed artifacts that the nightly path then reads offline.
  </what-built>
  <action>Operator (or Claude if outbound network is available) runs the one-time GBIF refresh — `cd data && uv run python run.py --refresh-checklist` (or `resolve_checklist_names(refresh=True)` directly) — then commits the three regenerated artifacts: checklist_name_resolution_audit.csv, checklist_fuzzy_review.csv, dbt/seeds/gbif_checklist_synonyms.csv. This is a checkpoint because the network call is off-nightly and cannot run in offline CI (135-VALIDATION.md §Manual-Only Verifications).</action>
  <read_first>
    - 135-VALIDATION.md §Manual-Only Verifications — "One-time GBIF --refresh lookup produced the committed seed" is off-nightly and cannot run offline in CI.
    - 135-RESEARCH.md §RCN-03 — 178 names × 0.3s ≈ 54s one-time; rate-paced; defensive parse.
  </read_first>
  <how-to-verify>
    1. From repo root, run the one-time refresh against the working DuckDB with network available:
       `cd data && uv run python run.py --refresh-checklist`  (or invoke resolve_checklist_names(refresh=True) directly if a full pipeline run is undesirable).
    2. Confirm three artifacts are written/updated and git-tracked:
       - `data/checklist_name_resolution_audit.csv` — one row per distinct name, every row has a `source` in exact|synonym_seed|gbif|fuzzy|slash_lca|unresolved and a numeric confidence.
       - `data/checklist_fuzzy_review.csv` — >= 13 candidate rows.
       - `data/dbt/seeds/gbif_checklist_synonyms.csv` — GBIF-resolved mappings (header present even if empty).
    3. Confirm the gate is green: `cd data && uv run python -c "from resolve_checklist_names import check_checklist_resolution_gate as g; g()"` prints "checklist-resolution-gate: OK" (no source='unresolved' rows). If unresolved rows remain, the gate hard-fails — investigate those names before promotion.
    4. `git add` the three committed CSVs.
  </how-to-verify>
  <resume-signal>Type "approved" once the three artifacts are committed and the gate prints OK (or describe unresolved names that need attention).</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Full dbt build + homonym guard green; new pytest suite green</name>
  <files>data/dbt/tests/assert_no_anthophila_homonyms.sql</files>
  <read_first>
    - 135-VALIDATION.md §Sampling Rate — wave-merge command: new test files green + `bash dbt/run.sh build`.
    - data/dbt/tests/assert_no_anthophila_homonyms.sql (Plan 135-01) — confirm it runs in the full build.
  </read_first>
  <action>Run the full dbt build and confirm green, including the RCN-07 homonym guard and the new gbif_checklist_synonyms seed tests. Run the three new/extended pytest files green. Do NOT run or fix the 18 pre-existing dbt_sandbox failures in test_resolve_taxon_ids.py/test_dbt_diff.py — they are deferred (RESEARCH Pitfall 7); scope the pytest run to the new files. If the homonym guard fails, a real homonym exists in the resolved checklist names — surface it (do not weaken the test).</action>
  <verify>
    <automated>cd data && bash dbt/run.sh build 2>&1 | tail -5 && uv run pytest tests/test_resolve_checklist_names.py tests/test_checklist_pipeline.py tests/test_canonical_name.py -x 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `bash dbt/run.sh build` completes successfully (homonym guard + seed tests pass).
    - `cd data && bash dbt/run.sh test --select assert_no_anthophila_homonyms` PASSES.
    - The three new/extended pytest files are fully green.
    - The 18 pre-existing dbt_sandbox failures are NOT in scope (not newly broken, not fixed).
  </acceptance_criteria>
  <done>Full dbt build green with homonym guard; new pytest files green; deferred failures untouched.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: HUMAN-REVIEW GATE — curator reviews resolution audit before Phase 136</name>
  <files>data/dbt/seeds/occurrence_synonyms.csv</files>
  <what-built>
    The committed `checklist_name_resolution_audit.csv` lists every checklist name → taxon_id decision with source tier + 0–1 confidence; `checklist_fuzzy_review.csv` lists the fuzzy candidates. Per the ROADMAP HUMAN-REVIEW GATE, Phase 136 must not begin until the curator reviews these and promotes any trustworthy GBIF/fuzzy matches into `occurrence_synonyms.csv`.
  </what-built>
  <action>Curator reviews checklist_name_resolution_audit.csv (sorted by confidence) and checklist_fuzzy_review.csv, then promotes any trusted GBIF/fuzzy match by adding a one-line row to data/dbt/seeds/occurrence_synonyms.csv (the only promotion mechanism, D-03) and reruns the dbt build green. This is the ROADMAP HUMAN-REVIEW GATE; it is curator-only and never auto-advanced. Unconfirmed candidates stay unpromoted (auditable, inert) and do not block the build.</action>
  <how-to-verify>
    1. Open `data/checklist_name_resolution_audit.csv`; sort by `confidence` ascending to surface the lowest-trust decisions first.
    2. Review `gbif` and `fuzzy` rows: for each match the curator trusts, add a one-line row to `data/dbt/seeds/occurrence_synonyms.csv` (synonym,accepted_name,source) — this is the only promotion mechanism (D-03). GBIF/fuzzy rows are inert until promoted (D-02).
    3. Review `data/checklist_fuzzy_review.csv` (the 13+ misspelling candidates) and promote any confirmed corrections the same way.
    4. If any promotions were made, rerun `cd data && bash dbt/run.sh build` and confirm green.
    5. Leave unconfirmed candidates unpromoted — they remain auditable but inactive; this does not block the build (the D-04 gate only blocks on no-match-anywhere).
  </how-to-verify>
  <resume-signal>Type "approved" once the audit + fuzzy CSVs are reviewed and any trusted matches are promoted into occurrence_synonyms.csv (build re-green). Phase 136 may then proceed.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| GBIF API → committed seed | One-time off-nightly network response baked into git-tracked CSV |
| curator → occurrence_synonyms.csv | Human promotion is the only path from auto-candidate to live synonym |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-135-03 | Tampering / data-integrity | one-time GBIF lookup feeding committed seed | mitigate | Committed seed + zero nightly network calls (resolve-checklist-names no-op without flag) + curator promotion gate (Task 4); GBIF tier is promote-only (D-02) |
| T-135-04 | Information disclosure | unresolved names silently entering pipeline | mitigate | checklist-resolution-gate STEPS entry hard-fails the build on source='unresolved' (D-04); reported in audit CSV + build output |
| T-135-09 | Repudiation | over-matched auto-resolution promoted blindly | mitigate | HUMAN-REVIEW GATE (Task 4) — curator-only promotion; this checkpoint is never auto-advanced |
| T-135-SC | Tampering | pip installs | mitigate | No installs; pygbif/rapidfuzz pre-approved Phase 134 (no [ASSUMED]/[SUS] packages) |
</threat_model>

<verification>
- Nightly path (`uv run python run.py` no flag) makes zero GBIF calls; resolve-checklist-names is a no-op; gate reads the committed audit only.
- One-time refresh produced + committed the audit/fuzzy/seed artifacts; gate green.
- Full `bash dbt/run.sh build` green incl. RCN-07 homonym guard; new pytest files green.
- Curator reviewed the audit; HUMAN-REVIEW GATE satisfied before Phase 136.
</verification>

<success_criteria>
- RCN-02: committed audit CSV with per-name source+confidence; gate enforces no-match-anywhere blocking.
- RCN-03: zero nightly network calls; committed seed is the only nightly source.
- RCN-07: homonym guard runs in the full build and passes with the verified Anthophila ancestor.
- ROADMAP HUMAN-REVIEW GATE satisfied (curator sign-off) before Phase 136.
</success_criteria>

<output>
Create `.planning/phases/135-name-reconciliation/135-05-SUMMARY.md` when done.
</output>
