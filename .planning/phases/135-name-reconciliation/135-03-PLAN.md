---
phase: 135-name-reconciliation
plan: 03
type: execute
wave: 1
depends_on: ["135-01"]
files_modified:
  - data/checklist_pipeline.py
  - data/checklist_synonyms.csv
autonomous: true
requirements: [RCN-01, RCN-06]
must_haves:
  truths:
    - "checklist_records_full carries a canonical_name column = normalize_scientific_name(verbatim_name)"
    - "Slash-compound rows store the LCA's canonical name in canonical_name, verbatim retained in verbatim_name"
    - "reconcile() and SYNONYMS_PATH are gone from checklist_pipeline; load_checklist no longer calls reconcile"
    - "checklist_synonyms.csv has no active mappings (single synonym source = occurrence_synonyms/int_synonyms)"
  artifacts:
    - path: "data/checklist_pipeline.py"
      provides: "canonical_name column in _load_checklist_records_full; reconcile() retired"
      contains: "canonical_name"
    - path: "data/checklist_synonyms.csv"
      provides: "retired header-only synonym file (or removed) with retirement comment in git"
  key_links:
    - from: "data/checklist_pipeline.py"
      to: "data/canonical_name.py normalize_scientific_name"
      via: "per-distinct-name normalization applied to the fresh CREATE OR REPLACE TABLE"
      pattern: "normalize_scientific_name"
---

<objective>
Add a `canonical_name` column to `checklist_data.checklist_records_full` (computed via `normalize_scientific_name()`, with slash-compounds resolving to the LCA's accepted canonical name) and retire the dead `reconcile()` / `SYNONYMS_PATH` / `checklist_synonyms.csv` path so all checklist synonym resolution flows through the single `occurrence_synonyms` / `int_synonyms` dbt subsystem.

Purpose: RCN-01 (normalization stored on the table for the dbt stg model to JOIN on) + RCN-06 (single synonym source; retire the disjoint Python path per D-07).
Output: modified checklist_pipeline.py + retired checklist_synonyms.csv.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/135-name-reconciliation/135-RESEARCH.md
@.planning/phases/135-name-reconciliation/135-PATTERNS.md

<interfaces>
From data/checklist_pipeline.py (current, Phase 134 output):
  _load_checklist_records_full(con)   # CREATE OR REPLACE TABLE checklist_records_full (13 cols)
  _update_occurrences_canonical_name(con)  # lines 127–159 — the KEPT normalize-on-distinct pattern to mirror
  reconcile(con)                      # lines 162–221 — REMOVE
  SYNONYMS_PATH (line 28), UNMATCHED_PATH (line 29)  # REMOVE
  load_checklist()                    # line ~439 calls reconcile(con) — drop that call

From data/canonical_name.py:
  normalize_scientific_name(name) -> str | None   # line 73
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add canonical_name column to _load_checklist_records_full (RCN-01)</name>
  <files>data/checklist_pipeline.py</files>
  <read_first>
    - data/checklist_pipeline.py lines 127–159 (_update_occurrences_canonical_name) — the KEPT pattern: SELECT DISTINCT names, build mapping via normalize_scientific_name, apply (135-PATTERNS.md §checklist_pipeline.py).
    - data/checklist_pipeline.py CREATE OR REPLACE TABLE block (lines ~324–340) — add `canonical_name VARCHAR` to the fresh schema (avoids the ALTER-ADD-COLUMN re-run pitfall, RESEARCH Pitfall 5).
    - 135-RESEARCH.md §RCN-05 Pitfall 4 — slash detection must precede normalize; slash rows get the LCA's canonical name (e.g. the subgenus/genus accepted name), not the slash string.
  </read_first>
  <behavior>
    - checklist_records_full gains a non-null-for-resolvable `canonical_name` column.
    - Non-slash rows: canonical_name = normalize_scientific_name(verbatim_name).
    - Slash rows ('/' in verbatim_name): canonical_name = the LCA's accepted canonical name; verbatim_name unchanged (RCN-05 verbatim retention).
    - Re-running load_checklist twice is idempotent (CREATE OR REPLACE keeps the column fresh).
  </behavior>
  <action>Add `canonical_name VARCHAR` to the `CREATE OR REPLACE TABLE checklist_data.checklist_records_full (...)` schema. After the load, populate canonical_name following the `_update_occurrences_canonical_name` pattern: SELECT DISTINCT verbatim_name, build a mapping where non-slash names use normalize_scientific_name(). For slash-compound names, do LOCAL slash detection — this plan does NOT depend on 135-02, so do not import from resolve_checklist_names: detect `'/' in verbatim_name`, split the verbatim string on `'/'`, and construct the component binomials locally (a ~3-line check), then resolve to the LCA canonical name via a taxa.csv.gz ancestry lookup local to this module. Then UPDATE the table. Per D-05, the verbatim slash string stays in verbatim_name. Keep `_update_occurrences_canonical_name` untouched.</action>
  <verify>
    <automated>cd data && uv run pytest tests/test_checklist_pipeline.py -x -k "canonical or full" 2>&1 | tail -6</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "canonical_name" data/checklist_pipeline.py` >= 2 (schema + population).
    - The CREATE OR REPLACE TABLE schema lists `canonical_name VARCHAR`.
    - Slash detection (`'/' in verbatim_name`) precedes any normalize_scientific_name call on slash rows.
    - No import from resolve_checklist_names (135-02) — slash split + LCA lookup are local to this module (plans run in parallel in Wave 1).
    - test_canonical_name.py trailing-space test stays green (RCN-01 normalization unchanged).
  </acceptance_criteria>
  <done>checklist_records_full has a populated canonical_name column; slash rows carry the LCA canonical name with verbatim retained.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Retire reconcile() / SYNONYMS_PATH / checklist_synonyms.csv (RCN-06)</name>
  <files>data/checklist_pipeline.py, data/checklist_synonyms.csv</files>
  <read_first>
    - data/checklist_pipeline.py — SYNONYMS_PATH (line 28), UNMATCHED_PATH (line 29), reconcile() (lines 162–221), the `reconcile(con)` call in load_checklist (line ~439). 135-PATTERNS.md §checklist_pipeline.py shows the desired load_checklist() tail after removal.
    - 135-RESEARCH.md §RCN-06 — what to remove vs keep; checklist_synonyms.csv is header-only; either keep header-only with a retirement comment or delete it.
    - data/tests/test_checklist_pipeline.py (Plan 135-01) — test_no_active_reconcile_call (asserts 'reconcile' not in load_checklist source) + test_single_synonym_source (asserts checklist_synonyms.csv has 0 data rows AND no SYNONYMS_PATH in module).
  </read_first>
  <behavior>
    - inspect.getsource(load_checklist) contains no 'reconcile' (RCN-06).
    - Module has no SYNONYMS_PATH / UNMATCHED_PATH constant and no reconcile() function.
    - checklist_synonyms.csv has zero data rows; a git/comment trail records that occurrence_synonyms/int_synonyms is now the single source.
    - load_checklist still runs _load_checklist_records, _load_checklist_records_full, _update_occurrences_canonical_name (no behavioral regression elsewhere).
  </behavior>
  <action>Delete the `SYNONYMS_PATH` and `UNMATCHED_PATH` module constants, the `reconcile()` function body, and the `reconcile(con)` call in load_checklist(). Replace the call site with a comment: "reconcile() removed per D-07: checklist synonym resolution flows through occurrence_synonyms / int_synonyms (RCN-06)." Leave data/checklist_synonyms.csv as the header-only file with no data rows (or delete it) — add the retirement note in the commit message. Remove any now-unused imports tied only to reconcile/UNMATCHED.</action>
  <verify>
    <automated>cd data && uv run pytest tests/test_checklist_pipeline.py::test_no_active_reconcile_call tests/test_checklist_pipeline.py::test_single_synonym_source -x 2>&1 | tail -6</automated>
  </verify>
  <acceptance_criteria>
    - test_no_active_reconcile_call PASSES.
    - test_single_synonym_source PASSES (checklist_synonyms.csv has 0 data rows; no SYNONYMS_PATH in module).
    - `grep -c "def reconcile" data/checklist_pipeline.py` == 0.
    - `grep -c "SYNONYMS_PATH" data/checklist_pipeline.py` == 0.
    - Remaining checklist tests still pass: `cd data && uv run pytest tests/test_checklist_pipeline.py -x` green.
  </acceptance_criteria>
  <done>reconcile/SYNONYMS_PATH retired; single-synonym-source assertion green; no checklist-load regression.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| source CSV → checklist_records_full | committed Bartholomew CSV (Phase 134), trusted committed data |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-135-05 | Tampering | retiring reconcile() could drop a live synonym mapping | accept | checklist_synonyms.csv is already header-only (RESEARCH/codebase verified) — reconcile() currently no-ops; retirement removes dead code only |
| T-135-06 | Denial (build break) | canonical_name population on re-run | mitigate | Column added to CREATE OR REPLACE TABLE schema (always fresh) — avoids ALTER-ADD-COLUMN-exists failure (RESEARCH Pitfall 5) |
| T-135-SC | Tampering | pip installs | mitigate | No installs in this plan |
</threat_model>

<verification>
- `cd data && uv run pytest tests/test_checklist_pipeline.py -x` green (RCN-06 tests pass; no regression).
- canonical_name column present and populated; slash rows carry LCA canonical name, verbatim retained.
</verification>

<success_criteria>
- RCN-01: checklist_records_full.canonical_name = normalize_scientific_name(verbatim_name) (LCA name for slash rows).
- RCN-06: reconcile/SYNONYMS_PATH retired; single synonym source asserted.
</success_criteria>

<output>
Create `.planning/phases/135-name-reconciliation/135-03-SUMMARY.md` when done.
</output>
</content>
</invoke>
