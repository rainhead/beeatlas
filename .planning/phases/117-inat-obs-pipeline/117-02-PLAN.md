---
phase: 117
plan: 02
type: execute
wave: 1
depends_on:
  - 117-01
files_modified:
  - data/inat_obs_pipeline.py
  - data/run.py
autonomous: true
requirements:
  - PIPE-01
  - PIPE-02
  - PIPE-03
  - PIPE-04
tags:
  - pipeline
  - duckdb
  - parquet
  - canonicalization
must_haves:
  truths:
    - "Running `cd data && uv run python -c 'from inat_obs_pipeline import load_inat_obs; load_inat_obs()'` produces inat_obs.parquet under EXPORT_DIR with 12 columns"
    - "Every row in inat_obs.parquet has canonical_name resolved by canonical_name.canonicalize() applied to scientific_name (PIPE-02)"
    - "Rows whose id appears in dbt_sandbox.int_waba_link.specimen_observation_id (with raw waba fallback) are absent from the parquet (PIPE-03 — 821 known overlaps excluded)"
    - "floral_host is populated from the 'field:associated species with names lookup' CSV column where present and NULL where absent (PIPE-04)"
    - "Running `cd data && uv run pytest tests/test_inat_obs_pipeline.py -x` is GREEN (Plan 01's 4 RED tests turn green)"
    - "run.py STEPS registers ('inat-obs', load_inat_obs) between ('places-load', ...) and ('dbt-build', ...) so the new step runs after waba/ecdysis and before dbt"
  artifacts:
    - path: "data/inat_obs_pipeline.py"
      provides: "load_inat_obs() entry point + _load_excluded_ids() dedup helper"
      exports: ["load_inat_obs", "_load_excluded_ids", "CSV_PATH", "DB_PATH"]
      min_lines: 80
    - path: "data/run.py"
      provides: "Pipeline orchestrator with new inat-obs step registered"
      contains: "(\"inat-obs\", load_inat_obs)"
  key_links:
    - from: "data/inat_obs_pipeline.py"
      to: "data/canonical_name.py"
      via: "from canonical_name import canonicalize"
      pattern: "from canonical_name import canonicalize"
    - from: "data/inat_obs_pipeline.py"
      to: "dbt_sandbox.int_waba_link (DuckDB view)"
      via: "SELECT specimen_observation_id with try/except CatalogException fallback to inaturalist_waba_data raw tables"
      pattern: "dbt_sandbox.int_waba_link"
    - from: "data/run.py"
      to: "data/inat_obs_pipeline.py"
      via: "from inat_obs_pipeline import load_inat_obs; STEPS append"
      pattern: "from inat_obs_pipeline import load_inat_obs"
---

<objective>
Implement the iNat observations pipeline step: a new `data/inat_obs_pipeline.py` module exposing `load_inat_obs()` that reads `data/raw/inat_expert_obs.csv`, canonicalizes scientific names via D-04, dedups against WABA-linked specimen_observation_ids, and writes 12-column `inat_obs.parquet` to EXPORT_DIR. Wire the step into `data/run.py`'s STEPS list between `places-load` and `dbt-build`.

Purpose: PIPE-01 through PIPE-04 — produce a deduplicated, schema-correct parquet on every nightly run. After this plan, Plan 01's RED tests are GREEN.

Output: `data/inat_obs_pipeline.py` (new module) and a modified `data/run.py` registering the new step.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/117-inat-obs-pipeline/117-CONTEXT.md
@.planning/phases/117-inat-obs-pipeline/117-RESEARCH.md
@.planning/phases/117-inat-obs-pipeline/117-PATTERNS.md
@.planning/phases/117-inat-obs-pipeline/117-VALIDATION.md
@.planning/phases/117-inat-obs-pipeline/117-01-SUMMARY.md

@data/checklist_pipeline.py
@data/canonical_name.py
@data/run.py

<interfaces>
<!-- Module contract for inat_obs_pipeline.py (consumed by run.py and the Plan 01 tests). -->

Public surface:
- DB_PATH: str           (from os.environ DB_PATH; defaults to data/beeatlas.duckdb — copy verbatim from checklist_pipeline.py)
- CSV_PATH: pathlib.Path (Path(__file__).parent / "raw" / "inat_expert_obs.csv")
- _EXPORT_DIR: pathlib.Path (from os.environ EXPORT_DIR; defaults to repo_root/public/data — copy from run.py lines 51-54)
- load_inat_obs() -> None
- _load_excluded_ids(con: duckdb.DuckDBPyConnection) -> set[int]

Output table schema (`inat_obs_data.observations`) — 12 columns, in this exact ordinal order:
  obs_id BIGINT
  observed_on DATE
  lat DOUBLE
  lon DOUBLE
  canonical_name VARCHAR
  scientific_name VARCHAR
  user_login VARCHAR
  image_url VARCHAR
  license VARCHAR
  floral_host VARCHAR
  quality_grade VARCHAR
  obs_url VARCHAR

Output parquet: `${EXPORT_DIR}/inat_obs.parquet` (FORMAT PARQUET, CODEC SNAPPY).

CSV → output column mapping (per 117-RESEARCH.md Pattern 4):
  id                                            -> obs_id (cast int())
  observed_on                                   -> observed_on
  latitude                                      -> lat (float() if truthy else None)
  longitude                                     -> lon (float() if truthy else None)
  scientific_name                               -> scientific_name AND canonicalize() input
  user_login                                    -> user_login
  image_url                                     -> image_url
  license                                       -> license
  field:associated species with names lookup    -> floral_host (.get() fallback to None)
  quality_grade                                 -> quality_grade
  (synthesized)                                 -> obs_url = f"https://www.inaturalist.org/observations/{obs_id}"
  (synthesized)                                 -> canonical_name = canonicalize(scientific_name)

Dedup source (try/except chain):
  1. dbt_sandbox.int_waba_link.specimen_observation_id (preferred)
  2. inaturalist_waba_data.observations JOIN inaturalist_waba_data.observations__ofvs ON _dlt_root_id=_dlt_id WHERE field_id=18116 (fallback when dbt_sandbox absent — first-ever run)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement data/inat_obs_pipeline.py</name>
  <files>data/inat_obs_pipeline.py</files>
  <read_first>
    - data/checklist_pipeline.py (canonical analog — see 117-PATTERNS.md §`data/inat_obs_pipeline.py`; copy imports block, DB_PATH constant, `CREATE SCHEMA IF NOT EXISTS`, `CREATE OR REPLACE TABLE`, executemany, `# noqa: T201` print pattern, try/finally con.close())
    - data/canonical_name.py (signature + None-handling behavior; do NOT re-implement)
    - data/run.py lines 51-54 (`_EXPORT_DIR` env-driven Path constant — copy verbatim)
    - data/nightly.sh lines 138-171 (informs EXPORT_DIR contract; do not modify in this task)
    - data/tests/test_inat_obs_pipeline.py (Plan 01 stubs — the module must satisfy them; see fixture pattern for what schemas it pre-creates)
    - .planning/phases/117-inat-obs-pipeline/117-RESEARCH.md §`Complete Module Skeleton` and §`Pitfall 1/2/3/4/5`
    - .planning/phases/117-inat-obs-pipeline/117-PATTERNS.md §`Dedup helper function pattern`
  </read_first>
  <behavior>
    - load_inat_obs() with the committed Plan 01 CSV and a fresh DuckDB produces an `inat_obs_data.observations` table whose `information_schema.columns` row count is 12, in the exact ordinal order listed in `<interfaces>`.
    - load_inat_obs() with the committed Plan 01 CSV produces `${EXPORT_DIR}/inat_obs.parquet` and the parquet read back via duckdb has the same 12 columns.
    - Every row where `scientific_name` is non-empty has `canonical_name` non-null (PIPE-02 — `canonicalize()` returns None only for None / empty / whitespace).
    - Rows whose `obs_id` (from CSV column `id`) appears in the dedup ID set are absent from the output table. With the committed 45,354-row CSV and the production-like DB, the row count delta should approximate the 821 known overlaps (informational; the test asserts a single seeded ID is excluded).
    - `floral_host` is the verbatim CSV value of `field:associated species with names lookup` when present, NULL when absent or empty string.
    - `obs_url` is `https://www.inaturalist.org/observations/{obs_id}` for every row.
    - When `dbt_sandbox` schema does not exist (first-ever run), the dedup query falls back to the raw `inaturalist_waba_data` join (117-RESEARCH.md Pitfall 2; field_id = 18116 per Pattern 2 fallback).
    - load_inat_obs() prints (via `print(... )  # noqa: T201`) a one-line summary including total row count and dedup count.
    - load_inat_obs() uses `try/finally con.close()` to release the DuckDB file lock.
  </behavior>
  <action>
    Create `data/inat_obs_pipeline.py` following the checklist_pipeline.py analog. The module docstring opens with `"""Phase 117 iNat expert observations pipeline.` and references PIPE-01..05.

    Module-level constants (copy verbatim from analog files):
    - `DB_PATH = os.environ.get("DB_PATH", str(Path(__file__).parent / "beeatlas.duckdb"))`
    - `CSV_PATH = Path(__file__).parent / "raw" / "inat_expert_obs.csv"`
    - `_EXPORT_DIR = Path(os.environ.get("EXPORT_DIR", str(Path(__file__).parent.parent / "public" / "data")))`
    - `_FLORAL_HOST_FIELD = "field:associated species with names lookup"`
    - `_OBS_URL_PREFIX = "https://www.inaturalist.org/observations/"`

    Private helper `_load_excluded_ids(con) -> set[int]`: SELECT DISTINCT CAST(specimen_observation_id AS BIGINT) FROM dbt_sandbox.int_waba_link WHERE specimen_observation_id IS NOT NULL. Wrap in `try / except duckdb.CatalogException` and fall back to the raw `inaturalist_waba_data` join query shown in 117-RESEARCH.md Pattern 2 with field_id=18116. Return `{r[0] for r in rows}`.

    Public `load_inat_obs() -> None`:
    1. `con = duckdb.connect(DB_PATH)` inside `try: ... finally: con.close()`
    2. `excluded_ids = _load_excluded_ids(con)`
    3. `con.execute("CREATE SCHEMA IF NOT EXISTS inat_obs_data")` (117-RESEARCH.md Pitfall 1)
    4. Open `CSV_PATH` with `csv.DictReader`; iterate rows; for each row:
       - `obs_id = int(row["id"])` — explicit int cast (V5 input validation; 117-RESEARCH.md Pitfall 3)
       - `if obs_id in excluded_ids: continue`
       - `sci_name = (row.get("scientific_name") or "").strip() or None`
       - Append tuple in the EXACT column order from `<interfaces>`: `(obs_id, row.get("observed_on") or None, float(row["latitude"]) if row.get("latitude") else None, float(row["longitude"]) if row.get("longitude") else None, canonicalize(sci_name), sci_name, row.get("user_login") or None, row.get("image_url") or None, row.get("license") or None, row.get(_FLORAL_HOST_FIELD) or None, row.get("quality_grade") or None, f"{_OBS_URL_PREFIX}{obs_id}")`
    5. `CREATE OR REPLACE TABLE inat_obs_data.observations (...)` with the 12 columns and types from `<interfaces>` — column order is load-bearing (PIPE-01)
    6. `con.executemany("INSERT INTO inat_obs_data.observations VALUES (?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?)", rows)`
    7. Compute and print row count + dedup count + null-canonical-name count via `con.execute("SELECT count(*) ...").fetchone()[0]`; print with `# noqa: T201`
    8. `_EXPORT_DIR.mkdir(parents=True, exist_ok=True)`
    9. `out_path = _EXPORT_DIR / "inat_obs.parquet"`; `con.execute(f"COPY inat_obs_data.observations TO '{out_path}' (FORMAT PARQUET, CODEC SNAPPY)")`
    10. Print file size with `# noqa: T201`

    Do NOT import or use pandas. Do NOT use DuckDB `read_csv_auto` (117-RESEARCH.md Pitfall 3). Do NOT use pyarrow. Do NOT call dbt from within this module. Do NOT extend `inaturalist_pipeline.py`.

    Validation (V5 — input validation per 117-RESEARCH.md Security Domain): explicitly cast `obs_id` to `int()`. The CSV header is already verified in Plan 01; load_inat_obs() may additionally raise a clear KeyError when accessing `row["id"]` if the header is missing the `id` column — that is acceptable fast-fail behavior.
  </action>
  <verify>
    <automated>cd data &amp;&amp; uv run pytest tests/test_inat_obs_pipeline.py -x</automated>
  </verify>
  <acceptance_criteria>
    - `data/inat_obs_pipeline.py` exists and defines `load_inat_obs`, `_load_excluded_ids`, `DB_PATH`, `CSV_PATH`, `_EXPORT_DIR`, `_FLORAL_HOST_FIELD`, `_OBS_URL_PREFIX` at module scope
    - File imports `canonicalize` from `canonical_name` (grep `from canonical_name import canonicalize` returns 1 match)
    - File imports `csv`, `os`, `duckdb`, and `pathlib.Path`
    - File does NOT import pandas or pyarrow (`grep -E 'import (pandas|pyarrow)' data/inat_obs_pipeline.py` returns nothing)
    - File does NOT import from `inaturalist_pipeline` (`grep 'from inaturalist_pipeline' data/inat_obs_pipeline.py` returns nothing)
    - File contains `CREATE SCHEMA IF NOT EXISTS inat_obs_data` (grep returns 1 match)
    - File contains `CREATE OR REPLACE TABLE inat_obs_data.observations` (grep returns 1 match)
    - File contains `dbt_sandbox.int_waba_link` AND `duckdb.CatalogException` AND `inaturalist_waba_data.observations__ofvs` AND `field_id = 18116` (all four greps return at least 1 match — primary path + fallback present)
    - File contains `COPY inat_obs_data.observations TO` ... `FORMAT PARQUET, CODEC SNAPPY` (grep returns 1 match)
    - All four Plan 01 tests turn GREEN: `cd data && uv run pytest tests/test_inat_obs_pipeline.py -x` exits 0
    - Smoke run: `cd data && uv run python -c "from inat_obs_pipeline import load_inat_obs; load_inat_obs()"` exits 0 and creates `public/data/inat_obs.parquet`
    - Smoke parquet has 12 columns: `cd data && uv run python -c "import duckdb; c=duckdb.connect(); cols=c.execute(\"SELECT column_name FROM parquet_schema('../public/data/inat_obs.parquet') WHERE column_name != 'duckdb_schema'\").fetchall(); print(len(cols), cols)"` reports 12
  </acceptance_criteria>
  <done>The pipeline module exists, imports cleanly, satisfies all four Plan 01 integration tests, and produces a 12-column parquet from the committed CSV.</done>
</task>

<task type="auto">
  <name>Task 2: Register the inat-obs step in run.py</name>
  <files>data/run.py</files>
  <read_first>
    - data/run.py lines 30-46 (existing import block) and 80-102 (STEPS list — locate the slot between `("places-load", load_places_step)` and `("dbt-build", _run_dbt_build)`)
    - data/run.py lines 1-15 (module docstring — update the pipeline-order comment if it enumerates steps)
    - .planning/phases/117-inat-obs-pipeline/117-CONTEXT.md (`Step placement in run.py` — must run after `ecdysis`/`waba` and before `dbt-build`)
    - .planning/phases/117-inat-obs-pipeline/117-PATTERNS.md §`data/run.py (config, batch — self-modification)`
  </read_first>
  <action>
    Add `from inat_obs_pipeline import load_inat_obs` to the import block in `data/run.py` (alphabetical or grouped with the other pipeline-module imports near lines 30-46).

    Insert `("inat-obs", load_inat_obs),` into the STEPS list between `("places-load", load_places_step)` and `("dbt-build", _run_dbt_build)`. The exact position satisfies the CONTEXT D-discretion constraint: must run after ecdysis/waba (so `dbt_sandbox.int_waba_link` or its raw fallback is populated) and before dbt-build (so Phase 118's models can read inat_obs_data if needed in a later milestone).

    If the module docstring (lines 1-15) lists steps in execution order, add `inat-obs` to that list in the same position. If the docstring only describes usage, leave it.

    Do NOT reorder any existing steps. Do NOT add or remove any other entries.
  </action>
  <verify>
    <automated>cd data &amp;&amp; uv run python -c "import run; steps = [s[0] for s in run.STEPS]; assert 'inat-obs' in steps, steps; assert steps.index('inat-obs') &gt; steps.index('places-load'), steps; assert steps.index('inat-obs') &lt; steps.index('dbt-build'), steps; print('OK', steps)"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "from inat_obs_pipeline import load_inat_obs" data/run.py` returns exactly 1 match
    - `grep -n '("inat-obs", load_inat_obs)' data/run.py` returns exactly 1 match
    - The smoke command above confirms `inat-obs` appears in `STEPS` strictly between `places-load` and `dbt-build`
    - `cd data && uv run pytest tests/ -x` exits 0 (full data test suite green — Nyquist per-wave gate)
    - Running `cd data && DB_PATH=/tmp/beeatlas-smoke.duckdb EXPORT_DIR=/tmp/beeatlas-smoke uv run python -c "from inat_obs_pipeline import load_inat_obs; load_inat_obs()"` produces `/tmp/beeatlas-smoke/inat_obs.parquet` (validates _EXPORT_DIR env override works the way nightly.sh will exercise it in Plan 03)
  </acceptance_criteria>
  <done>`inat-obs` is registered in STEPS at the correct position; the full data test suite is green; an end-to-end smoke run produces inat_obs.parquet under a tmp EXPORT_DIR.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| committed CSV → load_inat_obs() | Trusted git-tracked input; threat focus is schema/type correctness, not injection. |
| DuckDB connection → file system (`DB_PATH`, `EXPORT_DIR`) | Both paths come from environment; defaults are repo-relative. Nightly.sh sets them to ephemeral tmp dirs. No untrusted-user input crosses this boundary. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-117-02-IV | Tampering / Input Validation (V5) | `obs_id` parsing in load_inat_obs() | mitigate | Explicit `int(row["id"])` cast per 117-RESEARCH.md Pitfall 3; fails fast with ValueError on non-numeric id. |
| T-117-02-IV2 | Tampering / Input Validation (V5) | `latitude`/`longitude` parsing | mitigate | Wrapped `float(row["latitude"]) if row.get("latitude") else None` — empty strings become NULL, malformed floats raise ValueError (acceptable fail-fast). |
| T-117-02-IS | Information Disclosure | Print statements include row counts | accept | Counts are aggregate; no PII or row content logged. |
| T-117-02-SC | Tampering / Supply Chain | new packages | n/a — no installs | No new packages added; 117-RESEARCH.md Package Legitimacy Audit confirms zero new dependencies. |
| T-117-02-DOS | Denial of Service | 45K row in-memory list before executemany | accept | Row size is bounded by the committed CSV; ~45K tuples of 12 small scalars is well under available RAM. Matches `checklist_pipeline.py` pattern (also accumulates a Python list before executemany). |

No high-severity threats. ASVS L1 V5 input validation is met by explicit type casts at parse points.
</threat_model>

<verification>
- `cd data && uv run pytest tests/test_inat_obs_pipeline.py -x` exits 0 (Plan 01's RED → GREEN)
- `cd data && uv run pytest tests/ -x` exits 0 (full data suite — Nyquist per-wave gate)
- `cd data && uv run python -c "from inat_obs_pipeline import load_inat_obs"` imports cleanly
- `inat-obs` step appears in `run.STEPS` strictly between `places-load` and `dbt-build`
- End-to-end smoke against `data/beeatlas.duckdb` (the local snapshot) produces `public/data/inat_obs.parquet` with 12 columns; row count is approximately `total_csv_rows - count(distinct excluded ids in DB)`
</verification>

<success_criteria>
1. PIPE-01: `inat_obs.parquet` has exactly the 12 columns in the order specified by `<interfaces>` (Plan 01's `test_schema_has_12_columns` passes)
2. PIPE-02: every row with non-empty `scientific_name` has a non-null `canonical_name` resolved via `canonical_name.canonicalize()` (Plan 01's `test_canonical_name_non_null` passes)
3. PIPE-03: rows whose `obs_id` appears in `dbt_sandbox.int_waba_link.specimen_observation_id` (or the raw waba fallback) are absent from the output (Plan 01's `test_dedup_excludes_specimen_obs` passes; against the local DB, count is approximately 821 rows excluded)
4. PIPE-04: `floral_host` equals the source CSV value when present and NULL when absent (Plan 01's `test_floral_host_mapping` passes)
5. `inat-obs` step runs from `run.py main()` in the correct order (after waba, before dbt-build)
</success_criteria>

<output>
Create `.planning/phases/117-inat-obs-pipeline/117-02-SUMMARY.md` when done. Record:
- Smoke-run row counts: total CSV rows, dedup IDs loaded from DB, final parquet row count
- Parquet file size (for nightly.sh capacity awareness in Plan 03)
- Whether the dedup query took the primary `dbt_sandbox.int_waba_link` path or the raw `inaturalist_waba_data` fallback
- Any deviations from the planned column order or types (none expected)
</output>
</content>
</invoke>