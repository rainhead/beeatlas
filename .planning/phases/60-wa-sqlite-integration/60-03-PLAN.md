---
phase: 60-wa-sqlite-integration
plan: 03
type: execute
wave: 3
depends_on: ["60-02"]
files_modified:
  - BENCHMARK.md
autonomous: false
requirements: []

must_haves:
  truths:
    - "App loads and renders map with specimen/sample dots in the browser"
    - "Filters work correctly (taxon, year, month, county, ecoregion, collector, elevation)"
    - "Table view works correctly (sort, paginate)"
    - "BENCHMARK.md has wa-sqlite numbers filled in"
  artifacts:
    - path: "BENCHMARK.md"
      provides: "wa-sqlite benchmark numbers"
      contains: "wa-sqlite"
  key_links:
    - from: "BENCHMARK.md"
      to: "frontend/src/sqlite.ts"
      via: "[BENCHMARK] console.log output"
      pattern: "wa-sqlite"
---

<objective>
Verify the wa-sqlite migration works end-to-end in the browser and record benchmark numbers.

Purpose: Confirm runtime correctness (tests only verify mock-level behavior, not actual SQL execution) and complete the benchmark comparison artifact.

Output: BENCHMARK.md with wa-sqlite column filled; user-verified working application.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/60-wa-sqlite-integration/60-CONTEXT.md
@.planning/phases/60-wa-sqlite-integration/60-02-SUMMARY.md
@BENCHMARK.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Browser verification of wa-sqlite migration</name>
  <files>frontend/src/sqlite.ts</files>
  <action>
User verifies the wa-sqlite migration works end-to-end in the browser. The executor starts the dev server with `cd frontend && npm run dev` and instructs the user to verify.
  </action>
  <what-built>Replaced DuckDB WASM with wa-sqlite + hyparquet throughout the frontend. All source files now import from sqlite.ts. SQL dialect rewrites applied. All 165 tests pass.</what-built>
  <how-to-verify>
1. Run `cd frontend && npm run dev` to start the dev server
2. Open http://localhost:5173 in Chrome
3. **Map loads**: Verify specimen dots appear on the Washington state map (should see hundreds of colored dots)
4. **Console check**: Open DevTools Console. Look for two `[BENCHMARK]` log lines:
   - `[BENCHMARK] WASM instantiate: XXX ms | heap: X.X -> X.X MB`
   - `[BENCHMARK] tablesReady: XXX ms total from init start | heap after tables: X.X MB | first-query latency: XXX ms`
5. **Filter test**: Select a taxon from the dropdown (e.g. "Bombus (genus)") -- map dots should filter. Clear the filter.
6. **Year filter**: Set year range (e.g. 2023-2024) -- dots should filter
7. **Table view**: Click the table view toggle -- table should show rows with correct data
8. **CSV export**: In table view, click "Download CSV" -- should download a CSV file
9. **No errors**: Console should show no red errors (warnings are OK)

Record the 6 benchmark numbers from the console `[BENCHMARK]` lines for the next task.
  </how-to-verify>
  <verify>User confirms app works in browser</verify>
  <done>User approves: map loads, filters work, table works, CSV exports, no console errors</done>
  <resume-signal>Type "approved" with the 6 benchmark numbers, or describe issues</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Fill BENCHMARK.md with wa-sqlite numbers</name>
  <files>BENCHMARK.md</files>
  <read_first>BENCHMARK.md</read_first>
  <action>
Update BENCHMARK.md with the wa-sqlite benchmark numbers provided by the user in the checkpoint.

1. Change the second column header from `wa-sqlite (Phase 61 after migration)` to `wa-sqlite (Phase 60)`

2. Fill in the 6 metric values from the user's checkpoint response:
   - WASM instantiate time (ms)
   - tablesReady total time (ms)
   - First-query latency (ms)
   - Heap before init (MB)
   - Heap after instantiate (MB)
   - Heap after tablesReady (MB)

The values come from the two `[BENCHMARK]` console.log lines emitted by sqlite.ts:
- Line 1: `WASM instantiate: {instantiate_time} ms | heap: {heap_before} -> {heap_after_instantiate} MB`
- Line 2: `tablesReady: {tables_ready_time} ms total from init start | heap after tables: {heap_after_tables} MB | first-query latency: {first_query} ms`
  </action>
  <verify>
    <automated>grep -c '___' /Users/rainhead/dev/beeatlas/BENCHMARK.md</automated>
  </verify>
  <acceptance_criteria>
    - `BENCHMARK.md` contains `wa-sqlite (Phase 60)` in the column header
    - `grep -c '___' BENCHMARK.md` returns 0 (no unfilled placeholders)
    - All 6 metric rows have numeric values in the wa-sqlite column
  </acceptance_criteria>
  <done>BENCHMARK.md has wa-sqlite column header updated and all 6 metrics filled with measured values</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

No new trust boundaries introduced -- this plan only verifies existing work and updates a documentation file.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-60-06 | Information Disclosure | BENCHMARK.md | accept | Contains only performance numbers (timing, heap size). No sensitive data. |
</threat_model>

<verification>
1. User confirms app works in browser (map, filters, table, CSV)
2. `grep -c '___' BENCHMARK.md` returns 0
3. BENCHMARK.md column header reads "wa-sqlite (Phase 60)"
</verification>

<success_criteria>
- User has verified the app works correctly in the browser
- BENCHMARK.md has all 6 wa-sqlite metrics filled in
- Phase 60 success criteria met: no duckdb-wasm imports, both parquets loaded via hyparquet+wa-sqlite, all SQL queries execute correctly, all 165 tests pass, TypeScript clean, benchmark filled
</success_criteria>

<output>
After completion, create `.planning/phases/60-wa-sqlite-integration/60-03-SUMMARY.md`
</output>
