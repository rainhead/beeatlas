---
phase: 116-code-quality-fixes
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - public/data/species.parquet
  - public/data/species.json
  - public/data/seasonality.json
autonomous: true
requirements: [CODE-03]

must_haves:
  truths:
    - "uv run pytest data/ exits 0 with zero failures"
    - "public/data/species.parquet matches sandbox schema (sandbox cols + appended slug VARCHAR)"
    - "public/data/species.json is byte-identical to data/dbt/target/sandbox/species.json"
    - "public/data/seasonality.json is byte-identical to data/dbt/target/sandbox/seasonality.json"
    - "The three previously failing dbt_diff tests now pass without skipping"
  artifacts:
    - path: "public/data/species.parquet"
      provides: "regenerated species table with current sandbox schema + slug"
    - path: "public/data/species.json"
      provides: "regenerated species JSON feed matching sandbox"
    - path: "public/data/seasonality.json"
      provides: "regenerated seasonality JSON feed matching sandbox"
  key_links:
    - from: "data/species_export.py:main"
      to: "public/data/{species.parquet,species.json,seasonality.json}"
      via: "reads data/dbt/target/sandbox/occurrences.parquet + species.parquet, writes to EXPORT_DIR"
      pattern: "EXPORT_DIR|public/data"
---

<objective>
Resolve the 3 pre-existing failures in `data/tests/test_dbt_diff.py` (test_species_parquet_schema_matches, test_species_json_matches, test_seasonality_json_matches) by regenerating the three public artifacts from the current dbt sandbox so they match sandbox output. Closes CODE-03.

Purpose: Get `uv run pytest data/` back to exit 0. The failures are a staleness issue — the sandbox grew a `checklist_count` column and JSON content was updated, but `public/data/` was not regenerated.
Output: Refreshed `public/data/species.parquet`, `public/data/species.json`, `public/data/seasonality.json` and a green full test suite.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@CLAUDE.md
</context>

<interfaces>
From data/species_export.py:
- `main()` connects to DuckDB at `DB_PATH`, calls `export_species_parquet(con)`, which writes all three artifacts (species.parquet, species.json, seasonality.json) into EXPORT_DIR (defaults to `public/data/`).

From data/tests/test_dbt_diff.py (lines 24-25):
- `SANDBOX = Path(__file__).resolve().parent.parent / "dbt" / "target" / "sandbox"`
- `PUBLIC = Path(__file__).resolve().parent.parent.parent / "public" / "data"`

Failing tests reference:
- `test_species_parquet_schema_matches` (line 324): expects `p_cols[:-1] == s_cols` and `p_cols[-1] == ('slug', 'VARCHAR')`.
- `test_species_json_matches` (line 372): byte-comparison of species.json.
- `test_seasonality_json_matches` (line 383): byte-comparison of seasonality.json.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Confirm sandbox is fresh and regenerate the three public artifacts</name>
  <files>public/data/species.parquet, public/data/species.json, public/data/seasonality.json</files>
  <read_first>
    - data/species_export.py (lines 240-253 — main() entry point; confirms it writes all three artifacts from sandbox)
    - data/tests/test_dbt_diff.py (lines 300-392 — the three failing test bodies and PUBLIC/SANDBOX path definitions)
    - data/run.py (lines 56-79 — _run_dbt_build to confirm sandbox artifacts location)
  </read_first>
  <action>
    Step A: Verify sandbox prerequisites exist by listing `data/dbt/target/sandbox/`. Required files: `occurrences.parquet`, `species.parquet`, `checklist.parquet`. If `species.parquet` or `occurrences.parquet` is missing, run `bash data/dbt/run.sh build` to regenerate the sandbox before proceeding.

    Step B: Regenerate the three public artifacts by running `species_export.main()` so it reads from the current sandbox and writes to `public/data/`. Run from the `data/` directory so relative paths resolve: `cd data && uv run python -c "from species_export import main; main()"`. This produces all three files: species.parquet (19 cols = sandbox cols + slug VARCHAR), species.json, seasonality.json.

    Step C: Run only the three previously failing tests to verify they now pass: `cd data && uv run pytest tests/test_dbt_diff.py::test_species_parquet_schema_matches tests/test_dbt_diff.py::test_species_json_matches tests/test_dbt_diff.py::test_seasonality_json_matches -v`.

    Step D: Run the full data test suite to confirm nothing else regressed: `cd data && uv run pytest tests/ -v --tb=short`. Exit code must be 0. If any test that previously passed now fails, investigate before declaring done — do NOT mask regressions by skipping.

    Step E: Verify the new `public/data/species.parquet` has the expected schema using `cd data && uv run python -c "import duckdb; cols=duckdb.execute(\"DESCRIBE SELECT * FROM read_parquet('../public/data/species.parquet')\").fetchall(); print(len(cols), cols[-1])"` — expect the last column to be `('slug', 'VARCHAR', ...)` and total column count to match sandbox + 1.

    Do NOT use `git add -f`. Note: `public/data/*` is gitignored (only `places.geojson` and `places.json` are excepted); the species artifacts exist on disk but are not git-tracked. Regeneration success is confirmed by tests passing, not by git status.
  </action>
  <verify>
    <automated>cd data && uv run pytest tests/ -v --tb=short</automated>
  </verify>
  <acceptance_criteria>
    - `cd data && uv run pytest tests/ -v` exits 0 with no failures and no new skips (compared to baseline).
    - `cd data && uv run pytest tests/test_dbt_diff.py::test_species_parquet_schema_matches tests/test_dbt_diff.py::test_species_json_matches tests/test_dbt_diff.py::test_seasonality_json_matches` exits 0 with all three reported as PASSED.
    - `cd data && uv run python -c "import duckdb; rows=duckdb.execute(\"DESCRIBE SELECT * FROM read_parquet('../public/data/species.parquet')\").fetchall(); assert rows[-1][0]=='slug' and rows[-1][1]=='VARCHAR', rows[-1]"` exits 0.
    - `cd data && uv run python -c "from pathlib import Path; s=Path('dbt/target/sandbox/species.json').read_bytes(); p=Path('../public/data/species.json').read_bytes(); assert s==p, f'differ at byte {next((i for i,(a,b) in enumerate(zip(s,p)) if a!=b), None)}'"` exits 0.
    - `cd data && uv run python -c "from pathlib import Path; s=Path('dbt/target/sandbox/seasonality.json').read_bytes(); p=Path('../public/data/seasonality.json').read_bytes(); assert s==p"` exits 0.
    - `public/data/species.parquet`, `public/data/species.json`, and `public/data/seasonality.json` exist on disk and match sandbox (confirmed by the byte/schema assertions above). These files are gitignored and will not appear in `git status`.
  </acceptance_criteria>
  <done>The three failing tests pass, full `uv run pytest data/tests/` exits 0, and the three public artifacts are byte/schema-aligned with the sandbox.</done>
</task>

</tasks>

<verification>
- `cd data && uv run pytest tests/ -v --tb=short` exits 0 with zero failures.
- All three previously failing tests are now in the PASSED list.
- No tests that were previously passing have flipped to failing.
</verification>

<success_criteria>
- `uv run pytest data/` (from repo root) and `cd data && uv run pytest tests/` both exit 0.
- Regenerated public artifacts match sandbox per the byte/schema checks above.
</success_criteria>

<output>
Create `.planning/phases/116-code-quality-fixes/116-03-SUMMARY.md` when done.
</output>
