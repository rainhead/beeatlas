---
phase: 087-incremental-materialization-experiment
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - data/dbt/models/intermediate/int_combined.sql
  - .planning/phases/087-incremental-materialization-experiment/run_results-baseline.json
  - .planning/phases/087-incremental-materialization-experiment/run_results-incr-full.json
  - .planning/phases/087-incremental-materialization-experiment/run_results-incr-noop.json
  - .planning/phases/087-incremental-materialization-experiment/run_results-incr-change.json
  - .planning/phases/087-incremental-materialization-experiment/timings.md
  - .planning/phases/087-incremental-materialization-experiment/baseline-rowcount.txt
  - .planning/phases/087-incremental-materialization-experiment/pre-experiment-sha.txt
autonomous: true
requirements:
  - TEST-03
requirements_addressed:
  - TEST-03

must_haves:
  truths:
    - "A baseline run_results.json from the current materialized='table' int_combined exists with status=success"
    - "int_combined.sql is configured with materialized='incremental' and an is_incremental() guard"
    - "ARM 2 of int_combined UNION ALL is skipped on incremental runs (AND FALSE guard) to avoid duplicate rows"
    - "Two further run_results.json captures exist: one full-refresh (incremental table first build) and one no-op + one data-change cycle"
    - "int_combined row count post-experiment matches pre-experiment baseline (no ARM 2 duplication)"
    - "Wall-clock observations and per-node execution_time are recorded in timings.md"
    - "Baseline measurement was taken with a cold fs-cache (sync + best-effort purge) and the flush state is documented in timings.md"
    - "Pre-experiment commit SHA is captured to pre-experiment-sha.txt so Plan 02 can assert a byte-identical revert"
  artifacts:
    - path: ".planning/phases/087-incremental-materialization-experiment/run_results-baseline.json"
      provides: "Pre-experiment full-build timing baseline"
      contains: '"status": "success"'
    - path: ".planning/phases/087-incremental-materialization-experiment/run_results-incr-full.json"
      provides: "First incremental run (--full-refresh) timing"
      contains: '"status": "success"'
    - path: ".planning/phases/087-incremental-materialization-experiment/run_results-incr-noop.json"
      provides: "Incremental no-op run timing (no source change)"
      contains: '"status": "success"'
    - path: ".planning/phases/087-incremental-materialization-experiment/run_results-incr-change.json"
      provides: "Incremental run after UPDATE+UPDATE-back data-change simulation"
      contains: '"status": "success"'
    - path: ".planning/phases/087-incremental-materialization-experiment/timings.md"
      provides: "Wall-clock + per-node execution_time table for all four runs"
      min_lines: 20
    - path: ".planning/phases/087-incremental-materialization-experiment/baseline-rowcount.txt"
      provides: "Pre-experiment int_combined COUNT(*) — single integer — anchor for Task 3 dedup assertion"
      contains: ""
    - path: ".planning/phases/087-incremental-materialization-experiment/pre-experiment-sha.txt"
      provides: "Pre-experiment git HEAD SHA — anchor for Plan 02 Task 2 byte-identical revert assertion"
      contains: ""
    - path: "data/dbt/models/intermediate/int_combined.sql"
      provides: "Experimental incremental config + ARM 1 watermark + ARM 2 skip guard"
      contains: "materialized='incremental'"
  key_links:
    - from: "data/dbt/models/intermediate/int_combined.sql"
      to: "data/dbt/target/run_results.json"
      via: "dbt build emits run_results on every invocation"
      pattern: "bash data/dbt/run.sh build --select int_combined"
    - from: ".planning/phases/087-.../run_results-*.json"
      to: ".planning/phases/087-.../timings.md"
      via: "execution_time extraction"
      pattern: "execution_time"
    - from: ".planning/phases/087-.../baseline-rowcount.txt"
      to: "Plan 01 Task 3 ARM 2 dedup assertion + Plan 02 Task 2 row count check"
      via: "single-integer sidecar consumed by automated verify blocks"
      pattern: "cat .*baseline-rowcount.txt"
    - from: ".planning/phases/087-.../pre-experiment-sha.txt"
      to: "Plan 02 Task 2 byte-identical revert assertion"
      via: "git diff <pre-sha> -- int_combined.sql"
      pattern: "git diff .* int_combined.sql"
---

<objective>
Capture observed evidence of how `materialized='incremental'` behaves on the only viable candidate model in the dbt project — `int_combined` — and record per-run timings into JSON captures + a summary table.

Purpose: Phase 87 requirement TEST-03 mandates that `dbt build` be run twice with at least one model configured incremental, and that observed behavior (wall-clock, status, ARM 2 dedup) be recorded. The captures produced by this plan are the evidence Plan 02 will consume to write the findings doc and recommendation for Phase 88.

Output: 4 captured `run_results-*.json` files in the phase directory, a `timings.md` summary table, an `int_combined.sql` file in its experimental (incremental) state, and two instrumentation sidecars (`baseline-rowcount.txt`, `pre-experiment-sha.txt`) that Plan 02 consumes to assert dedup + byte-identical revert. Plan 02 will revert the SQL file after consuming the evidence.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/087-incremental-materialization-experiment/087-RESEARCH.md
@.planning/phases/087-incremental-materialization-experiment/087-PATTERNS.md
@.planning/phases/087-incremental-materialization-experiment/087-VALIDATION.md
@CLAUDE.md

<interfaces>
<!-- Pre-experiment state of int_combined.sql, extracted so executor knows exactly what to replace -->

From data/dbt/models/intermediate/int_combined.sql (lines 1-6, the config block to be replaced):
- Current header comment (lines 1-5) explains the materialized='table' rationale and mirrors export.py:135-197
- Current config (line 6): `{{ config(materialized='table') }}`
- Structure below line 6: ARM 1 SELECT (lines 8-43) FROM int_ecdysis_base FULL OUTER JOIN int_samples_base LEFT JOIN int_specimen_obs_base; UNION ALL (line 45); ARM 2 SELECT (lines 47-86) WHERE sob.longitude IS NOT NULL AND sob.latitude IS NOT NULL.
- ARM 1 has column `e.modified` (line 26) — the watermark for is_incremental() WHERE clause.
- ARM 2 sets `NULL AS modified` (line 65) and `NULL AS ecdysis_id` (line 49) — the reason ARM 2 must be skipped on incremental runs (Pitfall 3).

The exact SQL to swap into int_combined.sql is in 087-RESEARCH.md `## Code Examples` lines 311-353, with the ARM 2 skip guard pattern further detailed in 087-PATTERNS.md `### data/dbt/models/intermediate/int_combined.sql` "ARM 2 incremental skip".

From data/dbt/run.sh (read-only — DO NOT modify, version pin is load-bearing per Pitfall 5):
- `bash data/dbt/run.sh <subcommand> [args]` invokes `uvx --from dbt-core==1.10.1 --with dbt-duckdb==1.10.1 dbt <subcommand> ... --profiles-dir data/dbt --project-dir data/dbt`
- After every run, `data/dbt/target/run_results.json` is overwritten — capture it immediately.

From .planning/phases/087-incremental-materialization-experiment/087-RESEARCH.md `## Measurement Protocol` lines 192-237:
- Run 1 (baseline): `time bash data/dbt/run.sh build --select int_combined+ --full-refresh` → copy run_results.json to run_results-baseline.json
- Run 2a (incremental first build with --full-refresh): `time bash data/dbt/run.sh build --select int_combined+ --full-refresh` → run_results-incr-full.json
- Run 2b (incremental no-op): `time bash data/dbt/run.sh build --select int_combined+` → run_results-incr-noop.json
- Run 2c (incremental with simulated data change): UPDATE ecdysis_data.occurrences SET modified = NOW() WHERE id = <pick-one>; `time bash data/dbt/run.sh build --select int_combined+`; UPDATE back to original value → run_results-incr-change.json

From 087-VALIDATION.md `## Manual-Only Verifications` row 2: "First measurement must include a `sync && purge` or equivalent fs-cache flush; document the flush in the findings." Dev workstation is Darwin (per CLAUDE.md) — `purge` requires sudo, so guard it: `sudo purge 2>/dev/null || true; sync`. The `sync` always works; `purge` is best-effort. Document the actual flush state achieved in timings.md.

Baseline wall-clock reference (measured 2026-05-13): `bash data/dbt/run.sh build --select int_combined+` ≈ 0.82s of model+test work. Best-case incremental savings on int_combined alone: < 0.24s. Do NOT expect dramatic numbers.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Capture baseline run_results (cold fs-cache) + row count + pre-experiment SHA</name>
  <files>
    .planning/phases/087-incremental-materialization-experiment/run_results-baseline.json,
    .planning/phases/087-incremental-materialization-experiment/timings.md,
    .planning/phases/087-incremental-materialization-experiment/baseline-rowcount.txt,
    .planning/phases/087-incremental-materialization-experiment/pre-experiment-sha.txt
  </files>
  <read_first>
    - data/dbt/models/intermediate/int_combined.sql (confirm current state is materialized='table' with no incremental config — DO NOT modify yet)
    - data/dbt/run.sh (read-only — pin is load-bearing)
    - .planning/phases/087-incremental-materialization-experiment/087-RESEARCH.md `## Measurement Protocol` lines 192-237
    - .planning/phases/087-incremental-materialization-experiment/087-VALIDATION.md `## Per-Task Verification Map` row 87-01-01
    - .planning/phases/087-incremental-materialization-experiment/087-VALIDATION.md `## Manual-Only Verifications` row 2 (cold-cache requirement)
  </read_first>
  <action>
    Step 1 — capture pre-experiment SHA: `git rev-parse HEAD > .planning/phases/087-incremental-materialization-experiment/pre-experiment-sha.txt`. This is the anchor Plan 02 Task 2 will diff against to assert a byte-identical revert of `int_combined.sql`. Step 2 — flush fs-cache before the baseline measurement (per 087-VALIDATION.md `## Manual-Only Verifications` row 2): run `sudo purge 2>/dev/null || true; sync`. `sync` always works; `purge` is macOS-specific and best-effort (silently no-ops if sudo is unavailable). Step 3 — immediately run `time bash data/dbt/run.sh build --select int_combined+ --full-refresh` from the repo root. Capture stderr+stdout to a temporary file so the `time` output is preserved. Step 4 — immediately after the run, `cp data/dbt/target/run_results.json .planning/phases/087-incremental-materialization-experiment/run_results-baseline.json` (the next dbt invocation will overwrite the target file). Step 5 — record the baseline int_combined row count for Task 3's dedup assertion: `uv run --project data python -c "import duckdb; print(duckdb.connect('data/beeatlas.duckdb', read_only=True).execute('SELECT COUNT(*) FROM dbt_sandbox.int_combined').fetchone()[0])" > .planning/phases/087-incremental-materialization-experiment/baseline-rowcount.txt`. The read_only=True flag avoids contention with dbt's exclusive lock (per RESEARCH `## Common Pitfalls`). Step 6 — write `.planning/phases/087-incremental-materialization-experiment/timings.md` with a `## baseline` section using the table skeleton in 087-PATTERNS.md `### .planning/phases/087-.../087-FINDINGS.md` "Measured Timings" (row "baseline (table)"). Record real wall-clock from `time`, per-node execution_time for `int_combined` and `marts.occurrences` from the captured JSON, and a "Cache state" notes-column entry that documents the achieved flush (e.g., "fs-cache flushed: sync ok, purge ok" OR "fs-cache flushed: sync ok, purge skipped (no sudo)"). Note in timings.md that this run uses the unmodified `materialized='table'` config. Do NOT modify int_combined.sql, run.sh, marts/occurrences.sql, or marts/species.sql.
  </action>
  <verify>
    <automated>test -f .planning/phases/087-incremental-materialization-experiment/run_results-baseline.json && jq -e '.results | map(select(.unique_id | contains("int_combined"))) | .[0].status == "success"' .planning/phases/087-incremental-materialization-experiment/run_results-baseline.json && test -f .planning/phases/087-incremental-materialization-experiment/timings.md && grep -q "^## baseline" .planning/phases/087-incremental-materialization-experiment/timings.md && grep -qi "fs-cache flushed" .planning/phases/087-incremental-materialization-experiment/timings.md && test -s .planning/phases/087-incremental-materialization-experiment/baseline-rowcount.txt && grep -Eq '^[0-9]+$' .planning/phases/087-incremental-materialization-experiment/baseline-rowcount.txt && test -s .planning/phases/087-incremental-materialization-experiment/pre-experiment-sha.txt && git cat-file -e "$(cat .planning/phases/087-incremental-materialization-experiment/pre-experiment-sha.txt)" && test -z "$(git diff data/dbt/models/intermediate/int_combined.sql)" && test -z "$(git diff data/dbt/run.sh)"</automated>
  </verify>
  <acceptance_criteria>
    - .planning/phases/087-incremental-materialization-experiment/run_results-baseline.json exists
    - run_results-baseline.json contains a result for `model.beeatlas.int_combined` with `"status": "success"`
    - .planning/phases/087-incremental-materialization-experiment/timings.md exists with a `## baseline` section that records wall-clock + int_combined execution_time + occurrences execution_time
    - timings.md `## baseline` section contains the literal phrase `fs-cache flushed` in the Cache state notes column (documenting actual flush state — `sync` always succeeds, `purge` is best-effort)
    - .planning/phases/087-incremental-materialization-experiment/baseline-rowcount.txt exists and contains a single integer (the pre-experiment `COUNT(*) FROM dbt_sandbox.int_combined`)
    - .planning/phases/087-incremental-materialization-experiment/pre-experiment-sha.txt exists and contains a resolvable git object SHA (anchor for Plan 02 Task 2 byte-identical revert assertion)
    - `git diff data/dbt/models/intermediate/int_combined.sql` is empty (no source edits in this task)
    - `git diff data/dbt/run.sh` is empty
  </acceptance_criteria>
  <done>Baseline JSON is on disk in the phase directory with a documented cold-cache flush, baseline-rowcount.txt + pre-experiment-sha.txt sidecars are written for downstream tasks to consume, timings.md has a populated `## baseline` row, and the dbt source tree is untouched. This is the pre-experiment reference point that Plan 02 will use to anchor its recommendation and revert assertion.</done>
</task>

<task type="auto">
  <name>Task 2: Convert int_combined.sql to incremental and run first --full-refresh capture</name>
  <files>
    data/dbt/models/intermediate/int_combined.sql,
    .planning/phases/087-incremental-materialization-experiment/run_results-incr-full.json,
    .planning/phases/087-incremental-materialization-experiment/timings.md
  </files>
  <read_first>
    - data/dbt/models/intermediate/int_combined.sql (current state — read in full; you will modify config block at top + add 2 is_incremental() guards)
    - data/dbt/models/intermediate/int_species_universe.sql lines 1-10 (sibling header comment shape — multi-line `--` block above `{{ config(...) }}`)
    - .planning/phases/087-incremental-materialization-experiment/087-RESEARCH.md `## Code Examples` lines 311-353 (exact incremental config + is_incremental() guards to apply)
    - .planning/phases/087-incremental-materialization-experiment/087-RESEARCH.md `## Common Pitfalls` Pitfalls 1, 3, 5 (do NOT touch marts; ARM 2 must be skipped; do NOT touch run.sh)
    - .planning/phases/087-incremental-materialization-experiment/087-PATTERNS.md `### data/dbt/models/intermediate/int_combined.sql (modified)` (the three insertion points: config block replacement, ARM 1 watermark insertion after line 43, ARM 2 skip after line 86)
  </read_first>
  <action>
    Edit `data/dbt/models/intermediate/int_combined.sql` to apply three changes (use Edit tool, not Write — preserve all rows of SQL between the edits): (1) Replace the config block at the top of the file. Keep the existing 5-line comment header (`-- UNION ALL ...` through `-- Mirrors export.py:135-197`) and prepend an additional comment line `-- EXPERIMENTAL — Phase 087: revert unless Phase 88 adopts (see .planning/phases/087-.../087-FINDINGS.md).`. Replace line 6 `{{ config(materialized='table') }}` with the multi-key config block from 087-RESEARCH.md lines 318-323 (materialized='incremental', unique_key='ecdysis_id', incremental_strategy='delete+insert', on_schema_change='fail'). (2) In ARM 1, after the line `LEFT JOIN {{ ref('int_specimen_obs_base') }} sob ON sob.waba_obs_id = e.specimen_observation_id` and before the blank line that precedes `UNION ALL`, insert the watermark guard: an `{% if is_incremental() %} WHERE e.modified > (SELECT COALESCE(MAX(modified), '1900-01-01') FROM {{ this }}) {% endif %}` block (exact SQL in 087-RESEARCH.md `## Code Examples` lines 332-334, and in 087-PATTERNS.md "ARM 1 is_incremental() watermark filter"). (3) In ARM 2, at the very end of the file (after `WHERE sob.longitude IS NOT NULL AND sob.latitude IS NOT NULL`), append the ARM 2 skip guard: `{% if is_incremental() %} AND FALSE {% endif %}` block (exact SQL in 087-PATTERNS.md "ARM 2 incremental skip"). After saving the file, run `time bash data/dbt/run.sh build --select int_combined+ --full-refresh` and immediately `cp data/dbt/target/run_results.json .planning/phases/087-incremental-materialization-experiment/run_results-incr-full.json`. Append a `## incr-full` section to timings.md with the wall-clock + per-node execution_time. Verify the dbt log line for int_combined now reads "incremental model" (grep stderr capture). Do NOT commit the SQL change yet — Plan 02 will handle the revert decision.
  </action>
  <verify>
    <automated>grep -q "materialized='incremental'" data/dbt/models/intermediate/int_combined.sql && grep -q "unique_key='ecdysis_id'" data/dbt/models/intermediate/int_combined.sql && grep -q "incremental_strategy='delete+insert'" data/dbt/models/intermediate/int_combined.sql && grep -q "is_incremental()" data/dbt/models/intermediate/int_combined.sql && grep -c "is_incremental()" data/dbt/models/intermediate/int_combined.sql | grep -q "^2$" && grep -q "AND FALSE" data/dbt/models/intermediate/int_combined.sql && test -f .planning/phases/087-incremental-materialization-experiment/run_results-incr-full.json && jq -e '.results | map(select(.unique_id | contains("int_combined"))) | .[0].status == "success"' .planning/phases/087-incremental-materialization-experiment/run_results-incr-full.json && grep -q "^## incr-full" .planning/phases/087-incremental-materialization-experiment/timings.md && test -z "$(git diff data/dbt/run.sh)" && test -z "$(git diff data/dbt/models/marts/occurrences.sql)" && test -z "$(git diff data/dbt/models/marts/species.sql)"</automated>
  </verify>
  <acceptance_criteria>
    - data/dbt/models/intermediate/int_combined.sql contains `materialized='incremental'`
    - data/dbt/models/intermediate/int_combined.sql contains `unique_key='ecdysis_id'`
    - data/dbt/models/intermediate/int_combined.sql contains `incremental_strategy='delete+insert'`
    - data/dbt/models/intermediate/int_combined.sql contains `on_schema_change='fail'`
    - data/dbt/models/intermediate/int_combined.sql contains exactly two `is_incremental()` blocks (one ARM 1 watermark, one ARM 2 skip)
    - data/dbt/models/intermediate/int_combined.sql contains `AND FALSE` (the ARM 2 incremental skip guard)
    - data/dbt/models/intermediate/int_combined.sql header includes the `-- EXPERIMENTAL — Phase 087: revert unless Phase 88 adopts` comment line
    - run_results-incr-full.json exists with status=success for int_combined
    - timings.md has an `## incr-full` section with wall-clock + execution_times recorded
    - `git diff data/dbt/run.sh` is empty (run.sh untouched per Pitfall 5)
    - `git diff data/dbt/models/marts/occurrences.sql` is empty (marts not touched per Pitfall 1)
    - `git diff data/dbt/models/marts/species.sql` is empty (marts not touched per Pitfall 1)
  </acceptance_criteria>
  <done>int_combined.sql is in its experimental incremental state with both is_incremental() guards in place, the first incremental --full-refresh build succeeded and is captured, and the marts + run.sh files remain untouched.</done>
</task>

<task type="auto">
  <name>Task 3: Run incremental no-op + data-change cycle, assert no ARM 2 duplication numerically</name>
  <files>
    .planning/phases/087-incremental-materialization-experiment/run_results-incr-noop.json,
    .planning/phases/087-incremental-materialization-experiment/run_results-incr-change.json,
    .planning/phases/087-incremental-materialization-experiment/timings.md
  </files>
  <read_first>
    - .planning/phases/087-incremental-materialization-experiment/087-RESEARCH.md `## Measurement Protocol` "Run 2b" and "Run 2c" (lines ~207-235) — UPDATE+UPDATE-back pattern, no row count delta
    - .planning/phases/087-incremental-materialization-experiment/087-RESEARCH.md `## Common Pitfalls` Pitfall 3 (ARM 2 dup check is the verification gate for this task)
    - .planning/phases/087-incremental-materialization-experiment/baseline-rowcount.txt (the row-count anchor written by Task 1)
  </read_first>
  <action>
    Run two further `dbt build` invocations against the now-incremental int_combined. (A) No-op run: `time bash data/dbt/run.sh build --select int_combined+` then `cp data/dbt/target/run_results.json .planning/phases/087-incremental-materialization-experiment/run_results-incr-noop.json`. Append a `## incr-noop` section to timings.md (wall-clock + execution_times). Verify via dbt log capture that int_combined was executed in "incremental" mode (not "table"). (B) Data-change run: Pick one row in `ecdysis_data.occurrences` (use `SELECT id, modified FROM ecdysis_data.occurrences LIMIT 1` against data/beeatlas.duckdb via `uv run --project data python -c "import duckdb; con = duckdb.connect('data/beeatlas.duckdb', read_only=True); print(con.execute('SELECT id, modified FROM ecdysis_data.occurrences LIMIT 1').fetchone())"`). Record the original modified value. Run `UPDATE ecdysis_data.occurrences SET modified = NOW() WHERE id = <chosen-id>`. Then `time bash data/dbt/run.sh build --select int_combined+` and `cp data/dbt/target/run_results.json .planning/phases/087-incremental-materialization-experiment/run_results-incr-change.json`. Append a `## incr-change` section to timings.md. Immediately revert the source UPDATE: `UPDATE ecdysis_data.occurrences SET modified = '<original-value>' WHERE id = <chosen-id>`. (C) ARM 2 dedup numeric assertion: read the baseline row count from `baseline-rowcount.txt` (written by Task 1). Capture the current `COUNT(*) FROM dbt_sandbox.int_combined` via `uv run --project data python -c "import duckdb; print(duckdb.connect('data/beeatlas.duckdb', read_only=True).execute('SELECT COUNT(*) FROM dbt_sandbox.int_combined').fetchone()[0])"` (read_only=True avoids dbt's exclusive lock per RESEARCH `## Common Pitfalls`). The two values MUST match within ±1 row (the +1 tolerance accounts for the modified-bumped row; the verify block enforces this numerically). Record BOTH numbers in timings.md under a new `## row counts` section with explicit labels "baseline:" and "current:" so the assertion is human-auditable. Also run `bash data/dbt/run.sh test --select int_combined` and confirm exit 0 (existing not_null_int_combined_is_provisional test catches ARM 2 NULL pollution if it occurred). Do NOT commit the source UPDATE; the revert must complete in this task.
  </action>
  <verify>
    <automated>test -f .planning/phases/087-incremental-materialization-experiment/run_results-incr-noop.json && jq -e '.results | map(select(.unique_id | contains("int_combined"))) | .[0].status == "success"' .planning/phases/087-incremental-materialization-experiment/run_results-incr-noop.json && test -f .planning/phases/087-incremental-materialization-experiment/run_results-incr-change.json && jq -e '.results | map(select(.unique_id | contains("int_combined"))) | .[0].status == "success"' .planning/phases/087-incremental-materialization-experiment/run_results-incr-change.json && grep -q "^## incr-noop" .planning/phases/087-incremental-materialization-experiment/timings.md && grep -q "^## incr-change" .planning/phases/087-incremental-materialization-experiment/timings.md && grep -q "^## row counts" .planning/phases/087-incremental-materialization-experiment/timings.md && BASELINE=$(cat .planning/phases/087-incremental-materialization-experiment/baseline-rowcount.txt) && CURRENT=$(uv run --project data python -c "import duckdb; print(duckdb.connect('data/beeatlas.duckdb', read_only=True).execute('SELECT COUNT(*) FROM dbt_sandbox.int_combined').fetchone()[0])") && DIFF=$(( CURRENT - BASELINE )) && [ $DIFF -ge -1 ] && [ $DIFF -le 1 ] && bash data/dbt/run.sh test --select int_combined</automated>
  </verify>
  <acceptance_criteria>
    - run_results-incr-noop.json exists with int_combined status=success
    - run_results-incr-change.json exists with int_combined status=success
    - timings.md has `## incr-noop`, `## incr-change`, and `## row counts` sections
    - `## row counts` section records BOTH baseline and current int_combined row counts with explicit labels
    - Current int_combined `COUNT(*)` is within ±1 of the integer in `baseline-rowcount.txt` (asserted numerically in the verify block, not just by section existence)
    - `bash data/dbt/run.sh test --select int_combined` exits 0 (no ARM 2 dedup regression caught by existing tests)
    - The source UPDATE on `ecdysis_data.occurrences.modified` has been reverted (the `<original-value>` was restored in the same task); no transient source pollution remains
  </acceptance_criteria>
  <done>All four run_results-*.json captures (baseline + incr-full + incr-noop + incr-change) exist in the phase directory; timings.md has populated sections for all four runs plus a numerically-asserted row-count check against baseline-rowcount.txt; the existing dbt test suite still passes against the incremental int_combined; the source UPDATE used to simulate data change has been reverted. The plan is complete and Plan 02 has all the evidence it needs to write findings + revert.</done>
</task>

</tasks>

<verification>
After all tasks complete:

```bash
# All four captures exist and have a successful int_combined run
for label in baseline incr-full incr-noop incr-change; do
  jq -e '.results | map(select(.unique_id | contains("int_combined"))) | .[0].status == "success"' \
    .planning/phases/087-incremental-materialization-experiment/run_results-${label}.json
done

# Sidecar instrumentation files exist and are well-formed
test -s .planning/phases/087-incremental-materialization-experiment/baseline-rowcount.txt
grep -Eq '^[0-9]+$' .planning/phases/087-incremental-materialization-experiment/baseline-rowcount.txt
test -s .planning/phases/087-incremental-materialization-experiment/pre-experiment-sha.txt
git cat-file -e "$(cat .planning/phases/087-incremental-materialization-experiment/pre-experiment-sha.txt)"

# timings.md has all four sections + row counts + cache-flush note
grep -E "^## (baseline|incr-full|incr-noop|incr-change|row counts)" .planning/phases/087-incremental-materialization-experiment/timings.md | wc -l   # expect 5
grep -qi "fs-cache flushed" .planning/phases/087-incremental-materialization-experiment/timings.md

# Experimental SQL is in place
grep -c "is_incremental()" data/dbt/models/intermediate/int_combined.sql   # expect 2
grep -c "materialized='incremental'" data/dbt/models/intermediate/int_combined.sql   # expect 1

# Forbidden files NOT modified
git diff --exit-code data/dbt/run.sh data/dbt/models/marts/occurrences.sql data/dbt/models/marts/species.sql

# dbt test suite still passes against incremental int_combined
bash data/dbt/run.sh test --select int_combined
```
</verification>

<success_criteria>
- 4 run_results JSON captures (baseline + 3 incremental variants) exist with int_combined success status
- timings.md exists with wall-clock and per-node execution_time for all 4 runs, a row-count check, and a documented fs-cache flush state on the baseline row
- baseline-rowcount.txt and pre-experiment-sha.txt sidecars exist for Plan 02 to consume
- int_combined.sql is in the experimental incremental state with ARM 1 watermark + ARM 2 skip guards
- ARM 2 dedup verified numerically: post-experiment row count within ±1 of baseline-rowcount.txt; existing dbt tests still pass
- data/dbt/run.sh, data/dbt/models/marts/occurrences.sql, data/dbt/models/marts/species.sql remain unchanged (Pitfalls 1 and 5 respected)
- Source data UPDATE used in data-change simulation has been reverted in the same task
</success_criteria>

<output>
After completion, create `.planning/phases/087-incremental-materialization-experiment/087-01-SUMMARY.md` with:
- What was measured (the 4 runs)
- Headline numbers (wall-clock for each run, int_combined execution_time deltas)
- Whether ARM 2 dedup held (numeric: baseline N vs current M, diff ≤ 1)
- Cache state achieved on baseline run (sync ok + purge ok/skipped)
- Open observations to feed into Plan 02's findings doc (e.g., "downstream occurrences mart rebuilt fully on every run regardless — confirms Pitfall 2")
</output>
