---
phase: 087-incremental-materialization-experiment
plan: 02
type: execute
wave: 2
depends_on:
  - "087-01"
files_modified:
  - .planning/phases/087-incremental-materialization-experiment/087-FINDINGS.md
  - data/dbt/models/intermediate/int_combined.sql
autonomous: true
requirements:
  - TEST-03
requirements_addressed:
  - TEST-03

must_haves:
  truths:
    - "A written finding records: does incremental work with external materializations? does it speed up nightly builds? what is the wall-clock comparison?"
    - "The findings doc contains a `## Recommendation` section addressed to Phase 88 with one of two outcomes: 'use incremental' + selector, OR 'full rebuilds' + stated reason"
    - "The findings doc Measured Timings table records the cache state of the baseline run (sourced from timings.md, written by Plan 01 Task 1)"
    - "The experimental change to int_combined.sql is removed from the working tree — either by reverting before any commit OR by a follow-up commit that restores the pre-experiment file"
    - "The diff of `data/dbt/models/intermediate/int_combined.sql` between the post-revert working tree and the pre-experiment SHA (captured by Plan 01 Task 1 to pre-experiment-sha.txt) is byte-identical (empty)"
    - "Post-revert, int_combined builds as materialized='table' and the full dbt build returns to baseline behavior"
  artifacts:
    - path: ".planning/phases/087-incremental-materialization-experiment/087-FINDINGS.md"
      provides: "Phase 87 evidence + recommendation doc that Phase 88's planner will read"
      contains: "## Recommendation"
      min_lines: 60
    - path: "data/dbt/models/intermediate/int_combined.sql"
      provides: "Restored pre-experiment SQL (materialized='table'), byte-identical to pre-experiment SHA"
      contains: "materialized='table'"
  key_links:
    - from: ".planning/phases/087-.../run_results-*.json + timings.md"
      to: ".planning/phases/087-.../087-FINDINGS.md"
      via: "evidence transcription"
      pattern: "execution_time|wall-clock"
    - from: ".planning/phases/087-.../087-FINDINGS.md `## Recommendation`"
      to: "Phase 88 planner"
      via: "direct reference in Phase 88 ROADMAP entry"
      pattern: "## Recommendation"
    - from: ".planning/phases/087-.../pre-experiment-sha.txt"
      to: "Task 2 byte-identical revert assertion"
      via: "git diff $(cat pre-experiment-sha.txt) -- int_combined.sql"
      pattern: "git diff .* int_combined.sql"
---

<objective>
Translate the evidence captured by Plan 01 into a phase-level findings document with an unambiguous recommendation for Phase 88, then restore the dbt source tree to a state byte-identical to the pre-experiment SHA.

Purpose: Phase 87's contract with Phase 88 is the recommendation in `087-FINDINGS.md`. Phase 88's planner needs a single section to read that answers: "should `nightly.sh` use `dbt build --select <selector>` with incremental, or just `dbt build` with full rebuilds?" The doc must be backed by the four run_results captures (including their cache-state notes), not by priors. After the doc lands, the experimental SQL change is reverted so the working tree is clean for Phase 88 to start from — and the revert is asserted byte-identical against the pre-experiment SHA captured by Plan 01 Task 1.

Output: `.planning/phases/087-incremental-materialization-experiment/087-FINDINGS.md` (new), and a restored `data/dbt/models/intermediate/int_combined.sql` matching the pre-experiment commit byte-for-byte (asserted via `git diff <pre-experiment-sha> -- ...` returning empty). The revert may be implemented either as a direct working-tree revert (single git checkout/Edit) or as a follow-up commit on top of Plan 01's experimental commit — either pattern satisfies the goal-backward truth and the byte-identical assertion.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/087-incremental-materialization-experiment/087-RESEARCH.md
@.planning/phases/087-incremental-materialization-experiment/087-PATTERNS.md
@.planning/phases/087-incremental-materialization-experiment/087-VALIDATION.md
@.planning/phases/087-incremental-materialization-experiment/087-01-PLAN.md

<interfaces>
<!-- The exact evidence Plan 02 consumes -->

From .planning/phases/087-incremental-materialization-experiment/ (produced by Plan 01):
- run_results-baseline.json — pre-experiment full build (materialized='table')
- run_results-incr-full.json — first incremental run with --full-refresh
- run_results-incr-noop.json — incremental no-op (no source changes)
- run_results-incr-change.json — incremental after UPDATE+UPDATE-back on ecdysis_data.occurrences.modified
- timings.md — wall-clock + per-node execution_time table with sections baseline / incr-full / incr-noop / incr-change / row counts; baseline row includes a "Cache state" notes-column entry (e.g., "fs-cache flushed: sync ok, purge ok")
- baseline-rowcount.txt — single integer, pre-experiment `COUNT(*) FROM dbt_sandbox.int_combined`
- pre-experiment-sha.txt — single short SHA, the git HEAD at the start of the experiment; Task 2 diffs against this to assert a byte-identical revert

From .planning/phases/087-incremental-materialization-experiment/087-RESEARCH.md `## Decision Criteria for Phase 88` lines 389-407:
- Adopt incremental if ALL of: ≥30% wall-clock drop on incr-noop vs baseline; byte-identical occurrences.parquet; no duplicate rows; Phase 88 CLEAN-02 accommodated by --full-refresh step.
- Keep full rebuilds if ANY of: <30% savings; ARM 2 requires separate rebuild path (it does — Plan 01 used `AND FALSE`); external mart dominates wall-clock; bookkeeping overhead exceeds savings.
- Expected outcome (HIGH confidence): full rebuilds.

From .planning/phases/087-incremental-materialization-experiment/087-PATTERNS.md `### .planning/phases/087-.../087-FINDINGS.md (new)`:
- Analog doc: .planning/phases/086-port-remaining-transforms/086-VERIFICATION.md
- Frontmatter shape: phase, verified (ISO datetime), status (passed for spike if recommendation is recorded), score, deferred
- Section skeleton: Observable Truths table; Answers to ROADMAP Questions; Measured Timings table (must include a "Cache state" Notes column entry for the baseline row, transcribed from timings.md); Recommendation for Phase 88; Rollback Status checklist

From .planning/phases/087-incremental-materialization-experiment/087-RESEARCH.md `## Rollback Path` lines 411-423:
- The experiment is a single-file edit (int_combined.sql)
- Rollback options: `git revert <commit>` (if Plan 01 committed) OR `git checkout -- data/dbt/models/intermediate/int_combined.sql` (if uncommitted)
- Belt-and-suspenders: after the revert, run `bash data/dbt/run.sh build --select int_combined+ --full-refresh` to guarantee DuckDB rebuilds int_combined as a fresh table
- Byte-identical assertion: `git diff $(cat pre-experiment-sha.txt) -- data/dbt/models/intermediate/int_combined.sql` MUST be empty

Pre-experiment SQL state (for reference — the target of the revert):
- File: data/dbt/models/intermediate/int_combined.sql
- Header: 5-line `--` comment block ending with `-- Mirrors export.py:135-197 (combined CTE).`
- Config (line 6 of pre-experiment file): `{{ config(materialized='table') }}`
- No `is_incremental()` blocks anywhere
- No `AND FALSE` guard at end of ARM 2
- No `-- EXPERIMENTAL — Phase 087` comment line
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write 087-FINDINGS.md with evidence + recommendation for Phase 88</name>
  <files>.planning/phases/087-incremental-materialization-experiment/087-FINDINGS.md</files>
  <read_first>
    - .planning/phases/087-incremental-materialization-experiment/run_results-baseline.json
    - .planning/phases/087-incremental-materialization-experiment/run_results-incr-full.json
    - .planning/phases/087-incremental-materialization-experiment/run_results-incr-noop.json
    - .planning/phases/087-incremental-materialization-experiment/run_results-incr-change.json
    - .planning/phases/087-incremental-materialization-experiment/timings.md
    - .planning/phases/087-incremental-materialization-experiment/087-RESEARCH.md `## TEST-03: The Definitive Answer` (lines 67-115)
    - .planning/phases/087-incremental-materialization-experiment/087-RESEARCH.md `## Decision Criteria for Phase 88` (lines 389-407)
    - .planning/phases/087-incremental-materialization-experiment/087-PATTERNS.md `### .planning/phases/087-incremental-materialization-experiment/087-FINDINGS.md (new)` (section skeleton)
    - .planning/phases/086-port-remaining-transforms/086-VERIFICATION.md (frontmatter + observable-truths-table analog)
  </read_first>
  <action>
    Create `.planning/phases/087-incremental-materialization-experiment/087-FINDINGS.md` following the skeleton in 087-PATTERNS.md lines 67-126. Use YAML frontmatter (phase, verified ISO datetime, status: passed, score, deferred). Sections in order: (1) `# Phase 087: Incremental Materialization Experiment — Findings` with Phase Goal verbatim from ROADMAP success criteria 1-3 and executed date. (2) `## Observable Truths` table with 5 rows mirroring 087-PATTERNS.md lines 87-93 (each truth → status VERIFIED + evidence pointer to the run_results file or timings.md section). (3) `## Answers to ROADMAP Questions` with three Q/A pairs: Q1 "Does materialized='incremental' work with external materializations?" → A grounded in 087-RESEARCH.md `## TEST-03: The Definitive Answer` (cite the dbt-duckdb 1.10.1 README statement and issue #74); Q2 "Does it speed up nightly builds?" → A backed by the actual wall-clock numbers from timings.md (compare baseline vs incr-noop vs incr-change); Q3 "Wall-clock comparison?" → A table reproducing the timings.md numbers verbatim. (4) `## Measured Timings` table with 4 rows (baseline / incr-full / incr-noop / incr-change), each row including wall-clock (`time`), int_combined execution_time, occurrences execution_time, and a Notes column. The baseline row's Notes column MUST include the cache-state entry transcribed from timings.md (e.g., "fs-cache flushed: sync ok, purge ok") — this documents that the baseline was taken cold per 087-VALIDATION.md `## Manual-Only Verifications` row 2. Pull the actual numbers from timings.md and the run_results-*.json files using `jq '.results[] | select(.unique_id | contains("int_combined") or contains("marts.occurrences")) | {unique_id, execution_time, status}'`. (5) `## ARM 2 Dedup Check` — short subsection recording the pre vs post row counts from timings.md `## row counts` (which Plan 01 Task 3 asserted numerically against baseline-rowcount.txt) and whether the `delete+insert` + `AND FALSE` guard prevented duplication; cite Pitfall 3. (6) `## Recommendation` — the load-bearing section Phase 88 will read. Apply the decision criteria from 087-RESEARCH.md lines 389-407: if measured incr-noop wall-clock dropped ≥30% vs baseline AND no dup observed AND downstream occurrences mart did not dominate → recommend "use incremental" with `--select int_combined+` as the selector and note the Phase 88 CLEAN-02 `--full-refresh` requirement; otherwise recommend "full rebuilds" with the specific reason(s) from the measured evidence (external mart dominates / savings <30% / ARM 2 complexity tax). The recommendation MUST be evidence-anchored — quote the actual numbers, not the prior-expected ones. (7) `## Required Follow-up if Adopted` — single bullet citing 087-RESEARCH.md Pitfall 4 (CLEAN-02 column drop requires --full-refresh). (8) `## Rollback Status` — checklist for Task 2 to tick: (a) int_combined.sql reverted to materialized='table'; (b) byte-identical diff against pre-experiment SHA is empty; (c) belt-and-suspenders `--full-refresh` succeeded; (d) row count matches baseline. Leave the boxes unticked — Task 2 ticks them. Do NOT modify any other file in this task; do NOT touch int_combined.sql, run.sh, or the marts.
  </action>
  <verify>
    <automated>test -f .planning/phases/087-incremental-materialization-experiment/087-FINDINGS.md && grep -q "^## Recommendation" .planning/phases/087-incremental-materialization-experiment/087-FINDINGS.md && grep -q "^## Observable Truths" .planning/phases/087-incremental-materialization-experiment/087-FINDINGS.md && grep -q "^## Answers to ROADMAP Questions" .planning/phases/087-incremental-materialization-experiment/087-FINDINGS.md && grep -q "^## Measured Timings" .planning/phases/087-incremental-materialization-experiment/087-FINDINGS.md && grep -q "^## Rollback Status" .planning/phases/087-incremental-materialization-experiment/087-FINDINGS.md && grep -qi "fs-cache flushed" .planning/phases/087-incremental-materialization-experiment/087-FINDINGS.md && grep -Eq "(use incremental|full rebuilds)" .planning/phases/087-incremental-materialization-experiment/087-FINDINGS.md && [ $(wc -l < .planning/phases/087-incremental-materialization-experiment/087-FINDINGS.md) -ge 60 ]</automated>
  </verify>
  <acceptance_criteria>
    - .planning/phases/087-incremental-materialization-experiment/087-FINDINGS.md exists
    - Has YAML frontmatter with `phase`, `verified`, `status` keys
    - Contains sections: `## Observable Truths`, `## Answers to ROADMAP Questions`, `## Measured Timings`, `## ARM 2 Dedup Check`, `## Recommendation`, `## Rollback Status`
    - The Measured Timings baseline row's Notes column contains the literal phrase `fs-cache flushed` (transcribed from timings.md)
    - `## Recommendation` section contains EITHER the phrase "use incremental" with an explicit `--select` selector mentioned, OR the phrase "full rebuilds" with a stated reason
    - The Q1 answer cites 087-RESEARCH.md or the dbt-duckdb README on incremental + external being unsupported
    - The Measured Timings table includes actual numbers transcribed from the run_results JSON / timings.md (not placeholders like `[TBD]` or `<n>`)
    - File is at least 60 lines (substantive content, not a stub)
    - No edits to data/dbt/models/intermediate/int_combined.sql in this task (still in experimental state at end of task)
  </acceptance_criteria>
  <done>087-FINDINGS.md is on disk with all required sections populated from real evidence (including the cold-cache baseline state), and the recommendation is unambiguous — Phase 88's planner can read the doc and act without re-running the experiment.</done>
</task>

<task type="auto">
  <name>Task 2: Revert int_combined.sql byte-identically to pre-experiment SHA and verify clean baseline build</name>
  <files>
    data/dbt/models/intermediate/int_combined.sql,
    .planning/phases/087-incremental-materialization-experiment/087-FINDINGS.md
  </files>
  <read_first>
    - data/dbt/models/intermediate/int_combined.sql (current experimental state)
    - .planning/phases/087-incremental-materialization-experiment/pre-experiment-sha.txt (the SHA to diff against — written by Plan 01 Task 1)
    - .planning/phases/087-incremental-materialization-experiment/baseline-rowcount.txt (the row-count anchor for the Rollback Status checklist)
    - .planning/phases/087-incremental-materialization-experiment/087-RESEARCH.md `## Rollback Path` (lines 411-423)
    - .planning/phases/087-incremental-materialization-experiment/087-FINDINGS.md `## Rollback Status` (the checklist this task ticks)
    - Output of `git log --oneline -- data/dbt/models/intermediate/int_combined.sql | head -5` (decide between `git checkout --` for uncommitted edits OR `git revert <hash>` if Plan 01 committed the experimental change)
  </read_first>
  <action>
    Detect whether Plan 01's experimental change is committed: run `git log --oneline -- data/dbt/models/intermediate/int_combined.sql | head -3`. If the most recent commit modifying that file introduced the incremental config (look for "Phase 087" or "incremental" in the commit message OR diff the file against HEAD~1 to confirm), revert via either (a) `git revert <experimental-hash> --no-edit` to create an explicit revert commit (preserves "this was tried" history per RESEARCH Open Question 1), or (b) Edit the file directly to restore the pre-experiment shape: remove the `-- EXPERIMENTAL — Phase 087 ...` comment line; replace the multi-key config block with the single-line `{{ config(materialized='table') }}`; delete the ARM 1 `{% if is_incremental() %} WHERE e.modified > ... {% endif %}` block inserted after `LEFT JOIN ... int_specimen_obs_base ...`; delete the ARM 2 `{% if is_incremental() %} AND FALSE {% endif %}` block appended after `WHERE sob.longitude IS NOT NULL AND sob.latitude IS NOT NULL`. Either approach is acceptable — choose (a) commit-then-revert if Plan 01 committed (cleaner history), (b) direct edit if Plan 01 left changes in the working tree. The acceptance gate is byte-identical, so the simplest reliable path is: `git checkout $(cat .planning/phases/087-incremental-materialization-experiment/pre-experiment-sha.txt) -- data/dbt/models/intermediate/int_combined.sql` — this guarantees the working-tree file matches the pre-experiment SHA byte-for-byte. After reverting, run the belt-and-suspenders rebuild: `bash data/dbt/run.sh build --select int_combined+ --full-refresh`. Confirm exit 0. Then `bash data/dbt/run.sh test --select int_combined` — confirm exit 0. Assert the byte-identical revert by diffing the working-tree file against the pre-experiment SHA: `PRE_SHA=$(cat .planning/phases/087-incremental-materialization-experiment/pre-experiment-sha.txt); git diff $PRE_SHA -- data/dbt/models/intermediate/int_combined.sql` — output MUST be empty (any leftover `-- EXPERIMENTAL` comment or partial revert would surface here). Finally, edit 087-FINDINGS.md `## Rollback Status` and tick all four checkboxes: (a) reverted, (b) byte-identical diff against pre-experiment SHA is empty, (c) `--full-refresh` succeeded, (d) row count matches baseline-rowcount.txt — capture the actual post-revert COUNT(*) via `uv run --project data python -c "import duckdb; print(duckdb.connect('data/beeatlas.duckdb', read_only=True).execute('SELECT COUNT(*) FROM dbt_sandbox.int_combined').fetchone()[0])"` and write both numbers (baseline + current) into the (d) checkbox line. Do NOT modify run.sh, marts, or the run_results-*.json captures (they remain as historical evidence).
  </action>
  <verify>
    <automated>! grep -q "is_incremental()" data/dbt/models/intermediate/int_combined.sql && ! grep -q "materialized='incremental'" data/dbt/models/intermediate/int_combined.sql && ! grep -q "AND FALSE" data/dbt/models/intermediate/int_combined.sql && ! grep -q "EXPERIMENTAL" data/dbt/models/intermediate/int_combined.sql && grep -q "{{ config(materialized='table') }}" data/dbt/models/intermediate/int_combined.sql && PRE_SHA=$(cat .planning/phases/087-incremental-materialization-experiment/pre-experiment-sha.txt) && git cat-file -e "$PRE_SHA" && [ -z "$(git diff $PRE_SHA -- data/dbt/models/intermediate/int_combined.sql)" ] && bash data/dbt/run.sh build --select int_combined+ --full-refresh && bash data/dbt/run.sh test --select int_combined && grep -q "^- \[x\]" .planning/phases/087-incremental-materialization-experiment/087-FINDINGS.md && [ $(grep -c "^- \[x\]" .planning/phases/087-incremental-materialization-experiment/087-FINDINGS.md) -ge 4 ] && BASELINE=$(cat .planning/phases/087-incremental-materialization-experiment/baseline-rowcount.txt) && CURRENT=$(uv run --project data python -c "import duckdb; print(duckdb.connect('data/beeatlas.duckdb', read_only=True).execute('SELECT COUNT(*) FROM dbt_sandbox.int_combined').fetchone()[0])") && [ "$BASELINE" = "$CURRENT" ] && test -z "$(git diff data/dbt/run.sh)" && test -z "$(git diff data/dbt/models/marts/occurrences.sql)" && test -z "$(git diff data/dbt/models/marts/species.sql)"</automated>
  </verify>
  <acceptance_criteria>
    - data/dbt/models/intermediate/int_combined.sql does NOT contain `is_incremental()`
    - data/dbt/models/intermediate/int_combined.sql does NOT contain `materialized='incremental'`
    - data/dbt/models/intermediate/int_combined.sql does NOT contain `AND FALSE`
    - data/dbt/models/intermediate/int_combined.sql does NOT contain `EXPERIMENTAL` (the Phase 087 marker comment is gone)
    - data/dbt/models/intermediate/int_combined.sql DOES contain `{{ config(materialized='table') }}` (single-line, pre-experiment shape)
    - `git diff $(cat .planning/phases/087-incremental-materialization-experiment/pre-experiment-sha.txt) -- data/dbt/models/intermediate/int_combined.sql` produces empty output (byte-identical revert)
    - `bash data/dbt/run.sh build --select int_combined+ --full-refresh` exits 0 (belt-and-suspenders rebuild)
    - `bash data/dbt/run.sh test --select int_combined` exits 0
    - 087-FINDINGS.md `## Rollback Status` has ≥4 ticked checkboxes `- [x]` (reverted / byte-identical / --full-refresh ok / row count matches)
    - `git diff data/dbt/run.sh` is empty
    - `git diff data/dbt/models/marts/occurrences.sql` is empty
    - `git diff data/dbt/models/marts/species.sql` is empty
    - Post-revert int_combined row count exactly matches the integer in `baseline-rowcount.txt` — recorded in the ticked Rollback Status (d) checkbox
    - run_results-*.json captures from Plan 01 are still on disk (historical evidence preserved)
  </acceptance_criteria>
  <done>The dbt source tree is byte-identical to the pre-experiment SHA, the int_combined model rebuilds cleanly as a regular table, all dbt tests pass, and the findings doc's Rollback Status checklist is fully ticked with both row-count numbers recorded. The phase is complete: Phase 88 can read 087-FINDINGS.md `## Recommendation` and act on it.</done>
</task>

</tasks>

<verification>
After all tasks complete:

```bash
# Findings doc exists with recommendation + cache-state note
test -f .planning/phases/087-incremental-materialization-experiment/087-FINDINGS.md
grep -q "^## Recommendation" .planning/phases/087-incremental-materialization-experiment/087-FINDINGS.md
grep -Eq "(use incremental|full rebuilds)" .planning/phases/087-incremental-materialization-experiment/087-FINDINGS.md
grep -qi "fs-cache flushed" .planning/phases/087-incremental-materialization-experiment/087-FINDINGS.md

# Rollback complete + byte-identical
! grep -q "is_incremental()" data/dbt/models/intermediate/int_combined.sql
! grep -q "EXPERIMENTAL" data/dbt/models/intermediate/int_combined.sql
grep -q "{{ config(materialized='table') }}" data/dbt/models/intermediate/int_combined.sql
PRE_SHA=$(cat .planning/phases/087-incremental-materialization-experiment/pre-experiment-sha.txt)
[ -z "$(git diff $PRE_SHA -- data/dbt/models/intermediate/int_combined.sql)" ]

# dbt pipeline back to baseline behavior
bash data/dbt/run.sh build --select int_combined+ --full-refresh
bash data/dbt/run.sh test --select int_combined

# Historical evidence preserved
ls .planning/phases/087-incremental-materialization-experiment/run_results-*.json | wc -l   # expect 4

# Forbidden files still untouched
git diff --exit-code data/dbt/run.sh data/dbt/models/marts/occurrences.sql data/dbt/models/marts/species.sql
```
</verification>

<success_criteria>
- 087-FINDINGS.md exists with evidence-anchored Recommendation section that Phase 88 can act on, including the baseline cache-state note transcribed from timings.md
- int_combined.sql restored byte-identically to the pre-experiment SHA (`git diff <sha> -- int_combined.sql` is empty)
- Belt-and-suspenders `--full-refresh` rebuild succeeds; existing dbt tests pass
- Rollback Status checklist in 087-FINDINGS.md fully ticked (≥4 boxes, including the byte-identical assertion)
- run.sh, marts/occurrences.sql, marts/species.sql remain unmodified across the entire phase
- 4 run_results-*.json captures from Plan 01 remain on disk as historical evidence
</success_criteria>

<output>
After completion, create `.planning/phases/087-incremental-materialization-experiment/087-02-SUMMARY.md` with:
- Headline of the findings doc's recommendation (one line: "Phase 88: use incremental with --select X" OR "Phase 88: full rebuilds because Y")
- Rollback method used (commit-then-revert vs direct working-tree revert vs `git checkout <pre-sha> --`)
- Byte-identical revert assertion result (diff against pre-experiment SHA was empty)
- Post-revert verification result (rebuild + test exit codes, row-count match)
- Pointer to 087-FINDINGS.md for the full evidence trail
</output>
