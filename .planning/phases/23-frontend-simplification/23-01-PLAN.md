---
phase: 23-frontend-simplification
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/parquet.ts
  - frontend/src/bee-map.ts
autonomous: true
requirements:
  - FRONT-01

must_haves:
  truths:
    - "Clicking a specimen with an iNat link shows the correct iNaturalist URL in the sidebar"
    - "No network request for links.parquet is made on page load"
    - "The loadLinksMap function and _linksMap field no longer exist in the codebase"
  artifacts:
    - path: "frontend/src/parquet.ts"
      provides: "Ecdysis parquet loading with inat_observation_id column"
      contains: "inat_observation_id"
    - path: "frontend/src/bee-map.ts"
      provides: "BeeMap component without links.parquet dependency"
  key_links:
    - from: "frontend/src/parquet.ts"
      to: "ecdysis.parquet"
      via: "columns array includes inat_observation_id"
      pattern: "inat_observation_id"
    - from: "frontend/src/bee-map.ts"
      to: "frontend/src/parquet.ts"
      via: "feature.get('inat_observation_id') in buildSamples"
      pattern: "f\\.get\\('inat_observation_id'\\)"
---

<objective>
Remove the separate links.parquet loading pipeline from the frontend. The inat_observation_id column is now embedded in ecdysis.parquet (added by Phase 21's export.py), so the frontend reads it directly off already-loaded ecdysis features -- no second parquet file, no merge step, no _linksMap.

Purpose: Simplify frontend data loading by eliminating an unnecessary network request and merge step, now that the export pipeline embeds inat_observation_id directly in ecdysis.parquet.
Output: Two modified frontend files with links.parquet code paths deleted and inat_observation_id read from feature properties.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/23-frontend-simplification/23-CONTEXT.md
@frontend/src/parquet.ts
@frontend/src/bee-map.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update parquet.ts -- add inat_observation_id column, delete loadLinksMap</name>
  <files>frontend/src/parquet.ts</files>
  <read_first>frontend/src/parquet.ts</read_first>
  <action>
Make these exact changes to frontend/src/parquet.ts (per D-01, D-06, D-07, D-10):

1. DELETE lines 18-30: Remove the `linkColumns` constant and the entire `loadLinksMap` function. Also remove `loadLinksMap` from the module's exports (it is used as a named export by bee-map.ts).

2. ADD `'inat_observation_id'` to the `columns` array (currently lines 32-47). Add it after `'ecoregion_l3'` as the last entry:
   ```
   const columns = [
     'ecdysis_id',
     'occurrenceID',
     'longitude',
     'latitude',
     'year',
     'month',
     'scientificName',
     'recordedBy',
     'fieldNumber',
     'genus',
     'family',
     'floralHost',
     'county',
     'ecoregion_l3',
     'inat_observation_id',
   ];
   ```

3. ADD `inat_observation_id` property to the `feature.setProperties()` call inside the `ParquetSource` constructor (currently around line 60-72). Add after the `ecoregion_l3` line, with BigInt-to-Number coercion matching the existing v1.4 pattern:
   ```
   inat_observation_id: obj.inat_observation_id != null ? Number(obj.inat_observation_id) : null,
   ```
  </action>
  <verify>
    <automated>cd /Users/rainhead/dev/beeatlas && npx tsc --noEmit --project frontend/tsconfig.json 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - parquet.ts does NOT contain the string `loadLinksMap`
    - parquet.ts does NOT contain the string `linkColumns`
    - parquet.ts columns array contains `'inat_observation_id'`
    - parquet.ts setProperties block contains `inat_observation_id: obj.inat_observation_id != null ? Number(obj.inat_observation_id) : null`
    - TypeScript compilation passes with no errors
  </acceptance_criteria>
  <done>parquet.ts has inat_observation_id in columns and setProperties; loadLinksMap and linkColumns are deleted</done>
</task>

<task type="auto">
  <name>Task 2: Update bee-map.ts -- remove links.parquet loading, rewire buildSamples</name>
  <files>frontend/src/bee-map.ts</files>
  <read_first>frontend/src/bee-map.ts, frontend/src/parquet.ts</read_first>
  <action>
Make these exact changes to frontend/src/bee-map.ts (per D-02, D-03, D-04, D-05, D-08, D-09):

1. LINE ~6: Change the import from parquet.ts to remove `loadLinksMap`:
   BEFORE: `import { ParquetSource, loadLinksMap } from "./parquet.ts";`
   AFTER:  `import { ParquetSource } from "./parquet.ts";`

2. LINE ~9: DELETE the links.parquet import entirely:
   DELETE: `import linksDump from './assets/links.parquet?url';`

3. LINE ~131: Update `buildSamples` function signature -- remove the `linksMap` parameter and read inat_observation_id from feature properties:
   BEFORE: `function buildSamples(features: Feature[], linksMap?: Map<string, number>): Sample[] {`
   AFTER:  `function buildSamples(features: Feature[]): Sample[] {`

4. LINE ~145: Replace the linksMap lookup with a direct feature property read:
   BEFORE: `const inatId = linksMap ? (linksMap.get(f.get('occurrenceID') as string) ?? null) : null;`
   AFTER:  `const inatId = f.get('inat_observation_id') as number | null ?? null;`

5. LINE ~231: DELETE the `_linksMap` field declaration:
   DELETE: `private _linksMap: Map<string, number> = new Map();`

6. LINE ~531: Remove second argument from buildSamples call:
   BEFORE: `this.selectedSamples = buildSamples(toShow, this._linksMap);`
   AFTER:  `this.selectedSamples = buildSamples(toShow);`

7. LINES ~770-772: DELETE the entire loadLinksMap promise chain:
   DELETE: ```
   loadLinksMap(linksDump).catch(() => new Map<string, number>()).then(map => {
     this._linksMap = map;
   });
   ```

8. LINE ~788: Remove second argument from buildSamples call inside the then-block:
   BEFORE: `this.selectedSamples = buildSamples(toShow, this._linksMap);`
   AFTER:  `this.selectedSamples = buildSamples(toShow);`
  </action>
  <verify>
    <automated>cd /Users/rainhead/dev/beeatlas && npx tsc --noEmit --project frontend/tsconfig.json 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - bee-map.ts does NOT contain the string `linksDump`
    - bee-map.ts does NOT contain the string `_linksMap`
    - bee-map.ts does NOT contain the string `loadLinksMap`
    - bee-map.ts does NOT contain the string `links.parquet`
    - bee-map.ts contains `function buildSamples(features: Feature[]): Sample[]` (no linksMap param)
    - bee-map.ts contains `f.get('inat_observation_id')` in buildSamples
    - bee-map.ts buildSamples call sites have single argument: `buildSamples(toShow)` (no second arg)
    - TypeScript compilation passes with no errors
  </acceptance_criteria>
  <done>bee-map.ts has no links.parquet references; buildSamples reads inat_observation_id from feature properties; TypeScript compiles cleanly</done>
</task>

</tasks>

<verification>
1. TypeScript compilation: `cd frontend && npx tsc --noEmit` passes with zero errors
2. Vite build: `cd frontend && npx vite build` completes successfully
3. Grep verification: `grep -r 'links.parquet\|loadLinksMap\|_linksMap\|linkColumns\|linksDump' frontend/src/` returns NO results
</verification>

<success_criteria>
- TypeScript compiles cleanly with no errors
- Vite production build succeeds
- Zero references to links.parquet, loadLinksMap, _linksMap, linkColumns, or linksDump in frontend/src/
- inat_observation_id is read from ecdysis feature properties in buildSamples
- The iNat link sidebar behavior is preserved (same Sample type, same inatObservationId field populated)
</success_criteria>

<output>
After completion, create `.planning/phases/23-frontend-simplification/23-01-SUMMARY.md`
</output>
