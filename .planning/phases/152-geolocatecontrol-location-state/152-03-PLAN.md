---
phase: 152-geolocatecontrol-location-state
plan: 03
type: execute
wave: 3
depends_on: ["152-02"]
files_modified:
  - .planning/phases/152-geolocatecontrol-location-state/152-HUMAN-UAT.md
autonomous: false
requirements: [LOC-01, LOC-03]
user_setup:
  - service: real-iOS-device
    why: "iOS standalone-mode geolocation permission behavior differs from a Safari tab and is NOT reproducible in the Xcode Simulator (RESEARCH.md Pitfall 3 / iOS Standalone Geolocation Gotcha). LOC-01/LOC-03 real-device acceptance requires a physical iPhone/iPad with the /app PWA installed to the home screen."
must_haves:
  truths:
    - "On a real iOS device, launching the installed /app from the home screen (standalone), tapping the GeolocateControl with permission cleared, shows the OS permission dialog; granting it shows the blue dot; a subsequent relaunch auto-activates GPS with no prompt (D-03 path)"
    - "In a desktop/mobile browser, allowing location shows the blue dot + accuracy ring and the recenter button returns the viewport (LOC-01)"
    - "With DevTools 'offline' active, tapping the control still produces a GPS fix and renders the dot without network (LOC-01 offline GPS)"
    - "Denying location surfaces the app-level banner with a brief explanation, and the map/filters/table remain fully interactive (LOC-03)"
  artifacts:
    - path: ".planning/phases/152-geolocatecontrol-location-state/152-HUMAN-UAT.md"
      provides: "Real-device + manual-browser verification checklist with recorded pass/fail per scenario"
      contains: "iOS standalone"
  key_links:
    - from: "152-HUMAN-UAT.md"
      to: "VALIDATION.md Manual-Only Verifications"
      via: "each manual row becomes a checklist scenario"
      pattern: "iOS standalone"
---

<objective>
Real-device + manual-browser UAT for Phase 152. The automatable LOC-02 invariants are proven by Plans 01/02; this plan covers the four behaviors that CANNOT be exercised headless: blue dot/recenter, offline GPS, denial banner, and — critically — iOS standalone-mode geolocation permission behavior, which differs from a Safari tab and is not reproducible in the Simulator.

Purpose: This is a `UI hint: yes` phase. Per project convention (`feedback_uat_ui_phases`), yolo mode must NOT auto-advance past UAT on UI phases — this plan is `autonomous: false` and ends in a blocking human-verify checkpoint. It records the verdict in `152-HUMAN-UAT.md` so the phase gate has a durable artifact.

Output: `152-HUMAN-UAT.md` checklist with recorded pass/fail per scenario; a blocking checkpoint that pauses for the human verdict.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/152-geolocatecontrol-location-state/152-CONTEXT.md
@.planning/phases/152-geolocatecontrol-location-state/152-RESEARCH.md
@.planning/phases/152-geolocatecontrol-location-state/152-VALIDATION.md
@.planning/phases/152-geolocatecontrol-location-state/152-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write 152-HUMAN-UAT.md — real-device + manual-browser verification checklist</name>
  <files>.planning/phases/152-geolocatecontrol-location-state/152-HUMAN-UAT.md</files>
  <read_first>
    - .planning/phases/152-geolocatecontrol-location-state/152-VALIDATION.md "Manual-Only Verifications" table — the four rows (blue dot/recenter, offline GPS, denial banner, iOS standalone) are the binding scenario list; transcribe each into a checklist scenario with numbered steps + expected result + pass/fail field.
    - .planning/phases/152-geolocatecontrol-location-state/152-RESEARCH.md "iOS Standalone Geolocation Gotcha (Research Flag)" — the exact 4-step iOS device procedure (install on real device, clear Settings → Safari → Location, launch standalone from home screen, tap control → dialog appears → grant shows dot → relaunch auto-activates).
    - .planning/phases/151-pwa-manifest-installability/151-HUMAN-UAT.md (the most recent prior UAT artifact in the repo, committed in 9634c733) — match its structure/format for consistency. (If the exact path differs, find it with `ls .planning/phases/151-*/*HUMAN-UAT.md`.)
  </read_first>
  <action>
    Create `.planning/phases/152-geolocatecontrol-location-state/152-HUMAN-UAT.md` as a real-device + manual-browser verification checklist. Match the format of the Phase 151 HUMAN-UAT.md (same project, just shipped). Include a short preamble noting the build/URL under test (the `/app` route) and that LOC-02 source invariants are already automated (link `src/tests/geolocation.test.ts`). Then four scenarios, each with: a title, numbered reproduction steps, the expected result, and a `Result:` field (PASS / FAIL / notes) left blank for the human:
    Scenario 1 — Blue dot + recenter (LOC-01): in a browser, open `/app`, tap the GeolocateControl, ALLOW location → expect a blue dot + accuracy ring; pan the map away from the dot, tap the control again (recenter) → expect the viewport returns to center on the dot.
    Scenario 2 — Offline GPS (LOC-01): with the page loaded, enable DevTools "offline" (Network throttling → Offline), tap the control → expect a GPS fix arrives and the dot renders with NO network (GPS is independent of the basemap style; this is the offline-field-mode core value).
    Scenario 3 — Denial banner (LOC-03): reset/deny location for the site, tap the control (or trigger an error) → expect the app-level banner with a brief explanation (e.g. "Location access blocked — enable in Settings…"); confirm the map, filter panel, and table remain fully interactive (open a filter, switch to table view) — the rest of the app is unaffected; confirm the banner dismisses with ✕.
    Scenario 4 — iOS standalone permission (LOC-01/LOC-03, REAL DEVICE ONLY): on a physical iPhone/iPad — (a) install `/app` to the home screen; (b) clear the site's location permission in Settings → Safari → Location; (c) launch the app from the home screen (standalone, not in a Safari tab); (d) tap the GeolocateControl and confirm the permission dialog appears; (e) grant it and confirm the blue dot shows; (f) relaunch the app from the home screen and confirm GPS auto-activates with NO prompt (D-03 already-granted path). Note prominently: this is NOT reproducible in the Xcode Simulator — a real device is required (RESEARCH Pitfall 3).
    End the file with a "Verdict" line (PASS requires all four scenarios PASS; any FAIL → record the failure and route to gap closure).
  </action>
  <verify>
    <automated>test -f .planning/phases/152-geolocatecontrol-location-state/152-HUMAN-UAT.md && grep -q "iOS standalone" .planning/phases/152-geolocatecontrol-location-state/152-HUMAN-UAT.md && grep -qi "offline" .planning/phases/152-geolocatecontrol-location-state/152-HUMAN-UAT.md && grep -qi "recenter" .planning/phases/152-geolocatecontrol-location-state/152-HUMAN-UAT.md && grep -qi "banner" .planning/phases/152-geolocatecontrol-location-state/152-HUMAN-UAT.md</automated>
  </verify>
  <acceptance_criteria>
    - `152-HUMAN-UAT.md` exists and contains all four scenarios (blue dot/recenter, offline GPS, denial banner, iOS standalone), each with numbered steps + expected result + a blank `Result:` field.
    - The iOS standalone scenario includes the full RESEARCH procedure: install to home screen, clear Settings → Safari → Location, launch standalone, tap → dialog, grant → dot, relaunch → auto-activate; and the "Simulator not valid — real device required" note.
    - The greps above all pass (file exists; mentions iOS standalone, offline, recenter, banner).
    - The file references the automated proof (`src/tests/geolocation.test.ts`) so the reader knows LOC-02 is already covered.
  </acceptance_criteria>
  <done>152-HUMAN-UAT.md exists with all four manual scenarios (incl. the real-device iOS standalone procedure) and a verdict section ready for the human to fill in.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Blocking human-verify — run the 152-HUMAN-UAT.md scenarios and record the verdict</name>
  <files>.planning/phases/152-geolocatecontrol-location-state/152-HUMAN-UAT.md</files>
  <action>
    HUMAN CHECKPOINT (not an autonomous task). Do not auto-advance. Hand control to the human to execute the four scenarios in 152-HUMAN-UAT.md and record the verdict, then wait for the resume signal. See <what-built>/<how-to-verify>/<resume-signal> below for the operative content.
  </action>
  <what-built>
    Phase 152 delivered: a Mapbox GeolocateControl on the `/app` map (blue dot + accuracy ring + recenter, offline-capable GPS, granted-only auto-trigger); location state lifted to `<bee-atlas>._userLocation` via a composed `user-location-changed` event (pure-presenter invariant, proven by automated source-analysis tests); and an app-level denial/unavailable banner. Plan 01 + Plan 02 are fully automated-green (`npm test` passes, build succeeds). The blue dot, recenter, offline GPS, denial banner, and iOS standalone permission behavior cannot be verified headless and need real eyes + real geolocation + a real iOS device.
  </what-built>
  <how-to-verify>
    1. Build/serve the `/app` route (the offline PWA): `npm run dev` for local browser checks, or use the deployed `/app` URL for the installed iOS test.
    2. Open `.planning/phases/152-geolocatecontrol-location-state/152-HUMAN-UAT.md` and run all four scenarios in order, recording PASS/FAIL + notes in each `Result:` field:
       - Scenario 1 (browser): ALLOW location → blue dot + accuracy ring appear; pan away, tap control → viewport recenters on the dot.
       - Scenario 2 (browser): DevTools → Network → Offline; tap control → a GPS fix arrives and the dot renders with no network.
       - Scenario 3 (browser): deny location → app-level banner shows a brief explanation; confirm map + filter panel + table stay interactive; dismiss the banner with ✕.
       - Scenario 4 (REAL iOS DEVICE, not Simulator): install `/app` to the home screen; clear Settings → Safari → Location; launch standalone from the home screen; tap control → permission dialog appears; grant → blue dot shows; relaunch → GPS auto-activates with no prompt.
    3. Fill in the Verdict line in 152-HUMAN-UAT.md (PASS = all four scenarios PASS).
  </how-to-verify>
  <verify>
    <automated>grep -qiE "verdict.*(pass|fail)" .planning/phases/152-geolocatecontrol-location-state/152-HUMAN-UAT.md</automated>
  </verify>
  <resume-signal>Type "approved" if all four scenarios PASS (and 152-HUMAN-UAT.md verdict is recorded), or describe which scenario failed and how (so it can be routed to `/gsd-plan-phase 152 --gaps`).</resume-signal>
  <done>All four scenarios executed; verdict (PASS/FAIL) recorded in 152-HUMAN-UAT.md; human resume signal received.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Tester device → app | Manual verification only; the human-verify checkpoint confirms the runtime permission/PII behavior modeled in Plan 02 (no new code surface here). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-152-07 | Information Disclosure | real-device UAT exposes the tester's actual location | accept | Inherent to verifying geolocation; the tester is the data subject and consents by running the test. No location is persisted or transmitted (verified in Plan 02: client-side `@state` only). |
| T-152-SC | Tampering | npm/pip/cargo installs | accept | ZERO package installs in this plan (documentation + manual verification only). |
</threat_model>

<verification>
- `152-HUMAN-UAT.md` exists with all four scenarios and a verdict section (automated greps in Task 1).
- The blocking checkpoint pauses for the human verdict; PASS requires all four manual scenarios PASS with the iOS test on a real device.
- A FAIL routes to `/gsd-plan-phase 152 --gaps` rather than auto-advancing (UI-hint phase convention).
</verification>

<success_criteria>
- All four manual/real-device scenarios (LOC-01 blue dot/recenter, LOC-01 offline GPS, LOC-03 denial banner, LOC-01/LOC-03 iOS standalone) are documented and executed, with the verdict recorded in 152-HUMAN-UAT.md.
- The phase does not auto-advance past UAT (autonomous: false; blocking human-verify checkpoint).
</success_criteria>

<output>
Create `.planning/phases/152-geolocatecontrol-location-state/152-03-SUMMARY.md` when done (after the human verdict is recorded).
</output>
