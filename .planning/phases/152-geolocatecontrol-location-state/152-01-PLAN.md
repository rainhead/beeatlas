---
phase: 152-geolocatecontrol-location-state
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/tests/geolocation.test.ts
  - src/tests/bee-atlas.test.ts
  - src/tests/cache-state.test.ts
autonomous: true
requirements: [LOC-02]
must_haves:
  truths:
    - "src/tests/geolocation.test.ts exists and runs as a source-analysis suite (no DOM mount)"
    - "The geolocation suite asserts the four LOC-02 pure-presenter invariants (RED until Plan 02 implements them)"
    - "The mapbox-gl vi.mock in bee-atlas.test.ts and cache-state.test.ts exposes addControl + a GeolocateControl stub, so a mounted <bee-map> no longer throws in firstUpdated()"
  artifacts:
    - path: "src/tests/geolocation.test.ts"
      provides: "Source-analysis gate for the LOC-02 state-owner/pure-presenter invariant"
      min_lines: 20
      contains: "user-location-changed"
    - path: "src/tests/bee-atlas.test.ts"
      provides: "Updated mapbox-gl mock with addControl + GeolocateControl"
      contains: "GeolocateControl"
    - path: "src/tests/cache-state.test.ts"
      provides: "Updated mapbox-gl mock with addControl + GeolocateControl"
      contains: "GeolocateControl"
  key_links:
    - from: "src/tests/geolocation.test.ts"
      to: "src/bee-map.ts"
      via: "readFileSync at module scope"
      pattern: "readFileSync.*bee-map\\.ts"
    - from: "src/tests/geolocation.test.ts"
      to: "src/bee-atlas.ts"
      via: "readFileSync at module scope"
      pattern: "readFileSync.*bee-atlas\\.ts"
---

<objective>
Wave 0 test scaffold for Phase 152. Create the source-analysis suite that gates the LOC-02 state-owner/pure-presenter invariant BEFORE any implementation lands, and repair the two existing `mapbox-gl` `vi.mock()` factories so a mounted `<bee-map>` survives the new `firstUpdated()` `addControl(new GeolocateControl(...))` call.

Purpose: The source-analysis tests are the authoritative proof for LOC-02 Success Criterion 3 (`<bee-map>` emits, never stores; `<bee-atlas>` owns the state). They must exist and be RED before Plan 02 implements them, so the implementation is gated rather than retro-fitted. The mock repair prevents the new control construction from throwing in any test that mounts a real `<bee-map>`.

Output: `src/tests/geolocation.test.ts` (new), plus mock extensions in `src/tests/bee-atlas.test.ts` and `src/tests/cache-state.test.ts`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/152-geolocatecontrol-location-state/152-CONTEXT.md
@.planning/phases/152-geolocatecontrol-location-state/152-RESEARCH.md
@.planning/phases/152-geolocatecontrol-location-state/152-PATTERNS.md
@.planning/phases/152-geolocatecontrol-location-state/152-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create src/tests/geolocation.test.ts source-analysis suite (LOC-02 invariants)</name>
  <files>src/tests/geolocation.test.ts</files>
  <read_first>
    - src/tests/bee-map.test.ts — the template: copy the file header (readFileSync + resolve + fileURLToPath + __dirname at module scope, ~lines 1-7), the positive `@property` guard idiom (~lines 23-25), and the negative `@state` guard idiom (~lines 133-136). This is an exact-analog file; mirror its structure.
    - src/bee-map.ts — confirm the file path resolved relative to `../bee-map.ts`; this is the source the suite reads.
    - src/bee-atlas.ts — confirm the file path resolved relative to `../bee-atlas.ts`; this is the second source the suite reads.
    - .planning/phases/152-geolocatecontrol-location-state/152-PATTERNS.md "src/tests/geolocation.test.ts (CREATE)" section — the five exact assertions to write.
  </read_first>
  <action>
    Create src/tests/geolocation.test.ts as a pure source-analysis suite — NO DOM mount, NO import of the components under test, mirroring src/tests/bee-map.test.ts. At module scope, read two sources with readFileSync: `beeMapSrc` from `../bee-map.ts` and `beeAtlasSrc` from `../bee-atlas.ts` (use the resolve/dirname/fileURLToPath idiom from bee-map.test.ts). Write exactly five `test(...)` assertions, all of which will be RED until Plan 02 lands (that is intentional — this suite is the gate):
    (1) `beeMapSrc` does NOT match `/@state[\s\S]{0,20}_userLocation/` — guards the pure-presenter invariant (`<bee-map>` must not store location state).
    (2) `beeMapSrc` does NOT match `/private\s+_userLocation/` — second guard against storing location on the presenter.
    (3) `beeMapSrc` matches `/user-location-changed/` — the control emits the exact event name `user-location-changed`.
    (4) `beeAtlasSrc` matches `/@state[\s\S]{0,20}_userLocation/` — the `_userLocation` reactive state lives on the coordinator `<bee-atlas>`.
    (5) `beeAtlasSrc` matches `/@user-location-changed/` — `<bee-atlas>` binds the `@user-location-changed` listener on `<bee-map>` in `render()`.
    The exact event name is `user-location-changed` and the exact state field is `_userLocation` — do not vary these strings. Give each test a descriptive name that cites the invariant and LOC-02 (mirroring the bee-map.test.ts naming convention that names the requirement, e.g. OFF-04).
  </action>
  <verify>
    <automated>npx tsc --noEmit && test -f src/tests/geolocation.test.ts && grep -q 'user-location-changed' src/tests/geolocation.test.ts && grep -q "readFileSync.*bee-atlas" src/tests/geolocation.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - src/tests/geolocation.test.ts exists and is a source-analysis suite (contains `readFileSync`, contains NO `appendChild` / `document.createElement` / component mount).
    - File reads BOTH `../bee-map.ts` and `../bee-atlas.ts` at module scope: `grep -c "readFileSync" src/tests/geolocation.test.ts` returns >= 2.
    - File contains exactly the five assertions described in the action, including the literal strings `user-location-changed`, `_userLocation`, and the two `.not.toMatch` negative guards: `grep -c "not.toMatch" src/tests/geolocation.test.ts` returns >= 2.
    - `npx tsc --noEmit` exits 0 (the test file type-checks).
    - Running `npm test -- src/tests/geolocation.test.ts` executes the suite and reports failing assertions (RED is expected here — the implementation does not exist yet; this proves the gate is live, not vacuous).
  </acceptance_criteria>
  <done>geolocation.test.ts exists, type-checks, runs as a source-analysis suite, and its five LOC-02 assertions are RED (will go GREEN when Plan 02 lands).</done>
</task>

<task type="auto">
  <name>Task 2: Extend the mapbox-gl vi.mock in bee-atlas.test.ts and cache-state.test.ts (addControl + GeolocateControl stub)</name>
  <files>src/tests/bee-atlas.test.ts, src/tests/cache-state.test.ts</files>
  <read_first>
    - src/tests/bee-atlas.test.ts — the existing `vi.mock('mapbox-gl', ...)` factory (~lines 30-62): the `MapMock` implementation object and the `default: { accessToken, Map }` return shape.
    - src/tests/cache-state.test.ts — the IDENTICAL `vi.mock('mapbox-gl', ...)` factory (~lines 25-56). The two blocks must end up identical.
    - .planning/phases/152-geolocatecontrol-location-state/152-PATTERNS.md "src/tests/bee-atlas.test.ts + src/tests/cache-state.test.ts" section — the exact stub to add.
    - .planning/phases/152-geolocatecontrol-location-state/152-RESEARCH.md "Pitfall 6" — why the mock gap throws (new firstUpdated calls `this._map.addControl(new GeolocateControl(...))`).
  </read_first>
  <action>
    In BOTH src/tests/bee-atlas.test.ts and src/tests/cache-state.test.ts, make two identical edits to the `vi.mock('mapbox-gl', ...)` factory:
    (a) Add `addControl: vi.fn()` to the `MapMock` implementation object (the object returned by `vi.fn().mockImplementation(() => ({ ... }))`) — `bee-map.ts` firstUpdated() calls `this._map.addControl(geolocate)`, which is absent from the current mock.
    (b) Add a `GeolocateControl` export to the `default: { ... }` return object, alongside `Map`: `GeolocateControl: vi.fn().mockImplementation(() => ({ on: vi.fn(), trigger: vi.fn(() => true) }))`. The `on` stub lets the `geolocate`/`error` event wiring run without throwing; `trigger` returns `true` to mimic a successful toggle.
    The two blocks must remain identical to each other after the edit (they are copies). Do NOT change any other part of these test files.
  </action>
  <verify>
    <automated>grep -c "GeolocateControl" src/tests/bee-atlas.test.ts | grep -qv '^0$' && grep -c "GeolocateControl" src/tests/cache-state.test.ts | grep -qv '^0$' && grep -q "addControl" src/tests/bee-atlas.test.ts && grep -q "addControl" src/tests/cache-state.test.ts && npm test -- src/tests/bee-atlas.test.ts src/tests/cache-state.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "addControl: vi.fn()" src/tests/bee-atlas.test.ts` and same for cache-state.test.ts — the map mock exposes addControl.
    - `grep -q "GeolocateControl: vi.fn" src/tests/bee-atlas.test.ts` and same for cache-state.test.ts — the default export exposes a GeolocateControl stub with `on` and `trigger`.
    - The GeolocateControl stub returns an object containing both `on:` and `trigger:` in both files.
    - `npm test -- src/tests/bee-atlas.test.ts src/tests/cache-state.test.ts` exits 0 — both suites still pass with the extended mock (no regression; the mock change is additive).
    - The two `vi.mock('mapbox-gl', ...)` blocks are byte-identical between the two files (a `diff` of the extracted block shows no differences).
  </acceptance_criteria>
  <done>Both test files' mapbox-gl mocks expose `addControl` on the map instance and a `GeolocateControl` constructor stub; both suites pass; the new control construction in Plan 02 will not throw against these mocks.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new in this plan) | Test-only changes; no runtime trust boundary crossed. The geolocation PII surface is introduced in Plan 02 and modeled there. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-152-01 | Tampering | test mocks (`GeolocateControl` stub) | accept | Test doubles only; never shipped to production bundle. No high-severity surface. |
| T-152-SC | Tampering | npm/pip/cargo installs | accept | This plan installs ZERO packages (RESEARCH.md "No New Package Installs"). GeolocateControl ships in the already-installed `mapbox-gl@3.24.1`. No legitimacy gate needed. |
</threat_model>

<verification>
- `npm test -- src/tests/geolocation.test.ts` runs the new suite (RED expected — gate is live).
- `npm test -- src/tests/bee-atlas.test.ts src/tests/cache-state.test.ts` exits 0 (mock extension is non-regressing).
- `npx tsc --noEmit` exits 0.
- Full suite `npm test` may show the five new geolocation assertions RED — that is the intended Wave 0 state; Plan 02 turns them GREEN.
</verification>

<success_criteria>
- geolocation.test.ts exists, type-checks, and encodes the five LOC-02 source invariants (RED, gating Plan 02).
- Both mapbox-gl mocks expose `addControl` + a `GeolocateControl` stub; their `vi.mock` blocks remain identical.
- No production source files were modified in this plan (test-only wave).
</success_criteria>

<output>
Create `.planning/phases/152-geolocatecontrol-location-state/152-01-SUMMARY.md` when done.
</output>
