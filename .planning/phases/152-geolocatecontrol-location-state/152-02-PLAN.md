---
phase: 152-geolocatecontrol-location-state
plan: 02
type: execute
wave: 2
depends_on: ["152-01"]
files_modified:
  - src/bee-map.ts
  - src/bee-atlas.ts
autonomous: true
requirements: [LOC-01, LOC-02, LOC-03]
must_haves:
  truths:
    - "A GeolocateControl (trackUserLocation + enableHighAccuracy + showAccuracyCircle) is added to the map immediately after new mapboxgl.Map(), not inside the 'load' callback — so the blue dot + accuracy ring + recenter work offline via GPS (LOC-01)"
    - "On map init, if navigator.permissions reports geolocation 'granted', the control auto-triggers inside a resolved permissions.query().then() callback (never synchronously after addControl); other states do nothing (D-03)"
    - "<bee-map> emits user-location-changed via the existing composed _emit() helper and stores NO location @state — it is a pure presenter (LOC-02)"
    - "<bee-atlas> owns @state _userLocation ({lat,lon,accuracy}|null) and @state _locationError; binds @user-location-changed on <bee-map> in render(); a denial (error code 1) sets _locationError and clears _userLocation, an unavailable/timeout (code 2/3) sets _locationError, a fix sets _userLocation (LOC-02, LOC-03)"
    - "A denied/unavailable location shows an app-level banner on <bee-atlas> (modeled on the _updateAvailable banner) and the rest of the app — map, filters, table — is unaffected (LOC-03)"
  artifacts:
    - path: "src/bee-map.ts"
      provides: "GeolocateControl construction + geolocate/error event wiring + D-03 granted-only auto-trigger; emits user-location-changed"
      contains: "GeolocateControl"
    - path: "src/bee-atlas.ts"
      provides: "_userLocation + _locationError @state, _onUserLocationChanged handler, @user-location-changed binding, location-error banner"
      contains: "_userLocation"
  key_links:
    - from: "src/bee-map.ts"
      to: "_emit('user-location-changed', ...)"
      via: "geolocate + error event handlers on the GeolocateControl"
      pattern: "_emit\\(\\s*'user-location-changed'"
    - from: "src/bee-atlas.ts (<bee-map> in render)"
      to: "_onUserLocationChanged"
      via: "@user-location-changed binding"
      pattern: "@user-location-changed=\\$\\{this\\._onUserLocationChanged\\}"
    - from: "src/bee-atlas.ts _onUserLocationChanged"
      to: "_userLocation / _locationError @state"
      via: "success sets _userLocation, error sets _locationError"
      pattern: "this\\._userLocation\\s*="
---

<objective>
Implement Phase 152: add the Mapbox `GeolocateControl` to `<bee-map>` (blue dot + accuracy ring + recenter, offline-capable GPS, granted-only auto-trigger), and lift location state to `<bee-atlas>` as `@state _userLocation` with an app-level denial banner — preserving the state-owner/pure-presenter invariant. This turns the five RED source-analysis assertions from Plan 01 GREEN.

Purpose: Deliver LOC-01 (control + config + offline GPS), LOC-02 (state ownership + composed-event relay), and LOC-03 (graceful denial). `_userLocation` is the foundation Phase 153 ("near me") consumes; its shape `{ lat, lon, accuracy }` anticipates the haversine query.

Output: Wired `GeolocateControl` in `src/bee-map.ts`; `_userLocation`/`_locationError` state, handler, binding, and banner in `src/bee-atlas.ts`. All automated tests GREEN.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/152-geolocatecontrol-location-state/152-CONTEXT.md
@.planning/phases/152-geolocatecontrol-location-state/152-RESEARCH.md
@.planning/phases/152-geolocatecontrol-location-state/152-PATTERNS.md
@.planning/phases/152-geolocatecontrol-location-state/152-VALIDATION.md
@.planning/phases/152-geolocatecontrol-location-state/152-01-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add GeolocateControl to bee-map.ts — construction, geolocate/error wiring, D-03 granted-only auto-trigger (LOC-01, LOC-02 emit side)</name>
  <files>src/bee-map.ts</files>
  <behavior>
    - The Plan 01 source-analysis assertions for `<bee-map>` go GREEN: `bee-map.ts` matches `/user-location-changed/`; `bee-map.ts` does NOT match `/@state[\s\S]{0,20}_userLocation/` and does NOT match `/private\s+_userLocation/`.
    - The geolocate-event handler reads `e.coords.latitude`, `e.coords.longitude`, `e.coords.accuracy` (NOT a raw GeolocationPosition) and emits `{ lat, lon, accuracy }`.
    - The error-event handler emits `{ error: { code, message } }`.
    - The auto-trigger only calls `geolocate.trigger()` when `navigator.permissions.query` resolves to `state === 'granted'`, and is guarded by `if (navigator.permissions)`.
  </behavior>
  <read_first>
    - src/bee-map.ts firstUpdated() (~lines 378-392) — the map construction block (`new mapboxgl.Map({...})` ending ~line 389, then `this._map.boxZoom.disable()` at line 392). The new control goes immediately after the Map constructor and before/around the existing post-construct setup, NOT inside the `this._map.on('load', ...)` callback at line 407.
    - src/bee-map.ts _emit() helper (lines 164-168) — the exact composed-event dispatcher to reuse; do NOT write a new dispatch.
    - src/bee-map.ts existing _emit call sites (e.g. line ~524 `this._emit('view-moved', {...})`, line ~310 typed-generic `_emit<...>('boundary-mode-changed', mode)`) — copy this call shape.
    - .planning/phases/152-geolocatecontrol-location-state/152-PATTERNS.md "src/bee-map.ts — add GeolocateControl + wire events" — the exact construction + wiring + auto-trigger excerpt.
    - .planning/phases/152-geolocatecontrol-location-state/152-RESEARCH.md "Pattern 1/2", "Offline GPS and Dot Rendering", "Common Pitfalls 1-2" — the landmines: trigger-timing, style-independence.
  </read_first>
  <action>
    In src/bee-map.ts firstUpdated(), immediately AFTER the `new mapboxgl.Map({...})` block (the assignment ending ~line 389) and NOT inside the `this._map.on('load', ...)` callback (line 407): construct and add the GeolocateControl.
    Construct with exactly these options: `{ trackUserLocation: true, positionOptions: { enableHighAccuracy: true }, showAccuracyCircle: true }` (D-01). Leave `followUserLocation`, `showUserLocation`, `showButton` at their `true` defaults (recenter button + dot) and `showUserHeading` at its `false` default (heading is out of scope). Add it via `this._map.addControl(geolocate)` with no position argument — top-right is the Mapbox default (D-02). Store the instance as a plain instance field `this._geolocateControl` (typed `mapboxgl.GeolocateControl | null`, initialized null at the class field declarations) — this is an instance field, NOT an `@state` (the pure-presenter invariant forbids reactive location state on `<bee-map>`).
    Why immediately after the constructor and not inside 'load': the blue dot + accuracy ring are DOM Markers appended to `map.getCanvasContainer()` with no style dependency, and offline the style never loads so `'load'` never fires (Phase 151 fix). Gating the control behind `'load'` would break offline GPS (LOC-01 SC-2).
    Wire two control events using the existing `_emit()` helper (reuse, do not re-implement dispatch):
    - geolocate: `geolocate.on('geolocate', (e) => this._emit('user-location-changed', { lat: e.coords.latitude, lon: e.coords.longitude, accuracy: e.coords.accuracy }))`. The payload is the control's serialized event exposing `coords` (a GeolocationCoordinates) + `timestamp` — read `e.coords.*`, NOT a raw GeolocationPosition. Type the handler param as `{ coords: GeolocationCoordinates; timestamp: number }`.
    - error: `geolocate.on('error', (e) => this._emit('user-location-changed', { error: { code: e.code, message: e.message } }))`. Type the handler param as `{ code: number; message: string }`. The control's `error` event is the reliable denial/unavailable signal (D-04): code 1 = PERMISSION_DENIED, 2 = POSITION_UNAVAILABLE, 3 = TIMEOUT.
    The exact emitted event name is `user-location-changed` (matches Plan 01's gate).
    D-03 granted-only auto-trigger: AFTER the event wiring, add `if (navigator.permissions) { navigator.permissions.query({ name: 'geolocation' as PermissionName }).then(status => { if (status.state === 'granted') geolocate.trigger(); }).catch(() => {}); }`. The `.trigger()` MUST live inside the resolved `.then()` callback — calling it synchronously after `addControl()` finds the control's internal `_setup` flag still false (it is set only after the control's own `navigator.permissions.query()` microtask resolves in `_setupUI`) and silently no-ops. Guard the whole block with `if (navigator.permissions)` for browsers (old iOS < 16) lacking the API; the empty `.catch()` swallows an unsupported-name rejection. Do NOT listen to `PermissionStatus.change` for revocation — it is unreliable on Safari/iOS (the control's `error` event is the revocation signal instead).
    Do NOT add any `@state` for location data to `<bee-map>` and do NOT store the lat/lon anywhere on the presenter — emit only.
  </action>
  <verify>
    <automated>npx tsc --noEmit && grep -q "GeolocateControl" src/bee-map.ts && grep -q "trackUserLocation: true" src/bee-map.ts && grep -q "enableHighAccuracy: true" src/bee-map.ts && grep -q "showAccuracyCircle: true" src/bee-map.ts && grep -Eq "_emit\(\s*'user-location-changed'" src/bee-map.ts && grep -q "navigator.permissions" src/bee-map.ts</automated>
  </verify>
  <acceptance_criteria>
    - `bee-map.ts` constructs `new mapboxgl.GeolocateControl({...})` with literal options `trackUserLocation: true`, `positionOptions: { enableHighAccuracy: true }`, `showAccuracyCircle: true` (all three greppable).
    - The control is added via `this._map.addControl(...)` and the call appears OUTSIDE the `this._map.on('load', ...)` callback body — confirm by reading: the `addControl` line is between the `new mapboxgl.Map({...})` assignment and the `this._map.on('load',` registration.
    - `grep -Eq "_emit\(\s*'user-location-changed'" src/bee-map.ts` — the control emits the exact event name via the existing `_emit()` helper (NOT a hand-rolled `dispatchEvent`).
    - The geolocate handler reads `e.coords.latitude` / `e.coords.longitude` / `e.coords.accuracy`: `grep -q "e.coords.latitude" src/bee-map.ts` (and lon/accuracy).
    - The error handler emits an `{ error: { code, message } }` shape: `grep -q "error:" src/bee-map.ts` within the error wiring.
    - D-03 auto-trigger is guarded and conditional: `grep -q "navigator.permissions" src/bee-map.ts`, `grep -q "=== 'granted'" src/bee-map.ts`, and `.trigger()` appears inside a `.then(` callback (read to confirm it is NOT a top-level synchronous call after addControl).
    - Pure-presenter invariant holds: `bee-map.ts` does NOT match `/@state[\s\S]{0,20}_userLocation/` and does NOT match `/private\s+_userLocation/` (the stored instance field is `this._geolocateControl`, never `_userLocation`).
    - `npx tsc --noEmit` exits 0.
    - The bee-map portion of `npm test -- src/tests/geolocation.test.ts` (assertions 1-3) is GREEN.
  </acceptance_criteria>
  <done>GeolocateControl is constructed with the D-01 options, added top-right right after the Map constructor (offline-safe), wired to emit `user-location-changed` on geolocate/error via `_emit()`, and auto-triggers only on already-granted permission inside a resolved permissions.query().then(). `<bee-map>` stores no location @state.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Lift location state to bee-atlas.ts — _userLocation/_locationError @state, handler, @user-location-changed binding, denial banner (LOC-02 owner side, LOC-03)</name>
  <files>src/bee-atlas.ts</files>
  <behavior>
    - The remaining Plan 01 assertions go GREEN: `bee-atlas.ts` matches `/@state[\s\S]{0,20}_userLocation/` and `/@user-location-changed/`.
    - On a `user-location-changed` event carrying `{ lat, lon, accuracy }`, `_userLocation` is set and `_locationError` is cleared.
    - On a `user-location-changed` event carrying `{ error: { code, message } }`, `_locationError` is set true and `_userLocation` is cleared to null (clears stale position on revocation).
    - The denial banner renders when `_locationError` is true and is dismissible; the map/pane content render path is unchanged.
  </behavior>
  <read_first>
    - src/bee-atlas.ts @state declarations (lines 109-119) — the `_updateAvailable` / `_offline` / `_installable` `@state` block; new fields follow the identical `@state() private _field` shape.
    - src/bee-atlas.ts <bee-map> binding block (lines 301-322) — the existing `@view-moved` … `@selection-drawn` listener bindings (lines 313-321); the new `@user-location-changed` binding inserts after `@selection-drawn` (line 321).
    - src/bee-atlas.ts _updateAvailable banner (lines 361-374) — the exact conditional-banner template to copy, and `_onBannerDismiss` for the dismiss-handler shape.
    - .planning/phases/152-geolocatecontrol-location-state/152-PATTERNS.md "src/bee-atlas.ts" section — the @state shapes, binding insertion point, handler, and banner excerpt.
    - .planning/phases/152-geolocatecontrol-location-state/152-RESEARCH.md "_userLocation Shape Recommendation", "Pattern 3/4", "Open Questions 1" (distinct copy for code 1 vs 2/3).
  </read_first>
  <action>
    In src/bee-atlas.ts, make four coordinated edits (this completes the LOC-02 state-owner side and LOC-03):
    (1) Add two `@state` fields alongside the existing banner/offline state (after line ~119): `_userLocation` typed `{ lat: number; lon: number; accuracy: number } | null = null;` and `_locationError` typed `boolean = false;`, both declared with `@state() private`. Use this exact `{ lat, lon, accuracy }` shape (D-05 / Claude's discretion per RESEARCH.md) — minimal, anticipates Phase 153 haversine; do NOT include timestamp or altitude. The error is a separate boolean (cleaner than encoding it in `_userLocation`, mirrors `_updateAvailable`). Optionally also add `_locationErrorKind` typed `'denied' | 'unavailable' | null = null` (also `@state() private`) if you implement distinct copy (recommended — see below).
    (2) Add the `@user-location-changed=${this._onUserLocationChanged}` binding on `<bee-map>` in `render()`, immediately after the `@selection-drawn=${this._onSelectionDrawn}` binding (line 321), before the `></bee-map>` close.
    (3) Add the `_onUserLocationChanged(e: CustomEvent<...>)` handler. Type the detail as the union of `{ lat: number; lon: number; accuracy: number }` and `{ error: { code: number; message: string } }`. Branch on `'error' in e.detail`: on error, set `this._locationError = true` and `this._userLocation = null` (clear stale position on revocation — security: do not keep a position after permission is lost); on success, set `this._userLocation = e.detail` and `this._locationError = false`. RECOMMENDED (Claude's discretion, D-04 / RESEARCH Open Question 1): derive distinct copy — when `e.detail.error.code === 1` treat as 'denied'; when `code === 2 || code === 3` treat as 'unavailable' — by setting `_locationErrorKind` accordingly, so the banner can show "Location access blocked — enable in Settings → Safari → Location" vs "Unable to determine your location". Optionally validate that a success payload's `accuracy` is a finite, non-negative number before storing (RESEARCH Security V5 — avoid NaN propagating into Phase 153 haversine); on a non-finite value, ignore the fix rather than storing garbage.
    (4) Add the location-error banner in `render()`, modeled exactly on the `_updateAvailable` banner (lines 361-374) but placed after it. Conditional on `this._locationError`; a `div` with class `location-error-banner`, `role="alert"` (error semantics, not `role="status"`), `aria-live="polite"`; a `span` with class `location-error-banner__body` holding the copy; and a dismiss button with class `location-error-banner__dismiss`, `aria-label="Dismiss location error"`, whose `@click` sets `this._locationError = false`. If you implemented `_locationErrorKind`, switch the body copy on it (denied vs unavailable). The banner lives on `<bee-atlas>` (app-level), NOT inside `<bee-map>`'s shadow DOM — the native disabled control state remains the in-map affordance; this banner adds discoverability. Dismissing it must not affect map/filter/table state (it only flips `_locationError`).
    Do NOT call `_runFilterQuery()` or any SQLite-worker query from this handler — `_userLocation` is consumed only in Phase 153 (D-05); updating it costs at most a `render()` + lit-html diff. Confirm no `updated()`/`willUpdate()` hook re-queries on `_userLocation` change.
    Add CSS for `.location-error-banner` / `__body` / `__dismiss` mirroring the `.update-banner` styles (so the banner is visible and consistently styled).
  </action>
  <verify>
    <automated>npx tsc --noEmit && grep -Eq "@state\(\)\s+private\s+_userLocation" src/bee-atlas.ts && grep -q "_locationError" src/bee-atlas.ts && grep -q "@user-location-changed=" src/bee-atlas.ts && grep -q "location-error-banner" src/bee-atlas.ts && npm test -- src/tests/geolocation.test.ts && npm test</automated>
  </verify>
  <acceptance_criteria>
    - `grep -Eq "@state\(\)\s+private\s+_userLocation" src/bee-atlas.ts` and the declared type is exactly `{ lat: number; lon: number; accuracy: number } | null` (confirm by reading).
    - `grep -q "_locationError" src/bee-atlas.ts` — the error boolean state exists.
    - `grep -q "@user-location-changed=" src/bee-atlas.ts` and the binding target is `this._onUserLocationChanged` on the `<bee-map>` element in render() (confirm by reading).
    - The handler sets `this._userLocation = e.detail` on success and `this._userLocation = null` + `this._locationError = true` on error: `grep -q "'error' in e.detail" src/bee-atlas.ts` and confirm both branches by reading.
    - `grep -q "location-error-banner" src/bee-atlas.ts` and the banner uses `role="alert"` and a dismiss button that sets `_locationError = false`.
    - The handler does NOT call `_runFilterQuery` (or any worker query): reading the `_onUserLocationChanged` body shows no `_runFilterQuery` call.
    - `npm test -- src/tests/geolocation.test.ts` exits 0 — all five Plan 01 assertions are now GREEN (LOC-02 invariant proven).
    - `npm test` (full suite) exits 0 — no regression to bee-atlas.test.ts / cache-state.test.ts (which now mount with the extended mock).
    - `npx tsc --noEmit` exits 0 and `npm run build` succeeds.
  </acceptance_criteria>
  <done>`<bee-atlas>` owns `@state _userLocation` ({lat,lon,accuracy}|null) and `@state _locationError`; binds `@user-location-changed`; the handler routes success→`_userLocation` and error→banner (clearing stale position); the app-level denial banner renders and dismisses without touching map/filter/table state. All five geolocation.test.ts assertions GREEN; full suite green; build succeeds.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| GPS hardware → browser → `<bee-map>` | `navigator.geolocation` delivers precise location (sensitive PII) into the app via the control. |
| `<bee-map>` → `<bee-atlas>` | `user-location-changed` CustomEvent carries lat/lon/accuracy across the component boundary, in-process only. |
| App → network | This is a static-hosting app with NO server. Location must NOT cross to any network endpoint in this phase. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-152-02 | Information Disclosure | precise GPS coordinates (`_userLocation`) | mitigate | Location stays client-side: `_userLocation` is in-memory `@state` only, never sent to a network endpoint (no server exists), never written to URL or localStorage in this phase. Acceptance criterion: no `fetch`/`XMLHttpRequest`/`navigator.sendBeacon` references the location payload; the handler only assigns `@state`. |
| T-152-03 | Information Disclosure | logging of coordinates | mitigate | No `console.log`/`console.warn` of `e.coords` / `_userLocation` lat/lon in either file. Acceptance: `grep` for console logging of coords returns nothing in the new code. |
| T-152-04 | Spoofing / stale data | position retained after permission revoked | mitigate | On `error` (code 1 PERMISSION_DENIED) the handler clears `_userLocation = null` and surfaces the banner — no stale position lingers (RESEARCH Security table). |
| T-152-05 | Tampering / Elevation | silent geolocation activation | mitigate | No silent activation: the OS permission prompt is user-gated; the only programmatic `.trigger()` is the D-03 already-granted path (the user previously consented). `enableHighAccuracy` is requested only after consent. |
| T-152-06 | Tampering (input) | non-finite accuracy → NaN into Phase 153 haversine | accept (low) | Optional finite-number guard on `accuracy` recommended in the handler; low severity (browser-sourced number, no injection vector). |
| T-152-SC | Tampering | npm/pip/cargo installs | accept | ZERO package installs (RESEARCH.md). GeolocateControl ships in already-installed `mapbox-gl@3.24.1`. No `[ASSUMED]`/`[SUS]` package, no legitimacy checkpoint required. |

**Severity assessment:** No high-severity threats. All Information-Disclosure surfaces are mitigated by the no-server architecture + client-only `@state`. Phase clears the block-on-high-severity gate.
</threat_model>

<verification>
- `npm test -- src/tests/geolocation.test.ts` exits 0 — all five LOC-02 source invariants GREEN.
- `npm test` (full suite) exits 0 — bee-atlas.test.ts + cache-state.test.ts pass with the extended mock; no regression.
- `npx tsc --noEmit` exits 0; `npm run build` succeeds.
- Manual/device behaviors (blue dot, recenter, offline GPS, denial banner, iOS standalone) are NOT verified here — they are covered by Plan 03 (autonomous: false UAT). This plan delivers all automatable proof.
- Security: no network send / no coord logging of `_userLocation` in src/bee-map.ts or src/bee-atlas.ts (T-152-02/03).
</verification>

<success_criteria>
- LOC-01: GeolocateControl with trackUserLocation + enableHighAccuracy + showAccuracyCircle added immediately after `new mapboxgl.Map()` (offline-safe), top-right; recenter is the control default.
- LOC-02: `<bee-map>` emits `user-location-changed` via `_emit()` and stores no location @state; `<bee-atlas>` owns `@state _userLocation` and binds `@user-location-changed` — all five source-analysis assertions GREEN.
- LOC-03: error event (code 1/2/3) sets the app-level banner state and clears stale position; map/filters/table unaffected.
- D-03 granted-only auto-trigger runs inside a resolved permissions.query().then(); guarded for missing navigator.permissions.
- Full test suite green; build succeeds.
</success_criteria>

<output>
Create `.planning/phases/152-geolocatecontrol-location-state/152-02-SUMMARY.md` when done.
</output>
