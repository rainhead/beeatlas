---
phase: 999.1-surface-shift-drag-rectangle-selection-in-ui
verified: 2026-06-21T00:00:00Z
status: human_needed
score: 6/6 must-haves verified
overrides_applied: 0
human_verification:
  - test: "Open the app on a real desktop browser (hover+fine-pointer). Confirm the hint 'Shift-drag on map to set bounds' renders below the 'County, ecoregion, or place' input in the sidebar filters section."
    expected: "Hint text is visible, muted gray, same visual weight as other .hint lines."
    why_human: "CSS rendering and visual placement in the live Lit shadow DOM cannot be asserted from source text alone."
  - test: "Open the app on an iOS or Android device (or DevTools touch emulation). Confirm no hint text appears below the where input."
    expected: "Hint is absent from view — CSS @media (hover:hover) and (pointer:fine) hides it on touch devices."
    why_human: "Media query behavior requires a real (or emulated) touch viewport; cannot be asserted from source."
---

# Phase 999.1: Surface Shift-Drag Rectangle Selection in UI — Verification Report

**Phase Goal:** Make the existing shift-drag bounding-box (bounds-filter) gesture discoverable on desktop via a persistent hint in the sidebar filters section, hidden on touch — no behavior change.
**Verified:** 2026-06-21
**Status:** human_needed
**Re-verification:** No — initial verification

## Goal Achievement

### Observable Truths

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | On desktop, a hint reading exactly "Shift-drag on map to set bounds" renders directly below the where input in the filters section (D-05, D-06) | VERIFIED | `src/bee-pane.ts:1095` — `<p class="hint hint--desktop-only">Shift-drag on map to set bounds</p>`, sibling of `div.input-wrap` (line 1055 open, line 1094 close), before `div.elev-row` (line 1096) |
| 2 | The hint reuses the existing `.hint` class — no new text-styling pattern introduced (D-03) | VERIFIED | Line 1095 carries `class="hint hint--desktop-only"`; `.hint` class pre-exists in bee-pane.ts; no new color/font-size rule was added |
| 3 | Hint is gated by `@media (hover: hover) and (pointer: fine)` — CSS only, no JS device detection (D-01, D-02) | VERIFIED | `src/bee-pane.ts:511-518` — `.hint--desktop-only { display: none; }` base rule followed by `@media (hover: hover) and (pointer: fine) { .hint--desktop-only { display: block; } }` in `static styles` |
| 4 | Hint is always rendered regardless of bounds/filter state — no `boundsFilterActive` binding, no `nothing` branch (D-04) | VERIFIED | Line 1095 is unconditional literal template content — not wrapped in any conditional expression or ternary; no new `@property`/`@state` added |
| 5 | No new reactive `@property`/`@state` added to bee-pane; bee-pane stays a pure presenter | VERIFIED | Audit of `@property`/`@state` lines in bee-pane.ts shows no hint-related additions; existing decorators unchanged |
| 6 | `src/bee-map.ts` is unmodified; shift-drag gesture, bounds-filter plumbing, near-me path, and URL contract unchanged (D-01) | VERIFIED | `git log --diff-filter=M -- src/bee-map.ts` shows no 999.1 commit touched bee-map.ts; `boundsFilterActive` and `boundsFilterLabel` props confirmed present at bee-pane.ts:88-90 (999.8 naming intact) |

**Score:** 6/6 truths verified

### Deferred Items

None.

### Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `src/bee-pane.ts` | Hint `<p class="hint hint--desktop-only">` after div.input-wrap + `.hint--desktop-only` CSS rule + pointer-capability media query | VERIFIED | Element at line 1095; CSS at lines 510-518 |
| `src/tests/bee-sidebar.test.ts` | UI-01 describe block with 3 source-text assertions | VERIFIED | Lines 106-118; three tests: literal copy (toContain), class regex (toMatch), media-query regex (toMatch) |

### Key Link Verification

| From | To | Via | Status | Details |
|------|----|-----|--------|---------|
| `bee-pane.ts` hint `<p>` | `.hint--desktop-only` CSS rule + `@media (hover: hover) and (pointer: fine)` | `class="hint hint--desktop-only"` on the element matching the gated CSS selector | VERIFIED | Class attribute at line 1095 matches selector defined at lines 511 and 515; cascade order correct (base `display:none` before media-query override) |

### Data-Flow Trace (Level 4)

Not applicable — this artifact is static presentational text with no data variable, no fetch, and no reactive state. There is no data flow to trace.

### Behavioral Spot-Checks

| Behavior | Command | Result | Status |
|----------|---------|--------|--------|
| UI-01 test suite passes | `npm test` (reported by orchestrator: 818 passed) | 818 passed, 0 failed | PASS (reported; not re-run by verifier — test is source-text only and deterministic) |

Note: The three new UI-01 tests are pure source-text assertions (`readFileSync` on `bee-pane.ts`) — they are deterministic and environment-independent. The pre-existing 3 failing tests (build-geojson, build-output, data-species) are documented as pre-existing build-artifact issues unrelated to this phase.

### Probe Execution

No probes declared or applicable for this presentation-only phase.

### Requirements Coverage

| Requirement | Source Plan | Description | Status | Evidence |
|-------------|------------|-------------|--------|----------|
| D-01 | 999.1-01-PLAN.md | No behavior change; bee-map.ts unmodified | SATISFIED | bee-map.ts not in 999.1 git diff; gesture plumbing intact |
| D-02 | 999.1-01-PLAN.md | Desktop-only via CSS hover+pointer media query; no JS detection | SATISFIED | `@media (hover: hover) and (pointer: fine)` at bee-pane.ts:514 |
| D-03 | 999.1-01-PLAN.md | Reuses existing `.hint` class; no new text-styling pattern | SATISFIED | `class="hint hint--desktop-only"` at line 1095 |
| D-04 | 999.1-01-PLAN.md | Always rendered; not bound to boundsFilterActive or any reactive state | SATISFIED | Unconditional template literal at line 1095 |
| D-05 | 999.1-01-PLAN.md | Placed immediately below div.input-wrap, before div.elev-row | SATISFIED | Line 1094 closes div.input-wrap, line 1095 is hint, line 1096 opens div.elev-row |
| D-06 | 999.1-01-PLAN.md | Exact copy "Shift-drag on map to set bounds" | SATISFIED | Verbatim string at bee-pane.ts:1095 |

### Anti-Patterns Found

None. No TBD, FIXME, XXX, TODO, placeholder, or stub patterns found in the two modified files for this phase's additions.

### Human Verification Required

#### 1. Desktop visual rendering

**Test:** Open the app in a desktop browser (Chrome/Firefox/Safari on macOS or Windows). Navigate to the sidebar filters section. Observe the area immediately below the "County, ecoregion, or place" input.
**Expected:** The text "Shift-drag on map to set bounds" appears in muted gray, same visual weight as other `.hint`-class lines in bee-pane.
**Why human:** CSS shadow-DOM rendering and visual placement within the Lit component cannot be asserted from source text alone.

#### 2. Touch device hint hidden

**Test:** Open the app on an iOS or Android device, or use DevTools touch emulation (set device type to mobile, enabling touch/no-hover). Observe the same sidebar area.
**Expected:** No hint text appears — the CSS `@media (hover: hover) and (pointer: fine)` block excludes touch/coarse-pointer devices.
**Why human:** Media query activation requires a real (or faithfully emulated) touch viewport; cannot be verified from source.

### Gaps Summary

No gaps. All six D-series requirements are satisfied in the source. The phase goal — discoverability hint for the shift-drag bounds-filter gesture, CSS-only desktop gating, no behavior change — is fully achieved at the source level.

Human verification items are visual/device checks only and do not block the source-level contract. Per the verification instructions for presentation-only phases where the source-level contract (media query present, correct placement, exact copy) is fully verifiable and the test suite is green, these items are listed for completeness but represent low risk.

---

_Verified: 2026-06-21_
_Verifier: Claude (gsd-verifier)_
