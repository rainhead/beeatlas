---
phase: 178-thin-write-layer-inat-oauth
plan: 09
type: execute
wave: 7
depends_on: ["178-08"]
files_modified:
  - docs/runbooks/notes-write-launch-gate.md
autonomous: false
requirements: [WRITE-02, WRITE-03, WRITE-04]
user_setup: []

must_haves:
  truths:
    - "Sign-in works end-to-end from beeatlas.net; no client_secret or iNat token is observable in the bundle, network tab, URL, or localStorage; the session cookie is HttpOnly+Secure+SameSite=Strict"
    - "A forged-author POST is rejected; a cross-origin POST from a foreign origin is rejected"
    - "The redirect_uri exact-match pin (https://api.beeatlas.net/auth/callback) holds; PKCE works end-to-end (or the plain fallback is used)"
    - "A forced error under Waitress returns a generic error, NOT a stack trace (app.debug=False + generic error handler)"
  artifacts:
    - path: "docs/runbooks/notes-write-launch-gate.md"
      provides: "recorded security-UAT results appended to the launch runbook"
  key_links:
    - from: "security UAT"
      to: "the live api.beeatlas.net deploy (178-08)"
      via: "real-browser cross-origin/SameSite/CORS behavior pytest cannot exercise"
      pattern: "cross-origin"
---

<objective>
The security UAT gate — the human proof that pytest cannot give (real-browser SameSite/CORS,
network/localStorage inspection, live iNat OAuth). Proves the security-critical properties the phase
brief requires: (a) no token/secret leaks to the client, (b) a forged-author request is rejected,
(c) a cross-origin POST is rejected — plus the redirect_uri pin and the traceback guard (restated for
Waitress: app.debug=False + generic error handler, no cgitb since Waitress renders no traceback page).

Purpose: WRITE-02 (no secret/token leak) + WRITE-03 (forged-author + cross-origin rejection) proven
live. This is a `UI hint: yes` phase — do NOT auto-advance past this UAT (autonomous:false).
Output: recorded UAT results appended to the launch runbook; phase ready for /gsd-verify-work.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/178-thin-write-layer-inat-oauth/178-CONTEXT.md
@.planning/phases/178-thin-write-layer-inat-oauth/178-RESEARCH.md
@.planning/phases/178-thin-write-layer-inat-oauth/178-VALIDATION.md
@docs/runbooks/notes-write-launch-gate.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Security UAT — no leak, forged-author + cross-origin rejection, redirect pin, traceback guard (Waitress)</name>
  <files>docs/runbooks/notes-write-launch-gate.md</files>
  <read_first>
    - .planning/phases/178-thin-write-layer-inat-oauth/178-VALIDATION.md (§ Manual-Only Verifications — the exact cross-origin fetch + no-leak + redirect-pin scripts)
    - .planning/phases/178-thin-write-layer-inat-oauth/178-RESEARCH.md (§ Pitfall 2/A4 raw JWT header, § Pitfall 1/A3 scope, § Open Questions 2 PKCE-vs-plain fallback, § Pattern 3 SameSite/CORS; the ⚠ SUPERSESSION banner — the flup cgitb Pitfall 3 is obsolete, restated below for Waitress)
    - .planning/phases/178-thin-write-layer-inat-oauth/178-CONTEXT.md (§ D-17 — the traceback guard under Waitress is app.debug=False + a generic error handler, NOT flup cgitb)
  </read_first>
  <action>
    Pause for the operator to run the live security UAT against the deployed api.beeatlas.net (served
    by Waitress behind Apache mod_proxy_http) using the seven-step script below (sign-in, no-leak
    inspection, forged-author POST, cross-origin POST, redirect_uri pin, PKCE-or-fallback, traceback
    guard), and append a dated PASS/FAIL results block to docs/runbooks/notes-write-launch-gate.md. Do
    NOT auto-advance past this UAT (UI hint: yes). Route any FAIL to `/gsd-plan-phase 178 --gaps`.
  </action>
  <what-built>
    Live sign-in on beeatlas.net → api.beeatlas.net: OAuth PKCE flow, HttpOnly session cookie, the
    allowlist-guarded POST /api/write-check, flask-cors scoped to beeatlas.net, ProxyFix trusting one
    hop, and the generic error handler (app.debug=False). All unit-tested; this task proves the
    live-browser and live-iNat behavior.
  </what-built>
  <how-to-verify>
    1. Sign in from https://beeatlas.net via "Sign in with iNaturalist" → complete the iNat OAuth
       round-trip → land back signed in; the header shows your iNat login + allowlisted-or-not.
    2. No-leak: with DevTools open across the whole flow, confirm NO `client_secret` and NO iNat
       access token / api_token JWT appears in: the JS bundle (view-source / sources), any network
       request/response body, the redirect URLs, or localStorage/sessionStorage. Confirm the session
       cookie is HttpOnly (not readable via `document.cookie`), Secure, and SameSite=Strict.
    3. Forged-author: as an allowlisted author, `fetch('https://api.beeatlas.net/api/write-check',
       {method:'POST', credentials:'include', headers:{'Content-Type':'application/json'},
       body:JSON.stringify({author_id: 999999})})` from the beeatlas.net console → confirm the
       returned identity is YOUR derived id, NOT 999999 (the body author is ignored).
    4. Cross-origin: from a foreign origin (e.g. a scratch page on example.com or `null` origin),
       POST the same endpoint with credentials:'include' → confirm it is BLOCKED (CORS preflight
       rejects the read, and/or the server returns 403 on the Origin check).
    5. Redirect pin: confirm the OAuth flow only succeeds with redirect_uri
       https://api.beeatlas.net/auth/callback (D-12/D-13) — a mismatch is rejected by Doorkeeper.
    6. PKCE: confirm the flow used the PKCE `code_challenge`; if Doorkeeper rejected it for the
       confidential client, confirm the plain-exchange fallback (D-02) was used and note which.
    7. Traceback guard (Waitress): deliberately trigger an app error → confirm a generic error body,
       NOT a stack trace (app.debug=False + the generic error handler; Waitress renders no cgitb page —
       the old flup footgun no longer applies).
    8. Append a dated results block to docs/runbooks/notes-write-launch-gate.md (PASS/FAIL per item).
  </how-to-verify>
  <acceptance_criteria>
    - Sign-in completes end-to-end from beeatlas.net; whoami reflects the allowlist status.
    - No client_secret and no iNat token observed anywhere client-side; session cookie is HttpOnly+Secure+SameSite=Strict.
    - The forged-author POST returns the server-derived identity (body author ignored).
    - The cross-origin POST is rejected.
    - The redirect_uri exact-match pin holds; PKCE-or-fallback is confirmed and recorded.
    - A forced error returns a generic response, no stack trace (app.debug=False + generic handler under Waitress).
    - Results appended to the launch runbook.
  </acceptance_criteria>
  <verify>
    <automated>MISSING — real-browser + live-iNat UAT; pytest test_client cannot exercise SameSite/CORS/localStorage. Results recorded in docs/runbooks/notes-write-launch-gate.md + the SUMMARY</automated>
  </verify>
  <resume-signal>Type "security UAT passed" with the per-item results, or describe any failure to route to gap closure.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| attacker origin → api.beeatlas.net | cross-site forged POST attempt; must be rejected by CORS + Origin check |
| live browser session | the only place SameSite/CORS/HttpOnly cookie behavior is truly observable |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-178-09 | Spoofing | forged-author write | mitigate | UAT step 3 proves the body author is ignored (server-derived identity wins) |
| T-178-11 | Spoofing / Tampering | cross-site forged POST | mitigate | UAT step 4 proves the foreign-origin POST is blocked (CORS + Origin gate) |
| T-178-19 | Information Disclosure | secret/token leak to client | mitigate | UAT step 2 proves no secret/token in bundle/network/URL/storage; cookie HttpOnly |
| T-178-06 | Tampering / Spoofing | open redirect | mitigate | UAT step 5 proves the redirect_uri exact-match pin holds |
| T-178-15 | Information Disclosure | traceback leak under Waitress | mitigate | UAT step 7 confirms a forced error shows a generic response (app.debug=False + generic handler; no cgitb under Waitress) |
</threat_model>

<verification>
All seven UAT items pass and are recorded in docs/runbooks/notes-write-launch-gate.md. Any failure
routes to `/gsd-plan-phase 178 --gaps`. Do NOT auto-advance past this checkpoint (UI hint: yes).
</verification>

<success_criteria>
- No token/secret leaks to the client; session cookie is HttpOnly+Secure+SameSite=Strict.
- Forged-author and cross-origin POSTs are both rejected (WRITE-03).
- redirect_uri pin holds; PKCE-or-fallback confirmed; traceback guard holds under Waitress (generic error, no trace).
</success_criteria>

## Artifacts this plan produces
- `docs/runbooks/notes-write-launch-gate.md` → appended dated security-UAT results block (no new source symbols; this is the human verification gate)

<output>
Create `.planning/phases/178-thin-write-layer-inat-oauth/178-09-SUMMARY.md` when done.
</output>
</content>
