---
phase: 178-thin-write-layer-inat-oauth
plan: 03
type: execute
wave: 2
depends_on: ["178-01"]
files_modified:
  - data/pyproject.toml
  - api/__init__.py
  - api/main.py
  - api/config.py
  - api/tests/test_app.py
  - data/notes_app/main.py
  - data/notes_app/__init__.py
  - data/tests/test_notes_app.py
autonomous: true
requirements: [WRITE-01]
user_setup: []

must_haves:
  truths:
    - "The write/auth service lives at top-level api/ (not under data/) and imports the store from data/notes_store/"
    - "pytest collects both data/tests and api/tests from a single `cd data && uv run pytest` run"
    - "api/config.py loads secrets from gitignored api/secrets.toml (client_id/secret, redirect_uri, signing_key) and fails loud on placeholders at runtime"
  artifacts:
    - path: "api/main.py"
      provides: "Flask WSGI app relocated from data/notes_app/main.py"
      contains: "app = Flask"
    - path: "api/config.py"
      provides: "secrets loader for api/secrets.toml + the pinned redirect_uri constant"
    - path: "api/__init__.py"
      provides: "api as an importable package"
  key_links:
    - from: "api/ code"
      to: "notes_store (data/)"
      via: "pytest pythonpath = ['.', '..'] makes both importable"
      pattern: "pythonpath"
---

<objective>
Establish the top-level `api/` package as BeeAtlas's auth + write service (D-15) and install the
two new dependencies (gated by 178-01). Relocate the 177 Flask health skeleton from
`data/notes_app/main.py` to `api/main.py`, retire the `notes_app` package, add an `api/config.py`
secrets loader for the already-seeded gitignored `api/secrets.toml` (D-14), and wire pytest so a
single `cd data && uv run pytest` collects both the store tests (`data/tests`) and the new API tests
(`api/tests`) while keeping `notes_store` importable from `data/`.

Purpose: WRITE-01 — a thin Flask/WSGI app served under Apache mod_fcgid, isolated from the
derived pipeline; placing it outside `data/` reinforces the STORE-04 isolation story.
Output: importable `api` package + secrets loader + test harness; everything downstream builds here.

This is app-level infrastructure — NOT a "notes app". Notes are merely the first feature (Phase 179).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/178-thin-write-layer-inat-oauth/178-CONTEXT.md
@data/notes_app/main.py
@data/tests/test_notes_app.py
@data/pyproject.toml
@api/secrets.example.toml
@api/secrets.toml
@data/notes_store/db.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install deps + create the api/ package (relocate skeleton, retire notes_app)</name>
  <files>data/pyproject.toml, api/__init__.py, api/main.py, data/notes_app/main.py, data/notes_app/__init__.py</files>
  <read_first>
    - .planning/phases/178-thin-write-layer-inat-oauth/178-01-SUMMARY.md (confirm the flup6/flask-cors legitimacy gate is APPROVED before running `uv add`)
    - data/notes_app/main.py (the exact Flask skeleton to move verbatim into api/main.py)
    - data/pyproject.toml (dependency list to extend; requires-python >=3.14)
  </read_first>
  <action>
    First confirm 178-01 approved the packages, then run `cd data && uv add flup6 flask-cors`
    (itsdangerous/requests/flask already present). Create `api/__init__.py` with a one-line
    docstring naming this BeeAtlas's auth + write API (D-15). Move the Flask app from
    data/notes_app/main.py into `api/main.py` (keep the `/health` route and the `app = Flask(__name__)`
    WSGI callable; update the module docstring to say routes are added by later 178 plans and drop
    the stale "SCOPE GUARD: no write routes" line). Delete data/notes_app/main.py and
    data/notes_app/__init__.py (retire the `notes_app` package entirely). Do NOT relocate
    data/notes_store/ (D-16 — it stays put). Ensure api/main.py imports cleanly with the store on
    path (it does not import notes_store yet — that comes in later plans).
  </action>
  <acceptance_criteria>
    - `flup6` and `flask-cors` appear in data/pyproject.toml dependencies after `uv add`.
    - `api/main.py` exists with `app = Flask(__name__)` and the `/health` route; `api/__init__.py` exists.
    - `data/notes_app/` no longer contains main.py/__init__.py (package retired).
    - `grep -rn "notes_app" data/ api/ --include=*.py` returns nothing outside __pycache__.
  </acceptance_criteria>
  <verify>
    <automated>cd data && uv run python -c "import flup, flask_cors; import sys; sys.path.insert(0,'..'); from api.main import app; assert app.name=='api.main' or 'api' in app.import_name; print('ok')"</automated>
  </verify>
  <done>Deps installed; api package importable; notes_app gone.</done>
</task>

<task type="auto">
  <name>Task 2: api/config.py secrets loader (D-14) + pytest pythonpath/testpaths wiring</name>
  <files>api/config.py, data/pyproject.toml</files>
  <read_first>
    - api/secrets.toml + api/secrets.example.toml (D-14 — the gitignored [inaturalist_oauth] client_id/client_secret/redirect_uri + [session] signing_key keys; REPLACE_ME placeholders; root .gitignore already covers api/secrets.toml)
    - data/config.py (the project's module-level-constant-from-file loader convention to mirror)
    - data/notes_store/roles.py (tomllib load-at-import pattern to mirror)
    - data/pyproject.toml ([tool.pytest.ini_options] — testpaths=["tests"], addopts, markers)
  </read_first>
  <action>
    Create `api/config.py` mirroring the data/config.py + notes_store/roles.py load-from-file
    conventions and honoring D-14 (secrets live only in the gitignored api/secrets.toml, never
    committed, never in the client bundle): read `api/secrets.toml` (path relative to this file) via
    tomllib and expose module-level constants INAT_CLIENT_ID, INAT_CLIENT_SECRET, REDIRECT_URI, and
    SESSION_SIGNING_KEY, plus a pinned constant REDIRECT_URI value that MUST equal
    `https://api.beeatlas.net/auth/callback` (assert it matches the toml value; this is the
    exact-match pin from D-12/D-13). Provide a `require_real_secrets()` helper that raises loudly
    if any secret still contains `REPLACE_ME` — call it from request/route code (later plans),
    NOT at import (so tests and the frontend build never need real secrets). Load the toml
    defensively: if `api/secrets.toml` is absent, expose empty/placeholder values rather than
    crashing at import (the file is gitignored and absent in CI/tests). Then edit
    data/pyproject.toml `[tool.pytest.ini_options]`: set `pythonpath = [".", ".."]` (so
    `notes_store` imports resolve from data/ and `api` resolves from the repo root) and
    `testpaths = ["tests", "../api/tests"]`. Ensure api/tests exists (created in Task 3).
  </action>
  <acceptance_criteria>
    - `api/config.py` exposes INAT_CLIENT_ID, INAT_CLIENT_SECRET, REDIRECT_URI, SESSION_SIGNING_KEY.
    - `REDIRECT_URI == "https://api.beeatlas.net/auth/callback"` (the exact-match pin) is asserted.
    - `require_real_secrets()` raises when a value contains `REPLACE_ME`.
    - Importing api/config.py with a REPLACE_ME secrets.toml does NOT raise (only `require_real_secrets()` does).
    - data/pyproject.toml has `pythonpath = [".", ".."]` and `testpaths = ["tests", "../api/tests"]`.
  </acceptance_criteria>
  <verify>
    <automated>cd data && uv run python -c "import sys; sys.path.insert(0,'..'); import api.config as c; assert c.REDIRECT_URI=='https://api.beeatlas.net/auth/callback'; print('ok')"</automated>
  </verify>
  <done>Secrets loader present with pinned redirect_uri; pytest collects api/tests.</done>
</task>

<task type="auto">
  <name>Task 3: Relocate + update the app skeleton test into api/tests</name>
  <files>api/tests/test_app.py, data/tests/test_notes_app.py</files>
  <read_first>
    - data/tests/test_notes_app.py (the three existing health/skeleton tests + the SCOPE-GUARD test that asserts no write verbs exist)
  </read_first>
  <action>
    Move data/tests/test_notes_app.py to `api/tests/test_app.py`, updating imports from
    `notes_app.main` to `api.main`. Keep `test_health_direct` and `test_health_route_via_client`.
    Update the SCOPE-GUARD test: the "no write verbs" assertion is now intentionally obsolete
    (write routes land in 178-06) — replace it with a health-route smoke assertion (e.g. `/health`
    is registered and returns 200) and a comment noting the scope boundary moved intentionally in
    Phase 178. Delete the old data/tests/test_notes_app.py. Confirm the relocated test collects via
    the new `testpaths`/`pythonpath` config from `cd data && uv run pytest`.
  </action>
  <acceptance_criteria>
    - `api/tests/test_app.py` exists; imports `from api.main import app`; no reference to `notes_app`.
    - The obsolete "no write verbs" scope-guard assertion is removed/replaced (write routes are now in-scope).
    - `data/tests/test_notes_app.py` no longer exists.
  </acceptance_criteria>
  <verify>
    <automated>cd data && uv run pytest ../api/tests/test_app.py -x</automated>
  </verify>
  <done>Relocated app test passes via the unified pytest config.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| gitignored secrets file → app runtime | client_secret + signing_key must never enter git or the client bundle |
| PyPI → app runtime | flup6/flask-cors enter here (install gated by 178-01) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-178-SC | Tampering | `uv add flup6 flask-cors` | mitigate | install runs only after the 178-01 blocking-human legitimacy gate approves both |
| T-178-03 | Information Disclosure | api/secrets.toml (client_secret, signing_key) | mitigate | file is gitignored (.gitignore already covers `api/secrets.toml`) per D-14; config loader never logs values; `require_real_secrets()` gate is server-side only |
| T-178-04 | Tampering / Spoofing | redirect_uri drift | mitigate | REDIRECT_URI pinned + asserted == `https://api.beeatlas.net/auth/callback` in config.py |
</threat_model>

<verification>
`cd data && uv run pytest` collects and passes both data/tests and api/tests; `api.config.REDIRECT_URI`
is the pinned callback; `notes_app` is fully retired; flup6/flask-cors installed.
</verification>

<success_criteria>
- api/ is an importable package hosting the relocated Flask skeleton + config loader.
- Single `cd data && uv run pytest` covers store + API tests.
- Secrets are loaded from the gitignored file (D-14) with the redirect_uri exact-match pin; placeholders fail loud only at request time.
</success_criteria>

## Artifacts this plan produces
- `api/__init__.py` (package marker), `api/main.py` (relocated Flask `app` + `/health`), `api/config.py` (new symbols: `INAT_CLIENT_ID`, `INAT_CLIENT_SECRET`, `REDIRECT_URI`, `SESSION_SIGNING_KEY`, `require_real_secrets()`)
- `api/tests/test_app.py` (relocated skeleton tests)
- `data/pyproject.toml` → `flup6`, `flask-cors` deps + pytest `pythonpath`/`testpaths` for api/tests
- Deleted: `data/notes_app/` package + `data/tests/test_notes_app.py`

<output>
Create `.planning/phases/178-thin-write-layer-inat-oauth/178-03-SUMMARY.md` when done.
</output>
