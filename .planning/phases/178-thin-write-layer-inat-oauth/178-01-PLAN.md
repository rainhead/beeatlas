---
phase: 178-thin-write-layer-inat-oauth
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: []
autonomous: false
requirements: [WRITE-01]
user_setup: []

must_haves:
  truths:
    - "waitress and flask-cors are confirmed legitimate on PyPI before any install task runs"
  artifacts: []
  key_links:
    - from: "178-03 foundation (uv add)"
      to: "this legitimacy gate"
      via: "blocking-human approval must precede the package install"
      pattern: "checkpoint approved before uv add"
---

<objective>
Gate the two new Python packages this phase installs (`waitress`, `flask-cors`) behind a
blocking human legitimacy verification before any `uv add` runs. `flask-cors` already carries a
RESEARCH.md § Package Legitimacy Audit `slopcheck [OK]` (but surfaced via WebSearch, so `[ASSUMED]`
name provenance). **`waitress` is a post-research substitution (D-17) — it REPLACES the rejected
`flup6`** and was therefore never run through the RESEARCH audit, so it is `[ASSUMED]` and MUST be
confirmed fresh against the live registry + its maintainer repo before install per the phase's
package-legitimacy protocol.

Purpose: A supply-chain compromise of the write layer's WSGI server or CORS handler would undermine
the entire auth/write surface. This is the T-178-SC mitigation.
Output: A recorded human approval (or rejection) unblocking plan 178-03's `uv add`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/178-thin-write-layer-inat-oauth/178-CONTEXT.md
@.planning/phases/178-thin-write-layer-inat-oauth/178-RESEARCH.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: Verify waitress and flask-cors legitimacy on PyPI</name>
  <files></files>
  <read_first>
    - .planning/phases/178-thin-write-layer-inat-oauth/178-CONTEXT.md (§ D-17 — Waitress replaces the rejected flup6; Pylons Project maintained pure-Python WSGI server)
    - .planning/phases/178-thin-write-layer-inat-oauth/178-RESEARCH.md (§ Package Legitimacy Audit — flask-cors slopcheck [OK]; the ⚠ SUPERSESSION banner marking all flup6 material obsolete)
  </read_first>
  <action>
    Pause for the operator. Present the two packages (waitress, flask-cors) with their PyPI URLs and
    their maintainer repos, and ask the operator to confirm each maps to its stated source repo (not a
    typosquat) before plan 178-03 runs `uv add`. `waitress` is a D-17 post-research substitution
    (replacing flup6) with no prior slopcheck record — optionally run
    `slopcheck install waitress --ecosystem pypi` in a disposable venv for supporting evidence, but the
    core gate is the operator confirming the PyPI page + the Pylons Project repo. Do NOT auto-approve;
    this legitimacy gate ignores workflow.auto_advance. Record the approval (or the removal/replacement
    directive) in the SUMMARY.
  </action>
  <what-built>
    `flask-cors` 6.0.5 (github.com/corydolphin/flask-cors, actively maintained) was slopchecked
    `[OK]` during RESEARCH (tagged `[ASSUMED]` only because the name was WebSearch-sourced). `waitress`
    (github.com/Pylons/waitress, docs.pylonsproject.org/projects/waitress) is the maintained,
    pure-Python WSGI server chosen in D-17 to replace the rejected decade-stale `flup6`; it is 3.x
    (~3.0.2), actively released with security fixes through 2026, and pure-Python so nothing compiles
    against the project's Python 3.14. Because waitress entered after the RESEARCH audit, it carries
    NO prior slopcheck verdict and MUST be confirmed live here.
  </what-built>
  <how-to-verify>
    1. Open https://pypi.org/project/waitress/ — confirm it exists, is version 3.x (~3.0.2), links to
       the Pylons Project source repo (github.com/Pylons/waitress), and is the pure-Python WSGI server
       (not a typosquat). Confirm recent releases (maintained through 2026).
    2. Open https://pypi.org/project/flask-cors/ — confirm it exists, is the corydolphin/flask-cors
       project, and is widely downloaded (standard Flask ecosystem package).
    3. Confirm neither is a look-alike/typosquat of a more popular package name.
  </how-to-verify>
  <acceptance_criteria>
    - Operator has visited both PyPI project pages and confirmed each maps to the expected source repo (waitress → Pylons/waitress, flask-cors → corydolphin/flask-cors).
    - Approval (or rejection) is explicit; auto-advance is NOT permitted for this task even in yolo mode.
    - No reference to flup6 survives — waitress is the confirmed WSGI server.
  </acceptance_criteria>
  <verify>
    <automated>MISSING — human legitimacy gate; approval recorded in the plan SUMMARY, no automated command</automated>
  </verify>
  <resume-signal>Type "approved" to unblock plan 178-03's install, or name the package to remove and its replacement.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| PyPI registry → project build env | Untrusted third-party code enters the write layer at `uv add` time |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-178-SC | Tampering | `uv add waitress flask-cors` (pip/PyPI) | mitigate | flask-cors slopcheck [OK] (RESEARCH audit) + this blocking-human PyPI verification of BOTH packages before install; waitress is a post-research [ASSUMED] substitution confirmed live here; `[SLOP]` forbidden |
</threat_model>

<verification>
Gate is satisfied when the operator has explicitly approved both packages against live PyPI
(waitress → Pylons/waitress; flask-cors → corydolphin/flask-cors). No code changes in this plan; the
sole output is the recorded approval that unblocks 178-03.
</verification>

<success_criteria>
- Both packages confirmed legitimate on pypi.org against their stated source repos.
- Explicit approval recorded; 178-03 may proceed with `uv add waitress flask-cors`.
</success_criteria>

## Artifacts this plan produces
- No source artifacts. Produces a recorded human approval (SUMMARY) that unblocks the install in 178-03.

<output>
Create `.planning/phases/178-thin-write-layer-inat-oauth/178-01-SUMMARY.md` when done.
</output>
</content>
</invoke>
