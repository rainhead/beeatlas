---
phase: 178-thin-write-layer-inat-oauth
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: []
autonomous: false
requirements: [WRITE-01]
user_setup: []

must_haves:
  truths:
    - "flup6 and flask-cors are confirmed legitimate on PyPI before any install task runs"
  artifacts: []
  key_links:
    - from: "178-03 foundation (uv add)"
      to: "this legitimacy gate"
      via: "blocking-human approval must precede the package install"
      pattern: "checkpoint approved before uv add"
---

<objective>
Gate the two new Python packages this phase installs (`flup6`, `flask-cors`) behind a
blocking human legitimacy verification before any `uv add` runs. RESEARCH.md § Package
Legitimacy Audit already ran `slopcheck install` (both `[OK]`) but surfaced both package
*names* via WebSearch (`[ASSUMED]` provenance), so an operator must confirm them against the
live registry before install per the phase's package-legitimacy protocol.

Purpose: A supply-chain compromise of the write layer's FastCGI bridge or CORS handler would
undermine the entire auth/write surface. This is the T-178-SC mitigation.
Output: A recorded human approval (or rejection) unblocking plan 178-03's `uv add`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/178-thin-write-layer-inat-oauth/178-RESEARCH.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: Verify flup6 and flask-cors legitimacy on PyPI</name>
  <files></files>
  <read_first>
    - .planning/phases/178-thin-write-layer-inat-oauth/178-RESEARCH.md (§ Package Legitimacy Audit — the slopcheck [OK] results + [ASSUMED] name provenance for flup6 / flask-cors)
  </read_first>
  <action>
    Pause for the operator. Present the two packages (flup6, flask-cors) with their PyPI URLs and
    the RESEARCH.md slopcheck [OK] evidence, and ask the operator to confirm each maps to its stated
    source repo (not a typosquat) before plan 178-03 runs `uv add`. Do NOT auto-approve; this
    legitimacy gate ignores workflow.auto_advance. Record the approval (or the removal/replacement
    directive) in the SUMMARY.
  </action>
  <what-built>
    RESEARCH.md ran `slopcheck install flup6 flask-cors --ecosystem pypi` in a disposable venv:
    both returned `[OK]`. `flup6` 1.1.1 (bitbucket.org/denisenkom/flup, last release 2015 —
    stale but functionally verified importable on Python 3.14.6 this research session);
    `flask-cors` 6.0.5 (github.com/corydolphin/flask-cors, actively maintained). Both are
    tagged `[ASSUMED]` only because the package *names* were WebSearch-sourced before registry
    confirmation — no `[SLOP]` or `[SUS]` verdict.
  </what-built>
  <how-to-verify>
    1. Open https://pypi.org/project/flup6/ — confirm it exists, version 1.1.1, source link to
       the flup repo, and that it is the FastCGI/WSGI bridge (not a typosquat).
    2. Open https://pypi.org/project/flask-cors/ — confirm it exists, is the corydolphin/flask-cors
       project, and is widely downloaded (standard Flask ecosystem package).
    3. Confirm neither is a look-alike/typosquat of a more popular package name.
  </how-to-verify>
  <acceptance_criteria>
    - Operator has visited both PyPI project pages and confirmed each maps to the expected source repo.
    - Approval (or rejection) is explicit; auto-advance is NOT permitted for this task even in yolo mode.
  </acceptance_criteria>
  <verify>
    <automated>MISSING — human legitimacy gate; approval recorded in the plan SUMMARY, no automated command</automated>
  </verify>
  <resume-signal>Type "approved" to unblock plan 178-03's install, or name the package to remove and its replacement.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| PyPI registry → project build env | Untrusted third-party code enters the write layer at `uv add` time |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-178-SC | Tampering | `uv add flup6 flask-cors` (pip/PyPI) | mitigate | slopcheck [OK] (RESEARCH audit) + this blocking-human PyPI verification before install; `[SLOP]` forbidden |
</threat_model>

<verification>
Gate is satisfied when the operator has explicitly approved both packages against live PyPI.
No code changes in this plan; the sole output is the recorded approval that unblocks 178-03.
</verification>

<success_criteria>
- Both packages confirmed legitimate on pypi.org against their stated source repos.
- Explicit approval recorded; 178-03 may proceed with `uv add flup6 flask-cors`.
</success_criteria>

## Artifacts this plan produces
- No source artifacts. Produces a recorded human approval (SUMMARY) that unblocks the install in 178-03.

<output>
Create `.planning/phases/178-thin-write-layer-inat-oauth/178-01-SUMMARY.md` when done.
</output>
