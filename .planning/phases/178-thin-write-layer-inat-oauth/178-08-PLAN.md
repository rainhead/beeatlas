---
phase: 178-thin-write-layer-inat-oauth
plan: 08
type: execute
wave: 6
depends_on: ["178-06", "178-07"]
files_modified:
  - infra/lib/beeatlas-stack.ts
  - docs/runbooks/notes-write-launch-gate.md
  - data/roles_allowlist.toml
autonomous: false
requirements: [WRITE-01, WRITE-04]
user_setup:
  - service: inaturalist_oauth
    why: "Fill the real client_secret + session signing_key the Flask service loads at runtime (app already registered — D-12/D-13 done)"
    env_vars:
      - name: client_secret
        source: "iNaturalist → Account → Applications → the registered BeeAtlas app → client secret (paste into api/secrets.toml [inaturalist_oauth] on maderas)"
      - name: signing_key
        source: "generate on maderas: python -c \"import secrets; print(secrets.token_urlsafe(64))\" → api/secrets.toml [session]"
    dashboard_config:
      - task: "Confirm the app's redirect URI is exactly https://api.beeatlas.net/auth/callback (D-13 already set)"
        location: "iNaturalist → Account → Applications → BeeAtlas app"
  - service: maderas
    why: "Apache mod_proxy_http reverse proxy + certbot TLS for api.beeatlas.net, and a supervisor to keep the Waitress loopback process running; no passwordless sudo, so this is human-only"
    dashboard_config:
      - task: "Enable mod_proxy/mod_proxy_http/mod_headers; create the api.beeatlas.net vhost (certbot TLS; ProxyPass / http://127.0.0.1:<port>/ + ProxyPassReverse; RequestHeader set X-Forwarded-Proto https); deploy the api/ tree; set NOTES_DB_PATH=~/beeatlas-store/notes.db; start Waitress (api/serve.py) under the chosen supervisor (systemd-user else cron @reboot per D-18); run `alembic upgrade head` (→ 0002 users table)"
        location: "maderas host (45.79.96.48)"

must_haves:
  truths:
    - "api.beeatlas.net resolves to maderas (45.79.96.48) over TLS and serves the Flask app via Apache mod_proxy_http reverse-proxying the Waitress loopback process"
    - "Waitress runs under a supervisor (a --user systemd unit if maderas has systemd, else a cron @reboot entry) so it survives reboots/crashes (D-18)"
    - "The users-table migration (0002) is applied on the live maderas store"
    - "Public writes are gated: writes_enabled stays false until the operator confirms the demonstrated 177-07 restore, then is flipped true"
  artifacts:
    - path: "infra/lib/beeatlas-stack.ts"
      provides: "surgical Route53 A-record for api.beeatlas.net → 45.79.96.48"
      contains: "api"
    - path: "docs/runbooks/notes-write-launch-gate.md"
      provides: "the WRITE-04 launch checklist + the mod_proxy_http/Waitress/supervisor go-live runbook (both D-18 supervisor branches)"
  key_links:
    - from: "writes_enabled launch flag"
      to: "docs/runbooks/notes-store-dr.md Drill Log (177-07 restore PASS)"
      via: "operator confirms the restore before flipping the flag"
      pattern: "restore.*PASS"
    - from: "Apache mod_proxy_http vhost"
      to: "Waitress on 127.0.0.1:<port>"
      via: "ProxyPass / + ProxyPassReverse; X-Forwarded-Proto set for ProxyFix"
      pattern: "ProxyPass"
---

<objective>
Take the write layer live on maderas and encode the WRITE-04 launch gate. Surgical CDK edit adds the
`api.beeatlas.net` A-record (plain IP → 45.79.96.48, NOT a CloudFront alias — D-11, never
`cdk destroy`). The operator then enables Apache `mod_proxy_http` + `mod_headers`, creates the
`api.beeatlas.net` vhost (certbot TLS terminating at Apache, `ProxyPass`/`ProxyPassReverse` to the
Waitress loopback process, `X-Forwarded-Proto https` for ProxyFix), deploys the `api/` tree, fills the
real secrets, starts Waitress (`api/serve.py`) under a supervisor (systemd-user if present, else cron
`@reboot` per D-18), and runs the users migration on the live store. Finally the WRITE-04 gate:
`writes_enabled` stays false until the operator confirms the already-demonstrated 177-07 restore (Drill
Log PASS 2026-07-03), then flips it and adds the first real author to the allowlist.

Purpose: WRITE-01 (served on real infra via Waitress + mod_proxy_http) + WRITE-04 (public writes gated
on the proven restore).
Output: CDK A-record, launch-gate + go-live runbook, live api.beeatlas.net (Apache reverse proxy →
Waitress, supervised), applied 0002 migration, the gate flipped.

Serving pivot (D-17/D-18): the vhost uses `mod_proxy_http` reverse-proxying a persistent Waitress
loopback process — NOT `mod_fcgid`/`flup6`/`.fcgi`. Waitress needs a supervisor because it is a
persistent process; the operator confirms whether maderas has systemd FIRST (the "no systemd" claim
was never verified), then picks the systemd-user unit or the cron `@reboot` branch.

Precedent: mirrors the Phase-177 177-07 operator plan (autonomous:false; maderas has no passwordless sudo).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/178-thin-write-layer-inat-oauth/178-CONTEXT.md
@.planning/phases/178-thin-write-layer-inat-oauth/178-RESEARCH.md
@infra/lib/beeatlas-stack.ts
@docs/runbooks/notes-store-dr.md
@data/roles_allowlist.toml
</context>

<tasks>

<task type="auto">
  <name>Task 1: CDK A-record for api.beeatlas.net + WRITE-04 launch-gate + go-live runbook (mod_proxy_http/Waitress/supervisor)</name>
  <files>infra/lib/beeatlas-stack.ts, docs/runbooks/notes-write-launch-gate.md</files>
  <read_first>
    - infra/lib/beeatlas-stack.ts (find the Route53 zone construct — `netZone`/the beeatlas.net hosted zone — to attach the A-record; never `cdk destroy`, surgical edit only per memory project_cdk_stack_composition)
    - .planning/phases/178-thin-write-layer-inat-oauth/178-RESEARCH.md (§ Code Examples — the exact ARecord(api → 45.79.96.48) snippet; it is a PLAIN A record, not a CloudFront alias)
    - .planning/phases/178-thin-write-layer-inat-oauth/178-CONTEXT.md (§ D-17 topology — Apache mod_proxy_http → Waitress loopback; § D-18 — supervisor branches: systemd-user vs cron @reboot)
    - docs/runbooks/notes-store-dr.md (§ Drill Log — the 177-07 restore PASS row this launch gate references; the store path convention ~/beeatlas-store/notes.db)
  </read_first>
  <action>
    Add a surgical Route53 `ARecord` to infra/lib/beeatlas-stack.ts: recordName `api`, on the
    beeatlas.net hosted zone, target the plain IP 45.79.96.48 (NOT a CloudFront alias). Do not touch
    the siteBucket, CloudFront distribution, OIDC role, or the AuthoritativeBackupBucket — additive
    only (`cdk diff` must be purely additive). Create docs/runbooks/notes-write-launch-gate.md
    encoding BOTH the go-live runbook and the WRITE-04 launch checklist:
    (A) Apache go-live: enable `mod_proxy`, `mod_proxy_http`, `mod_headers`; create the
    api.beeatlas.net vhost with certbot TLS terminating at Apache, `ProxyPreserveHost On`,
    `ProxyPass / http://127.0.0.1:<port>/` + `ProxyPassReverse / http://127.0.0.1:<port>/` (port =
    api.config SERVE_PORT, default 8080), and `RequestHeader set X-Forwarded-Proto "https"` so
    ProxyFix reads the real scheme; deploy the api/ tree; set NOTES_DB_PATH=~/beeatlas-store/notes.db.
    (B) Waitress supervisor (D-18) — FIRST confirm whether maderas has systemd (`systemctl --version`
    / `pidof systemd`); document BOTH branches and record which was used: (b1) systemd present → a
    `~/.config/systemd/user/beeatlas-api.service` `--user` unit running the venv python on api/serve.py
    with `WantedBy=default.target`, then `loginctl enable-linger <user>` + `systemctl --user enable
    --now beeatlas-api`; (b2) no systemd → a crontab `@reboot` entry launching api/serve.py (venv
    python, NOTES_DB_PATH set, output to a log), optionally a `flock`-guarded per-minute keepalive cron
    for crash restart.
    (C) WRITE-04 launch checklist: (1) confirm the 177-07 restore Drill Log shows PASS before any real
    author is added; (2) the `writes_enabled` flag in api/secrets.toml `[launch]` defaults false and
    MUST stay false until the restore is confirmed; (3) go-live order — fill secrets, deploy vhost +
    start Waitress under the supervisor, apply migration 0002, flip `writes_enabled=true`, add the first
    author to roles_allowlist.toml and commit; (4) note that writes are never truly "public" (the
    allowlist gates every write) so the gate = allowlist + the demonstrated restore + the flag.
  </action>
  <acceptance_criteria>
    - beeatlas-stack.ts adds an `api` A-record targeting 45.79.96.48 (plain IP); no other resource changed.
    - `cd infra && npx cdk diff` (or the npm equivalent) reports only the additive A-record (operator verifies at deploy).
    - docs/runbooks/notes-write-launch-gate.md exists and documents: the mod_proxy_http vhost (ProxyPass to 127.0.0.1:<port> + X-Forwarded-Proto), BOTH D-18 supervisor branches, and the writes_enabled procedure referencing the 177-07 restore Drill Log.
    - No mention of mod_fcgid / flup6 / .fcgi in the runbook.
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/rainhead/dev/beeatlas && grep -q "api" infra/lib/beeatlas-stack.ts && grep -q "45.79.96.48" infra/lib/beeatlas-stack.ts && test -f docs/runbooks/notes-write-launch-gate.md && grep -q "ProxyPass" docs/runbooks/notes-write-launch-gate.md && grep -qi "waitress" docs/runbooks/notes-write-launch-gate.md && ! grep -qiE "mod_fcgid|flup|\.fcgi" docs/runbooks/notes-write-launch-gate.md && echo ok</automated>
  </verify>
  <done>A-record + go-live/launch-gate runbook (mod_proxy_http + Waitress + both supervisor branches) in place; ready for operator deploy.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: Operator — deploy api.beeatlas.net (CDK + Apache mod_proxy_http/certbot + Waitress supervisor + secrets + migration)</name>
  <files></files>
  <read_first>
    - docs/runbooks/notes-write-launch-gate.md (Task 1 go-live steps — mod_proxy_http vhost + the chosen D-18 supervisor branch)
    - docs/runbooks/notes-store-dr.md (§ store path = ~/beeatlas-store/notes.db; the maderas env conventions)
    - .planning/phases/178-thin-write-layer-inat-oauth/178-RESEARCH.md (§ Pitfall 5 — set NOTES_DB_PATH to ~/beeatlas-store/notes.db; § Environment Availability — Apache/certbot are operator-domain)
    - .planning/phases/178-thin-write-layer-inat-oauth/178-CONTEXT.md (§ D-17/D-18 — Waitress loopback + mod_proxy_http; confirm systemd first)
    - api/secrets.example.toml (the keys to fill in api/secrets.toml on maderas)
    - api/serve.py (the Waitress entrypoint the supervisor launches)
  </read_first>
  <action>
    Pause for the operator to perform the maderas deploy (interactive sudo — no CLI-only path). Hand
    them the go-live checklist below: `cdk deploy` the additive A-record; enable mod_proxy/mod_proxy_http/
    mod_headers; create the api.beeatlas.net vhost (certbot TLS; ProxyPass/ProxyPassReverse to
    127.0.0.1:<port>; X-Forwarded-Proto https); deploy the api/ tree; set
    NOTES_DB_PATH=~/beeatlas-store/notes.db; fill the real client_secret + signing_key in
    api/secrets.toml (leave writes_enabled=false); confirm whether maderas has systemd and start
    Waitress (api/serve.py) under the matching supervisor branch (systemd-user unit + enable-linger, or
    cron @reboot); run `alembic upgrade head` (→ 0002); and confirm /health over TLS + no traceback on a
    forced error + the Waitress process restarts after a simulated kill (supervisor works). Record which
    supervisor branch was used, and the results, in the SUMMARY + runbook.
  </action>
  <what-built>
    The full api/ service (routes, session, authz, oauth, ProxyFix, generic error handler, Waitress
    serve entrypoint) plus the A-record + runbook are committed and unit-green. What remains is live
    deployment on maderas — interactive sudo work with no CLI-only path: the Apache reverse proxy, TLS,
    and the Waitress supervisor.
  </what-built>
  <how-to-verify>
    1. `cd infra && cdk deploy BeeAtlasStack` — confirm `cdk diff` is purely additive (only the api
       A-record); verify `api.beeatlas.net` resolves to 45.79.96.48.
    2. On maderas: enable mod_proxy/mod_proxy_http/mod_headers; create the api.beeatlas.net vhost with
       certbot TLS, `ProxyPass / http://127.0.0.1:<port>/`, `ProxyPassReverse / http://127.0.0.1:<port>/`,
       and `RequestHeader set X-Forwarded-Proto "https"`. Deploy the api/ tree; set
       NOTES_DB_PATH=~/beeatlas-store/notes.db (NOT the /opt default).
    3. Fill api/secrets.toml [inaturalist_oauth].client_secret and [session].signing_key with the real
       values (client_id + redirect_uri already seeded); leave [launch] writes_enabled = false for now.
    4. Confirm systemd (`systemctl --version` / `pidof systemd`). Start Waitress via api/serve.py under
       the matching branch: systemd-user unit (`loginctl enable-linger`, `systemctl --user enable --now
       beeatlas-api`) OR a cron `@reboot` entry (optionally a flock keepalive). Confirm the process is
       listening on 127.0.0.1:<port> and NOT on 0.0.0.0.
    5. Run `alembic upgrade head` against the live store (→ 0002; confirm `alembic current` == 0002).
    6. `curl https://api.beeatlas.net/health` → `{"status":"ok"}` over TLS (proves Apache → Waitress).
    7. Deliberately trigger an app error (e.g. a malformed /auth/callback) → confirm a generic error,
       NOT a stack trace (app.debug=False + generic error handler proven live; the flup cgitb footgun is
       gone under Waitress).
    8. Simulate a crash (kill the Waitress process) → confirm the supervisor restarts it (systemd) or a
       @reboot/keepalive brings it back — record which branch and the observed behavior.
  </how-to-verify>
  <acceptance_criteria>
    - api.beeatlas.net serves /health over TLS via Apache mod_proxy_http → Waitress on 127.0.0.1:<port>.
    - Waitress runs under the chosen supervisor (systemd-user or cron @reboot per D-18); the branch used is recorded.
    - Migration 0002 applied on the live store (`alembic current` == 0002).
    - Real client_secret + signing_key are set in api/secrets.toml on maderas (never committed).
    - A forced error returns a generic page, not a stack trace (debug=False + generic handler confirmed live).
  </acceptance_criteria>
  <verify>
    <automated>MISSING — operator-only maderas deploy (interactive sudo, no CLI-only path); evidence recorded in the SUMMARY + runbook</automated>
  </verify>
  <resume-signal>Type "deployed" once /health is green over TLS via Waitress, the supervisor branch is recorded, and migration 0002 is applied, or describe the blocker.</resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: WRITE-04 launch gate — confirm restore, flip writes_enabled, add first author</name>
  <files></files>
  <read_first>
    - docs/runbooks/notes-store-dr.md (§ Drill Log — confirm the 177-07 restore row shows PASS)
    - docs/runbooks/notes-write-launch-gate.md (the go-live checklist)
    - data/roles_allowlist.toml (add the first real author login here)
  </read_first>
  <action>
    Pause for the operator to close the WRITE-04 gate: confirm the 177-07 restore Drill Log shows
    PASS, verify POST /api/write-check returns 503 while writes_enabled=false, then set
    writes_enabled=true in api/secrets.toml on maderas and reload/restart the Waitress process, add the
    first real author to data/roles_allowlist.toml and commit, and verify that author's POST
    /api/write-check returns 200. Record the 503→200 transition + the restore confirmation in the
    SUMMARY + runbook.
  </action>
  <what-built>
    The write endpoint refuses writes (503) while `writes_enabled` is false. The gate is the operator
    confirming the already-demonstrated restore before opening writes.
  </what-built>
  <how-to-verify>
    1. Confirm the 177-07 restore Drill Log shows PASS (2026-07-03) — the backup is proven.
    2. While writes_enabled=false, POST /api/write-check as an allowlisted author → confirm 503 (gate closed).
    3. Set api/secrets.toml [launch] writes_enabled = true on maderas and reload/restart the Waitress process.
    4. Add the first real author login to data/roles_allowlist.toml and commit (git history is the audit trail).
    5. POST /api/write-check as that author (from beeatlas.net) → confirm 200 with the server-derived identity.
  </how-to-verify>
  <acceptance_criteria>
    - The 177-07 restore PASS is confirmed before the flag is flipped.
    - writes_enabled=false yields 503; after flipping true (and restarting Waitress), an allowlisted author gets 200.
    - The first real author is committed to roles_allowlist.toml.
  </acceptance_criteria>
  <verify>
    <automated>MISSING — operator gate; the 503→200 transition + restore confirmation are recorded in the SUMMARY + runbook</automated>
  </verify>
  <resume-signal>Type "writes open" once the restore is confirmed, the flag is flipped, and an allowlisted author write returns 200.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| public internet → api.beeatlas.net | TLS-terminated Apache; the only server runtime BeeAtlas adds |
| Apache (TLS term) → Waitress on 127.0.0.1:<port> | reverse-proxy hop; Waitress bound to loopback only, reachable exclusively through Apache |
| operator secrets on maderas | client_secret + signing_key live only in the gitignored on-host secrets.toml |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-178-22 | Information Disclosure | plaintext transport | mitigate | certbot TLS terminates at Apache for api.beeatlas.net; Secure cookie only sent over HTTPS; the Apache→Waitress hop is loopback-local |
| T-178-23 | EoP | writes opened before backup proven | mitigate | writes_enabled stays false until the 177-07 restore PASS is confirmed (WRITE-04) |
| T-178-25 | Spoofing / Tampering | direct-to-Waitress bypass or forged X-Forwarded-* | mitigate | Waitress binds 127.0.0.1 only (unreachable except via Apache); ProxyFix trusts exactly one hop; Apache sets X-Forwarded-Proto (D-17) |
| T-178-15 | Information Disclosure | stack-trace leak on unhandled exception | mitigate | live forced-error check confirms app.debug=False + generic handler (no traceback; the flup cgitb footgun is gone under Waitress) |
| T-178-24 | Tampering | CDK stack destruction | mitigate | surgical additive A-record only; never `cdk destroy` (memory project_cdk_stack_composition) |
</threat_model>

<verification>
api.beeatlas.net serves /health over TLS via Apache mod_proxy_http → Waitress on loopback, supervised
(systemd-user or cron @reboot, recorded); migration 0002 live; a forced error shows no traceback;
writes_enabled gates writes 503→200 only after the 177-07 restore is confirmed; the first author is
committed. Evidence recorded in the SUMMARY + the two runbooks.
</verification>

<success_criteria>
- The write layer is live on maderas at api.beeatlas.net over TLS, served by Waitress behind Apache mod_proxy_http (WRITE-01), supervised per D-18.
- The users-table migration is applied on the live store.
- Public writes are gated on the demonstrated restore, then opened by flipping writes_enabled + adding the first author (WRITE-04).
</success_criteria>

## Artifacts this plan produces
- `infra/lib/beeatlas-stack.ts` → surgical Route53 A-record `api.beeatlas.net → 45.79.96.48`
- `docs/runbooks/notes-write-launch-gate.md` → the mod_proxy_http/Waitress/supervisor go-live runbook + the WRITE-04 launch checklist
- `data/roles_allowlist.toml` → the first real author (operator-committed)
- Live: api.beeatlas.net (Apache mod_proxy_http → Waitress on loopback, supervised + certbot), migration 0002 applied, writes_enabled flipped

<output>
Create `.planning/phases/178-thin-write-layer-inat-oauth/178-08-SUMMARY.md` when done.
</output>
</content>
