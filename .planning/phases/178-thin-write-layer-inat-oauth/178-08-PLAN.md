---
phase: 178-thin-write-layer-inat-oauth
plan: 08
type: execute
wave: 6
depends_on: ["178-06", "178-07"]
files_modified:
  - infra/lib/beeatlas-stack.ts
  - docs/runbooks/notes-write-launch-gate.md
  - data/roles_allowlist.toml
autonomous: false
requirements: [WRITE-01, WRITE-04]
user_setup:
  - service: inaturalist_oauth
    why: "Fill the real client_secret + session signing_key the Flask service loads at runtime (app already registered — D-12/D-13 done)"
    env_vars:
      - name: client_secret
        source: "iNaturalist → Account → Applications → the registered BeeAtlas app → client secret (paste into api/secrets.toml [inaturalist_oauth] on maderas)"
      - name: signing_key
        source: "generate on maderas: python -c \"import secrets; print(secrets.token_urlsafe(64))\" → api/secrets.toml [session]"
    dashboard_config:
      - task: "Confirm the app's redirect URI is exactly https://api.beeatlas.net/auth/callback (D-13 already set)"
        location: "iNaturalist → Account → Applications → BeeAtlas app"
  - service: maderas
    why: "Apache mod_fcgid vhost + certbot TLS for api.beeatlas.net; no passwordless sudo, so this is human-only"
    dashboard_config:
      - task: "Create the api.beeatlas.net Apache vhost (mod_fcgid → api/app.fcgi), obtain a certbot cert, deploy the api/ tree, set NOTES_DB_PATH=~/beeatlas-store/notes.db, run `alembic upgrade head` (→ 0002 users table)"
        location: "maderas host (45.79.96.48)"

must_haves:
  truths:
    - "api.beeatlas.net resolves to maderas (45.79.96.48) over TLS and serves the Flask app via mod_fcgid"
    - "The users-table migration (0002) is applied on the live maderas store"
    - "Public writes are gated: writes_enabled stays false until the operator confirms the demonstrated 177-07 restore, then is flipped true"
  artifacts:
    - path: "infra/lib/beeatlas-stack.ts"
      provides: "surgical Route53 A-record for api.beeatlas.net → 45.79.96.48"
      contains: "api"
    - path: "docs/runbooks/notes-write-launch-gate.md"
      provides: "the WRITE-04 launch checklist referencing the 177-07 restore Drill Log"
  key_links:
    - from: "writes_enabled launch flag"
      to: "docs/runbooks/notes-store-dr.md Drill Log (177-07 restore PASS)"
      via: "operator confirms the restore before flipping the flag"
      pattern: "restore.*PASS"
---

<objective>
Take the write layer live on maderas and encode the WRITE-04 launch gate. Surgical CDK edit adds the
`api.beeatlas.net` A-record (plain IP → 45.79.96.48, NOT a CloudFront alias — D-11, never
`cdk destroy`). The operator then configures the Apache mod_fcgid vhost + certbot TLS, deploys the
`api/` tree, fills the real secrets, and runs the users migration on the live store. Finally the
WRITE-04 gate: `writes_enabled` stays false until the operator confirms the already-demonstrated
177-07 restore (Drill Log PASS 2026-07-03), then flips it and adds the first real author to the
allowlist.

Purpose: WRITE-01 (served on real infra) + WRITE-04 (public writes gated on the proven restore).
Output: CDK A-record, launch-gate runbook, live api.beeatlas.net, applied 0002 migration, the gate flipped.

Precedent: mirrors the Phase-177 177-07 operator plan (autonomous:false; maderas has no passwordless sudo).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/178-thin-write-layer-inat-oauth/178-CONTEXT.md
@.planning/phases/178-thin-write-layer-inat-oauth/178-RESEARCH.md
@infra/lib/beeatlas-stack.ts
@docs/runbooks/notes-store-dr.md
@data/roles_allowlist.toml
</context>

<tasks>

<task type="auto">
  <name>Task 1: CDK A-record for api.beeatlas.net + WRITE-04 launch-gate runbook</name>
  <files>infra/lib/beeatlas-stack.ts, docs/runbooks/notes-write-launch-gate.md</files>
  <read_first>
    - infra/lib/beeatlas-stack.ts (find the Route53 zone construct — `netZone`/the beeatlas.net hosted zone — to attach the A-record; never `cdk destroy`, surgical edit only per memory project_cdk_stack_composition)
    - .planning/phases/178-thin-write-layer-inat-oauth/178-RESEARCH.md (§ Code Examples — the exact ARecord(api → 45.79.96.48) snippet; it is a PLAIN A record, not a CloudFront alias)
    - docs/runbooks/notes-store-dr.md (§ Drill Log — the 177-07 restore PASS row this launch gate references)
  </read_first>
  <action>
    Add a surgical Route53 `ARecord` to infra/lib/beeatlas-stack.ts: recordName `api`, on the
    beeatlas.net hosted zone, target the plain IP 45.79.96.48 (NOT a CloudFront alias). Do not touch
    the siteBucket, CloudFront distribution, OIDC role, or the AuthoritativeBackupBucket — additive
    only (`cdk diff` must be purely additive). Create docs/runbooks/notes-write-launch-gate.md
    encoding the WRITE-04 launch checklist: (1) confirm the 177-07 restore Drill Log shows PASS
    before any real author is added; (2) the `writes_enabled` flag in api/secrets.toml `[launch]`
    defaults false and MUST stay false until the restore is confirmed; (3) the go-live steps — fill
    secrets, deploy vhost, apply migration 0002, flip `writes_enabled=true`, add the first author to
    roles_allowlist.toml and commit; (4) note that writes are never truly "public" (the allowlist
    gates every write) so the gate = allowlist + the demonstrated restore + the flag.
  </action>
  <acceptance_criteria>
    - beeatlas-stack.ts adds an `api` A-record targeting 45.79.96.48 (plain IP); no other resource changed.
    - `cd infra && npx cdk diff` (or `npm run` equivalent) reports only the additive A-record (operator verifies at deploy).
    - docs/runbooks/notes-write-launch-gate.md exists and references the 177-07 restore Drill Log + the writes_enabled procedure.
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/rainhead/dev/beeatlas && grep -q "api" infra/lib/beeatlas-stack.ts && grep -q "45.79.96.48" infra/lib/beeatlas-stack.ts && test -f docs/runbooks/notes-write-launch-gate.md && echo ok</automated>
  </verify>
  <done>A-record + launch-gate runbook in place; ready for operator deploy.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: Operator — deploy api.beeatlas.net (CDK + Apache/mod_fcgid/certbot + secrets + migration)</name>
  <files></files>
  <read_first>
    - docs/runbooks/notes-write-launch-gate.md (Task 1 go-live steps)
    - docs/runbooks/notes-store-dr.md (§ store path = ~/beeatlas-store/notes.db; the maderas env conventions)
    - .planning/phases/178-thin-write-layer-inat-oauth/178-RESEARCH.md (§ Pitfall 5 — set NOTES_DB_PATH to ~/beeatlas-store/notes.db; § Environment Availability — mod_fcgid/certbot are operator-domain)
    - api/secrets.example.toml (the keys to fill in api/secrets.toml on maderas)
  </read_first>
  <action>
    Pause for the operator to perform the maderas deploy (interactive sudo — no CLI-only path). Hand
    them the go-live checklist below: `cdk deploy` the additive A-record, create the mod_fcgid vhost
    + certbot cert for api.beeatlas.net pointing at api/app.fcgi, deploy the api/ tree, set
    NOTES_DB_PATH=~/beeatlas-store/notes.db, fill the real client_secret + signing_key in
    api/secrets.toml (leave writes_enabled=false), run `alembic upgrade head` (→ 0002), and confirm
    /health over TLS + no traceback on a forced error. Record the results in the SUMMARY + runbook.
  </action>
  <what-built>
    The full api/ service (routes, session, authz, oauth, .fcgi wrapper) plus the A-record + runbook
    are committed and unit-green. What remains is live deployment on maderas — interactive sudo work
    with no CLI-only path.
  </what-built>
  <how-to-verify>
    1. `cd infra && cdk deploy BeeAtlasStack` — confirm `cdk diff` is purely additive (only the api
       A-record); verify `api.beeatlas.net` resolves to 45.79.96.48.
    2. On maderas: create the Apache mod_fcgid vhost for api.beeatlas.net pointing at api/app.fcgi;
       obtain a certbot TLS cert; deploy the api/ tree; set NOTES_DB_PATH=~/beeatlas-store/notes.db in
       the vhost env (SetEnv/PassEnv or the wrapper), NOT the /opt default.
    3. Fill api/secrets.toml [inaturalist_oauth].client_secret and [session].signing_key with the real
       values (client_id + redirect_uri already seeded); leave [launch] writes_enabled = false for now.
    4. Run `alembic upgrade head` against the live store (→ 0002; confirm `alembic current` == 0002).
    5. `curl https://api.beeatlas.net/health` → `{"status":"ok"}` over TLS.
    6. Deliberately trigger an app error (e.g. a malformed /auth/callback) → confirm a generic error,
       NOT a cgitb traceback (Pitfall 3 — debug=False proven live).
  </how-to-verify>
  <acceptance_criteria>
    - api.beeatlas.net serves /health over TLS via mod_fcgid.
    - Migration 0002 applied on the live store (`alembic current` == 0002).
    - Real client_secret + signing_key are set in api/secrets.toml on maderas (never committed).
    - A forced error returns a generic page, not a traceback (debug=False confirmed live).
  </acceptance_criteria>
  <verify>
    <automated>MISSING — operator-only maderas deploy (interactive sudo, no CLI-only path); evidence recorded in the SUMMARY + runbook</automated>
  </verify>
  <resume-signal>Type "deployed" once /health is green over TLS and migration 0002 is applied, or describe the blocker.</resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: WRITE-04 launch gate — confirm restore, flip writes_enabled, add first author</name>
  <files></files>
  <read_first>
    - docs/runbooks/notes-store-dr.md (§ Drill Log — confirm the 177-07 restore row shows PASS)
    - docs/runbooks/notes-write-launch-gate.md (the go-live checklist)
    - data/roles_allowlist.toml (add the first real author login here)
  </read_first>
  <action>
    Pause for the operator to close the WRITE-04 gate: confirm the 177-07 restore Drill Log shows
    PASS, verify POST /api/write-check returns 503 while writes_enabled=false, then set
    writes_enabled=true in api/secrets.toml on maderas and reload the app, add the first real author
    to data/roles_allowlist.toml and commit, and verify that author's POST /api/write-check returns
    200. Record the 503→200 transition + the restore confirmation in the SUMMARY + runbook.
  </action>
  <what-built>
    The write endpoint refuses writes (503) while `writes_enabled` is false. The gate is the operator
    confirming the already-demonstrated restore before opening writes.
  </what-built>
  <how-to-verify>
    1. Confirm the 177-07 restore Drill Log shows PASS (2026-07-03) — the backup is proven.
    2. While writes_enabled=false, POST /api/write-check as an allowlisted author → confirm 503 (gate closed).
    3. Set api/secrets.toml [launch] writes_enabled = true on maderas and reload the app.
    4. Add the first real author login to data/roles_allowlist.toml and commit (git history is the audit trail).
    5. POST /api/write-check as that author (from beeatlas.net) → confirm 200 with the server-derived identity.
  </how-to-verify>
  <acceptance_criteria>
    - The 177-07 restore PASS is confirmed before the flag is flipped.
    - writes_enabled=false yields 503; after flipping true, an allowlisted author gets 200.
    - The first real author is committed to roles_allowlist.toml.
  </acceptance_criteria>
  <verify>
    <automated>MISSING — operator gate; the 503→200 transition + restore confirmation are recorded in the SUMMARY + runbook</automated>
  </verify>
  <resume-signal>Type "writes open" once the restore is confirmed, the flag is flipped, and an allowlisted author write returns 200.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| public internet → api.beeatlas.net | TLS-terminated Apache/mod_fcgid; the only server runtime BeeAtlas adds |
| operator secrets on maderas | client_secret + signing_key live only in the gitignored on-host secrets.toml |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-178-22 | Information Disclosure | plaintext transport | mitigate | certbot TLS on api.beeatlas.net; Secure cookie only sent over HTTPS |
| T-178-23 | EoP | writes opened before backup proven | mitigate | writes_enabled stays false until the 177-07 restore PASS is confirmed (WRITE-04) |
| T-178-15 | Information Disclosure | flup6 cgitb traceback leak | mitigate | live forced-error check confirms debug=False (Pitfall 3) |
| T-178-24 | Tampering | CDK stack destruction | mitigate | surgical additive A-record only; never `cdk destroy` (memory project_cdk_stack_composition) |
</threat_model>

<verification>
api.beeatlas.net serves /health over TLS via mod_fcgid; migration 0002 live; a forced error shows no
traceback; writes_enabled gates writes 503→200 only after the 177-07 restore is confirmed; the first
author is committed. Evidence recorded in the SUMMARY + the two runbooks.
</verification>

<success_criteria>
- The write layer is live on maderas at api.beeatlas.net over TLS (WRITE-01).
- The users-table migration is applied on the live store.
- Public writes are gated on the demonstrated restore, then opened by flipping writes_enabled + adding the first author (WRITE-04).
</success_criteria>

## Artifacts this plan produces
- `infra/lib/beeatlas-stack.ts` → surgical Route53 A-record `api.beeatlas.net → 45.79.96.48`
- `docs/runbooks/notes-write-launch-gate.md` → the WRITE-04 launch checklist
- `data/roles_allowlist.toml` → the first real author (operator-committed)
- Live: api.beeatlas.net (mod_fcgid + certbot), migration 0002 applied, writes_enabled flipped

<output>
Create `.planning/phases/178-thin-write-layer-inat-oauth/178-08-SUMMARY.md` when done.
</output>
