---
phase: 171-per-collector-event-stream
plan: 02
type: execute
wave: 2
depends_on: ["171-01"]
files_modified:
  - _data/collectors.js
  - _pages/collector-detail.njk
  - _pages/collector-events-page.njk
  - src/styles/places.css
  - src/tests/page-scaffold.test.ts
  - src/tests/data-collectors.test.ts
autonomous: true
requirements: [STREAM-01, STREAM-02, STREAM-03]
must_haves:
  truths:
    - "/collectors/{login}/ renders a 'Collection history' feed (first page) with reverse-chron event rows"
    - "/collectors/{login}/page/N/ static sub-pages are generated for high-volume collectors with prev/next nav"
    - "Species names link to /species/{slug}/ for species and genus tiers; undetermined / non-bee render as plain text"
    - "The 16 sample-host-only collectors show the empty state 'No specimen events recorded yet.'"
    - "Determiner names render as 'by {name}', omitted entirely when blank; the collector page stays JS-free"
  artifacts:
    - path: "_pages/collector-events-page.njk"
      provides: "Paginated sub-page template (pages 2+)"
      contains: "collectorEventPages"
    - path: "_pages/collector-detail.njk"
      provides: "Extended with first_page_events feed + empty state + Older-events link"
      contains: "event-feed"
    - path: "_data/collectors.js"
      provides: "Loader extended with collectorEventPages + HMR dev guard"
      exports: ["collectorsArray", "collectorEventPages"]
    - path: "src/styles/places.css"
      provides: "Event-feed + pagination CSS per UI-SPEC"
      contains: ".event-feed"
  key_links:
    - from: "_data/collectors.js"
      to: "public/data/collector_event_pages.json"
      via: "readFileSync guarded by ELEVENTY_RUN_MODE === 'build'"
      pattern: "ELEVENTY_RUN_MODE"
    - from: "_pages/collector-events-page.njk"
      to: "collectors.collectorEventPages"
      via: "pagination size:1, permalink /collectors/{login}/page/{N}/"
      pattern: "collectorEventPages"
    - from: "_pages/collector-detail.njk"
      to: "/species/{slug}/"
      via: "rank-aware event-row taxon link"
      pattern: "/species/"
---

<objective>
Render the event feed on the static collector page (STREAM-01/02/03): extend the
`_data/collectors.js` loader to expose the sub-page array (HMR-guarded), extend
`_pages/collector-detail.njk` to render the first page of events + empty state, add
`_pages/collector-events-page.njk` to generate `/collectors/{login}/page/N/` sub-pages
via Eleventy `size:1` 2D pagination, and append the event-feed CSS from the approved
UI-SPEC.

Purpose: turn the Plan 01 JSON substrate into the bookmarkable, JS-free,
reverse-chronological Collected→Identified timeline that is the milestone's "tighten
learning cycles / togetherness" surface.

Output:
- `_data/collectors.js` — exports `{ collectorsArray, collectorEventPages }`; the 24 MB
  sub-page file loads only when `ELEVENTY_RUN_MODE === 'build'` (HMR stays fast in serve;
  Pitfall 6 / RESEARCH Open Q1).
- `_pages/collector-detail.njk` — first_page_events feed + empty state + "Older events →" link.
- `_pages/collector-events-page.njk` — new sub-page template with prev/next nav.
- `src/styles/places.css` — `.event-feed`, `.event-row`, `.event-type--*`, `.event-pagination` (verbatim from UI-SPEC).

This plan depends on Plan 01: it consumes the committed `collectors.json` (first_page_events)
and `collector_event_pages.json` it produced.

Locked decisions implemented (render): D-PAGE-01 (Eleventy `size:1` sub-pages, zero JS), D-FEED-01/D-SORT (flat reverse-chron rows in array order), D-CARD-01 (determiner `by {name}` / omit when blank), D-CARD-02 (species + genus taxon links; undetermined → text), D-CARD-03 (no specimen/place links), D-EMPTY (empty-state copy), D-EVENT-02 (awaiting-ID row).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/171-per-collector-event-stream/171-CONTEXT.md
@.planning/phases/171-per-collector-event-stream/171-PATTERNS.md
@.planning/phases/171-per-collector-event-stream/171-UI-SPEC.md
@.planning/phases/171-per-collector-event-stream/171-01-SUMMARY.md

# Analogs (source of truth — mirror these):
@_pages/collector-detail.njk
@_data/collectors.js
@src/styles/places.css
@src/tests/page-scaffold.test.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend _data/collectors.js loader with collectorEventPages + HMR dev guard</name>
  <files>_data/collectors.js, src/tests/data-collectors.test.ts</files>
  <read_first>
    - _data/collectors.js (the whole file — the readFileSync/repoRoot pattern + the "no parquet" contract comment)
    - src/tests/data-collectors.test.ts (the `does NOT read parquet` regression guard at line ~44; the Phase 171 block added in Plan 01 reads artifacts directly — this task adds the loader-contract assertion)
    - .planning/phases/171-per-collector-event-stream/171-PATTERNS.md §"_data/collectors.js" (the verified extended-loader shape)
    - .planning/phases/171-per-collector-event-stream/171-RESEARCH.md §"Q2 / Pitfall 6" + §"Open Questions 1" (the 24 MB HMR guard rationale)
  </read_first>
  <behavior>
    - Loader default export gains a `collectorEventPages` property alongside `collectorsArray`.
    - `collector_event_pages.json` (24 MB) is read ONLY when `process.env.ELEVENTY_RUN_MODE === 'build'`; in serve/watch/vitest it is `[]` (HMR sub-100 ms; Pitfall 6).
    - The loader still reads no parquet (existing regression guard must keep passing).
  </behavior>
  <action>
    Extend `_data/collectors.js`: keep the existing `collectorsArray` read of `public/data/collectors.json`
    unchanged. Add `const collectorEventPages = process.env.ELEVENTY_RUN_MODE === 'build' ?
    JSON.parse(readFileSync(join(repoRoot, 'public/data/collector_event_pages.json'), 'utf8')) : [];` and change
    the default export to `{ collectorsArray, collectorEventPages }`. Update the header comment to document the
    contract (exports both arrays) and the ELEVENTY_RUN_MODE guard (Eleventy 3.x sets ELEVENTY_RUN_MODE to
    'serve' | 'watch' | 'build'; only 'build' — i.e. `npm run build` / CI deploy — loads the 24 MB sub-page file,
    so HMR in `npm run dev` and the vitest import both see `[]`). Do NOT add any parquet read.
    In `src/tests/data-collectors.test.ts`, add a loader-contract test: import the loader and assert
    `Array.isArray(collectors.collectorEventPages)` is true (it will be `[]` under vitest since ELEVENTY_RUN_MODE
    is unset — assert Array-ness only, NOT non-empty; the non-empty assertion lives in the Plan 01 artifact-read
    block). Keep / re-assert the `does NOT read parquet` guard over the extended file source.
  </action>
  <acceptance_criteria>
    - `grep -c "ELEVENTY_RUN_MODE" _data/collectors.js` >= 1 and the only `collector_event_pages.json` read is inside that conditional.
    - `grep -c "collectorEventPages" _data/collectors.js` >= 1 and the default export object names both `collectorsArray` and `collectorEventPages`.
    - `grep -ic "parquet" _data/collectors.js` counts only comment lines (no `readFileSync(... parquet ...)`); the existing `does NOT read parquet` vitest assertion stays green.
    - `npm test -- data-collectors` exits 0 (loader-contract Array.isArray test + the Plan 01 artifact-shape tests all green).
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/rainhead/dev/beeatlas && npm test -- data-collectors 2>&1 | tail -8</automated>
  </verify>
  <done>The loader exposes collectorEventPages, gated so HMR stays fast; the no-parquet contract and the artifact-shape tests are green.</done>
</task>

<task type="auto">
  <name>Task 2: Event-feed templates (collector-detail extension + collector-events-page) + UI-SPEC CSS + scaffold test</name>
  <files>_pages/collector-detail.njk, _pages/collector-events-page.njk, src/styles/places.css, src/tests/page-scaffold.test.ts</files>
  <read_first>
    - _pages/collector-detail.njk (front matter + article wrapper + the "View on the atlas →" link after which the feed section is appended)
    - .planning/phases/171-per-collector-event-stream/171-UI-SPEC.md §"Component Inventory" (the 4 event-row variants, label rules, taxon-link rules, date format, empty state, pagination variants, copywriting contract) + §"CSS Additions" (the verbatim CSS block)
    - .planning/phases/171-per-collector-event-stream/171-PATTERNS.md §"_pages/collector-events-page.njk" + §"_pages/collector-detail.njk" + §"src/styles/places.css" (front matter, event-row macro, prev/next nav, CSS placement)
    - src/tests/page-scaffold.test.ts (the front-matter regex pattern to mirror for the new template)
    - src/styles/places.css (existing tokens + .places-list / .metadata patterns the new CSS mirrors)
  </read_first>
  <action>
    Extend `_pages/collector-detail.njk`: after the existing `{%- endif -%}` that closes the atlas-link block
    (before `</article>`), add a `<section class="event-feed-section"><h2>Collection history</h2>…</section>`.
    When `collector.total_event_count > 0`, render `<ol class="event-feed" reversed>` iterating
    `collector.first_page_events`, and — when `collector.total_event_pages > 1` — a
    `<nav class="event-pagination" aria-label="Event history pages">` containing only
    `<a href="/collectors/{{ collector.login | urlencode }}/page/2/">Older events →</a>`. When
    `total_event_count == 0`, render the empty state `<p class="metadata">No specimen events recorded yet.</p>`
    (D-EMPTY — the 16 sample-host-only collectors). Implement the event row exactly per UI-SPEC §"Component 2"
    (the four variants): a `<time class="event-date" datetime="{{ event.event_date }}">` column; an
    `event-type` span whose modifier is `--collected` (Collected), `--identified` (Identified with
    `event.is_current` true), or `--reidentified` (Identified with `event.is_current` false) and whose label text
    is "Collected" / "Identified" / "Re-identified"; an `event-taxon` span that renders
    `<a href="/species/{{ event.species_slug }}/">{{ event.species_name }}</a>` when
    `event.species_slug` is set AND NOT `event.is_pending`, else plain `{{ event.species_name }}`; an
    `event-determiner` span `by {{ event.determiner }}` ONLY when `event.determiner` is truthy; and an
    `event-pending` "awaiting ID" span only when `event.is_pending`. Rely on Nunjucks AUTO-ESCAPING for
    `event.species_name`, `event.determiner`, `event.species_slug` — do NOT apply `| safe` to any of them
    (T-171-01). Factor the row markup so the same markup is reused in the sub-page template (a `{% macro %}` in a
    shared partial, or duplicated inline — implementer's choice; if duplicated, keep them byte-identical).

    Create `_pages/collector-events-page.njk` mirroring `collector-detail.njk`'s front matter but paginating
    `collectors.collectorEventPages` with `size: 1`, `alias: evpage`, permalink
    `"/collectors/{{ evpage.login | urlencode }}/page/{{ evpage.page_num }}/index.html"`, `layout: default.njk`,
    and an `eleventyComputed.title`. Body: `<link rel="stylesheet" href="/src/styles/places.css">` +
    `<article class="places-page place-detail">`, an `<h1>{{ evpage.login }}</h1>`, then the same
    `event-feed-section` / `<ol class="event-feed" reversed>` over `evpage.events`, followed by the full
    `event-pagination` nav: a "← Newer events" link pointing to `/collectors/{{ evpage.login | urlencode }}/`
    when `evpage.page_num == 2` else `/page/{{ evpage.page_num - 1 }}/`; a `<span class="page-indicator">Page
    {{ evpage.page_num }} of {{ evpage.total_pages }}</span>`; and an "Older events →" link to
    `/page/{{ evpage.page_num + 1 }}/` only when `evpage.page_num < evpage.total_pages` (Nunjucks integer
    arithmetic in `{{ }}` is supported — RESEARCH A2). Use exactly the UI-SPEC copywriting strings.

    Append the verbatim `src/styles/places.css` block from UI-SPEC §"CSS Additions" (`.event-feed-section`,
    `.event-feed`, `.event-row`, `.event-date`, `.event-type` + the three `--collected/--identified/--reidentified`
    color rules, `.event-taxon`, `.event-determiner`, `.event-pending`, `.event-pagination`, `.page-indicator`,
    the `a` + `a:focus-visible`) after the existing content — using the existing `var(--border…)` /
    `var(--text-muted…)` / `var(--accent…)` tokens, no new color literals.

    Extend `src/tests/page-scaffold.test.ts` with a describe block for `_pages/collector-events-page.njk`
    asserting (readFileSync + regex, mirroring the existing species.njk tests) that it declares
    `layout: default.njk`, paginates `collectors.collectorEventPages` with `size: 1`, and has the permalink
    `/collectors/{{ evpage.login | urlencode }}/page/{{ evpage.page_num }}/index.html`. Also assert
    `collector-detail.njk` now contains `class="event-feed"` (feed section wired) and that NEITHER template
    contains `<script` (JS-free invariant — Pitfall 5) nor `| safe` (T-171-01 auto-escaping).
  </action>
  <acceptance_criteria>
    - `_pages/collector-events-page.njk` exists; `grep -c "collectorEventPages" _pages/collector-events-page.njk` >= 1 and `grep -c "page/{{ evpage.page_num }}/index.html" _pages/collector-events-page.njk` >= 1.
    - `grep -c "event-feed" _pages/collector-detail.njk` >= 1 and `grep -c "No specimen events recorded yet" _pages/collector-detail.njk` >= 1 (empty state).
    - `grep -c "event-type--reidentified" src/styles/places.css` >= 1 and `grep -c ".event-pagination" src/styles/places.css` >= 1 (UI-SPEC CSS appended).
    - `grep -c "| safe" _pages/collector-detail.njk _pages/collector-events-page.njk` == 0 and `grep -ci "<script" _pages/collector-detail.njk _pages/collector-events-page.njk` == 0 (auto-escaping + JS-free).
    - `npm test -- page-scaffold` exits 0 (new template front matter + feed wiring assertions green).
    - `npm test` exits 0 (full frontend suite; no regressions in collector/scaffold/data tests).
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/rainhead/dev/beeatlas && npm test -- page-scaffold 2>&1 | tail -6 && npm test 2>&1 | tail -8</automated>
  </verify>
  <done>The collector page renders the first-page feed (or empty state) and the sub-page template generates /collectors/{login}/page/N/ with prev/next nav and rank-aware taxon links, styled per the approved UI-SPEC; scaffold + full vitest suite green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| exported event strings → rendered HTML | determiner + species names (Ecdysis/iNat origin) are interpolated into Nunjucks templates served as static HTML |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-171-01 | Tampering (stored XSS) | `event.species_name` / `event.determiner` / `event.species_slug` in the two `.njk` templates | mitigate | Rely on Nunjucks/Eleventy default auto-escaping; NEVER apply `\| safe` / `\| escape(false)` to determiner or taxon strings. Asserted by the scaffold grep (`\| safe` count == 0). |
| T-171-02 | Information disclosure | rendered names + iNat logins | accept | Already-public iNaturalist/Ecdysis data; no new PII (D-CARD-03 excludes specimen + place context). Operator decision 2026-06-24: public pages over public data. |
| T-171-SC | Tampering (supply chain) | npm/pip installs | accept (N/A) | No new packages (RESEARCH); no install tasks → no legitimacy checkpoint. |

No high-severity threats; the page is static, JS-free, no user input, no new network calls.
</threat_model>

<verification>
- `npm test` — full frontend suite green (data-collectors loader + artifact tests, page-scaffold, no regressions; feedback_run_tests_before_push).
- `cd data && uv run pytest -m "not integration"` — still green (no data files changed in this plan; run to confirm no cross-impact).
- `grep -rn "| safe" _pages/collector-detail.njk _pages/collector-events-page.njk` returns nothing (auto-escaping enforced — T-171-01).
- `grep -rin "<script" _pages/collector-detail.njk _pages/collector-events-page.njk` returns nothing (JS-free invariant — Pitfall 5).
</verification>

<success_criteria>
- `_data/collectors.js` exports `{ collectorsArray, collectorEventPages }` with the ELEVENTY_RUN_MODE='build' guard; no parquet read.
- `collector-detail.njk` renders the first-page feed (reverse-chron rows, rank-aware links, determiner, awaiting-ID) or the empty state, plus an "Older events →" link when more pages exist.
- `collector-events-page.njk` paginates the sub-page array `size:1` to `/collectors/{login}/page/N/` with correct prev/next nav and page indicator.
- Event-feed CSS appended verbatim from the approved UI-SPEC; no new color literals.
- `npm test` green.
</success_criteria>

<output>
Create `.planning/phases/171-per-collector-event-stream/171-02-SUMMARY.md` when done.
</output>

## Artifacts this phase produces (Plan 02)

| Kind | Symbol / path | Notes |
|------|---------------|-------|
| Loader export | `collectorEventPages` (in `_data/collectors.js` default export) | new, HMR-guarded |
| Template | `_pages/collector-events-page.njk` | NEW; permalink `/collectors/{login}/page/{N}/` |
| Template | `_pages/collector-detail.njk` event-feed section | extended |
| Permalink | `/collectors/{login}/page/{N}/index.html` | sub-pages |
| CSS classes | `.event-feed-section`, `.event-feed`, `.event-row`, `.event-row--pending`, `.event-row--reidentified`, `.event-date`, `.event-type` (+`--collected`/`--identified`/`--reidentified`), `.event-taxon`, `.event-determiner`, `.event-pending`, `.event-pagination`, `.page-indicator` | in `src/styles/places.css` |
| Test (vitest) | `src/tests/page-scaffold.test.ts` collector-events-page block | extended |
| Test (vitest) | `src/tests/data-collectors.test.ts` loader-contract assertion | extended |
