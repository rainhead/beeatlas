---
phase: 171
slug: per-collector-event-stream
status: draft
nyquist_compliant: false
wave_0_complete: false
created: 2026-06-27
---

# Phase 171 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution.
> Source: 171-RESEARCH.md §"Validation Architecture". This phase is a
> data-export + static-frontend phase (no dbt contract change, no SPA type
> change); the gate is `npm test` (Vitest) + the Python export's golden-fixture
> pytest, mirroring Phase 169.

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | Vitest (frontend, existing) + pytest (data export, existing) |
| **Config file** | `vitest.config.ts` (existing); `data/` pytest (existing) |
| **Quick run command** | `npm test` |
| **Full suite command** | `npm test` && `cd data && uv run pytest -m "not integration"` |
| **Estimated runtime** | ~30–60 seconds |

---

## Sampling Rate

- **After every task commit:** Run `npm test` (and `cd data && uv run pytest` when `data/` changed)
- **After every plan wave:** Run the full suite
- **Before `/gsd-verify-work`:** Full suite must be green
- **Max feedback latency:** ~60 seconds

> NOTE (per memory `feedback_run_tests_before_push`): because this phase touches
> BOTH `data/` (new export step) and frontend (`_data`/`_pages`/tests), the gate
> MUST run `npm test` AND `cd data && uv run pytest -m "not integration"` — do not
> rely on a single-language regression gate.

---

## Per-Task Verification Map

| Req ID | Behavior | Test Type | Automated Command | File Exists |
|--------|----------|-----------|-------------------|-------------|
| STREAM-01 | each `collectorsArray` entry carries a `first_page_events` array | unit | `npm test -- data-collectors` | ✅ extend |
| STREAM-01 | `first_page_events` items have required shape (event_type, species_name, sort_ts, determiner, is_current, species_url-or-null) | unit | `npm test -- data-collectors` | ❌ Wave 0 |
| STREAM-01 | new templates render in scaffold-check | smoke | `npm test -- page-scaffold` | ✅ add new templates |
| STREAM-01 | export golden-fixture: events reverse-chron, Identified=modified / Collected=event_date | unit (pytest) | `cd data && uv run pytest -k collectors_events` | ❌ Wave 0 |
| STREAM-02 | `waba_specimen` rows appear as Collected events ("awaiting ID"); catalogued specimen is one continuous row (no delete+create) | unit (pytest) | `cd data && uv run pytest -k collectors_events` | ❌ Wave 0 |
| STREAM-03 | `collector_event_pages.json` is a non-empty Array; every entry has `login`, `page_num ≥ 2`, `total_pages`, `events` | unit | `npm test -- data-collectors` | ❌ Wave 0 |
| STREAM-03 | per-page event count ≤ chunk size (bounded DOM) | unit (pytest) | `cd data && uv run pytest -k collectors_events` | ❌ Wave 0 |

*Status: ⬜ pending · ✅ green · ❌ red · ⚠️ flaky*

---

## Wave 0 Requirements

- [ ] `src/tests/data-collectors.test.ts` — extend (or create) with STREAM-01/02/03 shape + paging assertions above
- [ ] `data/tests/` — golden-fixture pytest for the new events export (reverse-chron order, modified-vs-event_date timestamping, waba_specimen awaiting-ID, chunk bound), mirroring `collectors_export` test pattern
- [ ] `public/data/collector_event_pages.json` + extended `collectors.json` — generated by the new export step; must exist for the frontend tests to pass (run `cd data && uv run python <new_export>.py` in Wave 0)

---

## Manual-Only Verifications

| Behavior | Requirement | Why Manual | Test Instructions |
|----------|-------------|------------|-------------------|
| Feed reads as a coherent reverse-chron timeline on a real collector page (desktop + mobile) | STREAM-01 | Visual/UX — exact timeline rendering is UI-SPEC-driven | Load `/collectors/{login}/` for a high-volume collector (e.g. top collector ~14.9k events) + a small one + a sample-host-only one (empty state); page through to `/page/2/`; verify reverse-chron order, determiner names, species links, awaiting-ID rows |

*Per memory `feedback_uat_ui_phases`: this phase is `UI hint: yes` — verification steps must NOT auto-advance past UAT in yolo mode (`auto_advance: false` on UAT/verify tasks).*

---

## Validation Sign-Off

- [ ] All tasks have automated verify or Wave 0 dependencies
- [ ] Sampling continuity: no 3 consecutive tasks without automated verify
- [ ] Wave 0 covers all MISSING references
- [ ] No watch-mode flags
- [ ] Feedback latency < 60s
- [ ] `nyquist_compliant: true` set in frontmatter

**Approval:** pending
