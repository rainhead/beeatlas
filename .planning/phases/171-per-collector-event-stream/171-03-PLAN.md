---
phase: 171-per-collector-event-stream
plan: 03
type: execute
wave: 3
depends_on: ["171-01", "171-02"]
files_modified:
  - .planning/phases/171-per-collector-event-stream/171-HUMAN-UAT.md
autonomous: false
requirements: [STREAM-01, STREAM-02, STREAM-03]
must_haves:
  truths:
    - "npm test is green AND cd data && uv run pytest -m 'not integration' is green (both languages — this phase touched data/ and frontend)"
    - "Eleventy build mode generates /collectors/{login}/page/2/index.html for a high-volume collector (sub-pages ship)"
    - "Human UAT confirms the feed reads as a coherent reverse-chron Collected→Identified timeline on desktop + mobile, including pagination, determiner names, rank-aware species links, and the awaiting-ID + empty states"
  artifacts:
    - path: ".planning/phases/171-per-collector-event-stream/171-HUMAN-UAT.md"
      provides: "Operator UAT checklist (high-volume, small, sample-host-only collectors + pagination)"
  key_links:
    - from: "deploy.yml Eleventy build"
      to: "public/data/collector_event_pages.json (committed)"
      via: "ELEVENTY_RUN_MODE='build' loader → size:1 pagination → sub-page HTML"
      pattern: "collector_event_pages"
---

<objective>
Close Phase 171: prove the event feed is correct end-to-end and ships from a clean
checkout, then hand the visual/UX confirmation to the operator.

Purpose: this is a `UI hint: yes` phase touching BOTH `data/` (new export) and the
frontend, so the gate runs BOTH test suites (feedback_run_tests_before_push) and must
NOT auto-advance past human UAT (feedback_uat_ui_phases). It also closes the CI-surface
risk: the collector sub-pages are generated by the deploy.yml Eleventy build from the
COMMITTED `collector_event_pages.json`, so "does CI build on a clean checkout?" is an
explicit acceptance criterion (feedback_ci_is_verification_surface).

Output:
- A full-suite + build-mode regression check (automated).
- `171-HUMAN-UAT.md` — the operator checklist.
- A blocking human-verify checkpoint (autonomous: false, auto_advance: false).

Locked decisions verified: D-EVENT-01/02, D-FEED-01/02, D-SORT, D-PAGE-01, D-CARD-01/02/03, D-EMPTY — confirmed end-to-end via the operator UAT scenarios.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/171-per-collector-event-stream/171-CONTEXT.md
@.planning/phases/171-per-collector-event-stream/171-VALIDATION.md
@.planning/phases/171-per-collector-event-stream/171-UI-SPEC.md
@.planning/phases/171-per-collector-event-stream/171-01-SUMMARY.md
@.planning/phases/171-per-collector-event-stream/171-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Full-suite regression + clean-checkout build verification</name>
  <files>none — verification only (runs npm test + pytest; inspects Eleventy build output)</files>
  <read_first>
    - .planning/phases/171-per-collector-event-stream/171-VALIDATION.md (the full-suite gate + manual-only verification list)
    - .github/workflows/deploy.yml (the "Fetch build-time data from S3" step + npm test + npm run build job — the clean-checkout CI path)
    - package.json scripts (`build` = validate-species → validate-db → typecheck → eleventy → validate-bundle-size; `dev` = eleventy --serve)
    - _data/collectors.js (the ELEVENTY_RUN_MODE='build' guard whose 'build' branch must fire during the real build)
  </read_first>
  <action>
    Run BOTH suites and confirm green: `npm test` and `cd data && uv run pytest -m "not integration"`
    (this phase changed `data/` and the frontend; the single-language regression gate is insufficient —
    feedback_run_tests_before_push). Confirm the committed artifacts are in place and tracked:
    `git status --short public/data/collectors.json public/data/collector_event_pages.json` shows them staged/committed
    and `git check-ignore public/data/collector_event_pages.json` exits non-zero (the allowlist works).
    Verify sub-page generation in BUILD mode (the loader's 24 MB branch only fires when ELEVENTY_RUN_MODE='build',
    which Eleventy sets for a non-serve invocation): run the Eleventy build step (e.g. `npx @11ty/eleventy` — it sets
    ELEVENTY_RUN_MODE='build' itself; if the local full `npm run build` is blocked by stale S3-fetched data such as a
    missing species.json/manifest.json, run the Eleventy step in isolation and note that the full validate-db gate is
    exercised by CI). Assert that the build output contains a high-volume collector's sub-page —
    `_site` (or the Eleventy output dir) has `collectors/<a-collector-with-total_event_pages>1>/page/2/index.html`
    and that it contains an `event-row`. Record in the SUMMARY: the actual `collector_event_pages.json` byte size,
    the number of collectors with sub-pages, and the max page count. Because the deploy.yml build runs on a clean
    checkout reading the COMMITTED JSON, note explicitly that CI is the clean-checkout gate and that no nightly/S3
    step is required for collector pages (unlike the contract phases 167/168/170).
  </action>
  <acceptance_criteria>
    - `npm test` exits 0 (full frontend suite).
    - `cd data && uv run pytest -m "not integration"` exits 0 (full fast data suite).
    - `git check-ignore public/data/collector_event_pages.json` exits non-zero (committed, not ignored).
    - A build-mode Eleventy run produces at least one `collectors/<login>/page/2/index.html` containing `event-row` (sub-pages ship).
    - No `<script` tag appears in any generated `collectors/**/index.html` for the feed (JS-free invariant).
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/rainhead/dev/beeatlas && npm test 2>&1 | tail -6 && cd /Users/rainhead/dev/beeatlas/data && uv run pytest -m "not integration" 2>&1 | tail -6</automated>
  </verify>
  <done>Both suites green; the committed sub-page artifact generates real /page/N/ HTML in build mode; CI clean-checkout path confirmed.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Operator UAT — event feed reads as a coherent reverse-chron timeline</name>
  <files>.planning/phases/171-per-collector-event-stream/171-HUMAN-UAT.md</files>
  <read_first>
    - .planning/phases/171-per-collector-event-stream/171-UI-SPEC.md (the visual/copy contract the operator verifies against)
    - .planning/phases/171-per-collector-event-stream/171-VALIDATION.md §"Manual-Only Verifications"
    - project memory project_uat_playwright_sidebar (headless UAT option) — though collector pages are plain static routes, so a browser visit to `/collectors/{login}/` suffices
  </read_first>
  <action>
    Write `.planning/phases/171-per-collector-event-stream/171-HUMAN-UAT.md` as a checklist covering, against
    `npm run dev` (or a built preview): (1) a HIGH-VOLUME collector (the top collector, ~14.9k events per RESEARCH —
    confirm the login from collectors.json `total_event_count`) — feed renders, events are newest-first, paging to
    `/page/2/` works, "Older events →" / "← Newer events" / "Page N of M" all resolve, the last page omits the
    Older link; (2) a SMALL collector (single page, no pagination nav); (3) a SAMPLE-HOST-ONLY collector
    (total_event_count == 0) — the empty state "No specimen events recorded yet." shows and no broken feed;
    (4) row correctness — a re-determined specimen shows its arc (e.g. genus "Re-identified" above the current
    species "Identified"), determiner names render as "by {name}" (and blank determiners show no orphan "by"),
    species links land on real `/species/{Genus}/{epithet}/` AND a genus-only determination links to
    `/species/{Genus}/`, "undetermined" rows render as plain text (no broken link), and a waba_specimen row shows
    "Collected … awaiting ID"; (5) mobile width — rows wrap, dates align, tap targets on pagination links are
    comfortable. Then PRESENT the checklist to the operator and PAUSE. Do not advance on yolo/auto (auto_advance:
    false — feedback_uat_ui_phases). Record the operator's pass/fail per scenario in 171-HUMAN-UAT.md.
  </action>
  <what-built>
    A reverse-chronological Collected→Identified event feed on every collector page, paginated into static
    `/collectors/{login}/page/N/` sub-pages for high-volume collectors, with rank-aware species/genus links,
    determiner names, awaiting-ID and re-identified states, and an empty state for sample-host-only collectors.
  </what-built>
  <how-to-verify>
    1. `npm run dev`, then visit `/collectors/{top-collector-login}/` (highest `total_event_count` in
       public/data/collectors.json). Confirm the "Collection history" feed is newest-first.
    2. Click "Older events →" to `/page/2/`; confirm prev/next nav + "Page 2 of M"; page to the last page and
       confirm the Older link disappears; the "← Newer events" on page 2 returns to `/collectors/{login}/`.
       (Sub-page links require a BUILD-mode render — `npm run build` preview — since the dev HMR guard returns []
       for collectorEventPages; verify pagination on a built preview if dev shows no sub-pages.)
    3. Visit a small collector (single page, no nav) and a sample-host-only collector (empty state
       "No specimen events recorded yet.").
    4. Confirm a re-ID arc (Re-identified above Identified), "by {determiner}" rendering, species + genus links
       resolve, "undetermined" is plain text, and a waba_specimen row reads "Collected … awaiting ID".
    5. Narrow to mobile width and confirm rows wrap and dates align.
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues per scenario</resume-signal>
  <done>171-HUMAN-UAT.md exists with per-scenario operator results; operator has approved (or filed gaps for a follow-up gap-closure pass).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| committed JSON → CI build → public HTML | the deploy.yml Eleventy build turns committed event JSON into public static pages |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-171-01 | Tampering (stored XSS) | rendered determiner / taxon strings in generated HTML | mitigate (verify) | UAT + the Plan 02 `\| safe`-count==0 grep confirm auto-escaping held through to generated HTML; the build-verify greps generated `collectors/**` for `<script`. |
| T-171-02 | Information disclosure | public collector pages | accept | Public data, operator decision 2026-06-24; UAT confirms no unexpected PII (no emails/coords/specimen links — D-CARD-03). |
| T-171-SC | Tampering (supply chain) | npm/pip installs | accept (N/A) | No new packages this phase. |

No high-severity threats.
</threat_model>

<verification>
- `npm test` green AND `cd data && uv run pytest -m "not integration"` green (both languages — feedback_run_tests_before_push).
- Build-mode Eleventy run emits `/collectors/{login}/page/2/index.html` with `event-row` for a high-volume collector.
- `171-HUMAN-UAT.md` exists; operator scenarios recorded; checkpoint blocking (autonomous: false; does not auto-advance — feedback_uat_ui_phases).
</verification>

<success_criteria>
- Both test suites green; sub-pages generate in build mode from the committed artifact (CI clean-checkout path confirmed).
- Operator UAT checklist written, presented, and the run paused for approval.
- STREAM-01/02/03 each demonstrably satisfied: reverse-chron feed (01), waba_specimen Collected/awaiting-ID event + continuous-row cataloguing (02), bounded paginated sub-pages for 500+ records (03).
</success_criteria>

<output>
Create `.planning/phases/171-per-collector-event-stream/171-03-SUMMARY.md` when done.
</output>

## Artifacts this phase produces (Plan 03)

| Kind | Symbol / path | Notes |
|------|---------------|-------|
| UAT checklist | `.planning/phases/171-per-collector-event-stream/171-HUMAN-UAT.md` | NEW |
| Verification | full-suite + build-mode sub-page generation evidence (recorded in SUMMARY) | — |
