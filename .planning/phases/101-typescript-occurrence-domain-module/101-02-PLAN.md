---
phase: 101-typescript-occurrence-domain-module
plan: 02
type: execute
wave: 2
depends_on:
  - 101-01
files_modified:
  - src/bee-atlas.ts
  - src/bee-table.ts
  - src/features.ts
  - src/filter.ts
  - src/bee-occurrence-detail.ts
  - src/bee-map.ts
autonomous: true
requirements:
  - TS-01
  - TS-02
  - TS-03

must_haves:
  truths:
    - "All six caller files import the relevant occurrence.ts functions and delegate to them instead of constructing IDs or naming type discriminants inline"
    - "src/bee-table.ts no longer defines a local rowOccId helper — it imports occIdFromRow"
    - "src/bee-occurrence-detail.ts uses isSpecimenBacked / isProvisional in its render dispatch; the non-specimen partition uses !isSpecimenBacked (not isSampleOnly) so provisional rows are still included before the isProvisional branch fires"
    - "url-state.ts line 192 startsWith guard is unchanged (URL validation, not domain parsing)"
    - "All existing Vitest tests continue to pass; npm test exits 0"
    - "tsc --noEmit exits 0"
    - "The success-criteria greps from ROADMAP Phase 101 return only src/occurrence.ts (or empty results outside it)"
  artifacts:
    - path: "src/bee-atlas.ts"
      provides: "Import of occIdFromRow and parseOccId; four replaced sites"
      contains: "from './occurrence.ts'"
    - path: "src/bee-table.ts"
      provides: "Local rowOccId deleted; imports occIdFromRow"
      contains: "from './occurrence.ts'"
    - path: "src/features.ts"
      provides: "occIdFromRow and isSpecimenId imports; two replaced sites"
      contains: "from './occurrence.ts'"
    - path: "src/filter.ts"
      provides: "Inline ID construction replaced via occIdFromRow"
      contains: "from './occurrence.ts'"
    - path: "src/bee-occurrence-detail.ts"
      provides: "Predicate imports; isSpecimenBacked / isProvisional in render dispatch"
      contains: "from './occurrence.ts'"
    - path: "src/bee-map.ts"
      provides: "isSpecimenId import replaces startsWith('ecdysis:') guard"
      contains: "from './occurrence.ts'"
  key_links:
    - from: "src/bee-atlas.ts"
      to: "src/occurrence.ts"
      via: "import"
      pattern: "import \\{ [^}]*(occIdFromRow|parseOccId)[^}]* \\} from './occurrence.ts'"
    - from: "src/bee-occurrence-detail.ts"
      to: "src/occurrence.ts"
      via: "import"
      pattern: "import \\{ [^}]*(isSpecimenBacked|isProvisional)[^}]* \\} from './occurrence.ts'"
---

<objective>
Migrate the six caller files to import from `src/occurrence.ts` instead of constructing prefixed occurrence IDs or naming type discriminants inline. After this plan, the only file in `src/` that contains the literal substrings `'ecdysis:'` / `'inat:'` as ID prefixes is `src/occurrence.ts` (with two intentional exceptions: `src/url-state.ts:192`, which is URL-input validation per RESEARCH Open Question 3, and any test fixtures asserting the output format).

Purpose: Eliminate the duplicated ID construction and discriminant patterns identified in RESEARCH. Make the occurrence-type taxonomy from CLAUDE.md ("Specimen", "Sample", "Occurrence record") readable in code via named predicates.

Output: Six modified files; `npm test` green; `tsc --noEmit` clean; success-criteria greps from ROADMAP Phase 101 return the expected results.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/101-typescript-occurrence-domain-module/101-RESEARCH.md
@.planning/phases/101-typescript-occurrence-domain-module/101-PATTERNS.md
@.planning/phases/101-typescript-occurrence-domain-module/101-01-PLAN.md
@./CLAUDE.md

<!-- The module created in Plan 01 — this plan's tasks call its exports. -->
@src/occurrence.ts

<!-- Files modified by this plan — executor must read each before editing. -->
@src/bee-atlas.ts
@src/bee-table.ts
@src/features.ts
@src/filter.ts
@src/bee-occurrence-detail.ts
@src/bee-map.ts
</context>

<interfaces>
<!-- Contracts available from src/occurrence.ts (created in Plan 01). -->
<!-- Executor uses these directly — no need to re-read occurrence.ts. -->

```typescript
export function occIdFromRow(row: OccurrenceRow): string | null;
export function parseOccId(id: string): { source: 'ecdysis' | 'inat'; numericId: number } | null;
export function isSpecimenBacked(row: OccurrenceRow): boolean;
export function isSampleOnly(row: OccurrenceRow): boolean;
export function isProvisional(row: OccurrenceRow): boolean;
export function isSpecimenId(occId: string): boolean;
```

Key semantic notes:
- `occIdFromRow` returns `string | null` (null when both ecdysis_id and observation_id are null).
- `isSampleOnly` EXCLUDES provisional rows by design. The non-specimen partition in bee-occurrence-detail.ts uses `!isSpecimenBacked` (which DOES include provisional rows), and `isProvisional` then dispatches within that partition.
- `parseOccId` returns a structured `{ source, numericId }` object — callers do NOT need to re-split or re-check the prefix.

Exact site inventory (from PATTERNS.md sites; verified by grep):
- bee-atlas.ts: line 473–479 (parser → parseOccId); lines 748, 1006, 1026 (constructors → occIdFromRow); lines 932–938 (parser via filter/map → parseOccId or loop)
- bee-table.ts: lines 39–43 (delete local rowOccId helper); call sites at 312, 313, 351 keep `string | null` semantics
- features.ts: lines 46–48 (constructor → occIdFromRow); line 55 (`obj.ecdysis_id != null` → isSpecimenBacked); line 81 (`startsWith('ecdysis:')` → isSpecimenId)
- filter.ts: lines 323–324 (queryVisibleIds; raw primitives — see Task 4 action for the chosen approach)
- bee-occurrence-detail.ts: lines 247–259 (predicate dispatch; see RESEARCH Pitfall 2)
- bee-map.ts: line 966 (`startsWith('ecdysis:')` → isSpecimenId)
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Replace ID construction sites in bee-atlas.ts, bee-table.ts, features.ts</name>
  <files>
    src/bee-atlas.ts,
    src/bee-table.ts,
    src/features.ts
  </files>
  <read_first>
    - src/occurrence.ts (the module being consumed — confirm signatures match the <interfaces> block)
    - src/bee-atlas.ts (full file; the executor must see the surrounding code at lines 473–480, 748, 932–938, 1006, 1026 and the existing import block at lines 1–9)
    - src/bee-table.ts (full file — lines 1–10 imports, 39–43 the rowOccId definition, and the three call sites at 312, 313, 351 that depend on the `string | null` return)
    - src/features.ts (full file; the existing import block at lines 1–3, the constructor site at lines 46–48, the `obj.ecdysis_id != null` site at line 55, and the `startsWith` site at line 81)
    - .planning/phases/101-typescript-occurrence-domain-module/101-PATTERNS.md (lines 160–256 — exact replacement diffs for bee-atlas.ts, bee-table.ts, features.ts)
  </read_first>
  <behavior>
    - All four bee-atlas.ts construction sites (lines 748, 1006, 1026) and the parser at 473–479 delegate to occurrence.ts functions; behavior is byte-identical for non-null cases (rows from SQLite always have a non-null ID).
    - bee-atlas.ts:932–938 (filter + map pair extracting ecdysis IDs then iNat IDs from `selectedOccIds`) is rewritten to a single loop using `parseOccId`, preserving the existing output arrays (ecdysisIds, inatIds).
    - bee-table.ts local rowOccId is removed; the three call sites (lines 312, 313, 351) keep using the same identifier name `rowOccId`-style — but the underlying call is `occIdFromRow(row)` returning `string | null`. The null-check at line 352 (`if (occId && this.selectedIds?.has(occId))`) continues to work because occIdFromRow's contract is `string | null`.
    - features.ts:46–48 ID construction uses `occIdFromRow(obj as OccurrenceRow)`. If `occIdFromRow` returns null in the row-iteration path (which it should not, since `loadOccurrenceGeoJSON` filters rows from SQL with a lat/lon predicate that implies a non-null source ID), the iteration must either skip the row or fall back identically to existing behavior. Document the chosen branch in code.
    - features.ts:55 (`if (obj.ecdysis_id != null)`) is replaced with `if (isSpecimenBacked(obj as OccurrenceRow))`.
    - features.ts:81 (`f.properties.occId.startsWith('ecdysis:')`) is replaced with `isSpecimenId(f.properties.occId as string)`.
    - Existing Vitest tests continue to pass (no behavior change).
  </behavior>
  <action>
    Migrate three files. For each file, add the import line first, then perform the line-level replacements.

    A. src/bee-atlas.ts:
    - Add import after the `./filter.ts` import (currently at line 3): `import { occIdFromRow, parseOccId } from './occurrence.ts';`
    - Replace the parser at lines 473–479 with a single loop: iterate `this._selectedOccIds ?? []`, call `parseOccId(id)`, skip null, and push `parsed.numericId` to `selEcdysisIds` or `selInatIds` based on `parsed.source`. Preserve the surrounding scope variables `selEcdysisIds` and `selInatIds`.
    - Replace lines 748, 1006, 1026 (three sites with the pattern `r.ecdysis_id != null ? `ecdysis:${r.ecdysis_id}` : `inat:${Number(r.observation_id)}``) with `occIdFromRow(r)` or `occIdFromRow(obj)` depending on the surrounding identifier. Note: the previous patterns produced `inat:0` via `Number(null)`; `occIdFromRow` returns null in that case. Each call site uses the result as a string id, so the executor MUST verify the surrounding context and either (i) confirm the row is guaranteed non-null via type narrowing, or (ii) skip the row if `occIdFromRow` returns null. Add a defensive `if (!occId) continue;` (or equivalent) at each site if the surrounding scope iterates rows; for non-iterating uses the executor may use a non-null assertion ONLY if a comment justifies why the row is guaranteed to have an ID at that point.
    - Replace lines 932–938 (the `.filter(id => id.startsWith('ecdysis:')).map(id => id.slice('ecdysis:'.length))` pattern, repeated for both prefixes) with a single `for...of` loop that calls `parseOccId` and pushes to the appropriate output array — mirroring the pattern from the 473–479 replacement.

    B. src/bee-table.ts:
    - Replace the `import type { OccurrenceRow, SpecimenSortBy } from './filter.ts';` line by adding a new line after it: `import { occIdFromRow } from './occurrence.ts';`
    - Delete the local `function rowOccId(row: OccurrenceRow): string | null { ... }` block (lines 39–43).
    - At each of the three former `rowOccId(` call sites (lines 312, 313, 351), rename the call to `occIdFromRow(`. The surrounding null-check logic (e.g., the `occId && this.selectedIds?.has(occId)` at line 352) is unchanged because `occIdFromRow` returns `string | null` identically.

    C. src/features.ts:
    - Add imports after line 3: `import { occIdFromRow, isSpecimenBacked, isSpecimenId } from './occurrence.ts';` and `import type { OccurrenceRow } from './filter.ts';`
    - Replace lines 46–48 (`const occId = obj.ecdysis_id != null ? 'ecdysis:' + obj.ecdysis_id : 'inat:' + Number(obj.observation_id);`) with `const occId = occIdFromRow(obj as OccurrenceRow);`. If the surrounding loop pushes a feature with `properties.occId` and assumes occId is non-null, add a `if (occId == null) continue;` guard (preserve existing behavior — rows with both null IDs are pathological and should not produce features).
    - Replace line 55 (`if (obj.ecdysis_id != null) {`) with `if (isSpecimenBacked(obj as OccurrenceRow)) {`.
    - Replace line 81 (`features.filter(f => f.properties.occId.startsWith('ecdysis:'))`) with `features.filter(f => isSpecimenId(f.properties.occId as string))`.

    Do NOT touch src/filter.ts, src/bee-occurrence-detail.ts, or src/bee-map.ts in this task — those are Task 2 and Task 3. Do NOT modify any test files.

    After all replacements, run `npm test` and `npx tsc --noEmit` to confirm no regressions. If a test fails because a fixture row has both IDs null and now produces a different downstream effect, fix the SOURCE (add a guard / skip) rather than altering the fixture.
  </action>
  <verify>
    <automated>npm test &amp;&amp; npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "from './occurrence.ts'" src/bee-atlas.ts` returns 1
    - `grep -c "from './occurrence.ts'" src/bee-table.ts` returns 1
    - `grep -c "from './occurrence.ts'" src/features.ts` returns 1
    - `grep -c "^function rowOccId" src/bee-table.ts` returns 0 (local helper deleted)
    - `grep -c "rowOccId" src/bee-table.ts` returns 0 (no stale references)
    - `grep -cE 'ecdysis:\$\{|inat:\$\{|"ecdysis:"|"inat:"|'\''ecdysis:'\''|'\''inat:'\''' src/bee-atlas.ts` returns 0 (no remaining inline construction or string-literal usage)
    - `grep -cE "ecdysis:\\\$\\{|inat:\\\$\\{" src/features.ts` returns 0 (no template-literal construction)
    - `grep -c "'ecdysis:'" src/features.ts` returns 0 (no single-quoted prefix at line 47)
    - `grep -c "startsWith('ecdysis:')" src/features.ts` returns 0 (line 81 replaced with isSpecimenId)
    - `grep -c "obj.ecdysis_id != null" src/features.ts` returns 0 (line 55 predicate replaced)
    - `grep -c "id.startsWith('ecdysis:')" src/bee-atlas.ts` returns 0 (parser sites replaced with parseOccId)
    - `npm test` exits 0
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>
    bee-atlas.ts, bee-table.ts, features.ts all import from `./occurrence.ts`; bee-table.ts's local rowOccId is gone; the grep gates above all pass; full test suite is green and tsc is clean.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Replace remaining sites in filter.ts, bee-occurrence-detail.ts, bee-map.ts</name>
  <files>
    src/filter.ts,
    src/bee-occurrence-detail.ts,
    src/bee-map.ts
  </files>
  <read_first>
    - src/occurrence.ts (confirm signatures available — occIdFromRow, isSpecimenBacked, isProvisional, isSpecimenId)
    - src/filter.ts (full file; the import header, the OccurrenceRow definition at lines 30–66, and the queryVisibleIds function around lines 310–330 with the two construction sites at 323–324)
    - src/bee-occurrence-detail.ts (full file; the import block at lines 1–3, and the render dispatch block at lines 247–259 — read RESEARCH Pitfall 2 first)
    - src/bee-map.ts (full file or at minimum the import header and the section around line 966 — note CLAUDE.md says the `speicmenLayer` typo is intentionally deferred; do NOT fix it incidentally)
    - .planning/phases/101-typescript-occurrence-domain-module/101-RESEARCH.md (Pitfall 2 — bee-occurrence-detail.ts must use !isSpecimenBacked for non-specimen filter, NOT isSampleOnly; circular-import note around lines 222–223)
    - .planning/phases/101-typescript-occurrence-domain-module/101-PATTERNS.md (lines 261–322 — exact diffs for filter.ts, bee-occurrence-detail.ts, bee-map.ts; lines 372–373 circular-import warning)
  </read_first>
  <behavior>
    - filter.ts queryVisibleIds (lines 310–330) still populates `ids` Set with the same prefixed strings as before; behavior is byte-identical.
    - filter.ts has no runtime import from occurrence.ts (circular-import safety per PATTERNS.md lines 372–373). The inline construction at lines 323–324 is replaced by an in-file helper that uses the same logic — OR the executor imports occIdFromRow as a type-erased operation since occurrence.ts only does `import type` from filter.ts (no runtime cycle). The action below specifies the chosen approach.
    - bee-occurrence-detail.ts lines 247–259: the non-specimen partition uses `!isSpecimenBacked` (NOT `isSampleOnly`), so provisional rows are still in the partition; the `.map()` dispatches on `isProvisional` to choose between `_renderProvisional` and `_renderSampleOnly`. Existing render tests in bee-sidebar.test.ts continue to pass.
    - bee-map.ts line 966 startsWith guard is replaced with `isSpecimenId(f.properties.occId)`; behavior identical.
    - The `speicmenLayer` typo in bee-map.ts is NOT touched (CLAUDE.md constraint).
  </behavior>
  <action>
    Migrate three files.

    A. src/filter.ts (resolves the circular-import question):
    - Add a runtime import at the top of the file, alongside any existing imports: `import { occIdFromRow } from './occurrence.ts';`. This is SAFE despite filter.ts being the OccurrenceRow type owner because occurrence.ts only does `import type { OccurrenceRow }` — TypeScript's `import type` is erased before bundle resolution, so there is no runtime cycle. Confirm by running `npm test` after the change; if the test runner reports a circular-dependency warning, fall back to constructing a minimal partial-row inline (set ecdysis_id/observation_id from the local scope, is_provisional to false) and call occIdFromRow on it.
    - Replace lines 323–324:
      Before:
        `if (ecdysisId != null) ids.add(\`ecdysis:${Number(ecdysisId)}\`);`
        `if (obsId != null) ids.add(\`inat:${Number(obsId)}\`);`
      After: construct a partial OccurrenceRow `{ ecdysis_id: ecdysisId as number | null, observation_id: obsId as number | null, is_provisional: false } as OccurrenceRow`, call occIdFromRow on it, push to `ids` if non-null. Preserve the surrounding loop and the rowCount semantics.
    - If the executor finds that constructing a full OccurrenceRow cast is awkward at this site, the alternative is to keep two ids.add lines but extract the prefix substrings via two single-line template helpers `occIdFromRow({ ecdysis_id: ecdysisId, observation_id: null, is_provisional: false } as OccurrenceRow)` for the ecdysis case and the symmetric call for inat. Either approach is acceptable as long as no `ecdysis:` or `inat:` literal remains in filter.ts.

    B. src/bee-occurrence-detail.ts (RESEARCH Pitfall 2):
    - Add import after line 3: `import { isSpecimenBacked, isProvisional } from './occurrence.ts';`. Do NOT import `isSampleOnly` — that predicate's exclusion-of-provisional semantics would break this site (RESEARCH Pitfall 2).
    - Replace lines 247–259. The current block has two filters (line 247 for `r.ecdysis_id != null`, line 248 for `r.ecdysis_id == null`) and a `.map()` that dispatches on `row.is_provisional` (line 255–259). After the replacement:
      - Line 247: `const specimenBacked = this.occurrences.filter(isSpecimenBacked);`
      - Line 248: `const nonSpecimen = this.occurrences.filter(r => !isSpecimenBacked(r)).sort((a, b) => b.date.localeCompare(a.date));` — note the rename from `sampleOnly` to `nonSpecimen` to reflect that this partition includes BOTH sample-only and provisional rows.
      - Update the `${sampleOnly.map(...)}` reference in the render template to `${nonSpecimen.map(...)}`.
      - Inside the `.map()`, replace `row.is_provisional ? ... : ...` with `isProvisional(row) ? ... : ...`.

    C. src/bee-map.ts:
    - Add import alongside the file's existing imports (find the natural location near the top): `import { isSpecimenId } from './occurrence.ts';`
    - Replace line 966 (`f => f.properties.occId.startsWith('ecdysis:')`) with `f => isSpecimenId(f.properties.occId as string)`. Preserve the surrounding filter / scope.
    - Do NOT rename `speicmenLayer` (CLAUDE.md says the typo is intentionally deferred).

    Do not modify any test files. After the changes, run `npm test` and `npx tsc --noEmit`.
  </action>
  <verify>
    <automated>npm test &amp;&amp; npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "from './occurrence.ts'" src/filter.ts` returns 1
    - `grep -c "from './occurrence.ts'" src/bee-occurrence-detail.ts` returns 1
    - `grep -c "from './occurrence.ts'" src/bee-map.ts` returns 1
    - `grep -cE 'ecdysis:\$\{|inat:\$\{' src/filter.ts` returns 0 (template-literal construction gone from lines 323–324)
    - `grep -c "startsWith('ecdysis:')" src/bee-map.ts` returns 0 (line 966 replaced)
    - `grep -c "row.is_provisional ?" src/bee-occurrence-detail.ts` returns 0 (replaced with isProvisional(row))
    - `grep -c "r.ecdysis_id == null" src/bee-occurrence-detail.ts` returns 0 (replaced with !isSpecimenBacked(r))
    - `grep -c "r.ecdysis_id != null" src/bee-occurrence-detail.ts` returns 0 (replaced with isSpecimenBacked)
    - `grep -c "isSampleOnly" src/bee-occurrence-detail.ts` returns 0 (the wrong predicate; RESEARCH Pitfall 2 — must use !isSpecimenBacked)
    - `grep -c "speicmenLayer" src/bee-map.ts` returns 1 or more (typo intentionally preserved per CLAUDE.md)
    - `npm test` exits 0 — in particular `bee-sidebar.test.ts` provisional-render tests still pass (proves the !isSpecimenBacked partition still includes provisional rows)
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>
    filter.ts, bee-occurrence-detail.ts, bee-map.ts all import from `./occurrence.ts`; the bee-occurrence-detail.ts non-specimen partition uses `!isSpecimenBacked`; the bee-map.ts speicmenLayer typo is untouched; full test suite green; tsc clean.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Phase-level grep gates and full-suite verification</name>
  <files>(no files modified — verification only)</files>
  <read_first>
    - .planning/ROADMAP.md (Phase 101 Success Criteria — the four bullets at lines 496–500)
    - .planning/phases/101-typescript-occurrence-domain-module/101-RESEARCH.md (lines 87–94 — list of patterns to preserve as-is in url-state.ts, features.ts:81, bee-map.ts:966; note features.ts:81 and bee-map.ts:966 ARE migrated in Task 1 and Task 2 respectively)
  </read_first>
  <action>
    Run the success-criteria greps against the current src/ tree, plus the full Vitest suite and tsc check. Document the exact output of each grep in the plan SUMMARY (Task is documentation + gate, not code edits).

    The greps to run (executor records exact output):

    1. `grep -rln "'ecdysis:'" src/ | sort` — expected: only `src/occurrence.ts` and `src/url-state.ts` (the latter is the URL-input validation guard at line 192, explicitly preserved per RESEARCH Open Question 3). Test files under `src/tests/` may also appear if they assert the output format — that is acceptable.

    2. `grep -rln "'inat:'" src/ | sort` — expected: same allowed set as above.

    3. `grep -rlnE 'ecdysis:\$\{|inat:\$\{' src/ | sort` — expected: only `src/occurrence.ts` (template-literal construction must exist nowhere else).

    4. `grep -rln "\"ecdysis:\"" src/ | sort` — expected: empty (the literal double-quoted form was never used; this grep is from the ROADMAP success criteria text and should return no hits).

    5. `grep -rln "\"inat:\"" src/ | sort` — expected: empty.

    6. `grep -rln 'r\.ecdysis_id != null\|row\.ecdysis_id != null\|obj\.ecdysis_id != null' src/ --include='*.ts' | grep -v tests/ | sort` — expected: empty in production code (all TypeScript discriminants migrated to isSpecimenBacked). Note: SQL strings inside template literals like `ecdysis_id IS NOT NULL` in bee-atlas.ts and filter.ts are SQL predicates, not TypeScript discriminants, and are out of scope. The grep above targets the JavaScript `!= null` discriminant only.

    7. `grep -rln "row\.is_provisional\|r\.is_provisional\|obj\.is_provisional" src/ --include='*.ts' | grep -v tests/ | grep -v occurrence.ts | sort` — expected: empty (all uses migrated to isProvisional).

    8. `grep -rln "from './occurrence.ts'" src/ | sort` — expected: at least bee-atlas.ts, bee-table.ts, bee-occurrence-detail.ts, features.ts, filter.ts, bee-map.ts (six files).

    Then run:

    9. `npm test` — must exit 0.
    10. `npx tsc --noEmit` — must exit 0.
    11. `npm run build` — must exit 0 (catches any build-time issue not caught by tsc alone).

    If any grep returns unexpected hits (a previously-missed inline construction or discriminant), fix the source file directly and re-run all gates. If a grep returns expected-empty results, proceed.

    Record the full output of greps 1–8 (file lists), the exit codes of greps 9–11, and any remediation in the plan SUMMARY.
  </action>
  <verify>
    <automated>npm test &amp;&amp; npx tsc --noEmit &amp;&amp; npm run build &amp;&amp; bash -c "G1=\$(grep -rln \"'ecdysis:'\" src/ | grep -v -E '(occurrence\\.ts|url-state\\.ts|/tests/)' | wc -l | tr -d ' '); G2=\$(grep -rlnE 'ecdysis:\\\$\\{|inat:\\\$\\{' src/ | grep -v -E '(occurrence\\.ts|/tests/)' | wc -l | tr -d ' '); G3=\$(grep -rln \"row\\.is_provisional\\|r\\.is_provisional\\|obj\\.is_provisional\" src/ --include='*.ts' | grep -v -E '(occurrence\\.ts|/tests/)' | wc -l | tr -d ' '); echo \"G1=\$G1 G2=\$G2 G3=\$G3\"; test \"\$G1\" = 0 -a \"\$G2\" = 0 -a \"\$G3\" = 0"</automated>
  </verify>
  <acceptance_criteria>
    - `npm test` exits 0
    - `npx tsc --noEmit` exits 0
    - `npm run build` exits 0
    - `grep -rln "'ecdysis:'" src/ | grep -v -E '(occurrence\.ts|url-state\.ts|/tests/)'` returns no lines (the single-quoted prefix appears only in occurrence.ts, url-state.ts validation, or test fixtures)
    - `grep -rlnE 'ecdysis:\$\{|inat:\$\{' src/ | grep -v -E '(occurrence\.ts|/tests/)'` returns no lines (no inline template-literal construction outside occurrence.ts)
    - `grep -rln "row\.is_provisional\|r\.is_provisional\|obj\.is_provisional" src/ --include='*.ts' | grep -v -E '(occurrence\.ts|/tests/)'` returns no lines (no inline is_provisional discriminant in production code)
    - `grep -rln "from './occurrence.ts'" src/` returns 6 file paths matching: bee-atlas.ts, bee-table.ts, features.ts, filter.ts, bee-occurrence-detail.ts, bee-map.ts (plus src/tests/occurrence.test.ts which imports `from '../occurrence.ts'` — different path, not counted)
    - The plan SUMMARY records the exact output (file lists and exit codes) of all eleven gates above
  </acceptance_criteria>
  <done>
    All eleven verification gates pass; SUMMARY records the exact outputs; phase success criteria from ROADMAP are demonstrably satisfied.
  </done>
</task>

</tasks>

<verification>
End-of-plan checks (executor runs all before writing SUMMARY):

1. `npm test` exits 0 — including occurrence.test.ts and all previously-existing tests (bee-sidebar.test.ts, bee-table.test.ts, filter.test.ts, url-state.test.ts)
2. `npx tsc --noEmit` exits 0
3. `npm run build` exits 0
4. ROADMAP Phase 101 success criteria #1 satisfied: only src/occurrence.ts (and intentional carve-outs: url-state.ts:192 validation, test files) contain the `'ecdysis:'` literal
5. ROADMAP Phase 101 success criteria #2 satisfied: same for `'inat:'`
6. ROADMAP Phase 101 success criteria #3 satisfied: isSpecimenBacked, isSampleOnly, isProvisional are named exports of src/occurrence.ts; no inline discriminant condition for occurrence type exists in any production .ts file outside src/occurrence.ts
7. ROADMAP Phase 101 success criteria #4 satisfied: occurrence.test.ts covers occIdFromRow, parseOccId, and the three predicates (plus isSpecimenId)
8. All six caller files import from `./occurrence.ts`
9. bee-map.ts's `speicmenLayer` typo is preserved (CLAUDE.md constraint)
</verification>

<success_criteria>
- All ROADMAP Phase 101 success criteria pass (verified by the greps and test commands in Task 3)
- TS-01 satisfied: occurrence.ts owns ID construction and parsing; all six caller files import from it
- TS-02 satisfied: isSpecimenBacked, isSampleOnly, isProvisional exported; inline discriminants replaced
- TS-03 satisfied: occurrence.test.ts covers all exports; npm test green
- No regression in any existing test
</success_criteria>

<output>
Create `.planning/phases/101-typescript-occurrence-domain-module/101-02-SUMMARY.md` when done. Include:
- Exact output of greps 1–8 from Task 3 (file path lists)
- Exit codes from greps 9–11 (npm test, tsc, build)
- Any places where the executor chose between alternative replacement patterns (e.g., the filter.ts partial-row vs. direct-call decision) and why
- Confirmation that bee-map.ts's `speicmenLayer` typo is intact
</output>
