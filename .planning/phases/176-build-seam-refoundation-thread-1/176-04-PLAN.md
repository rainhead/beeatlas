---
phase: 176-build-seam-refoundation-thread-1
plan: 04
type: execute
wave: 3
depends_on: [176-02, 176-03]
files_modified: []
autonomous: false
requirements: [SEAM-03]
must_haves:
  truths:
    - "The first post-merge nightly (maderas `run.sh build`, the real contract gate) produces a byte-identical manifest.json — same 16 keys in the same order, occurrences_db_tables + generated_at present — and the site is unchanged"
    - "The baseline pull reports 9 pulled with 0 unmapped(drift); the deploy.yml build-time fetch is green (6 artifacts, species_hosts tolerated if absent)"
  artifacts: []
  key_links: []
---

<objective>
Confirm the pure refactor is behavior-identical in production. The full pipeline cannot run locally (memory `project_local_dbt_build_not_runnable`); the nightly `run.sh build` on maderas is the real contract gate. This plan is a single blocking operator-verification checkpoint after Plans 02+03 merge.

Purpose: Close SEAM-03's "regression-safe: the manifest and pulled/fetched file set are unchanged" against production, the one surface the local pytest floor cannot reach.
Output: Operator confirmation (or a captured discrepancy → gap-closure input).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@data/nightly.sh
@.github/workflows/deploy.yml
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Operator — verify the first post-merge nightly is behavior-identical</name>
  <what-built>
    Plans 01–03 replaced the three hand-synced key lists (nightly.sh publish/manifest block, nightly.sh baseline-classifier heredoc, deploy.yml build-time fetch) with one declarative `data/artifacts.toml` read by `data/artifacts.py`. No dbt contract changed, so NO `SKIP_INTEGRATION_GATE` dance is needed — a normal nightly should pass the integration gate unaided and publish a byte-identical manifest.
  </what-built>
  <how-to-verify>
    After this phase is merged to main, on the next scheduled maderas nightly (or a manual `bash data/nightly.sh` run):
    1. Watch the "--- pulling published artifacts for integration baseline ---" block — confirm the summary reads `baseline pull: 9 pulled, 0 skipped, 0 failed, 0 unmapped(drift)`. Any `unmapped(drift)` WARN means a manifest key is missing from artifacts.toml (a real drift to fix).
    2. Confirm the "--- integration test gate ---" passes unaided (no SKIP_INTEGRATION_GATE).
    3. After publish, fetch `s3://<bucket>/data/manifest.json` (or https://beeatlas.net/data/manifest.json) and confirm: exactly the 16 keys — occurrences, occurrences_db, species, seasonality, higher_taxa, counties, ecoregions, places, places_meta, checklist, photos, species_hosts, collectors, collector_event_pages, occurrences_db_tables, generated_at — in that order; occurrences_db_tables is an inline array; generated_at is a fresh timestamp. (Diffing against the prior night's manifest structure: only the hash suffixes + generated_at should differ.)
    4. Confirm the triggered GitHub Actions deploy's "Fetch build-time data from S3" step is green (6 artifacts fetched; species_hosts fetched or the "tolerates absence" note printed) and the site builds + deploys.
    5. Spot-check the live site (species page, collectors page) renders unchanged.
  </how-to-verify>
  <resume-signal>Type "approved" if the manifest keys/order are unchanged, the baseline pull shows 9/0-drift, and the deploy is green. Otherwise describe the discrepancy (e.g. an unmapped-drift key, a wrong hashed name, a fetch failure) so it can be routed to gap-closure.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Local pytest floor → production nightly | The one gap the local tests cannot cover — bash/YAML wiring against real S3. This checkpoint is that coverage. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-176-07 | Tampering | A wiring bug surfacing only against real S3 (wrong manifest, stale publish) | mitigate | This blocking operator checkpoint verifies the first post-merge nightly's baseline-pull count, manifest key set/order, and deploy fetch before the phase is accepted |
| T-176-SC | Tampering | Supply chain | accept | No install introduced anywhere in this phase |
</threat_model>

<verification>
- Operator confirms: baseline pull `9 pulled, 0 unmapped(drift)`; integration gate passes unaided; manifest has the 16 keys in order; deploy build-time fetch green; site unchanged.
</verification>

<success_criteria>
- The refactor is proven regression-safe in production: byte-identical manifest key set/order and unchanged baseline-pull (9) + build-time-fetch (6) sets on the first post-merge nightly (SEAM-03).
</success_criteria>

<output>
Create `.planning/phases/176-build-seam-refoundation-thread-1/176-04-SUMMARY.md` when done.
</output>
