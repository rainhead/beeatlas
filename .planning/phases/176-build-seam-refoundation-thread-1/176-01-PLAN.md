---
phase: 176-build-seam-refoundation-thread-1
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - data/artifacts.toml
  - data/artifacts.py
  - data/tests/test_artifacts.py
autonomous: true
requirements: [SEAM-01, SEAM-02, SEAM-04, SEAM-05]
must_haves:
  truths:
    - "data/artifacts.toml declares all 16 published manifest artifacts (14 hashed + 2 metadata) with logical name, source_file, provenance, kind, baseline_diff, build_time_fetch, gzip, content_type (SEAM-01)"
    - "python3 data/artifacts.py validate exits 0 on the real contract and fails loud (exit 1, names the artifact) on an unknown kind, metadata-with-filename, or authoritative-with-baseline_diff=true (SEAM-02, SEAM-04, SEAM-05)"
    - "The manifest verb emits a byte-identical manifest.json: 16 keys in declared order, occurrences_db_tables as an inline compact JSON array, generated_at quoted, no trailing comma on the last key, trailing newline after }"
    - "baseline-pull-plan yields exactly the 9 baseline_diff artifacts (occurrences, counties, ecoregions, species, seasonality, higher_taxa, photos, collectors, species_hosts); build-time-fetch yields exactly the 6 (species, seasonality, higher_taxa, collectors, collector_event_pages, species_hosts) with species_hosts optional=true"
    - "A synthetic authoritative artifact is machine-forced baseline_diff=false and structurally absent from baseline-pull-plan output (SEAM-04)"
  artifacts:
    - path: data/artifacts.toml
      provides: "The single declarative artifact contract — sole source of truth for every published artifact"
      contains: "[artifacts.occurrences]"
    - path: data/artifacts.py
      provides: "Stdlib-only loader + CLI (verbs: publish-plan, manifest, baseline-pull-plan, build-time-fetch, validate) + classification predicates"
      exports: ["load", "validate", "hashed_artifacts", "baseline_diff_artifacts", "build_time_fetch_artifacts", "authoritative_names"]
    - path: data/tests/test_artifacts.py
      provides: "pytest coverage of every fail-loud invariant + byte-exact manifest golden + set-equality regression floor"
      min_lines: 120
  key_links:
    - from: data/artifacts.py
      to: data/artifacts.toml
      via: "load() reads Path(__file__).parent / 'artifacts.toml'"
      pattern: "Path\\(__file__\\)\\.parent"
    - from: data/tests/test_artifacts.py
      to: data/artifacts.py
      via: "from artifacts import ... (mirrors test_config.py; data/ on sys.path)"
      pattern: "from artifacts import"
---

<objective>
Create the declarative artifact contract (`data/artifacts.toml`), the tested stdlib-only loader/CLI (`data/artifacts.py`), and the pytest suite that locks its behavior. This plan produces NO consumer wiring — it is the risk-free foundation that plans 02–04 depend on, and it is 100% verifiable locally with `pytest` (the full pipeline cannot run locally — memory `project_local_dbt_build_not_runnable`).

Purpose: Collapse the three hand-synced key lists into one machine-checkable contract (SEAM-01), replace ad-hoc classification with a tested module (SEAM-02), establish the derived-vs-authoritative split before any authoritative data exists (SEAM-04), and machine-enforce the two schema-evolution regimes (SEAM-05).
Output: `data/artifacts.toml`, `data/artifacts.py`, `data/tests/test_artifacts.py`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

# Loader analog — the closest existing pattern (tomllib.load at import, typed accessors):
@data/config.py
@data/tests/test_config.py
# pytest config (testpaths=["tests"], data/ on sys.path, `-m 'not integration'` default):
@data/pyproject.toml
# The three source-of-truth sites this contract must reproduce exactly:
@data/nightly.sh
@.github/workflows/deploy.yml
</context>

<artifacts_this_phase_produces>
New symbols introduced by Phase 176 (all created in this plan):
- `data/artifacts.toml` — 16 `[artifacts.<logical_name>]` tables (14 hashed + 2 metadata), in manifest order.
- `data/artifacts.py` — module. Public functions: `load(path=None) -> dict`, `validate(spec) -> None`, `hashed_artifacts(spec)`, `metadata_artifacts(spec)`, `baseline_diff_artifacts(spec)`, `build_time_fetch_artifacts(spec)`, `authoritative_names(spec)`, `derived_names(spec)`. CLI verbs: `publish-plan`, `manifest`, `baseline-pull-plan`, `build-time-fetch`, `validate`.
- `data/tests/test_artifacts.py` — pytest suite.
- (Plan 03) `docs/adr/0002-derived-vs-authoritative-artifacts.md` — the SEAM-05 regime documentation.

Documented exclusion (not a silent gap): the three stable-directory publishes (`feeds/`, `species-maps/`, `place-maps/` — recursive `aws s3 cp --recursive`) are NOT manifest artifacts, are not one of the three named hand-synced key lists, and do not fit the per-file metadata schema (no single local filename, no hash, no manifest key). They stay hardcoded in `nightly.sh` this phase; recorded as out-of-contract in the ADR (Plan 03).
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Write data/artifacts.toml — the declarative contract</name>
  <files>data/artifacts.toml</files>
  <read_first>
    - data/nightly.sh lines 306–346 (the 14 `_upload_hashed`/`_upload_hashed_gz` calls + the manifest heredoc — the exact source_file/basename/content-type/gzip/order to encode)
    - data/nightly.sh lines 153–171 (LOCAL_NAMES / NON_FILE_KEYS / INTENTIONALLY_SKIPPED — the exact baseline_diff classification to encode)
    - .github/workflows/deploy.yml lines 45–67 (the 6 build-time fetches + the species_hosts `// empty` guard — the exact build_time_fetch classification to encode)
    - data/config.py (tomllib pattern)
  </read_first>
  <action>
    Create `data/artifacts.toml` with a header comment block (purpose: single source of truth for every published artifact; Python emits plans only, all S3 I/O stays in nightly.sh per CLAUDE.md) and 16 `[artifacts.<logical_name>]` tables declared in MANIFEST ORDER.

    Per-artifact fields (schema):
    - `provenance` — "derived" | "authoritative" (all 16 are "derived" this phase)
    - `kind` — "hashed" | "metadata"
    - `source_file` — filename in EXPORT_DIR the publish block reads AND the local destination name for baseline-pull / build-time-fetch (hashed only; omit for metadata)
    - `hash_basename` — the basename passed to `_upload_hashed*` (hashed only)
    - `baseline_diff` — bool (default false)
    - `build_time_fetch` — bool (default false)
    - `build_time_fetch_optional` — bool (default false; only species_hosts)
    - `gzip` — bool (default false; only occurrences_db)
    - `content_type` — string (omit unless set; only the 4 geojson/json)
    - `metadata_type` — "json" | "string" (metadata only)

    Declare exactly these 16 tables IN THIS ORDER with these values:

    | logical name | provenance | kind | source_file | hash_basename | baseline_diff | build_time_fetch | gzip | content_type | metadata_type |
    |---|---|---|---|---|---|---|---|---|---|
    | occurrences | derived | hashed | occurrences.parquet | occurrences | true | false | false | — | — |
    | occurrences_db | derived | hashed | occurrences.db | occurrences | false | false | true | — | — |
    | species | derived | hashed | species.json | species | true | true | false | — | — |
    | seasonality | derived | hashed | seasonality.json | seasonality | true | true | false | — | — |
    | higher_taxa | derived | hashed | higher_taxa.json | higher_taxa | true | true | false | — | — |
    | counties | derived | hashed | counties.geojson | counties | true | false | false | application/json | — |
    | ecoregions | derived | hashed | ecoregions.geojson | ecoregions | true | false | false | application/json | — |
    | places | derived | hashed | places.geojson | places | false | false | false | application/json | — |
    | places_meta | derived | hashed | places.json | places_meta | false | false | false | application/json | — |
    | checklist | derived | hashed | checklist.parquet | checklist | false | false | false | — | — |
    | photos | derived | hashed | photos.json | photos | true | false | false | — | — |
    | species_hosts | derived | hashed | species_hosts.json | species_hosts | true | true | false | — | — |
    | collectors | derived | hashed | collectors.json | collectors | true | true | false | — | — |
    | collector_event_pages | derived | hashed | collector_event_pages.json | collector_event_pages | false | true | false | — | — |
    | occurrences_db_tables | derived | metadata | — | — | false | false | — | — | json |
    | generated_at | derived | metadata | — | — | false | false | — | — | string |

    Set `build_time_fetch_optional = true` ONLY on `species_hosts`. Notes on the non-obvious rows: `occurrences_db` shares `hash_basename = "occurrences"` with `occurrences` (they differ by extension); `places_meta` reads `source_file = "places.json"` but hashes under basename `places_meta`; `collector_event_pages` is `baseline_diff=false` (too large to baseline, ~29 MB) yet `build_time_fetch=true`. Do NOT invent a `species.parquet` key — the manifest `species` key maps to `species.json` (A3 rule).
  </action>
  <acceptance_criteria>
    - `python3 -c "import tomllib; d=tomllib.load(open('data/artifacts.toml','rb')); print(len(d['artifacts']))"` prints `16`
    - `python3 -c "import tomllib; d=tomllib.load(open('data/artifacts.toml','rb')); print(list(d['artifacts']))"` prints the 16 logical names in exactly the manifest order (occurrences … generated_at)
    - grep confirms `species_hosts` has `build_time_fetch_optional = true` and no other artifact does: `grep -c 'build_time_fetch_optional' data/artifacts.toml` returns 1
    - `occurrences_db` block contains `gzip = true` and `hash_basename = "occurrences"`; `places_meta` block contains `source_file = "places.json"` and `hash_basename = "places_meta"`
  </acceptance_criteria>
  <verify>
    <automated>python3 -c "import tomllib; d=tomllib.load(open('data/artifacts.toml','rb'))['artifacts']; names=list(d); assert names==['occurrences','occurrences_db','species','seasonality','higher_taxa','counties','ecoregions','places','places_meta','checklist','photos','species_hosts','collectors','collector_event_pages','occurrences_db_tables','generated_at'], names; print('OK', len(names))"</automated>
  </verify>
  <done>artifacts.toml parses, declares 16 artifacts in manifest order, with the exact field values in the table above.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Write data/artifacts.py — loader, validation, and CLI verbs (stdlib-only)</name>
  <files>data/artifacts.py</files>
  <read_first>
    - data/config.py (the tomllib-at-module-load + Path(__file__).parent idiom to mirror)
    - data/nightly.sh lines 277–349 (the exact manifest byte layout to reproduce; `_upload_hashed` returns `<basename>-<12hex>.<ext>` on stdout)
    - data/nightly.sh lines 136–210 (the classifier semantics baseline-pull-plan replaces: pull baseline_diff keys, skip metadata + non-baseline, WARN on unknown)
    - .github/workflows/deploy.yml lines 45–67 (build-time-fetch semantics + species_hosts optional guard)
  </read_first>
  <behavior>
    - load(path=None): parses the toml (default data/artifacts.toml), applies field defaults (gzip/baseline_diff/build_time_fetch/build_time_fetch_optional=false, content_type=None), returns an ORDER-PRESERVING dict {logical_name: fields}. Does NOT validate (so tests can load invalid tomls).
    - validate(spec): raises ValueError naming the offending artifact + rule on: kind not in {hashed,metadata}; provenance not in {derived,authoritative}; hashed missing source_file/hash_basename; metadata having source_file/hash_basename or missing metadata_type in {json,string}; authoritative with baseline_diff=true; content_type on a non-hashed; build_time_fetch_optional without build_time_fetch. Passes on the real contract.
    - `validate` verb: load()+validate() the default toml → print "OK: 16 artifacts (16 derived, 0 authoritative)" exit 0; on ValueError print message to stderr, exit 1.
    - `publish-plan` verb: one TSV row per hashed artifact in declared order: `name\tsource_file\thash_basename\t{true|false gzip}\t{content_type or "-"}`.
    - `manifest <mapfile> --meta k=v ...` verb: read mapfile (`logical\thashed` lines) + --meta pairs; assert the map keys == exactly the declared hashed logical names AND the meta keys == exactly the declared metadata logical names (else exit 1 naming the missing/extra/unknown key); emit the byte-exact manifest.json to stdout.
    - `baseline-pull-plan <manifest.json>` verb: for each key in the live manifest — unknown (not in toml) → WARN to stderr (drift), no row; metadata or baseline_diff=false → skip; baseline_diff=true with empty value → WARN skip; baseline_diff=true with value → emit `name\thashed\tsource_file`.
    - `build-time-fetch` verb: one TSV row per build_time_fetch artifact in declared order: `name\tsource_file\t{true|false optional}`.
  </behavior>
  <action>
    Create `data/artifacts.py` importing ONLY stdlib: `tomllib`, `json`, `sys`, `argparse`, `pathlib`. This module MUST be runnable by CI's bare `python3` (deploy.yml has no uv/venv) — do NOT import config, duckdb, or any third-party package. Resolve the contract via `_DEFAULT_PATH = Path(__file__).parent / "artifacts.toml"`; `load(path=None)` defaults to it (parametrized for tests).

    Implement `load`, `validate`, and the predicate helpers `hashed_artifacts`, `metadata_artifacts`, `baseline_diff_artifacts`, `build_time_fetch_artifacts`, `authoritative_names`, `derived_names` (each returns declared-order results). All CLI verbs call `load()` then `validate()` before emitting, so every consumer fails loud on a malformed contract.

    MANIFEST BYTE LAYOUT (reproduce data/nightly.sh's heredoc exactly — this is the SC-3 byte-identity floor): print `{` then, for each of the 16 artifacts in declared order, a line `  "<name>": <value><sep>` where the leading indent is two spaces, `<sep>` is `,` for every artifact except the last (generated_at → no comma), and `<value>` is: for hashed → the mapped hashed name in double quotes; for metadata_type=json → the raw --meta value verbatim (NO quotes — it is already a compact JSON array); for metadata_type=string → the --meta value in double quotes. Then print `}`. Use `print()` (its trailing newline reproduces the heredoc's final `}\n`). Do NOT use json.dumps for the manifest — it would expand occurrences_db_tables across lines and break byte-identity. The verb orders strictly by toml declaration, so the mapfile's line order is irrelevant.

    Wire an `if __name__ == "__main__":` argparse dispatch over the 5 verbs. Print all plan rows to stdout, all WARN/errors to stderr.
  </action>
  <acceptance_criteria>
    - `python3 data/artifacts.py validate` exits 0 and prints `OK: 16 artifacts (16 derived, 0 authoritative)`
    - `python3 data/artifacts.py publish-plan | wc -l` prints `14`; the occurrences_db row's 4th field is `true` (gzip) and the counties row's 5th field is `application/json`
    - `python3 data/artifacts.py build-time-fetch | wc -l` prints `6`; the species_hosts row's 3rd field is `true` and every other row's 3rd field is `false`
    - Runs under bare system `python3` (no uv) — confirmed by invoking `python3` directly above (imports are stdlib-only)
  </acceptance_criteria>
  <verify>
    <automated>python3 data/artifacts.py validate && test "$(python3 data/artifacts.py publish-plan | wc -l | tr -d ' ')" = "14" && test "$(python3 data/artifacts.py build-time-fetch | wc -l | tr -d ' ')" = "6"</automated>
  </verify>
  <done>artifacts.py loads + validates the real contract, is stdlib-only (runs under bare python3), and all 5 verbs emit the correct row counts and fields.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Write data/tests/test_artifacts.py — invariants, byte-exact manifest golden, set-equality regression floor</name>
  <files>data/tests/test_artifacts.py</files>
  <read_first>
    - data/tests/test_config.py (import idiom `from config import ...`; mirror as `from artifacts import ...`)
    - data/tests/conftest.py (tests are a package; data/ is on sys.path)
    - data/artifacts.py (the module under test, from Task 2)
    - data/nightly.sh lines 327–346 (the exact manifest heredoc the golden must reproduce)
  </read_first>
  <behavior>
    - Test the real contract loads + validates and declares 16 artifacts in manifest order.
    - Fail-loud tests (each builds a small in-memory/temp toml, loads it, asserts validate raises ValueError): unknown kind; metadata with a source_file; authoritative with baseline_diff=true; hashed missing source_file; content_type on metadata; build_time_fetch_optional without build_time_fetch.
    - SEAM-04 synthetic-authoritative test: a temp toml with one authoritative artifact (baseline_diff=false) → validate passes, authoritative_names() returns it, and it is ABSENT from baseline_diff_artifacts(); the same artifact with baseline_diff=true → validate raises.
    - Set-equality regression floor (SC-3): baseline_diff_artifacts names == the 9 LOCAL_NAMES keys; each baseline artifact's source_file == the current LOCAL_NAMES value; build_time_fetch_artifacts names == the 6 deploy.yml keys with species_hosts optional=true.
    - Byte-exact manifest golden: call the manifest emitter with a synthetic map (each hashed value = `<name>-DEADBEEF0000.<ext>`) + meta occurrences_db_tables='["a","b"]', generated_at='2026-01-01T00:00:00Z'; assert output equals a hand-written expected string with 2-space indent, occurrences_db_tables inline as `["a","b"]`, generated_at quoted, no trailing comma on generated_at, and a trailing newline.
    - manifest coverage failure: passing a map missing one hashed key (or with an extra/unknown key) causes a non-zero exit / raised error.
  </behavior>
  <action>
    Create `data/tests/test_artifacts.py` importing `from artifacts import load, validate, baseline_diff_artifacts, build_time_fetch_artifacts, authoritative_names` (data/ is on sys.path exactly as test_config.py relies on). For fail-loud and synthetic-authoritative cases, write minimal toml strings to a `tmp_path` file and `load(tmp_path)`. For the byte-exact golden, either call a module-level manifest-render function (extract the emitter into a testable function that returns the string, invoked by both the CLI verb and this test) or run the CLI verb via `subprocess` and compare stdout — prefer the direct function call. Encode the 9 baseline source_files and 6 build-time keys as explicit expected literals so a future contract edit that drifts them turns this test red. Do NOT mark any test `@pytest.mark.integration` — this whole file runs in the fast default tier.
  </action>
  <acceptance_criteria>
    - `cd data && uv run pytest tests/test_artifacts.py -q` passes with >= 10 tests
    - `cd data && uv run pytest tests/test_artifacts.py -q -p randomly` passes (order-stable — the suite is a hard project gate, memory feedback_bee_atlas_test_mounting)
    - The byte-exact golden test fails if the manifest indent, key order, occurrences_db_tables inline-array formatting, or trailing-comma placement changes
    - `python3 data/artifacts.py validate` still exits 0
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/rainhead/dev/beeatlas/data && uv run pytest tests/test_artifacts.py -q</automated>
  </verify>
  <done>test_artifacts.py is green (and randomized-order stable), covers every fail-loud invariant, the synthetic-authoritative exclusion, the 9/6 set-equality floors, and the byte-exact manifest golden.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Python → S3 | The ONLY boundary. `artifacts.py` emits plans/classifications as text ONLY and performs zero S3/subprocess I/O — all `aws` calls stay in `nightly.sh`/`deploy.yml` (CLAUDE.md invariant: "Python knows nothing about S3"). This plan creates no S3-touching code. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-176-01 | Tampering | A mis-encoded artifact in artifacts.toml → wrong/stale/duplicated publish | mitigate | `validate()` fails loud on structural errors; the byte-exact manifest golden + the 9/6 set-equality tests lock the derived-artifact behavior; the manifest coverage check rejects a map that misses/adds a key |
| T-176-02 | Information disclosure | Secrets exposure | accept | Unchanged — this plan adds no S3 code, no secret, no network call; artifacts.py is pure local text transformation |
| T-176-SC | Tampering | Supply chain (npm/pip/cargo installs) | accept | No package install in this phase — artifacts.py is stdlib-only (tomllib/json/sys/argparse/pathlib); pytest is a pre-existing dev dependency. No RESEARCH.md Package Legitimacy Audit required (no installs) |
</threat_model>

<verification>
- `python3 data/artifacts.py validate` exits 0.
- `cd data && uv run pytest tests/test_artifacts.py -q` green; `-p randomly` stable.
- The byte-exact manifest golden proves the manifest verb reproduces nightly.sh's current layout.
- `python3 data/artifacts.py publish-plan|build-time-fetch|baseline-pull-plan` all run under bare system python3 (CI compatibility).
</verification>

<success_criteria>
- artifacts.toml declares all 16 published manifest artifacts with full metadata, in manifest order (SEAM-01).
- artifacts.py is a tested, stdlib-only module exposing the 5 verbs + classification predicates (SEAM-02).
- Fail-loud invariants enforced + covered by pytest: unknown artifact, metadata⇒no filename, authoritative⇒not baseline_diff, every manifest key resolves to exactly one artifact (SEAM-02, SEAM-04).
- A synthetic authoritative artifact is forced baseline_diff=false and excluded from the baseline pull set (SEAM-04 established before any authoritative data exists).
- The two regimes are machine-distinguished (authoritative⇒baseline_diff=false enforced); the prose documentation lands in Plan 03 (SEAM-05).
</success_criteria>

<output>
Create `.planning/phases/176-build-seam-refoundation-thread-1/176-01-SUMMARY.md` when done.
</output>
