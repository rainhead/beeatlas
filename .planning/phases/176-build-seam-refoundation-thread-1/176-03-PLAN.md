---
phase: 176-build-seam-refoundation-thread-1
plan: 03
type: execute
wave: 2
depends_on: [176-01]
files_modified:
  - .github/workflows/deploy.yml
  - docs/adr/0002-derived-vs-authoritative-artifacts.md
  - CLAUDE.md
autonomous: true
requirements: [SEAM-03, SEAM-05]
must_haves:
  truths:
    - "deploy.yml's 'Fetch build-time data from S3' step iterates `python3 data/artifacts.py build-time-fetch` instead of the 6 hardcoded `jq -r .<key>` lines; the species_hosts tolerate-absence guard is preserved via the emitted `optional` flag (SEAM-03)"
    - "The build-time fetch runs under CI's bare system python3 (no uv/setup-python) because artifacts.py is stdlib-only — CI is a verification surface (memory feedback_ci_is_verification_surface)"
    - "The two schema-evolution regimes are documented as an ADR (docs/adr/0002) and pointed to from CLAUDE.md: derived = diff-against-live baseline + bypass-and-rebuild valid; authoritative = forward-only migrations only, rebuild/bypass verbs forbidden (SEAM-05)"
  artifacts:
    - path: .github/workflows/deploy.yml
      provides: "Build-time fetch driven by the declarative contract — the third and last hand-synced key list eliminated"
      contains: "artifacts.py build-time-fetch"
    - path: docs/adr/0002-derived-vs-authoritative-artifacts.md
      provides: "The SEAM-05 regime documentation + the stable-dir out-of-contract exclusion note"
      min_lines: 30
    - path: CLAUDE.md
      provides: "Known-State pointer to artifacts.toml as the single artifact contract + the ADR"
  key_links:
    - from: .github/workflows/deploy.yml
      to: data/artifacts.py
      via: "python3 data/artifacts.py build-time-fetch | while read key local optional"
      pattern: "artifacts\\.py build-time-fetch"
    - from: CLAUDE.md
      to: docs/adr/0002-derived-vs-authoritative-artifacts.md
      via: "Known State reference"
      pattern: "0002-derived-vs-authoritative"
---

<objective>
Eliminate the third and final hand-synced key list — deploy.yml's 6 hardcoded build-time fetches — by driving it from `artifacts.py build-time-fetch` (SEAM-03), and document the two schema-evolution regimes as an ADR referenced from CLAUDE.md (SEAM-05). Independent of Plan 02 (touches deploy.yml/docs/CLAUDE.md, not nightly.sh) — both depend only on Plan 01.

Purpose: Complete SEAM-03 (no hand-synced key list survives in any of the three sites) and land SEAM-05's prose documentation on top of Plan 01's machine enforcement.
Output: A contract-driven deploy.yml fetch step, `docs/adr/0002-derived-vs-authoritative-artifacts.md`, and a CLAUDE.md pointer.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/176-build-seam-refoundation-thread-1/176-01-SUMMARY.md

# The files being changed + the contract/CLI + the ADR format analog:
@.github/workflows/deploy.yml
@data/artifacts.py
@data/artifacts.toml
@docs/adr/0001-mapbox-basemap-cache.md
@CLAUDE.md
</context>

<artifacts_this_plan_produces>
- Modified `.github/workflows/deploy.yml` — the "Fetch build-time data from S3" step consumes `artifacts.py build-time-fetch`.
- New `docs/adr/0002-derived-vs-authoritative-artifacts.md` — SEAM-05 regime documentation.
- Modified `CLAUDE.md` — Known-State pointer.
</artifacts_this_plan_produces>

<tasks>

<task type="auto">
  <name>Task 1: Drive deploy.yml's build-time fetch from the contract</name>
  <files>.github/workflows/deploy.yml</files>
  <read_first>
    - .github/workflows/deploy.yml lines 34–67 (the current step: manifest cp, `cp manifest.json public/data/`, the 5 required `jq -r .<key>` fetches, and the guarded `.species_hosts // empty` fetch)
    - data/artifacts.py (the `build-time-fetch` verb: emits `name\tlocal\toptional` for the 6 build_time_fetch artifacts, species_hosts optional=true)
  </read_first>
  <action>
    In the "Fetch build-time data from S3" step, keep the `mkdir -p public/data`, the `aws s3 cp .../manifest.json /tmp/manifest.json`, and the `cp /tmp/manifest.json public/data/manifest.json` (validate-db needs it) lines. Replace the 5 hardcoded required fetches AND the guarded species_hosts fetch (lines ~49–67) with a single loop:

    `python3 data/artifacts.py build-time-fetch` piped into `while IFS=$'\t' read -r key local optional; do ...`. Inside the loop resolve `HASHED=$(jq -r --arg k "$key" '.[$k] // empty' /tmp/manifest.json)`; if `HASHED` is empty then — if `optional` == "true" echo a "not yet in manifest (pre-first-nightly); loader tolerates absence" note and `continue`, else `echo "ERROR: required build-time artifact $key missing from manifest" >&2; exit 1`; otherwise `aws s3 cp s3://${{ vars.S3_BUCKET_NAME }}/data/$HASHED public/data/$local`. This preserves the exact current behavior: the 5 required artifacts hard-fail if absent, species_hosts tolerates absence.

    The `build` job already checks out the repo (so `data/artifacts.py` + `data/artifacts.toml` are present) and runs on ubuntu-latest whose system `python3` (3.12) has `tomllib` — artifacts.py is stdlib-only, so NO setup-python / uv step is needed. Update the step's leading comment to say the fetch list is now sourced from `data/artifacts.toml` via `artifacts.py build-time-fetch` (drop the stale enumeration of the six filenames).
  </action>
  <acceptance_criteria>
    - `python3 data/artifacts.py build-time-fetch` emits exactly 6 tab-separated rows; the species_hosts row's 3rd field is `true`, all others `false` (this is the CI path — runs under bare python3)
    - The hardcoded fetch variables are gone: `grep -c 'SPECIES_FILE=\$(jq' .github/workflows/deploy.yml` == 0, and `grep -c 'SPECIES_HOSTS_FILE=\$(jq' .github/workflows/deploy.yml` == 0
    - `grep -q 'artifacts.py build-time-fetch' .github/workflows/deploy.yml` succeeds
    - The manifest copy for validate-db is retained: `grep -q 'cp /tmp/manifest.json public/data/manifest.json' .github/workflows/deploy.yml` succeeds
    - YAML still parses: `python3 -c "import sys; sys.exit(0)"` after `python3 - <<'PY'\nimport yaml,sys\nyaml.safe_load(open('.github/workflows/deploy.yml'))\nPY` (or `ruby -ryaml` / `yq` if available) exits 0
  </acceptance_criteria>
  <verify>
    <automated>test "$(python3 data/artifacts.py build-time-fetch | wc -l | tr -d ' ')" = "6" && grep -q 'artifacts.py build-time-fetch' .github/workflows/deploy.yml && test "$(grep -c 'SPECIES_FILE=$(jq\|SPECIES_HOSTS_FILE=$(jq' .github/workflows/deploy.yml)" = "0"</automated>
  </verify>
  <done>deploy.yml's build-time fetch is contract-driven, runs under bare CI python3, hard-fails on a missing required artifact, and tolerates a missing species_hosts — no hardcoded fetch key list remains.</done>
</task>

<task type="auto">
  <name>Task 2: Document the two schema-evolution regimes (ADR) + CLAUDE.md pointer</name>
  <files>docs/adr/0002-derived-vs-authoritative-artifacts.md, CLAUDE.md</files>
  <read_first>
    - docs/adr/0001-mapbox-basemap-cache.md (the ADR format/heading style to mirror)
    - .planning/STATE.md lines 60–78 (the v8.0 derived-vs-authoritative anchoring decisions this ADR records)
    - .planning/REQUIREMENTS.md lines 19–23 (SEAM-01..05) and line 77 (the "notes as derived data = category error" out-of-scope row)
    - CLAUDE.md "Known State" section (where the pointer goes)
  </read_first>
  <action>
    Write `docs/adr/0002-derived-vs-authoritative-artifacts.md` (ADR format matching 0001: Status / Context / Decision / Consequences). Record:
    - The single contract: `data/artifacts.toml` + `data/artifacts.py` are the sole source of truth for every published manifest artifact; the three former hand-synced sites (nightly.sh publish/manifest block, nightly.sh baseline-classifier heredoc, deploy.yml build-time fetch) now consume it; adding/changing an artifact edits only the toml.
    - The `derived` vs `authoritative` classification: provenance follows the data's ULTIMATE source, not its production mechanism — any byte tracing to a user write is `authoritative`; everything else is `derived`. `notes.json` (Phase 179) will be mechanically a projection but classified `authoritative`.
    - The two schema-evolution regimes as DISTINCT and their enforcement:
      · `derived` = diff-against-live baseline (`baseline_diff=true`, pulled + diffed by `test_dbt_diff`/block-1c) AND bypass-and-rebuild valid (`SKIP_INTEGRATION_GATE=1` publish is a legitimate one-time verb for an intended contract change).
      · `authoritative` = forward-only migrations only; NEVER a dbt model, `baseline_diff` machine-forced false so block-1c never pulls/diffs it; rebuild/bypass verbs are FORBIDDEN (there is no source to rebuild from — "rebuild" = "delete").
    - The machine enforcement points (in artifacts.py, Plan 01): `validate()` rejects `authoritative` + `baseline_diff=true`; authoritative artifacts are structurally absent from `baseline-pull-plan`; an unknown/unclassified manifest key fails loud.
    - The documented exclusion: the three stable-directory recursive publishes (`feeds/`, `species-maps/`, `place-maps/`) are intentionally OUT of the artifact contract (not manifest artifacts, no single local filename/hash) and remain hardcoded in nightly.sh — a scoped, recorded exclusion, not a silent gap.

    Then add a concise pointer in CLAUDE.md's "Known State" bullet list: `data/artifacts.toml` (+ tested `data/artifacts.py`) is the single declarative contract for every published artifact — every artifact carries a `derived`|`authoritative` classification and the two schema-evolution regimes are enforced (authoritative ⇒ never a dbt model, `baseline_diff=false`, forward-only migrations); see docs/adr/0002. Keep it one or two lines, link-not-duplicate (per the user's README/docs convention).
  </action>
  <acceptance_criteria>
    - `docs/adr/0002-derived-vs-authoritative-artifacts.md` exists and contains the strings "derived", "authoritative", "forward-only", "baseline_diff", and "artifacts.toml"
    - The ADR explicitly states rebuild/bypass verbs are forbidden for authoritative data and that stable-dir publishes are an intentional out-of-contract exclusion
    - CLAUDE.md references `0002-derived-vs-authoritative` and names `data/artifacts.toml` as the single artifact contract: `grep -q 'artifacts.toml' CLAUDE.md` and `grep -q '0002-derived-vs-authoritative' CLAUDE.md` both succeed
  </acceptance_criteria>
  <verify>
    <automated>test -f docs/adr/0002-derived-vs-authoritative-artifacts.md && grep -q 'forward-only' docs/adr/0002-derived-vs-authoritative-artifacts.md && grep -q 'artifacts.toml' CLAUDE.md && grep -q '0002-derived-vs-authoritative' CLAUDE.md</automated>
  </verify>
  <done>The ADR documents both regimes + the stable-dir exclusion; CLAUDE.md points to the contract and the ADR.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CI (GitHub Actions) → S3 read | The `build` job assumes an OIDC role for read-only S3 access to fetch build-time data. Unchanged by this plan — same role, same bucket, same objects; only the LIST of keys moves from hardcoded to contract-derived. |
| Python → S3 | Preserved: `artifacts.py build-time-fetch` emits key/local/optional rows as text; the `aws s3 cp` stays in the workflow shell. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-176-05 | Tampering | Contract drift → deploy fetches the wrong build-time file (stale species page) | mitigate | The 6-row build-time-fetch set is locked by Plan 01's set-equality test; the CI step runs the same verb, so a drift turns the pytest red before deploy; required-vs-optional behavior preserved (hard-fail on a missing required artifact) |
| T-176-06 | Information disclosure | Secrets in deploy.yml | accept | Unchanged — OIDC role assumption + `vars.S3_BUCKET_NAME`; no secret added; the contract is public config |
| T-176-SC | Tampering | Supply chain | accept | No install; artifacts.py stdlib-only runs on CI's pre-existing python3 (no new action, no new package) |
</threat_model>

<verification>
- `python3 data/artifacts.py build-time-fetch` → 6 rows, species_hosts optional=true (CI path, bare python3).
- grep gates: no `SPECIES_FILE=$(jq` / `SPECIES_HOSTS_FILE=$(jq`; `artifacts.py build-time-fetch` present; manifest copy retained.
- deploy.yml YAML parses.
- ADR exists with both regimes + exclusion note; CLAUDE.md points to it.
- `cd data && uv run pytest tests/test_artifacts.py -q` green (the build-time-fetch set floor).
</verification>

<success_criteria>
- The third hand-synced key list is eliminated: deploy.yml consumes the contract; no hand-synced key list survives in any of the three former sites (SEAM-03).
- The build-time fetch runs under CI's bare python3 (stdlib-only), preserving the species_hosts tolerate-absence guard.
- The two schema-evolution regimes are documented (ADR 0002) and pointed to from CLAUDE.md, complementing Plan 01's machine enforcement (SEAM-05).
</success_criteria>

<output>
Create `.planning/phases/176-build-seam-refoundation-thread-1/176-03-SUMMARY.md` when done.
</output>
