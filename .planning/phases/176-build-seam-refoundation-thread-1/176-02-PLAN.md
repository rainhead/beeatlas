---
phase: 176-build-seam-refoundation-thread-1
plan: 02
type: execute
wave: 2
depends_on: [176-01]
files_modified:
  - data/nightly.sh
autonomous: true
requirements: [SEAM-02, SEAM-03]
must_haves:
  truths:
    - "nightly.sh's publish block uploads each hashed artifact by iterating `artifacts.py publish-plan` rows (via the existing _upload_hashed / _upload_hashed_gz helpers) and assembles manifest.json via `artifacts.py manifest` — no hardcoded manifest key list remains (SEAM-03)"
    - "nightly.sh's ~70-line inline baseline-classifier heredoc is replaced by iterating `artifacts.py baseline-pull-plan`; LOCAL_NAMES / NON_FILE_KEYS / INTENTIONALLY_SKIPPED are gone (SEAM-02, SEAM-03)"
    - "All S3/CloudFront I/O (aws cp, invalidation, occurrences_db_tables sqlite read, generated_at timestamp) stays in bash; Python is called only for plans/classification (CLAUDE.md invariant preserved)"
    - "First-run/skip/WARN-on-drift/`|| true` semantics of the baseline pull are preserved; the manifest upload (no-cache) and the stable-dir syncs + CloudFront invalidation are byte-unchanged"
  artifacts:
    - path: data/nightly.sh
      provides: "Publish + baseline-pull driven by the declarative contract instead of hand-synced key lists"
      contains: "artifacts.py"
  key_links:
    - from: data/nightly.sh
      to: data/artifacts.py
      via: "publish-plan (upload loop), manifest (JSON assembly), baseline-pull-plan (baseline pull)"
      pattern: "artifacts\\.py (publish-plan|manifest|baseline-pull-plan)"
---

<objective>
Rewire the two hand-synced key lists in `data/nightly.sh` — the publish/manifest block (lines ~306–349) and the inline baseline-classifier heredoc (lines ~136–210) — to consume the Plan 01 contract via `artifacts.py` verbs. Pure refactor: the manifest stays byte-identical and the baseline pull yields the identical 9-file set. All S3 I/O stays in bash.

Purpose: Eliminate two of the three hand-synced sites (SEAM-03) and replace the inline classifier heredoc with tested module calls (SEAM-02).
Output: A modified `data/nightly.sh` that derives its publish, manifest, and baseline-pull behavior entirely from `data/artifacts.toml`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/176-build-seam-refoundation-thread-1/176-01-SUMMARY.md

# The file being rewired + the contract/CLI it now consumes:
@data/nightly.sh
@data/artifacts.py
@data/artifacts.toml
</context>

<artifacts_this_plan_produces>
No new files. Modifies `data/nightly.sh` to call `python3 "$SCRIPT_DIR/artifacts.py" {publish-plan,manifest,baseline-pull-plan}`. Invoke via script path (not `-m`) so it is CWD-robust; `python3` (not `uv run`) is acceptable since artifacts.py is stdlib-only, but keep the surrounding `uv run` idiom if the executor prefers — either resolves. `SCRIPT_DIR` is `data/`.
</artifacts_this_plan_produces>

<tasks>

<task type="auto">
  <name>Task 1: Drive the publish + manifest assembly from the contract</name>
  <files>data/nightly.sh</files>
  <read_first>
    - data/nightly.sh lines 265–349 (the `_upload_hashed` / `_upload_hashed_gz` helpers, the 14 upload calls, the occ_db_tables sqlite read, the manifest heredoc, the no-cache manifest upload)
    - data/artifacts.py (the `publish-plan` and `manifest` verb contracts + the exact TSV field order)
    - data/artifacts.toml (the 14 hashed artifacts + 2 metadata keys)
  </read_first>
  <action>
    Replace the 14 hand-written `_upload_hashed*` calls (lines ~306–325) and the manifest heredoc (lines ~327–346) with a contract-driven loop. Keep the `_upload_hashed` and `_upload_hashed_gz` helper definitions unchanged, and keep the `occ_db_tables` sqlite-read and the `generated_at` timestamp computed in bash (S3/local I/O stays in bash).

    Publish loop: iterate `python3 "$SCRIPT_DIR/artifacts.py" publish-plan` (fields: name, source_file, hash_basename, gzip, content_type). For each row build the optional-args array (`--content-type <ctype>` unless the field is `-`), call `_upload_hashed_gz "$EXPORT_DIR/<source_file>" "<hash_basename>" "${args[@]}"` when gzip is `true` else `_upload_hashed`, and store the returned hashed name in a bash associative array keyed by logical `name`. `_upload_hashed*` still echo the hashed filename to stdout and the aws cp to stderr, so `h=$(_upload_hashed ...)` captures only the hashed name (unchanged).

    Manifest assembly: write the `name<TAB>hashed` pairs from the associative array to a `mktemp` file, then `python3 "$SCRIPT_DIR/artifacts.py" manifest "$MAPFILE" --meta occurrences_db_tables="$occ_db_tables" --meta generated_at="$(_ts)" > "$EXPORT_DIR/manifest.json"`. Leave the subsequent `aws s3 cp ... --cache-control no-cache ... manifest.json` upload, the three stable-dir recursive syncs (feeds/, species-maps/, place-maps/), and the CloudFront invalidation entirely unchanged.
  </action>
  <acceptance_criteria>
    - `bash -n data/nightly.sh` exits 0 (syntax valid)
    - `grep -c '_upload_hashed "\$EXPORT_DIR' data/nightly.sh` returns 0 — the hand-written per-artifact upload calls are gone (replaced by the loop)
    - `grep -q 'artifacts.py publish-plan' data/nightly.sh` and `grep -q 'artifacts.py manifest' data/nightly.sh` both succeed
    - The manifest heredoc key list is gone: `grep -c '"occurrences": "\$occ_name"' data/nightly.sh` returns 0
    - occ_db_tables sqlite read and `_ts` generated_at are still computed in bash (grep confirms `sqlite3.connect` and `--meta generated_at` present); no `aws` call appears inside any `python3 artifacts.py` invocation
    - The manifest verb + byte-exact golden is already locked by `cd data && uv run pytest tests/test_artifacts.py -q` (green)
  </acceptance_criteria>
  <verify>
    <automated>bash -n data/nightly.sh && grep -q 'artifacts.py publish-plan' data/nightly.sh && grep -q 'artifacts.py manifest' data/nightly.sh && test "$(grep -c '"occurrences": "\$occ_name"' data/nightly.sh)" = "0"</automated>
  </verify>
  <done>The publish loop and manifest assembly are contract-driven; no hardcoded artifact/manifest key list remains in the publish block; syntax valid; the byte-exact manifest is guaranteed by Plan 01's golden test.</done>
</task>

<task type="auto">
  <name>Task 2: Replace the inline baseline-classifier heredoc with baseline-pull-plan</name>
  <files>data/nightly.sh</files>
  <read_first>
    - data/nightly.sh lines 118–210 (block 1c: the `_PREV_MANIFEST` pull, the `uv run python3 -c "..."` heredoc with LOCAL_NAMES/NON_FILE_KEYS/INTENTIONALLY_SKIPPED, the pulled/skipped/failed/drift summary, the `|| true`)
    - data/artifacts.py (the `baseline-pull-plan` verb: emits `name\thashed\tsource_file` rows for baseline_diff artifacts; WARNs on unknown/drift; skips metadata + non-baseline)
  </read_first>
  <action>
    Replace the entire `uv run python3 -c "..."` heredoc (lines ~136–206) with a bash loop over `python3 "$SCRIPT_DIR/artifacts.py" baseline-pull-plan "$_PREV_MANIFEST"`. For each emitted row (name, hashed, local) run `aws --profile "$AWS_PROFILE" s3 cp --no-progress "s3://$BUCKET/data/$hashed" "$REPO_ROOT/public/data/$local"`, tracking pulled/failed counts and emitting the same `baseline pull: N pulled, ... failed` summary and the same WARN-on-failure behavior. The verb prints drift WARNs (unknown manifest keys) to stderr itself, so the bash side no longer maintains any classification set. Preserve the outer `if aws ... cp manifest.json "$_PREV_MANIFEST"` / `else WARN "no manifest.json in S3 (first run)"` structure and the trailing `|| true` so a partial baseline never aborts the run before the integration gate. Keep the A3 comment intent (species→species.json) — the contract already encodes it, so the note can point to artifacts.toml.
  </action>
  <acceptance_criteria>
    - `bash -n data/nightly.sh` exits 0
    - The hand-maintained classification sets are gone: `grep -c 'LOCAL_NAMES' data/nightly.sh` == 0, `grep -c 'NON_FILE_KEYS' data/nightly.sh` == 0, `grep -c 'INTENTIONALLY_SKIPPED' data/nightly.sh` == 0
    - `grep -q 'artifacts.py baseline-pull-plan' data/nightly.sh` succeeds
    - The first-run skip branch and `|| true` are preserved: `grep -q 'no manifest.json in S3' data/nightly.sh` succeeds
    - `python3 data/artifacts.py baseline-pull-plan <a fixture manifest>` emits exactly the 9 baseline rows (locked by Plan 01's set-equality test); an unknown key in the fixture produces a stderr WARN and no row
  </acceptance_criteria>
  <verify>
    <automated>bash -n data/nightly.sh && grep -q 'artifacts.py baseline-pull-plan' data/nightly.sh && test "$(grep -c 'LOCAL_NAMES\|NON_FILE_KEYS\|INTENTIONALLY_SKIPPED' data/nightly.sh)" = "0"</automated>
  </verify>
  <done>Block 1c's inline classifier is replaced by a baseline-pull-plan loop; no LOCAL_NAMES/NON_FILE_KEYS/INTENTIONALLY_SKIPPED remain; first-run + failure semantics preserved.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Python → S3 | `artifacts.py` emits pull/publish plans as text; every `aws s3 cp` / CloudFront call stays in `nightly.sh`. This plan must NOT move any S3 call into Python (CLAUDE.md invariant). |
| S3 (last night's live manifest) → nightly baseline | Untrusted only in the sense of drift: a manifest key with no contract entry is surfaced as a WARN, never silently pulled or skipped. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-176-03 | Tampering | A wiring bug in the publish loop → wrong hashed name in manifest, or a stale/incomplete baseline pull | mitigate | Manifest byte-identity is locked by Plan 01's golden; the 9-row baseline set is locked by Plan 01's set-equality test; `bash -n` + grep gates confirm the hand-synced lists are gone and the verb calls are present; the real end-to-end gate is the maderas nightly `run.sh build` (Plan 04 operator verify) |
| T-176-04 | Denial of service | Baseline pull failure aborting the publish prematurely | accept | Preserved existing behavior — `|| true` keeps the run going; the integration gate (not the pull) decides publish |
| T-176-SC | Tampering | Supply chain | accept | No package install; no new dependency; artifacts.py stdlib-only |
</threat_model>

<verification>
- `bash -n data/nightly.sh` clean.
- grep gates: no `LOCAL_NAMES`/`NON_FILE_KEYS`/`INTENTIONALLY_SKIPPED`; no hardcoded manifest heredoc key line; `publish-plan`/`manifest`/`baseline-pull-plan` all present.
- `cd data && uv run pytest tests/test_artifacts.py -q` green (the behavioral floor — nightly.sh only calls these verbs).
- Real contract gate deferred to the first post-merge maderas nightly (Plan 04).
</verification>

<success_criteria>
- Two of the three hand-synced sites eliminated: the publish/manifest block and the baseline-classifier heredoc now derive from artifacts.toml (SEAM-03).
- The ~70-line inline classifier heredoc is replaced by tested `artifacts.py` calls (SEAM-02).
- Manifest byte-identical; baseline pull yields the identical 9-file set; all S3 I/O remains in bash.
</success_criteria>

<output>
Create `.planning/phases/176-build-seam-refoundation-thread-1/176-02-SUMMARY.md` when done.
</output>
