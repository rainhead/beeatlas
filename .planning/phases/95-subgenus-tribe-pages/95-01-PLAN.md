---
phase: 95
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - _data/species.js
  - _pages/subgenus.njk
  - src/tests/data-species.test.ts
  - src/tests/build-output.test.ts
autonomous: true
requirements:
  - URL-03
  - SUBG-01
  - SUBG-02
  - SUBG-03
tags:
  - eleventy
  - subgenus
  - taxon-pages
  - ssg
must_haves:
  truths:
    - "Visiting /species/Andrena/Melandrena/ returns a static HTML page after `npm run build`"
    - "The subgenus page lists species in the subgenus with per-species color swatches and occurrence counts"
    - "The subgenus page embeds the multi-color subgenus SVG occurrence map"
    - "Each species entry on the subgenus page links to /species/{Genus}/{specificEpithet}/"
    - "Subgenus species swatch hex colors match the same species' fill in the subgenus SVG"
  artifacts:
    - path: "_data/species.js"
      provides: "subgenusList default-export key consumed by Eleventy pagination"
      contains: "subgenusList"
    - path: "_pages/subgenus.njk"
      provides: "Eleventy template that paginates species.subgenusList"
      contains: "species.subgenusList"
    - path: "_site/species/Andrena/Melandrena/index.html"
      provides: "Built subgenus page (post-build artifact, asserted by build-output test)"
  key_links:
    - from: "_pages/subgenus.njk"
      to: "species.subgenusList"
      via: "Eleventy pagination front matter"
      pattern: "data:\\s*species\\.subgenusList"
    - from: "_pages/subgenus.njk"
      to: "/data/species-maps/subgenus/{Genus}/{Subgenus}.svg"
      via: "<img> src attribute"
      pattern: "/data/species-maps/subgenus/"
    - from: "subgenusList[i].species[j]"
      to: "/species/{Genus}/{specificEpithet}/"
      via: "Nunjucks <a href> using sp.slug"
      pattern: "href=\"/species/\\{\\{ sp.slug \\}\\}/\""
---

<objective>
Add subgenus static pages: extend `_data/species.js` with a `subgenusList` data
key and create `_pages/subgenus.njk` that paginates over it, producing one
static HTML page per (genus, subgenus) pair with occurrences. Pages render the
pre-built multi-color subgenus SVG (Phase 93 output) plus a swatch-decorated
species list whose color indices exactly match the SVG's per-species hues.

Purpose: Implements URL-03, SUBG-01, SUBG-02, SUBG-03 — closes the "subgenus"
half of Phase 95.

Output: 103 new `_site/species/{Genus}/{Subgenus}/index.html` pages at build
time; new `subgenusList` key on the species data default export; new unit and
build-output tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/95-subgenus-tribe-pages/95-RESEARCH.md
@.planning/phases/95-subgenus-tribe-pages/95-PATTERNS.md
@.planning/phases/95-subgenus-tribe-pages/95-UI-SPEC.md
@.planning/phases/95-subgenus-tribe-pages/95-VALIDATION.md
@_data/species.js
@_pages/genus.njk
@src/tests/data-species.test.ts
@src/tests/build-output.test.ts

<interfaces>
<!-- Existing exports this plan extends. Executor should reuse, not duplicate. -->

From _data/species.js (existing default export — extend, do not replace):
```javascript
export default { tree, flat, byScientificName, counties, ecoregionL3, speciesList, genusList };
```
After this plan it must read:
```javascript
export default { tree, flat, byScientificName, counties, ecoregionL3, speciesList, genusList, subgenusList };
```
(Plan 95-02 will further extend with `tribeList`.)

From _data/species.js (existing helper — call directly, do NOT duplicate):
```javascript
function hslToHex(h, s, l) { /* HSL→hex matching Python colorsys.hls_to_rgb */ }
```

species.json record fields used by subgenusList:
  genus (string), subgenus (string|null|""), family, subfamily, tribe,
  canonical_name, specific_epithet (string|null), occurrence_count (number),
  scientificName, slug ("Genus/specificEpithet" — already hierarchical post-Phase 92).

From _pages/genus.njk (reference template — copy/adapt structure exactly):
  pagination front matter, breadcrumb shape, `<h1>` in `<em>`, `.metadata`
  line, `.media-grid`, `.species-list`, `.swatch`, `loading="lazy"`,
  `<script type="module" src="/src/entries/taxon-page.ts"></script>`.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add subgenusList to _data/species.js with parity-correct color index</name>
  <files>_data/species.js, src/tests/data-species.test.ts</files>
  <read_first>
    - _data/species.js (lines 76-139 — hslToHex helper and existing genusList block; insertion point is just before the `export default` on line 139)
    - .planning/phases/95-subgenus-tribe-pages/95-RESEARCH.md (Pattern 2 subgenusList block; Pitfall 1 color index mismatch; Pitfall 2 unresolved-only groups; Open Questions recommendation)
    - .planning/phases/95-subgenus-tribe-pages/95-PATTERNS.md ("`_data/species.js` — extend with `subgenusList`" section, full diff guidance)
    - .planning/phases/95-subgenus-tribe-pages/95-UI-SPEC.md (Data Requirements section — exact `species.subgenusList` shape contract)
    - src/tests/data-species.test.ts (lines 40-65 — analog `genusList` test pattern to mirror)
    - public/data/species.json (verify field names: subgenus, tribe, canonical_name, specific_epithet, occurrence_count, slug, scientificName, family, subfamily)
  </read_first>
  <behavior>
    - Test: `species.subgenusList` is an array with length > 0 (103 expected groups; assertion: `> 50`).
    - Test: A `find(g => g.genus === 'Andrena' && g.subgenus === 'Melandrena')` returns a defined entry with numeric `speciesCount` and numeric `totalOccurrences`.
    - Test: `subgenusList` species array for Andrena/Melandrena is sorted alphabetically by `canonical_name`.
    - Test: Every `subgenusList[i].species[j]` has `specific_epithet !== null` (display list excludes unresolved records).
    - Test: For Andrena/Melandrena, every species has a `hexColor` matching `/^#[0-9a-f]{6}$/`.
    - Test: For at least one subgenus group containing both unresolved and resolved members (e.g., choose any group in species.json where the resolved species' alphabetical position would shift if unresolved records were excluded), the FIRST resolved species' `hexColor` equals `hslToHex(i * 360 / n, 70, 50)` where `i` is its zero-based index in the `withOcc` (resolved + unresolved sorted by canonical_name) array and `n = withOcc.length`. This pins the color-index parity invariant from Pitfall 1.
    - Test: `subgenusList.every(g => g.totalOccurrences > 0)` — the final filter excludes zero-occurrence groups.
  </behavior>
  <action>
    Step 1 (RED): In `src/tests/data-species.test.ts`, append new test cases inside the existing `describe('_data/species.js (PAGE-02)', ...)` block. Use `find()` lookups against `(species as any).subgenusList`. Pin tests to:
      (a) shape — `subgenusList` is an array, length > 50, Andrena/Melandrena entry defined with numeric `speciesCount` and `totalOccurrences`;
      (b) display list — every `species[j].specific_epithet !== null`;
      (c) sort — Andrena/Melandrena `species` array sorted by `canonical_name` (use the `[...names].sort(localeCompare)` equality pattern from existing genusList tests);
      (d) color parity — pick the first resolved species in Andrena/Melandrena and assert its `hexColor` equals the value computed by replicating the formula `hslToHex(i * 360 / n, 70, 50)` with `i` derived from its position in the resolved+unresolved canonical-name-sorted `withOcc` array. Compute the expected hex literal at test-write time by inspecting species.json (do not call hslToHex inside the test — assert against a frozen literal so the test fails if the production formula drifts);
      (e) totals filter — `subgenusList.every(g => g.totalOccurrences > 0)`.
    Run `VITEST_SKIP_BUILD=1 npm test -- data-species` and confirm new tests fail (RED).

    Step 2 (GREEN): In `_data/species.js`, insert a `subgenusList` block immediately after the existing `genusList` block (after line 137, before the `export default`). The block must:
      - Iterate `flat` and skip records where `sp.subgenus` is null, undefined, or trims to empty string.
      - Group on the composite key ``${sp.genus}::${sp.subgenus}`` into a `subgenusMap` whose values carry `{ genus, subgenus, family, subfamily, tribe, allMembers: [] }`.
      - Build the result by `Object.values(subgenusMap)` then `.sort((a, b) => a.genus.localeCompare(b.genus) || a.subgenus.localeCompare(b.subgenus))`.
      - Per group, compute `withOcc = allMembers.filter(sp => sp.occurrence_count > 0).sort((a, b) => a.canonical_name.localeCompare(b.canonical_name))`.
      - `n = withOcc.length`.
      - `colorByCanon = Object.fromEntries(withOcc.map((sp, i) => [sp.canonical_name, sp.specific_epithet !== null ? hslToHex(i * 360 / n, 70, 50) : '#aaaaaa']))` — unresolved records keep `#aaaaaa` exactly (matches Python `_UNRESOLVED_COLOR`); resolved species use the HSL formula via the existing `hslToHex` helper (do NOT redefine it).
      - `species = withOcc.filter(sp => sp.specific_epithet !== null).map(sp => ({ ...sp, hexColor: colorByCanon[sp.canonical_name] }))`.
      - Return shape: `{ genus, subgenus, family, subfamily, tribe, species, speciesCount: species.length, totalOccurrences: withOcc.reduce((acc, sp) => acc + sp.occurrence_count, 0) }`. NOTE: `totalOccurrences` sums `withOcc` (all members including unresolved), per RESEARCH Pattern 2 — this is the value that matches the SVG's record total.
      - Append `.filter(g => g.totalOccurrences > 0)` as the final terminal step (excludes zero-occurrence groups; required because some pipeline rows may have `occurrence_count === 0`).

    Step 3 (GREEN): Extend the existing `export default { ... }` line to add `subgenusList` as a key. Do NOT add named exports (named exports break the Eleventy data cascade — see RESEARCH "Don't Hand-Roll" and CLAUDE.md/Phase 94 PATTERNS constraint).

    Step 4: Run `VITEST_SKIP_BUILD=1 npm test -- data-species` and confirm all new tests pass.

    Do NOT modify the existing `genusList` block, the `hslToHex` helper, or any other section of `_data/species.js`. Do NOT add a new file.
  </action>
  <verify>
    <automated>VITEST_SKIP_BUILD=1 npm test -- data-species</automated>
  </verify>
  <acceptance_criteria>
    - `_data/species.js` default export contains the key `subgenusList` (grep: `grep -E "export default \\{[^}]*subgenusList" _data/species.js` matches).
    - `_data/species.js` does NOT contain any line matching `^export (const|function|let|var|\\{) ` other than the existing `export default` (no named exports introduced; grep: `grep -nE "^export " _data/species.js | grep -v "^.*:export default" | wc -l` returns 0).
    - `_data/species.js` does NOT contain a second `function hslToHex` definition (grep: `grep -c "function hslToHex" _data/species.js` returns 1).
    - `VITEST_SKIP_BUILD=1 npm test -- data-species` exits 0 with all new subgenusList tests passing.
    - Node script `node -e "import('./_data/species.js').then(m => { const l = m.default.subgenusList; console.log(l.length, l.find(g => g.genus==='Andrena' && g.subgenus==='Melandrena')?.species?.length); })"` prints two integers (group count > 50, Melandrena species count > 0).
  </acceptance_criteria>
  <done>
    `subgenusList` is exposed via the default export with the contract from 95-UI-SPEC.md Data Requirements; color indices match Python `_group_colors`; new unit tests pass under `VITEST_SKIP_BUILD=1 npm test -- data-species`.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create _pages/subgenus.njk and build-output assertions</name>
  <files>_pages/subgenus.njk, src/tests/build-output.test.ts</files>
  <read_first>
    - _pages/genus.njk (entire 33-line file — exact analog template to copy/adapt)
    - .planning/phases/95-subgenus-tribe-pages/95-PATTERNS.md ("`_pages/subgenus.njk`" section — line-by-line adaptation guide)
    - .planning/phases/95-subgenus-tribe-pages/95-UI-SPEC.md (Layout Contract — Subgenus Page Layout; Copywriting Contract; SVG Map Dimensions and Paths)
    - .planning/phases/95-subgenus-tribe-pages/95-RESEARCH.md (Pattern 4 subgenus template; Pitfall 2 empty-state handling)
    - src/tests/build-output.test.ts (entire file — `describe.skipIf(SKIP_BUILD)` block, `findTaxonChunk`, existing genus page tests at lines 96-111 — analog pattern to mirror)
    - public/data/species-maps/subgenus/Andrena/ (confirm `Melandrena.svg` exists on disk before asserting against it)
    - public/data/species.json (confirm Andrena/Melandrena has at least one species with `specific_epithet !== null` AND `occurrence_count > 0` — pick a known species like `milwaukeensis` for the link assertion)
  </read_first>
  <behavior>
    - Build emits `_site/species/Andrena/Melandrena/index.html`.
    - Built page contains `<em>Melandrena</em>` (subgenus h1 in italics).
    - Built page contains the literal SVG path `/data/species-maps/subgenus/Andrena/Melandrena.svg`.
    - Built page contains `class="species-list"`.
    - Built page contains a link with `href="/species/Andrena/milwaukeensis/"` (or whichever Melandrena species is verified present in species.json with occurrences).
    - Every `<img>` on the subgenus page has `loading="lazy"`.
    - Built page contains breadcrumb anchor `<a href="/species/Andrena/">Andrena</a>` (SUBG-03 genus link in breadcrumb).
    - Built page does NOT contain `<seasonality-viz` (group pages have no seasonality chart per UI-SPEC).
    - Built page contains the script tag `src="/src/entries/taxon-page.ts"`.
  </behavior>
  <action>
    Step 1 (RED): In `src/tests/build-output.test.ts`, append new tests inside the existing `describe.skipIf(SKIP_BUILD)('build output (PAGE-07, PAGE-09)', ...)` block (do NOT create a new describe). Reuse `ROOT` and the existing `beforeAll` build invocation. Add tests:
      - `'emits _site/species/Andrena/Melandrena/index.html (SUBG-01, URL-03, PIPE-01)'` — read the file, expect `'<em>Melandrena</em>'`, `'/data/species-maps/subgenus/Andrena/Melandrena.svg'`, `'class="species-list"'`.
      - `'subgenus page links each species to its species page (SUBG-03)'` — match `/href="\\/species\\/Andrena\\/milwaukeensis\\/"/` (use a verified Andrena/Melandrena species; confirm against species.json before encoding).
      - `'subgenus page breadcrumb links to genus (SUBG-03)'` — match `/<a href="\\/species\\/Andrena\\/">Andrena<\\/a>/`.
      - `'every <img> on a subgenus page has loading="lazy" (SUBG-02 carry-forward)'` — pattern adapted from existing line 86-94.
      - `'subgenus page does not embed seasonality-viz'` — `expect(html).not.toContain('<seasonality-viz')`.
    Run `npm test -- build-output` (without VITEST_SKIP_BUILD) and confirm new tests fail because the file does not exist yet (RED).

    Step 2 (GREEN): Create `_pages/subgenus.njk`. Copy `_pages/genus.njk` as the structural base. Apply these exact substitutions:
      - Front matter: `data: species.subgenusList`, `alias: subgenus`, `permalink: "/species/{{ subgenus.genus }}/{{ subgenus.subgenus }}/"`, `title: "{{ subgenus.subgenus }} ({{ subgenus.genus }}) — BeeAtlas"`, `layout: default.njk`.
      - Breadcrumb: `{{ subgenus.family }}<span class="sep">/</span><a href="/species/{{ subgenus.genus }}/">{{ subgenus.genus }}</a><span class="sep">/</span>{{ subgenus.subgenus }}` (genus is a link; subgenus is plain text — current page).
      - h1: `<h1><em>{{ subgenus.subgenus }}</em></h1>` (subgenus IS in `<em>` — Latinized binomial part).
      - Metadata line: `<p class="metadata">{{ subgenus.speciesCount }} species · {{ subgenus.totalOccurrences }} records</p>`.
      - SVG `<img>`: `src="/data/species-maps/subgenus/{{ subgenus.genus }}/{{ subgenus.subgenus }}.svg"`, `alt="Occurrence map for subgenus {{ subgenus.subgenus }} ({{ subgenus.genus }})"`, keep `loading="lazy"` and `style="aspect-ratio: 15/8; width: 100%;"`.
      - Species list: copy the `<ul class="species-list">` loop verbatim, swapping `genus.species` → `subgenus.species`. Wrap the entire `<ul>` in `{%- if subgenus.speciesCount > 0 -%} ... {%- endif -%}` to silently omit the list for the 14 unresolved-only subgenus groups (per RESEARCH Open Questions recommendation; the SVG map still renders so users see occurrence locations).
      - Script tag: `<script type="module" src="/src/entries/taxon-page.ts"></script>` (verbatim).
    Do NOT introduce a new CSS file (`taxon-pages.css` already supplies `.taxon-page`, `.media-grid`, `.species-list`, `.swatch`, `.breadcrumb`, `.metadata`, `.sep`, `.count`).

    Step 3 (GREEN): Run `npm test -- build-output` and confirm all new subgenus tests pass. The full `build-output` test set must still be green (existing genus/species tests must not regress).
  </action>
  <verify>
    <automated>npm test -- build-output</automated>
  </verify>
  <acceptance_criteria>
    - `_pages/subgenus.njk` exists.
    - File contains literal `data: species.subgenusList` (grep: `grep -c "data: species.subgenusList" _pages/subgenus.njk` returns 1).
    - File contains literal `permalink: "/species/{{ subgenus.genus }}/{{ subgenus.subgenus }}/"` (grep returns 1).
    - File contains `<h1><em>{{ subgenus.subgenus }}</em></h1>` (grep returns 1).
    - File contains `loading="lazy"` (grep returns ≥ 1).
    - File contains `<a href="/species/{{ subgenus.genus }}/">{{ subgenus.genus }}</a>` (breadcrumb genus link).
    - File contains `{%- if subgenus.speciesCount > 0 -%}` guarding the `<ul class="species-list">` block.
    - File contains `<script type="module" src="/src/entries/taxon-page.ts"></script>` (grep returns 1).
    - `npm test -- build-output` exits 0; all new subgenus assertions and existing genus/species assertions pass.
    - After build, `_site/species/Andrena/Melandrena/index.html` exists (verify with `test -f _site/species/Andrena/Melandrena/index.html`).
    - After build, `find _site/species -mindepth 3 -maxdepth 3 -name index.html | grep -c "/[A-Z][a-z]*/[A-Z][a-z]*/index.html"` returns ≥ 50 (at least 50 capitalized/capitalized subgenus pages emitted, distinguishing from species pages whose second segment is lowercase).
  </acceptance_criteria>
  <done>
    Subgenus pages are emitted at `/species/{Genus}/{Subgenus}/` with multi-color SVG, swatch-decorated species list, genus-linked breadcrumb, and species-page links. New build-output tests pass under `npm test -- build-output`.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| build → static HTML | Eleventy renders trusted build-time data (species.json) into static HTML at SSG time; output is served as static files via CloudFront. No runtime user input crosses any boundary on these pages. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-95-01 | Tampering | species.json input → subgenus/tribe templates | accept | Build-time data is pipeline-controlled (data/dbt/ + species_export.py). No untrusted authoring path; species.json is regenerated nightly from S3 round-trip already gated by maderas crontab. |
| T-95-02 | Information Disclosure | rendered subgenus HTML | accept | Pages contain only public taxonomic and aggregate occurrence data already exposed via the atlas map and existing genus pages; no PII surfaces. |
| T-95-03 | Spoofing (URL collision) | `/species/{Genus}/{Subgenus}/` vs `/species/{Genus}/{specificEpithet}/` | mitigate | Capitalization-based structural disambiguation: subgenus names are capitalized, specific epithets are lowercase. Verified by RESEARCH Pitfall 3 against current species.json. Build will collide (Eleventy writes both to the same path) if a species ever gets a capitalized epithet — this would surface as a duplicate-key error or overwrite at build time, not a runtime exploit. No additional runtime guard required. |
</threat_model>

<verification>
- `VITEST_SKIP_BUILD=1 npm test -- data-species` exits 0 (Task 1 unit gate).
- `npm test -- build-output` exits 0 (Task 2 build gate).
- `_data/species.js` has exactly one `function hslToHex` definition and exactly one `export default` line.
- `_pages/subgenus.njk` exists; `_site/species/Andrena/Melandrena/index.html` exists after build.
- No new `.css` files added under `src/styles/`.
</verification>

<success_criteria>
- URL-03: Visiting `/species/Andrena/Melandrena/` (post-build) returns the subgenus page (verified by `test -f _site/species/Andrena/Melandrena/index.html`).
- SUBG-01: Subgenus page lists species in the subgenus with specimen counts (built page contains `class="species-list"` and `{{ sp.occurrence_count }} records` rendered text per species).
- SUBG-02: Subgenus page contains the multi-color subgenus SVG `<img>` referencing `/data/species-maps/subgenus/Andrena/Melandrena.svg`.
- SUBG-03: Each species `<a>` on the subgenus page targets `/species/{{ sp.slug }}/` (verified by `href="/species/Andrena/milwaukeensis/"` regex match).
- All Phase 94 build-output tests still pass (no regression).
</success_criteria>

<output>
After completion, create `.planning/phases/95-subgenus-tribe-pages/95-01-SUMMARY.md`
</output>
