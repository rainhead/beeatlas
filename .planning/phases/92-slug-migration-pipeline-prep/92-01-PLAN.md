---
phase: 92-slug-migration-pipeline-prep
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - data/tests/test_species_export.py
  - data/tests/test_species_maps.py
  - src/tests/validate-species.test.ts
autonomous: true
requirements:
  - PIPE-03
must_haves:
  truths:
    - "Running pytest on test_species_export.py fails BEFORE the species_export.py edit (RED state) and passes AFTER (GREEN state)"
    - "Running pytest on test_species_maps.py fails BEFORE the species_maps.py edit (RED state) and passes AFTER (GREEN state)"
    - "Existing test_dbt_diff.py slug-column assertion still passes (slug remains the last column of species.parquet)"
    - "validate-species.test.ts fixture slug value matches the new Genus/epithet conceptual format"
  artifacts:
    - path: "data/tests/test_species_export.py"
      provides: "PIPE-03a + PIPE-03b unit assertions for new slug format"
      contains:
        - "def test_slug_hierarchical"
        - "def test_no_old_slug_format"
    - path: "data/tests/test_species_maps.py"
      provides: "PIPE-03c unit assertion that _write_species_svg creates Genus/ subdir"
      contains:
        - "def test_write_species_svg_creates_subdir"
    - path: "src/tests/validate-species.test.ts"
      provides: "Updated fixture slug value 'Osmia/lignaria'"
  key_links:
    - from: "data/tests/test_species_export.py"
      to: "data/species_export.py"
      via: "import species_export as se_mod; from species_export import export_species_parquet, SPECIES_COLUMNS"
      pattern: "import species_export"
    - from: "data/tests/test_species_maps.py"
      to: "data/species_maps.py"
      via: "from species_maps import _write_species_svg"
      pattern: "from species_maps import"
---

<objective>
Wave 0: Create the failing test scaffolding for PIPE-03 BEFORE any code change. Tests assert the new `Genus/specificEpithet` slug format in `species_export.py` and the subdirectory write behavior in `species_maps.py`. Update the existing `validate-species.test.ts` fixture slug value for conceptual consistency.

Purpose: Establish the Nyquist-compliant feedback signal so Plan 02 executor sees RED → GREEN as it implements the slug migration. Without these tests, Plan 02 has no per-task automated verification beyond a full-pipeline `npm run build`.

Output:
- `data/tests/test_species_export.py` (new) — 2 unit tests covering PIPE-03a, PIPE-03b
- `data/tests/test_species_maps.py` (new) — 1 unit test covering PIPE-03c (pure-function `_write_species_svg` call with slug containing `/`)
- `src/tests/validate-species.test.ts` (modify line 14) — fixture slug `'osmia-lignaria'` → `'Osmia/lignaria'`
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/92-slug-migration-pipeline-prep/92-RESEARCH.md
@.planning/phases/92-slug-migration-pipeline-prep/92-PATTERNS.md
@.planning/phases/92-slug-migration-pipeline-prep/92-VALIDATION.md

<interfaces>
<!-- Key contracts the executor needs. Extracted from data/species_export.py and data/species_maps.py. -->

From data/species_export.py:
- `DB_PATH` (module attr, env-driven via `DB_PATH`)
- `ASSETS_DIR: Path` (module attr, env-driven via `EXPORT_DIR`; defaults to repo `public/data/`)
- `DBT_SANDBOX_DIR: Path` (module attr, env-driven via `DBT_SANDBOX_DIR`)
- `SPECIES_COLUMNS: list[str]` — 19 names, `'slug'` is the last entry
- `export_species_parquet(con: duckdb.DuckDBPyConnection) -> None`
  - Reads `DBT_SANDBOX_DIR/species.parquet` (18 cols, no slug)
  - Reads `DBT_SANDBOX_DIR/occurrences.parquet`
  - Writes `ASSETS_DIR/species.parquet`, `ASSETS_DIR/species.json`, `ASSETS_DIR/seasonality.json`

From data/species_maps.py:
- `ASSETS_DIR: Path`, `DB_PATH` (module attrs, same env-driven pattern)
- `SVG_NS = "http://www.w3.org/2000/svg"`
- `_write_species_svg(slug: str, points: list[tuple[float, float]], backdrop: ET.Element, out_dir: Path) -> int`
  - Currently writes `out_dir / f"{slug}.svg"`; after Plan 02 it will mkdir parent first
- `generate_species_maps(con: duckdb.DuckDBPyConnection | None = None) -> None`

From data/tests/conftest.py:
- `fixture_con` (function-scoped DuckDB connection with schemas + seed data)
- `export_dir` (tmp_path-based output directory; provided as a fixture)

From data/tests/test_dbt_diff.py:
- `SANDBOX = Path(__file__).resolve().parent.parent / "dbt" / "target" / "sandbox"`
- `_SANDBOX_GUARD = pytest.mark.skipif(not (SANDBOX / "...").exists(), reason="...")`
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create data/tests/test_species_export.py with slug-format assertions</name>
  <files>data/tests/test_species_export.py</files>
  <read_first>
    - data/species_export.py (target module — see how `export_species_parquet` consumes `DBT_SANDBOX_DIR/species.parquet` and writes `ASSETS_DIR/species.parquet`; observe lines 140-143 for the slug-assignment block this test will eventually cover)
    - data/tests/test_dbt_diff.py (analog: SANDBOX path constant at lines 24-25; `_SANDBOX_GUARD` skipif at lines 27-30; parquet column type-assertion at lines 334-348)
    - data/tests/test_feeds.py (analog: monkeypatching `ASSETS_DIR` + `DB_PATH` pattern at lines 38-46; module-import pattern at lines 11-14)
    - data/tests/conftest.py (existing `fixture_con` shape so the test can reuse it)
    - .planning/phases/92-slug-migration-pipeline-prep/92-PATTERNS.md (lines 92-151 — exact test scaffolding pattern to copy)
  </read_first>
  <behavior>
    - Test 1 `test_slug_hierarchical`: After calling `export_species_parquet(con)` with `ASSETS_DIR` monkeypatched to `tmp_path`, every row in the resulting `species.parquet` with `specific_epithet IS NOT NULL` has `slug == f"{genus}/{epithet}"`. Sample up to 20 such rows.
    - Test 2 `test_no_old_slug_format`: After the same export run, `SELECT COUNT(*) FROM read_parquet('{tmp_path}/species.parquet') WHERE slug LIKE '%-%' AND specific_epithet IS NOT NULL` returns 0. (Genus values like "Lasioglossum (Dialictus)" contain spaces, never hyphens; epithets are single tokens. The old flat slug was the only place hyphens appeared.)
    - Both tests guarded by `_SANDBOX_GUARD` so they skip cleanly when `data/dbt/target/sandbox/species.parquet` is absent.
    - RED state expected on first commit: tests must FAIL when run against current unedited `species_export.py` (which still emits `andrena-milwaukeensis`-style slugs).
  </behavior>
  <action>
    Create a new test file at the path under `<files>`. Mirror the imports, `SANDBOX`/`_SANDBOX_GUARD` constants, and `monkeypatch.setattr(se_mod, 'ASSETS_DIR', tmp_path)` pattern from the analog files. Use `import species_export as se_mod` and `from species_export import export_species_parquet, SPECIES_COLUMNS`. Both tests must use `tmp_path` (pytest builtin) plus `monkeypatch`, and call `duckdb.connect()` (no fixture_con needed — `export_species_parquet` reads from on-disk parquet, not in-memory tables). Apply `_SANDBOX_GUARD` to both. Do NOT modify any production code in this task — `species_export.py` stays as-is so the tests fail RED. Run `cd data && uv run pytest tests/test_species_export.py -q` after creating the file and capture the failure output as evidence of RED state (record the assertion failure messages in your task notes; do not commit the failure output to the repo).
  </action>
  <verify>
    <automated>cd data && uv run pytest tests/test_species_export.py -q; test $? -eq 1  # exit 1 = test failures = RED state (or exit 5 if collection-only). Confirm by reading stdout: must contain "FAILED tests/test_species_export.py::test_slug_hierarchical" and "FAILED tests/test_species_export.py::test_no_old_slug_format" when the SANDBOX parquet exists; otherwise "SKIPPED" for both is acceptable evidence the guard works.</automated>
  </verify>
  <done>
    File `data/tests/test_species_export.py` exists. Running `cd data && uv run pytest tests/test_species_export.py -q --collect-only` lists exactly two test items (`test_slug_hierarchical`, `test_no_old_slug_format`). Both tests either FAIL against current `species_export.py` OR SKIP when SANDBOX parquet is absent — they do NOT pass (which would mean the assertions are wrong). No production-source file is modified.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create data/tests/test_species_maps.py with subdir-write assertion</name>
  <files>data/tests/test_species_maps.py</files>
  <read_first>
    - data/species_maps.py (target module — `_write_species_svg` signature at lines 132-137, body lines 138-172, current write at line 167-171 has no `mkdir` for parents)
    - data/tests/test_feeds.py (analog: monkeypatch pattern lines 38-46; imports lines 1-14)
    - data/tests/test_transforms.py (analog for pure-function tests that do not need fixture_con)
    - .planning/phases/92-slug-migration-pipeline-prep/92-PATTERNS.md (lines 188-198 — exact `test_write_species_svg_creates_subdir` scaffold to copy)
  </read_first>
  <behavior>
    - Test `test_write_species_svg_creates_subdir`: Call `_write_species_svg(slug="Andrena/milwaukeensis", points=[], backdrop=ET.Element(f"{{{SVG_NS}}}svg"), out_dir=tmp_path)`. Assert that `tmp_path / "Andrena" / "milwaukeensis.svg"` exists after the call. Currently this raises `FileNotFoundError` because the parent directory `tmp_path/Andrena/` is not created — that is the RED state. After Plan 02 adds the `out_path.parent.mkdir` line, this test passes.
    - The empty `points=[]` list keeps the test pure (no projection logic exercised); only the path-write behavior is asserted.
  </behavior>
  <action>
    Create the new test file at the path under `<files>`. Import the constants and function: `from species_maps import _write_species_svg, SVG_NS` (verify `SVG_NS` is module-level in species_maps.py before importing; if it is a function-local string, import via `import species_maps as sm_mod` and use `sm_mod.SVG_NS` — read species_maps.py first to confirm). Use `xml.etree.ElementTree as ET` to build the minimal backdrop element. Do not modify production code. Run `cd data && uv run pytest tests/test_species_maps.py -q` and confirm the test fails with `FileNotFoundError: [Errno 2] No such file or directory: '.../Andrena/milwaukeensis.svg'`.
  </action>
  <verify>
    <automated>cd data && uv run pytest tests/test_species_maps.py -q; test $? -eq 1  # exit 1 = failure = RED state. Confirm stdout contains "FAILED tests/test_species_maps.py::test_write_species_svg_creates_subdir" and an error trace mentioning FileNotFoundError or No such file.</automated>
  </verify>
  <done>
    File `data/tests/test_species_maps.py` exists with exactly one test function. Running `cd data && uv run pytest tests/test_species_maps.py -q --collect-only` reports one test item. The test FAILS when executed (RED state). No production code is modified.
  </done>
</task>

<task type="auto">
  <name>Task 3: Update validate-species.test.ts fixture slug value</name>
  <files>src/tests/validate-species.test.ts</files>
  <read_first>
    - src/tests/validate-species.test.ts (target file — fixture array at line 13-15; `SPECIES_JSON` const)
    - scripts/validate-species.mjs (consumer — confirms slug is NOT validated against any pattern in `validateSpeciesPhotos`; only `scientificName` is keyed for cross-reference)
  </read_first>
  <behavior>
    - After the edit, the `SPECIES_JSON` fixture entry's `slug` value is the string `'Osmia/lignaria'` (with the literal slash) instead of `'osmia-lignaria'`.
    - All existing assertions in this test file continue to pass — the slug value is never compared to a format pattern; this change is for conceptual consistency with the new production slug format.
  </behavior>
  <action>
    On line 14 of `src/tests/validate-species.test.ts`, change the single string literal `'osmia-lignaria'` to `'Osmia/lignaria'`. All other fields on the line (scientificName, canonical_name, on_checklist, occurrence_count) stay exactly as they are. Do not edit any other line in the file. After the edit, run `npm test -- --run src/tests/validate-species.test.ts` and confirm all tests pass.
  </action>
  <verify>
    <automated>npm test -- --run src/tests/validate-species.test.ts 2>&1 | tee /tmp/p92-01-validate.log; grep -q "Tests.*passed" /tmp/p92-01-validate.log && ! grep -q "Tests.*failed" /tmp/p92-01-validate.log</automated>
  </verify>
  <done>
    Line 14 of `src/tests/validate-species.test.ts` contains `slug: 'Osmia/lignaria'`. Running `npm test -- --run src/tests/validate-species.test.ts` reports 0 failed tests. No other lines in the file are changed.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| dbt mart → species_export.py (test scope) | dbt-validated columns `genus`, `specific_epithet` cross into the test assertions; tests will read these values via DuckDB SQL. Values come from the dbt source contract with NOT NULL on the WA checklist subset. |
| Test fixtures → filesystem | `tmp_path` is pytest-managed, isolated per test run. No traversal risk. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-92-01-01 | Tampering | data/tests/test_species_export.py | accept | Test file under repo control; CI runs `cd data && uv run pytest` on clean checkout. No external input to tamper with. |
| T-92-01-02 | Information Disclosure | data/tests/test_species_maps.py | accept | Test writes to pytest `tmp_path`, cleaned up automatically. No PII in SVG content. |
| T-92-01-03 | Tampering | src/tests/validate-species.test.ts fixture | accept | Fixture is a hardcoded array literal; not user-controlled. |

This wave-0 plan adds tests only — no production code path is modified, so no new attack surface is introduced. Plan 02's threat model covers the slug-injection / path-traversal analysis for the production edits.
</threat_model>

<verification>
Run `cd data && uv run pytest tests/test_species_export.py tests/test_species_maps.py -q` — expect 3 failing tests (or skipped if SANDBOX absent). Run `npm test -- --run src/tests/validate-species.test.ts` — expect 0 failures. Run `cd data && uv run pytest tests/test_dbt_diff.py -q` — expect green (existing slug-column type assertion still holds because Plan 01 does not touch production code).
</verification>

<success_criteria>
- [ ] `data/tests/test_species_export.py` exists with two test functions, both FAILING (or SKIPPED) against current production code
- [ ] `data/tests/test_species_maps.py` exists with one test function, FAILING against current production code
- [ ] `src/tests/validate-species.test.ts` line 14 slug fixture reads `'Osmia/lignaria'`
- [ ] `npm test -- --run src/tests/validate-species.test.ts` reports 0 failed tests
- [ ] No production source file (`data/species_export.py`, `data/species_maps.py`, `_pages/species.njk`, `content/species-photos.toml`) is modified by this plan
</success_criteria>

<output>
After completion, create `.planning/phases/92-slug-migration-pipeline-prep/92-01-SUMMARY.md` recording: test files created, RED state evidence (assertion messages and pytest exit codes), and confirmation that no production code was touched.
</output>
