---
phase: 136-deduplication
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - data/tests/test_checklist_dedup.py
  - data/checklist_dedup.py
  - data/dbt/models/intermediate/int_checklist_collapsed.sql
  - data/dbt/models/intermediate/int_dedup_candidates.sql
  - data/dbt/models/intermediate/int_checklist_dedup_status.sql
  - data/dbt/seeds/dedup_decisions.csv
  - data/dbt/seeds/schema.yml
autonomous: true
requirements: [DUP-01, DUP-02, DUP-03]

must_haves:
  truths:
    - "All 11 dedup tests exist and are RED (collapse/candidate/status/gate behaviors unimplemented)"
    - "checklist_dedup.py module imports cleanly with stub functions and module-level constants"
    - "The three int_* dbt model files exist as placeholders that the DAG can ref()"
    - "dedup_decisions.csv exists header-only and is registered in seeds/schema.yml"
  artifacts:
    - path: "data/tests/test_checklist_dedup.py"
      provides: "11 RED test stubs covering DUP-01/02/03 from VALIDATION.md"
      contains: "def test_no_exact_duplicates_after_collapse"
      min_lines: 120
    - path: "data/checklist_dedup.py"
      provides: "Module under test: constants + stub write_dedup_candidates/check_dedup_gate/_normalize_collector/_collectors_match"
      contains: "DEDUP_DISTANCE_THRESHOLD_M"
    - path: "data/dbt/models/intermediate/int_checklist_collapsed.sql"
      provides: "DUP-01 collapse model placeholder"
    - path: "data/dbt/models/intermediate/int_dedup_candidates.sql"
      provides: "DUP-02 candidate model placeholder"
    - path: "data/dbt/models/intermediate/int_checklist_dedup_status.sql"
      provides: "DUP-03 status-view placeholder"
    - path: "data/dbt/seeds/dedup_decisions.csv"
      provides: "Committed curated decisions seed (header-only)"
      contains: "pair_key,dedup_status,note"
    - path: "data/dbt/seeds/schema.yml"
      provides: "dedup_decisions seed registration with not_null/unique/accepted_values tests"
      contains: "name: dedup_decisions"
  key_links:
    - from: "data/tests/test_checklist_dedup.py"
      to: "data/checklist_dedup.py"
      via: "import checklist_dedup"
      pattern: "import checklist_dedup"
---

<objective>
Lay the Wave 0 test-first scaffold for Phase 136 deduplication: write the 11 RED pytest stubs (from 136-VALIDATION.md), create the `checklist_dedup.py` module with stub functions + module-level constants, create the three intermediate dbt model files as placeholders, and add the header-only `dedup_decisions.csv` seed plus its `schema.yml` registration.

Purpose: Pin every DUP-01/DUP-02/DUP-03 behavior as an executable test before any implementation, mirroring how Phase 135 sequenced 135-01 RED scaffolding before implementation. Subsequent waves turn these tests green.
Output: One failing-test file, one stub Python module, three placeholder SQL models, one seed CSV, one schema.yml entry.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/136-deduplication/136-CONTEXT.md
@.planning/phases/136-deduplication/136-RESEARCH.md
@.planning/phases/136-deduplication/136-PATTERNS.md
@.planning/phases/136-deduplication/136-VALIDATION.md

<interfaces>
<!-- Stub contracts this plan creates. Implementation lands in waves 2-4. -->
checklist_dedup.py module-level constants (PATTERNS.md lines 290-305):
  DB_PATH = os.environ.get("DB_PATH", str(Path(__file__).parent / "beeatlas.duckdb"))
  DEDUP_CANDIDATE_CSV = Path(__file__).parent / "dedup_candidate_pairs.csv"
  DEDUP_DECISIONS_CSV = Path(__file__).parent / "dbt" / "seeds" / "dedup_decisions.csv"
  DEDUP_DISTANCE_THRESHOLD_M = 1000.0   # D-07, named tunable constant

Stub function signatures (raise NotImplementedError until later waves):
  def _normalize_collector(name: str | None) -> frozenset[str]
  def _collectors_match(a: str | None, b: str | None) -> bool
  def write_dedup_candidates() -> int          # run.py STEP signature: callable taking no args
  def check_dedup_gate() -> None               # run.py STEP gate signature

seed columns (D-01/D-02): pair_key,dedup_status,note
  pair_key format: "<ObjectID>|<ecdysis_id>" (VARCHAR)
  dedup_status enum: confirmed | rejected (unreviewed = no row)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Stub checklist_dedup.py + three placeholder dbt models + seed + schema entry</name>
  <files>data/checklist_dedup.py, data/dbt/models/intermediate/int_checklist_collapsed.sql, data/dbt/models/intermediate/int_dedup_candidates.sql, data/dbt/models/intermediate/int_checklist_dedup_status.sql, data/dbt/seeds/dedup_decisions.csv, data/dbt/seeds/schema.yml</files>
  <read_first>
    - data/resolve_taxon_ids.py (lines 1-110 — module structure, DB_PATH constant line 34, _csv_safe() lines 63-80, check_resolution_gate() lines 83-110: the gate + path-constant template)
    - data/resolve_checklist_names.py (lines 1-60 — module-level constants + AUDIT_CSV path + CSV-writer structure analog)
    - data/dbt/seeds/occurrence_synonyms.csv (the committed-seed precedent for dedup_decisions.csv)
    - data/dbt/seeds/schema.yml (existing entries — exact structure to mirror for the dedup_decisions entry)
    - data/dbt/models/intermediate/int_synonyms.sql (the LEFT JOIN view analog for int_checklist_dedup_status)
    - data/dbt/models/intermediate/int_combined.sql (materialized='table' config line 11)
  </read_first>
  <action>
    Create `data/checklist_dedup.py` as a stub module: copy the module-level path constants and `_csv_safe()` helper verbatim from `resolve_taxon_ids.py` (lines 34, 63-80), add `DEDUP_CANDIDATE_CSV`, `DEDUP_DECISIONS_CSV`, and the named constant `DEDUP_DISTANCE_THRESHOLD_M = 1000.0` (D-07). Define four functions with docstrings referencing their requirement (D-01/D-05/DUP-02/DUP-03) but `raise NotImplementedError` bodies: `_normalize_collector(name)`, `_collectors_match(a, b)`, `write_dedup_candidates()`, `check_dedup_gate()`. Do NOT implement logic — implementation is waves 2-4.

    Create the three placeholder dbt models as minimal valid SQL the DAG can build/ref:
    - `int_checklist_collapsed.sql`: `{{ config(materialized='table') }}` then `SELECT * FROM {{ ref('stg_checklist__records_full') }}` with a header comment "PLACEHOLDER — DUP-01 collapse implemented in 136-02".
    - `int_dedup_candidates.sql`: `{{ config(materialized='table') }}` then a `SELECT ... WHERE false` shell that emits the pair_key/checklist_ObjectID/ecdysis_id column shape (so 136-03 can fill the join), header comment "PLACEHOLDER — DUP-02 candidate join in 136-03".
    - `int_checklist_dedup_status.sql`: `{{ config(materialized='view') }}` then `SELECT *, CAST(NULL AS VARCHAR) AS dedup_status FROM {{ ref('int_checklist_collapsed') }}`, header comment "PLACEHOLDER — DUP-03 LEFT JOIN in 136-04".

    Create `data/dbt/seeds/dedup_decisions.csv` header-only: exactly the line `pair_key,dedup_status,note`. Add the `dedup_decisions` seed entry to `data/dbt/seeds/schema.yml` after the last existing seed, per PATTERNS.md lines 264-279: `pair_key` (not_null, unique), `dedup_status` (not_null, accepted_values ['confirmed','rejected']), `note` (description only). Keep the existing seed entries untouched.

    Do NOT wire anything into run.py yet (that is 136-04). Do NOT touch int_combined, occurrences mart, or the 33-column contract.
  </action>
  <verify>
    <automated>cd data && uv run python -c "import checklist_dedup; print(checklist_dedup.DEDUP_DISTANCE_THRESHOLD_M)"</automated>
  </verify>
  <acceptance_criteria>
    - `cd data && uv run python -c "import checklist_dedup"` succeeds and prints `1000.0`.
    - `data/dbt/seeds/dedup_decisions.csv` contains exactly the header `pair_key,dedup_status,note` and no data rows.
    - `grep -c "name: dedup_decisions" data/dbt/seeds/schema.yml` returns 1; existing seed entries (occurrence_synonyms, auto_synonyms, gbif_checklist_synonyms, curated_taxon_ids) are still present.
    - The three new SQL files exist under `data/dbt/models/intermediate/` and each begins with a `{{ config(materialized=...) }}` line.
    - `checklist_dedup.py` defines `_normalize_collector`, `_collectors_match`, `write_dedup_candidates`, `check_dedup_gate` (grep confirms all four `def` lines).
    - No edit touches `int_combined.sql`, `marts/occurrences.sql`, or any contract column count.
  </acceptance_criteria>
  <done>Stub module imports, three placeholder models exist, header-only seed registered in schema.yml. No implementation logic present.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Write the 11 RED dedup test stubs in test_checklist_dedup.py</name>
  <files>data/tests/test_checklist_dedup.py</files>
  <read_first>
    - data/tests/test_resolution_gate.py (lines 1-67 — exact gate-test structure: _write_csv helper, monkeypatch.setattr path redirect, pytest.raises(SystemExit), capsys "OK" assertion)
    - data/tests/test_resolve_checklist_names.py (lines 65-138 — isolated DuckDB fixture pattern: tmp_path DB, monkeypatch.setenv DB_PATH, importlib.reload, minimal CREATE TABLE seeding)
    - data/tests/conftest.py (lines 545-565 — spatial-extension load pattern for a NEW in-memory connection: con.execute("INSTALL spatial; LOAD spatial"))
    - data/dbt/models/intermediate/stg_checklist__records_full.sql — the input column shapes (ObjectID, canonical_name, lat, lon, year, month, day, date_quality, recordedBy) the collapse/candidate SQL operates on
    - data/dbt/models/intermediate/int_ecdysis_base.sql — Ecdysis match-target columns (ecdysis_id, ecdysis_lat, ecdysis_lon, event_date, year, month, recordedBy, canonical_name; NO day column — derive via TRY_CAST)
    - .planning/phases/136-deduplication/136-VALIDATION.md (the authoritative 11-test per-requirement map and exact test names)
  </read_first>
  <behavior>
    - test_no_exact_duplicates_after_collapse (DUP-01): after running int_checklist_collapsed SQL over a seeded table with a known duplicate group, zero (canonical_name,lat,lon,year,month,day,recordedBy) tuples appear >1 time.
    - test_collapsed_count_correct (DUP-01): a group of N identical rows yields one survivor with collapsed_count=N; a unique row yields collapsed_count=1.
    - test_lowest_objectid_survives (DUP-01): within a duplicate group the survivor's ObjectID = MIN(ObjectID) of the group.
    - test_null_date_excluded_from_candidates (DUP-02): a checklist row with date_quality != 'full' never appears in int_dedup_candidates.
    - test_null_coord_excluded_from_candidates (DUP-02): a checklist row with NULL lat/lon never appears in candidates.
    - test_candidate_csv_written (DUP-02): write_dedup_candidates() produces dedup_candidate_pairs.csv with the documented column set incl. pair_key.
    - test_collector_normalization (D-05): _collectors_match('J Smith','John Smith') is True; _collectors_match('A Jones','B Jones') is False; exact token-set equality matches.
    - test_distance_1km_window (DUP-02): a pair just inside 1.0 km is a candidate; a pair just outside 1.0 km is not — guarding the lat-first ST_Distance_Sphere axis order.
    - test_unreviewed_pair_not_suppressed (DUP-03): a candidate pair with NO dedup_decisions row yields dedup_status NULL (not 'confirmed') in int_checklist_dedup_status.
    - test_confirmed_pair_suppressed (DUP-03): a candidate whose pair_key has dedup_status='confirmed' in the seed yields dedup_status='confirmed' on that ObjectID.
    - test_dedup_gate (DUP-03): check_dedup_gate() sys.exits when a 'confirmed' decision references a pair_key absent from the regenerated candidates; prints "dedup-gate: OK" otherwise.
  </behavior>
  <action>
    Create `data/tests/test_checklist_dedup.py` with all 11 test functions named exactly as in 136-VALIDATION.md (the Per-Task Verification Map). Tests are RED stubs that encode the intended behavior and fail because waves 2-4 are unimplemented — do NOT mark them xfail/skip; they must FAIL (red), mirroring 135-01 RED scaffolding.

    Structure per 136-PATTERNS.md (lines 387-466):
    - SQL behavior tests (collapse, candidates, status): build an isolated `:memory:` DuckDB, `con.execute("INSTALL spatial; LOAD spatial")`, CREATE minimal tables shaped like `stg_checklist__records_full` and `int_ecdysis_base`, then execute the model SQL. Since the models are placeholders this wave, the assertions will fail — that is the RED state. Read the model SQL via the dbt files (strip the `{{ ref() }}`/`{{ config() }}` Jinja and substitute the seeded table names) OR assert against the eventual behavior so the test is red now and green once 136-02/03/04 implement the SQL.
    - Gate tests: use tmp_path + monkeypatch.setattr(checklist_dedup, "DEDUP_CANDIDATE_CSV", ...) / "DEDUP_DECISIONS_CSV" to write synthetic CSVs, then assert pytest.raises(SystemExit) / capsys "dedup-gate: OK" — exact shape from test_resolution_gate.py.
    - Collector tests: pure-Python calls to checklist_dedup._collectors_match — no DB.

    Mark the SQL/integration-touching tests fast-tier (no `@pytest.mark.integration`) per project test-tier discipline — they must run under `-m 'not integration'` in seconds using isolated in-memory DuckDB, never the dbt_sandbox session fixture.
  </action>
  <verify>
    <automated>cd data && uv run pytest tests/test_checklist_dedup.py --collect-only -q 2>&1 | grep -c "test_"</automated>
  </verify>
  <acceptance_criteria>
    - `cd data && uv run pytest tests/test_checklist_dedup.py --collect-only -q` lists all 11 named tests (matching VALIDATION.md exactly).
    - `cd data && uv run pytest tests/test_checklist_dedup.py -x` FAILS (RED) — at least one test fails because collapse/candidate/status SQL and Python helpers are unimplemented; zero tests are skipped or xfailed.
    - Gate tests use monkeypatch.setattr to redirect DEDUP_CANDIDATE_CSV / DEDUP_DECISIONS_CSV (grep confirms `monkeypatch.setattr`).
    - No test carries `@pytest.mark.integration` (all fast-tier).
  </acceptance_criteria>
  <done>All 11 tests collected by name; suite is RED with zero skips/xfails; gate tests redirect file paths via monkeypatch.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| source CSV → curator-facing CSV | Collector / locality strings from the Bartholomew source flow into `dedup_candidate_pairs.csv`, opened in a spreadsheet by the curator |
| committed seed → build | `dedup_decisions.csv` is human-edited input consumed by the build |

This is a static-hosted, offline data pipeline. No runtime server, no network input, no auth surface. The realistic threat surface is data-integrity / supply-chain, not network attack.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-136-01 | Tampering | CSV cells (collector/locality) written to dedup_candidate_pairs.csv | mitigate | `_csv_safe()` formula-injection guard copied verbatim from resolve_taxon_ids.py; applied per string cell in the writer (implemented 136-03; stub carries the helper here) |
| T-136-02 | Tampering | dedup_decisions.csv seed silently suppressing real points | mitigate | This wave only registers the header-only seed + accepted_values test; the build gate that blocks orphaned/invalid confirmations lands in 136-04 (check_dedup_gate) |
| T-136-SC | Tampering | npm/pip/cargo installs | mitigate | No new packages — RESEARCH Package Legitimacy Audit confirms all deps pre-existing; nothing to install |
</threat_model>

<verification>
- `cd data && uv run python -c "import checklist_dedup"` imports cleanly.
- `cd data && uv run pytest tests/test_checklist_dedup.py --collect-only -q` lists 11 tests.
- `cd data && uv run pytest tests/test_checklist_dedup.py -x` is RED (expected at Wave 0).
- No change to int_combined / occurrences mart / contract.
</verification>

<success_criteria>
- 11 RED tests exist and are collected by their VALIDATION.md names.
- `checklist_dedup.py` stub imports with all four functions + constants.
- Three placeholder int_* models exist and are DAG-buildable.
- Header-only `dedup_decisions.csv` registered in seeds/schema.yml.
</success_criteria>

<output>
Create `.planning/phases/136-deduplication/136-01-SUMMARY.md` when done
</output>
