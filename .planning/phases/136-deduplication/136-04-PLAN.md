---
phase: 136-deduplication
plan: 04
type: execute
wave: 4
depends_on: ["136-03"]
files_modified:
  - data/dbt/models/intermediate/int_checklist_dedup_status.sql
  - data/checklist_dedup.py
  - data/run.py
autonomous: false
requirements: [DUP-03]

must_haves:
  truths:
    - "int_checklist_dedup_status exposes dedup_status on each collapsed checklist record via LEFT JOIN through candidates to the committed decisions seed — the regenerated-audit-vs-curated-seed split (D-01)"
    - "An unreviewed candidate (no seed row) yields dedup_status NULL — it does NOT suppress"
    - "ANY confirmed pair for an ObjectID sets dedup_status='confirmed' on that record (D-08)"
    - "check_dedup_gate() fails the build on a confirmed decision whose pair_key is absent from regenerated candidates (stale seed)"
    - "run.py runs dedup-candidates then dedup-gate between dbt-build and generate-sqlite"
    - "A full dbt build succeeds and the curator HUMAN-REVIEW GATE blocks Phase 137"
  artifacts:
    - path: "data/dbt/models/intermediate/int_checklist_dedup_status.sql"
      provides: "DUP-03 joinable dedup_status view (Phase 137 consumes WHERE dedup_status IS DISTINCT FROM 'confirmed')"
      contains: "dedup_status"
    - path: "data/checklist_dedup.py"
      provides: "check_dedup_gate() — orphaned-confirmed-pair_key build gate"
      contains: "def check_dedup_gate"
    - path: "data/run.py"
      provides: "dedup-candidates + dedup-gate STEPS between dbt-build and generate-sqlite"
      contains: "dedup-candidates"
  key_links:
    - from: "data/dbt/models/intermediate/int_checklist_dedup_status.sql"
      to: "dedup_decisions"
      via: "LEFT JOIN through int_dedup_candidates by pair_key"
      pattern: "ref\\('dedup_decisions'\\)"
    - from: "data/run.py"
      to: "data/checklist_dedup.py"
      via: "import write_dedup_candidates, check_dedup_gate"
      pattern: "from checklist_dedup import"
---

<objective>
Implement DUP-03: the `int_checklist_dedup_status` LEFT JOIN view exposing `dedup_status` on each collapsed checklist record (the joinable status Phase 137 consumes), the `check_dedup_gate()` build gate, the run.py wiring (`dedup-candidates` + `dedup-gate` STEPS between `dbt-build` and `generate-sqlite`), a full dbt build, and the curator HUMAN-REVIEW GATE that blocks Phase 137.

Purpose: This closes the human-in-the-loop loop — only a curator-confirmed pair ever sets `dedup_status='confirmed'`; unreviewed candidates render as normal points. The gate guards against stale seed rows. This phase stops at producing the joinable status; it does NOT promote rows or filter any point layer (Phase 137/138).
Output: One implemented view, one implemented gate, run.py STEPS wired, dbt build green, three DUP-03 tests green, and a blocking curator checkpoint.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/136-deduplication/136-CONTEXT.md
@.planning/phases/136-deduplication/136-RESEARCH.md
@.planning/phases/136-deduplication/136-PATTERNS.md
@.planning/phases/136-deduplication/136-03-SUMMARY.md

<interfaces>
<!-- int_checklist_dedup_status output: int_checklist_collapsed columns + dedup_status -->
SELECT cl.*, <CASE bool_or(dd.dedup_status='confirmed') OVER (PARTITION BY cl.ObjectID)> AS dedup_status
  materialized = 'view'
  Phase 137 consumes: WHERE dedup_status IS DISTINCT FROM 'confirmed'

<!-- run.py STEPS shape (verified): dbt-build at the tail, then generate-sqlite -->
("dbt-build", _run_dbt_build),
("dedup-candidates", write_dedup_candidates),   # NEW — DUP-02 audit CSV
("dedup-gate", check_dedup_gate),               # NEW — DUP-03 orphan guard
("generate-sqlite", generate_sqlite_export),

<!-- gate contract (from 136-01 stub) -->
check_dedup_gate() -> None   # sys.exit on orphaned confirmed pair_key; else print "dedup-gate: OK ..."
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement int_checklist_dedup_status view + check_dedup_gate (DUP-03)</name>
  <files>data/dbt/models/intermediate/int_checklist_dedup_status.sql, data/checklist_dedup.py</files>
  <read_first>
    - data/dbt/models/intermediate/int_checklist_dedup_status.sql (the 136-01 placeholder being implemented)
    - data/dbt/models/intermediate/int_synonyms.sql (the materialized='view' + LEFT JOIN precedence analog — exact match)
    - data/checklist_dedup.py (the 136-01 check_dedup_gate stub + DEDUP_CANDIDATE_CSV/DEDUP_DECISIONS_CSV constants)
    - data/resolve_taxon_ids.py (lines 83-110 — check_resolution_gate() fail-fast sys.exit template)
    - .planning/phases/136-deduplication/136-PATTERNS.md (lines 178-217 — the int_checklist_dedup_status LEFT JOIN pattern with bool_or OVER PARTITION BY ObjectID; lines 323-343 — gate structure)
    - .planning/phases/136-deduplication/136-RESEARCH.md (lines 369-449 — Pattern 3 seed+LEFT JOIN+gate; D-08 ANY confirmed suppresses)
  </read_first>
  <behavior>
    - int_checklist_dedup_status returns every int_checklist_collapsed row plus a dedup_status column.
    - A record with NO candidate / no seed row gets dedup_status NULL (unreviewed = not suppressed).
    - A record whose ObjectID has at least one candidate pair with seed dedup_status='confirmed' gets dedup_status='confirmed' (D-08: ANY confirmed).
    - check_dedup_gate() sys.exits when a 'confirmed' seed row's pair_key is absent from the regenerated dedup_candidate_pairs.csv (stale seed); prints "dedup-gate: OK" otherwise; OK no-op when the seed is header-only.
  </behavior>
  <action>
    Implement `int_checklist_dedup_status.sql` with `{{ config(materialized='view') }}` (cheap LEFT JOIN over materialized tables, mirroring int_synonyms). SELECT `cl.*` from `{{ ref('int_checklist_collapsed') }} cl` LEFT JOIN `{{ ref('int_dedup_candidates') }} cand ON cand.checklist_ObjectID = cl.ObjectID` LEFT JOIN `{{ ref('dedup_decisions') }} dd ON dd.pair_key = cand.pair_key`, computing `dedup_status` as a window expression: `CASE WHEN bool_or(dd.dedup_status = 'confirmed') OVER (PARTITION BY cl.ObjectID) THEN 'confirmed' ELSE MAX(dd.dedup_status) OVER (PARTITION BY cl.ObjectID) END` (D-08 ANY confirmed suppresses; unreviewed → NULL). Add a header comment noting Phase 137 consumes `WHERE dedup_status IS DISTINCT FROM 'confirmed'`. Use SELECT DISTINCT or a dedup so the fan-out from multiple candidate pairs per ObjectID collapses back to one row per checklist record.

    Implement `check_dedup_gate()` in checklist_dedup.py per the check_resolution_gate template: if DEDUP_DECISIONS_CSV missing or header-only → print "dedup-gate: OK (no decisions seed)" and return; if decisions exist but DEDUP_CANDIDATE_CSV missing → sys.exit with an actionable message; read both CSVs, find `confirmed` decisions whose pair_key is absent from the regenerated candidate keys (orphans), `sys.exit(f"dedup-gate: {len(orphans)} confirmed suppression(s) reference pair_keys not in current candidates (stale seed?): ...")` naming the orphans; else print `dedup-gate: OK ({n} confirmed, {m} rejected)`.

    Do NOT filter rows out of any layer (no point layer until 137/138). Do NOT touch int_combined / occurrences mart / the 33-column contract.
  </action>
  <verify>
    <automated>cd data && uv run pytest tests/test_checklist_dedup.py::test_unreviewed_pair_not_suppressed tests/test_checklist_dedup.py::test_confirmed_pair_suppressed tests/test_checklist_dedup.py::test_dedup_gate -x</automated>
  </verify>
  <acceptance_criteria>
    - `pytest tests/test_checklist_dedup.py::test_unreviewed_pair_not_suppressed -x` passes (NULL status for unreviewed).
    - `pytest tests/test_checklist_dedup.py::test_confirmed_pair_suppressed -x` passes (confirmed propagates per ObjectID).
    - `pytest tests/test_checklist_dedup.py::test_dedup_gate -x` passes (orphaned confirmed pair_key → SystemExit; clean → "dedup-gate: OK").
    - The view is `materialized='view'` and refs `int_checklist_collapsed`, `int_dedup_candidates`, and `dedup_decisions`.
    - `grep -c "IS DISTINCT FROM 'confirmed'" data/dbt/models/intermediate/int_checklist_dedup_status.sql` returns ≥1 (the Phase-137 consumption comment).
  </acceptance_criteria>
  <done>dedup_status view exposes confirmed/NULL per record; gate fails on stale confirmed pair_keys; all three DUP-03 tests green.</done>
</task>

<task type="auto">
  <name>Task 2: Wire dedup-candidates + dedup-gate into run.py STEPS and run a full dbt build</name>
  <files>data/run.py</files>
  <read_first>
    - data/run.py (lines 30-47 import block; lines 84-110 STEPS list — dbt-build then generate-sqlite; the resolution-gate/inactive-gate placement precedent)
    - .planning/phases/136-deduplication/136-PATTERNS.md (lines 355-383 — the exact import + STEPS insertion pattern)
    - data/dbt/run.sh (the dbt build entrypoint — the build that must stay green incl. the 33-column occurrences contract)
  </read_first>
  <action>
    Add `from checklist_dedup import write_dedup_candidates, check_dedup_gate` to the run.py import block alongside the other pipeline imports. Insert two STEPS entries IMMEDIATELY after `("dbt-build", _run_dbt_build)` and BEFORE `("generate-sqlite", generate_sqlite_export)`: `("dedup-candidates", write_dedup_candidates)` (writes dedup_candidate_pairs.csv — requires the built dbt_sandbox.int_dedup_candidates) then `("dedup-gate", check_dedup_gate)` (asserts no orphaned confirmed pairs). Update the module-level STEPS docstring/comment ordering line (run.py top-of-file comment lists the step chain) to include the two new steps. Do NOT add any S3/git logic to run.py (nightly.sh owns that). Do NOT reorder existing steps.

    Then run a full dbt build to confirm the three new int_* models + the dedup_decisions seed build cleanly and the existing 33-column occurrences contract is unchanged (this phase must NOT bump it — promotion is Phase 137).
  </action>
  <verify>
    <automated>cd data && grep -n "dedup-candidates\|dedup-gate\|from checklist_dedup import" run.py && bash dbt/run.sh build</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "dedup-candidates" data/run.py` and `grep -n "dedup-gate" data/run.py` each return a line, both between `dbt-build` and `generate-sqlite`.
    - `grep -n "from checklist_dedup import write_dedup_candidates, check_dedup_gate" data/run.py` returns a line.
    - `bash data/dbt/run.sh build` succeeds, building int_checklist_collapsed, int_dedup_candidates, int_checklist_dedup_status, and the dedup_decisions seed, with the occurrences contract still at 33 columns (no contract bump).
    - run.py contains no new S3/git logic.
  </acceptance_criteria>
  <done>run.py runs dedup-candidates then dedup-gate between dbt-build and generate-sqlite; full dbt build green; contract unchanged at 33 columns.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 3: HUMAN-REVIEW GATE — curator reviews dedup_candidate_pairs.csv before Phase 137</name>
  <action>This is a blocking human checkpoint, not autonomous work. Pause execution and hand off to the curator: present dedup_candidate_pairs.csv for review, let them record confirmations in dedup_decisions.csv, and do not begin Phase 137 until they signal approval. See how-to-verify for the exact curator steps.</action>
  <what-built>
    DUP-01 internal collapse (int_checklist_collapsed), DUP-02 cross-source candidate generation (int_dedup_candidates + dedup_candidate_pairs.csv audit CSV), and DUP-03 joinable dedup_status (int_checklist_dedup_status + dedup_decisions seed + check_dedup_gate). The full fast test suite is green and a full dbt build succeeds. This is the v4.7 HUMAN-REVIEW GATE that blocks Phase 137.
  </what-built>
  <how-to-verify>
    1. Confirm a fresh build wrote the audit CSV: `ls -la data/dedup_candidate_pairs.csv` exists and has a `pair_key` header plus candidate rows.
    2. Open `data/dedup_candidate_pairs.csv`. Each row is a (checklist record, Ecdysis specimen) candidate pair with distance_m, dates, and both collectors. Review them — these are conservative flags (false-split biased; expect noise).
    3. For each pair you judge to be a TRUE cross-source duplicate, copy its `pair_key` into `data/dbt/seeds/dedup_decisions.csv` with `dedup_status=confirmed` and a short `note`. Mark clear non-duplicates `rejected` if you want a record; leave genuinely uncertain pairs OUT (unreviewed → both points keep rendering, the safe default).
    4. Re-run `bash data/dbt/run.sh seed && bash data/dbt/run.sh build` then `cd data && uv run python run.py` (or just the dedup steps) — the `dedup-gate` step must print "dedup-gate: OK"; if it reports orphaned confirmed pair_keys, a pair_key you confirmed no longer matches a generated candidate (fix the typo or re-confirm).
    5. Confirm the fast suite is green: `cd data && uv run pytest -m 'not integration'`.
  </how-to-verify>
  <resume-signal>Type "approved" once you have reviewed dedup_candidate_pairs.csv and recorded your confirmations (or confirmed there are none to confirm). Phase 137 may then begin.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| committed seed → build | dedup_decisions.csv is human-edited input that drives point suppression downstream |
| build gate → Phase 137 | The gate is the only guard that a confirmed suppression maps to a real candidate |

Static-hosted offline pipeline; no runtime server, no network input, no auth surface. Threat surface is data-integrity / human-in-the-loop integrity.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-136-02 | Tampering | malicious/malformed dedup_decisions.csv silently suppressing real points | mitigate | check_dedup_gate() fails the build on any confirmed pair_key absent from regenerated candidates (no suppression without a live candidate); accepted_values seed test rejects statuses outside {confirmed,rejected}; unreviewed = NULL = not suppressed |
| T-136-07 | Repudiation | a confirmed suppression with no rationale | mitigate | seed `note` column records curator rationale; audit CSV is regenerated every build as the reviewable surface |
| T-136-08 | Integrity | nightly rebuild clobbering a human decision | mitigate | D-01 two-file split: build only rewrites the audit CSV; decisions seed is committed and never auto-written; LEFT JOIN by stable pair_key |
| T-136-SC | Tampering | npm/pip/cargo installs | mitigate | No new packages; existing duckdb/dbt/stdlib only (RESEARCH Package Legitimacy Audit) |
</threat_model>

<verification>
- `cd data && uv run pytest tests/test_checklist_dedup.py -x` — all 11 tests green.
- `cd data && uv run pytest -m 'not integration'` — full fast suite green.
- `bash data/dbt/run.sh build` — green; occurrences contract unchanged at 33 columns.
- `data/dedup_candidate_pairs.csv` produced; dedup-gate prints OK.
- HUMAN-REVIEW GATE reached before Phase 137.
</verification>

<success_criteria>
- int_checklist_dedup_status exposes dedup_status (confirmed/NULL) per collapsed record; Phase 137 can filter on it.
- check_dedup_gate blocks the build on stale confirmed pair_keys.
- run.py runs dedup-candidates + dedup-gate between dbt-build and generate-sqlite.
- All 11 dedup tests + full dbt build green; contract unchanged.
- Curator review of dedup_candidate_pairs.csv completed; HUMAN-REVIEW GATE satisfied.
</success_criteria>

<output>
Create `.planning/phases/136-deduplication/136-04-SUMMARY.md` when done
</output>
