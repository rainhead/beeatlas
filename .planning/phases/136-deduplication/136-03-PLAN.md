---
phase: 136-deduplication
plan: 03
type: execute
wave: 3
depends_on: ["136-02"]
files_modified:
  - data/dbt/models/intermediate/int_dedup_candidates.sql
  - data/checklist_dedup.py
autonomous: true
requirements: [DUP-02]

must_haves:
  truths:
    - "int_dedup_candidates pairs collapsed checklist records against Ecdysis specimens on exact canonical_name + coarser-shared-precision date + 1.0 km proximity"
    - "ST_Distance_Sphere uses lat-first ST_Point(lat, lon) — the load-bearing axis-order trap is honored and commented"
    - "NULL-date (date_quality != 'full' or NULL Ecdysis date) rows are never candidates"
    - "NULL-coordinate rows are never candidates"
    - "write_dedup_candidates() applies the D-05 collector filter in Python and writes dedup_candidate_pairs.csv with pair_key + _csv_safe cells"
  artifacts:
    - path: "data/dbt/models/intermediate/int_dedup_candidates.sql"
      provides: "DUP-02 cross-source candidate join (collapsed × Ecdysis); carries raw collector strings for Python filtering"
      contains: "ST_Distance_Sphere"
    - path: "data/checklist_dedup.py"
      provides: "write_dedup_candidates() — queries int_dedup_candidates, applies _collectors_match, writes audit CSV"
      contains: "def write_dedup_candidates"
  key_links:
    - from: "data/dbt/models/intermediate/int_dedup_candidates.sql"
      to: "int_checklist_collapsed"
      via: "ref() join (post-collapse survivor ObjectID — pair_key stability)"
      pattern: "ref\\('int_checklist_collapsed'\\)"
    - from: "data/checklist_dedup.py"
      to: "int_dedup_candidates"
      via: "SELECT from dbt_sandbox.int_dedup_candidates"
      pattern: "int_dedup_candidates"
---

<objective>
Implement DUP-02 cross-source candidate generation: the `int_dedup_candidates` dbt model joining `int_checklist_collapsed` × `int_ecdysis_base` on exact accepted-name + coarser-shared-precision date (D-06) + 1.0 km proximity (D-07, lat-first ST_Distance_Sphere), and the Python `write_dedup_candidates()` writer that applies the D-05 collector filter and emits `dedup_candidate_pairs.csv`.

Purpose: This is the conservative cross-source flag (false-split bias). The candidate model MUST take `int_checklist_collapsed` (not stg) as input so pair_key carries the post-collapse survivor ObjectID (pair_key stability, D-02). The collector token-set filter from 136-02 is applied here in Python (cleaner than SQL initials logic).
Output: One implemented spatial-join model, one implemented CSV writer, four DUP-02 tests green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/136-deduplication/136-CONTEXT.md
@.planning/phases/136-deduplication/136-RESEARCH.md
@.planning/phases/136-deduplication/136-PATTERNS.md
@.planning/phases/136-deduplication/136-02-SUMMARY.md

<interfaces>
<!-- int_dedup_candidates output columns the Python writer reads -->
pair_key (= "<ObjectID>|<ecdysis_id>"), checklist_ObjectID, ecdysis_id, canonical_name,
checklist_lat, checklist_lon, ecdysis_lat, ecdysis_lon, distance_m,
checklist_year, checklist_month, checklist_day, date_quality, ecdysis_date,
ecdysis_year, ecdysis_month, ecdysis_day,
checklist_collector, ecdysis_collector

<!-- Ecdysis source (int_ecdysis_base): NO day column — derive via TRY_CAST -->
ecdysis_id, ecdysis_lat, ecdysis_lon, event_date (VARCHAR), year, month, recordedBy, canonical_name

<!-- Collector filter from 136-02 -->
checklist_dedup._collectors_match(checklist_collector, ecdysis_collector) -> bool
checklist_dedup.DEDUP_DISTANCE_THRESHOLD_M = 1000.0
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement int_dedup_candidates spatial+date+name join (DUP-02, D-06, D-07)</name>
  <files>data/dbt/models/intermediate/int_dedup_candidates.sql</files>
  <read_first>
    - data/dbt/models/intermediate/int_dedup_candidates.sql (the 136-01 placeholder being implemented)
    - data/dbt/models/intermediate/int_ecdysis_base.sql (lines 7-26 — Ecdysis columns: ecdysis_id, ecdysis_lat, ecdysis_lon, event_date VARCHAR, year, month, recordedBy, canonical_name; NO day column)
    - data/dbt/models/intermediate/int_checklist_collapsed.sql (the 136-02 collapse output — the join's left input, carrying the survivor ObjectID)
    - data/dbt/models/marts/occurrences.sql (lines 24-42 — ST_Point(lon, lat) convention used elsewhere for ST_Within; this is the WRONG order for ST_Distance_Sphere)
    - .planning/phases/136-deduplication/136-PATTERNS.md (lines 77-174 — full candidate-join pattern with the axis-order comment and bounding-box prefilter)
    - .planning/phases/136-deduplication/136-RESEARCH.md (lines 239-310, 507-567 — D-06 date precision SQL, Pitfall 1 axis order, Pitfall 3 year_only leak, Pitfall 6 event_date precision)
  </read_first>
  <behavior>
    - A checklist record and an Ecdysis specimen with same canonical_name + same Y-M-D (both have day) within 1.0 km appear as a candidate pair.
    - A month-precision checklist record (day NULL) matches a full-date Ecdysis of the same Y-M within 1.0 km (coarser shared precision, D-06).
    - A checklist row with date_quality != 'full' is never a candidate.
    - An Ecdysis row with NULL/year-only event_date is never a candidate.
    - A pair just inside 1.0 km is a candidate; a pair just outside is not (test_distance_1km_window guards lat-first axis order).
    - NULL-coord rows on either side are never candidates.
  </behavior>
  <action>
    Implement `int_dedup_candidates.sql` with `{{ config(materialized='table') }}`. Build an `ecdysis_dated` CTE selecting from `{{ ref('int_ecdysis_base') }}` with `TRY_CAST(EXTRACT('day' FROM TRY_CAST(event_date AS DATE)) AS INTEGER) AS day`, filtered to `ecdysis_lat/lon IS NOT NULL AND year IS NOT NULL AND month IS NOT NULL AND event_date IS NOT NULL` (D-06: exclude year-only/NULL Ecdysis dates). Join `{{ ref('int_checklist_collapsed') }} cl` to `ecdysis_dated ec` on: `cl.canonical_name = ec.canonical_name`; `cl.date_quality = 'full'` (D-06 — filter on date_quality, NOT just year IS NOT NULL, per Pitfall 3/Anti-Pattern 4); `cl.year = ec.year AND cl.month = ec.month`; day compared only when both present (`cl.day IS NULL OR ec.day IS NULL OR cl.day = ec.day`); `cl.lat/lon IS NOT NULL`; a bounding-box prefilter (`ABS` lat/lon deltas) then the precise `ST_Distance_Sphere(ST_Point(cl.lat, cl.lon), ST_Point(ec.ecdysis_lat, ec.ecdysis_lon)) <= 1000.0`.

    CRITICAL: ST_Distance_Sphere uses `ST_Point(lat, lon)` — latitude FIRST — the OPPOSITE of `ST_Point(lon, lat)` used everywhere else for ST_Within. Carry a prominent header comment in this file documenting the axis-order trap and the 1000.0 m = D-07 named threshold (verified: 1 deg lat ≈ 111 km with lat-first, ≈ 59 km if swapped).

    SELECT the pair_key `(CAST(cl.ObjectID AS VARCHAR) || '|' || CAST(ec.ecdysis_id AS VARCHAR))` and all interface columns including `distance_m`, both collectors as raw strings (`checklist_collector`, `ecdysis_collector`), and date fields. Do NOT apply the collector filter in SQL — that is Python (D-05, Task 2). Do NOT touch int_combined / occurrences mart / contract.
  </action>
  <verify>
    <automated>cd data && uv run pytest tests/test_checklist_dedup.py::test_null_date_excluded_from_candidates tests/test_checklist_dedup.py::test_null_coord_excluded_from_candidates tests/test_checklist_dedup.py::test_distance_1km_window -x</automated>
  </verify>
  <acceptance_criteria>
    - `pytest tests/test_checklist_dedup.py::test_null_date_excluded_from_candidates -x` passes.
    - `pytest tests/test_checklist_dedup.py::test_null_coord_excluded_from_candidates -x` passes.
    - `pytest tests/test_checklist_dedup.py::test_distance_1km_window -x` passes (1.0 km window correct → axis order correct).
    - The file refs `int_checklist_collapsed` (NOT `stg_checklist__records_full`) for pair_key stability.
    - `grep -c "ST_Point(cl.lat, cl.lon)" data/dbt/models/intermediate/int_dedup_candidates.sql` returns ≥1 (lat-first); the file carries an axis-order comment (`grep -vi '^--' ... | grep ST_Point(lon` confirms no lon-first ST_Point feeds ST_Distance_Sphere).
    - The candidate filter uses `date_quality = 'full'`, not a bare `year IS NOT NULL`.
  </acceptance_criteria>
  <done>int_dedup_candidates joins collapsed × Ecdysis with correct axis order, coarser-precision date, 1 km window; NULL date/coord excluded; DUP-02 SQL tests green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement write_dedup_candidates() — collector filter + _csv_safe audit CSV (DUP-02, D-01, D-05)</name>
  <files>data/checklist_dedup.py</files>
  <read_first>
    - data/checklist_dedup.py (the 136-01 stub + 136-02 collector helpers)
    - data/resolve_checklist_names.py (lines 37-60 — AUDIT_CSV path + csv.DictWriter writer structure analog)
    - data/resolve_taxon_ids.py (lines 63-80 — _csv_safe() applied per cell)
    - .planning/phases/136-deduplication/136-RESEARCH.md (lines 451-480, 668-676 — write_dedup_candidates query + the NOTE that the collector filter lives in Python, not SQL)
    - .planning/phases/136-deduplication/136-PATTERNS.md (lines 345-347 — DictWriter + _csv_safe convention)
  </read_first>
  <behavior>
    - write_dedup_candidates() queries dbt_sandbox.int_dedup_candidates, keeps only pairs where _collectors_match(checklist_collector, ecdysis_collector) is True, and writes dedup_candidate_pairs.csv.
    - The CSV header includes pair_key and the documented columns; every string cell passes through _csv_safe().
    - Returns the count of pairs written.
  </behavior>
  <action>
    Implement `write_dedup_candidates()`: connect to DuckDB at `DB_PATH`, `con.execute("INSTALL spatial; LOAD spatial")` (the candidate table query path may touch spatial-typed columns), SELECT the interface columns from `dbt_sandbox.int_dedup_candidates` ordered by `canonical_name, checklist_ObjectID, ecdysis_id`. In Python, filter rows to those where `_collectors_match(checklist_collector, ecdysis_collector)` is True (D-05 — collector filter applied here, not in SQL). Write `DEDUP_CANDIDATE_CSV` with `csv.DictWriter`, applying `_csv_safe()` to each string cell (T-136-01 formula-injection guard). Return the number of pairs written and print a one-line summary (`dedup-candidates: wrote N pairs`) matching the project's step-logging style. Make the function callable with no args so it slots into run.py STEPS as `("dedup-candidates", write_dedup_candidates)` in 136-04.
  </action>
  <verify>
    <automated>cd data && uv run pytest tests/test_checklist_dedup.py::test_candidate_csv_written -x</automated>
  </verify>
  <acceptance_criteria>
    - `pytest tests/test_checklist_dedup.py::test_candidate_csv_written -x` passes.
    - The writer applies `_collectors_match` before emitting (grep confirms `_collectors_match` in write_dedup_candidates).
    - Every string cell is wrapped with `_csv_safe` (grep confirms `_csv_safe` in the writer).
    - The CSV header includes `pair_key`.
    - `write_dedup_candidates` takes no required positional args (run.py STEP-compatible).
  </acceptance_criteria>
  <done>write_dedup_candidates applies the collector token-set filter, writes a formula-injection-safe audit CSV with pair_key, returns the pair count; candidate-CSV test green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| source CSV → dedup_candidate_pairs.csv | Collector/locality strings reach the curator-opened spreadsheet |
| dbt_sandbox table → Python writer | Built candidate table read by the Python audit writer |

Static-hosted offline pipeline; no runtime server, no network input, no auth surface. Threat surface is data-integrity / supply-chain.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-136-01 | Tampering | collector/locality cells in dedup_candidate_pairs.csv | mitigate | `_csv_safe()` applied per string cell in write_dedup_candidates (formula-injection guard, verified pattern from resolve_taxon_ids.py) |
| T-136-05 | Integrity | wrong-axis ST_Distance_Sphere silently corrupting the 1 km window (over- or under-flagging) | mitigate | lat-first ST_Point(lat, lon) + mandatory axis-order comment; test_distance_1km_window guards the window with an in/out boundary pair |
| T-136-06 | Integrity | candidate generation reading stg (pre-collapse) → orphaned pair_keys | mitigate | model refs int_checklist_collapsed (post-collapse survivor ObjectID); pair_key stability invariant; the 136-04 gate detects orphans |
| T-136-SC | Tampering | npm/pip/cargo installs | mitigate | No new packages; stdlib csv/re + existing duckdb only |
</threat_model>

<verification>
- `cd data && uv run pytest tests/test_checklist_dedup.py -x` — DUP-01 + DUP-02 tests green (status/gate tests still red until 136-04).
- `cd data && uv run pytest -m 'not integration'` — no fast-tier regressions.
- No change to int_combined / occurrences mart / contract.
</verification>

<success_criteria>
- int_dedup_candidates joins post-collapse checklist × Ecdysis with lat-first ST_Distance_Sphere, coarser-precision date, 1 km window; NULL date/coord excluded.
- write_dedup_candidates applies the collector filter + _csv_safe, emits pair_key audit CSV.
- All four DUP-02 tests green.
</success_criteria>

<output>
Create `.planning/phases/136-deduplication/136-03-SUMMARY.md` when done
</output>
