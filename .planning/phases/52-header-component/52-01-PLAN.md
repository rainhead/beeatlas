---
phase: 52-header-component
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/bee-header.ts
  - frontend/src/tests/bee-header.test.ts
autonomous: true
requirements:
  - HDR-01
  - HDR-02
  - HDR-03
  - HDR-04

must_haves:
  truths:
    - "bee-header component accepts layerMode and viewMode as @property inputs"
    - "bee-header emits layer-changed CustomEvent with composed:true when a layer tab is clicked"
    - "bee-header emits view-changed CustomEvent with composed:true when a view icon is clicked"
    - "Specimens and Samples tabs render with active/inactive visual distinction based on layerMode prop"
    - "Species and Plants tabs render as disabled (pointer-events: none, opacity: 0.4)"
    - "Map and Table icon buttons render with active/inactive distinction based on viewMode prop"
    - "Below 640px, inline tabs are hidden and a details/summary hamburger is shown"
    - "Above 640px, hamburger is hidden and inline tabs are shown"
  artifacts:
    - path: "frontend/src/bee-header.ts"
      provides: "bee-header Lit custom element"
      exports: ["BeeHeader"]
      contains: "@customElement('bee-header')"
    - path: "frontend/src/tests/bee-header.test.ts"
      provides: "Unit tests for bee-header property interface and event emission"
      contains: "describe("
  key_links:
    - from: "frontend/src/bee-header.ts"
      to: "lit"
      via: "import { LitElement, css, html } from 'lit'"
      pattern: "import.*from 'lit'"
    - from: "frontend/src/bee-header.ts"
      to: "CustomEvent"
      via: "dispatchEvent with bubbles:true, composed:true"
      pattern: "composed: true"
---

<objective>
Create the `<bee-header>` Lit custom element with nav tabs for layer switching, disabled placeholder tabs, Map/Table view icon toggle, and responsive hamburger menu.

Purpose: Establishes the header component that Phase 53 and 54 will build on. The component is a pure presenter: props in, events out.
Output: `frontend/src/bee-header.ts` (component) and `frontend/src/tests/bee-header.test.ts` (unit tests)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/52-header-component/52-CONTEXT.md
@.planning/phases/52-header-component/52-RESEARCH.md
@.planning/phases/52-header-component/52-UI-SPEC.md

<interfaces>
<!-- Existing patterns the executor needs -->

From frontend/src/bee-sidebar.ts (CustomEvent dispatch pattern):
```typescript
this.dispatchEvent(new CustomEvent<'specimens' | 'samples'>('layer-changed', {
  bubbles: true,
  composed: true,
  detail: mode,
}));
this.dispatchEvent(new CustomEvent<'map' | 'table'>('view-changed', {
  bubbles: true,
  composed: true,
  detail: mode,
}));
```

From frontend/src/tests/bee-sidebar.test.ts (test pattern for property interface):
```typescript
const { BeeFilterControls } = await import('../bee-filter-controls.ts');
const props = (BeeFilterControls as unknown as { elementProperties: Map<string, unknown> }).elementProperties;
expect(props.has('filterState')).toBe(true);
```

From frontend/src/index.css (CSS custom properties available through shadow DOM):
```css
--accent: #2c7a2c;
--header-bg: rgb(8, 13, 38);
--text-hint: #767676;
```

From frontend/index.html (GitHub SVG icon to move into bee-header):
```html
<a href="https://github.com/rainhead/beeatlas" target="_blank" rel="noopener" aria-label="GitHub repository" class="github-link">
  <svg xmlns="http://www.w3.org/2000/svg" width="24" height="24" viewBox="0 0 24 24" fill="currentColor" aria-hidden="true">
    <path d="M12 0C5.374 0 0 5.373 0 12c0 5.302 3.438 9.8 8.207 11.387.599.111.793-.261.793-.577v-2.234c-3.338.726-4.033-1.416-4.033-1.416-.546-1.387-1.333-1.756-1.333-1.756-1.089-.745.083-.729.083-.729 1.205.084 1.839 1.237 1.839 1.237 1.07 1.834 2.807 1.304 3.492.997.107-.775.418-1.305.762-1.604-2.665-.305-5.467-1.334-5.467-5.931 0-1.311.469-2.381 1.236-3.221-.124-.303-.535-1.524.117-3.176 0 0 1.008-.322 3.301 1.23A11.509 11.509 0 0 1 12 5.803c1.02.005 2.047.138 3.006.404 2.291-1.552 3.297-1.23 3.297-1.23.653 1.653.242 2.874.118 3.176.77.84 1.235 1.911 1.235 3.221 0 4.609-2.807 5.624-5.479 5.921.43.372.823 1.102.823 2.222v3.293c0 .319.192.694.801.576C20.566 21.797 24 17.3 24 12c0-6.627-5.373-12-12-12z"/>
  </svg>
</a>
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create bee-header test file (Wave 0)</name>
  <files>frontend/src/tests/bee-header.test.ts</files>
  <read_first>
    - frontend/src/tests/bee-sidebar.test.ts (test pattern: vi.mock, elementProperties inspection, dynamic import)
    - frontend/src/bee-sidebar.ts (existing CustomEvent dispatch pattern to verify against)
  </read_first>
  <behavior>
    - Test: BeeHeader has @property declarations for `layerMode` and `viewMode` (via elementProperties map)
    - Test: BeeHeader has @customElement decorator registering tag 'bee-header'
    - Test: Clicking a layer tab dispatches `layer-changed` CustomEvent with correct detail value
    - Test: Clicking an inactive layer tab dispatches event; clicking the already-active tab does NOT dispatch
    - Test: Clicking a view icon button dispatches `view-changed` CustomEvent with correct detail value
    - Test: Species and Plants elements exist in the shadow DOM with disabled attribute or pointer-events:none styling
    - Test: A `<details>` element exists in shadow DOM (hamburger container)
  </behavior>
  <action>
    Create `frontend/src/tests/bee-header.test.ts` following the established test pattern from `bee-sidebar.test.ts`:

    1. Add the same `vi.mock` blocks for `../duckdb.ts`, `../features.ts`, and `../region-layer.ts` (needed because bee-header imports from lit which may trigger side-effect chains in happy-dom).

    2. Write test suite `describe('HDR: bee-header property interface')`:
       - Test `layerMode` and `viewMode` exist on `elementProperties` map (dynamic import `../bee-header.ts`, cast to `{ elementProperties: Map<string, unknown> }`)

    3. Write test suite `describe('HDR: bee-header event emission')`:
       - Create element via `document.createElement('bee-header')`, append to document body
       - Set `.layerMode = 'specimens'`
       - Add event listener for `layer-changed`, click the Samples tab button (query shadow DOM for button with text "Samples"), assert event.detail === 'samples'
       - Set `.layerMode = 'specimens'` again, click Specimens tab, assert NO event fires (no-op for already active)
       - Set `.viewMode = 'map'`, listen for `view-changed`, click the Table view button (query by aria-label "Table view"), assert event.detail === 'table'

    4. Write test suite `describe('HDR: disabled placeholder tabs')`:
       - Query shadow DOM for elements containing text "Species" and "Plants"
       - Assert they have `disabled` attribute or are `<span>` elements (not buttons)

    5. Write test suite `describe('HDR: hamburger menu')`:
       - Query shadow DOM for `details` element, assert it exists
       - Query for `summary` element inside it, assert it exists

    Tests should FAIL initially (bee-header.ts does not exist yet). That is the expected RED state.
  </action>
  <verify>
    <automated>cd frontend && npm test -- --run tests/bee-header.test.ts 2>&1 | tail -5</automated>
  </verify>
  <done>Test file exists at frontend/src/tests/bee-header.test.ts with failing tests for property interface, event emission, disabled tabs, and hamburger element</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement bee-header component (GREEN)</name>
  <files>frontend/src/bee-header.ts</files>
  <read_first>
    - frontend/src/tests/bee-header.test.ts (tests to satisfy)
    - frontend/src/bee-sidebar.ts (CustomEvent dispatch pattern lines 286-317, toggle CSS lines 145-169)
    - frontend/src/index.css (CSS custom properties: --header-bg, --accent, --text-hint)
    - frontend/index.html (h1 and GitHub SVG icon to absorb into component per D-01)
    - .planning/phases/52-header-component/52-UI-SPEC.md (visual contract)
    - .planning/phases/52-header-component/52-CONTEXT.md (locked decisions D-01 through D-08)
  </read_first>
  <behavior>
    - layerMode prop controls which tab shows as active (accent color + underline)
    - viewMode prop controls which icon button shows as active
    - Clicking inactive layer tab emits layer-changed with bubbles:true, composed:true
    - Clicking inactive view icon emits view-changed with bubbles:true, composed:true
    - Species/Plants tabs are disabled spans with opacity:0.4, pointer-events:none
    - details/summary hamburger present, hidden above 640px, shown at/below 640px
    - Inline tabs hidden at/below 640px, shown above 640px
  </behavior>
  <action>
    Create `frontend/src/bee-header.ts` as a Lit custom element per D-01. The component absorbs the h1 title ("BeeAtlas") and GitHub icon link from index.html into its template (cleaner than splitting across files, per RESEARCH.md recommendation).

    **Imports:**
    ```typescript
    import { LitElement, css, html } from 'lit';
    import { customElement, property, state } from 'lit/decorators.js';
    ```

    **Class declaration:**
    ```typescript
    @customElement('bee-header')
    export class BeeHeader extends LitElement {
      @property({ attribute: false })
      layerMode: 'specimens' | 'samples' = 'specimens';

      @property({ attribute: false })
      viewMode: 'map' | 'table' = 'map';
    ```

    Note: `_hamburgerOpen` @state is NOT needed per RESEARCH.md Open Question 2 -- let native `<details>` manage open/close. No icon swap (hamburger stays as hamburger icon). Per D-03, hamburger state is internal; since we use native `<details>`, it is inherently internal with zero JS. Do NOT add `@state() private _hamburgerOpen`.

    **Static styles** (all in shadow DOM, per D-07 breakpoint at 640px):

    `:host` -- `display: flex; justify-content: space-between; align-items: center; width: 100%; background-color: var(--header-bg); color: white;`

    `.left-group` -- `display: flex; align-items: center; gap: 0;`

    `h1` -- `font-size: 1.2rem; margin: 1rem 0.5rem; font-weight: 400;` (matches existing index.css h1)

    `.inline-tabs` -- `display: flex; align-items: stretch;`

    `.tab-btn` -- Per UI-SPEC: `padding: 0.6rem 1rem; border: none; border-bottom: 2px solid transparent; background: transparent; cursor: pointer; font-size: 0.9rem; font-weight: 500; color: rgba(255, 255, 255, 0.7);`

    `.tab-btn:hover` -- `color: white; background: rgba(255, 255, 255, 0.08);`

    `.tab-btn.active` -- `color: var(--accent); border-bottom-color: var(--accent);`

    `.tab-btn[disabled]` -- `opacity: 0.4; pointer-events: none; cursor: default;`

    `.right-group` -- `display: flex; align-items: center; gap: 4px; padding-right: 0.5rem;`

    `.icon-btn` -- `background: transparent; border: none; cursor: pointer; color: white; opacity: 0.6; padding: 10px; min-width: 44px; min-height: 44px; display: flex; align-items: center; justify-content: center; border-bottom: 2px solid transparent;` (44px touch target per UI-SPEC)

    `.icon-btn:hover` -- `opacity: 0.9;`

    `.icon-btn.active` -- `opacity: 1.0; border-bottom-color: var(--accent);`

    `.github-link` -- `color: white; display: flex; align-items: center; padding: 0 0.5rem; opacity: 0.8; text-decoration: none;`

    `.github-link:hover` -- `opacity: 1;`

    `.hamburger-menu` -- `display: none;`

    `.hamburger-menu summary` -- `list-style: none; cursor: pointer; color: white; font-size: 1.5rem; padding: 0.5rem; min-width: 44px; min-height: 44px; display: flex; align-items: center; justify-content: center;`

    `.hamburger-menu summary::-webkit-details-marker` -- `display: none;`

    `.hamburger-items` -- `position: absolute; top: 100%; left: 0; right: 0; background: var(--header-bg); z-index: 100; display: flex; flex-direction: column; border-top: 1px solid rgba(255,255,255,0.1);`

    `.hamburger-items .tab-btn` -- `text-align: left; padding: 0.8rem 1rem;`

    **Responsive breakpoint (per D-07):**
    ```css
    @media (max-width: 640px) {
      .inline-tabs { display: none; }
      .hamburger-menu { display: block; position: relative; }
    }
    ```

    **Render method:**

    Left group: `<h1>BeeAtlas</h1>` + `.inline-tabs` containing 4 tab buttons (Specimens, Samples as clickable; Species, Plants as `disabled` buttons) + `.hamburger-menu` `<details>/<summary>` with same 4 items.

    Right group: Map icon button + Table icon button + GitHub link (SVG from index.html).

    **Tab rendering helper** `_renderTabItems()` returning an array of `html` templates:
    - `<button class="tab-btn ${this.layerMode === 'specimens' ? 'active' : ''}" @click=${() => this._onLayerClick('specimens')}>Specimens</button>`
    - `<button class="tab-btn ${this.layerMode === 'samples' ? 'active' : ''}" @click=${() => this._onLayerClick('samples')}>Samples</button>`
    - `<button class="tab-btn" disabled>Species</button>`
    - `<button class="tab-btn" disabled>Plants</button>`

    **Event methods** (per D-02, identical shape to bee-sidebar):
    - `_onLayerClick(mode: 'specimens' | 'samples')` -- early return if `mode === this.layerMode`, then `this.dispatchEvent(new CustomEvent('layer-changed', { bubbles: true, composed: true, detail: mode }))`
    - `_onViewClick(mode: 'map' | 'table')` -- early return if `mode === this.viewMode`, then `this.dispatchEvent(new CustomEvent('view-changed', { bubbles: true, composed: true, detail: mode }))`

    **Inline SVG icons** (per D-04, Heroicons outline 24x24):
    - Map icon: Use Heroicons `map` outline path `<svg xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 24 24" stroke-width="1.5" stroke="currentColor" width="24" height="24"><path stroke-linecap="round" stroke-linejoin="round" d="M9 6.75V15m6-6v8.25m.503 3.498 4.875-2.437c.381-.19.622-.58.622-1.006V4.82c0-.836-.88-1.38-1.628-1.006l-3.869 1.934c-.317.159-.69.159-1.006 0L9.503 3.252a1.125 1.125 0 0 0-1.006 0L3.622 5.689C3.24 5.88 3 6.27 3 6.695V19.18c0 .836.88 1.38 1.628 1.006l3.869-1.934c.317-.159.69-.159 1.006 0l4.994 2.497c.317.158.69.158 1.006 0Z"/></svg>`
    - Table icon: Use Heroicons `table-cells` outline path `<svg xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 24 24" stroke-width="1.5" stroke="currentColor" width="24" height="24"><path stroke-linecap="round" stroke-linejoin="round" d="M3.375 19.5h17.25m-17.25 0a1.125 1.125 0 0 1-1.125-1.125M3.375 19.5h7.5c.621 0 1.125-.504 1.125-1.125m-9.75 0V5.625m0 12.75v-12.75M3.375 5.625c0-.621.504-1.125 1.125-1.125h16.5c.621 0 1.125.504 1.125 1.125v12.75c0 .621-.504 1.125-1.125 1.125m-17.25 0h7.5m9.75 0h-9.75m9.75 0V5.625m0 12.75V5.625m0 0H10.875M3.375 5.625h7.5m0 0v12.75m0-12.75h9.75"/></svg>`

    Use `fill="none"` and `stroke="currentColor"` so CSS `color` controls icon color.

    Each icon button: `<button class="icon-btn ${this.viewMode === 'map' ? 'active' : ''}" aria-label="Map view" @click=${() => this._onViewClick('map')}>[map SVG]</button>` (and similar for table).
  </action>
  <verify>
    <automated>cd frontend && npm test -- --run tests/bee-header.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - frontend/src/bee-header.ts contains `@customElement('bee-header')`
    - frontend/src/bee-header.ts contains `@property({ attribute: false })` for both layerMode and viewMode
    - frontend/src/bee-header.ts contains `new CustomEvent('layer-changed', { bubbles: true, composed: true`
    - frontend/src/bee-header.ts contains `new CustomEvent('view-changed', { bubbles: true, composed: true`
    - frontend/src/bee-header.ts contains `<details` (hamburger container)
    - frontend/src/bee-header.ts contains `<summary` (hamburger trigger)
    - frontend/src/bee-header.ts contains `@media (max-width: 640px)`
    - frontend/src/bee-header.ts contains `disabled>Species` and `disabled>Plants`
    - frontend/src/bee-header.ts contains `aria-label="Map view"` and `aria-label="Table view"`
    - frontend/src/bee-header.ts contains `min-width: 44px` (touch target)
    - frontend/src/bee-header.ts contains `opacity: 0.4` (disabled tab styling)
    - All tests in frontend/src/tests/bee-header.test.ts pass
    - `cd frontend && npm test -- --run` exits 0 (full suite green)
  </acceptance_criteria>
  <done>bee-header.ts implements all HDR requirements; all bee-header tests pass; full test suite green</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| component props | layerMode and viewMode received from bee-atlas; typed string literals only |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-52-01 | Tampering | CustomEvent detail | accept | Event detail is typed string literal ('specimens'/'samples'/'map'/'table'); bee-atlas handler already validates against known values. No user-supplied strings enter event payloads. |
| T-52-02 | Information Disclosure | inline SVG | accept | SVG paths are static Heroicons content, no dynamic data. No PII or secrets. |
</threat_model>

<verification>
- `cd frontend && npm test -- --run tests/bee-header.test.ts` passes
- `cd frontend && npm test -- --run` full suite green
- `grep -c "customElement.*bee-header" frontend/src/bee-header.ts` returns 1
</verification>

<success_criteria>
- bee-header.ts exists as a self-contained Lit custom element
- All 4 HDR requirements addressed in component implementation
- Unit tests verify property interface, event emission, disabled placeholders, and hamburger element
- Full test suite remains green
</success_criteria>

<output>
After completion, create `.planning/phases/52-header-component/52-01-SUMMARY.md`
</output>
