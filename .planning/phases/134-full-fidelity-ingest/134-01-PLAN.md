---
phase: 134-full-fidelity-ingest
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - data/checklists/checklist_records_full.csv
  - data/pyproject.toml
  - data/uv.lock
autonomous: true
requirements: [ING-01]
must_haves:
  truths:
    - "The full-fidelity Bartholomew CSV (50,646 data rows) is committed under data/checklists/ and tracked by git-LFS"
    - "dateparser, pygbif, and rapidfuzz appear in data/pyproject.toml [project] dependencies"
    - "uv sync resolves and installs all three new dependencies under Python 3.14 with exit 0"
  artifacts:
    - path: "data/checklists/checklist_records_full.csv"
      provides: "Committed source data for the full-fidelity loader (LFS-tracked)"
      contains: "ObjectID,Family,Genus,Scientific Name,Locality,Latitude,Longitude,Date,recordedBy,County_join,x,y"
    - path: "data/pyproject.toml"
      provides: "Three new v4.7 pip dependencies"
      contains: "dateparser"
    - path: "data/uv.lock"
      provides: "Locked resolution proving deps install on Python 3.14"
  key_links:
    - from: "data/pyproject.toml"
      to: "data/uv.lock"
      via: "uv sync regenerates the lockfile with the three new deps"
      pattern: "dateparser"
    - from: ".gitattributes"
      to: "data/checklists/checklist_records_full.csv"
      via: "*.csv LFS filter routes the committed CSV through git-LFS"
      pattern: "filter=lfs"
---

<objective>
Commit the full-fidelity Bartholomew et al. 2024 source CSV into the repository (git-LFS tracked) and add the three v4.7 pip dependencies (`dateparser`, `pygbif`, `rapidfuzz`) to `data/pyproject.toml`, confirming they install cleanly under Python 3.14.

Purpose: This plan establishes the two prerequisites the loader plan (134-02) depends on — the committed data file it reads and the dependencies the milestone requires (per success-criterion #4). `pygbif`/`rapidfuzz` are not *used* in Phase 134 (they are Phase 135 tooling) but are added now because SC#4 mandates all three install cleanly under Python 3.14 in this phase (D-11).

Output: `data/checklists/checklist_records_full.csv` (LFS), updated `data/pyproject.toml` + `data/uv.lock`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/134-full-fidelity-ingest/134-CONTEXT.md
@.planning/phases/134-full-fidelity-ingest/134-PATTERNS.md
@.planning/research/STACK.md

<notes>
- Source file location: `/home/peter/final_checklist_records.csv` (50,646 data rows + 1 header = 50,647 lines). This is OUTSIDE the repo; it must be copied into `data/checklists/`.
- The committed filename is `checklist_records_full.csv` (planner's choice per PATTERNS.md; CONTEXT does not lock it). The loader plan (134-02) references this exact name via the `CHECKLIST_RECORDS_FULL_PATH` constant.
- `.gitattributes` already contains `*.csv filter=lfs diff=lfs merge=lfs -text` — LFS routing is automatic, NO `.gitattributes` change is needed.
- CSV header (verified against the real file): `ObjectID,Family,Genus,Scientific Name,Locality,Latitude,Longitude,Date,recordedBy,County_join,x,y`.
- Package legitimacy basis: all three deps are vetted in `.planning/research/STACK.md` (§"Version Compatibility" table + per-problem sections) with PyPI URLs, licenses (MIT / MIT / BSD-3-Clause), and Python 3.14 wheel status. They are NOT unknown/assumed packages.
- D-11 watch item: `dateparser` pulls in `regex` (a C-extension wheel). STACK.md rates 3.14 compatibility MEDIUM. If `uv sync` fails to build/resolve `regex`/`dateparser` on 3.14, this is a SC#4 BLOCKER and must be surfaced (see Task 2 acceptance criteria).
- `.nvmrc` / Node is irrelevant here; this is the Python (`uv`) workspace under `data/`.
</notes>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Commit the full-fidelity source CSV under data/checklists/ (LFS-tracked)</name>
  <read_first>
    - .gitattributes (confirm the `*.csv filter=lfs` line already routes CSVs through LFS — do NOT edit it)
    - .planning/phases/134-full-fidelity-ingest/134-PATTERNS.md (§"data/checklists/checklist_records_full.csv" — analog `wa_bee_checklist_records.tsv`, LFS routing)
  </read_first>
  <action>
    Copy `/home/peter/final_checklist_records.csv` to `data/checklists/checklist_records_full.csv` (use the exact target filename `checklist_records_full.csv` — the 134-02 loader's `CHECKLIST_RECORDS_FULL_PATH` constant depends on it). Do NOT modify, re-encode, re-order columns, or strip rows — commit the file byte-for-byte as the source of record. Do NOT touch `wa_bee_checklist_records.tsv` or any existing file in the directory (D-10: add-only). Stage the new CSV with git; because `.gitattributes` already routes `*.csv` through git-LFS, staging it automatically converts it to an LFS pointer — confirm this rather than committing the raw blob. Do NOT edit `.gitattributes`.
  </action>
  <verify>
    <automated>test -f data/checklists/checklist_records_full.csv && git check-attr filter -- data/checklists/checklist_records_full.csv | grep -q 'filter: lfs' && [ "$(wc -l < data/checklists/checklist_records_full.csv)" -eq 50647 ]</automated>
  </verify>
  <acceptance_criteria>
    - `data/checklists/checklist_records_full.csv` exists.
    - `git check-attr filter -- data/checklists/checklist_records_full.csv` prints `filter: lfs` (file routed through git-LFS).
    - `wc -l data/checklists/checklist_records_full.csv` reports `50647` (50,646 data rows + 1 header).
    - First line of the file equals `ObjectID,Family,Genus,Scientific Name,Locality,Latitude,Longitude,Date,recordedBy,County_join,x,y`.
    - `git status --short data/checklists/wa_bee_checklist_records.tsv` is empty (the old TSV is untouched — D-10).
    - After `git add`, `git diff --cached -- data/checklists/checklist_records_full.csv` shows an LFS pointer (`oid sha256:`), not 50k lines of CSV text.
  </acceptance_criteria>
  <done>The full-fidelity CSV is committed at data/checklists/checklist_records_full.csv as an LFS-tracked object with the correct header and 50,647 lines; the old TSV is untouched.</done>
</task>

<task type="auto">
  <name>Task 2: Add dateparser, pygbif, rapidfuzz to pyproject.toml and confirm they install on Python 3.14</name>
  <read_first>
    - data/pyproject.toml (the current [project] dependencies list — lines 1-14 per PATTERNS.md; append, do not reformat existing entries)
    - .planning/research/STACK.md (§"pyproject.toml Delta" and §"Version Compatibility" — the dateparser/regex 3.14 watch item)
    - .planning/phases/134-full-fidelity-ingest/134-CONTEXT.md (D-11)
  </read_first>
  <action>
    Append `dateparser`, `pygbif`, and `rapidfuzz` to the `[project].dependencies` list in `data/pyproject.toml` (D-11). Use bare unpinned names matching the existing `"requests"` / `"beautifulsoup4"` style — STACK.md notes minimum versions (dateparser>=1.4.0, pygbif>=0.6.6, rapidfuzz>=3.14.5) but CONTEXT does not lock pins; bare names let `uv` pick the latest 3.14-compatible release. Leave the `[dependency-groups].dev` block untouched. Then run `uv sync` from the `data/` directory to resolve and install; this regenerates `data/uv.lock`. D-11 watch item: `dateparser` depends on the `regex` C-extension wheel rated MEDIUM-confidence for 3.14 in STACK.md. If `uv sync` fails to build or resolve `regex`/`dateparser` under Python 3.14, do NOT silently work around it — record it as a SC#4 BLOCKER in the plan SUMMARY (quote the `uv` error) and stop, because SC#4 mandates all three install cleanly. Note for the SUMMARY: 134's date parsing is stdlib-first (D-09) and does not functionally depend on dateparser, but SC#4 still requires it to install.
  </action>
  <verify>
    <automated>cd data && grep -v '^#' pyproject.toml | grep -q dateparser && grep -v '^#' pyproject.toml | grep -q pygbif && grep -v '^#' pyproject.toml | grep -q rapidfuzz && uv sync && uv run python -c "import dateparser, pygbif, rapidfuzz; print('all three import OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `data/pyproject.toml` `[project].dependencies` lists `dateparser`, `pygbif`, and `rapidfuzz`.
    - `cd data && uv sync` exits 0 (deps resolve and install under Python 3.14).
    - `cd data && uv run python -c "import dateparser, pygbif, rapidfuzz"` exits 0.
    - `data/uv.lock` is regenerated and contains entries for `dateparser`, `pygbif`, `rapidfuzz`, and `regex`.
    - The `[dependency-groups].dev` block in `pyproject.toml` is unchanged (only the `[project].dependencies` list grew).
    - IF `uv sync` fails on `dateparser`/`regex`: the SUMMARY records a SC#4 BLOCKER with the verbatim `uv` error and the task stops (no silent removal of dateparser).
  </acceptance_criteria>
  <done>All three v4.7 dependencies are in pyproject.toml, uv sync succeeds on Python 3.14, uv.lock is updated, and all three packages import — OR a dateparser/regex 3.14 install failure is surfaced as a SC#4 blocker in the SUMMARY.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| external file → repo | `/home/peter/final_checklist_records.csv` is copied into the repo and becomes committed source-of-record data |
| PyPI → build environment | three new pip dependencies are resolved and installed into the `data/` uv environment |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-134-01 | Tampering | committed CSV content | accept | Source CSV is trusted (developer-supplied, Bartholomew et al. 2024 dataset); copied byte-for-byte, no transformation. Row-count + header acceptance checks detect accidental truncation/corruption. |
| T-134-SC | Tampering | dateparser/pygbif/rapidfuzz installs | mitigate | All three vetted in `.planning/research/STACK.md` with PyPI URLs, licenses, and Python 3.14 wheel status (not `[ASSUMED]`/`[SUS]`/`[SLOP]`). `uv.lock` pins exact resolved versions + hashes. No build-time install of unknown packages; no legitimacy checkpoint required because the audit exists in STACK.md. |
| T-134-02 | Denial of Service | nightly build (regex wheel on 3.14) | mitigate | D-11 watch item: a failed `regex`/`dateparser` build is caught at `uv sync` time in this plan and surfaced as a SC#4 blocker, not deferred to a nightly failure. 134's parsing is stdlib-first (D-09) so a dateparser absence does not break the loader. |
</threat_model>

<verification>
- `data/checklists/checklist_records_full.csv` exists, is LFS-tracked, has 50,647 lines and the expected 12-column header.
- `cd data && uv sync` exits 0; `dateparser`, `pygbif`, `rapidfuzz` all import under Python 3.14.
- `data/checklists/wa_bee_checklist_records.tsv` and all other pre-existing files are byte-unchanged (D-10 add-only).
</verification>

<success_criteria>
- ING-01 (partial): the full-fidelity CSV is committed under `data/checklists/` (loader wiring is completed in 134-02).
- SC#4: `dateparser`, `pygbif`, `rapidfuzz` are in `data/pyproject.toml` and install cleanly under Python 3.14 (or a blocker is surfaced).
</success_criteria>

<output>
Create `.planning/phases/134-full-fidelity-ingest/134-01-SUMMARY.md` when done. If `uv sync` fails on dateparser/regex under 3.14, record it as a SC#4 BLOCKER in the SUMMARY with the verbatim error.
</output>
