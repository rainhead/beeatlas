---
phase: 134-full-fidelity-ingest
plan: 01
subsystem: database
tags: [python, uv, git-lfs, dateparser, pygbif, rapidfuzz, checklist]

# Dependency graph
requires: []
provides:
  - "data/checklists/checklist_records_full.csv committed as LFS-tracked source of record (50,646 rows, 12-column Bartholomew et al. 2024 CSV)"
  - "dateparser, pygbif, rapidfuzz added to data/pyproject.toml and confirmed installing under Python 3.14"
  - "data/uv.lock regenerated with all three new packages locked at exact versions"
affects: [134-02, 135, 136]

# Tech tracking
tech-stack:
  added:
    - "dateparser 1.4.0 (BSD-3-Clause) — mixed-format date parsing for historical checklist records"
    - "pygbif 0.6.6 (MIT) — GBIF species/match API for accepted-name normalization (Phase 135+)"
    - "rapidfuzz 3.14.5 (MIT) — Jaro-Winkler / token-sort-ratio misspelling candidate generation (Phase 135+)"
  patterns: []

key-files:
  created:
    - "data/checklists/checklist_records_full.csv — LFS-tracked full-fidelity source CSV (50,647 lines)"
  modified:
    - "data/pyproject.toml — three new dependencies appended to [project].dependencies"
    - "data/uv.lock — regenerated with 109 packages resolved under Python 3.14.3"

key-decisions:
  - "D-11 watch item resolved: dateparser's regex C-extension wheel installs cleanly on Python 3.14.3 (regex 2026.5.9 pre-built wheel available); no SC#4 blocker"
  - "Bare unpinned names used for all three new deps (matching existing requests/beautifulsoup4 style); uv resolves latest compatible release"
  - "CSV copied byte-for-byte from /home/peter/final_checklist_records.csv with no transformation; LFS pointer confirmed via git diff --cached (oid sha256:80c3781125d5c84aee2f78ee069630c820fdd669c7825fa272df314d47e27c9c)"

patterns-established: []

requirements-completed: [ING-01]

# Metrics
duration: 2min
completed: 2026-06-04
---

# Phase 134 Plan 01: Full-Fidelity Ingest Prerequisites Summary

**Bartholomew et al. 2024 full-fidelity CSV (50,646 rows) committed as a git-LFS object and three v4.7 pip dependencies (dateparser 1.4.0, pygbif 0.6.6, rapidfuzz 3.14.5) locked and importable under Python 3.14**

## Performance

- **Duration:** 2 min
- **Started:** 2026-06-04T17:18:07Z
- **Completed:** 2026-06-04T17:19:47Z
- **Tasks:** 2
- **Files modified:** 3 (checklist_records_full.csv, pyproject.toml, uv.lock)

## Accomplishments

- Committed `data/checklists/checklist_records_full.csv` as a git-LFS pointer (oid sha256:80c3781...) — 50,647 lines, 12-column header `ObjectID,Family,Genus,Scientific Name,Locality,Latitude,Longitude,Date,recordedBy,County_join,x,y`, untransformed byte-for-byte copy of `/home/peter/final_checklist_records.csv`
- Added `dateparser`, `pygbif`, `rapidfuzz` to `data/pyproject.toml [project].dependencies`; `uv sync` resolved and installed all 109 packages under Python 3.14.3 with exit 0
- D-11 watch item resolved: `regex 2026.5.9` (dateparser's C-extension dependency) has a pre-built Python 3.14 wheel — no SC#4 blocker; all three packages import cleanly

## Task Commits

1. **Task 1: Commit full-fidelity Bartholomew CSV (LFS-tracked)** - `dbdc51b` (feat)
2. **Task 2: Add dateparser, pygbif, rapidfuzz to pyproject.toml** - `bdf9c5f` (chore)

## Files Created/Modified

- `data/checklists/checklist_records_full.csv` — LFS-tracked full-fidelity source CSV; the loader plan (134-02) reads this via `CHECKLIST_RECORDS_FULL_PATH`
- `data/pyproject.toml` — three new dependencies appended to `[project].dependencies`; `[dependency-groups].dev` block unchanged
- `data/uv.lock` — regenerated by `uv sync` with exact locked versions for all 109 packages including `dateparser 1.4.0`, `pygbif 0.6.6`, `rapidfuzz 3.14.5`, and `regex 2026.5.9`

## Decisions Made

- **D-11 watch item resolved (no blocker):** The `regex` C-extension wheel that `dateparser` depends on resolved and installed cleanly under Python 3.14.3 (`regex 2026.5.9`). The MEDIUM-confidence rating in STACK.md was overly cautious — a pre-built 3.14 wheel was available on PyPI. SC#4 is fully satisfied.
- **Bare unpinned names used for new deps:** Consistent with the existing `"requests"` and `"beautifulsoup4"` style; `uv` resolves the latest 3.14-compatible release. STACK.md notes minimum versions (dateparser>=1.4.0, pygbif>=0.6.6, rapidfuzz>=3.14.5) but the plan does not lock pins.

## Deviations from Plan

None - plan executed exactly as written.

## Issues Encountered

None.

## Known Stubs

None — this plan commits a data file and config only; no code with stub values.

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness

- `data/checklists/checklist_records_full.csv` is committed and LFS-tracked; the 134-02 loader reads it via `CHECKLIST_RECORDS_FULL_PATH = Path(__file__).parent / "checklists" / "checklist_records_full.csv"`
- All three v4.7 pip dependencies are in `pyproject.toml` and `uv.lock`; the 134-02 loader can import `dateparser` as a stdlib-first fallback (D-09)
- No blockers for 134-02

## Self-Check: PASSED

- FOUND: data/checklists/checklist_records_full.csv
- FOUND: data/pyproject.toml
- FOUND: data/uv.lock
- FOUND: .planning/phases/134-full-fidelity-ingest/134-01-SUMMARY.md
- FOUND: dbdc51b (Task 1 commit)
- FOUND: bdf9c5f (Task 2 commit)

---
*Phase: 134-full-fidelity-ingest*
*Completed: 2026-06-04*
