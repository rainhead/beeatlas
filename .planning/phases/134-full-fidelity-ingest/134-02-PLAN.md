---
phase: 134-full-fidelity-ingest
plan: 02
type: execute
wave: 2
depends_on: ["134-01"]
files_modified:
  - data/checklist_pipeline.py
  - data/tests/test_checklist_pipeline.py
autonomous: true
requirements: [ING-01, ING-02, ING-03]
must_haves:
  truths:
    - "checklist_data.checklist_records_full table exists carrying lat, lon, parsed date (year/month/day), recordedBy, locality, verbatim_name for every source row"
    - "The table keeps all 50,646 rows; invalid coordinates are tagged via coord_flag, never dropped"
    - "Each row carries a coord_flag of valid/null_coord/zero_coord/out_of_bbox per the tight WA bbox"
    - "Each row carries year/month/day nullable ints plus a date_quality enum (full/year_only/none)"
    - "The build logs the excluded-coordinate total plus a per-reason breakdown"
    - "1812-06-18 parses to year=1812 month=6 day=18; M/D/YYYY parses US month-first; empty dates are tagged none"
    - "The old _load_checklist_records loader, checklist_records table, and county-fill consumers are untouched"
  artifacts:
    - path: "data/checklist_pipeline.py"
      provides: "New _load_checklist_records_full() loader + date-parsing helper + coord-flag helper, wired into load_checklist()"
      contains: "checklist_records_full"
    - path: "data/tests/test_checklist_pipeline.py"
      provides: "Unit tests for date parsing/coord flagging + integration tests for the new table (row count, schema, coord/date assertions, idempotency)"
      contains: "checklist_records_full"
  key_links:
    - from: "data/checklist_pipeline.py load_checklist()"
      to: "checklist_data.checklist_records_full"
      via: "_load_checklist_records_full(con) call appended after _load_checklist_records(con)"
      pattern: "_load_checklist_records_full"
    - from: "data/checklist_pipeline.py CHECKLIST_RECORDS_FULL_PATH"
      to: "data/checklists/checklist_records_full.csv"
      via: "module-level path constant read by the new loader"
      pattern: "checklist_records_full.csv"
---

<objective>
Extend `data/checklist_pipeline.py` with a new `_load_checklist_records_full()` loader that reads the committed `checklist_records_full.csv` into a NEW DuckDB table `checklist_data.checklist_records_full`, tagging each row with a `coord_flag` (tight WA bbox validation, ING-02) and normalizing the messy `Date` column into `year`/`month`/`day` nullable integers plus a `date_quality` enum (ING-03). Wire it into `load_checklist()` and add pytest coverage.

Purpose: This is the core deliverable of Phase 134 (ING-01/02/03). It is strictly ADDITIVE (D-10): the old `_load_checklist_records()`, the `checklist_records` table, `wa_bee_checklist_records.tsv`, the `sources.yml` entry, and `marts/checklist.sql` are left exactly as-is. The phase only TAGS bad coordinates; the literal exclusion-from-point-arm happens downstream in Phase 137 by filtering on `coord_flag`.

Output: extended `data/checklist_pipeline.py`, extended `data/tests/test_checklist_pipeline.py`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/134-full-fidelity-ingest/134-CONTEXT.md
@.planning/phases/134-full-fidelity-ingest/134-PATTERNS.md
@.planning/research/PITFALLS.md
@data/checklist_pipeline.py
@data/tests/test_checklist_pipeline.py

<interfaces>
<!-- Contracts the executor needs. From data/checklist_pipeline.py — already in the module. -->
<!-- Existing path-constant style (lines 22-27): module-level Path constants. Add the new one alongside. -->
CHECKLIST_RECORDS_PATH = Path(__file__).parent / "checklists" / "wa_bee_checklist_records.tsv"  # OLD — leave untouched
# NEW constant to add:
CHECKLIST_RECORDS_FULL_PATH = Path(__file__).parent / "checklists" / "checklist_records_full.csv"

<!-- Existing private-loader signature + call style; new loader mirrors it exactly: -->
def _load_checklist_records(con: duckdb.DuckDBPyConnection) -> None: ...   # OLD — leave untouched
def load_checklist() -> None:
    con = duckdb.connect(DB_PATH)
    try:
        con.execute("CREATE SCHEMA IF NOT EXISTS checklist_data")
        # ... species / species_counties load ...
        _load_checklist_records(con)            # OLD call — leave in place (line 238)
        _load_checklist_records_full(con)       # NEW — append immediately after
        _update_occurrences_canonical_name(con)
        reconcile(con)
    finally:
        con.close()

<!-- Test fixture (test_checklist_pipeline.py lines 17-43): the `checklist_db` fixture monkeypatches DB_PATH,
     importlib.reloads the module, and supports monkeypatch.setattr for path constants. New tests reuse it
     and additionally monkeypatch.setattr(mod, "CHECKLIST_RECORDS_FULL_PATH", <tmp csv>) for unit-style cases. -->
</interfaces>

<source_csv_shape>
<!-- Real header (verified): -->
ObjectID,Family,Genus,Scientific Name,Locality,Latitude,Longitude,Date,recordedBy,County_join,x,y
<!-- Sample rows: -->
1,Halictidae,Agapostemon,"Agapostemon angelicus Cockerell, 1924",Auburn and vicinity,47.3075,-122.2272,1991-07-12T00:00:00,"Yanega, Douglas",King,-122.2272,47.3075
<!-- Date formats present (empirically, D-09): ISO datetime (43,602, e.g. 1991-07-12T00:00:00),
     ISO date (64, e.g. 1991-07-12), M/D/YYYY (291, e.g. 6/14/1905), empty (6,689). 0 malformed. Year span 1812-2022. -->
<!-- Coord empirical counts: ~45,927 valid, 4,595 null, 2 zero, 122 out_of_bbox. The x/y columns are a coarse
     fallback (often county centroids) and are IGNORED per D-02 — use Latitude/Longitude only. -->
</source_csv_shape>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add unit-testable date-parsing and coord-flag helpers with RED→GREEN tests</name>
  <read_first>
    - data/checklist_pipeline.py (the existing module — module docstring, imports, the OLD `_load_checklist_records` at lines 127-162 for style; you will ADD helpers, touch nothing existing)
    - data/tests/test_checklist_pipeline.py (the `checklist_db` fixture lines 17-43 and the existing assertion patterns — your new helper unit tests live in this same file)
    - .planning/phases/134-full-fidelity-ingest/134-CONTEXT.md (D-01 bbox bounds, D-05/D-07/D-08/D-09 date rules, the empirical date/coord profile in §Specific Ideas)
    - .planning/research/PITFALLS.md (§Pitfall 3 coordinate quality, §Pitfall 4 date parsing — both must be resolved in Python at ingest)
  </read_first>
  <behavior>
    Date-parsing helper (name it e.g. `_parse_checklist_date(raw: str) -> tuple[int|None, int|None, int|None, str]` returning (year, month, day, date_quality)):
    - Test: `"1812-06-18"` (ISO date) → (1812, 6, 18, "full") — pre-1900 must work; use datetime.date / datetime.fromisoformat, NOT strftime (the 1900 floor is strftime-only, D-09).
    - Test: `"1991-07-12T00:00:00"` (ISO datetime) → (1991, 7, 12, "full") — the time component is dropped (D-05).
    - Test: `"6/14/1905"` (M/D/YYYY) → (1905, 6, 14, "full") — parsed US month-first deterministically via strptime('%m/%d/%Y') or fixed DATE_ORDER='MDY', NEVER per-row auto-detect (D-08).
    - Test: `""` and whitespace-only and (if encountered) None → (None, None, None, "none") (D-07).
    - Test: a value that parses to a year but no month/day path is not expected in the current file (OTHER bucket = 0), but the enum value "year_only" must still be reachable in code for robustness (D-07) — assert the helper returns "year_only" for a year-only input like `"1995"` with (1995, None, None, "year_only").
    Coord-flag helper (name it e.g. `_coord_flag(lat: float|None, lon: float|None) -> str` returning one of valid/null_coord/zero_coord/out_of_bbox):
    - Test: lat/lon None (empty source cell) → "null_coord".
    - Test: lat=0 and lon=0 → "zero_coord" (checked BEFORE the bbox test — Gulf-of-Guinea guard, PITFALLS #3).
    - Test: lat=47.3075, lon=-122.2272 (in-state) → "valid".
    - Test: lat=40.0, lon=-122.0 (south of WA) → "out_of_bbox" (tight bbox lat ∈ [45.5, 49.0], lon ∈ [-124.85, -116.9], D-01).
    - Test: a boundary point exactly at lat=45.5, lon=-124.85 → "valid" (inclusive bounds).
  </behavior>
  <action>
    Add two module-level helper functions to `data/checklist_pipeline.py`: a date parser returning `(year, month, day, date_quality)` and a coordinate classifier returning the `coord_flag` string. Encode the tight WA bbox as `lat ∈ [45.5, 49.0]`, `lon ∈ [-124.85, -116.9]` (inclusive) per D-01. In the coord classifier, test null FIRST (either coord missing → `null_coord`), then exact-zero (`lat == 0 and lon == 0` → `zero_coord`), then bbox membership (`valid` vs `out_of_bbox`) — this ordering matters so 0/0 never reaches the bbox test (PITFALLS #3). Date parsing is stdlib-first (D-09): try ISO via `datetime.date.fromisoformat`/`datetime.datetime.fromisoformat` (drop time), then `datetime.strptime(raw, "%m/%d/%Y")` for the M/D/YYYY tail, then a pure-year branch (`int(raw)` → `year_only`); empty/unparseable → all-None + `none`. dateparser is the DOCUMENTED fallback tier only (D-09) — it is NOT required for the current file; you MAY leave a commented note or a final fallback branch, but do not make the helper's correctness depend on it. Keep `date_quality` values restricted to the enum `full` / `year_only` / `none` (D-05/D-07). Add the corresponding unit tests in `data/tests/test_checklist_pipeline.py` that call the helpers directly (no DuckDB needed) — write them RED first, then implement to GREEN. Do NOT modify `_load_checklist_records`, `reconcile`, `load_checklist`, or any existing function in this task.
  </action>
  <verify>
    <automated>cd data && uv run pytest tests/test_checklist_pipeline.py -k "parse or coord_flag" -x</automated>
  </verify>
  <acceptance_criteria>
    - `cd data && uv run pytest tests/test_checklist_pipeline.py -k "parse or coord_flag"` exits 0.
    - A test asserts the date helper maps `"1812-06-18"` → `(1812, 6, 18, "full")`.
    - A test asserts the date helper maps `"1991-07-12T00:00:00"` → `(1991, 7, 12, "full")` (time dropped).
    - A test asserts the date helper maps `"6/14/1905"` → `(1905, 6, 14, "full")` (US month-first).
    - A test asserts the date helper maps `""` → `(None, None, None, "none")`.
    - A test asserts the date helper maps `"1995"` → `(1995, None, None, "year_only")` (enum reachable).
    - A test asserts the coord helper maps `(None, None)` → `"null_coord"`, `(0, 0)` → `"zero_coord"`, `(47.3075, -122.2272)` → `"valid"`, `(40.0, -122.0)` → `"out_of_bbox"`, and `(45.5, -124.85)` → `"valid"`.
    - `git diff data/checklist_pipeline.py` shows ONLY additions (new helpers + a new path constant if added here) — no edits inside `_load_checklist_records`, `reconcile`, `_update_occurrences_canonical_name`, or `load_checklist`.
  </acceptance_criteria>
  <done>Two pure, unit-tested helpers (date → year/month/day/date_quality; lat/lon → coord_flag) exist in checklist_pipeline.py with passing RED→GREEN tests; no existing function was modified.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add _load_checklist_records_full() loader, wire it into load_checklist(), and add integration tests</name>
  <read_first>
    - data/checklist_pipeline.py (the OLD `_load_checklist_records` loader lines 127-162 — the exact template; `load_checklist()` lines 165-242 — the call site at line 238; the `print(...)  # noqa: T201` logging convention)
    - data/tests/test_checklist_pipeline.py (the `checklist_db` fixture + the schema/row-count/idempotency assertion patterns — your new integration tests mirror these)
    - .planning/phases/134-full-fidelity-ingest/134-PATTERNS.md (§"new _load_checklist_records_full() loader" — column list, CREATE OR REPLACE + executemany + per-reason log; §test section — the 7 tests to add)
    - .planning/phases/134-full-fidelity-ingest/134-CONTEXT.md (D-03 coord_flag values + counts, D-04 logging, D-12 preserve ObjectID/Family/Genus + raw verbatim_name)
  </read_first>
  <behavior>
    Integration tests against the new table (using the `checklist_db` fixture, which runs the REAL CSV committed in 134-01):
    - Row count: `SELECT count(*) FROM checklist_data.checklist_records_full` is BETWEEN 50000 AND 51000 (~50,646) (SC#1).
    - Schema: the table has all of `verbatim_name`, `locality`, `latitude`, `longitude`, `recordedBy`, `year`, `month`, `day`, `date_quality`, `coord_flag`, plus traceability `ObjectID`, `family`, `genus` (SC#1 + D-12); assert via `information_schema.columns`.
    - Coord guard (SC#2): zero rows where `coord_flag = 'valid' AND (latitude = 0 OR longitude = 0)`; AND zero rows where `coord_flag = 'valid'` with lat/lon outside the tight bbox.
    - Coord flag coverage: every row's `coord_flag` is in `('valid','null_coord','zero_coord','out_of_bbox')`; count of `null_coord` is in a sane band (~4,595, assert > 1000).
    - Date (SC#3): the row whose source date is the pre-1900 `1812-06-18`-equivalent has `year=1812, month=6, day=18, date_quality='full'`; at least one M/D/YYYY-sourced row parsed (assert ≥1 row with date_quality='full'); every row with empty/NULL source date has `date_quality='none'` AND `year IS NULL`.
    - date_quality domain: every row's `date_quality` is in `('full','year_only','none')`.
    - Idempotency: running `load_checklist()` twice yields the same `checklist_records_full` row count (CREATE OR REPLACE).
    - D-10 guard: the OLD `checklist_data.checklist_records` table still exists with its original 4-column schema after the new loader runs.
  </behavior>
  <action>
    Add `_load_checklist_records_full(con: duckdb.DuckDBPyConnection) -> None` to `data/checklist_pipeline.py`, mirroring the OLD `_load_checklist_records` template exactly (PATTERNS.md): `csv.DictReader(f)` with comma delimiter (NOT tab), build a `records: list[tuple]` in Python (calling the Task-1 date + coord helpers per row), `CREATE OR REPLACE TABLE checklist_data.checklist_records_full` with explicit typed columns, then one `con.executemany(...)` bulk insert. Read coordinates from the `Latitude`/`Longitude` source columns ONLY — ignore the redundant `x`/`y` columns (D-02). Set `verbatim_name` = the raw `Scientific Name` cell stored UNMODIFIED with authority intact (D-12); do NOT call `normalize_scientific_name` on it. Preserve `ObjectID` (BIGINT), raw `Family`→`family`, raw `Genus`→`genus` for traceability (D-12). Do NOT add a synthetic `checklist_id` (later-phase concern, D-12). Table columns (you choose final order, consistent with module style): `ObjectID BIGINT, family VARCHAR, genus VARCHAR, verbatim_name VARCHAR, locality VARCHAR, latitude DOUBLE, longitude DOUBLE, recordedBy VARCHAR, year BIGINT, month BIGINT, day BIGINT, date_quality VARCHAR, coord_flag VARCHAR`. Parse `Latitude`/`Longitude` to float or None (empty cell → None → `null_coord`). After insert, log TWO lines via `print(f"...")  # noqa: T201` (D-04): one summary count, and one per-reason coord breakdown of `null_coord`/`zero_coord`/`out_of_bbox` (the excluded total + each reason). Wire the new loader by appending `_load_checklist_records_full(con)` in `load_checklist()` immediately after the existing `_load_checklist_records(con)` call (line ~238) — leave the OLD call and every other line of `load_checklist()` untouched (D-10). No `run.py` change is needed (the `"checklist"` STEPS entry already calls `load_checklist`). Add the integration tests from the behavior block to `data/tests/test_checklist_pipeline.py`, reusing the existing `checklist_db` fixture; write them RED first where practical, then GREEN.
  </action>
  <verify>
    <automated>cd data && uv run pytest tests/test_checklist_pipeline.py -k "full or records_full" -x && uv run python -c "import checklist_pipeline" && uv run ruff check checklist_pipeline.py</automated>
  </verify>
  <acceptance_criteria>
    - `cd data && uv run pytest tests/test_checklist_pipeline.py -k "full or records_full"` exits 0.
    - `cd data && uv run pytest tests/test_checklist_pipeline.py` (whole file) exits 0 — existing Phase 76 tests still pass (D-10 add-only verified).
    - `checklist_data.checklist_records_full` has columns including `verbatim_name, locality, latitude, longitude, recordedBy, year, month, day, date_quality, coord_flag, ObjectID, family, genus`.
    - A test asserts `COUNT(*) BETWEEN 50000 AND 51000` on `checklist_records_full` (~50,646, SC#1).
    - A test asserts zero rows with `coord_flag='valid' AND (latitude=0 OR longitude=0)` (SC#2).
    - A test asserts the `1812-06-18` source row resolves to `year=1812, month=6, day=18, date_quality='full'` (SC#3).
    - A test asserts at least one M/D/YYYY-sourced row parsed to `date_quality='full'` (SC#3).
    - A test asserts every empty/NULL-date row has `date_quality='none'` and `year IS NULL` (SC#3).
    - A test asserts every `coord_flag ∈ ('valid','null_coord','zero_coord','out_of_bbox')` and every `date_quality ∈ ('full','year_only','none')`.
    - A test asserts the loader is idempotent (two `load_checklist()` runs → identical `checklist_records_full` count).
    - A test asserts `checklist_data.checklist_records` (the OLD table) still exists with its original 4 columns (`scientificName, county, year, month`) — D-10.
    - `git diff data/checklist_pipeline.py` shows the only change inside `load_checklist()` is the single added `_load_checklist_records_full(con)` line.
    - `uv run ruff check checklist_pipeline.py` passes (the two new `print` lines carry `# noqa: T201`).
    - Build output (captured during the test run or via a manual `uv run python -c "import checklist_pipeline; checklist_pipeline.load_checklist()"` against a scratch DB) prints the per-reason coord breakdown (null_coord / zero_coord / out_of_bbox counts).
  </acceptance_criteria>
  <done>checklist_data.checklist_records_full is produced by a new loader carrying all six target columns + coord_flag + y/m/d + date_quality for ~50,646 rows; coords are validated and date-normalized per D-01..D-09; the per-reason exclusion breakdown is logged; the old loader/table/tests are untouched; the whole pytest file is green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| committed CSV → DuckDB table | untrusted/messy source values (lat, lon, free-text dates) cross into the typed `checklist_records_full` table |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-134-03 | Tampering | bad coordinates entering the (future) point arm | mitigate | `_coord_flag` classifies every row (null_coord/zero_coord/out_of_bbox/valid) at Python ingest; 0/0 is guarded BEFORE the bbox test (PITFALLS #3). Phase 137 filters on `coord_flag='valid'`; 134 only tags. pytest asserts no `valid` row has lat/lon=0 or is out-of-bbox. |
| T-134-04 | Information Disclosure | silent NULL date propagation into year filters | mitigate | `date_quality` enum (full/year_only/none) makes parse outcome explicit (PITFALLS #4); empty-date rows are tagged `none` with NULL y/m/d, so downstream filters can exclude them rather than passing them permissively. pytest asserts the tag. |
| T-134-05 | Tampering | accidental disturbance of the working county-fill build | accept | D-10 add-only: new loader creates only `checklist_records_full`; a pytest assertion confirms the old `checklist_records` table + schema survive, and the whole existing test file must stay green. Risk accepted as bounded by the add-only constraint + regression tests. |
| T-134-06 | Tampering | executemany INSERT with messy string cells | accept | Parameterized `executemany` (no string interpolation) — same pattern as the existing loader; DuckDB binds values, no SQL injection surface. Source is trusted developer-committed data. |
</threat_model>

<verification>
- `cd data && uv run pytest tests/test_checklist_pipeline.py` is fully green (new + existing tests).
- `checklist_data.checklist_records_full` carries lat, lon, year/month/day, date_quality, coord_flag, recordedBy, locality, verbatim_name, ObjectID, family, genus for ~50,646 rows.
- The per-reason coord-exclusion breakdown is printed to build output (D-04).
- `data/checklist_pipeline.py` diff inside `load_checklist()` is exactly one added line; the old loader/table/`run.py` are untouched (D-10).
- `uv run ruff check checklist_pipeline.py` passes.
</verification>

<success_criteria>
- ING-01: `checklist_pipeline.py` loads the committed CSV into a DuckDB table carrying lat, lon, date (y/m/d + date_quality), recordedBy, locality, verbatim_name; pytest asserts ~50,646 rows and all six columns (SC#1).
- ING-02: coordinate validation tags null/zero/out-of-bbox rows; no valid row has lat=0/lon=0 or is out of the tight WA bbox; excluded count + per-reason breakdown logged (SC#2).
- ING-03: dates normalized to three nullable ints + date_quality enum; 1812-06-18 and M/D/YYYY parse; NULL-date rows tagged none (SC#3).
</success_criteria>

<output>
Create `.planning/phases/134-full-fidelity-ingest/134-02-SUMMARY.md` when done.
</output>
