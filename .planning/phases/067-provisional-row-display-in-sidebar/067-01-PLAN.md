---
phase: 067-provisional-row-display-in-sidebar
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - data/export.py
  - scripts/validate-schema.mjs
  - frontend/src/filter.ts
autonomous: true
requirements:
  - SID-01
  - SID-02

must_haves:
  truths:
    - "export.py produces specimen_inat_quality_grade in both ARM 1 and ARM 2 of the combined CTE and in the final SELECT"
    - "validate-schema.mjs EXPECTED list includes specimen_inat_quality_grade"
    - "OccurrenceRow interface has is_provisional boolean, specimen_inat_taxon_name string|null, specimen_inat_quality_grade string|null, and host_inat_login string|null (observer removed)"
    - "OCCURRENCE_COLUMNS array contains is_provisional, specimen_inat_taxon_name, specimen_inat_quality_grade, host_inat_login and does not contain observer"
    - "buildFilterSQL collector filter references host_inat_login not observer in SQL string"
  artifacts:
    - path: "data/export.py"
      provides: "specimen_inat_quality_grade in combined CTE and final SELECT"
      contains: "specimen_inat_quality_grade"
    - path: "scripts/validate-schema.mjs"
      provides: "schema gate entry for new column"
      contains: "specimen_inat_quality_grade"
    - path: "frontend/src/filter.ts"
      provides: "updated OccurrenceRow and OCCURRENCE_COLUMNS"
      exports: ["OccurrenceRow", "OCCURRENCE_COLUMNS", "buildFilterSQL"]
  key_links:
    - from: "frontend/src/filter.ts OCCURRENCE_COLUMNS"
      to: "DuckDB SELECT in queryOccurrences"
      via: "OCCURRENCE_COLUMNS.join(', ')"
      pattern: "OCCURRENCE_COLUMNS"
    - from: "data/export.py combined CTE"
      to: "occurrences.parquet schema"
      via: "COPY ... TO ... FORMAT PARQUET"
      pattern: "specimen_inat_quality_grade"
---

<objective>
Add `specimen_inat_quality_grade` to the pipeline export and update the frontend schema layer to reflect all Phase 66/67 column changes.

Purpose: The rendering phase (Plan 02) depends on `specimen_inat_quality_grade`, `is_provisional`, `specimen_inat_taxon_name`, and `host_inat_login` being present in `OccurrenceRow` and fetched by DuckDB queries. This plan closes the Phase 66 gap (missing `specimen_inat_quality_grade` from export) and renames `observer` to `host_inat_login` throughout the frontend schema layer.

Output:
- `data/export.py` — `specimen_inat_quality_grade` added to ARM 1, ARM 2, final SELECT
- `scripts/validate-schema.mjs` — `specimen_inat_quality_grade` added to EXPECTED
- `frontend/src/filter.ts` — `OccurrenceRow` and `OCCURRENCE_COLUMNS` updated; `buildFilterSQL` collector filter uses `host_inat_login`
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/067-provisional-row-display-in-sidebar/067-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add specimen_inat_quality_grade to export.py and validate-schema.mjs</name>
  <files>data/export.py, scripts/validate-schema.mjs</files>
  <read_first>
    - data/export.py — read the full file to understand the combined CTE structure; ARM 1 starts at line ~133, ARM 2 at line ~157, final SELECT at line ~238
    - scripts/validate-schema.mjs — read the full file; EXPECTED object is at line ~22
  </read_first>
  <action>
In `data/export.py`, add `specimen_inat_quality_grade` in three places:

1. ARM 1 of the `combined` CTE (after `sob.specimen_inat_family`):
   ```sql
   sob.quality_grade AS specimen_inat_quality_grade,
   ```

2. ARM 2 of the `combined` CTE (after the ARM 2 `sob.specimen_inat_family` line):
   ```sql
   sob.quality_grade AS specimen_inat_quality_grade,
   ```
   Note: ARM 2 already selects `sob.quality_grade AS inat_quality_grade` — the new column is a second alias for the same source field, selecting the WABA quality grade under its own name so the final SELECT can reference `j.specimen_inat_quality_grade` distinctly.

3. Final SELECT (after `j.specimen_inat_family,`):
   ```sql
   j.specimen_inat_quality_grade,
   ```

In `scripts/validate-schema.mjs`, add `'specimen_inat_quality_grade'` to the EXPECTED `'occurrences.parquet'` array, immediately after `'specimen_inat_family'` in the WABA specimen fields comment block:
```js
'specimen_inat_genus', 'specimen_inat_family', 'specimen_inat_quality_grade',
```
  </action>
  <verify>
    <automated>grep -n "specimen_inat_quality_grade" /Users/rainhead/dev/beeatlas/data/export.py && grep -n "specimen_inat_quality_grade" /Users/rainhead/dev/beeatlas/scripts/validate-schema.mjs</automated>
  </verify>
  <done>
    - `data/export.py` contains `specimen_inat_quality_grade` in ARM 1 (sob.quality_grade alias), ARM 2 (sob.quality_grade alias), and final SELECT (j.specimen_inat_quality_grade)
    - `scripts/validate-schema.mjs` EXPECTED array contains `'specimen_inat_quality_grade'`
  </done>
</task>

<task type="auto">
  <name>Task 2: Update filter.ts — rename observer, add new fields</name>
  <files>frontend/src/filter.ts</files>
  <read_first>
    - frontend/src/filter.ts — read the full file; OccurrenceRow is at line ~24, OCCURRENCE_COLUMNS at line ~52, buildFilterSQL collector filter at line ~229
  </read_first>
  <action>
Make the following changes to `frontend/src/filter.ts`:

**1. Update `CollectorEntry` interface** (line ~8):
Replace `observer: string | null` with `host_inat_login: string | null`

**2. Update `OccurrenceRow` interface** (line ~24–50):
- Remove `observer: string | null;`
- Add after `sample_id: number | null;`:
  ```typescript
  host_inat_login: string | null;
  is_provisional: boolean;
  specimen_inat_taxon_name: string | null;
  specimen_inat_quality_grade: string | null;
  ```

**3. Update `OCCURRENCE_COLUMNS` array** (line ~52–58):
- Remove `'observer'` from the array
- Add after `'sample_id'`:
  ```typescript
  'host_inat_login', 'is_provisional', 'specimen_inat_taxon_name', 'specimen_inat_quality_grade',
  ```

**4. Update `buildFilterSQL` collector filter** (line ~229–239):
In the collector filter block, the comment references `observer (iNat)` and the SQL uses `observer IN (...)`. Change:
- Comment: `// Collector filter — single OR clause combining recordedBy (ecdysis) and observer (iNat)` → `// Collector filter — single OR clause combining recordedBy (ecdysis) and host_inat_login (iNat)`
- `filter(c => c.observer !== null)` → `filter(c => c.host_inat_login !== null)`
- `.map(c => `'${c.observer!.replace(/'/g, "''")}'`)` → `.map(c => `'${c.host_inat_login!.replace(/'/g, "''")}'`)`
- `observer IN (${observers.join(',')})` → `host_inat_login IN (${observers.join(',')})`
  </action>
  <verify>
    <automated>cd /Users/rainhead/dev/beeatlas/frontend && npm test -- --run 2>&1 | tail -20</automated>
  </verify>
  <done>
    - `OccurrenceRow` interface has `host_inat_login: string | null`, `is_provisional: boolean`, `specimen_inat_taxon_name: string | null`, `specimen_inat_quality_grade: string | null` and does NOT have `observer`
    - `OCCURRENCE_COLUMNS` contains `'host_inat_login'`, `'is_provisional'`, `'specimen_inat_taxon_name'`, `'specimen_inat_quality_grade'` and does NOT contain `'observer'`
    - `buildFilterSQL` SQL string contains `host_inat_login IN` (not `observer IN`)
    - All existing Vitest tests pass
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| export.py → parquet | SQL query output trusted as-is; source data from dlt-fetched iNat observations |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-067-01 | Tampering | export.py SQL | accept | Pipeline runs in trusted server environment (maderas cron); output is read-only parquet for frontend |
</threat_model>

<verification>
1. `grep -c "specimen_inat_quality_grade" /Users/rainhead/dev/beeatlas/data/export.py` returns 3 (ARM 1 alias, ARM 2 alias, final SELECT)
2. `grep "specimen_inat_quality_grade" /Users/rainhead/dev/beeatlas/scripts/validate-schema.mjs` returns a line in the WABA fields block
3. `grep "observer" /Users/rainhead/dev/beeatlas/frontend/src/filter.ts` returns only the `CollectorEntry` comment and no SQL `observer IN` references
4. `grep "host_inat_login" /Users/rainhead/dev/beeatlas/frontend/src/filter.ts` appears in OccurrenceRow, OCCURRENCE_COLUMNS, and buildFilterSQL
5. `cd /Users/rainhead/dev/beeatlas/frontend && npm test -- --run` passes all tests
</verification>

<success_criteria>
- export.py produces `specimen_inat_quality_grade` in ARM 1 (null for non-WABA rows via LEFT JOIN), ARM 2 (populated from sob.quality_grade), and final SELECT
- validate-schema.mjs EXPECTED list includes `specimen_inat_quality_grade` alongside other WABA fields
- OccurrenceRow interface has all four new/renamed fields; `observer` field is removed
- OCCURRENCE_COLUMNS array matches OccurrenceRow fields (no observer, has host_inat_login + three new fields)
- Collector filter SQL uses `host_inat_login` column name
- All existing Vitest tests pass (no regressions from interface changes)
</success_criteria>

<output>
After completion, create `.planning/phases/067-provisional-row-display-in-sidebar/067-01-SUMMARY.md`
</output>
