---
phase: 088-production-cutover
plan: 03
type: execute
wave: 3
depends_on:
  - "088-02"
files_modified:
  - .planning/phases/088-production-cutover/088-CUTOVER-LOG.md
autonomous: false
requirements:
  - CUTOVER-04
  - VALIDATE-02
must_haves:
  truths:
    - "`data/nightly.sh` has been read and confirmed unchanged (already has `set -euo pipefail` + uploads the three artifacts produced by Wave 2's `_run_dbt_build`)"
    - "`bash -n data/nightly.sh` parses cleanly with no syntax errors"
    - "Manual frontend smoke check passes against dbt-produced parquet: map renders, filters work, table populates, species page works"
    - "`088-CUTOVER-LOG.md` contains the migration → dbt mapping table (CUTOVER-02 doc deliverable, 2 rows)"
    - "`088-CUTOVER-LOG.md` contains a `## Smoke Check` section with 4 checked-off behaviors + date + sign-off"
    - "`occurrences.parquet` loaded by the frontend is the 30-col dbt-produced file (VALIDATE-02 assertive: `test_occurrences_schema_matches` PASS)"
  artifacts:
    - path: ".planning/phases/088-production-cutover/088-CUTOVER-LOG.md"
      provides: "CUTOVER-02 migration mapping + VALIDATE-02 smoke check sign-off + CUTOVER-04 nightly.sh confirmation"
      contains: "## Migration Mapping"
  key_links:
    - from: "088-CUTOVER-LOG.md ## Migration Mapping"
      to: "RESEARCH § Migration → dbt Replacement Mapping table"
      via: "copy of the 2-row table with [VERIFIED] citations"
      pattern: "host_observation_id"
    - from: "088-CUTOVER-LOG.md ## Smoke Check"
      to: "RESEARCH § Smoke Check Protocol §3 (the 4-row pass-behavior table)"
      via: "4 checked-off behaviors"
      pattern: "Map renders|Filters work|Table populates|Species page"
---

<objective>
Close out Phase 88 by (a) confirming `data/nightly.sh` requires no changes per RESEARCH
(`set -euo pipefail` + `uv run python run.py` already propagate dbt's exit codes
correctly, the three S3-uploaded files match what Wave 2's `_run_dbt_build` writes to
EXPORT_DIR), (b) executing the manual frontend smoke check, and (c) writing the
phase's cutover log including the CUTOVER-02 migration → dbt mapping table and the
VALIDATE-02 sign-off.

Purpose: this wave produces the written CUTOVER-02 deliverable (mapping doc) and the
VALIDATE-02 sign-off (smoke check). Without these, the success criteria 2 and 5 are
not met even though the code changes from Waves 1-2 already satisfy them functionally.

Output: `088-CUTOVER-LOG.md` with `## Nightly.sh Confirmation`, `## Migration Mapping`,
and `## Smoke Check` sections. No code changes expected; if nightly.sh truly needs
a one-character edit (e.g. a stale comment referencing `export.py`), make it.

This plan has one human-verify checkpoint (smoke check) — hence `autonomous: false`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/088-production-cutover/088-RESEARCH.md
@.planning/phases/088-production-cutover/088-PATTERNS.md
@.planning/phases/088-production-cutover/088-VALIDATION.md
@.planning/phases/088-production-cutover/088-02-SUMMARY.md
@data/nightly.sh
@data/run.py
@.planning/phases/087-incremental-materialization-experiment/087-FINDINGS.md

<interfaces>
<!-- Critical excerpts the executor needs verbatim. -->

Current data/nightly.sh invariants (from PATTERNS.md `### data/nightly.sh`):
```bash
set -euo pipefail                                           # line 6
trap '... aws s3 cp ... "$DB_PATH" ...' EXIT                # line 24
export DB_PATH EXPORT_DIR                                   # line 39
cd "$SCRIPT_DIR"                                            # line 40
uv run python run.py                                        # line 41
for f in occurrences.parquet counties.geojson ecoregions.geojson; do
    aws ... s3 cp ... "$EXPORT_DIR/$f" "s3://$BUCKET/data/$f"
done                                                        # lines 47-49
```

Why no edits expected (RESEARCH § Code Examples > nightly.sh):
- `set -euo pipefail` already propagates exits
- `uv run python run.py` invokes the Wave-2-rewritten run.py, which raises
  CalledProcessError on dbt non-zero (Pattern 1, check=True)
- S3 upload lines 47-49 reference the three filenames `_run_dbt_build` copies to
  EXPORT_DIR — no contract change
- Trap on EXIT backs up DuckDB regardless of success/failure

Migration → dbt mapping (copy verbatim from RESEARCH § Migration → dbt Replacement Mapping):

| Migration in `_apply_migrations()` | Invariant Enforced | dbt Replacement | Evidence |
|------------------------------------|--------------------|-----------------|----------|
| Rename `ecdysis_data.occurrence_links.inat_observation_id` → `host_observation_id` (Phase 48) | The link table column is named `host_observation_id` | `data/dbt/models/staging/stg_ecdysis__occurrence_links.sql` selects from `source('ecdysis_data', 'occurrence_links')`; downstream `int_ecdysis_base.sql` line 28 references `links.host_observation_id`. The dbt source declaration in `sources.yml` enforces the contract. If the column is missing or differently named, dbt build fails at compile time with a binder error. | [VERIFIED — RESEARCH read of `stg_ecdysis__occurrence_links.sql` + `int_ecdysis_base.sql`] |
| Add `geom GEOMETRY` column to `geographies.us_counties`, `geographies.ecoregions`, `geographies.us_states` (Phase 47) | Geographies tables have a typed `geom` column (not just `geometry_wkt`) | `data/dbt/models/staging/stg_geo__us_counties.sql`, `stg_geo__ecoregions.sql`, `stg_geo__us_states.sql` each select `ST_GeomFromText(geometry_wkt) AS geom`. dbt staging always converts from `geometry_wkt` on read, so the source table doesn't need a pre-computed `geom` column at all. **Migration is OBVIATED**, not replaced. | [VERIFIED — RESEARCH read of `stg_geo__*.sql` + `emit_feature_collection.sql`] |

Smoke check pass-behavior table (verbatim from RESEARCH § Smoke Check Protocol § 3):

| UI surface | Expected | Fail signature |
|------------|----------|----------------|
| Map renders | Mapbox canvas appears with WA outline, ~47,000 specimen markers visible | Blank map, console error `parquetReadObjects failed` or `column ecdysis_id not found` |
| Filters work | Toggle taxon / county / year filter; specimen count badge updates; map markers update | Filter button does nothing; count stays at full set; console error |
| Table populates | Click table mode toggle; drawer slides up with ~47k rows; row click pans map | Empty table; row count shows 0; console error from sqlite.ts INSERT |
| Species page | Navigate to `/species/`; click a species (e.g. Apis mellifera); seasonality chart + county map render | 404 on species.json or seasonality.json; chart renders blank |
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Confirm nightly.sh requires no changes (CUTOVER-04)</name>
  <files>(read-only; document outcome in Task 3 cutover log)</files>
  <read_first>
    - `data/nightly.sh` (full file)
    - `.planning/phases/088-production-cutover/088-RESEARCH.md` `### nightly.sh (no edits required for CUTOVER-04)` AND `## Pitfall 6: dbt exit code 1 covers BOTH ERROR and FAIL`
    - `.planning/phases/088-production-cutover/088-PATTERNS.md` `### data/nightly.sh`
    - `.planning/phases/088-production-cutover/088-02-SUMMARY.md` (confirm Wave 2 _run_dbt_build writes the three filenames nightly.sh expects)
  </read_first>
  <action>
    1. Read `data/nightly.sh` fully. Verify each invariant listed in `<interfaces>`:
       - Line near top: `set -euo pipefail` present
       - Single `uv run python run.py` invocation, no flags that would mask errors
       - S3 upload loop references exactly `occurrences.parquet`, `counties.geojson`,
         `ecoregions.geojson` (the three files `_run_dbt_build` populates EXPORT_DIR with)

    2. Confirm the file parses cleanly:
       `bash -n data/nightly.sh` exits 0.

    3. Confirm there are NO `--exclude` arguments anywhere — Phase 85 resolved the
       awkward-fit tests, RESEARCH § 5 confirms PASS=44 WARN=0 ERROR=0 SKIP=0. Adding
       `--exclude` defensively is explicitly an anti-pattern.

    4. Check for any stale comment/string references to retired components
       (`export.py`, `validate-schema`, `_apply_migrations`). RESEARCH expects none,
       but if one is found, make the minimal one-line edit to remove the stale comment
       and commit.

    5. Record the confirmation outcome (no edits / minimal comment edit) — this feeds
       Task 3's `## Nightly.sh Confirmation` section.

    Do NOT add `--exclude`, do NOT add `--full-refresh`, do NOT change error handling.
    Do NOT touch `data/dbt/run.sh` (locked — version pin).
  </action>
  <verify>
    <automated>bash -n data/nightly.sh && grep -q "set -euo pipefail" data/nightly.sh && grep -q "uv run python run.py" data/nightly.sh && grep -q '"$EXPORT_DIR/feeds/"' data/nightly.sh && ! grep -q "export.py\|validate-schema\|_apply_migrations\|--exclude\|--full-refresh" data/nightly.sh</automated>
  </verify>
  <acceptance_criteria>
    - `bash -n data/nightly.sh` exits 0 (script parses)
    - `grep -q "set -euo pipefail" data/nightly.sh` (exit propagation in place)
    - `grep -q "uv run python run.py" data/nightly.sh` (single Python entrypoint)
    - `grep -E "occurrences\\.parquet|counties\\.geojson|ecoregions\\.geojson" data/nightly.sh` matches all three filenames (S3 upload contract preserved)
    - `! grep -q "export.py\|validate-schema\|_apply_migrations\|--exclude" data/nightly.sh` (no stale references; no defensive exclusions)
    - `data/nightly.sh` contains the `aws ... s3 cp --recursive ... "$EXPORT_DIR/feeds/" ...` line (the feeds upload step from the existing pipeline is preserved); verified by `grep -q '"$EXPORT_DIR/feeds/"' data/nightly.sh`
    - Either no commit made (no edits needed) OR a single small commit removing a stale comment
  </acceptance_criteria>
  <done>
    CUTOVER-04 satisfied: nightly.sh's exit-code semantics are correct (was already so;
    Wave 2 changes don't disturb them). Confirmation documented for Task 3.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Manual frontend smoke check (VALIDATE-02)</name>
  <what-built>
    After Wave 2, `data/run.py` runs `bash data/dbt/run.sh build` and `public/data/`
    contains the 30-col `occurrences.parquet` produced entirely by dbt (plus
    `counties.geojson`, `ecoregions.geojson`, `species.json`, `seasonality.json`). The
    frontend's `src/sqlite.ts` already declares the 30-col schema (Phase 85). VALIDATE-02
    asks the human to confirm the four UI surfaces render correctly against this
    dbt-produced parquet.
  </what-built>
  <how-to-verify>
    1. Confirm `public/data/occurrences.parquet` exists and was produced by Wave 2
       (mtime newer than `088-02-PLAN.md`'s commit timestamp). If absent, run
       `cd data && uv run python run.py` first.

    2. Start the dev server: `npm run dev`. Open the printed URL (typically
       http://localhost:8080).

    3. Walk through the four surfaces from RESEARCH § Smoke Check Protocol § 3
       (the table in `<interfaces>`):
       - **Map renders:** Mapbox canvas appears with WA outline; ~47,000 specimen
         markers visible. No console errors mentioning `parquetReadObjects failed` or
         `column ecdysis_id not found`.
       - **Filters work:** Open the filter panel (magnifying glass overlay). Toggle a
         taxon / county / year filter. Confirm the specimen count badge updates and
         map markers refresh.
       - **Table populates:** Click the table mode toggle. Drawer slides up with ~47k
         rows; click a row and confirm the map strip pans to that location.
       - **Species page:** Navigate to `/species/`. Click a species (e.g. *Apis
         mellifera*). Confirm seasonality chart + occurrence list render from
         `species.json` / `seasonality.json`.

    4. Take note of any console errors. The expected outcome is zero. If any of the
       four surfaces fail, escalate to the planner — do NOT silently mark "approved";
       a true VALIDATE-02 failure is a phase-blocking regression.

    5. Type the outcome in the resume-signal. Approved outcome example:
       `approved — all 4 surfaces green, zero console errors, smoke ran <timestamp>`.

    Optional (post-cutover production verification per RESEARCH § Smoke Check Protocol
    § 5): after this checkpoint, monitor maderas's next nightly run for production parity.
    That's not blocking on this task; capture in `## Follow-Ups` if relevant.
  </how-to-verify>
  <resume-signal>Type "approved — [4 surfaces green or list issues]" or describe failures</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Write 088-CUTOVER-LOG.md (CUTOVER-02 + CUTOVER-04 + VALIDATE-02 documentation)</name>
  <files>.planning/phases/088-production-cutover/088-CUTOVER-LOG.md</files>
  <read_first>
    - `.planning/phases/088-production-cutover/088-RESEARCH.md` `## Migration → dbt Replacement Mapping (CUTOVER-02)` AND `## Smoke Check Protocol`
    - `.planning/phases/088-production-cutover/088-PATTERNS.md` `### 088-FINDINGS.md (optional NEW doc) — migration → dbt mapping` (frontmatter analog from Phase 87)
    - `.planning/phases/087-incremental-materialization-experiment/087-FINDINGS.md` lines 1-30 (frontmatter + section layout pattern)
    - `.planning/phases/088-production-cutover/088-01-SUMMARY.md` and `088-02-SUMMARY.md` (cross-cite the wave outputs)
    - Task 1's nightly.sh confirmation outcome (this session)
    - Task 2's smoke-check sign-off (this session)
  </read_first>
  <action>
    Write `.planning/phases/088-production-cutover/088-CUTOVER-LOG.md` with the
    following structure (mirror Phase 87 FINDINGS.md frontmatter shape):

    ```
    ---
    phase: 088-production-cutover
    requirements: [CUTOVER-02, CUTOVER-04, VALIDATE-02]
    recorded: <today's date>
    pre_cutover_sha: <40-char SHA from .planning/phases/088-production-cutover/pre-cutover-sha.txt>
    ---

    # Phase 88 — Cutover Log

    ## Summary

    [2-3 sentences: dbt is the sole transform producer; export.py + _apply_migrations
    + validate-schema.mjs retired; frontend smoke green; nightly.sh unchanged.]

    ## Nightly.sh Confirmation (CUTOVER-04)

    [Outcome from Task 1: "No edits required" OR list the single comment edit made.
    Cite the line numbers verified: set -euo pipefail (line 6), uv run python run.py
    (line 41), S3 upload of the three artifacts (lines 47-49). State explicitly that
    no --exclude is added because Phase 85 resolved the awkward-fit tests (PASS=44
    ERROR=0 per 086-VERIFICATION).]

    ## Migration Mapping (CUTOVER-02)

    [Copy the 2-row table verbatim from <interfaces> block above — both rows with
    [VERIFIED] citations.]

    Net effect: _apply_migrations is deleted with zero invariant loss. Both branches
    are obviated by dbt's source-declaration contract (Phase 48 rename) and staging-
    layer ST_GeomFromText conversion (Phase 47 geom column).

    ## Smoke Check (VALIDATE-02)

    Date: <date>
    Sign-off: <Task 2 resume-signal verbatim>

    - [x] Map renders against dbt-produced occurrences.parquet (no console errors)
    - [x] Filters update map + table (specimen count badge live)
    - [x] Table populates (~47k rows, row click pans map)
    - [x] Species page renders (/species/ → species detail with seasonality + county map)

    Assertive check: `cd data && uv run pytest data/tests/test_dbt_diff.py::test_occurrences_schema_matches -x` exits 0 (was FAIL pre-cutover; PASS post-Wave 2 because public/data/occurrences.parquet is now 30-col from dbt).

    ## Out of Scope / Follow-Ups

    - Stale `public/data/samples.parquet` and `public/data/ecdysis.parquet` (pre-v3.0
      artifacts) remain on S3; tracked at `.planning/todos/pending/stale-public-data-cleanup.md`
      (file not created in this phase per RESEARCH § Open Questions 2).
    - Nightly-run failure notification (dead-man's switch) tracked at
      `.planning/todos/pending/nightly-run-failure-notification.md` — natural follow-on
      now that nightly.sh's exit semantics are settled.
    - First post-cutover production nightly on maderas should be observed to confirm
      S3 receives a fresh parquet and CloudFront serves it (RESEARCH § Pitfall 3).

    ## Rollback

    Single-commit revert. Phase 88's merge commit lands all three waves. To revert,
    `git revert <merge-commit>`. The pre-cutover SHA recorded at
    `.planning/phases/088-production-cutover/pre-cutover-sha.txt` is the alternative
    target for a hard reset if preferred.
    ```

    After writing, verify the structural acceptance criteria below. Commit message
    suggestion: `docs(088-03): cutover log — migration mapping + smoke sign-off (CUTOVER-02, CUTOVER-04, VALIDATE-02)`.
  </action>
  <verify>
    <automated>test -f .planning/phases/088-production-cutover/088-CUTOVER-LOG.md && grep -q "## Migration Mapping" .planning/phases/088-production-cutover/088-CUTOVER-LOG.md && grep -q "## Smoke Check" .planning/phases/088-production-cutover/088-CUTOVER-LOG.md && grep -q "host_observation_id" .planning/phases/088-production-cutover/088-CUTOVER-LOG.md && grep -q "ST_GeomFromText" .planning/phases/088-production-cutover/088-CUTOVER-LOG.md && grep -cE "^- \[x\] " .planning/phases/088-production-cutover/088-CUTOVER-LOG.md | awk '{ if ($1 < 4) exit 1 }'</automated>
  </verify>
  <acceptance_criteria>
    - `088-CUTOVER-LOG.md` exists with frontmatter including `requirements: [CUTOVER-02, CUTOVER-04, VALIDATE-02]` and a `pre_cutover_sha` value matching the file from Wave 1
    - `## Migration Mapping` section present with both rows from RESEARCH (mentions both `host_observation_id` and `ST_GeomFromText`)
    - `## Smoke Check` section present with at least 4 `- [x]` checked behaviors + a date + Task 2's sign-off string
    - `## Nightly.sh Confirmation` section present, documents the no-op outcome (or the specific stale-comment edit made in Task 1)
    - `## Rollback` section references the pre-cutover SHA file
    - File committed to git
  </acceptance_criteria>
  <done>
    All three Phase 88 documentation deliverables are written and committed:
    - CUTOVER-02 migration → dbt mapping table (closes success criterion 2)
    - CUTOVER-04 nightly.sh confirmation (closes success criterion 4)
    - VALIDATE-02 smoke sign-off (closes success criterion 5)
    Phase 88 is complete; ready for `/gsd-verify-work`.
  </done>
</task>

</tasks>

<verification>
After all three tasks:

1. `bash -n data/nightly.sh` exits 0; `grep -q "set -euo pipefail" data/nightly.sh`; no `--exclude` references
2. Manual smoke check signed off (Task 2 resume-signal recorded in Task 3's log)
3. `test -f .planning/phases/088-production-cutover/088-CUTOVER-LOG.md`
4. `grep -q "## Migration Mapping" .planning/phases/088-production-cutover/088-CUTOVER-LOG.md`
5. `grep -q "## Smoke Check" .planning/phases/088-production-cutover/088-CUTOVER-LOG.md`
6. `grep -cE "^- \\[x\\] " .planning/phases/088-production-cutover/088-CUTOVER-LOG.md` returns ≥ 4
7. `cd data && uv run pytest data/tests/test_dbt_diff.py::test_occurrences_schema_matches -x` exits 0 (assertive VALIDATE-02 verification)
</verification>

<success_criteria>
- `data/nightly.sh` confirmed unchanged (or minimally edited for stale comment); exit semantics intact
- Manual frontend smoke check passes against dbt-produced parquet (all 4 UI surfaces green)
- `088-CUTOVER-LOG.md` written with three required sections: Migration Mapping, Nightly.sh Confirmation, Smoke Check
- `test_occurrences_schema_matches` assertion in `test_dbt_diff.py` PASSES (was FAIL pre-cutover; assertive evidence of VALIDATE-02)
- No incremental materialization introduced
- No changes to `data/dbt/run.sh` or `data/checklist_unmatched.csv`
- All five Phase 88 requirement IDs (CUTOVER-01..04, VALIDATE-02) closed across the three waves
- Phase 88 ready for `/gsd-verify-work` and merge
</success_criteria>

<output>
After completion, create `.planning/phases/088-production-cutover/088-03-SUMMARY.md`
documenting:
- Nightly.sh confirmation outcome (no-op vs. minimal edit)
- Smoke check sign-off (date + Task 2 outcome)
- 088-CUTOVER-LOG.md sections written
- Cross-references to 088-01-SUMMARY.md and 088-02-SUMMARY.md
- v3.4 milestone status: Phase 88 complete; ready to mark v3.4 dbt Full Rewrite SHIPPED
</output>
