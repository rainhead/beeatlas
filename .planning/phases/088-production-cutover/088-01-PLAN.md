---
phase: 088-production-cutover
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/088-production-cutover/pre-cutover-sha.txt
  - scripts/validate-schema.mjs
  - package.json
  - .github/workflows/deploy.yml
  - CLAUDE.md
autonomous: true
requirements:
  - CUTOVER-03
must_haves:
  truths:
    - "`scripts/validate-schema.mjs` no longer exists in the repo"
    - "`package.json` `build` chain runs validate-species → typecheck → eleventy → validate-bundle-size with no validate-schema step"
    - "`.github/workflows/deploy.yml` has no `Validate parquet schema` step"
    - "`npm run build` exits 0 locally with the validate-schema gate removed"
    - "`CLAUDE.md` `## Known State` no longer claims `scripts/validate-schema.mjs` runs before every CI build"
    - "A pre-cutover SHA is recorded for single-command rollback of the full phase"
  artifacts:
    - path: ".planning/phases/088-production-cutover/pre-cutover-sha.txt"
      provides: "40-char rollback marker SHA (Phase-87 analog)"
      contains: "^[0-9a-f]{40}$"
    - path: "package.json"
      provides: "build chain without validate-schema"
      contains: "validate-species"
    - path: ".github/workflows/deploy.yml"
      provides: "deploy workflow without validate-schema CI step"
    - path: "CLAUDE.md"
      provides: "Known State paragraph reflecting dbt-contract enforcement"
  key_links:
    - from: "package.json `build` script"
      to: "npm run validate-species && npm run typecheck && eleventy && npm run validate-bundle-size"
      via: "string-literal in scripts.build"
      pattern: "\"build\":\\s*\"npm run validate-species"
    - from: ".github/workflows/deploy.yml"
      to: "npm test → npm run build (no validate-schema in between)"
      via: "step list ordering"
      pattern: "Run tests"
---

<objective>
Retire the JS-side `scripts/validate-schema.mjs` parquet schema gate. The dbt 30-column
contract on `marts/occurrences` (Phase 85) is now the sole runtime schema gate; the JS
validator duplicates work and blocks `npm run build` if `public/data/*.parquet` is absent.
Also capture the pre-cutover git SHA as a rollback marker.

Purpose: clear the CI-side gate so Phase 88 Waves 2-3 can land without a duplicate
schema check. The dbt contract enforces column names/types/order at every `bash
data/dbt/run.sh build`; the parquet on S3 cannot diverge from the contract without dbt
build failing first.

Output: deleted `scripts/validate-schema.mjs`, edited `package.json` + `deploy.yml`,
updated `CLAUDE.md`, and a new `pre-cutover-sha.txt` file.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/088-production-cutover/088-RESEARCH.md
@.planning/phases/088-production-cutover/088-PATTERNS.md
@.planning/phases/088-production-cutover/088-VALIDATION.md
@scripts/validate-schema.mjs
@package.json
@.github/workflows/deploy.yml
@CLAUDE.md

<interfaces>
<!-- Critical excerpts the executor needs verbatim. -->

From package.json (current — lines 17-29):
```json
"scripts": {
  "dev": "eleventy --serve",
  "build:data": "cd data && uv run python run.py",
  "validate-schema": "node scripts/validate-schema.mjs",
  "validate-species": "node scripts/validate-species.mjs",
  "validate-bundle-size": "node scripts/validate-bundle-size.mjs",
  "typecheck": "tsc --noEmit",
  "test": "vitest run",
  "build": "npm run validate-schema && npm run validate-species && npm run typecheck && eleventy && npm run validate-bundle-size",
  "preview": "vite preview --outDir _site",
  "fetch-data": "bash scripts/fetch-data.sh",
  "measure-lcp": "bash scripts/measure-lcp.sh"
}
```

Target shape (after edit):
- Delete the `"validate-schema": ...` line entirely.
- Rewrite `"build"` to `"npm run validate-species && npm run typecheck && eleventy && npm run validate-bundle-size"` (drop the leading `npm run validate-schema && `).

From .github/workflows/deploy.yml (current — lines 21-31):
```yaml
      - name: Install dependencies
        run: npm ci

      - name: Validate parquet schema
        run: npm run validate-schema

      - name: Run tests
        run: npm test

      - name: Build site
        run: npm run build
```

Target shape: delete the `Validate parquet schema` step (those two lines + blank line).

From CLAUDE.md (current — relevant lines):
- Line 45: `# Production build (validate-schema -> tsc --noEmit -> eleventy + Vite)` — comment in a code block. Update to remove the `validate-schema ->` prefix.
- Line 58: `- \`scripts/validate-schema.mjs\` runs before every CI build as a parquet schema gate` — DELETE this bullet (or replace with a `- The dbt 30-column contract on `marts/occurrences` is the schema gate; enforced at every `bash data/dbt/run.sh build`.` line if a positive statement is preferred).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Capture pre-cutover rollback SHA</name>
  <files>.planning/phases/088-production-cutover/pre-cutover-sha.txt</files>
  <read_first>
    - `.planning/phases/088-production-cutover/088-RESEARCH.md` `## Open Questions` item 3 (rollback-marker recommendation)
    - `.planning/phases/088-production-cutover/088-PATTERNS.md` section `### .planning/phases/088-production-cutover/pre-cutover-sha.txt (NEW) — rollback marker`
    - `.planning/phases/087-incremental-materialization-experiment/pre-experiment-sha.txt` (one-line, 40-char hex, trailing newline — exact analog)
  </read_first>
  <action>
    Run `git rev-parse HEAD > .planning/phases/088-production-cutover/pre-cutover-sha.txt`
    from the repo root. The file should contain a single 40-char hex SHA followed by a
    newline, byte-identical in shape to Phase 87's marker. Commit immediately so the
    rollback target is the commit that includes the marker itself — this lets a future
    `git revert <merge-commit>` cleanly back out the entire phase.
  </action>
  <verify>
    <automated>test -f .planning/phases/088-production-cutover/pre-cutover-sha.txt && grep -Eq "^[0-9a-f]{40}$" .planning/phases/088-production-cutover/pre-cutover-sha.txt</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/phases/088-production-cutover/pre-cutover-sha.txt` exists
    - File contains exactly one 40-char lowercase hex SHA followed by a single newline
    - File committed to git (`git log -1 --name-only -- .planning/phases/088-production-cutover/pre-cutover-sha.txt` shows a commit)
  </acceptance_criteria>
  <done>
    Pre-cutover rollback marker is committed; future `git revert` of the phase merge
    commit lands the repo back at the SHA recorded in this file.
  </done>
</task>

<task type="auto">
  <name>Task 2: Delete validate-schema.mjs and remove from package.json</name>
  <files>scripts/validate-schema.mjs, package.json</files>
  <read_first>
    - `scripts/validate-schema.mjs` (full — confirm the 122-line shape before deletion)
    - `package.json` lines 17-29 (scripts block — current shape)
    - `.planning/phases/088-production-cutover/088-RESEARCH.md` `### Pattern 3: Retire validate-schema.mjs in three coupled sites` AND `## State of the Art` (confirms dbt contract supersedes)
    - `.planning/phases/088-production-cutover/088-PATTERNS.md` `### scripts/validate-schema.mjs (DELETED)` AND `### package.json (modified)`
  </read_first>
  <action>
    Two coupled edits in a single commit per RESEARCH Pattern 3:

    1. Delete `scripts/validate-schema.mjs` (`git rm scripts/validate-schema.mjs`).
    2. Edit `package.json` scripts block:
       - Delete the `"validate-schema": "node scripts/validate-schema.mjs",` line entirely
       - Rewrite `"build"`: remove the leading `npm run validate-schema && ` segment so
         the chain becomes `npm run validate-species && npm run typecheck && eleventy && npm run validate-bundle-size`.

    Do NOT touch sibling scripts that have cosmetic comment references
    (`scripts/validate-species.mjs:9`, `scripts/validate-bundle-size.mjs:9` — RESEARCH
    flagged these as comment-only, not import-bearing). They can stay; no correctness
    impact.

    After the edits, smoke `npm run build` from the repo root. Expect it to exit 0
    (validate-species → typecheck → eleventy → validate-bundle-size all green). If
    `validate-species` fails because `public/data/species.json` is absent locally, that's
    a pre-existing local-state issue — note it in the commit message; do NOT add a
    `--no-validate` flag.

    Commit message suggestion: `feat(088-01): retire validate-schema.mjs gate (CUTOVER-03 part 1/2)`.
  </action>
  <verify>
    <automated>test ! -f scripts/validate-schema.mjs && ! grep -q "validate-schema" package.json && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `test ! -f scripts/validate-schema.mjs` (file gone)
    - `! grep -q "validate-schema" package.json` (no references in package.json)
    - `grep -q "\"build\": \"npm run validate-species" package.json` (build chain starts with validate-species)
    - `npm run build` exits 0 OR fails only on `validate-species` due to missing local `public/data/species.json` (acceptable; not introduced by this task)
  </acceptance_criteria>
  <done>
    `scripts/validate-schema.mjs` is gone; `package.json` no longer references it;
    `npm run build` chain is one step shorter and runs without the validate-schema gate.
  </done>
</task>

<task type="auto">
  <name>Task 3: Remove validate-schema step from deploy.yml and update CLAUDE.md</name>
  <files>.github/workflows/deploy.yml, CLAUDE.md</files>
  <read_first>
    - `.github/workflows/deploy.yml` lines 13-33 (build job step ordering)
    - `CLAUDE.md` lines 40-60 (`## Running Locally` code-block comment AND `## Known State` bullet — both reference validate-schema)
    - `.planning/phases/088-production-cutover/088-RESEARCH.md` `### .github/workflows/deploy.yml (post-deletion)` AND `## Project Constraints (from CLAUDE.md)` final paragraph (`Note for planner: Add a small final task to update CLAUDE.md`)
    - `.planning/phases/088-production-cutover/088-PATTERNS.md` `### .github/workflows/deploy.yml (modified)`
  </read_first>
  <action>
    Two coupled edits:

    1. `.github/workflows/deploy.yml`: delete the `- name: Validate parquet schema` step
       (the two-line YAML block plus surrounding blank line). The resulting build job
       step order is: `checkout → setup-node → npm ci → npm test → npm run build`.
       Confirm by reading the file after edit — no `validate-schema` substring should
       remain.

    2. `CLAUDE.md` two changes:
       a. Line 45 (inside the `## Running Locally` code block): change the comment
          `# Production build (validate-schema -> tsc --noEmit -> eleventy + Vite)`
          to `# Production build (tsc --noEmit -> eleventy + Vite)` — drop the
          `validate-schema -> ` prefix.
       b. Line 58 (in `## Known State`): delete the bullet
          `- \`scripts/validate-schema.mjs\` runs before every CI build as a parquet schema gate`.
          Replace with a positive statement reflecting the new gate:
          `- The dbt 30-column contract on \`marts/occurrences\` is enforced at every \`bash data/dbt/run.sh build\`; there is no separate JS schema validator.`

    Commit message suggestion: `feat(088-01): remove validate-schema from CI + CLAUDE.md (CUTOVER-03 part 2/2)`.
  </action>
  <verify>
    <automated>! grep -q "validate-schema" .github/workflows/deploy.yml CLAUDE.md && grep -q "dbt 30-column contract" CLAUDE.md</automated>
  </verify>
  <acceptance_criteria>
    - `! grep -q "validate-schema" .github/workflows/deploy.yml` (no references in deploy workflow)
    - `! grep -q "validate-schema" CLAUDE.md` (no references in project rules)
    - `grep -q "dbt 30-column contract" CLAUDE.md` (positive replacement statement present)
    - `grep -c "Validate parquet schema" .github/workflows/deploy.yml` returns 0
    - `.github/workflows/deploy.yml` parses cleanly (no broken YAML; smoke via `python3 -c "import yaml; yaml.safe_load(open('.github/workflows/deploy.yml'))"` exits 0)
  </acceptance_criteria>
  <done>
    CI deploy workflow no longer runs validate-schema; CLAUDE.md correctly describes the
    post-cutover schema-gate architecture (dbt contract). Wave 1 complete.
  </done>
</task>

</tasks>

<verification>
After all three tasks:

1. `test ! -f scripts/validate-schema.mjs`
2. `! grep -rq "validate-schema" scripts/ package.json .github/workflows/ CLAUDE.md`
3. `npm run build` exits 0 (or fails only on pre-existing local-state issues unrelated to validate-schema)
4. `git log -1 --name-only -- .planning/phases/088-production-cutover/pre-cutover-sha.txt` shows a commit (rollback marker captured)
5. `cat .planning/phases/088-production-cutover/pre-cutover-sha.txt` shows a 40-char hex SHA
</verification>

<success_criteria>
- All five validate-schema reference sites (validate-schema.mjs file + package.json script def + package.json build chain + deploy.yml step + CLAUDE.md bullet) are removed in this wave
- CLAUDE.md `## Known State` now describes the dbt contract as the schema gate
- Pre-cutover rollback SHA is captured for future single-command revert
- `npm run build` chain shortened by one step; runs without the validate-schema gate
- No changes to `data/run.py`, `data/dbt/run.sh`, `data/nightly.sh`, `data/export.py`, or any dbt model (those are Waves 2-3)
- CUTOVER-03 requirement fully satisfied
</success_criteria>

<output>
After completion, create `.planning/phases/088-production-cutover/088-01-SUMMARY.md`
documenting:
- Pre-cutover SHA captured (cite the 40-char value)
- 5 deletion sites resolved (file, script def, build chain, CI step, CLAUDE.md bullet)
- `npm run build` smoke result
- Wave 2 prerequisites confirmed (no shared files with Wave 2; W2 can proceed independently)
</output>
