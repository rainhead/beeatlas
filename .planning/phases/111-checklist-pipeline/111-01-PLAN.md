---
phase: 111-checklist-pipeline
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - data/tests/test_dbt_scaffold.py
  - data/dbt/models/marts/checklist.sql
  - data/dbt/models/marts/schema.yml
  - data/run.py
autonomous: true
requirements: [CHECK-01, CHECK-02, CHECK-04, EXT-01]

must_haves:
  truths:
    - "Running `bash data/dbt/run.sh build` writes `data/dbt/target/sandbox/checklist.parquet`"
    - "checklist.parquet has the 12 columns from CHECK-02 in the exact names and types declared in the schema contract"
    - "checklist.parquet has at least 2000 rows with no null canonical_name and no null specific_epithet"
    - "Every row in checklist.parquet has source='checklist'"
    - "`TRIM(family) = family` is true for every row in checklist.parquet"
    - "After `dbt build`, occurrences.parquet still has its baseline row count (< 50,000) — checklist rows did not enter int_combined"
    - "`data/run.py _run_dbt_build` copies checklist.parquet from sandbox to EXPORT_DIR alongside occurrences.parquet"
  artifacts:
    - path: "data/dbt/models/marts/checklist.sql"
      provides: "External-materialized parquet mart with county-centroid -> ecoregion spatial join, iNat family enrichment, source='checklist' literal"
      contains: "materialized='external'"
    - path: "data/dbt/models/marts/schema.yml"
      provides: "checklist model contract entry with 12 typed columns and not_null tests on canonical_name + specific_epithet"
      contains: "- name: checklist"
    - path: "data/tests/test_dbt_scaffold.py"
      provides: "_CHECKLIST_GUARD module-level skipif + 5 checklist content tests + 1 occurrences isolation test"
      contains: "_CHECKLIST_GUARD"
    - path: "data/run.py"
      provides: "_run_dbt_build artifact loop including checklist.parquet"
      contains: "checklist.parquet"
  key_links:
    - from: "data/dbt/models/marts/checklist.sql"
      to: "stg_checklist__species, stg_inat__taxon_lineage_extended, stg_geo__us_counties, stg_geo__ecoregions"
      via: "dbt ref() calls"
      pattern: "ref\\('stg_(checklist|inat|geo)__"
    - from: "data/dbt/models/marts/checklist.sql"
      to: "source('checklist_data', 'species_counties')"
      via: "dbt source() call producing 2,861-row (scientificName, county) grain"
      pattern: "source\\('checklist_data', 'species_counties'\\)"
    - from: "data/run.py"
      to: "EXPORT_DIR/checklist.parquet"
      via: "shutil.copy2 inside _run_dbt_build artifact tuple"
      pattern: "checklist\\.parquet"
---

<objective>
Add `checklist.parquet` as a first-class dbt mart: external-materialized SQL model with county-centroid ecoregion spatial join, iNat family enrichment, and `source='checklist'` literal; declared in the marts schema contract; copied to EXPORT_DIR by `run.py`; covered by pytest assertions that gate row count, null columns, family trim, source constancy, and isolation from `occurrences.parquet`.

Purpose: Deliver CHECK-01, CHECK-02, CHECK-04, EXT-01 for v4.0. The checklist mart is the data foundation Phase 112 (map layer) and Phase 113 (species pages) consume.

Output: New `checklist.sql`, schema.yml entry, new pytest block, updated `run.py` copy loop. After `bash data/dbt/run.sh build && cd data && uv run pytest -k checklist`, all new checklist tests pass and the occurrences isolation test still passes.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/111-checklist-pipeline/111-RESEARCH.md
@.planning/phases/111-checklist-pipeline/111-PATTERNS.md
@.planning/phases/111-checklist-pipeline/111-VALIDATION.md

@data/dbt/models/marts/occurrences.sql
@data/dbt/models/marts/schema.yml
@data/dbt/models/intermediate/int_species_universe.sql
@data/tests/test_dbt_scaffold.py
@data/run.py

<interfaces>
<!-- Sources of truth the executor must treat as fixed contracts. Do NOT re-derive these. -->

dbt sources/refs available (verified existing in research):
- source('checklist_data', 'species_counties') — 2,861 rows, columns: scientificName, county
- ref('stg_checklist__species') — columns: canonical_name, scientificName, genus, specific_epithet
- ref('stg_inat__canonical_to_taxon_id') — columns: canonical_name, taxon_id
- ref('stg_inat__taxon_lineage_extended') — columns: taxon_id, family, subfamily, tribe, genus, subgenus
- ref('stg_geo__us_counties') — columns: county, geom (polygon)
- ref('stg_geo__ecoregions') — columns: ecoregion_l3, geom (polygon)

External materialization config block (from data/dbt/models/marts/occurrences.sql lines 13-18):
- materialized='external'
- location='target/sandbox/checklist.parquet'  (relative; external_root in profiles.yml resolves to data/dbt/target/sandbox/)
- format='parquet'
- options={'CODEC': "'SNAPPY'"}

12-column output contract (CHECK-02, in this exact order, matching schema.yml):
canonical_name varchar (not_null)
scientificName varchar
genus varchar
specific_epithet varchar (not_null)
family varchar
lat double (NULL on every row)
lon double (NULL on every row)
year bigint (NULL on every row)
month bigint (NULL on every row)
county varchar
ecoregion_l3 varchar
source varchar (constant 'checklist')

Existing SANDBOX path constant in test_dbt_scaffold.py:
- SANDBOX = Path(__file__).resolve().parent.parent / "dbt" / "target" / "sandbox"

Existing run.py artifact tuple (line 74) to extend:
- ("occurrences.parquet", "counties.geojson", "ecoregions.geojson")
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wave 0 RED — checklist pytest assertions + Phase 111 isolation guard</name>
  <files>data/tests/test_dbt_scaffold.py</files>
  <read_first>
    - data/tests/test_dbt_scaffold.py (full file — current SANDBOX guard pattern and occurrences tests)
    - .planning/phases/111-checklist-pipeline/111-RESEARCH.md (section "pytest Assertions for checklist.parquet (CHECK-04)" and section "Pitfall 2: Checklist Rows Entering int_combined")
    - .planning/phases/111-checklist-pipeline/111-PATTERNS.md (section "data/tests/test_dbt_scaffold.py (test, new guard block)")
  </read_first>
  <behavior>
    - Test test_checklist_parquet_exists: asserts SANDBOX/'checklist.parquet' exists. Skipped pre-build via _CHECKLIST_GUARD.
    - Test test_checklist_row_count: SELECT COUNT(*) FROM read_parquet(checklist.parquet) returns >= 2000.
    - Test test_checklist_no_null_canonical_name: COUNT(*) WHERE canonical_name IS NULL == 0.
    - Test test_checklist_no_null_specific_epithet: COUNT(*) WHERE specific_epithet IS NULL == 0.
    - Test test_checklist_family_trim: COUNT(*) WHERE family <> TRIM(family) == 0.
    - Test test_checklist_source_constant: COUNT(DISTINCT source) == 1 AND DISTINCT source == 'checklist'.
    - Test test_occurrences_row_count_not_inflated_by_checklist: COUNT(*) FROM occurrences.parquet <= 50_000 (uses its own inline skipif on occurrences.parquet, NOT _CHECKLIST_GUARD).
    - All checklist tests share a single module-level `_CHECKLIST_GUARD = pytest.mark.skipif(not (SANDBOX / 'checklist.parquet').exists(), reason="run `bash data/dbt/run.sh build` first to produce checklist.parquet")` decorator.
    - Pre-build: every checklist test is SKIPPED (not failing). Post-build (after Task 2/3): every checklist test passes.
  </behavior>
  <action>
    Append a new section to `data/tests/test_dbt_scaffold.py` (after the existing occurrences/geojson tests, reusing the module-level `SANDBOX`, `duckdb`, `pytest`, `Path` imports). Add the module-level guard variable `_CHECKLIST_GUARD` exactly once. Add the six checklist test functions enumerated in `<behavior>` plus the isolation test. Use the f-string `parquet_path = str(SANDBOX / 'checklist.parquet')` idiom from the existing tests for query strings. The isolation test must NOT use `_CHECKLIST_GUARD`; it uses its own `@pytest.mark.skipif(not (SANDBOX / 'occurrences.parquet').exists(), ...)` so it runs even if checklist.parquet is missing. Do not modify any existing test. Use the SQL queries verbatim from RESEARCH.md "pytest Assertions for checklist.parquet (CHECK-04)" — they encode the exact column names CHECK-02/CHECK-04 require.
  </action>
  <verify>
    <automated>cd data && uv run pytest tests/test_dbt_scaffold.py -k checklist -v 2>&amp;1 | grep -E "SKIPPED|PASSED|FAILED"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '_CHECKLIST_GUARD' data/tests/test_dbt_scaffold.py` returns >= 7 (1 definition + 6 decorations).
    - `grep -c 'def test_checklist_' data/tests/test_dbt_scaffold.py` returns 6 (row_count, no_null_canonical_name, no_null_specific_epithet, family_trim, source_constant, parquet_exists).
    - `grep -c 'def test_occurrences_row_count_not_inflated_by_checklist' data/tests/test_dbt_scaffold.py` returns 1.
    - Pre-build pytest run: `cd data && uv run pytest tests/test_dbt_scaffold.py -k checklist --collect-only 2>&amp;1 | grep -c 'test_checklist_'` returns >= 6.
    - Pre-build pytest run: `cd data && uv run pytest tests/test_dbt_scaffold.py -k checklist 2>&amp;1 | grep -E 'passed|skipped'` reports all checklist tests as skipped (no failures, no errors).
    - The isolation test `test_occurrences_row_count_not_inflated_by_checklist` is not decorated with `_CHECKLIST_GUARD`. Confirm: `grep -B 1 'def test_occurrences_row_count_not_inflated_by_checklist' data/tests/test_dbt_scaffold.py` does NOT show `_CHECKLIST_GUARD` on the line above; it shows an inline `@pytest.mark.skipif(... occurrences.parquet ...)`.
  </acceptance_criteria>
  <done>Six checklist test functions + one occurrences isolation test exist in test_dbt_scaffold.py. All checklist tests skip cleanly when checklist.parquet is absent. No existing test is modified.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add checklist contract to marts/schema.yml + create checklist.sql mart</name>
  <files>data/dbt/models/marts/schema.yml, data/dbt/models/marts/checklist.sql</files>
  <read_first>
    - data/dbt/models/marts/schema.yml (full file — replicate existing `occurrences` model entry style and column declarations)
    - data/dbt/models/marts/occurrences.sql (lines 1-30 for config block; lines 26-70 for eco_dedup / eco_fallback / final_eco CTE pattern)
    - data/dbt/models/intermediate/int_species_universe.sql (LEFT JOIN pattern for stg_inat__canonical_to_taxon_id + stg_inat__taxon_lineage_extended)
    - .planning/phases/111-checklist-pipeline/111-RESEARCH.md (sections "Full checklist.sql Mart Structure", "schema.yml Contract for checklist Mart", "Pattern 2: County Centroid → Ecoregion Join", "Pitfall 1: Island/Kitsap County Centroid Falls in Water")
    - .planning/phases/111-checklist-pipeline/111-PATTERNS.md (section "data/dbt/models/marts/checklist.sql" — full mart structure)
  </read_first>
  <action>
    Create `data/dbt/models/marts/checklist.sql` as a new external-materialized parquet mart. Use the architecture-comment header (7 lines) and the full WITH/CTE structure from RESEARCH.md section "Full checklist.sql Mart Structure" verbatim. Key invariants the implementation must honor: (1) config block uses `materialized='external'`, `location='target/sandbox/checklist.parquet'`, `format='parquet'`, `options={'CODEC': "'SNAPPY'"}`; (2) the `sc` CTE joins `source('checklist_data', 'species_counties')` to `ref('stg_checklist__species')` USING (scientificName) — output grain is one row per (scientificName, county) pair, 2,861 rows expected; (3) family is fetched via the two-step join `LEFT JOIN ref('stg_inat__canonical_to_taxon_id') ctt ON ctt.canonical_name = sc.canonical_name; LEFT JOIN ref('stg_inat__taxon_lineage_extended') tle ON tle.taxon_id = ctt.taxon_id`, and stored as `TRIM(tle.family) AS family`; (4) `county_centroids` uses `ST_Centroid(geom)` from `ref('stg_geo__us_counties')`; (5) the `eco_fallback` CTE is MANDATORY (Island County and Kitsap County centroids fall in Puget Sound and miss `ST_Within` — the fallback uses correlated `ORDER BY ST_Distance LIMIT 1`); (6) `final_eco` UNION ALLs `eco_dedup WHERE ecoregion_l3 IS NOT NULL` with `eco_fallback`; (7) the final SELECT outputs the 12 columns in the order: canonical_name, scientificName, genus, specific_epithet, family, lat (NULL::DOUBLE), lon (NULL::DOUBLE), year (NULL::BIGINT), month (NULL::BIGINT), county, ecoregion_l3, source ('checklist' literal); (8) ALL varchar fields from raw sources (scientificName, genus, specific_epithet, county) are wrapped in TRIM(); (9) this mart MUST NOT contain any `ref('int_combined')` or `ref('int_ecdysis_base')` — checklist rows must never reach occurrences.parquet (locked STATE.md decision).

    Then extend `data/dbt/models/marts/schema.yml`: append a new model entry named `checklist` after the existing `species` entry. Set `config.contract.enforced: true`. Declare all 12 columns with data_type matching the SELECT casts (canonical_name varchar, scientificName varchar, genus varchar, specific_epithet varchar, family varchar, lat double, lon double, year bigint, month bigint, county varchar, ecoregion_l3 varchar, source varchar). Add `data_tests: [not_null]` to canonical_name and specific_epithet. No not_null test on family (Pitfall 3 noted — schema contract enforces type only, pytest enforces TRIM).
  </action>
  <verify>
    <automated>cd data && bash dbt/run.sh build 2>&amp;1 | tail -30 &amp;&amp; test -f dbt/target/sandbox/checklist.parquet &amp;&amp; uv run pytest tests/test_dbt_scaffold.py -k checklist -v</automated>
  </verify>
  <acceptance_criteria>
    - File `data/dbt/models/marts/checklist.sql` exists.
    - `grep -c "materialized='external'" data/dbt/models/marts/checklist.sql` returns 1.
    - `grep -c "target/sandbox/checklist.parquet" data/dbt/models/marts/checklist.sql` returns 1.
    - `grep -c "int_combined\|int_ecdysis_base" data/dbt/models/marts/checklist.sql` returns 0 (isolation guard at the source level).
    - `grep -c "eco_fallback\|ST_Distance" data/dbt/models/marts/checklist.sql` returns >= 2 (fallback CTE present).
    - `grep -c "'checklist'" data/dbt/models/marts/checklist.sql` returns >= 1 (source literal).
    - `grep -c "TRIM(" data/dbt/models/marts/checklist.sql` returns >= 5 (scientificName, genus, specific_epithet, county, family — verifies TRIM applied to all varchar outputs).
    - `grep -c "- name: checklist" data/dbt/models/marts/schema.yml` returns 1.
    - `grep -A 1 "- name: canonical_name" data/dbt/models/marts/schema.yml | grep -c "not_null"` returns >= 2 (occurrences entry + new checklist entry both declare not_null on canonical_name).
    - After `bash data/dbt/run.sh build`: file `data/dbt/target/sandbox/checklist.parquet` exists.
    - After build, `cd data && uv run pytest tests/test_dbt_scaffold.py -k checklist -v` reports all six checklist tests PASSED (none skipped, none failed).
    - After build, `cd data && uv run pytest tests/test_dbt_scaffold.py::test_occurrences_row_count_not_inflated_by_checklist -v` reports PASSED.
    - `duckdb -c "SELECT COUNT(*) FROM read_parquet('data/dbt/target/sandbox/checklist.parquet')"` returns a value >= 2000.
    - `duckdb -c "SELECT COUNT(DISTINCT source), MIN(source) FROM read_parquet('data/dbt/target/sandbox/checklist.parquet')"` returns `1, checklist`.
    - `duckdb -c "SELECT COUNT(*) FROM read_parquet('data/dbt/target/sandbox/checklist.parquet') WHERE lat IS NOT NULL OR lon IS NOT NULL OR year IS NOT NULL OR month IS NOT NULL"` returns 0 (county-range assertions carry no point/date data).
  </acceptance_criteria>
  <done>checklist.sql exists with external materialization, eco_fallback CTE, family enrichment, and 12-column output. schema.yml declares the checklist contract. `dbt build` succeeds and all Task 1 pytest assertions pass.</done>
</task>

<task type="auto">
  <name>Task 3: Wire checklist.parquet into run.py _run_dbt_build copy loop</name>
  <files>data/run.py</files>
  <read_first>
    - data/run.py (lines 60-80 — `_run_dbt_build` function body and artifact tuple)
    - .planning/phases/111-checklist-pipeline/111-PATTERNS.md (section "data/run.py (pipeline orchestrator, one-line change)")
    - .planning/phases/111-checklist-pipeline/111-RESEARCH.md (section "Pitfall 5: run.py copy step omitted")
  </read_first>
  <action>
    In `data/run.py`, locate the artifact iteration inside `_run_dbt_build` (the tuple currently reading `("occurrences.parquet", "counties.geojson", "ecoregions.geojson")` around line 74). Add `"checklist.parquet"` as a fourth element of that tuple. Preserve the surrounding `src = _DBT_SANDBOX / artifact; dst = _EXPORT_DIR / artifact; shutil.copy2(src, dst)` body unchanged. Do not introduce any other change to run.py — do not touch STEPS, the existing `checklist` STEP loader (which loads the raw TSV, unrelated to this copy step), or any helper.
  </action>
  <verify>
    <automated>cd data &amp;&amp; bash dbt/run.sh build 2>&amp;1 | tail -5 &amp;&amp; uv run python -c "import run; run._run_dbt_build()" &amp;&amp; test -f ../public/data/checklist.parquet &amp;&amp; uv run pytest -q</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '"checklist.parquet"' data/run.py` returns >= 1.
    - `grep -B 1 -A 4 "for artifact in" data/run.py | grep -c 'checklist.parquet'` returns 1 (the new element is inside the artifact tuple, not elsewhere in the file).
    - After `bash data/dbt/run.sh build && uv run python -c "import run; run._run_dbt_build()"` from the `data` directory: a file named `checklist.parquet` exists under EXPORT_DIR (default `public/data/checklist.parquet`).
    - `cd data && uv run pytest -q` exits 0 with no FAILED tests.
    - `git diff data/run.py` shows only the four-element tuple change — no other lines modified.
  </acceptance_criteria>
  <done>run.py copies checklist.parquet from sandbox to EXPORT_DIR alongside other dbt artifacts. Full pytest suite green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| committed CSV → DuckDB | Static repo asset; trusted (Bartholomew et al. 2024 checklist TSV is checked into the repo) |
| dbt sandbox → EXPORT_DIR | Local filesystem copy via run.py; trusted |
| dbt source ↔ marts/checklist.sql | Same DuckDB engine, no external IO |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-111-01 | Tampering | int_combined inadvertently consumes checklist rows | mitigate | checklist.sql forbidden from `ref('int_combined')` or `ref('int_ecdysis_base')`; Task 1 isolation test `test_occurrences_row_count_not_inflated_by_checklist` asserts occurrences.parquet ≤ 50,000 rows after build (baseline 47,876); Task 2 acceptance criterion greps for 0 occurrences of those refs in checklist.sql |
| T-111-02 | Information Disclosure | NULL family rows silently shipped | mitigate | dbt schema contract enforces varchar type on family; Task 1 `test_checklist_family_trim` asserts TRIM equality (catches whitespace and indirectly catches the lineage-missing case since NULL <> TRIM(NULL) is FALSE in DuckDB three-valued logic — see secondary check below) |
| T-111-03 | Repudiation | Source attribution lost in downstream UI | mitigate | source='checklist' literal in every row + Task 1 `test_checklist_source_constant` asserts DISTINCT source == {'checklist'} |
| T-111-04 | Denial of Service | Mart build OOMs on county centroid spatial join | accept | RESEARCH verified centroid+ST_Within against live DuckDB; 2,861 rows × 39 counties × ~85 ecoregions is well under DuckDB working set limits |
| T-111-SC | Tampering | npm/pip/cargo installs | n/a | No new packages added in this plan (RESEARCH "Package Legitimacy Audit: No new packages. Audit not applicable.") |
</threat_model>

<verification>
After all three tasks:

```bash
cd data && bash dbt/run.sh build
test -f dbt/target/sandbox/checklist.parquet
uv run pytest -q
# Expect: 0 failed; all checklist tests passed; isolation test passed.

uv run python -c "import run; run._run_dbt_build()"
test -f ../public/data/checklist.parquet

# Schema spot-check
duckdb -c "DESCRIBE SELECT * FROM read_parquet('data/dbt/target/sandbox/checklist.parquet')"
# Expect 12 columns in order: canonical_name VARCHAR, scientificName VARCHAR, genus VARCHAR,
#   specific_epithet VARCHAR, family VARCHAR, lat DOUBLE, lon DOUBLE, year BIGINT, month BIGINT,
#   county VARCHAR, ecoregion_l3 VARCHAR, source VARCHAR.

# Isolation spot-check
duckdb -c "SELECT COUNT(*) FROM read_parquet('data/dbt/target/sandbox/occurrences.parquet')"
# Expect: ≤ 50,000 (baseline pre-Phase-111 was 47,876).
```
</verification>

<success_criteria>
- `data/dbt/target/sandbox/checklist.parquet` exists after `bash data/dbt/run.sh build`.
- DESCRIBE on the parquet shows the 12 columns from CHECK-02 in the declared types.
- Row count ≥ 2000; no null canonical_name; no null specific_epithet; TRIM(family) = family on every row.
- DISTINCT source = {'checklist'} on every row.
- `occurrences.parquet` row count ≤ 50,000 after build (isolation preserved).
- `public/data/checklist.parquet` exists after `_run_dbt_build()` runs (run.py copy path wired).
- `cd data && uv run pytest -q` exits 0.
</success_criteria>

<output>
Create `.planning/phases/111-checklist-pipeline/111-01-SUMMARY.md` when done. The summary must record: actual row count produced, the ecoregion fallback county list (counties that hit `eco_fallback` rather than the primary `ST_Within`), and any species whose family came back NULL despite the iNat join (should be 0 per A2 in RESEARCH.md, but verify and record).
</output>
