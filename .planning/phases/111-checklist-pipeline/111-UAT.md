---
status: complete
phase: 111-checklist-pipeline
source: [111-01-SUMMARY.md, 111-02-SUMMARY.md]
started: 2026-05-24T03:00:00Z
updated: 2026-05-24T03:30:00Z
---

## Current Test

[testing complete]

## Tests

### 1. dbt build produces checklist.parquet
expected: Running `bash data/dbt/run.sh build` produces `data/dbt/target/sandbox/checklist.parquet` with >= 2000 rows, all 12 required columns, and all dbt contract tests pass (not_null on canonical_name + specific_epithet).
result: pass

### 2. Pytest checklist assertions pass
expected: `uv run pytest -k checklist -v` shows all 29 checklist tests pass — 6 content assertions (row count >= 2000, no null canonical_name, no null specific_epithet, TRIM(family)=family, source='checklist', parquet exists) plus the isolation test confirming checklist rows don't inflate occurrences.
result: pass

### 3. checklist.parquet copied to public/data/ by run.py
expected: After dbt build, `public/data/checklist.parquet` exists and contains >= 2000 rows. The copy happens in `_run_dbt_build()` in run.py.
result: pass

### 4. nightly.sh CloudFront delivery (GAP-03)
expected: After the next nightly run, manifest.json on CloudFront contains a 'checklist' key with a content-hashed filename (e.g., checklist-<12hex>.parquet), and a HEAD request to that URL returns HTTP 200 with Cache-Control: public, max-age=31536000, immutable.
result: pass
reported: "checklist-2d2805dce264.parquet"

## Summary

total: 4
passed: 4
issues: 0
pending: 0
skipped: 0

## Gaps

[none]
