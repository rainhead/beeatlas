---
phase: 111
status: pending
created: 2026-05-23
updated: 2026-05-23
---

# Phase 111 — Human Verification Items

These items require local execution and cannot be verified by automated checks alone.

## Gaps

### GAP-01: dbt build + checklist pytest assertions
- **status**: pending
- **requirement**: CHECK-01, CHECK-02, CHECK-04
- **action**: From `data/`: run `bash dbt/run.sh build && uv run pytest tests/test_dbt_scaffold.py -k checklist -v`
- **expected**: Build exits 0; all 6 checklist assertions pass (row count ≥ 2000, no null canonical_name, no null specific_epithet, TRIM(family)=family, source='checklist', parquet exists)
- **note**: The dbt sandbox is gitignored. Build evidence in SUMMARY.md came from a worktree; main-branch sandbox needs a local build to confirm.

### GAP-02: run.py EXPORT_DIR copy
- **status**: pending
- **requirement**: CHECK-02, CHECK-03
- **action**: After GAP-01 build, confirm `public/data/checklist.parquet` exists
- **expected**: File present at `public/data/checklist.parquet` after `_run_dbt_build()` runs

### GAP-03: CloudFront availability (nightly pipeline)
- **status**: pending
- **requirement**: CHECK-03
- **action**: After next nightly run, confirm `manifest.json` contains `checklist` key and the hashed URL returns HTTP 200 with immutable cache headers
- **expected**: `curl -I <CF_URL>/data/manifest.json` shows checklist key; URL from that key returns 200 + `Cache-Control: max-age=31536000, immutable`
- **note**: Requires actual nightly.sh execution — can only be verified post-deploy
