---
phase: 111-checklist-pipeline
plan: 02
type: execute
wave: 2
depends_on: [111-01]
files_modified:
  - data/nightly.sh
autonomous: true
requirements: [CHECK-03]

must_haves:
  truths:
    - "nightly.sh uploads checklist.parquet to s3://{BUCKET}/data/checklist-{hash}.parquet using the existing _upload_hashed function"
    - "manifest.json written by nightly.sh contains a 'checklist' key whose value is the hashed filename produced by _upload_hashed"
    - "CloudFront invalidation paths include /data/manifest.json (already covered by the existing invalidation step — no new path required since manifest.json is what gates discovery)"
    - "Existing manifest keys (occurrences, species, seasonality, counties, ecoregions, places, places_meta, generated_at) are preserved"
  artifacts:
    - path: "data/nightly.sh"
      provides: "_upload_hashed call for checklist.parquet + manifest.json key 'checklist'"
      contains: "checklist_name"
  key_links:
    - from: "data/nightly.sh"
      to: "s3://{BUCKET}/data/checklist-{hash}.parquet"
      via: "_upload_hashed function (existing, unchanged)"
      pattern: "_upload_hashed \"\\$EXPORT_DIR/checklist\\.parquet\""
    - from: "data/nightly.sh manifest.json heredoc"
      to: "checklist key consumed by Phase 112 frontend"
      via: "JSON write step"
      pattern: "\"checklist\":"
---

<objective>
Extend `data/nightly.sh` so the nightly pipeline uploads `checklist.parquet` to S3 (CloudFront origin) under a content-hashed name and exposes that name in `manifest.json` for downstream consumers (Phase 112 map layer).

Purpose: Deliver CHECK-03. Without this wiring, the parquet built by Plan 01 reaches `EXPORT_DIR` but never goes to S3, and Phase 112 has no way to fetch it.

Output: A single edit to `nightly.sh` adding one `_upload_hashed` call and one manifest key. No new functions, no new arguments to `_upload_hashed`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/111-checklist-pipeline/111-RESEARCH.md
@.planning/phases/111-checklist-pipeline/111-PATTERNS.md
@.planning/phases/111-checklist-pipeline/111-01-PLAN.md

@data/nightly.sh

<interfaces>
<!-- Existing nightly.sh contracts the executor must treat as fixed. -->

_upload_hashed function signature (data/nightly.sh lines 139-148):
  _upload_hashed <src_path> <basename> [optional aws-cli flags ...]
  - Returns (echoes) the hashed filename "{basename}-{first12-sha256}.{ext}".
  - Uploads to s3://$BUCKET/data/{hashed_name} with Cache-Control: public, max-age=31536000, immutable.
  - Plain parquet uses no --content-type override (S3 default application/octet-stream).

Existing upload-call ordering (lines 150-156), to be preserved with new line appended:
  occ_name=$(_upload_hashed "$EXPORT_DIR/occurrences.parquet" "occurrences")
  species_name=$(_upload_hashed "$EXPORT_DIR/species.json" "species")
  seasonality_name=$(_upload_hashed "$EXPORT_DIR/seasonality.json" "seasonality")
  counties_name=$(_upload_hashed "$EXPORT_DIR/counties.geojson" "counties" --content-type application/json)
  ecoregions_name=$(_upload_hashed "$EXPORT_DIR/ecoregions.geojson" "ecoregions" --content-type application/json)
  places_name=$(_upload_hashed "$EXPORT_DIR/places.geojson" "places" --content-type application/json)
  places_meta_name=$(_upload_hashed "$EXPORT_DIR/places.json" "places_meta" --content-type application/json)

Existing manifest.json heredoc keys (data/nightly.sh lines 158-169), in current order:
  occurrences, species, seasonality, counties, ecoregions, places, places_meta, generated_at

CloudFront invalidation block (line ~186): already invalidates /data/manifest.json — no change needed.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add checklist upload + manifest entry to nightly.sh</name>
  <files>data/nightly.sh</files>
  <read_first>
    - data/nightly.sh (full file — read lines 120-200 in particular for upload block, manifest heredoc, and CloudFront invalidation)
    - .planning/phases/111-checklist-pipeline/111-PATTERNS.md (section "data/nightly.sh (deployment wrapper, two additions)")
    - .planning/phases/111-checklist-pipeline/111-RESEARCH.md (section "Pattern 4: nightly.sh Upload + Manifest" and section "Pitfall 4: manifest.json Omits checklist Key")
  </read_first>
  <action>
    Make two atomic edits to `data/nightly.sh`:

    Edit 1 — add upload call: After the existing `places_meta_name=$(_upload_hashed ...)` line (currently line 156), insert a new line:
      `checklist_name=$(_upload_hashed "$EXPORT_DIR/checklist.parquet" "checklist")`
    No --content-type flag (parquet is plain octet-stream like occurrences.parquet). Do not reorder the existing calls.

    Edit 2 — extend manifest.json heredoc: Inside the `cat > "$EXPORT_DIR/manifest.json" <<JSON` block, insert a new line for the checklist key. Place it after the `"places_meta": "$places_meta_name",` line and before the `"generated_at": "$(_ts)"` line. The new line is:
      `  "checklist": "$checklist_name",`
    Preserve JSON comma rules — `generated_at` remains the last key (no trailing comma). Do not touch any other key.

    Do not modify the CloudFront invalidation block. `/data/manifest.json` is already in the invalidation paths and is the discovery file Phase 112 fetches; the new content-hashed `checklist-{hash}.parquet` is `immutable` and does not need invalidation.

    Do not introduce new shell variables outside `checklist_name`. Do not modify `_upload_hashed`. Do not add documentation comments to nightly.sh — code conventions in this file are terse.
  </action>
  <verify>
    <automated>bash -n data/nightly.sh &amp;&amp; grep -c 'checklist_name=$(_upload_hashed' data/nightly.sh &amp;&amp; grep -c '"checklist": "$checklist_name"' data/nightly.sh</automated>
  </verify>
  <acceptance_criteria>
    - `bash -n data/nightly.sh` exits 0 (no shell syntax errors).
    - `grep -c 'checklist_name=$(_upload_hashed "$EXPORT_DIR/checklist.parquet" "checklist")' data/nightly.sh` returns 1.
    - `grep -c '"checklist": "$checklist_name"' data/nightly.sh` returns 1.
    - `grep -c '_upload_hashed' data/nightly.sh` returns 9 (1 function definition + 7 existing call sites + 1 new call site for checklist).
    - The new upload line appears AFTER the `places_meta_name` line and BEFORE the `cat > "$EXPORT_DIR/manifest.json"` heredoc start. Verify with: `awk '/places_meta_name=/{p=NR} /checklist_name=/{c=NR} /cat > "\$EXPORT_DIR\/manifest.json"/{m=NR} END{exit !(p<c && c<m)}' data/nightly.sh` exits 0.
    - The new manifest key appears AFTER `"places_meta":` and BEFORE `"generated_at":`. Verify: `awk '/"places_meta":/{p=NR} /"checklist":/{c=NR} /"generated_at":/{g=NR} END{exit !(p<c && c<g)}' data/nightly.sh` exits 0.
    - `grep -c 'generated_at' data/nightly.sh` is unchanged (1 — the manifest entry; the key is still last in the heredoc).
    - The `aws s3 cp ... manifest.json` line and the CloudFront invalidation `--paths` list are unchanged. Verify: `git diff data/nightly.sh -- data/nightly.sh` shows only the two intentional insertions (the upload line and the manifest key line).
    - A dry-run heredoc sanity check produces valid JSON: `bash -c 'EXPORT_DIR=/tmp checklist_name=checklist-abc123.parquet occ_name=o species_name=s seasonality_name=z counties_name=c ecoregions_name=e places_name=p places_meta_name=pm _ts(){ echo 2026-05-23; }; sed -n "/^cat > .*manifest.json.*<<JSON/,/^JSON\$/p" data/nightly.sh | sed "1d;\$d" | envsubst' | python3 -c 'import sys, json; json.loads(sys.stdin.read())'` exits 0.
  </acceptance_criteria>
  <done>nightly.sh uploads checklist.parquet alongside the other artifacts and records the hashed filename under the 'checklist' key in manifest.json. Shell syntax valid; manifest JSON valid; no unrelated lines touched.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local EXPORT_DIR → S3 bucket | AWS CLI auth via existing AWS_PROFILE; no new credentials introduced |
| manifest.json → public CloudFront | Already public via CloudFront; the checklist key is metadata, not a secret |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-111-05 | Tampering | manifest.json malformed JSON breaks frontend bootstrap | mitigate | Task 1 acceptance criterion validates the heredoc parses as JSON via python3 -c 'json.loads' after envsubst |
| T-111-06 | Information Disclosure | New S3 object inherits wrong cache headers | accept | Re-uses the existing `_upload_hashed` function whose Cache-Control header is verified by inspection (line 145: immutable, 1 year). Content-hashed name guarantees safe immutability |
| T-111-07 | Denial of Service | Failed S3 upload aborts pipeline before manifest writes | mitigate | Existing `set -euo pipefail` (verify at top of nightly.sh) causes the failure to surface as a non-zero exit; manifest.json is written only after all uploads succeed, so an incomplete state is impossible |
| T-111-SC | Tampering | npm/pip/cargo installs | n/a | No package installs in this plan |
</threat_model>

<verification>
After Task 1 (full end-to-end smoke requires AWS credentials; structural checks run locally):

```bash
# Local syntax + shape checks (always runnable):
bash -n data/nightly.sh
grep -c 'checklist_name=$(_upload_hashed' data/nightly.sh   # → 1
grep -c '"checklist": "$checklist_name"' data/nightly.sh    # → 1

# Manifest JSON well-formedness (envsubst with dummy values, see Task 1 acceptance):
# Exits 0 if the heredoc is valid JSON.

# Full nightly smoke (requires AWS_PROFILE configured + sandbox built):
#   cd data && bash dbt/run.sh build && uv run python -c "import run; run._run_dbt_build()"
#   bash data/nightly.sh   # or the dry-run flag if one exists
# Then verify CloudFront:
#   curl -s https://d<CF-ID>.cloudfront.net/data/manifest.json | jq .checklist
#   # → "checklist-<12hex>.parquet"
#   curl -I https://d<CF-ID>.cloudfront.net/data/$(curl -s https://d<CF-ID>.cloudfront.net/data/manifest.json | jq -r .checklist)
#   # → HTTP/2 200 with Cache-Control: public, max-age=31536000, immutable
```

The CloudFront curl checks are deferred to manual UAT (Manual-Only Verifications row in 111-VALIDATION.md) — they require a real nightly run.
</verification>

<success_criteria>
- `bash -n data/nightly.sh` exits 0.
- New upload line and new manifest key present in the correct positions.
- Dry-run heredoc parses as valid JSON.
- No unrelated lines modified.
- Existing 7 upload calls + 8 manifest keys still present and ordered.
</success_criteria>

<output>
Create `.planning/phases/111-checklist-pipeline/111-02-SUMMARY.md` when done. Record the two `git diff data/nightly.sh` hunks verbatim and note that the CloudFront verification is deferred to the manual UAT step listed in 111-VALIDATION.md (cannot be exercised from the planner machine without AWS credentials and a real nightly run).
</output>
