---
context: phase
phase: 111-checklist-pipeline
task: null
total_tasks: 4
status: needs_review
last_updated: 2026-05-24T03:00:30Z
---

# BLOCKING CONSTRAINTS — Read Before Anything Else

> These are not suggestions. Each constraint below was discovered through code review.
> Acknowledge before proceeding.

- [ ] CR-01: checklist.sql uses INNER JOIN to final_eco — must be LEFT JOIN to avoid silently dropping county rows

**Do not fix unrelated files before addressing CR-01.**

## Critical Anti-Patterns

| Pattern | Description | Severity | Prevention Mechanism |
|---------|-------------|----------|---------------------|
| INNER JOIN on geo lookup | checklist.sql final SELECT uses INNER JOIN to final_eco CTE; any county name mismatch silently drops the row | blocking | Change to LEFT JOIN; run `uv run pytest -k checklist` to confirm row count unchanged |

<current_state>
Phase 111 (Checklist Pipeline) is fully executed — all 4 tasks committed, both waves merged. Verification returned `human_needed` (11/12). Two items remain before the phase can be marked `passed`:

1. **CR-01 fix** (1-line code change in checklist.sql)
2. **GAP-03** (nightly pipeline confirms CloudFront, non-blocking — can close async)
</current_state>

<completed_work>
- Plan 111-01 Wave 1: test RED commit (b061e73), checklist.sql GREEN (8595097), run.py copy (94cda04)
- Plan 111-02 Wave 2: nightly.sh upload + manifest.json key (87e3c59)
- Code review: 111-REVIEW.md committed (a4c1b47)
- Verification: 111-VERIFICATION.md committed (e1be8ef), human_needed
- UAT: GAP-01 resolved (dbt build + pytest ✓), GAP-02 resolved (public/data copy ✓), GAP-03 pending (nightly)
</completed_work>

<remaining_work>
1. Fix CR-01 in data/dbt/models/marts/checklist.sql — change INNER JOIN → LEFT JOIN in the final SELECT (the join against final_eco CTE)
2. Run: `cd data && bash dbt/run.sh build && uv run pytest -k checklist -v` — confirm row count unchanged
3. Delete `test_no_production_dbt_references` from data/tests/test_dbt_scaffold.py (WR-01 — always fails, spike-isolation test that is now obsolete)
4. Commit fixes
5. Re-run `/gsd-verify-work 111` to mark phase passed
6. (Async) After next nightly run: confirm manifest.json has 'checklist' key (GAP-03)
</remaining_work>

<decisions_made>
- LEFT JOIN (not INNER) is correct for final_eco join — preserves all rows, assigns NULL ecoregion for any unmatched county
- checklist.parquet copy to public/data/ requires run.py (not dbt build alone) — copy step is in `_run_dbt_build()`
</decisions_made>

<blockers>
- CR-01: 1-line fix needed in checklist.sql before verification can pass
- GAP-03: non-blocking, deferred to next nightly run
</blockers>

## Required Reading (in order)
1. `.planning/phases/111-checklist-pipeline/111-REVIEW.md` — CR-01 and WR-01 findings to fix
2. `.planning/phases/111-checklist-pipeline/111-UAT.md` — UAT status (GAP-01/02 resolved, GAP-03 pending)
3. `data/dbt/models/marts/checklist.sql` — the file to fix (INNER → LEFT JOIN)

## Infrastructure State
- `data/dbt/target/sandbox/checklist.parquet` — exists (built this session)
- `public/data/checklist.parquet` — exists (manually copied this session)
- nightly.sh — modified with checklist upload, not yet run on production

<context>
Full plan+execute cycle completed in one session. The phase is 95% done — only a 1-line SQL fix and one obsolete test deletion separate it from verification passing. The code review CR-01 finding is genuine (INNER JOIN is fragile) but doesn't cause data loss with the current WA county data.
</context>

<next_action>
Start with: Read data/dbt/models/marts/checklist.sql, find the final SELECT block, change the join on final_eco from INNER JOIN to LEFT JOIN. Then run dbt build + pytest to confirm.
</next_action>
