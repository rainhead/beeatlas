---
phase: 172-accomplishment-view
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - src/tests/fixtures/collectors.fixture.json
  - src/tests/data-collectors.test.ts
  - data/tests/test_collectors_export.py
  - data/tests/test_collector_maps.py
autonomous: true
requirements: [ACCOM-01, ACCOM-02, ACCOM-03, ACCOM-04]

must_haves:
  truths:
    - "npm test passes with collectors.fixture.json carrying the five new accomplishment fields"
    - "data/tests/test_collector_maps.py exists and fails for the right reason (collector_maps.py absent — turns GREEN in Plan 03)"
    - "data/tests/test_collectors_export.py asserts the new aggregation fields and fails until the export is extended (turns GREEN in Plan 02)"
  artifacts:
    - path: "src/tests/fixtures/collectors.fixture.json"
      provides: "Test fixture with active_since, seasons_count, county_count, ecoregion_count, species_by_genus on every entry"
      contains: "species_by_genus"
    - path: "src/tests/data-collectors.test.ts"
      provides: "Phase 172 fixture-shape assertions for the five new fields"
      contains: "active_since"
    - path: "data/tests/test_collectors_export.py"
      provides: "Aggregation field + species-list tests (RED until Plan 02)"
      contains: "species_by_genus"
    - path: "data/tests/test_collector_maps.py"
      provides: "Binary-fill county/ecoregion SVG golden-shape + determinism tests (RED until Plan 03)"
      contains: "_write_coverage_svg"
  key_links:
    - from: "src/tests/data-collectors.test.ts"
      to: "src/tests/fixtures/collectors.fixture.json"
      via: "readFileSync in a Phase 172 describe block"
      pattern: "collectors.fixture.json"
---

<objective>
Create the Nyquist Wave 0 failing-test scaffold for Phase 172. This plan writes
the tests and fixtures FIRST; the implementation plans (02 export, 03 SVG
generator) turn them GREEN.

Three test surfaces:
1. Frontend fixture + Vitest assertions for the five new `collectors.json` fields
   (self-consistent — `npm test` stays GREEN immediately).
2. Python `test_collectors_export.py` extended with aggregation-field assertions
   (RED — fails until Plan 02 extends the export).
3. New Python `test_collector_maps.py` with binary-fill SVG golden-shape +
   determinism tests (RED — fails until Plan 03 creates `collector_maps.py`).

Purpose: lock the observable contract for all four ACCOM requirements before any
implementation, so executors implement against a fixed target.
Output: extended fixture, extended frontend test, extended export test, new SVG test.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/172-accomplishment-view/172-CONTEXT.md
@.planning/phases/172-accomplishment-view/172-RESEARCH.md
@.planning/phases/172-accomplishment-view/172-PATTERNS.md
@.planning/phases/172-accomplishment-view/172-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend frontend fixture + add Phase 172 Vitest assertions (GREEN)</name>
  <files>src/tests/fixtures/collectors.fixture.json, src/tests/data-collectors.test.ts</files>
  <read_first>
    - src/tests/fixtures/collectors.fixture.json (the file to extend — current Phase 171 14-field shape)
    - src/tests/data-collectors.test.ts (the test to extend — note the Phase 171 `beforeAll` block reads the fixture via readFileSync at lines 80-89)
    - _data/collectors.js (CRITICAL: `collectorsArray` is `[]` on a clean checkout because public/data/collectors.json is gitignored/S3-delivered post Phase 171.1 — so new-field assertions MUST run against the fixture, NOT the live loader's `collectorsArray`)
    - .planning/phases/172-accomplishment-view/172-PATTERNS.md (section "src/tests/fixtures/collectors.fixture.json + data-collectors.test.ts")
  </read_first>
  <action>
    Add the five Phase 172 fields to BOTH existing entries (alice, bob) in
    src/tests/fixtures/collectors.fixture.json: `active_since` (int), `seasons_count`
    (int), `county_count` (int), `ecoregion_count` (int), and `species_by_genus` (a
    list of {genus, species:[{canonical_name, slug, count}]}). Give alice and bob
    distinct, realistic values; at least one entry must have a multi-genus
    species_by_genus with genera in alphabetical order and species alphabetical
    within genus (D-04 ordering). Slugs use the "Genus/epithet" format (D-04).

    In src/tests/data-collectors.test.ts add a NEW describe block
    "Phase 172 — accomplishment fields (ACCOM-01..04)" that reads the fixture via
    readFileSync (mirror the Phase 171 `beforeAll` at lines 80-89 — do NOT iterate
    the live-loader `collectors.collectorsArray`, which is empty on a clean checkout
    per memory feedback_no_committed_data_artifacts). Assert per entry: active_since
    is number, seasons_count is number, county_count is number, ecoregion_count is
    number, species_by_genus is an Array; and spot-check the first genus group:
    genus is string, species is Array, and each species has canonical_name (string),
    slug (string), count (number). Per D-04 also assert slug contains "/".
  </action>
  <verify>
    <automated>npm test -- data-collectors</automated>
  </verify>
  <acceptance_criteria>
    - `npm test` passes (GREEN) — the fixture and assertions are self-consistent.
    - The new describe block reads collectors.fixture.json (grep
      `grep -c "Phase 172" src/tests/data-collectors.test.ts` returns ≥ 1).
    - Both fixture entries contain all five new keys (grep `species_by_genus` and
      `active_since` each appear ≥ 2 times in the fixture).
    - No assertion iterates `collectors.collectorsArray` for the new fields.
  </acceptance_criteria>
  <done>npm test is GREEN with the extended fixture + Phase 172 fixture-shape assertions.</done>
</task>

<task type="auto">
  <name>Task 2: Extend test_collectors_export.py with aggregation-field tests (RED)</name>
  <files>data/tests/test_collectors_export.py</files>
  <read_first>
    - data/tests/test_collectors_export.py (the file to extend — `_write_test_occurrences_parquet` lines 26-70, `_write_test_species_parquet` lines 73-92, `_setup_env`, existing tests)
    - data/collectors_export.py (the target it tests — current `_QUERY` and records dict; will be extended in Plan 02)
    - .planning/phases/172-accomplishment-view/172-PATTERNS.md (section "data/tests/test_collectors_export.py (MODIFY — test)")
    - .planning/phases/172-accomplishment-view/172-RESEARCH.md (Validation Architecture → Existing Tests to Extend)
  </read_first>
  <action>
    Extend `_write_test_occurrences_parquet`: add columns `year` (int32), `county`
    (string), `ecoregion_l3` (string) to the schema and supply values per row.
    Include a GAP-YEAR for at least one collector (e.g. alice years 2020 and 2022,
    no 2021) so a max−min+1 span (3) differs from COUNT(DISTINCT year) (2) — this
    stress-tests D-05.

    Extend `_write_test_species_parquet`: add columns `genus`, `canonical_name`,
    `slug` so the Plan 02 `_SPECIES_QUERY` join resolves; keep `specific_epithet`
    (the D-04 species-rank gate). taxon_id=10 → genus "Testgenus",
    canonical_name "Testgenus testicus", slug "Testgenus/testicus".

    Add tests (each calls `_setup_env` + `export_collectors_step`, then reads
    collectors.json): `test_badge_fields_present_and_typed` (active_since int,
    seasons_count int — ACCOM-04); `test_seasons_count_is_distinct_years`
    (alice seasons_count == 2 for years {2020,2022}, NOT 3 — D-05);
    `test_active_since_is_min_year` (alice active_since == 2020 — D-05);
    `test_county_and_ecoregion_counts` (county_count / ecoregion_count are
    COUNT(DISTINCT …) over the D-01 rows — ACCOM-01/03 captions);
    `test_species_by_genus_structure` (list of {genus, species:[{canonical_name,
    slug, count}]}; species-rank only — ACCOM-02). These FAIL now (RED) because the
    export does not yet emit these fields — they turn GREEN in Plan 02.
  </action>
  <verify>
    <automated>cd data && uv run pytest tests/test_collectors_export.py -x 2>&1 | tail -15  # RED EXPECTED: new field assertions fail (KeyError/AssertionError) until Plan 02; existing tests must still pass</automated>
  </verify>
  <acceptance_criteria>
    - The new test functions are COLLECTED by pytest (no import/syntax errors in the test file).
    - The new field tests FAIL with KeyError/AssertionError on the missing
      active_since/seasons_count/county_count/ecoregion_count/species_by_genus keys —
      NOT on a fixture/parquet error.
    - The pre-existing tests (test_gate_excludes_inat_obs_only, test_required_keys,
      test_status_split_invariant, etc.) still PASS with the extended fixture schema.
  </acceptance_criteria>
  <done>test_collectors_export.py is extended; new aggregation tests are RED for the right reason; existing tests stay GREEN.</done>
</task>

<task type="auto">
  <name>Task 3: Create test_collector_maps.py SVG golden-shape + determinism tests (RED)</name>
  <files>data/tests/test_collector_maps.py</files>
  <read_first>
    - data/tests/test_species_maps.py (analog — header lines 1-23, monkeypatch-on-module pattern, ET.parse + findall class assertions)
    - data/species_maps.py (the module Plan 03 generalizes — `_write_species_svg`, `_build_county_backdrop`, `STYLE_CSS`, `SVG_NS`, the attrib-sort idempotency block lines 226-228)
    - .planning/phases/172-accomplishment-view/172-PATTERNS.md (section "data/tests/test_collector_maps.py (NEW — test)" — full test excerpts)
    - .planning/phases/172-accomplishment-view/172-RESEARCH.md (Patterns 1 + 2: `_write_coverage_svg`, `_load_ecoregion_geojsons`)
  </read_first>
  <action>
    Create data/tests/test_collector_maps.py. Import `collector_maps` as a module
    plus `_write_coverage_svg`, `_load_ecoregion_geojsons`, and `SVG_NS` from it
    (this import FAILS now — RED — until Plan 03 creates the module).

    Write these tests against the contract Plan 03 must satisfy:
    `test_write_coverage_svg_fills_contributed_polygon` (given a backdrop + one
    Polygon geom + filled_names={"King"}, the output SVG has exactly one
    `path[@class="checklist-county"]`); `test_write_coverage_svg_skips_unfilled`
    (a name NOT in filled_names produces zero checklist-county paths);
    `test_write_coverage_svg_handles_multipolygon` (MultiPolygon geom emits a
    checklist-county path); `test_write_coverage_svg_deterministic` (two calls with
    identical inputs produce byte-identical files — the attrib-sort idempotency
    contract); `test_load_ecoregion_geojsons_keys_on_NA_L3NAME` (write a tiny
    ecoregions.geojson with a feature whose properties carry `NA_L3NAME`; the loader
    returns a dict keyed by that value — Pitfall 2); and an end-to-end
    `test_generate_collector_maps_emits_per_login_svgs` (monkeypatch
    `collector_maps.ASSETS_DIR` to tmp_path, write a minimal occurrences.parquet
    with collector_inat_login/ecdysis_id/record_type/county/ecoregion_l3/year and an
    ecoregions.geojson, run `generate_collector_maps`, assert a `{login}.svg` and a
    `{login}-eco.svg` exist under tmp_path/collector-maps/). Use a minimal occurrences
    fixture helper that includes the D-01-passing and D-01-failing rows so the
    generator only emits maps for WABA contributors.
  </action>
  <verify>
    <automated>cd data && uv run pytest tests/test_collector_maps.py -x 2>&1 | tail -15  # RED EXPECTED: ModuleNotFoundError/ImportError (collector_maps.py absent) until Plan 03</automated>
  </verify>
  <acceptance_criteria>
    - pytest collects the file and fails with ModuleNotFoundError / ImportError on
      `import collector_maps` (or its symbols) — proving the test targets the
      not-yet-built module, NOT a syntax error in the test itself.
    - The test bodies reference `checklist-county`, `NA_L3NAME`, `{login}.svg`, and
      `{login}-eco.svg` (grep each appears ≥ 1).
    - No test depends on the real beeatlas.duckdb or public/data/ (uses tmp_path +
      monkeypatched ASSETS_DIR only).
  </acceptance_criteria>
  <done>test_collector_maps.py exists; all tests are RED via missing-module import; turned GREEN in Plan 03.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test fixtures → CI gate | Hand-written fixture data drives npm test / pytest; no production data flows here |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-172-FIX | Tampering | collectors.fixture.json drift vs real export shape | accept | Plan 02/03 turn the RED tests GREEN against the same shape; the fixture and the live export are reconciled by the cross-language field contract here |
| T-172-SC | Tampering | npm/pip/cargo installs | accept | No new packages this phase (RESEARCH Package Legitimacy Audit: all stdlib/existing deps) — no install surface |
</threat_model>

<artifacts_produced>
## Artifacts this phase produces (this plan)

- Modified `src/tests/fixtures/collectors.fixture.json` — adds fields `active_since`, `seasons_count`, `county_count`, `ecoregion_count`, `species_by_genus` to every entry.
- Modified `src/tests/data-collectors.test.ts` — new `describe('Phase 172 — accomplishment fields (ACCOM-01..04)')` block (fixture-driven assertions).
- Modified `data/tests/test_collectors_export.py` — extended fixture helpers (`_write_test_occurrences_parquet` gains `year`/`county`/`ecoregion_l3`; `_write_test_species_parquet` gains `genus`/`canonical_name`/`slug`) + new tests `test_badge_fields_present_and_typed`, `test_seasons_count_is_distinct_years`, `test_active_since_is_min_year`, `test_county_and_ecoregion_counts`, `test_species_by_genus_structure`.
- New `data/tests/test_collector_maps.py` — tests `test_write_coverage_svg_fills_contributed_polygon`, `test_write_coverage_svg_skips_unfilled`, `test_write_coverage_svg_handles_multipolygon`, `test_write_coverage_svg_deterministic`, `test_load_ecoregion_geojsons_keys_on_NA_L3NAME`, `test_generate_collector_maps_emits_per_login_svgs`.
</artifacts_produced>

<verification>
- `npm test` GREEN (Task 1 fixture is self-consistent).
- `cd data && uv run pytest tests/test_collectors_export.py tests/test_collector_maps.py` is RED *by design* — the new tests fail because Plans 02/03 have not run yet. This is the only wave where the Python suite is expected RED; Wave 1 (Plans 02 + 03) restores both-suites-GREEN.
- Pre-existing Python tests still pass under the extended fixture schemas.
</verification>

<success_criteria>
- Frontend fixture carries the five new fields; `npm test` GREEN against the fixture.
- New Python tests are collected and RED for the right reason (missing fields / missing module), not on harness errors.
- Existing tests remain GREEN.
</success_criteria>

<notes>
**171.1 coordination (resolved):** Phase 171.1 is COMPLETE — `collectors.json` is
already S3-delivered and gitignored, and `collectors.fixture.json` exists with the
Phase 171 shape. This plan EXTENDS that existing fixture in the same wave as the
test assertions, keeping `npm test` GREEN on a clean checkout regardless of S3 (per
critical_171.1_coordination). The new-field assertions deliberately target the
fixture (not the live `collectorsArray`, which is `[]` on a clean checkout) per
memory feedback_no_committed_data_artifacts — a regenerated committed
collectors.json is NEVER a valid verification surface.
</notes>

<output>
Create `.planning/phases/172-accomplishment-view/172-01-SUMMARY.md` when done.
</output>
