---
phase: 172-accomplishment-view
plan: 05
type: execute
wave: 3
depends_on: [172-04]
files_modified: []
autonomous: false
requirements: [ACCOM-01, ACCOM-02, ACCOM-03, ACCOM-04]

must_haves:
  truths:
    - "An operator confirms a real multi-county collector page renders all four accomplishments correctly (county map, ecoregion map + caption, genus-grouped species list with working /species/{slug}/ links, accurate 'Active since YYYY (N seasons)' badge)"
  artifacts:
    - path: ".planning/phases/172-accomplishment-view/172-HUMAN-UAT.md"
      provides: "Operator UAT sign-off record"
  key_links:
    - from: "rendered collector page"
      to: "/data/collector-maps/{login}.svg and {login}-eco.svg"
      via: "lazy <img> render"
      pattern: "/data/collector-maps/"
---

<objective>
Operator visual UAT for the Accomplishment View (this phase is UI hint: yes —
final verification must be a blocking human checkpoint, per memory
feedback_uat_ui_phases). All automated gates passed in Plans 01–04; this plan
confirms visual + functional correctness on a real collector page that automated
tests cannot judge.

Purpose: catch rendering/data-accuracy issues (wrong counties filled, broken
species links, off ecoregion count, layout breakage) before the phase closes.
Output: a HUMAN-UAT sign-off record.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/172-accomplishment-view/172-CONTEXT.md
@.planning/phases/172-accomplishment-view/172-UI-SPEC.md
@.planning/phases/172-accomplishment-view/172-VALIDATION.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Operator UAT — collector accomplishment view</name>
  <files>.planning/phases/172-accomplishment-view/172-HUMAN-UAT.md</files>
  <action>
    Pause for operator verification (autonomous: false — does not auto-advance in
    yolo mode per feedback_uat_ui_phases). The operator regenerates the gitignored
    collector data locally, builds + serves the site, opens a prolific multi-county
    collector page, and confirms all four accomplishment elements (badge, county
    map, ecoregion map + caption, genus-grouped species list with working species
    links) are present and accurate, then records PASS or issues in
    172-HUMAN-UAT.md. See the steps in `<how-to-verify>`.
  </action>
  <what-built>
    Plans 01–04 delivered: extended collectors.json (active_since, seasons_count,
    county_count, ecoregion_count, species_by_genus); a new collector_maps.py SVG
    generator producing per-collector county + ecoregion binary-fill maps; the
    collector-detail.njk render (badge + two coverage maps + genus-grouped species
    list) with places.css styles; run.py + nightly.sh wiring for stable-URL S3
    delivery + CloudFront invalidation. Full automated suite (npm test + pytest) is
    GREEN.
  </what-built>
  <how-to-verify>
    1. Regenerate local data so the page has the new fields + SVGs. The committed
       working tree has NO collectors.json/collector-maps (gitignored — S3-delivered).
       Per memory project_local_uat_stale_occurrences_db, run the relevant pipeline
       steps locally against a current occurrences.parquet:
       `cd data && uv run python collectors_export.py` (regenerate collectors.json)
       and `cd data && uv run python collector_maps.py` (regenerate the SVGs into
       public/data/collector-maps/). If occurrences.parquet/species.parquet are stale
       or absent locally, run the upstream export/dbt steps first (or `uv run python
       run.py` if the Ecdysis auth gate is satisfied).
    2. Build + serve: `npm run build` then `npm run dev` (or serve the build output).
    3. Open a prolific MULTI-COUNTY WABA collector page at
       `/collectors/{login}/` (pick a collector with many specimens — e.g. inspect
       collectors.json for a high specimen_count / county_count entry).
    4. Confirm, against that collector's known records:
       - The "Active since YYYY (N seasons)" badge shows the correct earliest year
         and a plausible distinct-season count (no streak/rank wording).
       - The county coverage map fills the counties the collector actually
         contributed to (blue-tint counties match; others blank); the "N counties"
         caption matches.
       - The ecoregion coverage map fills the right ecoregions and the REQUIRED
         "N ecoregions" caption is present and correct.
       - The species list is grouped under italic genus headings, genera + species
         alphabetical, each species link navigates to a real `/species/{slug}/` page,
         and per-species counts look right.
       - At ≥768px the two maps sit side-by-side; below 768px they stack. The
         existing headline stats, status split, atlas link, and event feed are
         unchanged and still correct.
    5. Record the result (collector tested, screenshots/notes, PASS or issues) in
       `.planning/phases/172-accomplishment-view/172-HUMAN-UAT.md`.
  </how-to-verify>
  <verify>
    <human-check>Operator confirms all four accomplishment elements render and are accurate on a real collector page; documents PASS or specific issues in 172-HUMAN-UAT.md.</human-check>
  </verify>
  <acceptance_criteria>
    - 172-HUMAN-UAT.md records the collector tested and an explicit PASS (or a list
      of issues to feed a gap-closure pass).
    - All four ACCOM elements verified visually + functionally; species links resolve.
  </acceptance_criteria>
  <done>Operator records PASS (all four accomplishment elements correct) in 172-HUMAN-UAT.md, or lists specific issues for a gap-closure pass.</done>
  <resume-signal>Type "approved" (UAT pass) or describe the issues found.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator → sign-off | Human visual verification gate; no code change |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-172-UAT | Repudiation | UAT sign-off | mitigate | Result recorded in 172-HUMAN-UAT.md (collector tested + PASS/issues) for an auditable trail |
| T-172-SC | Tampering | npm/pip/cargo installs | accept | No new packages; verification only (RESEARCH Package Legitimacy Audit) |
</threat_model>

<artifacts_produced>
## Artifacts this phase produces (this plan)

- New `.planning/phases/172-accomplishment-view/172-HUMAN-UAT.md` — operator UAT sign-off record.
</artifacts_produced>

<verification>
- Blocking human checkpoint (autonomous: false) — does not auto-advance in yolo mode (feedback_uat_ui_phases).
</verification>

<success_criteria>
- Operator confirms all four accomplishment elements are correct on a real collector page; PASS recorded in 172-HUMAN-UAT.md.
</success_criteria>

<notes>
This is a verification-only plan (no implementation) so the checkpoint is isolated
from code per the "checkpoint + implementation never in the same plan" rule.
Local UAT requires regenerating the gitignored collectors.json + collector-maps/
first (project_local_uat_stale_occurrences_db); a clean checkout has neither.
</notes>

<output>
Create `.planning/phases/172-accomplishment-view/172-05-SUMMARY.md` when done.
</output>
