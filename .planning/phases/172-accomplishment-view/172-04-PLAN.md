---
phase: 172-accomplishment-view
plan: 04
type: execute
wave: 2
depends_on: [172-02, 172-03]
files_modified:
  - _pages/collector-detail.njk
  - src/styles/places.css
  - data/run.py
  - data/nightly.sh
autonomous: true
requirements: [ACCOM-01, ACCOM-02, ACCOM-03, ACCOM-04]

must_haves:
  truths:
    - "collector-detail.njk renders the 'Active since YYYY (N seasons)' badge, the county + ecoregion coverage maps, and the genus-grouped species list"
    - "each species in the list links to /species/{slug}/ — ACCOM-02 / D-04"
    - "run.py STEPS runs collector-maps after collectors-events-export"
    - "nightly.sh uploads collector-maps/ to S3 (stable URL) and invalidates /data/collector-maps/*"
    - "npm run build compiles the extended template + CSS without error"
  artifacts:
    - path: "_pages/collector-detail.njk"
      provides: "Badge + coverage-section (two maps) + species-section render"
      contains: "collector-maps"
    - path: "src/styles/places.css"
      provides: "Coverage-map sizing + species-by-genus list styles (9 selectors)"
      contains: "/collector-maps/"
    - path: "data/run.py"
      provides: "collector-maps STEPS entry"
      contains: "collector-maps"
    - path: "data/nightly.sh"
      provides: "collector-maps/ S3 cp + CloudFront invalidation"
      contains: "collector-maps"
  key_links:
    - from: "_pages/collector-detail.njk"
      to: "/data/collector-maps/{login}.svg and {login}-eco.svg"
      via: "<img loading=\"lazy\" src=...>"
      pattern: "/data/collector-maps/"
    - from: "_pages/collector-detail.njk"
      to: "/species/{slug}/"
      via: "species_by_genus link"
      pattern: "/species/"
    - from: "data/run.py"
      to: "collector_maps.generate_collector_maps_step"
      via: "STEPS entry"
      pattern: "generate_collector_maps_step"
---

<objective>
Wire Phase 172 end-to-end: render the four accomplishment elements on the
collector page, and register/deliver the new SVG generator in the pipeline.

Render (per 172-UI-SPEC, reuse-first):
- the "Active since YYYY (N seasons)" badge (ACCOM-04 / D-05),
- the two coverage maps side-by-side at ≥768px (ACCOM-01 + ACCOM-03 / D-02 + D-03),
- the genus-grouped species list linking to /species/{slug}/ (ACCOM-02 / D-04),
plus the nine `places.css` selectors from the UI-SPEC CSS Change Summary.

Wire (D-06 pipeline + D-02 delivery):
- `run.py` STEPS gains `("collector-maps", generate_collector_maps_step)` after
  `collectors-events-export`,
- `nightly.sh` uploads `collector-maps/` to S3 (stable URL) + invalidates the path.

Purpose: make the pre-aggregated data and pre-generated SVGs visible on the page
and published nightly.
Output: extended template, CSS, run.py, nightly.sh.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/172-accomplishment-view/172-CONTEXT.md
@.planning/phases/172-accomplishment-view/172-UI-SPEC.md
@.planning/phases/172-accomplishment-view/172-PATTERNS.md
@_pages/collector-detail.njk
@_pages/species-detail.njk
@src/styles/places.css
</context>

<tasks>

<task type="auto">
  <name>Task 1: Render badge, coverage maps, and species list in collector-detail.njk + places.css</name>
  <files>_pages/collector-detail.njk, src/styles/places.css</files>
  <read_first>
    - _pages/collector-detail.njk (insertion points: badge after line 15 `<p class="metadata">` and before the status-split block; the coverage + species sections after the atlas-link `{%- endif -%}` on line 24 and before the `{#- Phase 171: Event feed section -#}` comment on line 25)
    - _pages/species-detail.njk (the `<img loading="lazy" src="/data/species-maps/{{ sp.slug }}.svg">` embed pattern + quantify usage)
    - src/styles/places.css (existing `.places-page img[src*="/place-maps/"]`, `.event-feed-section` margin, `.places-list li` patterns to mirror)
    - src/lib/quantify.js (the `quantify(noun)` / `quantify(noun, plural)` filter API)
    - .planning/phases/172-accomplishment-view/172-UI-SPEC.md (Element-by-Element Contract, Page Layout Contract, CSS Change Summary — the 9 selectors, locked copy, locked alt-text)
    - .planning/phases/172-accomplishment-view/172-PATTERNS.md (the exact Nunjucks blocks for badge / coverage-section / species-section, and the CSS block)
  </read_first>
  <action>
    In _pages/collector-detail.njk: insert the badge `<p class="metadata">Active
    since {{ collector.active_since }} ({{ collector.seasons_count | quantify("season") }})</p>`
    guarded by `{%- if collector.active_since -%}`, placed after the existing
    specimen/sample/species metadata line and before the status-split block (locked
    copy D-05 — do NOT substitute "years" for "seasons", no streak/rank language).

    After the atlas-link block (before the event-feed comment) insert
    `<section class="coverage-section">` containing a `<div class="coverage-maps">`
    with two `<div class="map-block">` entries: the county map `<img loading="lazy"
    src="/data/collector-maps/{{ collector.login }}.svg" alt="County coverage map for
    {{ collector.display_name }} — {{ collector.county_count | quantify('county','counties') }}">`
    + a `<p class="metadata">{{ collector.county_count | quantify("county","counties") }}</p>`
    caption (guarded `if collector.county_count > 0`), and the ecoregion map
    `{{ collector.login }}-eco.svg` with the parallel alt text + a REQUIRED
    `{{ collector.ecoregion_count | quantify("ecoregion") }}` caption (guarded
    `if collector.ecoregion_count > 0`); wrap the section in
    `{%- if collector.county_count > 0 or collector.ecoregion_count > 0 -%}`.

    Then insert `<section class="species-section">` with an `<h2>Species collected</h2>`
    and, for each `genus_group` in `collector.species_by_genus`, an
    `<h3 class="genus-heading"><em>{{ genus_group.genus }}</em></h3>` and a
    `<ul class="species-list">` of `<li><a href="/species/{{ sp.slug }}/">{{ sp.canonical_name }}</a>
    <span class="count">({{ sp.count }})</span></li>`, guarded by
    `{%- if collector.species_by_genus and collector.species_by_genus.length > 0 -%}`.

    In src/styles/places.css append the nine Phase-172 selectors from the UI-SPEC CSS
    Change Summary (clearly commented `/* Phase 172 */`): `.places-page
    img[src*="/collector-maps/"]` (aspect-ratio 15/8, width 100%, max-width 600px),
    `.coverage-section` (margin-top 1.5rem), `.coverage-maps` (flex column; grid
    1fr 1fr at min-width 768px), `.species-section` (margin-top 1.5rem),
    `.genus-section` (margin-bottom 1rem), `.genus-heading` (1rem, 700, italic,
    --text-body), `.species-section .species-list` (reset), `.species-section
    .species-list li` (flex baseline 8px gap 4px pad bottom-border), and
    `.species-section .species-list .count` (margin-left auto, 0.85rem, --text-muted).
    Do NOT load taxon-pages.css on the collector page (UI-SPEC).
  </action>
  <verify>
    <automated>npm run build 2>&1 | tail -20 && npm test -- data-collectors</automated>
  </verify>
  <acceptance_criteria>
    - `npm run build` completes without template/CSS errors (tsc --noEmit + Eleventy + Vite).
    - `npm test` still GREEN (no regression; the Phase 172 fixture assertions pass).
    - collector-detail.njk references `/data/collector-maps/{{ collector.login }}.svg`
      and `{{ collector.login }}-eco.svg` and links species to `/species/{{ sp.slug }}/`.
    - Badge copy is exactly "Active since {{ collector.active_since }} ({{ collector.seasons_count | quantify("season") }})".
    - places.css contains all nine Phase-172 selectors incl. `img[src*="/collector-maps/"]`.
  </acceptance_criteria>
  <done>The collector page renders the badge, two coverage maps, and the genus-grouped species list; build + tests green.</done>
</task>

<task type="auto">
  <name>Task 2: Register the collector-maps step in run.py + deliver via nightly.sh</name>
  <files>data/run.py, data/nightly.sh</files>
  <read_first>
    - data/run.py (the import block lines 31-52, the STEPS list lines 91-132 — insert after `("collectors-events-export", export_collectors_events_step)` and before `("places-maps", ...)`)
    - data/nightly.sh (the stable-URL S3 cp block lines 348-352 and the CloudFront invalidation lines 358-361)
    - .planning/phases/172-accomplishment-view/172-PATTERNS.md (the run.py + nightly.sh excerpts)
    - .planning/phases/172-accomplishment-view/172-RESEARCH.md (System Architecture Diagram; nightly.sh S3 upload pattern)
  </read_first>
  <action>
    In data/run.py add `from collector_maps import generate_collector_maps_step`
    alongside the other step imports, and insert
    `("collector-maps", generate_collector_maps_step)` into STEPS immediately after
    `("collectors-events-export", export_collectors_events_step)` and before
    `("places-maps", generate_place_maps_step)`. Update the module-docstring STEPS
    order comment to mention collector-maps. (run.py stays pure — no S3/git logic,
    per CLAUDE.md Known State.)

    In data/nightly.sh add, alongside the existing species-maps/place-maps lines, a
    recursive stable-URL upload:
    `aws --profile "$AWS_PROFILE" s3 cp --recursive --no-progress "$EXPORT_DIR/collector-maps/" "s3://$BUCKET/data/collector-maps/"`,
    and append `"/data/collector-maps/*"` to the CloudFront `create-invalidation`
    `--paths` list. Update the preceding comment to include collector-maps. Do NOT
    add collector-maps to manifest.json and do NOT add a deploy.yml fetch — these
    SVGs are stable-URL runtime `<img>` sources served directly from CloudFront,
    exactly like species-maps/ (D-02).
  </action>
  <verify>
    <automated>cd data && uv run pytest -m "not integration" -q 2>&1 | tail -5 && cd /Users/rainhead/dev/beeatlas && grep -v '^#' data/run.py | grep -c "generate_collector_maps_step" && grep -v '^#' data/nightly.sh | grep -c "collector-maps"</automated>
  </verify>
  <acceptance_criteria>
    - `cd data && uv run pytest -m "not integration"` GREEN — run.py imports cleanly
      (a broken `collector_maps` import would surface here).
    - run.py STEPS contains `("collector-maps", generate_collector_maps_step)` between
      collectors-events-export and places-maps; the import line is present.
    - nightly.sh (non-comment lines) contains the `collector-maps/` S3 `cp` line AND
      `/data/collector-maps/*` in the CloudFront invalidation `--paths`.
    - No `collector-maps` entry added to the manifest.json block; no deploy.yml change.
  </acceptance_criteria>
  <done>run.py runs collector-maps in the nightly sequence; nightly.sh publishes the SVGs to the stable S3 path + invalidates them.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| collectors.json → rendered HTML | Pipeline data (display_name, slug, counts) interpolated into the static page |
| EXPORT_DIR/collector-maps → S3/CloudFront | Pre-generated SVGs published to a public stable URL |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-172-XSS | Tampering / Information Disclosure | collector-detail.njk interpolation of display_name / canonical_name / slug | accept | Nunjucks auto-escapes by default; all values come from trusted pipeline parquet, no user input; slug/login already used safely in existing page links |
| T-172-PATH | Tampering | `<img src>` + S3 path uses {login} | mitigate | Login filename safety is enforced upstream in collector_maps.py (Plan 03 T-172-PATH); the template reuses the same login already used by the existing `permalink` urlencode |
| T-172-SC | Tampering | npm/pip/cargo installs | accept | No new packages; template + CSS + shell changes only (RESEARCH Package Legitimacy Audit) |
</threat_model>

<artifacts_produced>
## Artifacts this phase produces (this plan)

- Modified `_pages/collector-detail.njk` — new badge `<p class="metadata">`, `<section class="coverage-section">` (two `<img>` maps + captions), `<section class="species-section">` (genus-grouped, slug-linked list).
- Modified `src/styles/places.css` — 9 new selectors: `.places-page img[src*="/collector-maps/"]`, `.coverage-section`, `.coverage-maps` (+ ≥768px grid), `.species-section`, `.genus-section`, `.genus-heading`, `.species-section .species-list`, `.species-section .species-list li`, `.species-section .species-list .count`.
- Modified `data/run.py` — new import `generate_collector_maps_step` + new STEPS entry `("collector-maps", generate_collector_maps_step)`.
- Modified `data/nightly.sh` — new `collector-maps/` recursive S3 `cp` + `/data/collector-maps/*` CloudFront invalidation path.
</artifacts_produced>

<verification>
- `npm run build` GREEN (template + CSS compile) and `npm test` GREEN.
- `cd data && uv run pytest -m "not integration"` GREEN (run.py import-clean).
- Wave-2 gate: both suites GREEN (feedback_run_tests_before_push).
</verification>

<success_criteria>
- Badge, two coverage maps, and genus-grouped species list render with locked copy + alt text.
- Species links target /species/{slug}/; maps target /data/collector-maps/{login}(-eco).svg.
- collector-maps registered in run.py STEPS and delivered via nightly.sh stable-URL S3 + CloudFront.
- No manifest/deploy.yml change for the SVGs.
</success_criteria>

<notes>
**No data-before-code deadlock:** unlike a dbt contract bump, the template guards
every new section on `collector.*` fields, so a build against an older
collectors.json (pre-Plan-02 fields absent) simply omits the sections — no error.
The export (county_count/ecoregion_count/species_by_genus) and the SVGs are produced
in the SAME nightly run.py pass, so they land in S3 together; the post-nightly
deploy then fetches the fresh collectors.json (Phase 171.1 manifest path) while the
SVGs are already on CloudFront. Operator visual confirmation is Plan 05.
</notes>

<output>
Create `.planning/phases/172-accomplishment-view/172-04-SUMMARY.md` when done.
</output>
