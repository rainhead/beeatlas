---
phase: 172-accomplishment-view
plan: 02
type: execute
wave: 1
depends_on: [172-01]
files_modified:
  - data/collectors_export.py
autonomous: true
requirements: [ACCOM-01, ACCOM-02, ACCOM-03, ACCOM-04]

must_haves:
  truths:
    - "collectors.json entries carry active_since (MIN year) and seasons_count (COUNT DISTINCT year) — D-05"
    - "collectors.json entries carry county_count and ecoregion_count (COUNT DISTINCT over D-01 rows) — ACCOM-01/03 captions"
    - "collectors.json entries carry species_by_genus: a genus-grouped, alphabetically-ordered species list with slug links (species-rank only) — D-04"
    - "every new aggregation uses the D-01 WABA-contribution predicate verbatim"
  artifacts:
    - path: "data/collectors_export.py"
      provides: "Extended _QUERY (badge + caption counts) and a companion _SPECIES_QUERY with genus grouping"
      contains: "_SPECIES_QUERY"
  key_links:
    - from: "data/collectors_export.py"
      to: "occurrences.parquet"
      via: "read_parquet(?) o — year/county/ecoregion_l3 under the D-01 predicate"
      pattern: "ecdysis_id IS NOT NULL OR o.record_type IN"
    - from: "data/collectors_export.py"
      to: "species.parquet"
      via: "LEFT JOIN read_parquet(?) sp ON sp.taxon_id = o.taxon_id (genus/slug/specific_epithet)"
      pattern: "sp.taxon_id = o.taxon_id"
---

<objective>
Extend `data/collectors_export.py` so `collectors.json` carries all four
pre-aggregated accomplishment payloads (D-06: no browser GROUP BY):
- the active-seasons badge (`active_since`, `seasons_count` — ACCOM-04 / D-05),
- the county/ecoregion caption counts (`county_count`, `ecoregion_count` —
  ACCOM-01/03 captions),
- the genus-grouped species list (`species_by_genus` — ACCOM-02 / D-04).

This plan turns the RED `test_collectors_export.py` tests from Plan 01 GREEN.

Purpose: pre-compute every collector-page accomplishment number/list in the
pipeline so the static page renders without any client-side aggregation.
Output: extended `collectors_export.py`; regenerated `collectors.json` shape.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/172-accomplishment-view/172-CONTEXT.md
@.planning/phases/172-accomplishment-view/172-RESEARCH.md
@.planning/phases/172-accomplishment-view/172-PATTERNS.md
@data/collectors_export.py
@data/tests/test_collectors_export.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add badge + caption-count aggregates to _QUERY</name>
  <files>data/collectors_export.py</files>
  <read_first>
    - data/collectors_export.py (the `_QUERY` SELECT lines 33-73, the D-01 WHERE lines 69-70, the row unpacking lines 119-143)
    - data/dbt/models/marts/occurrences.sql (confirms `year`, `county`, `ecoregion_l3` columns exist on the mart — lines ~87-94)
    - data/tests/test_collectors_export.py (the Plan-01 RED tests this turns GREEN: badge / seasons_distinct / active_since / county_and_ecoregion)
    - .planning/phases/172-accomplishment-view/172-PATTERNS.md (section "data/collectors_export.py (MODIFY)" — exact SELECT additions + row unpacking)
  </read_first>
  <behavior>
    - active_since == MIN(year) over the collector's D-01 rows (alice {2020,2022} → 2020).
    - seasons_count == COUNT(DISTINCT year), NOT a max−min+1 span (alice → 2, not 3) — D-05.
    - county_count == COUNT(DISTINCT county) FILTER (county IS NOT NULL) over D-01 rows.
    - ecoregion_count == COUNT(DISTINCT ecoregion_l3) FILTER (ecoregion_l3 IS NOT NULL).
    - The D-01 WABA predicate is unchanged — no second scope introduced.
  </behavior>
  <action>
    Insert four aggregate columns into the existing `_QUERY` SELECT (after
    `status_awaiting`, before `GROUP BY`), all computed over the SAME existing D-01
    WHERE predicate (D-01): `MIN(o.year) AS active_since`,
    `COUNT(DISTINCT o.year) AS seasons_count`,
    `COUNT(DISTINCT o.county) FILTER (WHERE o.county IS NOT NULL) AS county_count`,
    `COUNT(DISTINCT o.ecoregion_l3) FILTER (WHERE o.ecoregion_l3 IS NOT NULL) AS ecoregion_count`.

    Use `MIN(o.year)` directly for active_since — a NULL-year row is correctly
    ignored by MIN. Do NOT write `MIN(COALESCE(o.year, <fallback>))`: per memory
    feedback_min_coalesce_aggregation, a fallback inside MIN can win the aggregate
    and mask the real earliest year. Handle a None active_since in Python.

    Extend the tuple unpacking in `export_collectors` to receive the four new
    columns, and add to the per-record dict: `"active_since": int(active_since) if
    active_since is not None else None`, `"seasons_count": int(seasons_count)`,
    `"county_count": int(county_count)`, `"ecoregion_count": int(ecoregion_count)`.
  </action>
  <verify>
    <automated>cd data && uv run pytest tests/test_collectors_export.py -x -k "badge or seasons or active_since or county_and_ecoregion"</automated>
  </verify>
  <acceptance_criteria>
    - The badge / seasons_distinct / active_since / county_and_ecoregion tests PASS.
    - The `_QUERY` WHERE predicate string is byte-unchanged from the D-01 predicate
      (grep confirms `ecdysis_id IS NOT NULL OR o.record_type IN ('waba_specimen', 'provisional_sample')` still present, exactly once, as the only WHERE scope).
    - No `MIN(COALESCE` appears anywhere in collectors_export.py.
  </acceptance_criteria>
  <done>Badge + caption-count fields are emitted into collectors.json; their tests pass.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add _SPECIES_QUERY + species_by_genus grouping</name>
  <files>data/collectors_export.py</files>
  <read_first>
    - data/collectors_export.py (the existing JOIN to species.parquet `sp.taxon_id = o.taxon_id`, the records-append block, the file-existence guards lines 104-111)
    - data/species_export.py (confirms species.parquet carries `genus`, `canonical_name`, `slug`, `specific_epithet`, `taxon_id`; slug format "Genus/epithet" lines 226-236)
    - data/tests/test_collectors_export.py (`test_species_by_genus_structure` — the contract this satisfies)
    - .planning/phases/172-accomplishment-view/172-PATTERNS.md (Pattern 3 + "Species list grouping" excerpt)
  </read_first>
  <behavior>
    - species_by_genus is a list of {genus, species:[{canonical_name, slug, count}]}.
    - Only species-rank determinations appear (sp.specific_epithet IS NOT NULL) — D-04.
    - Genera are alphabetical; species are alphabetical within each genus — D-04.
    - count is the per-species occurrence count (int) over the collector's D-01 rows.
    - Collectors with no species-rank determinations get an empty species_by_genus list.
  </behavior>
  <action>
    Add a module-level `_SPECIES_QUERY` constant: SELECT
    `o.collector_inat_login AS login, sp.genus, sp.canonical_name, sp.slug,
    COUNT(*) AS occ_count` FROM `read_parquet(?) o LEFT JOIN read_parquet(?) sp ON
    sp.taxon_id = o.taxon_id`, WHERE the D-01 predicate (verbatim) AND
    `sp.specific_epithet IS NOT NULL` (D-04 species-rank gate), GROUP BY
    login/genus/canonical_name/slug, ORDER BY login, genus, canonical_name.

    In `export_collectors`, after the main `_QUERY` rows are processed, run
    `_SPECIES_QUERY` with the same `[str(occ_parquet), str(species_parquet)]` params.
    Group results into login → genus → ordered list of {canonical_name, slug,
    count:int(occ_count)} (a defaultdict preserving the SQL ORDER BY insertion order),
    then attach to each record as
    `rec["species_by_genus"] = [{"genus": g, "species": lst} for g, lst in sorted(genus_dict.items())]`.
    Collectors with no species rows get `[]`.
  </action>
  <verify>
    <automated>cd data && uv run pytest tests/test_collectors_export.py -x</automated>
  </verify>
  <acceptance_criteria>
    - `test_species_by_genus_structure` PASSES and the full test_collectors_export.py suite is GREEN (including all pre-existing tests).
    - `_SPECIES_QUERY` contains both `sp.specific_epithet IS NOT NULL` and the D-01 predicate.
    - species_by_genus genera are emitted in sorted order (the grouping uses `sorted(...)`).
  </acceptance_criteria>
  <done>collectors.json carries species_by_genus per D-04; test_collectors_export.py is fully GREEN.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| EXPORT_DIR parquet → collectors.json | Pipeline-written, trusted parquet aggregated into a static JSON build input |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-172-SQL | Tampering | DuckDB read_parquet path params | accept | Parquet paths are bound parameters (`?`) from ASSETS_DIR, not string-interpolated; no external input reaches SQL |
| T-172-SCOPE | Information Disclosure | aggregation row scope | mitigate | Every new aggregate reuses the D-01 WABA predicate verbatim — no casual-observer rows leak into accomplishment counts (CONTEXT D-01) |
| T-172-SC | Tampering | npm/pip/cargo installs | accept | No new packages (RESEARCH Package Legitimacy Audit: stdlib + existing duckdb only) |
</threat_model>

<artifacts_produced>
## Artifacts this phase produces (this plan)

- Modified `data/collectors_export.py`:
  - `_QUERY` gains SELECT columns `active_since`, `seasons_count`, `county_count`, `ecoregion_count`.
  - New module-level constant `_SPECIES_QUERY`.
  - `export_collectors` row unpacking + records dict gain `active_since`, `seasons_count`, `county_count`, `ecoregion_count`, `species_by_genus`.
- New `collectors.json` fields (data contract, regenerated by the pipeline; the file itself is gitignored/S3-delivered): `active_since`, `seasons_count`, `county_count`, `ecoregion_count`, `species_by_genus`.
</artifacts_produced>

<verification>
- `cd data && uv run pytest tests/test_collectors_export.py` is fully GREEN.
- Wave 1 gate (run with Plan 03): `npm test` GREEN AND `cd data && uv run pytest -m "not integration"` GREEN (per feedback_run_tests_before_push — both suites run because this phase touches data/ and frontend).
</verification>

<success_criteria>
- All five new fields present in every collectors.json record with correct types.
- active_since == MIN(year); seasons_count == COUNT(DISTINCT year) (gap years excluded).
- species_by_genus is species-rank-only, genus- and species-alphabetical, slug-linked.
- D-01 predicate reused verbatim; no MIN(COALESCE()) anti-pattern.
</success_criteria>

<notes>
This plan modifies only `data/collectors_export.py` (no file overlap with Plan 03's
`data/collector_maps.py`) — the two Wave 1 plans are independent. The export reads
`occurrences.parquet` + `species.parquet` from EXPORT_DIR (D-06 / Pitfall 5), never
the dbt sandbox.
</notes>

<output>
Create `.planning/phases/172-accomplishment-view/172-02-SUMMARY.md` when done.
</output>
