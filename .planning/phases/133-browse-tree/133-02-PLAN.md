---
phase: 133-browse-tree
plan: 02
type: execute
wave: 2
depends_on: ["133-01"]
files_modified:
  - _pages/species.njk
  - src/tests/species-index.test.ts
autonomous: true
requirements: [TREE-01, TREE-02, TREE-04]
must_haves:
  truths:
    - "Visiting /species renders a nested <details>/<summary> tree of every bee taxon"
    - "Each node summary shows the name, the specimen·observation count split, and a 🗺 map affordance"
    - "Page-backed ranks (genus/subgenus/tribe/subfamily/species) link the name to the taxon page; family name is plain text"
    - "Intermediate-rank nodes (subfamily/tribe/subgenus) render with the hidden attribute by default; a control bar with filter input + 'Show all ranks' toggle sits above the tree"
  artifacts:
    - path: "_pages/species.njk"
      provides: "Build-time tree markup walking species.fullTree"
      contains: "tree-node"
    - path: "src/tests/species-index.test.ts"
      provides: "Source assertions for the new tree template markup"
      contains: "tree-node"
  key_links:
    - from: "_pages/species.njk"
      to: "species.fullTree"
      via: "Nunjucks recursive macro / walk"
      pattern: "fullTree"
    - from: "node-map link"
      to: "/?taxon=…&taxonRank=…"
      via: "Phase 130 map URL contract (read-only consumer)"
      pattern: "taxonRank="
---

<objective>
Replace the flat family→genus→species index in `_pages/species.njk` with the
expandable, bee-only `<details>/<summary>` tree, walking the `species.fullTree`
shape produced in Plan 01. Render per-node count splits and the dual link scheme
(name → taxon page; secondary 🗺 → descendant-filtered map). This is a static,
build-time DOM that works with zero JS (D-01, D-02).

Purpose: D-01 (full build-time DOM), D-02 (native details/summary), D-06 (name→page +
🗺→map), D-07 (family = plain text), D-08 (count split), and the full UI-SPEC markup
contract (D-11). Implements TREE-01 (default depth + deeper ranks present),
TREE-02 (count split per node), TREE-04 (bee-only nodes link to page and/or map).
Output: rewritten template + source-assertion tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/133-browse-tree/133-CONTEXT.md
@.planning/phases/133-browse-tree/133-PATTERNS.md
@.planning/phases/133-browse-tree/133-UI-SPEC.md
@CLAUDE.md

<interfaces>
<!-- Consumes species.fullTree from Plan 01 (133-01-SUMMARY.md). Node shape:
  { rank, name, taxon_id, specimen_count, inat_obs_count, occurrence_count, slug?, scientificName?, children: [] }
  rank ∈ { family, subfamily, tribe, genus, subgenus, species }.

  URL forms (133-PATTERNS.md §species.njk table; all built in Phase 132):
    species   → /species/{slug}/
    genus     → /species/{name}/
    subgenus  → /species/{genusName}/{name}/   (genus parent needed — carry it on the node or via ancestry)
    tribe     → /species/tribe/{name}/
    subfamily → /species/subfamily/{name}/
    family    → (no page — plain text, D-07)
  Map affordance (all ranks): /?taxon={name|urlencode}&amp;taxonRank={rank}
  (species uses scientificName | urlencode per species-detail.njk line 46.)

  Count format (UI-SPEC §Copywriting): compact `{specimen_count} · {inat_obs_count}`,
  U+00B7 middle dot, NO unit labels on the node summary. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wave 0 — tree template source assertions in species-index.test.ts</name>
  <files>src/tests/species-index.test.ts</files>
  <read_first>
    - src/tests/species-index.test.ts (existing — UPDATE the stale Phase 96 assertions that expect the OLD flat markup, e.g. groupby("family")/groupby("genus") and the `.family-section`/`.genus-row` expectations; replace with tree-markup assertions. Keep the layout/permalink and script-tag assertions.)
    - _pages/species.njk (current flat template being replaced)
    - .planning/phases/133-browse-tree/133-PATTERNS.md §_pages/species.njk (control bar, details/summary markup, URL table, count form)
    - .planning/phases/133-browse-tree/133-UI-SPEC.md §Component Inventory, §Copywriting Contract, §Accessibility Contract
  </read_first>
  <action>
    Update src/tests/species-index.test.ts so the `_pages/species.njk` describe block
    asserts the NEW tree markup (readFileSync source assertions, consistent with the
    file's existing style). Keep the passing layout/permalink and
    `src/entries/species-index.ts` script-tag assertions. Add/adjust assertions:
    - Frontmatter still declares `layout: default.njk` and `permalink: /species/index.html`.
    - Contains `class="taxon-page species-index"` wrapper (unchanged dual class).
    - Control bar: contains `id="species-filter"` with `type="search"` and
      `aria-label="Filter taxa"` (UPDATED copy) and `placeholder="Filter taxa…"`;
      contains `id="show-all-ranks"` checkbox; contains `class="species-index-controls"`.
    - Empty-state `<p id="filter-empty"` carries `hidden` and `No taxa match` and a
      `id="filter-query"` span, plus `role="status"` (or aria-live) per UI-SPEC.
    - Tree node markup: contains `details class="tree-node` and `<summary>`, the
      `node-name`, `node-counts`, and `node-map` classes, and `data-rank=` attributes.
    - Count separator: source contains the middle dot ` · ` (U+00B7) in a node-counts span.
    - Map affordance: contains `taxonRank=` and `aria-label="Map:` for the 🗺 link, and
      the 🗺 glyph (U+1F5FA).
    - Family is plain text: assert a family-rank summary uses `class="node-name"` on a
      `<span>` (no `<a>`) — assert the template branches name-as-link vs name-as-span on rank.
    - Removed: assert the source NO LONGER contains `groupby("family")`,
      `.family-section`, or the old `aria-label="Filter genera and species"`.
    These run with `VITEST_SKIP_BUILD=1`; they are RED until Task 2 rewrites the template.
  </action>
  <verify>
    <automated>VITEST_SKIP_BUILD=1 npx vitest run src/tests/species-index.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - Test file references `tree-node`, `node-counts`, `node-map`, `data-rank`, `show-all-ranks`, `Filter taxa`.
    - Stale flat-markup assertions (groupby/.family-section/old aria-label) are gone or inverted to `.not`.
    - Tests FAIL before Task 2 and pass after.
  </acceptance_criteria>
  <done>Updated source-assertion suite encodes the tree markup contract and is RED pending the template rewrite.</done>
</task>

<task type="auto">
  <name>Task 2: Rewrite _pages/species.njk as the expandable tree</name>
  <files>_pages/species.njk</files>
  <read_first>
    - _pages/species.njk (current flat template — replace body, keep frontmatter + script tag)
    - _pages/subfamily.njk (PRIMARY analog — nested details/count split/tribe-less fallback)
    - _pages/genus.njk, _pages/subgenus.njk, _pages/tribe.njk, _pages/species-detail.njk (URL forms + count convention; species-detail line ~46 for the 🗺 /?taxon=…&taxonRank=species form)
    - .planning/phases/133-browse-tree/133-PATTERNS.md §_pages/species.njk (control bar, details/summary blocks, URL table, count form, hidden-attribute rule)
    - .planning/phases/133-browse-tree/133-UI-SPEC.md (§Component Inventory, §Node layout within summary, §Visual distinction, §Accessibility Contract, §Copywriting Contract, §No-JS Fallback)
    - 133-01-SUMMARY.md (the exact fullTree node shape delivered by Plan 01)
  </read_first>
  <action>
    Rewrite the `<article class="taxon-page species-index">` body to render
    `species.fullTree`. Keep the frontmatter (layout/permalink/title) and the trailing
    `<script type="module" src="/src/entries/species-index.ts">` tag unchanged.

    Structure:
    - `<h1>Species</h1>` (unchanged heading copy).
    - Control bar `<div class="species-index-controls">` containing the `#species-filter`
      search input (`aria-label="Filter taxa"`, `placeholder="Filter taxa…"`,
      `autocomplete="off"`) and a `<label class="rank-toggle-label">` wrapping
      `<input type="checkbox" id="show-all-ranks">` + text "Show all ranks". Optionally
      add the `#rank-toggle-hint` paragraph wired via `aria-describedby` per UI-SPEC.
    - `<p id="filter-empty" hidden role="status">No taxa match "<span id="filter-query"></span>".</p>`
    - The tree: recurse `fullTree` with a Nunjucks `{% macro renderNode(node) %}` (or an
      include) emitting per node:
        `<details class="tree-node tree-node--{{ node.rank }}" data-rank="{{ node.rank }}"
          data-name="{{ node.name | lower }}"{% if node.rank in ['subfamily','tribe','subgenus'] %} hidden{% endif %}>`
        then a `<summary>` flex row: name element + `<span class="node-counts">{{ node.specimen_count }} · {{ node.inat_obs_count }}</span>`
        + `<a class="node-map" href="/?taxon={{ node.name | urlencode }}&amp;taxonRank={{ node.rank }}" aria-label="Map: {{ node.name }} occurrences">🗺</a>`.
      Name element by rank (D-06/D-07):
        - family: `<span class="node-name">{{ node.name }}</span>` (plain text, no link, no italic).
        - genus: `<a class="node-name" href="/species/{{ node.name }}/"><em>{{ node.name }}</em></a>`.
        - subgenus: `<a class="node-name" href="/species/{{ node.genusName }}/{{ node.name }}/"><em>{{ node.name }}</em></a>` (use the genus parent carried on the node; if not carried, attach it in Plan 01 — coordinate via SUMMARY).
        - tribe: `href="/species/tribe/{{ node.name }}/"`.
        - subfamily: `href="/species/subfamily/{{ node.name }}/"`.
      Then recurse children; species leaves render as `<li data-rank="species"
      data-name="{{ node.scientificName | lower }}">` inside a `<ul class="species-list">`
      with `<a class="node-name" href="/species/{{ node.slug }}/"><em>{{ node.scientificName }}</em></a>`,
      the node-counts span, and the species 🗺 link using `node.scientificName | urlencode`.
    - Use `&amp;` in href literals (Nunjucks autoescape leaves `&amp;` as written; match
      species-detail.njk). Do NOT use the `| safe` filter anywhere — all names go through
      autoescaping.
    - Map-link for ranks WITHOUT a page (family) still emitted (D-07: family gets 🗺 only).
    Set NO `open` attribute in HTML (JS handles auto-expand in Plan 04). Intermediate
    ranks get the `hidden` attribute (not display:none) so the no-JS fallback shows them.
  </action>
  <verify>
    <automated>VITEST_SKIP_BUILD=1 npx vitest run src/tests/species-index.test.ts && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `npm run build` succeeds (tsc --noEmit → eleventy + Vite); /species/index.html renders the tree DOM.
    - Built page contains `details class="tree-node`, `node-counts` spans with ` · `, `node-map` 🗺 links with `taxonRank=`.
    - Family summary uses `<span class="node-name">` (no `<a>`); genus/subgenus/tribe/subfamily/species use `<a class="node-name">`.
    - subfamily/tribe/subgenus `<details>` carry the `hidden` attribute; family/genus do not.
    - No `| safe` filter present; all `/?taxon=` hrefs use `| urlencode` on the name and `&amp;`.
    - All species-index.test.ts assertions green; no old flat markup remains.
  </acceptance_criteria>
  <done>/species renders the full bee-only details/summary tree with counts, dual links, family-as-plain-text, and hidden intermediate ranks — working with zero JS.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| build data → rendered HTML | Taxon names/counts from the (trusted, bee-only) feed are interpolated into static HTML. The only injection surface is template interpolation. |
| node-map href → map app | The 🗺 link constructs `/?taxon=<name>&taxonRank=<rank>`; names may contain spaces/periods/special chars. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-133-03 | Tampering (XSS) | taxon names in `<summary>`/`<li>` text and aria-labels | mitigate | Rely on Nunjucks autoescaping (default ON in Eleventy); forbid the `\| safe` filter; acceptance criterion asserts no `\| safe`. |
| T-133-04 | Tampering (URL injection) | `/?taxon=` href on the 🗺 link | mitigate | Apply `\| urlencode` to the taxon name in every map href; keep `&amp;` literal so the attribute parses as a single URL. |
| T-133-05 | Information disclosure | bycatch in rendered tree | mitigate | Template walks `fullTree` only (bee-only by construction in Plan 01); no separate data source introduced here. |
| T-133-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this plan; nothing to gate. |
</threat_model>

<verification>
- `npm run build` green; open `_site/species/index.html` and confirm tree nodes, counts, 🗺 links.
- `grep -c '| safe' _pages/species.njk` is 0.
- `grep -c 'taxonRank=' _pages/species.njk` is > 0.
</verification>

<success_criteria>
- Expandable bee-only tree renders at build time (TREE-01) with per-node count splits
  (TREE-02) and name→page + 🗺→map links (TREE-04), family as plain text (D-07),
  intermediate ranks hidden by default (D-03 markup), zero `| safe`, all hrefs encoded.
- species-index.test.ts green; npm build green.
</success_criteria>

<output>
Create `.planning/phases/133-browse-tree/133-02-SUMMARY.md` when done
</output>
