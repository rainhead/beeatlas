---
phase: 179-notes-feature-harvest-build-time-bake
plan: 04
type: execute
wave: 3
depends_on: [179-03]
files_modified:
  - src/lib/formatDate.js
  - eleventy.config.js
  - _pages/species-detail.njk
  - src/styles/taxon-pages.css
  - src/tests/formatDate.test.ts
autonomous: true
requirements: [NOTES-03]
user_setup: []

must_haves:
  truths:
    - "A species with baked notes renders a static <section class='notes-section'> stacked newest-first, each note's byline linking to /collectors/<login>/ when a collector page exists (else plain text), with no runtime API call on page load"
    - "A species with zero notes omits the section entirely (graceful empty state)"
    - "note.html is injected via `| safe` — the single trusted-HTML point — never re-escaped or re-sanitized client-side"
    - "The <bee-notes> mount element + data-handoff script are always emitted (even with zero baked notes) so the island can hydrate for an author"
  artifacts:
    - path: "src/lib/formatDate.js"
      provides: "formatDate(iso) -> 'Jul 4, 2026' (Intl.DateTimeFormat en-US short/numeric/numeric)"
    - path: "_pages/species-detail.njk"
      provides: "baked notes section + bee-notes mount + handoff script"
      contains: "notesForSpecies"
    - path: "src/styles/taxon-pages.css"
      provides: "Phase 179 .notes-section/.note/.note-body/.note-meta/.note-btn CSS"
  key_links:
    - from: "_pages/species-detail.njk"
      to: "notes[sp.canonical_name]"
      via: "Eleventy _data/notes.js loader"
      pattern: "notes\\[sp.canonical_name\\]"
    - from: "eleventy.config.js"
      to: "src/lib/formatDate.js"
      via: "addFilter('formatDate', formatDate)"
      pattern: "formatDate"
---

<objective>
Render the baked, static, offline-safe notes list on species pages (NOTES-03): a
Nunjucks `<section class="notes-section">` mirroring the `.collected-from` block,
newest-first, attributed byline (linked to the collector page when present), a
graceful empty state (section omitted when zero notes), plus the always-present
`<bee-notes>` mount + data-handoff script the 179-05 island hydrates over. Add the
`formatDate` Eleventy filter (none exists) and the Phase-179 CSS.

Purpose: This is the read path's source of truth — 100% static, no runtime API call
on page load. The island is pure enhancement layered on top of this. The `| safe`
injection of the server-sanitized `note.html` is the single trusted-HTML boundary
(D-04) — the content was already sanitized on write (179-01/179-02).
Output: `formatDate` filter, the njk notes section + mount, and the CSS block.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/179-notes-feature-harvest-build-time-bake/179-UI-SPEC.md
@.planning/phases/179-notes-feature-harvest-build-time-bake/179-PATTERNS.md
@.planning/phases/179-notes-feature-harvest-build-time-bake/179-CONTEXT.md

@_pages/species-detail.njk
@eleventy.config.js
@src/styles/taxon-pages.css
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: formatDate Eleventy filter</name>
  <read_first>
    - eleventy.config.js (the `import { quantify } from "./src/lib/quantify.js"` + `eleventyConfig.addFilter("quantify", quantify)` registration — the exact pattern to mirror)
    - src/lib/quantify.js (the sibling util module shape to match)
    - 179-UI-SPEC.md "Timestamp format" row + "Layout Contract" note that no date filter exists yet
  </read_first>
  <behavior>
    - formatDate('2026-07-04T17:31:14.339Z') === 'Jul 4, 2026'
    - formatDate('2026-01-09') === 'Jan 9, 2026'
    - formatDate('') and formatDate(undefined) return '' (no throw) — a missing timestamp must never crash the build
  </behavior>
  <files>src/lib/formatDate.js, eleventy.config.js, src/tests/formatDate.test.ts</files>
  <action>
    Create `src/lib/formatDate.js` exporting `formatDate(iso)` that returns
    `new Intl.DateTimeFormat('en-US', { month: 'short', day: 'numeric', year: 'numeric' }).format(new Date(iso))`
    ("Jul 4, 2026"), returning '' for empty/invalid input (guard `!iso` and `isNaN(date)`). Register it in
    `eleventy.config.js` exactly like `quantify`: import from "./src/lib/formatDate.js" and call
    `eleventyConfig.addFilter("formatDate", formatDate)`. This same formatter is reused verbatim by the 179-05
    island (imported from this .js) so baked and live timestamps never diverge.
    Create `src/tests/formatDate.test.ts` covering the <behavior>.
  </action>
  <verify>
    <automated>npm test -- formatDate</automated>
  </verify>
  <acceptance_criteria>
    - `src/lib/formatDate.js` exports `formatDate` using Intl.DateTimeFormat en-US month:'short'/day:'numeric'/year:'numeric'
    - `eleventy.config.js` imports and registers the `formatDate` filter alongside `quantify`
    - `npm test -- formatDate` passes including the empty/invalid-input guard
  </acceptance_criteria>
  <done>A shared formatDate filter renders "Jul 4, 2026" identically at build time and in the island, tolerating missing timestamps.</done>
</task>

<task type="auto">
  <name>Task 2: Baked notes section + bee-notes mount + Phase-179 CSS</name>
  <read_first>
    - _pages/species-detail.njk (the `.collected-from` conditional block lines ~60-72 to structurally mirror; the `seasonality-viz` mount script lines ~97-101; the `.taxon-action` links after which the notes section is placed)
    - 179-UI-SPEC.md "Layout Contract" (the authoritative verbatim Nunjucks for the baked section AND the bee-notes mount script) + "CSS Contract" (the verbatim Phase-179 CSS block) + "Accessibility Contract"
    - 179-PATTERNS.md section "_pages/species-detail.njk (notes <section> + <bee-notes> mount)"
    - src/styles/taxon-pages.css (end-of-file `.collected-from` block — append after it, with a `/* Phase 179: ... */` header per the file convention)
  </read_first>
  <behavior>
    - When notes[sp.canonical_name] has entries, the page emits `<section class="notes-section" id="notes">` with a `.note-list` of `<article class="note">` items in notes.json order (newest-first, producer-side), each with `<div class="note-body">{{ note.html | safe }}</div>` and a `.note-meta` footer
    - Byline renders `<a class="note-byline" href="{{ note.byline.collector_url }}">` when collector_url is present, else `<span class="note-byline">` (plain text)
    - `<time class="note-timestamp" datetime="{{ note.created }}">{{ note.created | formatDate }}</time>` and an `(edited)` span only when note.updated !== note.created
    - When notes[sp.canonical_name] is empty/absent, the `<section>` is omitted entirely (no empty box)
    - The `<bee-notes id="notes-el">` element + its `customElements.whenDefined('bee-notes')...` handoff script (setting canonicalName + bakedNotes) are ALWAYS emitted, regardless of whether the baked section rendered
  </behavior>
  <files>_pages/species-detail.njk, src/styles/taxon-pages.css</files>
  <action>
    Insert the baked notes section into `_pages/species-detail.njk` after the existing `.taxon-action` links, using
    179-UI-SPEC.md's "Layout Contract" block VERBATIM (it is the authoritative render contract, not merely an
    analog): the `{%- set notesForSpecies = notes[sp.canonical_name] -%}` conditional (mirrors `.collected-from`),
    `.notes-section`/`.notes-heading`/`.note-list`/`.note`/`.note-body` with `{{ note.html | safe }}`,
    `.note-meta` byline (linked vs plain per note.byline.collector_url), the `<time ... | formatDate>`, and the
    `(edited)` span guarded by `note.updated !== note.created`. Then ALWAYS emit `<bee-notes id="notes-el">` and
    its handoff `<script>` (setting el.canonicalName from `sp.canonical_name | dump | safe` and el.bakedNotes from
    `(notes[sp.canonical_name] or []) | dump | safe`), mirroring the seasonality-viz mount pattern.
    Append the Phase-179 CSS block from 179-UI-SPEC.md's "CSS Contract" VERBATIM to the end of
    `src/styles/taxon-pages.css` with a `/* Phase 179: Natural-history notes section ... */` header. Do NOT
    introduce new design tokens, a modal/dialog pattern, or a card-background treatment — all values come from
    existing src/index.css custom properties.
    `{{ note.html | safe }}` is the ONE trusted-HTML injection point — never wrap it in a second escape/sanitize.
  </action>
  <verify>
    <automated>grep -q 'notesForSpecies' _pages/species-detail.njk && grep -q 'note.html | safe' _pages/species-detail.njk && grep -q "customElements.whenDefined('bee-notes')" _pages/species-detail.njk && grep -q 'notes-section' src/styles/taxon-pages.css && npm test</automated>
  </verify>
  <acceptance_criteria>
    - `_pages/species-detail.njk` contains the `notesForSpecies` conditional, `{{ note.html | safe }}`, `| formatDate`, the collector_url-conditional byline, and the always-emitted `<bee-notes id="notes-el">` + handoff script
    - `src/styles/taxon-pages.css` ends with the Phase-179 `.notes-section` CSS block (no new design tokens, no modal, no card background)
    - The baked section is omitted when a species has zero notes; the mount element is emitted regardless
    - `npm test` stays green (tsc + existing JS suite), and the grep structural checks pass
  </acceptance_criteria>
  <done>Species pages render an attributed, newest-first, static notes list with a graceful empty state and no runtime API call; the mount point for the island is always present.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| notes.json → rendered HTML | `note.html` is injected verbatim via `| safe`; it must be trusted server-sanitized output, never re-parsed or re-sanitized here |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-179-XSS | Tampering | `{{ note.html | safe }}` injection | mitigate | the html was sanitized once server-side on write (179-01 nh3 allowlist); the bake trusts it and never double-escapes; no client-side markdown/sanitizer runs on the static path (D-04) |
| T-179-STATIC | Denial of Service / offline | page load | accept→mitigate | the baked section makes zero runtime API calls; it is the offline-safe source of truth (NOTES-03), independent of api.beeatlas.net availability |
</threat_model>

<artifacts_produced>
New symbols this plan introduces:
- `formatDate(iso)` — `src/lib/formatDate.js` (Eleventy filter + reused by the island)
- Eleventy `formatDate` filter registration — `eleventy.config.js`
- Baked `<section class="notes-section" id="notes">` + `<bee-notes id="notes-el">` mount + handoff script — `_pages/species-detail.njk`
- Phase-179 CSS block (`.notes-section`, `.note`, `.note-body`, `.note-meta`, `.note-btn*`, `.note-editor`, etc.) — `src/styles/taxon-pages.css`
- New test file `src/tests/formatDate.test.ts`
</artifacts_produced>

<verification>
- `npm test -- formatDate` green; `npm test` full JS suite green (tsc clean)
- grep structural checks on species-detail.njk (notesForSpecies, note.html | safe, formatDate, bee-notes mount) and taxon-pages.css (.notes-section) all pass
- Manual/UAT of the rendered visual is deferred to 179-06 (this plan's automated surface is the filter test + structural grep + tsc)
</verification>

<success_criteria>
- Baked notes render newest-first with linked/plain bylines and a graceful empty state, statically, with no page-load API call
- formatDate filter renders "Jul 4, 2026" and is shared with the island
- The bee-notes mount + handoff is always present; npm test + structural greps pass
</success_criteria>

<output>
Create `.planning/phases/179-notes-feature-harvest-build-time-bake/179-04-SUMMARY.md` when done.
</output>
