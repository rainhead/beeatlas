---
phase: 179-notes-feature-harvest-build-time-bake
plan: 06
type: execute
wave: 5
depends_on: [179-05]
files_modified: []
autonomous: false
requirements: [NOTES-01, NOTES-02, NOTES-03, NOTES-04]
user_setup: []

must_haves:
  truths:
    - "A live allowlisted author can create → edit → delete a note on a real species page against api.beeatlas.net, seeing each change immediately in the live island"
    - "A signed-out / offline / no-JS reader sees only the baked static list (never the island); the read path makes no runtime API call on page load"
    - "Forged-author and cross-origin note writes are both rejected (403) — mirrors the Phase-178 write-check UAT"
    - "After a nightly build cycle, a published note renders on the static species page with no runtime API call"
  artifacts: []
  key_links: []
---

<objective>
Human security + end-to-end UAT of the shipped notes feature — the manual-only
verifications from 179-VALIDATION.md that cannot be automated (live iNat OAuth session
+ cross-origin cookie flow + full nightly harvest→publish→deploy cycle). This phase
carries `UI hint: yes` and touches the write path, so it must NOT auto-advance past
UAT (memory feedback_uat_ui_phases); every checkpoint is a blocking human gate.

Purpose: The automated tests in 179-01..05 prove the units; this plan proves the
integrated, adversarial, and production-cycle behaviors a unit test cannot reach.
An unsanitized-write path or a missing ownership check are HIGH severity and must
not ship — the forged-author/cross-origin gate is the blocking security check.
Output: three signed-off human verifications (no code changes).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/179-notes-feature-harvest-build-time-bake/179-VALIDATION.md
@.planning/phases/179-notes-feature-harvest-build-time-bake/179-CONTEXT.md
@.planning/phases/179-notes-feature-harvest-build-time-bake/179-UI-SPEC.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: Live author create → edit → delete on a real species page</name>
  <what-built>
    The `<bee-notes>` hydrating island (179-05) on `_pages/species-detail.njk`, backed by
    the note CRUD + read endpoints (179-02) at api.beeatlas.net, over the baked static
    list (179-04). All units are green (179-01..05 pytest + vitest suites).
  </what-built>
  <action>
    Before the checkpoint, confirm the full suites are green (`npm test` + `cd data && uv run pytest`).
    Then present the verification steps below to the user and PAUSE for their sign-off — do not proceed
    or auto-advance (UI hint: yes; blocking human gate). Record the outcome verbatim in the SUMMARY.
  </action>
  <how-to-verify>
    1. Sign in as an allowlisted author (iNat OAuth) on a real species page, e.g. `https://beeatlas.net/species/<slug>/`.
    2. Confirm the "Add note" affordance appears; add a note using restricted markdown (e.g. `**bold**`, a `[link](https://example.com)`, and a short list). Confirm "Note posted." and that the note appears immediately in the live island with the byline as `@<your-login>` and today's date.
    3. Edit that note (change the text); confirm "Note updated." and the `(edited)` indicator appears.
    4. Delete that note (inline two-step confirm — NOT a browser dialog); confirm "Note deleted." and it disappears from the list.
    5. In a signed-out / private-window (or JS-disabled) session, load the same species page: confirm you see ONLY the baked static list (or nothing if the species has no baked notes yet), with no "Add note" affordance, and confirm via devtools Network that the page load itself made NO request to api.beeatlas.net (the read path is static).
  </how-to-verify>
  <verify>
    <human-check>User confirms create/edit/delete reflect live, markdown renders sanitized, and a signed-out/offline reader sees only the baked list with no runtime API call.</human-check>
  </verify>
  <acceptance_criteria>
    - Create/edit/delete each reflect in the live island immediately (before any nightly build)
    - The restricted-markdown renders as sanitized HTML (bold/italic/link/list); no raw markup leaks
    - A signed-out/offline/no-JS reader sees only the baked list and the page load makes no runtime API call
  </acceptance_criteria>
  <resume-signal>Type "approved" or describe issues (which step, expected vs actual).</resume-signal>
  <done>User has signed off that the live author round-trip and the static/offline reader path both behave as specified.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 2: Forged-author + cross-origin note-write rejection (adversarial)</name>
  <what-built>
    Note write endpoints (POST/PATCH/DELETE /api/notes) guarded by `require_author`
    (session + fresh allowlist + Origin/CSRF + launch gate) plus the per-note ownership
    check `note.author_id == g.identity['uid']` (179-02).
  </what-built>
  <action>
    Present the adversarial checks below and PAUSE for the user's sign-off (blocking human gate). Treat any
    accepted forged-author, cross-origin, or unauthenticated write as a HIGH-severity failure that blocks ship —
    do not mark the phase complete until all three are confirmed rejected. Record the outcome in the SUMMARY.
  </action>
  <how-to-verify>
    1. Forged author: as allowlisted author A (valid session), attempt PATCH and DELETE on a note owned by a DIFFERENT author B (by id). Confirm BOTH return 403 (mirrors the 178 write-check ownership invariant). Also confirm a client-supplied `author_id`/`author` field in a POST body is ignored — the created note is attributed to the session author, not the supplied id.
    2. Cross-origin: issue a POST /api/notes with an Origin header that is not beeatlas.net (e.g. via curl or a scratch page on another origin). Confirm it is rejected (403), matching the Phase-178 cross-origin write-check behavior.
    3. Unauthenticated: a POST/PATCH/DELETE with no session cookie returns 401.
    4. Spot-check: create a note whose body contains a `<script>alert(1)</script>` payload; confirm it renders inert (no script executes) on both the live island and the baked page.
  </how-to-verify>
  <verify>
    <human-check>User confirms cross-author PATCH/DELETE 403, cross-origin POST 403, unauthenticated write 401, client-supplied author ignored, and a script payload renders inert.</human-check>
  </verify>
  <acceptance_criteria>
    - Cross-author PATCH and DELETE both return 403; a client-supplied author field never changes attribution
    - A cross-origin note write is rejected (403); an unauthenticated write returns 401
    - No unsanitized content is ever stored (a `<script>` payload renders inert)
  </acceptance_criteria>
  <resume-signal>Type "approved" or describe the failing adversarial case.</resume-signal>
  <done>User has signed off that forged-author, cross-origin, and unauthenticated writes are all rejected and content is sanitized on write.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 3: notes.json appears on the public static site after a nightly cycle</name>
  <what-built>
    The nightly harvest (`data/notes_harvest.py`, run.py `notes-harvest` STEP), the
    `[artifacts.notes]` authoritative contract entry (build_time_fetch + optional), and
    the `_data/notes.js` loader (179-03) feeding the baked section (179-04).
  </what-built>
  <action>
    After a maderas nightly (or operator-triggered) harvest+publish+deploy cycle, present the checks below and
    PAUSE for the user's sign-off (blocking human gate). Confirm notes.json is NOT committed to git. Record the
    outcome in the SUMMARY.
  </action>
  <how-to-verify>
    1. After a maderas nightly run (or an operator-triggered run) that harvests + publishes, confirm `notes.json` is in the S3 manifest and fetched by deploy.yml's build-time-fetch step (no `required artifact missing` failure — `build_time_fetch_optional=true` guards the pre-first-nightly case).
    2. Load a species page that has an approved note in the store: confirm the note renders in the baked static `<section class="notes-section">` with the FULL resolved byline (display_name + `/collectors/<login>/` link when the author is a WABA collector) — not the live `@login` fallback.
    3. Confirm via devtools that the note is present in the served HTML source (server-rendered), not injected by a runtime API call.
    4. Confirm `notes.json` was NOT committed to git (it ships via S3 + manifest + deploy.yml only).
  </how-to-verify>
  <verify>
    <human-check>User confirms a harvested approved note renders on the static site with the resolved byline, is in the served HTML, and notes.json is not committed to git.</human-check>
  </verify>
  <acceptance_criteria>
    - A published approved note renders in the baked static section after a build cycle, with the harvest-resolved byline (display_name + collector link)
    - The page's note content is in the served HTML (no runtime API call on load)
    - `notes.json` is absent from git; the deploy build did not fail on a missing manifest key pre-first-nightly
  </acceptance_criteria>
  <resume-signal>Type "approved" or describe the harvest/publish/render discrepancy.</resume-signal>
  <done>User has signed off that the full nightly harvest→publish→bake cycle renders a note on the static site and notes.json is never committed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| adversary → note write API | forged-author, cross-origin, and unauthenticated writes are the adversarial surface verified here |
| store → public static site | only approved notes may reach the baked page; the full nightly cycle is verified end-to-end |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-179-AUTHZ | Elevation of Privilege | forged-author PATCH/DELETE | mitigate | human UAT confirms cross-author writes 403 and client-supplied author is ignored (HIGH — blocks ship if it fails) |
| T-179-CSRF | Spoofing/Tampering | cross-origin note write | mitigate | human UAT confirms require_author's Origin gate rejects a non-beeatlas.net write (HIGH — blocks ship) |
| T-179-XSS | Tampering | stored note content | mitigate | spot-check a `<script>` payload renders inert on both the live island and the baked page (sanitized on write, 179-01) |
| T-179-LEAK | Information Disclosure | baked notes.json | mitigate | UAT confirms only approved notes reach the public page; pending/removed never appear |
</threat_model>

<artifacts_produced>
No new code symbols — this plan produces three signed-off human verifications covering the manual-only items in 179-VALIDATION.md. It is a blocking UAT gate (autonomous: false; do not auto-advance — UI hint: yes).
</artifacts_produced>

<verification>
- All three human-verify checkpoints signed off "approved"
- Full suites green before UAT: `npm test` + `cd data && uv run pytest` (data/ + ../api/tests, deselecting integration)
- Any HIGH-severity failure (unsanitized write, missing ownership check, cross-origin/forged-author accepted) blocks ship
</verification>

<success_criteria>
- Live author create/edit/delete works end-to-end; guests/offline see only the baked list
- Forged-author and cross-origin writes are rejected; content is sanitized on write
- A harvested note renders on the static site after a nightly cycle with the resolved byline; notes.json never committed
</success_criteria>

<output>
Create `.planning/phases/179-notes-feature-harvest-build-time-bake/179-06-SUMMARY.md` when done.
</output>
