---
status: diagnosed
phase: 081-filter-ux-nav
source:
  - 081-01-SUMMARY.md
  - 081-02-SUMMARY.md
  - 081-03-SUMMARY.md
  - 081-04-SUMMARY.md
  - 081-05-SUMMARY.md
started: 2026-05-04T23:30:00Z
updated: 2026-05-04T23:30:00Z
---

## Current Test

[testing complete]

## Tests

### 1. Cold Start Smoke Test
expected: From a fresh state (kill any running dev server, clear caches), run `npm run dev` and load `/species/`. The page reaches a usable state — cards render, nav tree is visible, no console errors block interaction.
result: pass

### 2. Species Page Renders
expected: Visiting `/species/` shows ~735 species cards, each with name, count badge, seasonality viz strip, and a "View N occurrences →" link. Left/side region shows the taxon nav tree (family → subfamily → tribe → genus → subgenus).
result: issue
reported: "the content is there, but there is no layout. Nav tree is above the \"species cards\". Maybe that's intended at this phase."
severity: minor

### 3. Taxon Nav — Filter by Click
expected: Clicking a taxon in the nav (e.g. genus "Bombus") visually mutes (lowers opacity) all cards that don't belong to that taxon. The breadcrumb pill row shows the active taxon path. URL gains the corresponding param (e.g. `?gen=Bombus`).
result: issue
reported: "taxa in the nav link to the map"
severity: major

### 4. Taxon Nav — Card Counts Reflect Selection
expected: With a taxon active, each card's count badge shows occurrences within that taxon (per-card filtered count). Cards outside the active taxon are muted and show 0 / hidden counts — they don't claim partial counts.
result: blocked
blocked_by: prior-phase
reason: "Test 3 found nav links route to atlas instead of filtering in place — no way to reach 'taxon active' state on the species page."

### 5. Filter Widget — Month Range
expected: Opening the filter popover and selecting a month range (e.g. May–July) updates per-card count badges and seasonality viz immediately. Cards with zero records in that month range are muted. URL gains `m0`/`m1` params.
result: issue
reported: "yes, but the months are called \"1\" through \"12\""
severity: cosmetic
notes: "Core month-range filter behavior works. Issue is labeling only — months display as numeric 1–12 instead of Jan–Dec."

### 6. Filter Widget — Month Range Inversion Guard
expected: If the month range is set with `from > to` (e.g. set "from" to October when "to" is March), the other endpoint snaps so the range stays valid. No silent zeroing of all counts.
result: pass

### 7. Filter Widget — County / Ecoregion
expected: Selecting a county (e.g. King) or ecoregion checkbox filters card counts to only records in that geography. Multiple selections combine sensibly. URL gains `county=...` / `ecor=...` params.
result: issue
reported: "it's okay, but I see e.g.\n<p class=\"viz-fallback\"><!--?lit$431079212$-->2 records<!--?lit$431079212$-->, A</p>"
severity: minor
notes: "Filter behavior works. Issue is rendering: viz-fallback <p> contains literal lit-html comment markers and a stray ', A' suffix. Likely innerHTML/string interpolation of a TemplateResult instead of letting Lit render it, or a Lit template fragment is being serialized to a string in build."

### 8. Combined Filter — Taxon + Geo + Months
expected: Combining a taxon click, a county, and a month range narrows counts further. The breadcrumb pill row reflects all active filters; "Clear filters" removes them and restores the unfiltered view.
result: blocked
blocked_by: prior-phase
reason: "Test 3 — taxon nav clicks navigate to atlas instead of filtering in place; can't reach a 'taxon active' state on the species page to combine with geo/months."

### 9. Empty State
expected: A filter combination that yields zero records anywhere (e.g. obscure taxon × off-season months) shows an empty state placeholder, not just 735 muted cards.
result: pass

### 10. URL Round-Trip
expected: Copy the URL after applying filters, paste into a new tab — page loads with the same filters applied (cards muted/counted appropriately, breadcrumb populated, filter widget reflecting state).
result: pass

### 11. Browser Back / Forward
expected: Apply several filter changes, then click browser Back — each click steps backward through filter states without duplicate history entries (one Back per user action). Forward steps replay them. No race where Back undoes more or fewer than one step.
result: pass

### 12. No-Subgenus Bucket
expected: The "(no subgenus)" bucket in the nav is clickable and filters to species without a subgenus assignment. The URL uses a stable token (not the literal `(no subgenus)` label) and round-trips cleanly.
result: pass

### 13. Card SPA Link → Atlas
expected: Clicking "View N occurrences →" on a species card navigates to the atlas (`/`) with `?taxon=<scientificName>&taxonRank=species`. The atlas filters to that species (not its parent genus/family).
result: pass

## Summary

total: 13
passed: 7
issues: 4
pending: 0
skipped: 0
blocked: 2

## Gaps

- truth: "Species page presents nav tree and species cards in a coherent layout — at minimum a two-column or sidebar arrangement so the nav tree doesn't stack above all cards."
  status: failed
  reason: "User reported: the content is there, but there is no layout. Nav tree is above the \"species cards\". Maybe that's intended at this phase."
  severity: minor
  test: 2
  root_cause: "Visual layout was OUT OF SCOPE for phase 081. CONTEXT.md:111 ('Phase 81 doesn't change layout'), :169 ('Suggest running /gsd-ui-phase 81 if a styling pass is wanted alongside the new components'), :23 ('Out of scope for Phase 81: visual polish'). No species-page CSS exists in the codebase; the stacked appearance is the natural flow of unstyled block elements."
  artifacts:
    - path: ".planning/phases/081-filter-ux-nav/081-CONTEXT.md"
      issue: "Lines 23, 111, 169 explicitly defer layout — diagnosis confirms scope"
  missing:
    - "Defer layout to a follow-up UI phase (e.g. /gsd-ui-phase 81 or new phase 082-species-layout). Do NOT block phase 081 on this gap."
  debug_session: .planning/debug/species-page-no-layout.md
  notes: "Out of scope — defer."

- truth: "Clicking a taxon in the species-page nav filters the species page in place (mutes non-matching cards, updates breadcrumb, sets URL param like `?gen=Bombus`) — it does NOT navigate the user to the atlas/map."
  status: failed
  reason: "User reported: taxa in the nav link to the map"
  severity: major
  test: 3
  root_cause: "_includes/taxon-tree.njk emits cross-route <a href=\"/?taxon=…&taxonRank=…\"> at species (line 20), genus (line 51), and family (line 101) ranks. bee-taxon-nav.ts:74 explicitly comments 'Allow native <a> click for LINK-03 deep-link to SPA. Do NOT preventDefault.' The taxon-selected event IS dispatched and bee-species-page DOES handle it (mute, breadcrumb, URL push) — but native navigation unloads /species/ before in-place filter machinery runs. Collision: LINK-03 (nav as deep-link) vs NAV-03/NAV-04 (nav as in-place filter), both pinned to same click target; navigation wins."
  artifacts:
    - path: "_includes/taxon-tree.njk"
      issue: "Lines 20, 51, 101 emit cross-route anchors that hijack nav clicks before in-place filter can run"
    - path: "src/species/bee-taxon-nav.ts:74"
      issue: "Comment documents that preventDefault is suppressed; click delegate yields to native navigation"
  missing:
    - "Decouple LINK-03 from NAV-03/04. Either (a) drop embedded anchors from taxon-tree.njk and let bee-taxon-nav._onClick handle clicks (it already builds path/rank and dispatches taxon-selected); or (b) keep anchors as no-JS fallback but call e.preventDefault() in bee-taxon-nav._onClick when JS is live."
    - "Re-evaluate LINK-03: species-card 'View N occurrences →' (UAT test 13 PASS) already covers the cross-route deep-link need; LINK-03 from nav rail is arguably redundant."
  debug_session: .planning/debug/nav-routes-to-atlas-instead-of-filter.md

- truth: "Month-range filter labels show human month names (Jan, Feb, ... Dec), not numeric 1–12."
  status: failed
  reason: "User reported: yes, but the months are called \"1\" through \"12\""
  severity: cosmetic
  test: 5
  root_cause: "src/species/bee-species-filter.ts:91-107 renders the month range as two native <input type=\"number\" min=\"1\" max=\"12\"> controls bound directly to numeric monthFrom/monthTo. Native number inputs cannot display non-numeric labels. No month-name mapping exists in the file."
  artifacts:
    - path: "src/species/bee-species-filter.ts:88-109"
      issue: "Month range UI uses <input type=number>; emits numeric labels"
  missing:
    - "Replace <input type=\"number\"> with <select> elements: numeric value=1..12, human-readable text content (e.g. via Intl.DateTimeFormat short month names). Existing change handler's Number(...) cast still works on <select> values."
  debug_session: .planning/debug/month-labels-numeric.md

- truth: "Species cards render the seasonality fallback as clean text — no leaking lit-html comment markers, no stray suffix in the visible DOM."
  status: failed
  reason: "User reported visible markup: `<p class=\"viz-fallback\"><!--?lit$431079212$-->2 records<!--?lit$431079212$-->, A</p>`"
  severity: minor
  test: 7
  root_cause: "NOT a Lit-stringification bug. Two independent things bundled: (1) <!--?lit$NNN$--> are normal lit-html part markers — invisible to end users, only seen in DevTools Inspector; <seasonality-viz> renders into light DOM (createRenderRoot returns this, locked by D-04 so external CSS can style the SVG), so markers land in inspectable DOM. seasonality-viz.ts:70 has 2 child bindings → 2 marker pairs (matches reported HTML). (2) The ', A' suffix is intentional output: when total<5 and monthsWithData.length===1, the template appends `, ${monthLetter}` (lines 62-70). MONTH_LABELS at line 24 is single-letter ['J','F','M','A','M','J','J','A','S','O','N','D'] — 'A' is ambiguous (April OR August). The real defect, if any, is the ambiguous single-letter hint, not the markers."
  artifacts:
    - path: "src/species/seasonality-viz.ts:41-43"
      issue: "Light-DOM rendering (createRenderRoot returns this) — D-04 architectural decision; markers visible in DevTools but invisible to users"
    - path: "src/species/seasonality-viz.ts:70"
      issue: "Two child bindings produce two marker pairs in inspector"
    - path: "src/species/seasonality-viz.ts:24,62-68"
      issue: "Single-letter MONTH_LABELS used in fallback hint produces ambiguous 'A' (April vs August)"
  missing:
    - "DECIDE: are markers a real defect? They are invisible to users. If yes (e.g. for SEO/E2E concerns), collapse the two child bindings into one string expression to halve the markers, or render fallback imperatively via this.textContent. If no, document and close."
    - "DECIDE: is ', A' acceptable? Either (a) drop the month hint when monthsWithData.length===1 (single-letter ambiguous), or (b) use 3-letter abbreviations (Apr, Aug) for the hint while keeping single letters for axis labels. Confirm against 081-04-PLAN.md:177 before changing."
  debug_session: .planning/debug/lit-markers-in-fallback.md
  notes: "User's reported markup is what the code is supposed to produce. Largely a non-defect — the only real concern is the ambiguous month-letter."
