---
status: partial
phase: 081-filter-ux-nav
source:
  - 081-01-SUMMARY.md
  - 081-02-SUMMARY.md
  - 081-03-SUMMARY.md
  - 081-04-SUMMARY.md
  - 081-05-SUMMARY.md
started: 2026-05-04T22:56:49Z
updated: 2026-05-04T22:59:30Z
---

## Current Test

[testing complete]

## Tests

### 1. Cold Start Smoke Test
expected: From a fresh state (kill any running dev server, clear caches), run `npm run dev` and load `/species/`. The page reaches a usable state — cards render, nav tree is visible, no console errors block interaction.
result: issue
reported: "table occurrences has no column named canonical_name"
severity: blocker

### 2. Species Page Renders
expected: Visiting `/species/` shows ~735 species cards, each with name, count badge, seasonality viz strip, and a "View N occurrences →" link. Left/side region shows the taxon nav tree (family → subfamily → tribe → genus → subgenus).
result: blocked
blocked_by: prior-phase
reason: "Test 1 cold-start SQL error (occurrences.canonical_name missing) prevents page from loading"

### 3. Taxon Nav — Filter by Click
expected: Clicking a taxon in the nav (e.g. genus "Bombus") visually mutes (lowers opacity) all cards that don't belong to that taxon. The breadcrumb pill row shows the active taxon path. URL gains the corresponding param (e.g. `?gen=Bombus`).
result: blocked
blocked_by: prior-phase
reason: "Page does not load due to test-1 SQL error"

### 4. Taxon Nav — Card Counts Reflect Selection
expected: With a taxon active, each card's count badge shows occurrences within that taxon (per-card filtered count). Cards outside the active taxon are muted and show 0 / hidden counts — they don't claim partial counts.
result: blocked
blocked_by: prior-phase
reason: "Page does not load due to test-1 SQL error"

### 5. Filter Widget — Month Range
expected: Opening the filter popover and selecting a month range (e.g. May–July) updates per-card count badges and seasonality viz immediately. Cards with zero records in that month range are muted. URL gains `m0`/`m1` params.
result: blocked
blocked_by: prior-phase
reason: "Page does not load due to test-1 SQL error"

### 6. Filter Widget — Month Range Inversion Guard
expected: If the month range is set with `from > to` (e.g. set "from" to October when "to" is March), the other endpoint snaps so the range stays valid. No silent zeroing of all counts.
result: blocked
blocked_by: prior-phase
reason: "Page does not load due to test-1 SQL error"

### 7. Filter Widget — County / Ecoregion
expected: Selecting a county (e.g. King) or ecoregion checkbox filters card counts to only records in that geography. Multiple selections combine sensibly. URL gains `county=...` / `ecor=...` params.
result: blocked
blocked_by: prior-phase
reason: "Page does not load due to test-1 SQL error"

### 8. Combined Filter — Taxon + Geo + Months
expected: Combining a taxon click, a county, and a month range narrows counts further. The breadcrumb pill row reflects all active filters; "Clear filters" removes them and restores the unfiltered view.
result: blocked
blocked_by: prior-phase
reason: "Page does not load due to test-1 SQL error"

### 9. Empty State
expected: A filter combination that yields zero records anywhere (e.g. obscure taxon × off-season months) shows an empty state placeholder, not just 735 muted cards.
result: blocked
blocked_by: prior-phase
reason: "Page does not load due to test-1 SQL error"

### 10. URL Round-Trip
expected: Copy the URL after applying filters, paste into a new tab — page loads with the same filters applied (cards muted/counted appropriately, breadcrumb populated, filter widget reflecting state).
result: blocked
blocked_by: prior-phase
reason: "Page does not load due to test-1 SQL error"

### 11. Browser Back / Forward
expected: Apply several filter changes, then click browser Back — each click steps backward through filter states without duplicate history entries (one Back per user action). Forward steps replay them. No race where Back undoes more or fewer than one step.
result: blocked
blocked_by: prior-phase
reason: "Page does not load due to test-1 SQL error"

### 12. No-Subgenus Bucket
expected: The "(no subgenus)" bucket in the nav is clickable and filters to species without a subgenus assignment. The URL uses a stable token (not the literal `(no subgenus)` label) and round-trips cleanly.
result: blocked
blocked_by: prior-phase
reason: "Page does not load due to test-1 SQL error"

### 13. Card SPA Link → Atlas
expected: Clicking "View N occurrences →" on a species card navigates to the atlas (`/`) with `?taxon=<scientificName>&taxonRank=species`. The atlas filters to that species (not its parent genus/family).
result: blocked
blocked_by: prior-phase
reason: "Page does not load due to test-1 SQL error"

## Summary

total: 13
passed: 0
issues: 3
pending: 0
skipped: 0
blocked: 12

## Gaps

- truth: "Atlas at / loads without SQLite init errors — browser-side `occurrences` schema must accept every column present in the published `occurrences.parquet`."
  status: fixed
  fixed_in: f9d25cd
  reason: "Browser console: 'SQLiteError: table occurrences has no column named canonical_name' at sqlite-api.js prepare_v2. After pipeline rerun, the parquet now carries `canonical_name` (added by data/checklist_pipeline.py:42 ALTER TABLE), but src/sqlite.ts:66-99 hard-codes a 31-column CREATE TABLE that omits it. _insertRows builds the INSERT from Object.keys(rows[0]), so the new column is in the INSERT col list with no matching DDL slot."
  severity: blocker
  test: 1
  artifacts:
    - src/sqlite.ts:66-99 (CREATE TABLE occurrences — missing canonical_name)
    - src/sqlite.ts:142-152 (_insertRows — column list from parquet, not from DDL)
    - data/checklist_pipeline.py:42 (ALTER TABLE adds canonical_name to pipeline DB)
  missing:
    - canonical_name column in src/sqlite.ts CREATE TABLE
    - long-term: schema-drift guard between parquet columns and SQLite DDL (or generate DDL from parquet schema)
  notes: "Out of scope for Phase 081 (atlas data wiring, not species-page UX), but discovered during 081 UAT. Triage as a separate fix."

- truth: "Taxon nav summary labels never render the literal word `null` to the user — the no-data sentinel must be replaced with a human label at every level (family, subfamily, tribe, genus, subgenus)."
  status: fixed
  fixed_in: 9c4ac02
  reason: "User reports many taxa rendered as 'null' in the tree. BL-04 fix only mapped the 'null' sentinel to '(no subgenus)' inside renderSubgenus; renderSubfamily/renderTribe/renderGenus still print the raw key. _data/species.js emits 'null' as the sentinel at every level."
  severity: major
  test: post-test (observed during in-flight inspection)
  artifacts:
    - _includes/taxon-tree.njk:61,74,87 (summaries print raw key with no null-guard)
    - _data/species.js:54 (sentinel emitted at every level)
  missing:
    - level-aware label substitution in renderSubfamily / renderTribe / renderGenus, or a single shared helper

- truth: "Species page shows only bees — non-bee taxa (e.g. Braconidae, a parasitoid wasp family) must not appear in the taxon nav."
  status: failed
  reason: "User reports many taxa labeled 'Braconidae' in the nav. Likely upstream lineage-resolution mis-mapping (Phase 77/78), surfaces in Phase 081 UI."
  severity: major
  test: post-test (observed during in-flight inspection)
  artifacts: []
  missing: []
  notes: "Probably out-of-scope for 081 fix wave — flag for triage against the lineage pipeline."
