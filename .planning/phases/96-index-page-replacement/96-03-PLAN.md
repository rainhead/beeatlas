---
phase: 96-index-page-replacement
plan: 03
type: execute
wave: 3
depends_on:
  - 02
files_modified:
  - src/entries/species.ts
  - src/species/bee-species-page.ts
  - src/species/bee-species-filter.ts
  - src/species/bee-species-card.ts
  - src/species/bee-taxon-nav.ts
  - src/species/url-state.ts
  - src/styles/species.css
  - _includes/taxon-tree.njk
  - src/tests/bee-species-page.test.ts
  - src/tests/bee-species-filter.test.ts
  - src/tests/bee-species-card.test.ts
  - src/tests/bee-taxon-nav.test.ts
  - src/tests/species-url-state.test.ts
  - src/species/tests/a11y.test.ts
  - src/tests/arch.test.ts
autonomous: true
requirements:
  - URL-05
tags:
  - cleanup
  - eleventy

must_haves:
  truths:
    - "D-01: The old monolith files (`bee-species-page.ts`, `bee-species-filter.ts`, `bee-species-card.ts`, `bee-taxon-nav.ts`, `url-state.ts`, `taxon-tree.njk`, `src/entries/species.ts`, `src/styles/species.css`) are deleted from the repo in Phase 96 — not deferred"
    - "Their dedicated test files (`bee-species-page.test.ts`, `bee-species-filter.test.ts`, `bee-species-card.test.ts`, `bee-taxon-nav.test.ts`, `species-url-state.test.ts`, `src/species/tests/a11y.test.ts`) are deleted"
    - "src/tests/arch.test.ts no longer asserts the old coordinator / entry allowlist; it asserts the new `species-index.ts` allowlist instead"
    - "`src/species/seasonality-viz.ts` and `src/species/seasonality-cache.ts` are KEPT (still used by `_pages/species-detail.njk` via `src/entries/taxon-page.ts`)"
    - "`npm test` and `npm run build` both exit 0 after the deletions"
  artifacts:
    - path: "src/tests/arch.test.ts"
      provides: "Updated architectural test — drops PAGE-06 and species.ts allowlist, adds species-index.ts allowlist"
      contains: "species-index"
  key_links:
    - from: "src/tests/arch.test.ts"
      to: "src/entries/species-index.ts"
      via: "readFileSync + allowlist assertion"
      pattern: "src/entries/species-index\\.ts"
    - from: "src/species/seasonality-viz.ts (KEPT)"
      to: "src/entries/taxon-page.ts (KEPT)"
      via: "side-effect import on line 13 of taxon-page.ts"
      pattern: "'../species/seasonality-viz"
---

<objective>
Complete URL-05 by deleting the old `/species/` monolith — every source file, test file, and Nunjucks include that exists solely to support the now-replaced page. Update `src/tests/arch.test.ts` to drop assertions about the deleted files and add a new allowlist for `src/entries/species-index.ts`. Keep `seasonality-viz.ts` and `seasonality-cache.ts` (still loaded by `_pages/species-detail.njk`).

Purpose: `noUnusedLocals` does not catch orphaned side-effect imports, and dedicated test files for deleted components would keep passing — both create false confidence that the monolith is gone. CONTEXT.md D-01 explicitly requires deletion in this phase, not a follow-on. After this plan, every grep for `bee-species-page` / `bee-species-filter` / `bee-taxon-nav` / `bee-species-card` / `taxon-tree.njk` returns zero hits across the repo (except CHANGELOG/SUMMARY archive material).

Output: 14 files deleted, 1 file (`src/tests/arch.test.ts`) modified. Full test suite + build green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/96-index-page-replacement/96-CONTEXT.md
@.planning/phases/96-index-page-replacement/96-RESEARCH.md
@.planning/phases/96-index-page-replacement/96-PATTERNS.md

<interfaces>
<!-- src/tests/arch.test.ts (CURRENT — read with Read tool before editing): four describe blocks -->
<!-- 1. lines 82-107  describe('ARCH-04: src/species boundary (PAGE-08)')      → KEEP, trim/relabel comment -->
<!-- 2. lines 114-140 describe('PAGE-06: presenter→coordinator non-import')   → DELETE entire block (coordinator gone) -->
<!-- 3. lines 142-194 describe('src/entries/species.ts allowlist (PAGE-04 partial)') → DELETE entire block (entry gone) -->
<!-- 4. lines 203-224 describe('ARCH-04: src/lib/spa-link.ts boundary (D-05)') → KEEP unchanged -->
<!-- NEW block to ADD: describe('src/entries/species-index.ts allowlist (IDX-02, Phase 96)') — pattern in 96-PATTERNS.md -->

<!-- Files KEPT (do NOT delete): -->
<!--   src/species/seasonality-viz.ts        — used by src/entries/taxon-page.ts (line 13) which loads on species-detail.njk -->
<!--   src/species/seasonality-cache.ts      — supporting module for seasonality-viz.ts -->
<!--   src/tests/seasonality-viz.test.ts     — tests a kept module -->

<!-- src/species/tests/a11y.test.ts: the CARD_FIXTURE uses 'bee-species-card' as an HTML string but the file -->
<!-- imports bee-taxon-nav.ts at the module level. Since bee-taxon-nav.ts is deleted, the import fails. Delete the test file. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Delete monolith source files, the Nunjucks include, and the old species.css stylesheet</name>
  <files>
    src/entries/species.ts,
    src/species/bee-species-page.ts,
    src/species/bee-species-filter.ts,
    src/species/bee-species-card.ts,
    src/species/bee-taxon-nav.ts,
    src/species/url-state.ts,
    src/styles/species.css,
    _includes/taxon-tree.njk
  </files>
  <read_first>
    - .planning/phases/96-index-page-replacement/96-RESEARCH.md (§"Component Deletions: Complete Inventory" — verified deletion list; §"Files Confirmed SAFE TO KEEP")
    - .planning/phases/96-index-page-replacement/96-PATTERNS.md (§"Deletions" — full table)
    - .planning/phases/96-index-page-replacement/96-CONTEXT.md (§"D-01" — locked decision: delete in Phase 96)
    - src/entries/taxon-page.ts (re-confirm line 13 imports `../species/seasonality-viz.ts` — do NOT delete seasonality-viz.ts)
    - _pages/species-detail.njk (confirm `<seasonality-viz>` element usage — proof that seasonality-viz.ts must stay)
  </read_first>
  <action>
    Run `git rm` on the eight listed files (single commit-friendly atomic group). Confirm each file's current existence with `ls` before removal; abort the task and surface to the user if any of them is missing (would indicate a desync from research). The files are: `src/entries/species.ts`, `src/species/bee-species-page.ts`, `src/species/bee-species-filter.ts`, `src/species/bee-species-card.ts`, `src/species/bee-taxon-nav.ts`, `src/species/url-state.ts`, `src/styles/species.css`, `_includes/taxon-tree.njk`.
    Do NOT delete `src/species/seasonality-viz.ts` or `src/species/seasonality-cache.ts` — these are loaded by `src/entries/taxon-page.ts` (line 13) which `_pages/species-detail.njk` consumes. Deleting them would break every species detail page (URL-01, SPE-01..04).
    After the deletions, grep the entire repo (excluding `.planning/`, `node_modules/`, `_site/`, `.git/`) to confirm no remaining references to the deleted source modules. Acceptable residual references: documentation in `.planning/`, comments in test files that are themselves slated for deletion in Task 2.
  </action>
  <verify>
    <automated>cd /Users/rainhead/dev/beeatlas && test ! -e src/entries/species.ts && echo OK_species_ts</automated>
    <automated>cd /Users/rainhead/dev/beeatlas && test ! -e src/species/bee-species-page.ts && echo OK_bsp</automated>
    <automated>cd /Users/rainhead/dev/beeatlas && test ! -e src/species/bee-species-filter.ts && echo OK_bsf</automated>
    <automated>cd /Users/rainhead/dev/beeatlas && test ! -e src/species/bee-species-card.ts && echo OK_bsc</automated>
    <automated>cd /Users/rainhead/dev/beeatlas && test ! -e src/species/bee-taxon-nav.ts && echo OK_btn</automated>
    <automated>cd /Users/rainhead/dev/beeatlas && test ! -e src/species/url-state.ts && echo OK_us</automated>
    <automated>cd /Users/rainhead/dev/beeatlas && test ! -e src/styles/species.css && echo OK_css</automated>
    <automated>cd /Users/rainhead/dev/beeatlas && test ! -e _includes/taxon-tree.njk && echo OK_njk</automated>
    <automated>cd /Users/rainhead/dev/beeatlas && test -f src/species/seasonality-viz.ts && echo KEPT_sv</automated>
    <automated>cd /Users/rainhead/dev/beeatlas && test -f src/species/seasonality-cache.ts && echo KEPT_sc</automated>
    <automated>cd /Users/rainhead/dev/beeatlas && grep -rn "bee-species-page\|bee-species-filter\|bee-species-card\|bee-taxon-nav\|taxon-tree" --include="*.ts" --include="*.njk" --include="*.js" --include="*.css" src _pages _includes _data eleventy.config.js 2>/dev/null | grep -v "^src/tests/" | head -10</automated>
  </verify>
  <acceptance_criteria>
    - All eight listed files no longer exist on disk
    - `src/species/seasonality-viz.ts` still exists (KEPT)
    - `src/species/seasonality-cache.ts` still exists (KEPT)
    - `grep -rn "bee-species-page\|bee-species-filter\|bee-species-card\|bee-taxon-nav\|taxon-tree" --include="*.ts" --include="*.njk" --include="*.js" --include="*.css" src _pages _includes _data eleventy.config.js | grep -v "^src/tests/"` returns no matches (production code is clean; tests are cleaned in Task 2)
    - `git status` shows the eight files as deleted (D) in the index
  </acceptance_criteria>
  <done>Eight monolith production files deleted; seasonality-viz.ts + seasonality-cache.ts preserved; no remaining production references to deleted symbols.</done>
</task>

<task type="auto">
  <name>Task 2: Delete dedicated test files for the removed monolith components</name>
  <files>
    src/tests/bee-species-page.test.ts,
    src/tests/bee-species-filter.test.ts,
    src/tests/bee-species-card.test.ts,
    src/tests/bee-taxon-nav.test.ts,
    src/tests/species-url-state.test.ts,
    src/species/tests/a11y.test.ts
  </files>
  <read_first>
    - .planning/phases/96-index-page-replacement/96-RESEARCH.md (§"Test Files: Complete Deletion List" — six files)
    - .planning/phases/96-index-page-replacement/96-PATTERNS.md (§"Deletions" — confirmation of the same list)
    - src/species/tests/a11y.test.ts (confirm it imports `bee-taxon-nav.ts` which was deleted in Task 1; the test would now fail with ENOENT — deletion is mandatory not optional)
    - src/tests/seasonality-viz.test.ts (confirm this file is NOT in the deletion list — it tests `seasonality-viz.ts` which is kept)
  </read_first>
  <action>
    Run `git rm` on the six listed test files. Each file tests a module deleted in Task 1 or imports a module deleted in Task 1 (the a11y test imports `bee-taxon-nav.ts`).
    Do NOT delete `src/tests/seasonality-viz.test.ts` — it tests `seasonality-viz.ts` which is kept.
    After the deletions, check whether the directory `src/species/tests/` is now empty; if so, remove it. (It contained only `a11y.test.ts`.)
  </action>
  <verify>
    <automated>cd /Users/rainhead/dev/beeatlas && test ! -e src/tests/bee-species-page.test.ts && echo OK_p</automated>
    <automated>cd /Users/rainhead/dev/beeatlas && test ! -e src/tests/bee-species-filter.test.ts && echo OK_f</automated>
    <automated>cd /Users/rainhead/dev/beeatlas && test ! -e src/tests/bee-species-card.test.ts && echo OK_c</automated>
    <automated>cd /Users/rainhead/dev/beeatlas && test ! -e src/tests/bee-taxon-nav.test.ts && echo OK_n</automated>
    <automated>cd /Users/rainhead/dev/beeatlas && test ! -e src/tests/species-url-state.test.ts && echo OK_u</automated>
    <automated>cd /Users/rainhead/dev/beeatlas && test ! -e src/species/tests/a11y.test.ts && echo OK_a</automated>
    <automated>cd /Users/rainhead/dev/beeatlas && test -f src/tests/seasonality-viz.test.ts && echo KEPT_sv_test</automated>
    <automated>cd /Users/rainhead/dev/beeatlas && ls src/species/tests/ 2>&1 | head -5</automated>
  </verify>
  <acceptance_criteria>
    - All six listed test files no longer exist
    - `src/tests/seasonality-viz.test.ts` still exists (KEPT — tests a kept module)
    - `src/species/tests/` directory is either empty or removed
    - `git status` shows the six files as deleted (D) in the index
  </acceptance_criteria>
  <done>Six dedicated test files deleted; seasonality-viz.test.ts preserved; no orphaned test directory.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Surgically update src/tests/arch.test.ts — drop dead describe blocks, add species-index allowlist</name>
  <files>src/tests/arch.test.ts</files>
  <read_first>
    - src/tests/arch.test.ts (entire file — 224 lines; identify the four describe blocks at lines 82-107, 114-140, 142-194, 203-224)
    - .planning/phases/96-index-page-replacement/96-RESEARCH.md (§"arch.test.ts Surgery Required" — explicit instructions per describe block)
    - .planning/phases/96-index-page-replacement/96-PATTERNS.md (§"src/tests/arch.test.ts" — verbatim new describe block for species-index.ts allowlist)
    - src/entries/species-index.ts (the new entry created in Plan 02 — confirm it only imports `'../index.css'`, `'../styles/taxon-pages.css'`, `'../bee-header.ts'`)
  </read_first>
  <behavior>
    - Block 1 (lines 82-107, `ARCH-04: src/species boundary (PAGE-08)`): KEEP unchanged. After deletions, `src/species/` contains only `seasonality-viz.ts` and `seasonality-cache.ts`; the FORBIDDEN-import test still applies to those files. Optionally update the header comment "RED until Plan 03" to reflect the post-Phase 96 state — copy or trim as appropriate.
    - Block 2 (lines 114-140, `PAGE-06: presenter→coordinator non-import`): DELETE entire describe block. The coordinator `bee-species-page.ts` no longer exists; the test would assert against a deleted target.
    - Block 3 (lines 142-194, `src/entries/species.ts allowlist (PAGE-04 partial)`): DELETE entire describe block. The entry file no longer exists; the readFileSync would throw ENOENT.
    - Block 4 (lines 203-224, `ARCH-04: src/lib/spa-link.ts boundary (D-05)`): KEEP unchanged. `spa-link.ts` boundary contract is independent of this phase.
    - NEW BLOCK to add: `describe('src/entries/species-index.ts allowlist (IDX-02, Phase 96)', …)` modeled on the deleted Block 3 but smaller. Allowlist contains exactly: `'../index.css'`, `'../styles/taxon-pages.css'`, `'../bee-header.ts'`, `'../bee-header'`. Forbidden-substring guard: file content must NOT contain `bee-species-page`, `bee-species-filter`, `bee-atlas`, `filter`, `wa-sqlite`, `mapbox-gl`.
    - The helper constants/functions at the top of the file (`STATIC_IMPORT_RE`, `DYNAMIC_IMPORT_RE`, `extractImports`, `FORBIDDEN`, `ROOT`) are reused — do not redefine them in the new block.
  </behavior>
  <action>
    Edit src/tests/arch.test.ts surgically:
    1. Delete the entire `describe('PAGE-06: presenter→coordinator non-import', …)` block (currently lines 114-140 in the pre-edit file; line numbers shift as deletions happen — use the describe label as the anchor).
    2. Delete the entire `describe('src/entries/species.ts allowlist (PAGE-04 partial)', …)` block (currently lines 142-194).
    3. Optionally trim the file-header comment at lines 1-14 to drop the "Plan 03 creates them" language, replacing it with a short note that this file now guards (a) `src/species/` boundary on the kept seasonality files, (b) `src/lib/spa-link.ts` boundary, (c) `src/entries/species-index.ts` allowlist.
    4. Insert a new describe block AFTER the kept `ARCH-04: src/lib/spa-link.ts boundary` block (so the file ends with the new block). Use the structure in 96-PATTERNS.md §"src/tests/arch.test.ts" → "New block to ADD". The new block's ALLOWED set is `{ '../index.css', '../styles/taxon-pages.css', '../bee-header.ts', '../bee-header' }`. The FORBIDDEN_PATTERNS array is `['bee-species-page', 'bee-species-filter', 'bee-atlas', 'wa-sqlite', 'mapbox-gl']`. (Drop the bare `'filter'` token from the research patterns list — it would false-positive against any TS file with the word "filter" in a comment; the allowlist test already gates this more precisely.) The test reads `src/entries/species-index.ts`, extracts imports with both `STATIC_IMPORT_RE` and `DYNAMIC_IMPORT_RE`, asserts the disallowed set is empty, and asserts the forbidden patterns do not appear in the source.
  </action>
  <verify>
    <automated>cd /Users/rainhead/dev/beeatlas && grep -c "describe(" src/tests/arch.test.ts</automated>
    <automated>cd /Users/rainhead/dev/beeatlas && grep -c "PAGE-06" src/tests/arch.test.ts</automated>
    <automated>cd /Users/rainhead/dev/beeatlas && grep -c "src/entries/species.ts allowlist" src/tests/arch.test.ts</automated>
    <automated>cd /Users/rainhead/dev/beeatlas && grep -c "src/entries/species-index.ts allowlist" src/tests/arch.test.ts</automated>
    <automated>cd /Users/rainhead/dev/beeatlas && grep -c "ARCH-04: src/lib/spa-link.ts boundary" src/tests/arch.test.ts</automated>
    <automated>cd /Users/rainhead/dev/beeatlas && VITEST_SKIP_BUILD=1 npx vitest run src/tests/arch.test.ts 2>&1 | tail -25</automated>
    <automated>cd /Users/rainhead/dev/beeatlas && npx tsc --noEmit -p tsconfig.json 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "describe(" src/tests/arch.test.ts` returns `3` (was 4; -2 deletions, +1 addition)
    - `grep -c "PAGE-06" src/tests/arch.test.ts` returns `0` (block deleted)
    - `grep -c "src/entries/species.ts allowlist" src/tests/arch.test.ts` returns `0` (block deleted)
    - `grep -c "src/entries/species-index.ts allowlist" src/tests/arch.test.ts` returns `1` (new block present)
    - `grep -c "ARCH-04: src/lib/spa-link.ts boundary" src/tests/arch.test.ts` returns `1` (spa-link block preserved)
    - `grep -c "ARCH-04: src/species boundary" src/tests/arch.test.ts` returns `1` (species boundary block preserved)
    - `VITEST_SKIP_BUILD=1 npx vitest run src/tests/arch.test.ts` exits 0
    - `npx tsc --noEmit` exits 0 — no broken imports anywhere in the project
  </acceptance_criteria>
  <done>arch.test.ts has three describe blocks: src/species boundary (kept), spa-link boundary (kept), and species-index allowlist (new). All vitest assertions pass; tsc is clean.</done>
</task>

<task type="auto">
  <name>Task 4: Full test + build verification gate</name>
  <files>(no files modified — verification only)</files>
  <read_first>
    - .planning/phases/96-index-page-replacement/96-RESEARCH.md (§"Sampling Rate" — confirms `npm test` is the phase gate)
  </read_first>
  <behavior>
    - `npm test` (full build + all test files) exits 0
    - `npm run build` exits 0 independently (sanity)
    - `_site/species/index.html` exists with the new structure (regression check)
    - No file under `src/` references any deleted symbol (final grep sweep)
  </behavior>
  <action>
    Run the full test suite via `npm test`. If any test fails, do NOT mark this task complete — surface the failure to the user with the failing test name and the file/line. If `species.flat | groupby("family")` ordering produces a non-alphabetical genera sequence at build time (Pitfall 1 in RESEARCH.md §"Pitfall 1"), surface the observed order so the user can decide whether to add an explicit sort in `_data/species.js` as a follow-on.
    Then sweep for any leftover production references to deleted symbols: grep `src/`, `_pages/`, `_includes/`, `_data/`, `eleventy.config.js`, `vite.config.ts`, `package.json` for `bee-species-page`, `bee-species-filter`, `bee-species-card`, `bee-taxon-nav`, `taxon-tree`, `src/entries/species\.ts`, `src/species/url-state`, `src/styles/species\.css`. Expected: zero hits in production code (test files were already cleaned in Task 2; only `src/tests/build-output.test.ts` PAGE-09 chunk comment may still mention `species-page chunk` in comments — that was relabeled in Plan 01 task 1).
  </action>
  <verify>
    <automated>cd /Users/rainhead/dev/beeatlas && npm test 2>&1 | tail -30</automated>
    <automated>cd /Users/rainhead/dev/beeatlas && grep -rn "bee-species-page\|bee-species-filter\|bee-species-card\|bee-taxon-nav" --include="*.ts" --include="*.njk" --include="*.js" --include="*.css" src _pages _includes _data eleventy.config.js vite.config.ts 2>/dev/null | head -10</automated>
    <automated>cd /Users/rainhead/dev/beeatlas && grep -rn "src/entries/species\\.ts\|src/styles/species\\.css\|src/species/url-state" --include="*.ts" --include="*.njk" --include="*.js" src _pages _includes _data eleventy.config.js vite.config.ts 2>/dev/null | head -10</automated>
    <automated>cd /Users/rainhead/dev/beeatlas && test -f _site/species/index.html && grep -c "class=\"family-section\"" _site/species/index.html</automated>
  </verify>
  <acceptance_criteria>
    - `npm test` exits 0 (full suite including build-output)
    - Grep for `bee-species-page|bee-species-filter|bee-species-card|bee-taxon-nav` in `src _pages _includes _data eleventy.config.js vite.config.ts` returns zero hits
    - Grep for `src/entries/species\.ts|src/styles/species\.css|src/species/url-state` in the same scope returns zero hits
    - `_site/species/index.html` contains `class="family-section"` (regression sanity)
  </acceptance_criteria>
  <done>Full `npm test` green; no production code references any deleted symbol; the new index page still renders.</done>
</task>

</tasks>

<verification>
- `npm test` exits 0
- `npm run build` exits 0
- `git status` shows 14 deleted files and 1 modified file (`src/tests/arch.test.ts`)
- The kept files `src/species/seasonality-viz.ts`, `src/species/seasonality-cache.ts`, `src/tests/seasonality-viz.test.ts` are untouched
</verification>

<success_criteria>
- All five v3.6 phase-96 requirements (URL-05, IDX-01..04) are satisfied across the three plans
- URL-05 is fully demonstrable: zero production code references to `bee-species-page`, `bee-species-filter`, `bee-species-card`, `bee-taxon-nav`, or `taxon-tree.njk`
- The new index page renders correctly at build time (`_site/species/index.html`)
- The test surface is consistent: deleted components have no leftover test files; the new components have new test files; arch.test.ts no longer references the old monolith
</success_criteria>

<output>
After completion, create `.planning/phases/96-index-page-replacement/96-03-SUMMARY.md`
</output>
