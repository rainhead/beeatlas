---
phase: 96-index-page-replacement
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/tests/build-output.test.ts
  - src/tests/page-scaffold.test.ts
  - src/tests/species-index.test.ts
autonomous: true
requirements:
  - URL-05
  - IDX-01
  - IDX-02
  - IDX-03
  - IDX-04
tags:
  - eleventy
  - nunjucks
  - vitest

must_haves:
  truths:
    - "Running `VITEST_SKIP_BUILD=1 npm test` shows new RED tests asserting index-page contract (IDX-01..04, URL-05)"
    - "Running `npm test` (full build) shows new RED build-output assertions for the new index page"
    - "Old PAGE-01 / PAGE-07 / PAGE-09 index-page assertions in build-output.test.ts are removed (no longer asserting <bee-species-card> on /species/)"
    - "page-scaffold.test.ts asserts the new `species-index.ts` entry path, not the old `species.ts` path"
  artifacts:
    - path: "src/tests/species-index.test.ts"
      provides: "Unit test contract for the new Nunjucks template + species-index entry"
      contains: "groupby(\"family\")"
    - path: "src/tests/build-output.test.ts"
      provides: "Build-output assertions IDX-01..04 + URL-05"
      contains: "class=\"family-section\""
    - path: "src/tests/page-scaffold.test.ts"
      provides: "Scaffold assertion for new entry path"
      contains: "species-index.ts"
  key_links:
    - from: "src/tests/build-output.test.ts"
      to: "_site/species/index.html"
      via: "readFileSync"
      pattern: "_site/species/index.html"
    - from: "src/tests/species-index.test.ts"
      to: "_pages/species.njk and src/entries/species-index.ts"
      via: "readFileSync"
      pattern: "_pages/species.njk|src/entries/species-index.ts"
---

<objective>
Wave 0 test scaffolding for Phase 96. Replace stale PAGE-01/PAGE-07/PAGE-09 index-page assertions in `build-output.test.ts` with IDX-01..04 + URL-05 assertions; flip the `page-scaffold.test.ts` entry-path assertion to point at the new `species-index.ts`; create a new `src/tests/species-index.test.ts` unit-test file asserting the template structure and JS wiring contract.

After this plan, the test suite is in a RED state — the new assertions fail because the new template, entry, and CSS modifier do not yet exist. Plan 02 makes them GREEN by rewriting the template, creating the entry, and adding CSS rules.

Purpose: Lock the IDX-01..04 + URL-05 contract in code before any implementation lands. This catches regressions (e.g. a future refactor reintroducing `bee-species-card` on the index) and proves IDX-02 wiring (event listener + hidden toggling) at the source level rather than relying on a browser-driven test.

Output: Three modified/new test files. All three should be observable as RED via `VITEST_SKIP_BUILD=1 npm test` (for the two unit-style files) and `npm test` (for the build-output file).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/96-index-page-replacement/96-CONTEXT.md
@.planning/phases/96-index-page-replacement/96-RESEARCH.md
@.planning/phases/96-index-page-replacement/96-PATTERNS.md
@.planning/phases/96-index-page-replacement/96-UI-SPEC.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Replace stale index-page tests in build-output.test.ts with IDX-01..04 + URL-05 assertions</name>
  <files>src/tests/build-output.test.ts</files>
  <read_first>
    - src/tests/build-output.test.ts (current PAGE-01 / PAGE-07 / PAGE-09 assertions on `_site/species/index.html` and the `findSpeciesChunk()` helper)
    - .planning/phases/96-index-page-replacement/96-RESEARCH.md (§"build-output.test.ts Surgery Required" — exact line numbers and replacement text)
    - .planning/phases/96-index-page-replacement/96-PATTERNS.md (§"src/tests/build-output.test.ts" — verbatim replacement test blocks)
    - .planning/phases/96-index-page-replacement/96-UI-SPEC.md (§"Page Structure" — confirms `.family-section`, `#species-filter`, link patterns)
    - _data/species.js (confirm `Agapostemon` + `Agapostemon/femoratus` exist in `species.flat` — these are the values asserted by IDX-03 / IDX-04 tests)
  </read_first>
  <behavior>
    - Test "emits _site/species/index.html with .family-section elements (IDX-01, URL-05)" — RED initially; asserts `class="family-section"` appears AND `<bee-species-page` is absent
    - Test "index page has #species-filter input (IDX-02)" — RED initially; asserts `id="species-filter"` present
    - Test "index page has genus links to /species/{Genus}/ (IDX-03)" — RED initially; asserts `href="/species/Agapostemon/"` present
    - Test "index page has species links to /species/{Genus}/{epithet}/ (IDX-04)" — RED initially; asserts `href="/species/Agapostemon/femoratus/"` present
    - Old tests "emits _site/species/index.html with one <bee-species-card>…" (PAGE-01) and "every <img> tag has loading=\"lazy\" (PAGE-07)" on the index page are removed (PAGE-07 on species detail pages stays — that test is keyed off `_site/species/Agapostemon/femoratus/index.html`)
    - PAGE-09 chunk tests renamed to assert the `species-index` chunk; existing `findSpeciesChunk()` regex `/^species-.*\.js$/` already matches `species-index-<hash>.js` and is preserved
  </behavior>
  <action>
    Delete the `PAGE-01` `<bee-species-card>` count test (currently lines ~20-24) and the `PAGE-07` lazy-loading test scoped to `_site/species/index.html` (currently lines ~26-31). Do NOT touch the later PAGE-07 carry-forward test on `_site/species/Agapostemon/femoratus/index.html` — that one stays.
    Add four new tests immediately after the `beforeAll`: one per requirement (IDX-01+URL-05, IDX-02, IDX-03, IDX-04) using `readFileSync(resolve(ROOT, '_site/species/index.html'), 'utf-8')` and the regex patterns specified in 96-PATTERNS.md §"src/tests/build-output.test.ts". The IDX-01 test asserts BOTH `/class="family-section"/` matches AND `<bee-species-page` is absent (URL-05 piggybacks here).
    Rename existing PAGE-09 test labels to reference Phase 96 (`'emits a species-index chunk distinct from index-*.js (Phase 96, IDX-02)'` and `'species-index chunk does NOT contain mapboxgl symbol (Phase 96)'`). Keep `findSpeciesChunk()` helper unchanged — its regex `/^species-.*\.js$/` matches the new `species-index-<hash>.js` chunk name.
    Use the `Agapostemon` / `Agapostemon/femoratus` species pair as the genus/species link probe — confirmed present in `_data/species.js` source data and already used in adjacent species-detail tests (lines ~76-84).
  </action>
  <verify>
    <automated>cd /Users/rainhead/dev/beeatlas && VITEST_SKIP_BUILD=1 npx vitest run src/tests/build-output.test.ts 2>&1 | head -40</automated>
    <automated>cd /Users/rainhead/dev/beeatlas && grep -c "class=\"family-section\"" src/tests/build-output.test.ts</automated>
    <automated>cd /Users/rainhead/dev/beeatlas && grep -c "bee-species-card" src/tests/build-output.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - src/tests/build-output.test.ts contains the string `class="family-section"` (IDX-01 assertion)
    - src/tests/build-output.test.ts contains the string `id="species-filter"` (IDX-02 assertion)
    - src/tests/build-output.test.ts contains the regex literal `/href="\/species\/Agapostemon\/"/` (IDX-03 assertion)
    - src/tests/build-output.test.ts contains the regex literal `/href="\/species\/Agapostemon\/femoratus\/"/` (IDX-04 assertion)
    - src/tests/build-output.test.ts contains the string `'<bee-species-page'` inside a `.not.toContain` assertion (URL-05)
    - `grep -c "bee-species-card" src/tests/build-output.test.ts` returns `0` (old PAGE-01 assertion removed)
    - `grep -c "Phase 96" src/tests/build-output.test.ts` returns at least `2` (PAGE-09 labels relabeled)
    - The species-detail PAGE-07 carry-forward test (currently around lines 86-94) is still present — `grep "PAGE-07 carry-forward" src/tests/build-output.test.ts` returns a match
    - `VITEST_SKIP_BUILD=1 npx vitest run src/tests/build-output.test.ts` exits with code 0 (the describe is skipped under SKIP_BUILD so RED tests do not run yet — sanity gate)
  </acceptance_criteria>
  <done>build-output.test.ts has IDX-01..04 + URL-05 assertions in place; the old `<bee-species-card>` count and index-page lazy-loading assertions are gone; SKIP_BUILD mode still passes (the whole describe is gated by `describe.skipIf(SKIP_BUILD)`).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Flip page-scaffold.test.ts entry-path assertion to new species-index.ts</name>
  <files>src/tests/page-scaffold.test.ts</files>
  <read_first>
    - src/tests/page-scaffold.test.ts (entire file, 22 lines — small)
    - .planning/phases/96-index-page-replacement/96-PATTERNS.md (§"src/tests/page-scaffold.test.ts" — exact before/after replacement)
  </read_first>
  <behavior>
    - The permalink + layout test (currently lines 12-15, labeled PAGE-01) remains unchanged — the new index page preserves `layout: default.njk` and `permalink: /species/index.html`
    - The entry-path test is updated: instead of asserting `/src/entries/species.ts`, it asserts `/src/entries/species-index.ts`
    - The describe label and test label are updated to reference Phase 96 instead of PAGE-04
  </behavior>
  <action>
    Update the `describe` block label from `'_pages/species.njk (PAGE-01, PAGE-04)'` to `'_pages/species.njk (Phase 96 — index page, PAGE-01)'`. Leave the first test (permalink + layout) untouched. Replace the second test: change its label from `'references the species entry script (PAGE-04)'` to `'references the species-index entry script (Phase 96)'`, and change the regex from `/<script\s+type="module"\s+src="\/src\/entries\/species\.ts"/` to `/<script\s+type="module"\s+src="\/src\/entries\/species-index\.ts"/`.
  </action>
  <verify>
    <automated>cd /Users/rainhead/dev/beeatlas && VITEST_SKIP_BUILD=1 npx vitest run src/tests/page-scaffold.test.ts 2>&1 | tail -20</automated>
    <automated>cd /Users/rainhead/dev/beeatlas && grep -c "species-index\\\\.ts" src/tests/page-scaffold.test.ts</automated>
    <automated>cd /Users/rainhead/dev/beeatlas && grep -v '^//' src/tests/page-scaffold.test.ts | grep -c "entries/species\\\\.ts"</automated>
  </verify>
  <acceptance_criteria>
    - src/tests/page-scaffold.test.ts contains the regex literal `\/src\/entries\/species-index\.ts` (asserting new entry path)
    - `grep -v '^//' src/tests/page-scaffold.test.ts | grep -c "entries/species\\\\.ts"` returns `0` outside comments (old entry path no longer asserted in test logic; comments may still mention it but the live regex is replaced)
    - First test (permalink + layout) is unchanged — `grep -c "permalink:\\\\s\\*\\\\/species\\\\/index\\\\.html" src/tests/page-scaffold.test.ts` returns `1`
    - `VITEST_SKIP_BUILD=1 npx vitest run src/tests/page-scaffold.test.ts` produces a RED result on the entry-path test (since `_pages/species.njk` still references `/src/entries/species.ts` — Plan 02 fixes this) AND a GREEN result on the permalink+layout test
  </acceptance_criteria>
  <done>page-scaffold.test.ts asserts the new `species-index.ts` entry path; permalink + layout assertion is preserved; the test file fails on entry-path until Plan 02 rewrites the template.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Create src/tests/species-index.test.ts unit test for template structure and entry-wiring contract</name>
  <files>src/tests/species-index.test.ts</files>
  <read_first>
    - src/tests/page-scaffold.test.ts (reference for `readFileSync` + `ROOT` + `fileURLToPath` pattern at the top of the new test file)
    - .planning/phases/96-index-page-replacement/96-PATTERNS.md (§"src/tests/species-index.test.ts" — verbatim target structure with two describe blocks)
    - .planning/phases/96-index-page-replacement/96-UI-SPEC.md (§"Page Structure" — confirms exact attribute names: `data-family`, `data-genus`, `data-name`, `id="species-filter"`, `type="search"`)
    - .planning/phases/96-index-page-replacement/96-RESEARCH.md (§"Pattern 2" — confirms expected JS wiring strings: `getElementById('species-filter')`, `addEventListener('input'`, `.family-section`, `.genus-row`, `hidden`)
  </read_first>
  <behavior>
    - Two describe blocks: one for `_pages/species.njk` (template-level assertions covering IDX-01..04 + URL-05) and one for `src/entries/species-index.ts` (entry-wiring contract for IDX-02)
    - Template describe asserts: permalink + layout, references `species-index.ts` script, contains `groupby("family")` AND `groupby("genus")` (IDX-01), contains `#species-filter` `type="search"` input (IDX-02), absence of `bee-species-page` AND `bee-species-card` (URL-05)
    - Entry describe asserts: imports `'../index.css'` and `'../styles/taxon-pages.css'`, calls `getElementById('species-filter')`, attaches `addEventListener('input'`, references `.family-section` + `.genus-row` selectors, toggles `hidden`
    - All tests use `readFileSync` against source files (not built output) — they belong to the fast unit suite (`VITEST_SKIP_BUILD=1 npm test`)
  </behavior>
  <action>
    Create a new file at src/tests/species-index.test.ts. Use the file-header import block from 96-PATTERNS.md §"Test File Import Block Pattern" (verbatim copy of the four imports + ROOT resolution from src/tests/page-scaffold.test.ts).
    Add two describe blocks per 96-PATTERNS.md §"src/tests/species-index.test.ts": (1) `_pages/species.njk (Phase 96 — index page, IDX-01..04)` with five tests asserting permalink+layout, script src points to `species-index.ts`, presence of `groupby("family")` AND `groupby("genus")`, `#species-filter` `type="search"` input, absence of `bee-species-page` AND `bee-species-card` substrings; (2) `src/entries/species-index.ts (IDX-02 entry wiring)` with three tests asserting CSS side-effect imports (`'../index.css'` and `'../styles/taxon-pages.css'`), `getElementById('species-filter')` + `addEventListener('input'` calls, and presence of `.family-section`, `.genus-row`, `hidden` string tokens.
    Every test uses `readFileSync(resolve(ROOT, '<path>'), 'utf-8')` and `expect(src).toContain(...)` / `expect(src).toMatch(...)`. Do NOT load or evaluate the modules — these are static source assertions only.
  </action>
  <verify>
    <automated>cd /Users/rainhead/dev/beeatlas && test -f src/tests/species-index.test.ts && echo OK</automated>
    <automated>cd /Users/rainhead/dev/beeatlas && VITEST_SKIP_BUILD=1 npx vitest run src/tests/species-index.test.ts 2>&1 | tail -30</automated>
    <automated>cd /Users/rainhead/dev/beeatlas && grep -c "describe(" src/tests/species-index.test.ts</automated>
    <automated>cd /Users/rainhead/dev/beeatlas && grep -cE "groupby\\(\"family\"\\)|groupby\\(\"genus\"\\)" src/tests/species-index.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - File src/tests/species-index.test.ts exists
    - `grep -c "describe(" src/tests/species-index.test.ts` returns `2` (two describe blocks)
    - File contains the string `groupby("family")` AND `groupby("genus")` (IDX-01 assertions)
    - File contains the string `id="species-filter"` AND `type="search"` (IDX-02 assertions on template)
    - File contains the string `getElementById('species-filter')` AND `addEventListener('input'` (IDX-02 assertions on entry)
    - File contains the string `bee-species-page` inside a `.not.toContain` assertion (URL-05)
    - File contains the string `bee-species-card` inside a `.not.toContain` assertion (URL-05)
    - File imports `readFileSync` from `'node:fs'` and resolves `ROOT` via `fileURLToPath(import.meta.url)`
    - `VITEST_SKIP_BUILD=1 npx vitest run src/tests/species-index.test.ts` runs and the entry-block tests all FAIL with ENOENT or content-mismatch (entry file does not exist yet); the template-block tests fail with content mismatch (current `_pages/species.njk` still uses old structure). This is the desired RED state — Plan 02 turns it GREEN.
  </acceptance_criteria>
  <done>src/tests/species-index.test.ts exists with two describe blocks, eight tests total (five template + three entry), all currently RED. Plan 02 will create the entry file and rewrite the template to satisfy all eight.</done>
</task>

</tasks>

<verification>
- `VITEST_SKIP_BUILD=1 npx vitest run src/tests/species-index.test.ts src/tests/page-scaffold.test.ts` runs and reports the expected RED tests (entry file missing, template still old)
- `grep -c "bee-species-card" src/tests/build-output.test.ts` returns `0`
- `grep -c "Phase 96" src/tests/build-output.test.ts src/tests/page-scaffold.test.ts src/tests/species-index.test.ts` returns at least `4`
- No other test files are modified by this plan
</verification>

<success_criteria>
- All three test files contain the IDX-01..04 + URL-05 contract assertions enumerated in must_haves.truths
- The full RED state is verifiable via `VITEST_SKIP_BUILD=1 npm test` showing failing assertions on the new index-page contract (template not yet rewritten, entry not yet created)
- The build-output describe block remains skippable via `VITEST_SKIP_BUILD=1` so local fast-feedback runs do not require a full build
- No production code (Nunjucks templates, TS sources, CSS) is touched — that is Plan 02's job
</success_criteria>

<output>
After completion, create `.planning/phases/96-index-page-replacement/96-01-SUMMARY.md`
</output>
