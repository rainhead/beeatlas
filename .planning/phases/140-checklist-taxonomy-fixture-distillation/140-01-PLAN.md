---
phase: 140-checklist-taxonomy-fixture-distillation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - data/checklist_pipeline.py
  - data/resolve_checklist_names.py
  - data/tests/fixtures/checklist_sample.csv
  - data/tests/fixtures/taxa_subset.csv.gz
autonomous: true
requirements:
  - TFIXTURE-01
  - TFIXTURE-02
  - TFIXTURE-04

must_haves:
  truths:
    - "load_checklist() accepts an optional injected connection; called with no args it behaves exactly as before (creates+closes its own DB_PATH connection)"
    - "resolve_checklist_names.py exposes a module-level TAXA_PATH constant that resolve_checklist_names() uses instead of an inline path string"
    - "data/tests/fixtures/ contains a distilled checklist_sample.csv covering all 4 coord_flag and all 3 date_quality branches plus a slash-compound row, with provenance recorded in a header comment"
    - "data/tests/fixtures/ contains a 2-row taxa_subset.csv.gz with the Agapostemon angelicus/texanus ancestry rows for the LCA test"
    - "The existing (unmodified) test_checklist_pipeline.py and test_resolve_checklist_names.py fast-tier tests stay green after the production-code seam changes (behavior preserved)"
  artifacts:
    - path: "data/checklist_pipeline.py"
      provides: "load_checklist(con=None) connection-injection seam with _owns_connection guard"
      contains: "_owns_connection"
    - path: "data/resolve_checklist_names.py"
      provides: "module-level TAXA_PATH constant"
      contains: "TAXA_PATH ="
    - path: "data/tests/fixtures/checklist_sample.csv"
      provides: "distilled 8-row checklist sample with provenance header comment"
      contains: "Distilled from"
    - path: "data/tests/fixtures/taxa_subset.csv.gz"
      provides: "2-row Anthophila ancestry subset for angelicus/texanus LCA"
      min_lines: 1
  key_links:
    - from: "data/resolve_checklist_names.py:resolve_checklist_names"
      to: "data/resolve_checklist_names.py:TAXA_PATH"
      via: "_load_anthophila_ancestry(TAXA_PATH)"
      pattern: "_load_anthophila_ancestry\\(TAXA_PATH\\)"
    - from: "data/checklist_pipeline.py:load_checklist"
      to: "injected con or duckdb.connect(DB_PATH)"
      via: "_owns_connection = con is None guard"
      pattern: "_owns_connection"
---

<objective>
Build the two production-code seams and the two committed fixtures that the test rewrites (Plan 02) depend on. This plan creates the prerequisites; it does NOT rewrite any test (that is Plan 02).

Three deliverables:
1. **load_checklist() connection seam** (TFIXTURE-01 / D-05) — add an optional `con` parameter so a test can inject a shared in-memory connection; the nightly path (no args) is behavior-preserving.
2. **TAXA_PATH constant extraction** (TFIXTURE-02 / D-06) — promote the inline `taxa_path` local in `resolve_checklist_names()` to a module-level `TAXA_PATH` constant so tests can monkeypatch it.
3. **Committed fixtures** (TFIXTURE-01/02/04 / D-08, D-10) — `data/tests/fixtures/checklist_sample.csv` (8 rows, all branches) and `data/tests/fixtures/taxa_subset.csv.gz` (2 ancestry rows), each with provenance.

Purpose: The real nightly pipeline must remain byte-for-byte unchanged in behavior. The seams default to the existing behavior; the fixtures are inert until Plan 02 wires them into the new module-scoped fixture.

Output: 2 modified production modules (minimal, behavior-preserving diffs), a new `data/tests/fixtures/` directory with 2 committed fixture files.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/140-checklist-taxonomy-fixture-distillation/140-CONTEXT.md
@.planning/phases/140-checklist-taxonomy-fixture-distillation/140-RESEARCH.md
@.planning/phases/140-checklist-taxonomy-fixture-distillation/140-PATTERNS.md
@.planning/REQUIREMENTS.md
@./CLAUDE.md

<scope_fence>
STRICTLY OUT OF SCOPE here (these are Plan 02 or later phases):
- Rewriting any test (the module-scoped fixture, the assertion rewrites, the TAXA_PATH monkeypatch in tests) — Plan 02.
- dbt/parquet fixtures (TFIXTURE-03, Phase 141); fixing red tests (TFIX-*, Phase 141); bulk integration tagging (TTIER-02, Phase 141).
- Any change to run.py / nightly.sh / dbt models / the 33-column contract.
- TFIXTURE-05 (broadening session scope to other DuckDB builders).
This plan touches ONLY the two production modules and the two new fixture files. Do NOT modify any test file in this plan.
</scope_fence>

<interfaces>
<!-- Exact seam sites, verified from codebase 2026-06-06. -->

data/checklist_pipeline.py module-level constants (lines 29-44):
    DB_PATH = os.environ.get("DB_PATH", str(Path(__file__).parent / "beeatlas.duckdb"))
    CHECKLIST_PATH = Path(__file__).parent / "checklists" / "wa_bee_checklist.tsv"
    CHECKLIST_RECORDS_PATH = Path(__file__).parent / "checklists" / "wa_bee_checklist_records.tsv"
    CHECKLIST_RECORDS_FULL_PATH = Path(__file__).parent / "checklists" / "checklist_records_full.csv"
    TAXA_PATH = Path(__file__).parent / "raw" / "taxa.csv.gz"
    _TAXA_ANCESTRY: dict[str, dict] | None = None

data/checklist_pipeline.py load_checklist() current shape (lines 518-597):
    def load_checklist() -> None:
        con = duckdb.connect(DB_PATH)          # line 521 — owns lifecycle
        try:
            ... (CREATE SCHEMA checklist_data, table loads, _update_occurrences_canonical_name(con)) ...
        finally:
            con.close()                        # lines 596-597 — always closes
All sub-functions already take con as a parameter and do NOT close it.

data/checklist_pipeline.py WA bbox constants (verified — drives coord_flag):
    _WA_LAT_MIN = 45.5 ; _WA_LAT_MAX = 49.0 ; _WA_LON_MIN = -124.85 ; _WA_LON_MAX = -116.9
    coord_flag logic (lines 261-271): null_coord if either lat/lon None;
    zero_coord if lat==0 AND lon==0; valid if within bbox; else out_of_bbox.

data/resolve_checklist_names.py constants block (lines 37-40) and inline path (lines 268-269):
    DB_PATH = ...
    AUDIT_CSV = Path(__file__).parent / "checklist_name_resolution_audit.csv"
    FUZZY_REVIEW_CSV = Path(__file__).parent / "checklist_fuzzy_review.csv"
    GBIF_SEED_CSV = Path(__file__).parent / "dbt" / "seeds" / "gbif_checklist_synonyms.csv"
    ...
    taxa_path = str(Path(__file__).parent / "raw" / "taxa.csv.gz")   # line 268 — inline local
    taxa = _load_anthophila_ancestry(taxa_path)                       # line 269

_load_anthophila_ancestry(taxa_path) reads a tab-delimited gz via csv.DictReader with
columns: taxon_id, ancestry, rank_level, rank, name, active (resolve_checklist_names.py:66-98).
checklist_pipeline._load_taxa_ancestry() reads the SAME tab-delimited gz via csv.reader
(positional row[:6]: taxon_id, ancestry, rank_level, rank, name, active) (checklist_pipeline.py:61-78).
Both expect a header row.

CSV schema that _load_checklist_records_full() reads via csv.DictReader (checklist_pipeline.py:396-515):
    ObjectID,Family,Genus,Scientific Name,Locality,Latitude,Longitude,Date,recordedBy,County_join,x,y
(x and y columns are ignored; only Latitude/Longitude used.)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add the connection-injection seam to load_checklist() and extract TAXA_PATH in resolve_checklist_names.py</name>
  <files>data/checklist_pipeline.py, data/resolve_checklist_names.py</files>
  <read_first>
    - data/checklist_pipeline.py (lines 29-44 constants; 518-597 load_checklist body — the file being modified)
    - data/resolve_checklist_names.py (lines 37-40 constants block; 268-269 inline taxa_path — the file being modified)
    - .planning/phases/140-checklist-taxonomy-fixture-distillation/140-PATTERNS.md (§checklist_pipeline.py and §resolve_checklist_names.py — exact target signatures and diffs)
    - .planning/phases/140-checklist-taxonomy-fixture-distillation/140-RESEARCH.md (Critical Finding #1 Option A; Finding #6; Pattern 2 and Pattern 3)
  </read_first>
  <action>
    Two minimal, behavior-preserving production edits (D-05, D-06):

    (A) In `data/checklist_pipeline.py`, change `load_checklist()` to accept an optional injected connection. Change the signature from `def load_checklist() -> None:` to `def load_checklist(con: "duckdb.DuckDBPyConnection | None" = None) -> None:`. Immediately at the top of the body, add `_owns_connection = con is None` and wrap the existing `con = duckdb.connect(DB_PATH)` so it only runs when `_owns_connection` is True. In the existing `finally:` block, guard the close: `if _owns_connection: con.close()`. Leave the entire try-body (CREATE SCHEMA checklist_data, all table loads, `_update_occurrences_canonical_name(con)`) byte-for-byte unchanged. Keep the existing docstring; you may append one sentence noting the optional `con` injection. The nightly path calls `load_checklist()` with no arguments → identical behavior (creates and closes its own DB_PATH connection).

    (B) In `data/resolve_checklist_names.py`, add a module-level constant `TAXA_PATH = str(Path(__file__).parent / "raw" / "taxa.csv.gz")` in the constants block alongside `AUDIT_CSV` / `FUZZY_REVIEW_CSV` / `GBIF_SEED_CSV` (after line 40). Keep it a `str` (matches current usage — `gzip.open` accepts str). Then in `resolve_checklist_names()`, delete the inline `taxa_path = str(...)` local (line 268) and change line 269 to `taxa = _load_anthophila_ancestry(TAXA_PATH)`. No other call sites change.

    Do NOT touch any test file, run.py, nightly.sh, dbt, or the `_load_taxa_ancestry`/`_load_anthophila_ancestry` reader bodies. Keep both diffs as small as possible.
  </action>
  <acceptance_criteria>
    - `grep -n "def load_checklist" data/checklist_pipeline.py` shows the signature contains `con` and a default of `None`
    - `data/checklist_pipeline.py` contains `_owns_connection` and the close in `finally:` is guarded by `if _owns_connection`
    - `grep -c "^TAXA_PATH =" data/resolve_checklist_names.py` returns 1 (new module-level constant)
    - `data/resolve_checklist_names.py:resolve_checklist_names` calls `_load_anthophila_ancestry(TAXA_PATH)` (no inline `taxa_path = str(` local remains in that function)
    - No test file, run.py, nightly.sh, or dbt file is modified by this task
    - The existing fast-tier tests in both touched modules still pass unchanged (behavior preserved) — proves the seams are inert by default
  </acceptance_criteria>
  <verify>
    <automated>cd data && grep -q "_owns_connection" checklist_pipeline.py && grep -q "^TAXA_PATH =" resolve_checklist_names.py && grep -q "_load_anthophila_ancestry(TAXA_PATH)" resolve_checklist_names.py && uv run pytest tests/test_resolve_checklist_names.py -q && echo SEAMS_OK</automated>
  </verify>
  <done>load_checklist() takes an optional con with an _owns_connection guard (nightly path unchanged); resolve_checklist_names.py has a module-level TAXA_PATH the function now uses; the existing resolve_checklist_names tests stay green.</done>
</task>

<task type="auto">
  <name>Task 2: Author the two committed fixtures with provenance (checklist_sample.csv + taxa_subset.csv.gz)</name>
  <files>data/tests/fixtures/checklist_sample.csv, data/tests/fixtures/taxa_subset.csv.gz</files>
  <read_first>
    - .planning/phases/140-checklist-taxonomy-fixture-distillation/140-PATTERNS.md (§checklist_sample.csv — 8-row spec, provenance header, exact post-load counts table; §taxa_subset.csv.gz — 2-row content + schema + how both modules read it)
    - .planning/phases/140-checklist-taxonomy-fixture-distillation/140-RESEARCH.md (§4 branch enumeration; §6 taxa rows; Open Question #3 — ObjectID 147 is VALID not out_of_bbox, use ObjectID 8702 as the sole out_of_bbox row)
    - data/checklist_pipeline.py (lines 179-271 bbox + coord_flag logic; lines 396-515 _load_checklist_records_full to confirm CSV column reading; lines 61-78 _load_taxa_ancestry gz reader)
    - data/tests/fixtures.py (existing provenance-docstring style to mirror)
  </read_first>
  <action>
    Create `data/tests/fixtures/` and author two committed fixture files (D-08 minimal one-row-per-branch; D-10 per-fixture provenance).

    (A) `data/tests/fixtures/checklist_sample.csv` — 8 data rows + the standard 12-column header `ObjectID,Family,Genus,Scientific Name,Locality,Latitude,Longitude,Date,recordedBy,County_join,x,y`. Precede the header with comment lines (lines beginning with `#`) recording provenance per D-10: that it was distilled from `data/checklists/checklist_records_full.csv` (2026-06-06), the ObjectID→branch mapping, and the note that ObjectID 147 is VALID (not out_of_bbox) so 8702 is the sole out_of_bbox row. NOTE: confirm `_load_checklist_records_full()` uses `csv.DictReader` and tolerates leading `#` comment lines; if it does NOT skip comment lines, put the provenance in a sibling `checklist_sample.csv.README` or a leading comment style the DictReader ignores — your discretion, but the loaded data MUST be exactly the 8 rows below and provenance MUST be recorded somewhere in `data/tests/fixtures/`. The 8 rows (ObjectID, coord_flag, date_quality):
      - 1: valid, full (ISO datetime, e.g. 1991-07-12T00:00:00), Agapostemon angelicus, lat=47.3075 lon=-122.2272
      - 3: null_coord, full (empty lat/lon, date present 1957-08-06T00:00:00)
      - 17423: zero_coord, none (lat=0 lon=0, empty date)
      - 8702: out_of_bbox, none (lat=49.00003811 > 49.0, empty date)
      - 31311: valid, full (pre-1900 ISO 1812-06-18T00:00:00 → year=1812)
      - 1668: valid, full (M/D/YYYY 6/14/1905 → year=1905)
      - 1386: valid, none (slash verbatim_name `Agapostemon angelicus/texanus`, lat=48.53459 lon=-122.9235)
      - 99999: valid, year_only (synthetic; date=`1995`; the year_only branch has 0 real rows per code comment — synthetic row preserves branch coverage per D-08)
    These yield exact post-load counts (Plan 02 asserts these): total=8; coord_flag valid=5, null_coord=1, zero_coord=1, out_of_bbox=1; date_quality full=4 (ObjectIDs 1,3,31311,1668), none=3 (ObjectIDs 17423,8702,1386), year_only=1 (ObjectID 99999).

    (B) `data/tests/fixtures/taxa_subset.csv.gz` — a gzipped, tab-delimited file with header `taxon_id	ancestry	rank_level	rank	name	active` and exactly 2 data rows (the angelicus/texanus ancestry from RESEARCH §6, LCA node 606634):
      - 270393  (ancestry .../630955/49707/134106/335597/1597677/50086/606634)  10  species  Agapostemon angelicus  true
      - 1581468 (ancestry .../630955/49707/134106/335597/1597677/50086/606634/1581466)  10  species  Agapostemon texanus  true
    Use the exact ancestry strings from RESEARCH §6 / PATTERNS §taxa_subset. Generate the gz with a short `uv run python` one-liner using `gzip.open(..., "wt")` + `csv.writer(delimiter="\t")` (do NOT commit a Python generator script — author the file directly). Record this fixture's provenance in a sibling docstring/comment location in `data/tests/fixtures/` (a `.README` or comment file) since a gz cannot carry an inline comment — Plan 02 also repeats this provenance in the fixture docstring per D-10.

    Verify both fixtures load through the REAL code paths by a throwaway check (do not commit it): point checklist_pipeline.CHECKLIST_RECORDS_FULL_PATH at the sample and confirm 8 rows load; gzip-read the taxa subset and confirm 2 rows + header.
  </action>
  <acceptance_criteria>
    - `data/tests/fixtures/` directory exists
    - `data/tests/fixtures/checklist_sample.csv` exists with the 12-column header and exactly 8 data rows (ObjectIDs 1, 3, 17423, 8702, 31311, 1668, 1386, 99999)
    - Provenance text "Distilled from" referencing checklist_records_full.csv is present somewhere in data/tests/fixtures/ (D-10)
    - `data/tests/fixtures/taxa_subset.csv.gz` exists; `gzip -dc` of it yields a tab-delimited header line plus exactly 2 data rows for taxon_id 270393 and 1581468
    - Both ancestry strings contain `/606634` (the angelicus/texanus subgenus LCA node)
    - No test file is modified by this task
  </acceptance_criteria>
  <verify>
    <automated>cd data && test -d tests/fixtures && python3 -c "import csv;rows=[r for r in csv.DictReader([l for l in open('tests/fixtures/checklist_sample.csv') if not l.lstrip().startswith('#')])];ids=sorted(int(r['ObjectID']) for r in rows);assert ids==[1,3,1386,1668,8702,17423,31311,99999], ids;assert len(rows)==8" && python3 -c "import gzip,csv;rows=list(csv.reader(gzip.open('tests/fixtures/taxa_subset.csv.gz','rt'),delimiter='\t'));assert rows[0][0]=='taxon_id';data=rows[1:];assert len(data)==2,len(data);assert {data[0][0],data[1][0]}=={'270393','1581468'};assert all('606634' in r[1] for r in data)" && grep -rq "Distilled from" tests/fixtures/ && echo FIXTURES_OK</automated>
  </verify>
  <done>data/tests/fixtures/ holds the 8-row checklist_sample.csv (all coord_flag + date_quality branches + slash row) and the 2-row taxa_subset.csv.gz (angelicus/texanus LCA), each with recorded provenance; both load through the real reader code paths.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none) | This plan introduces no new trust boundary. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-140-01 | (n/a) | optional `con` param on load_checklist() + module-level TAXA_PATH constant + 2 committed test-fixture files (CSV + gz) | accept | No security-relevant attack surface. The seam adds an optional parameter consumed only by tests; the nightly path is unchanged. The fixtures are static committed test data with no new network, secrets, auth, or untrusted-input parsing. ASVS L1: nothing to mitigate. (Mirrors Phase 139 T-139-01.) |

No HIGH findings. No new package installs (no Package Legitimacy Gate needed).
</threat_model>

<verification>
Seam + fixture gates (cheap, seconds):

```
cd data && grep -q "_owns_connection" checklist_pipeline.py
cd data && grep -q "^TAXA_PATH =" resolve_checklist_names.py && grep -q "_load_anthophila_ancestry(TAXA_PATH)" resolve_checklist_names.py
cd data && uv run pytest tests/test_resolve_checklist_names.py tests/test_checklist_pipeline.py -m 'not integration' -q   # existing tests stay green (behavior preserved)
cd data && test -d tests/fixtures && gzip -dc tests/fixtures/taxa_subset.csv.gz | wc -l   # 3 lines (header + 2 rows)
```
</verification>

<success_criteria>
- load_checklist() accepts optional `con`; nightly path (no args) unchanged; `_owns_connection` guard present
- resolve_checklist_names.py has a module-level TAXA_PATH used by resolve_checklist_names()
- data/tests/fixtures/ exists with checklist_sample.csv (8 branch-covering rows) and taxa_subset.csv.gz (2 ancestry rows), both with recorded provenance (D-10)
- Existing fast-tier tests in both touched modules remain green (the seams are inert by default — behavior preserved)
- Scope fence honored: no test file rewritten, no run.py/nightly.sh/dbt changes
</success_criteria>

<output>
Create `.planning/phases/140-checklist-taxonomy-fixture-distillation/140-01-SUMMARY.md` when done.
</output>
