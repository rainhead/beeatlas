---
phase: 142-verify-budget-green-suite-nightly-wiring
plan: 02
type: execute
wave: 2
depends_on: ["142-01"]
files_modified:
  - data/nightly.sh
autonomous: true
requirements: [TTIER-03]
user_setup: []
must_haves:
  truths:
    - "nightly.sh pulls last-night's published artifacts into <repo_root>/public/data/ before run.py so test_dbt_diff compares fresh sandbox vs currently-live data"
    - "nightly.sh runs `uv run pytest -m integration` after run.py builds fresh dbt artifacts and before the export hashing/upload block"
    - "A failing integration test makes nightly.sh exit non-zero before the S3 push, so stale data stays live and the healthcheck ping is skipped"
    - "The existing EXIT-trap DuckDB/taxa.csv.gz backup still fires on the integration-gate failure exit"
  artifacts:
    - path: "data/nightly.sh"
      provides: "Integration-tier hard gate + pre-run public/data baseline pull (TTIER-03, D-01/D-01a/D-01b)"
      contains: "uv run pytest -m integration"
  key_links:
    - from: "data/nightly.sh block 1c"
      to: "<repo_root>/public/data/"
      via: "aws s3 cp of manifest-resolved hashed artifacts"
      pattern: "public/data"
    - from: "data/nightly.sh block 2b"
      to: "publish/upload block"
      via: "exit 1 on integration failure before hashing/upload"
      pattern: "uv run pytest -m integration"
---

<objective>
Wire the @integration (dataset-validation) tier into `data/nightly.sh` as a
hard gate that blocks the S3 publish on failure, with a pre-run S3 pull that
makes `test_dbt_diff` assert (not silently skip) against last-night's live data.

Purpose: Satisfies TTIER-03 — the nightly cron runs the slow tier against real
built data and surfaces failures (non-zero exit / logged). Implements locked
decisions D-01 (hard gate blocks publish), D-01a (gate after dbt build, before
publish; pre-run public/data baseline), and D-01b (ALL @integration tests gate).

Output: `data/nightly.sh` with two new blocks — 1c (pre-run published-artifact
pull to `public/data/`) and 2b (integration gate that exits non-zero before the
hashing/upload block) — plus a documented expected first-run `test_dbt_diff`
failure (self-heals on the second run after the new schema publishes).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/142-verify-budget-green-suite-nightly-wiring/142-CONTEXT.md
@.planning/phases/142-verify-budget-green-suite-nightly-wiring/142-RESEARCH.md
@.planning/phases/142-verify-budget-green-suite-nightly-wiring/142-PATTERNS.md
@.planning/phases/142-verify-budget-green-suite-nightly-wiring/142-01-SUMMARY.md

<interfaces>
<!-- Extracted from data/nightly.sh. Executor uses these directly. -->

nightly.sh env/vars (lines 30-44): set -euo pipefail; export PATH; SCRIPT_DIR; REPO_ROOT;
  BUCKET; DISTRIBUTION_ID; HEALTHCHECK_URL; DB_S3_KEY; DB_PATH=/tmp/beeatlas.duckdb;
  EXPORT_DIR=/tmp/beeatlas-export; AWS_PROFILE=beeatlas; TAXA_S3_KEY; TAXA_PATH.
nightly.sh helpers (lines 46-48): _ts(); _hash(); _elapsed() { echo $(( $(date +%s) - $1 ))s; }
nightly.sh EXIT trap (lines 85-96): backs up DuckDB + taxa.csv.gz + taxa_cache.json to S3 with `|| true`
  on each copy — fires on any exit (incl. exit 1) without masking the exit code. DO NOT MODIFY.

Existing 1b taxa pull block (lines 107-116) — analog for new block 1c; graceful-miss pattern:
  if ! aws --profile "$AWS_PROFILE" s3 cp --no-progress "s3://$BUCKET/$KEY" "$DEST" 2>/dev/null; then echo "..."; fi

Existing pipelines block (lines 118-125) — block 1c inserts BEFORE this (after line 116),
  block 2b inserts AFTER this (after line 125), before line 127/135 "--- hashing and uploading exports ---":
  # 2. Run pipelines ; export DB_PATH EXPORT_DIR ; cd "$SCRIPT_DIR" ; uv run python run.py

Manifest schema (nightly.sh writes it, lines 186-202) — keys → content-hashed filenames:
  occurrences, occurrences_db, species (= hashed species.json), seasonality, higher_taxa,
  counties, ecoregions, places, places_meta, checklist, photos, occurrences_db_tables, generated_at.
  NOTE (A3): the `species` manifest key is the hashed species.json, NOT species.parquet.

test_dbt_diff.py path constants (data/tests/test_dbt_diff.py lines 26-33):
  SANDBOX = <repo_root>/data/dbt/target/sandbox  (populated by run.py — present at gate time)
  PUBLIC  = <repo_root>/public/data              (gitignored; ABSENT in nightly unless block 1c pulls it)
  _SANDBOX_GUARD = skipif(not (SANDBOX/occurrences.parquet).exists()) — sandbox present ⇒ no skip;
  but if PUBLIC/occurrences.parquet is absent the diff read fails/skips → vacuous gate (Pitfall 1).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add block 1c — pre-run S3 pull of published artifacts into public/data/</name>
  <files>data/nightly.sh</files>
  <read_first>
    - data/nightly.sh (lines 27-44 env/vars; lines 85-96 EXIT trap; lines 107-116 the 1b taxa pull analog; lines 186-205 manifest schema/upload)
    - .planning/phases/142-verify-budget-green-suite-nightly-wiring/142-PATTERNS.md (block 1c section lines 23-91 — exact code pattern; graceful S3 miss lines 342-350)
    - .planning/phases/142-verify-budget-green-suite-nightly-wiring/142-RESEARCH.md (D-01a S3 baseline pull lines 368-412; Pitfall 1 vacuous skip; Open Question 3 / A3 species.parquet manifest gap)
    - data/tests/test_dbt_diff.py (lines 26-33 — PUBLIC/SANDBOX constants the pull must satisfy)
  </read_first>
  <action>
    Insert a new block 1c into `data/nightly.sh` AFTER the 1b taxa.csv.gz pull (after line 116) and BEFORE `# 2. Run pipelines` (line 118). The block: echo a `--- pulling published artifacts for integration baseline ---` header, set `_t0=$(date +%s)`, `mkdir -p "$REPO_ROOT/public/data"`, then attempt `aws --profile "$AWS_PROFILE" s3 cp --no-progress "s3://$BUCKET/data/manifest.json" "/tmp/beeatlas-prev-manifest.json" 2>/dev/null`. On success, invoke `uv run python3 -c "..."` (per RESEARCH "Don't Hand-Roll" — do not parse JSON in bash) that loads the manifest and, for each of `occurrences.parquet→manifest['occurrences']`, `counties.geojson→manifest['counties']`, `ecoregions.geojson→manifest['ecoregions']`, `species.json→manifest['species']`, `seasonality.json→manifest['seasonality']`, runs `aws s3 cp s3://$BUCKET/data/<hashed> $REPO_ROOT/public/data/<local>` via subprocess, printing a WARN to stderr on any per-file miss (use `.get()` and skip falsy values). Wrap the python invocation with `2>&1 || true` so a partial pull never aborts the pipeline. End with `echo "published artifact pull done in $(_elapsed $_t0)"`. On manifest-pull failure (first run), `echo "WARN: no manifest.json in S3 (first run) — test_dbt_diff will skip (not fail)"`.

    Document the A3 / Open-Question-3 reality in a comment in this block: the manifest `species` key points to the hashed `species.json` (not `species.parquet`); so `species.parquet`-dependent diff tests in the integration tier may skip in nightly because `public/data/species.parquet` is not pulled here — that is acceptable (those tests skip, they do not fail; the non-parquet diffs still gate). Do NOT attempt to invent a species.parquet manifest key.

    Do not touch the EXIT trap. Pass `cd data && bash -n nightly.sh` (syntax check) — a full nightly run requires maderas + S3 and is the manual verification (see VALIDATION.md Manual-Only).
  </action>
  <verify>
    <automated>cd data && bash -n nightly.sh</automated>
  </verify>
  <acceptance_criteria>
    - `data/nightly.sh` contains a `1c` block positioned after the `1b. Pull taxa.csv.gz` block and before `# 2. Run pipelines`
    - The block contains `mkdir -p "$REPO_ROOT/public/data"` and `s3 cp ... s3://$BUCKET/data/manifest.json`
    - The block resolves hashed artifacts via `uv run python3 -c` (no bash JSON parsing) and pulls at least occurrences.parquet, counties.geojson, ecoregions.geojson, species.json, seasonality.json into `$REPO_ROOT/public/data/`
    - The first-run (no manifest) path echoes a WARN and does not abort (uses `2>/dev/null` + graceful branch)
    - A comment documents that the `species` manifest key is species.json (not species.parquet) per A3
    - `cd data && bash -n nightly.sh` exits 0 (valid bash syntax)
    - The EXIT trap (lines 85-96) is unchanged
  </acceptance_criteria>
  <done>nightly.sh populates public/data/ from last-night's live manifest before run.py, giving test_dbt_diff a real regression baseline instead of a vacuous skip.</done>
</task>

<task type="auto">
  <name>Task 2: Add block 2b — integration hard gate before publish; document first-run diff behavior</name>
  <files>data/nightly.sh</files>
  <read_first>
    - data/nightly.sh (lines 118-125 the run.py block — analog for 2b; lines 127-135 the hashing/upload header 2b precedes; lines 85-96 EXIT trap; lines 213-225 invalidate + healthcheck tail)
    - .planning/phases/142-verify-budget-green-suite-nightly-wiring/142-PATTERNS.md (block 2b section lines 95-128 — exact code pattern)
    - .planning/phases/142-verify-budget-green-suite-nightly-wiring/142-RESEARCH.md (D-01 gate insertion lines 218-237; Pitfall 7 -x vs full inventory; Open Question 2 — expected first-run test_dbt_diff failure self-heals; D-01b all integration tests gate)
    - .planning/phases/142-verify-budget-green-suite-nightly-wiring/142-CONTEXT.md (D-01/D-01a/D-01b locked decisions)
  </read_first>
  <action>
    Insert a new block 2b into `data/nightly.sh` AFTER `echo "--- pipelines done in $(_elapsed $_t0) ---"` (line 125) and BEFORE the `# 3. Hash artifacts...` / `echo "--- hashing and uploading exports ---"` block (line 127/135). The block: a header comment stating this is a hard gate that blocks the S3 publish on any @integration failure (D-01/D-01b), and that dropping `-x` would give a full failure inventory at the cost of slower abort (Pitfall 7). Then echo `--- integration test gate ---`, `_t0=$(date +%s)`, `cd "$SCRIPT_DIR"`, and `if ! uv run pytest -m integration -x --tb=short -q; then echo "INTEGRATION GATE FAILED in $(_elapsed $_t0) — aborting publish" >&2; exit 1; fi`, then `echo "integration gate passed in $(_elapsed $_t0)"`. The explicit `exit 1` (clearer than relying on `set -e`) blocks the publish; because the gate is before the hashing/upload + CloudFront invalidate + healthcheck-ping tail, a failure leaves stale data live and skips the ping (monitoring catches it) — exactly D-01. The EXIT trap still fires on `exit 1`, preserving the DuckDB/taxa backup (D-01a) — do not modify it.

    Add a clearly-marked comment block (near 2b) documenting the EXPECTED first-run behavior per Open Question 2: on the FIRST nightly after the Phase 131 schema change, `test_dbt_diff` WILL fail because the currently-live `public/data/occurrences.parquet` carries the OLD schema while the fresh sandbox carries the new 33-col schema — this is correct regression behavior, not a defect. The operator should reconcile by allowing one publish of the new schema (e.g. a one-time gate bypass or manual upload), after which the SECOND run compares new-vs-new and the gate self-heals. State this so the operator does not mistake the first-run block for a bug.

    Validate syntax: `cd data && bash -n nightly.sh`. Structurally confirm ordering: the `uv run pytest -m integration` invocation must appear before the `--- hashing and uploading exports ---` line. (A live forced-failure dry-run on maderas is the manual verification per VALIDATION.md Manual-Only — note it in the SUMMARY for the operator.)
  </action>
  <verify>
    <automated>cd data && bash -n nightly.sh && grep -n "uv run pytest -m integration" nightly.sh</automated>
  </verify>
  <acceptance_criteria>
    - `data/nightly.sh` contains `uv run pytest -m integration -x --tb=short -q` inside a `2b` block
    - That invocation is positioned AFTER `--- pipelines done` and BEFORE the `--- hashing and uploading exports ---` block (verified by line order)
    - The gate exits non-zero (`exit 1`) on failure before any S3 upload / CloudFront invalidate / healthcheck ping
    - A comment documents the expected first-run `test_dbt_diff` schema-change failure and that it self-heals on the second run (Open Question 2)
    - The EXIT trap (lines 85-96) is unchanged so the DuckDB/taxa backup still fires on the gate-failure exit
    - `cd data && bash -n nightly.sh` exits 0
  </acceptance_criteria>
  <done>nightly.sh runs the full @integration tier after the dbt build and before publish; any failure exits non-zero and blocks the data refresh while preserving the EXIT-trap backup.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| S3 (CloudFront origin) → maderas filesystem | Block 1c pulls published artifacts (manifest + content-hashed files) from the project's own S3 bucket into `$REPO_ROOT/public/data/` |
| integration gate → publish step | Block 2b is the control point deciding whether fresh data is published; a vacuous pass (skip-not-assert) is the primary risk |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-142-01 | Tampering / Elevation | integration gate vacuous pass (test_dbt_diff skips instead of asserts) | mitigate | Block 1c populates `public/data/` so the diff asserts; SANDBOX is present post-run.py so guards don't skip; gate runs the full tier (D-01b). Plan 01 fixed test_at_least_13 so the tier is genuinely green, not green-by-skip |
| T-142-04 | Spoofing / Tampering | S3 artifact pull (block 1c) | accept | Pull uses the existing named profile `--profile beeatlas` (no embedded creds) from the project's own bucket; manifest parsed by `json.load` from trusted S3; writes only to the local (non-served) repo checkout — no injection path (RESEARCH Security Domain) |
| T-142-05 | Information Disclosure / DoS | manifest path traversal from remote filenames | mitigate | Resolved filenames are joined under a fixed `dest=$REPO_ROOT/public/data` with fixed local basenames (occurrences.parquet, etc.); the remote value is only the S3 key suffix, never the local write path — a malicious hashed name cannot escape public/data/ |
| T-142-06 | Denial of Service | integration tier runtime under maderas SIGKILL | accept | The slow tier is nightly cron (not the orchestrator's foreground Bash); A4 flags the 50k-row load runtime — note observed duration in SUMMARY for BASELINE follow-up |
</threat_model>

<verification>
- `cd data && bash -n nightly.sh` → exit 0 (both blocks syntactically valid).
- `grep -n "uv run pytest -m integration" data/nightly.sh` → match positioned before `--- hashing and uploading exports ---`.
- `grep -n "public/data" data/nightly.sh` → block 1c writes the baseline.
- Manual (VALIDATION.md Manual-Only): on maderas, a forced integration failure produces a non-zero exit and blocks the publish, observable in the nightly log; the EXIT-trap DuckDB backup still fires.
</verification>

<success_criteria>
- TTIER-03: nightly.sh invokes `-m integration` after the dbt build and before publish; a failure exits non-zero and blocks the refresh (observable in the log).
- The gate is honest: block 1c provides a real test_dbt_diff baseline (no vacuous skip), and Plan 01 ensured the integration tier is genuinely green.
- Expected first-run test_dbt_diff schema failure is documented as self-healing, not a defect.
- EXIT-trap DuckDB/taxa backup preserved on the gate-failure exit.
</success_criteria>

<output>
Create `.planning/phases/142-verify-budget-green-suite-nightly-wiring/142-02-SUMMARY.md` when done.
</output>
