---
phase: 142-verify-budget-green-suite-nightly-wiring
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - data/pyproject.toml
  - data/uv.lock
  - data/scripts/verify-clean-checkout.sh
  - data/tests/test_resolve_checklist_names.py
  - data/tests/BASELINE.md
autonomous: true
requirements: [TFIX-05, TPERF-02, TPERF-03]
must_haves:
  truths:
    - "Running `cd data && uv run pytest -m 'not integration' -p randomly -q` finishes with 0 failures and 0 errors under a randomized collection order"
    - "The fast suite wall-clock is measured and recorded as < 5 min in BASELINE.md"
    - "`bash data/scripts/verify-clean-checkout.sh` exits 0 with built/un-checked-in assets stripped, no network, no AWS"
    - "`test_at_least_13_fuzzy_candidates` produces >= 13 fuzzy candidates from a self-contained fixture (no real beeatlas.duckdb, no raw/taxa.csv.gz)"
  artifacts:
    - path: "data/pyproject.toml"
      provides: "pytest-randomly dev dependency for D-04 randomized green proof"
      contains: "pytest-randomly"
    - path: "data/scripts/verify-clean-checkout.sh"
      provides: "Reusable clean-checkout fast-suite proof (TPERF-03, D-02)"
      min_lines: 20
    - path: "data/tests/test_resolve_checklist_names.py"
      provides: "Fixed checklist_resolver_db fixture seeding >=13 fuzzy candidates"
      contains: "canonical_to_taxon_id"
    - path: "data/tests/BASELINE.md"
      provides: "Measured after-numbers for the fast suite (TPERF-02 accept criterion)"
      contains: "142"
  key_links:
    - from: "data/scripts/verify-clean-checkout.sh"
      to: "data/pyproject.toml addopts"
      via: "uv run pytest (default -m 'not integration' deselect)"
      pattern: "uv run pytest"
    - from: "data/tests/test_resolve_checklist_names.py checklist_resolver_db"
      to: "inaturalist_data.canonical_to_taxon_id bridge"
      via: "fuzzy candidate pool"
      pattern: "canonical_to_taxon_id"
---

<objective>
Prove the v4.8 fast suite is green, fast, and clean-checkout-safe, and fix the
last integration-tier red test so the nightly gate (Plan 02) is honest.

Purpose: Satisfies TFIX-05 (green), TPERF-02 (< 5 min, recorded), TPERF-03
(green on a clean checkout via a committed reusable script), and fixes
`test_at_least_13_fuzzy_candidates` — the integration-tier red test Plan 02's
hard gate must pass against. This plan is the verification + greening half of
the phase; Plan 02 is the nightly wiring half.

Output:
- `data/pyproject.toml` + `data/uv.lock` with `pytest-randomly` added (D-04).
- `data/scripts/verify-clean-checkout.sh` — committed clean-checkout proof (D-02).
- Fixed `checklist_resolver_db` fixture producing >= 13 fuzzy candidates.
- `data/tests/BASELINE.md` updated with measured after-numbers (per-tier table + History row).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/142-verify-budget-green-suite-nightly-wiring/142-CONTEXT.md
@.planning/phases/142-verify-budget-green-suite-nightly-wiring/142-RESEARCH.md
@.planning/phases/142-verify-budget-green-suite-nightly-wiring/142-PATTERNS.md

<interfaces>
<!-- Extracted from the codebase. Executor uses these directly. -->

data/pyproject.toml [dependency-groups] (current — append one line, keep ==/>= style):
  dev = ["pytest>=9.0.2", "dbt-duckdb==1.10.1"]
data/pyproject.toml [tool.pytest.ini_options]:
  testpaths = ["tests"]; addopts = "-m 'not integration'"
  markers: "integration: validates real datasets/built artifacts (nightly tier); deselected by default"
  DO NOT add --randomly-seed to addopts (RESEARCH Pitfall 6 — pinning masks order-dependence).

resolve_checklist_names.resolve_checklist_names(refresh: bool) reads (data/resolve_checklist_names.py ~276-324):
  - verbatim_names := SELECT DISTINCT verbatim_name FROM checklist_data.checklist_records_full WHERE verbatim_name IS NOT NULL
  - bridge := SELECT canonical_name, taxon_id FROM inaturalist_data.canonical_to_taxon_id WHERE taxon_id IS NOT NULL
  - candidate_names := list(bridge.keys())  ← the rapidfuzz fuzzy candidate pool
  - _generate_fuzzy_candidates(..., score_cutoff=85, limit=5) at line ~440; fuzzy_rows written to FUZZY_REVIEW_CSV
  So: >=13 fuzzy candidates requires (a) checklist_records_full seeded with the 178 unmatched verbatim names AND (b) canonical_to_taxon_id seeded with bee canonical names that score >=85 against those verbatim names.

checklist_resolver_db fixture today (data/tests/test_resolve_checklist_names.py ~65-145):
  - creates schema checklist_data + table checklist_records_full (14 cols incl ObjectID, verbatim_name, canonical_name, coord_flag)
  - seeds only 4 rows; NEVER creates inaturalist_data schema or canonical_to_taxon_id → bridge = {} → 0 fuzzy candidates
  - monkeypatches DB_PATH, _GBIF_PACE_SECONDS=0.0, AUDIT_CSV, FUZZY_REVIEW_CSV, GBIF_SEED_CSV, TAXA_PATH→FIXTURES_DIR/taxa_subset.csv.gz
  - test_at_least_13_fuzzy_candidates (line ~267) is @pytest.mark.integration; asserts len(fuzzy_review rows) >= 13

conftest.py canonical_to_taxon_id seed pattern (data/tests/conftest.py ~448-468) — copy this column order EXACTLY:
  CREATE: inaturalist_data.canonical_to_taxon_id(canonical_name TEXT PRIMARY KEY, taxon_id INTEGER, resolved_at TIMESTAMP, source TEXT)
  INSERT INTO inaturalist_data.canonical_to_taxon_id (canonical_name, taxon_id, resolved_at, source) VALUES
    ('andrena fulva', 200002, current_timestamp, 'inat_species'), ...

data/checklist_unmatched.csv (committed, 178 data rows + header): columns checklist_name, canonical_name, reason
  e.g. "Andrena evoluta,andrena evoluta,no occurrence row matches canonical_name"

refresh-geo.sh boilerplate (data/refresh-geo.sh 1-18): #!/usr/bin/env bash; set -euo pipefail;
  SCRIPT_DIR="$(cd "$(dirname "$0")" && pwd)"; REPO_ROOT="$(cd "$SCRIPT_DIR/.." && pwd)"
  (verify-clean-checkout.sh lives in data/scripts/, so REPO_ROOT is "$SCRIPT_DIR/../..")
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add pytest-randomly and prove the fast suite green + within budget</name>
  <files>data/pyproject.toml, data/uv.lock, data/tests/BASELINE.md</files>
  <read_first>
    - data/pyproject.toml (the [dependency-groups] block lines 19-23 and [tool.pytest.ini_options] block lines 25-30)
    - data/tests/BASELINE.md (per-tier table lines 22-32; Targets lines 36-43; History table lines 108-113 — the reserved "(pending)" 142 row)
    - .planning/phases/142-verify-budget-green-suite-nightly-wiring/142-RESEARCH.md (Pitfall 5 maderas SIGKILL; Pitfall 6 no seed pinning; budget = ~16s)
  </read_first>
  <action>
    Append `"pytest-randomly>=4.1.0",` to the `dev` list in `[dependency-groups]` in `data/pyproject.toml`, preserving the existing entry style (range pins use `>=`). Do NOT add `--randomly-seed` to `addopts` (D-04 / Pitfall 6). Then run `cd data && uv sync` to install the plugin and update `data/uv.lock`.

    Measure the fast suite: run `cd data && uv run pytest -m "not integration" -p randomly --tb=short -q` and capture (a) the `pytest-randomly` seed printed in the session header, (b) the pass/fail/error/skip counts, (c) the wall-clock. To get the wall-clock robustly under the maderas orchestrator SIGKILL constraint (Pitfall 5), prefer pytest's own footer duration or wrap with `time` on the SAME ~16s invocation — do NOT time the integration tier. Confirm 0 failures and 0 errors under the randomized order (TFIX-05); confirm the wall-clock is well under 5 min (TPERF-02). The 9 explicit `@pytest.mark.skip` (retired reconcile() code) are expected and are NOT asset-driven skips.

    Update `data/tests/BASELINE.md`: replace the "Per-Tier Baseline Estimates" ESTIMATES table with measured after-numbers (fast tier actual wall-clock + pass/skip counts), and replace the reserved `142 | ... | (pending)` History row with the real 2026-06-06 date and a one-line measured-result note (e.g. fast tier N passed / 9 skipped / Xs, randomized seed S). Keep the doc's two-tier framing intact. Optionally record the clean-checkout command and integration-tier inventory (Claude's discretion per D-03/BASELINE discretion).
  </action>
  <verify>
    <automated>cd data && uv run pytest -m "not integration" -p randomly --tb=short -q</automated>
  </verify>
  <acceptance_criteria>
    - `data/pyproject.toml [dependency-groups] dev` contains `pytest-randomly`
    - `data/uv.lock` contains a `pytest-randomly` entry
    - `cd data && uv run pytest -m "not integration" -p randomly -q` exits 0 with 0 failures and 0 errors
    - `addopts` in `data/pyproject.toml` does NOT contain `--randomly-seed`
    - `data/tests/BASELINE.md` History table contains a `142` row with a real date (not `(pending)`) and a measured runtime
    - The recorded fast-tier wall-clock in BASELINE.md is < 5 minutes
  </acceptance_criteria>
  <done>pytest-randomly installed; one randomized fast-suite run is green (0F/0E); measured runtime < 5 min recorded in BASELINE.md.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Fix test_at_least_13_fuzzy_candidates fixture (self-contained, >=13 hits)</name>
  <files>data/tests/test_resolve_checklist_names.py</files>
  <read_first>
    - data/tests/test_resolve_checklist_names.py (the checklist_resolver_db fixture lines 65-145; test_at_least_13_fuzzy_candidates lines 267-300)
    - data/resolve_checklist_names.py (lines 270-324 — verbatim_names + bridge query; lines 438-462 — _generate_fuzzy_candidates call with score_cutoff=85, limit=5)
    - data/tests/conftest.py (lines 440-468 — canonical_to_taxon_id CREATE + INSERT pattern; copy column order canonical_name, taxon_id, resolved_at, source EXACTLY)
    - data/checklist_unmatched.csv (committed 178-row source for verbatim names)
  </read_first>
  <behavior>
    - The fixture remains fully self-contained: no real `data/beeatlas.duckdb`, no `data/raw/taxa.csv.gz` (TAXA_PATH stays redirected to FIXTURES_DIR/taxa_subset.csv.gz so it survives a clean checkout — though this test is @integration, keep it clean-checkout-honest).
    - After the fix, `resolve_checklist_names(refresh=True)` writes a fuzzy_review.csv with >= 13 rows.
    - The other six tests in the file (test_noop_without_refresh, test_audit_csv_covers_all_names, test_fuzzy_candidates_written, test_fuzzy_review_gate, test_slash_lca, test_slash_verbatim_retained) continue to pass unchanged.
  </behavior>
  <action>
    Expand the `checklist_resolver_db` fixture so the bridge produces >= 13 fuzzy matches. Two additions inside the single `con` session, BEFORE `con.close()` on line 143:

    (1) Seed `checklist_data.checklist_records_full.verbatim_name` from the committed `data/checklist_unmatched.csv` (178 names). Compute the path as `Path(__file__).parent.parent / "checklist_unmatched.csv"`. Insert its `checklist_name` column as `verbatim_name` (canonical_name and coord_flag may be NULL/'valid'). Use DuckDB native `read_csv('<path>', header=true)` consistent with production `load_checklist()`. Keep the existing 4 hand-seeded rows OR replace them — but the slash-compound row `(3, 'Agapostemon texanus/angelicus ...', NULL, 'valid')` MUST remain present so test_slash_verbatim_retained still finds a '/' row.

    (2) Create `inaturalist_data` schema + `canonical_to_taxon_id` table (columns `canonical_name TEXT PRIMARY KEY, taxon_id INTEGER, resolved_at TIMESTAMP, source TEXT` — exact conftest order), and seed it with bee canonical names chosen to be near-matches (rapidfuzz score >= 85) of the 178 unmatched verbatim names. The unmatched set is dominated by `Andrena`, `Nomada`, `Megachile`, `Osmia`, `Lasioglossum`, `Bombus`, `Hylaeus`, `Ceratina` species. Seed canonical names that differ from unmatched names by 1-2 chars (e.g. `andrena evolata` vs `Andrena evoluta`, `nomada jenne` vs `Nomada jennei`) plus straightforward genus-overlap entries — enough that score_cutoff=85 yields >= 13 hits. Determine the exact seed list EMPIRICALLY: iterate (run the test, count fuzzy_review rows) until len >= 13, then commit the verified seed list. Do NOT weaken the test's `>= 13` assertion to mask a thin fixture (carry-forward of 141 D-07 spirit).

    Run the test explicitly opting into the integration marker since it is `@pytest.mark.integration`: `cd data && uv run pytest tests/test_resolve_checklist_names.py -m integration -q`. Also run the file's fast-tier tests to confirm no regression: `cd data && uv run pytest tests/test_resolve_checklist_names.py -q`.
  </action>
  <verify>
    <automated>cd data && uv run pytest tests/test_resolve_checklist_names.py -m integration -q && cd data && uv run pytest tests/test_resolve_checklist_names.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `cd data && uv run pytest tests/test_resolve_checklist_names.py::test_at_least_13_fuzzy_candidates -m integration -q` exits 0
    - The test's `>= 13` assertion is unchanged (not lowered)
    - `data/tests/test_resolve_checklist_names.py` fixture creates `inaturalist_data.canonical_to_taxon_id` and seeds `checklist_records_full` from `checklist_unmatched.csv`
    - `cd data && uv run pytest tests/test_resolve_checklist_names.py -q` exits 0 (fast-tier tests still pass)
    - The fixture does not read `data/beeatlas.duckdb` or `data/raw/taxa.csv.gz`
  </acceptance_criteria>
  <done>test_at_least_13_fuzzy_candidates passes against a self-contained fixture with the original >=13 threshold; the six other tests in the file still pass.</done>
</task>

<task type="auto">
  <name>Task 3: Create verify-clean-checkout.sh and prove green on a clean checkout</name>
  <files>data/scripts/verify-clean-checkout.sh</files>
  <read_first>
    - data/refresh-geo.sh (lines 1-18 — shebang, set -euo pipefail, SCRIPT_DIR/REPO_ROOT derivation: the shell boilerplate analog)
    - data/nightly.sh (line 30 — `export PATH="$HOME/.local/bin:$PATH"`)
    - .planning/phases/142-verify-budget-green-suite-nightly-wiring/142-PATTERNS.md (verify-clean-checkout.sh section lines 132-193 — full script pattern; A2 caveat: uv cache must be warm)
    - .planning/REQUIREMENTS.md (TPERF-03 lines 16-17 — strip dbt/target, public/data, raw/taxa.csv.gz, beeatlas.duckdb)
  </read_first>
  <action>
    Create `data/scripts/verify-clean-checkout.sh` (new directory `data/scripts/`). Use the `refresh-geo.sh` boilerplate: `#!/usr/bin/env bash`, a header comment documenting purpose (TPERF-03, D-02) and that Phase 143 CI can call it, `set -euo pipefail`, `export PATH="$HOME/.local/bin:$PATH"`, then `SCRIPT_DIR="$(cd "$(dirname "$0")" && pwd)"` and `REPO_ROOT="$(cd "$SCRIPT_DIR/../.." && pwd)"` (note `../..` because this script is one level deeper than nightly.sh).

    Mechanism (D-02 discretion → git worktree, per RESEARCH): create `WORKTREE="$(mktemp -d)"`; set an EXIT trap that removes the worktree (`git -C "$REPO_ROOT" worktree remove --force "$WORKTREE" 2>/dev/null || true; rm -rf "$WORKTREE"`); `git -C "$REPO_ROOT" worktree add --detach "$WORKTREE" HEAD`. Strip built/un-checked-in assets to simulate a clean checkout: `rm -rf "$WORKTREE/data/dbt/target" "$WORKTREE/public/data" "$WORKTREE/data/raw/taxa.csv.gz" "$WORKTREE/data/beeatlas.duckdb"`. Then `cd "$WORKTREE/data"`, `uv sync --frozen` (uses the local uv cache — no network; A2 assumption), and `uv run pytest -x --tb=short -q` (addopts deselects @integration by default, so this is the fast tier). On success echo a clear PASSED banner. Make it executable (`chmod +x`).

    Run it: `bash data/scripts/verify-clean-checkout.sh`. The worktree's stripped assets + default marker deselect must yield a green fast suite with no network/AWS. If it surfaces a clean-checkout-only failure (e.g. an asset-driven skip the D-05 conftest guard converts to a FAIL), that is exactly what TPERF-03 is meant to catch — fix the root cause, do not weaken the script.
  </action>
  <verify>
    <automated>bash data/scripts/verify-clean-checkout.sh</automated>
  </verify>
  <acceptance_criteria>
    - `data/scripts/verify-clean-checkout.sh` exists, is executable, and starts with `#!/usr/bin/env bash` + `set -euo pipefail`
    - The script uses `git worktree add --detach` and strips `data/dbt/target`, `public/data`, `data/raw/taxa.csv.gz`, `data/beeatlas.duckdb`
    - The script runs `uv sync --frozen` and `uv run pytest` inside the worktree's `data/`
    - `bash data/scripts/verify-clean-checkout.sh` exits 0
    - The script removes the worktree on exit (EXIT trap present)
  </acceptance_criteria>
  <done>The committed clean-checkout script passes the fast suite on a freshly stripped worktree with no network and no AWS, satisfying TPERF-03.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| developer/CI → fixture data | Committed `checklist_unmatched.csv` + in-fixture seeds are trusted, version-controlled inputs (no remote/untrusted data) |
| script → local git worktree | `verify-clean-checkout.sh` reads only `$REPO_ROOT` (trusted checkout) and writes only to a `mktemp -d` worktree it owns |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-142-02 | Tampering | `verify-clean-checkout.sh` worktree cleanup | mitigate | EXIT trap with `worktree remove --force` + `rm -rf` on a `mktemp -d` path only; never touches `$REPO_ROOT` working tree |
| T-142-03 | Denial of Service | budget measurement under maderas SIGKILL | mitigate | Time only the ~16s fast tier (`-m "not integration"`); never time the integration tier in one Bash run (RESEARCH Pitfall 5) |
| T-142-SC | Tampering | pytest-randomly install (uv/PyPI) | mitigate | Package legitimacy: pytest-randomly is [VERIFIED] in RESEARCH.md Package Legitimacy Audit (pytest-dev org, ~8 yrs, high downloads); pinned `>=4.1.0` via uv.lock |
</threat_model>

<verification>
- Fast suite green under randomized order: `cd data && uv run pytest -m "not integration" -p randomly -q` → 0 failures, 0 errors.
- Budget recorded: BASELINE.md History 142 row has a measured runtime < 5 min.
- Clean checkout: `bash data/scripts/verify-clean-checkout.sh` → exit 0.
- Integration red test fixed: `cd data && uv run pytest tests/test_resolve_checklist_names.py::test_at_least_13_fuzzy_candidates -m integration -q` → exit 0.
</verification>

<success_criteria>
- TFIX-05: fast suite green (0F/0E) under one default pytest-randomly run.
- TPERF-02: fast suite measured < 5 min, recorded in BASELINE.md.
- TPERF-03: committed clean-checkout script exits 0 with assets stripped, no network, no AWS.
- test_at_least_13_fuzzy_candidates passes with the original >=13 threshold from a self-contained fixture (unblocks Plan 02's honest gate).
</success_criteria>

<output>
Create `.planning/phases/142-verify-budget-green-suite-nightly-wiring/142-01-SUMMARY.md` when done.
</output>
