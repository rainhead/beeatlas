---
phase: 180-moderation-loop
plan: 05
type: execute
wave: 3
depends_on: ["180-02", "180-03", "180-04"]
files_modified: []
autonomous: false
requirements: [MOD-02, MOD-04]
user_setup:
  - service: maderas (self-hosted store + write API)
    why: "Migration 0004 must be applied to the live SQLite store; the end-to-end takedown UAT and the curl restore run against the deployed api.beeatlas.net"
    dashboard_config:
      - task: "Apply Alembic migration 0004 (alembic upgrade head) to the live notes store and restart the Waitress write service"
        location: "maderas: $HOME/beeatlas-store/notes.db + systemd-user Waitress unit"
must_haves:
  truths:
    - "Migration 0004 is applied to the live maderas notes store (note_revisions.reason column present)"
    - "An author submits a note → it renders on the live island → is baked into notes.json"
    - "A curator clicks Take down → the note vanishes from the live island immediately"
    - "The taken-down note is absent from the next baked notes.json (one build cycle)"
    - "A curl restore returns the note to status='approved' and it reappears after the next harvest"
  artifacts: []
  key_links:
    - from: "curator browser (live island)"
      to: "api.beeatlas.net /api/notes/{id}/takedown"
      via: "Take down button → POST → refetch"
      pattern: "takedown"
---

<objective>
Close Phase 180 with the operator migration apply and the blocking end-to-end
MOD-04 human UAT walkthrough. Migration 0004 (the `reason` column) must be
applied to the live maderas SQLite store before the takedown route can insert a
ledger row on production; then a human verifies the full loop: author submits →
note publishes (live island + next bake) → curator takes it down → note gone from
the live island immediately and absent from the next baked `notes.json`.

Purpose: MOD-04 is verified by an end-to-end submit → publish → takedown
walkthrough (ROADMAP success criterion 4). This is a BLOCKING human UAT gate —
per ROADMAP and memory `feedback_uat_ui_phases`, do NOT auto-advance past it.

Output: a migrated production store, an operator-confirmed takedown loop, and a
verified curl-only restore (D-07). No code is written in this plan.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/180-moderation-loop/180-CONTEXT.md
@.planning/phases/180-moderation-loop/180-VALIDATION.md
@api/README.md
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: Operator applies migration 0004 to the live maderas store</name>
  <read_first>
    - api/README.md — the dev loop + systemd-user Waitress + mod_proxy deployment context and how migrations were applied for 0002/0003.
    - data/notes_store/migrations/versions/0004_add_note_revision_reason.py — the revision to apply.
    - STATE.md (Phase 179 decision) — `NOTES_DB_PATH` on maderas is `$HOME/beeatlas-store/notes.db` (the code default `/opt/beeatlas-store/notes.db` is absent on maderas); the operator must target the live path.
  </read_first>
  <files>maderas: $HOME/beeatlas-store/notes.db (live SQLite store); the systemd-user Waitress unit</files>
  <action>Operator-only, on maderas: after the merged Phase 180 code is pulled on the host, run `alembic upgrade head` against the live notes store with `NOTES_DB_PATH=$HOME/beeatlas-store/notes.db` through the `data/` venv (mirroring the 0002/0003 apply), then restart the systemd-user Waitress write service so the app reloads against the migrated schema. This is a deploy step — migrations are never run by run.py/nightly (STORE-02). Claude cannot perform this step (the SQLite store lives on maderas and is not locally runnable per `project_store_tech_sqlite_on_maderas`); it is surfaced as a blocking operator checkpoint.</action>
  <what-built>
    Migration 0004 (adds nullable `note_revisions.reason`) and the curator
    takedown/restore routes that write to it. Migrations are operator-run on
    maderas, never by run.py/nightly (STORE-02); this is a deploy step,
    mirroring the 0002/0003 apply precedent.
  </what-built>
  <how-to-verify>
    On maderas, from the deployed api checkout:
    1. Pull the merged Phase 180 code (`git pull` on the host BEFORE running anything — bash parses scripts at invocation).
    2. Run `alembic upgrade head` against the LIVE store with `NOTES_DB_PATH=$HOME/beeatlas-store/notes.db` (the same env contract the systemd unit + runbook use), through the `data/` venv (`uv run`), from the notes_store migrations dir — matching how 0002/0003 were applied.
    3. Confirm the column exists: `PRAGMA table_info(note_revisions)` on `$HOME/beeatlas-store/notes.db` lists `reason` (nullable).
    4. Restart the systemd-user Waitress write service so the app reloads against the migrated schema.
    5. Confirm `alembic current` reports revision `0004`.
  </how-to-verify>
  <verify>
    <automated>MISSING — operator-only step against the live maderas store (not locally runnable; the SQLite store is on maderas per project_store_tech_sqlite_on_maderas). Verified by operator via PRAGMA table_info + alembic current in how-to-verify.</automated>
  </verify>
  <acceptance_criteria>
    - CLI output: `alembic current` on maderas reports revision `0004`.
    - CLI output: `PRAGMA table_info(note_revisions)` on `$HOME/beeatlas-store/notes.db` includes a nullable `reason` column.
    - behavior assertion: the Waitress write service is restarted and serving after the migration.
  </acceptance_criteria>
  <done>Migration 0004 is applied to the live maderas store and the Waitress write service serves against the migrated schema.</done>
  <resume-signal>Type "migrated" once `alembic current` shows 0004 and the reason column is present on the live store, or describe the failure.</resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: End-to-end MOD-04 takedown UAT + curl restore</name>
  <read_first>
    - .planning/phases/180-moderation-loop/180-VALIDATION.md — the Manual-Only Verifications table (the exact submit → publish → takedown walkthrough + the curl restore step).
    - .planning/phases/180-moderation-loop/180-CONTEXT.md §specifics — the MOD-04 end-to-end definition and the deliberate takedown-has-UI / restore-is-curl-only asymmetry (D-07).
  </read_first>
  <files>live site species pages + api.beeatlas.net (no repo files modified)</files>
  <action>Operator-only, blocking human UAT: walk the full MOD-04 loop on the live site and confirm each of the six how-to-verify steps below. This gate must NOT auto-advance (ROADMAP end-to-end human UAT gate; memory `feedback_uat_ui_phases`). Claude drives nothing here — it presents the checklist and waits for the operator's approval or a defect report.</action>
  <what-built>
    The full moderation loop: the curator-gated "Take down" control on the live
    `<bee-notes>` island (Plan 03) calling the takedown route (Plan 02), with the
    read endpoint + nightly harvest excluding `hidden` notes by construction
    (Plan 04). Restore is curl-only (D-07 — no UI).
  </what-built>
  <how-to-verify>
    Against the live site + api.beeatlas.net (curator signed in as the allowlisted `rainhead` curator):
    1. As an author, submit a note on a species page → confirm it renders on the live island immediately (shows `@login` byline until the next bake).
    2. Confirm the note is baked into the next `notes.json` (observe/trigger the nightly harvest, or confirm it appears on the static species page after the next build cycle) — MOD-04 "within one build cycle".
    3. As the curator, click "Take down" on that note → confirm the inline confirm row → confirm the note vanishes from the live island IMMEDIATELY (server-driven refetch, no reload) and the "Note taken down." banner shows.
    4. Confirm the taken-down note is ABSENT from the next baked `notes.json` / static species page after the next build cycle.
    5. Restore via curl (D-07, no UI): `POST https://api.beeatlas.net/api/notes/{id}/restore` with `-H "Origin: https://beeatlas.net"` and the curator's session cookie → confirm 200, `status='approved'`, and that the note reappears after the next harvest.
    6. Negative check: confirm a signed-in NON-curator author does not see the "Take down" control on other authors' notes.
  </how-to-verify>
  <verify>
    <human-check>Operator confirms all 6 steps: submit→publish, live-island immediate removal on takedown, absence from the next bake, curl restore returns the note, and the non-curator negative check. This is a blocking human UAT gate — do NOT auto-advance (ROADMAP; feedback_uat_ui_phases).</human-check>
  </verify>
  <acceptance_criteria>
    - behavior assertion: a submitted note renders on the live island and is baked into `notes.json` within one build cycle.
    - behavior assertion: a curator takedown removes the note from the live island immediately and it is absent from the next baked `notes.json`.
    - behavior assertion: a curl `POST /api/notes/{id}/restore` returns the note to `status='approved'`; it reappears after the next harvest.
    - behavior assertion: a signed-in non-curator author does not see the "Take down" control.
  </acceptance_criteria>
  <done>The end-to-end takedown loop and the curl restore are operator-verified; the non-curator negative check holds.</done>
  <resume-signal>Type "approved" once all six UAT steps pass, or describe the issues found.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator → live authoritative store | forward-only schema apply on production maderas SQLite |
| curator browser / curl → live write API | authz-gated state transitions on production |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-180-DR | Denial of Service / data loss | live migration apply | mitigate | Migration 0004 is a single additive nullable column (no data rewrite, no NOT-NULL tightening); forward-only with no downgrade. Snapshot backups (177) protect the store. Operator verifies `alembic current`/PRAGMA before serving. |
| T-180-01 | Elevation of Privilege | live takedown/restore authz | mitigate (verify) | UAT confirms the non-curator negative check; server-side `_is_curator_fresh` (Plan 02) is the enforcement. |
| T-180-04 | Information Disclosure | public leak of hidden notes | mitigate (verify) | UAT confirms the taken-down note is absent from both the live island and the next bake. |
| T-180-SC | Tampering (supply chain) | package installs | accept | No new packages (RESEARCH Package Legitimacy Audit: not applicable). |
</threat_model>

## Artifacts This Plan Produces

No new code symbols. Produces: a migrated production store (revision 0004 applied on maderas) and an operator-signed end-to-end MOD-04 UAT + curl-restore confirmation.

<verification>
- Operator: `alembic current` shows 0004 and `PRAGMA table_info(note_revisions)` lists nullable `reason` on the live store.
- Human UAT: the six-step submit → publish → takedown → curl-restore walkthrough passes (blocking gate; no auto-advance).
</verification>

<success_criteria>
- Migration 0004 is live on maderas and the Waitress service is serving against the migrated schema.
- The end-to-end takedown loop is operator-verified: publish, immediate live-island removal, absence from the next bake.
- Curl restore returns the note to approved and it reappears after harvest; the non-curator negative check holds.
</success_criteria>

<output>
Create `.planning/phases/180-moderation-loop/180-05-SUMMARY.md` when done.
</output>
