---
phase: 180-moderation-loop
plan: 04
type: execute
wave: 2
depends_on: ["180-01"]
files_modified:
  - data/tests/test_notes_harvest.py
  - data/tests/test_notes_store_schema.py
autonomous: true
requirements: [MOD-01, MOD-03, MOD-04]
must_haves:
  truths:
    - "A hidden-status note is excluded from the nightly harvest (export_notes) — MOD-04 by construction"
    - "Every note carries the 4 audit fields (author_id, status, created_at, updated_at); note_revisions carries the nullable reason column"
    - "The declared roles allowlist is the auditable role source (MOD-01, verified via existing test)"
    - "A <script>/onerror= payload renders inert (MOD-03 XSS, verified via existing test)"
  artifacts:
    - path: "data/tests/test_notes_harvest.py"
      provides: "hidden-status exclusion test"
      contains: "hidden"
    - path: "data/tests/test_notes_store_schema.py"
      provides: "reason column + audit-field schema assertion"
      contains: "reason"
  key_links:
    - from: "data/tests/test_notes_harvest.py"
      to: "notes_harvest.export_notes"
      via: "hidden fixture row excluded"
      pattern: "status=\"hidden\""
---

<objective>
Prove the MOD-04 "excluded from the public site" and MOD-03 "XSS + audit fields"
invariants that Phase 180 relies on holding BY CONSTRUCTION — no new production
code, verification only (D-11). MOD-04's exclusion is guaranteed because both the
nightly harvest and the read endpoint scope to `status='approved'`, so the new
`hidden` value is excluded automatically; this plan writes the tests that lock
that behavior in. MOD-01 (declared role source) and MOD-03 (XSS sanitize +
4 audit fields) are already implemented in 177/179 — this plan runs and, where
needed, extends their existing tests.

Purpose: MOD-04 (takedown clears the public site within one build cycle),
MOD-03 (XSS + audit fields), MOD-01 (roles from a declared, auditable place).

Output: a `hidden`-exclusion harvest test and a `reason`/audit-field schema
assertion; plus green runs of the existing MOD-01/MOD-03 verification tests.
Depends on Plan 01 (the `reason` column the schema assertion checks).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/180-moderation-loop/180-CONTEXT.md
@.planning/phases/180-moderation-loop/180-RESEARCH.md
@.planning/phases/180-moderation-loop/180-01-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Hidden-status harvest-exclusion test (MOD-04)</name>
  <read_first>
    - data/tests/test_notes_harvest.py — `test_harvest_approved_only_newest_first_with_byline` already seeds `pending` and `removed` notes and asserts they are excluded from `export_notes()` output. Add a `hidden`-status row to the same exclusion pattern (reuse `_make_db`, `_make_user`, `_write_collectors_json`, `export_notes`).
    - data/notes_harvest.py (line 103) — the `Note.status == "approved"` filter that makes `hidden` excluded by construction (no code change here — this is verification only).
  </read_first>
  <files>data/tests/test_notes_harvest.py</files>
  <behavior>
    - A note with `status="hidden"` seeded alongside approved/pending/removed rows never appears in the `export_notes()` output JSON (same assertion shape as the existing pending/removed exclusion).
  </behavior>
  <action>Add a `hidden`-status `Note` fixture row (a new species key or an extra note under an existing species) to the harvest test and assert `export_notes()` excludes it — mirroring the existing `pending`/`removed` exclusion assertions. Name the test so `-k hidden` selects it (e.g. `test_harvest_excludes_hidden`). Do NOT modify `data/notes_harvest.py` — MOD-04 exclusion is by construction; changing the harvest would violate D-06/RESEARCH "verify, don't modify".</action>
  <verify>
    <automated>cd data && uv run pytest tests/test_notes_harvest.py -k hidden -x</automated>
  </verify>
  <acceptance_criteria>
    - test command: `cd data && uv run pytest tests/test_notes_harvest.py -k hidden -x` passes.
    - source assertion: the new test seeds a `status="hidden"` note and asserts it is absent from `export_notes()` output.
    - source assertion: `data/notes_harvest.py` is unchanged by this plan (git diff shows no production-code edit).
    - test command: full harvest file green — `cd data && uv run pytest tests/test_notes_harvest.py -x`.
  </acceptance_criteria>
  <done>The hidden-exclusion invariant (MOD-04) is test-locked without touching harvest code.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Audit-field + reason-column schema assertion; run MOD-01/MOD-03 verifications</name>
  <read_first>
    - data/tests/test_notes_store_schema.py — the existing schema tests over `Note`/`NoteRevision`; add an assertion that `note_revisions.reason` exists and is nullable, and (if not already asserted) that `Note` carries the 4 audit fields `author_id`, `status`, `created_at`, `updated_at` (MOD-03).
    - data/notes_store/models.py — the post-Plan-01 `NoteRevision.reason` field and the `Note` audit columns the assertions check.
    - data/tests/test_notes_render.py — the existing XSS `<script>`/`onerror=` inert-render test (MOD-03); run it as verification, extend only if a takedown/restore-specific case is missing.
    - data/tests/test_notes_seed_roles.py — the existing declared-allowlist role-source test (MOD-01); run it as verification.
  </read_first>
  <files>data/tests/test_notes_store_schema.py</files>
  <behavior>
    - Schema test asserts `note_revisions` has a `reason` column that is nullable.
    - Schema test asserts `Note` exposes `author_id`, `status`, `created_at`, `updated_at` (the 4 MOD-03 audit fields).
  </behavior>
  <action>Extend `data/tests/test_notes_store_schema.py` to assert the `reason` column exists on `note_revisions` and is nullable, and that the 4 audit fields are present on `Note` (add whichever assertion is missing; do not duplicate an existing one). Then confirm the pre-shipped MOD-01/MOD-03 verifications are green by running the existing `test_notes_seed_roles.py` (roles from the declared allowlist) and `test_notes_render.py` (XSS inert render) — these are verification-only (D-11), no new production code.</action>
  <verify>
    <automated>cd data && uv run pytest tests/test_notes_store_schema.py tests/test_notes_render.py tests/test_notes_seed_roles.py -x</automated>
  </verify>
  <acceptance_criteria>
    - test command: `cd data && uv run pytest tests/test_notes_store_schema.py tests/test_notes_render.py tests/test_notes_seed_roles.py -x` passes.
    - source assertion: `test_notes_store_schema.py` asserts `note_revisions.reason` is present and nullable.
    - source assertion: `test_notes_store_schema.py` asserts `Note` carries `author_id`, `status`, `created_at`, `updated_at`.
    - behavior assertion: MOD-01 (`test_notes_seed_roles`) and MOD-03 XSS (`test_notes_render`) pass unchanged, confirming the pre-shipped substrate.
  </acceptance_criteria>
  <done>Audit fields + reason column are schema-asserted; MOD-01 and MOD-03 verifications are green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| write API / harvest → public static site | only `status='approved'` notes may cross to the public surface; `hidden`/`removed`/`pending` must not leak |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-180-04 | Tampering / Information Disclosure | harvest + read exclusion of non-approved notes | mitigate (verify) | This plan is the verification surface: a `hidden` note must never appear in `export_notes()` (nor `GET /api/notes`, covered by Plan 02). Test-locked here so a future harvest refactor that drops the `status='approved'` filter fails loudly. |
| T-180-XSS | Tampering | note body render | accept (verify) | XSS sanitization (nh3) is unchanged from 179-01; `test_notes_render.py` re-run confirms `<script>`/`onerror=` renders inert. |
| T-180-SC | Tampering (supply chain) | package installs | accept | No new packages (RESEARCH Package Legitimacy Audit: not applicable). |
</threat_model>

## Artifacts This Plan Produces

Test-only: a hidden-status harvest-exclusion test (`test_notes_harvest.py`) and a `reason`/audit-field schema assertion (`test_notes_store_schema.py`). No new production symbols.

<verification>
- MOD-04 harvest exclusion: `cd data && uv run pytest tests/test_notes_harvest.py -k hidden -x`.
- MOD-03 schema + XSS, MOD-01 roles: `cd data && uv run pytest tests/test_notes_store_schema.py tests/test_notes_render.py tests/test_notes_seed_roles.py -x`.
- Wave-merge regression (both suites): `npm test` and `cd data && uv run pytest -m "not integration"`.
</verification>

<success_criteria>
- A hidden note is excluded from `export_notes()` (MOD-04), verified without editing harvest code.
- `note_revisions.reason` and the 4 `Note` audit fields are schema-asserted (MOD-03).
- The pre-shipped MOD-01 (declared roles) and MOD-03 (XSS inert) tests are green.
</success_criteria>

<output>
Create `.planning/phases/180-moderation-loop/180-04-SUMMARY.md` when done.
</output>
