---
phase: 075-authoring-scaffold
plan: 02
type: execute
wave: 2
depends_on: ["075-01"]
files_modified:
  - .planning/ROADMAP.md
  - .planning/STATE.md
autonomous: false
requirements: [D-01, D-02, D-03, D-04, D-05]
tags: [eleventy, vite, lit, uat, milestone-close]

must_haves:
  truths:
    - "`npm run dev` from repo root starts an Eleventy dev server with Vite middleware on port 8080 (or the port the dev server actually announces)"
    - "Visiting `/_scaffold-check/` in the dev server renders the bee-header chrome in the browser (visible BeeAtlas title bar with map/table icon buttons + GitHub link)"
    - "Visiting `/_scaffold-check/` shows the build-info table with resolved version strings (NOT the literal `{{ build.eleventyVersion }}` placeholder)"
    - "Visiting `/` in the dev server still renders the SPA correctly (Mapbox tiles load, filter panel opens, dot data loads) — no regression from Phase 74 baseline"
    - "`CLAUDE.md` 'Running Locally' section is consistent with what was actually observed during dev-server smoke (the dev URL list does NOT change in v3.1; `/_scaffold-check/` is a deploy diagnostic, not a primary URL)"
    - "ROADMAP.md Phase 75 checkbox is checked and Plans subsection shows both 075-01 and 075-02 as completed"
    - "STATE.md `progress.completed_phases` is incremented to 2; `last_activity` reflects v3.1 milestone completion"
    - "Phase summary `075-PHASE-SUMMARY.md` exists rolling up plans 01 + 02"
  artifacts:
    - path: ".planning/ROADMAP.md"
      provides: "Phase 75 marked complete; plan list reflects actual filenames; v3.1 milestone roll-up updated"
      contains: "[x] Phase 75"
    - path: ".planning/STATE.md"
      provides: "Phase 75 marked complete; phase pointer at 75 of 75; progress percent reflects completion"
      contains: "completed_phases: 2"
    - path: ".planning/phases/075-authoring-scaffold/075-PHASE-SUMMARY.md"
      provides: "Phase 75 roll-up summary covering plans 01 + 02; documents v3.1 milestone completion"
  key_links:
    - from: "ROADMAP.md v3.1 milestone block"
      to: "Phase 75 completion checkbox"
      via: "[x] Phase 75: Authoring Scaffold and Verification"
      pattern: "\\[x\\] Phase 75"
    - from: "STATE.md progress block"
      to: "milestone-complete state"
      via: "completed_phases: 2; total_phases: 2; percent: 100"
      pattern: "completed_phases: 2"
    - from: "075-PHASE-SUMMARY.md"
      to: "075-01-SUMMARY.md and 075-02-SUMMARY.md"
      via: "rolls up plan-level summaries"
      pattern: "075-01"
---

<objective>
Close out Phase 75 — the second and final phase of v3.1 — with a
manual UAT confirming the orphan diagnostic page renders bee-header
chrome correctly in a browser, a regression check that the SPA at `/`
is unchanged, and milestone-completion paperwork (ROADMAP.md,
STATE.md, phase summary).

This plan has ONE checkpoint task (the manual UAT) and TWO autonomous
tasks (doc check + milestone close). Plan 075-01 already verified the
build-shape via grep gates and the load-bearing A5 probe; this plan
verifies the in-browser rendering and ships v3.1.

Purpose: Get user sign-off that the Eleventy + Vite + Lit pipeline
produces the expected chrome in a real browser (the only
verification-shape that grep cannot answer), and update planning
artifacts to reflect milestone completion so v3.2 (Species Tab) can
begin from a clean slate.

Output: Manual UAT pass, two updated planning files, one phase
summary file. No source-code or build-config changes — this plan only
edits planning artifacts and observes browser behavior.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@.planning/phases/075-authoring-scaffold/075-CONTEXT.md
@.planning/phases/075-authoring-scaffold/075-RESEARCH.md
@.planning/phases/075-authoring-scaffold/075-01-PLAN.md

@.planning/phases/074-eleventy-build-wrapper/074-PHASE-SUMMARY.md
@.planning/phases/074-eleventy-build-wrapper/074-03-PLAN.md

@CLAUDE.md

<interfaces>
<!-- ROADMAP.md current state of v3.1 milestone block (lines 309–323):

## 🔲 v3.1 Eleventy Build Wrapper (Phases 74–75)
... (milestone goal + out-of-scope + pattern reference) ...
- [x] Phase 74: Eleventy Outer Build Integration (3/3 plans) — completed 2026-04-30
- [ ] Phase 75: Authoring Scaffold and Verification

After this plan, the v3.1 block updates to:
- [x] Phase 75: Authoring Scaffold and Verification (2/2 plans) — completed YYYY-MM-DD
And the milestone heading flips:
- "## 🔲 v3.1 Eleventy Build Wrapper" → "## ✅ v3.1 Eleventy Build Wrapper"

Phase 75's plan list (currently absent from ROADMAP because it was
filled in by /gsd-plan-phase 75) needs to be added in the same block:

### Phase 75: Authoring Scaffold and Verification
**Goal**: Eleventy authoring scaffold (input dir, layout chain, base template)
in place ready for v3.2 content; orphan page at /_scaffold-check/ verifies
the Eleventy + Vite + Lit pipeline end-to-end.
**Depends on**: Phase 74
**Requirements**: D-01, D-02, D-03, D-04, D-05 (CONTEXT.md decisions)
**Plans**: 2 plans
Plans:
- [x] 075-01-PLAN.md — Layout chain (base.njk + default.njk), bee-header entry, _data/build.js, orphan diagnostic page; build smoke + A5 probe
- [x] 075-02-PLAN.md — Manual UAT, milestone-close paperwork (ROADMAP/STATE/phase-summary)

(The exact wording follows the existing Phase 74 entry's structure on
ROADMAP.md lines 324–339 — the executor reads that block first to
mirror the format.)
-->

<!-- STATE.md current state (top of file):

```yaml
gsd_state_version: 1.0
milestone: v3.1
milestone_name: Eleventy Build Wrapper
status: Phase 74 complete; Phase 75 next
last_updated: "2026-04-30T07:45:00.000Z"
last_activity: 2026-04-30 — Phase 74 (Eleventy Outer Build) complete; UAT approved incl. HMR; CI green
progress:
  total_phases: 2
  completed_phases: 1
  total_plans: 3
  completed_plans: 3
  percent: 50
```

After this plan:
- status: "v3.1 milestone complete; v3.2 next"
- last_updated: ISO timestamp from when this plan executes
- last_activity: "YYYY-MM-DD — Phase 75 (Authoring Scaffold) complete; v3.1 ready to merge"
- progress.completed_phases: 2
- progress.total_plans: 5  (3 from Phase 74 + 2 from Phase 75)
- progress.completed_plans: 5
- progress.percent: 100

The "Current Position" section also needs updating — it currently reads
"Phase: 75 of 75 — authoring-scaffold (context gathered, ready to plan)".
After this plan: "Phase: 75 of 75 — authoring-scaffold (complete; v3.1
ready to merge)".
-->

<!-- CLAUDE.md "Running Locally" section (verified 2026-04-30, post-074-03):

```bash
# Dev server (Eleventy + Vite middleware, hot-reload)
npm run dev

# Tests (Vitest)
npm test

# Production build (validate-schema -> tsc --noEmit -> eleventy + Vite)
npm run build

# Data pipeline
cd data && python run.py

# Data pipeline tests (pytest)
cd data && uv run pytest
```

This section is already correct for Phase 75. The dev URL is unchanged
(SPA still at `/`); `/_scaffold-check/` is a deploy diagnostic, not a
primary user-facing URL, so it does not need to be added to the
"Running Locally" command list. Task 1 confirms (via grep) that
CLAUDE.md is consistent with the post-Phase-75 state and updates ONLY
if the dev-server UAT in Task 1 reveals a port-number drift (e.g., if
the dev URL changed from 8080 to something else — no expected, but
verify-and-record).
-->

<!-- DEV SERVER PORT (from 074-03 UAT, captured in 074-03 SUMMARY):
The Eleventy dev server announces `http://localhost:8080/`. This was
the port observed and approved during Phase 74-03's UAT. If the port
differs in this plan's UAT, capture it and update CLAUDE.md (the
existing comment already says "Dev server (Eleventy + Vite middleware,
hot-reload)" — the URL is not in the comment, so no edit needed unless
the port changed). -->
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Manual UAT — render bee-header chrome at /_scaffold-check/ + SPA regression check</name>
  <what-built>
    Plan 075-01 created the Eleventy authoring scaffold:
    - Two-layer Nunjucks layout chain (`_layouts/base.njk` + `_layouts/default.njk`)
    - Side-effect Vite entry (`src/entries/bee-header.ts`) that bundles
      `<bee-header>` for content pages
    - Build-time metadata (`_data/build.js`) exposing version strings
    - Orphan diagnostic page (`_pages/scaffold-check.njk`) at
      `/_scaffold-check/` rendering bee-header chrome + a build-info table

    `npm run build` was verified end-to-end via grep (the load-bearing
    A5 probe). This task verifies the human-visible rendering: that
    bee-header chrome appears in a real browser at `/_scaffold-check/`
    AND that the SPA at `/` still works.
  </what-built>
  <how-to-verify>
    Run the dev server and verify both URLs in a browser.

    **Setup:**
    ```bash
    npm run dev
    ```
    Expected console output:
    - Eleventy banner ("[11ty] Wrote N files in ...s ...") — N should
      be at least 2 (`index.html` + `_scaffold-check/index.html`)
    - "Server at http://localhost:8080" (or the port Eleventy
      announces; capture the actual port for the SUMMARY).

    **Verification — `/_scaffold-check/` (the new page):**

    1. Open `http://localhost:8080/_scaffold-check/` in a browser.

    2. **Bee-header chrome renders.** A horizontal bar at the top of
       the page shows:
       - "BeeAtlas" title text on the left
       - Map and Table icon buttons on the right (the map button
         visibly active by default, since `viewMode` defaults to `'map'`)
       - A GitHub icon link to the right
       - Background color: dark (CSS var `--header-bg`); text white
       This is the same chrome the SPA renders — proves the Lit
       component upgraded successfully and the bundle loaded.

    3. **Build-info table renders with resolved values.** Below the
       chrome, a `<h1>Scaffold Check</h1>` heading and a `<table>`
       showing seven rows. Each row's value column should be a real
       string, NOT a literal `{{ build.eleventyVersion }}` placeholder.
       Examples of expected values:
       - Eleventy: `3.1.5` (or whatever version is in `package-lock.json`)
       - Plugin (eleventy-plugin-vite): `7.1.1` (or current)
       - Vite: `6.4.2` (root vite version)
       - Lit: `3.2.1`
       - Node: `v24.12.0` (or whatever the dev machine has)
       - Built at: an ISO timestamp (refreshed when `npm run dev`
         started, NOT on every request — Pitfall #5)
       - Git SHA: a 7-character short hash, OR `unknown` if the dev
         tree is in an unusual state (shallow clone, etc.). NOT empty.

    4. **DevTools sanity** (open browser DevTools → Network tab,
       filter to JS):
       - One request resolves to `/src/entries/bee-header.ts` in dev
         mode (Vite middleware serves the source file directly with
         on-the-fly transform). Status: 200.
       - The bee-header element appears as `<bee-header>` in the DOM
         tree (Elements panel) — Lit's shadow root contains the actual
         markup.
       - Console: no uncaught errors. Some Vite-middleware
         development logs are expected and acceptable.

    5. **HMR for layout files** (optional but valuable):
       - Edit `_layouts/default.njk` — change the `<h1>` text in
         scaffold-check (well, default doesn't have an h1; instead
         test by editing scaffold-check.njk's "Scaffold Check"
         heading text to "Scaffold Verification") and save.
       - The page should re-render. Layout/template edits trigger
         Eleventy to re-template AND Vite to refresh — full reload
         is acceptable; HMR proper only applies to `src/**.ts` per
         Phase 74-03 UAT.
       - Revert the change.

    **Verification — `/` (the SPA, regression check):**

    6. Open `http://localhost:8080/` in a browser (separate tab).

    7. **SPA loads correctly.** Header, map view, filter button
       overlay all render. Mapbox tiles appear (NOT blank gray) —
       confirms `VITE_MAPBOX_TOKEN` flows through to `import.meta.env`
       under the same dev-server process.

    8. **Data layer loads.** Small dots appear on the map after a
       brief load.

    9. **Filter panel works.** Click the magnifying-glass overlay
       button. The What/Who/Where/When panel opens. Apply any
       filter. The dot count updates.

    10. **Stop the dev server** (`Ctrl-C` in the terminal).

    **Capture for SUMMARY:**
    - Actual dev-server port (expected 8080; record if different)
    - Bee-header bundle gzipped size (re-run `gzip -c
      _site/assets/bee-header-*.js | wc -c` from the latest production
      build)
    - Resolved versions visible in the build-info table

    **Do NOT consider the UAT passing if:**
    - The bee-header chrome at `/_scaffold-check/` does NOT render
      (suggests the bundle didn't load — check DevTools Network tab
      for a 404 on `/src/entries/bee-header.ts` or the hashed bundle
      URL)
    - The build-info table shows literal `{{ build.eleventyVersion }}`
      placeholders (suggests `_data/build.js` was not picked up — check
      the file is named `build.js` exactly, not `build-info.js` or
      `buildInfo.js`)
    - The SPA at `/` is broken (would mean Phase 75 introduced a
      regression; the new files should be additive only)
    - Any uncaught console error related to `<bee-header>`
      registration (`DOMException: NotSupportedError` would suggest
      double-registration; check that `src/entries/bee-header.ts` does
      NOT call `customElements.define` manually)

    **Acceptable observations** (NOT failures):
    - Brief flash of un-styled element on first paint of
      `/_scaffold-check/` — custom-element upgrade is async; this is
      Pitfall #4 and is accepted for v3.1.
    - Eleventy + Vite logs are more verbose than vanilla Vite in dev
      console — same as Phase 74-03 UAT.
    - Initial load of `/_scaffold-check/` in dev mode requests the
      `.ts` source file directly (not a hashed `.js` — Vite middleware
      transforms on-the-fly in dev; production build still hashes).
  </how-to-verify>
  <resume-signal>
    Type "approved" if both URLs work as described — bee-header chrome
    renders at `/_scaffold-check/`, build-info table shows resolved
    version strings, and the SPA at `/` is unchanged from its Phase 74
    state. Or describe any specific issues observed.
  </resume-signal>
</task>

<task type="auto">
  <name>Task 2: Verify CLAUDE.md is current; spot-check planning docs</name>
  <files>(no edits expected; verification only — edit only if Task 1's UAT revealed a doc drift)</files>
  <read_first>
    - CLAUDE.md (the "Running Locally" section was last updated by Phase 74-03)
    - .planning/PROJECT.md (the milestone scope; verify Phase 75's outcome is consistent with its target features)
    - .planning/phases/074-eleventy-build-wrapper/074-03-PLAN.md (`<interfaces>` block — confirms the post-074 CLAUDE.md shape)
  </read_first>
  <action>
    The `/_scaffold-check/` URL is a deploy diagnostic, not a primary
    user-facing URL. CONTEXT decision #1 explicitly says "not linked
    from anywhere; not user-facing". Therefore CLAUDE.md's "Running
    Locally" section does NOT need a new entry pointing at
    `/_scaffold-check/`.

    The expectation entering this task is that CLAUDE.md is already
    correct (Phase 74-03 ended in the right state) and Task 1's UAT
    confirmed the dev server announces port 8080 (the same port the
    074-03 SUMMARY recorded).

    1. **Verify CLAUDE.md "Running Locally" section is current**:
       ```bash
       grep -A 12 "## Running Locally" CLAUDE.md
       ```
       Expected (verbatim, all six lines present):
       - `# Dev server (Eleventy + Vite middleware, hot-reload)`
       - `npm run dev`
       - `# Tests (Vitest)`
       - `npm test`
       - `# Production build (validate-schema -> tsc --noEmit -> eleventy + Vite)`
       - `npm run build`
       - `# Data pipeline` / `cd data && python run.py`
       - `# Data pipeline tests (pytest)` / `cd data && uv run pytest`

       If grep finds all six expected command lines (no `cd frontend`
       prefix anywhere; both `npm run dev` and `npm run build` present),
       no edit is needed — proceed to step 2.

       If grep reveals doc drift (e.g., a `cd frontend` reference
       slipped in, or the new build chain is missing) — surface it to
       the user before editing; this would mean a prior phase's doc
       update did not stick.

    2. **Spot-check `.planning/PROJECT.md` for stale scope language**:
       ```bash
       grep -n "Eleventy authoring scaffold" .planning/PROJECT.md
       ```
       Expected: matches the v3.1 milestone target features list
       (line ~11). The line currently reads "Eleventy authoring
       scaffold in place (input dir, layout, base template) ready for
       v3.2 content but with no new content pages added in this
       milestone." After Phase 75 ships, this scope is FULFILLED —
       PROJECT.md does NOT need to be edited because PROJECT.md
       describes the milestone's target features, not its completion
       state. The completion state belongs in ROADMAP.md (Task 3) and
       STATE.md (Task 3).

    3. **Spot-check that nothing in `_pages/`, `_layouts/`, `_data/`,
       or `src/entries/` regressed during the UAT**:
       ```bash
       test -f _layouts/base.njk
       test -f _layouts/default.njk
       test -f _data/build.js
       test -f src/entries/bee-header.ts
       test -f _pages/scaffold-check.njk
       ```
       (All five must exist — they were created in 075-01. This is a
       defensive check in case the UAT step accidentally edited a
       file and forgot to revert.)

    Edit ONLY if Task 1's UAT revealed something the planner did not
    anticipate (e.g., the dev server now uses a different port AND
    that port appears in any doc — currently it does not, but verify).
    Otherwise, this task is a pure no-op verification step that
    proceeds to Task 3.
  </action>
  <verify>
    <automated>set -e; \
grep -q "## Running Locally" CLAUDE.md; \
grep -q "npm run dev" CLAUDE.md; \
grep -q "npm run build" CLAUDE.md; \
grep -q "npm test" CLAUDE.md; \
! grep -q "cd frontend" CLAUDE.md; \
test -f _layouts/base.njk; \
test -f _layouts/default.njk; \
test -f _data/build.js; \
test -f src/entries/bee-header.ts; \
test -f _pages/scaffold-check.njk; \
grep -q "Eleventy authoring scaffold" .planning/PROJECT.md</automated>
  </verify>
  <done>
    CLAUDE.md "Running Locally" section is current (no `cd frontend`,
    has `npm run dev` + `npm run build` + `npm test`). All five files
    created in 075-01 still exist. PROJECT.md milestone scope language
    is unchanged (it describes targets, not completion). No edits to
    docs were needed (or, if they were, they were narrowly scoped to
    drift discovered during Task 1's UAT).
  </done>
</task>

<task type="auto">
  <name>Task 3: Update ROADMAP.md, STATE.md, and write phase summary</name>
  <files>.planning/ROADMAP.md, .planning/STATE.md, .planning/phases/075-authoring-scaffold/075-PHASE-SUMMARY.md</files>
  <read_first>
    - .planning/ROADMAP.md (lines 309–339 specifically — the v3.1 milestone block + Phase 74 detail; mirror Phase 74's entry shape for Phase 75)
    - .planning/STATE.md (top — the YAML frontmatter and Current Position section)
    - .planning/phases/074-eleventy-build-wrapper/074-PHASE-SUMMARY.md (the structural template to mirror — frontmatter, Plans table, Requirements Status, Patterns Established, Phase Boundary Preserved)
    - .planning/phases/075-authoring-scaffold/075-01-SUMMARY.md (created by 075-01; rolled up here)
  </read_first>
  <action>
    Update milestone-tracking docs and write the phase roll-up summary.

    1. **Update `.planning/ROADMAP.md` — v3.1 milestone block**:

       a. Find the heading `## 🔲 v3.1 Eleventy Build Wrapper (Phases 74–75)`
          and change it to `## ✅ v3.1 Eleventy Build Wrapper (Phases 74–75)`.

       b. Find the line `- [ ] Phase 75: Authoring Scaffold and
          Verification` and change it to:
          `- [x] Phase 75: Authoring Scaffold and Verification (2/2 plans) — completed YYYY-MM-DD`
          (with `YYYY-MM-DD` = today's date in ISO form, matching
          Phase 74's completion-date format).

       c. Find Phase 74's detail block (`### Phase 74: Eleventy Outer
          Build Integration`, lines ~324–339) and add a Phase 75
          detail block IMMEDIATELY AFTER it, mirroring Phase 74's
          structure verbatim:

          ```markdown
          ### Phase 75: Authoring Scaffold and Verification
          **Goal**: Eleventy authoring scaffold (input dir, layout chain, base template) in place ready for v3.2 content; orphan page at `/_scaffold-check/` verifies the Eleventy + Vite + Lit pipeline produces a working `_site/` artifact end-to-end.
          **Depends on**: Phase 74
          **Requirements**: D-01 (orphan page at /_scaffold-check/), D-02 (Nunjucks templating), D-03 (two-layer base+default chain), D-04 (embed bee-header), D-05 (multi-entry Vite build — auto-satisfied by plugin)
          **Success Criteria** (what must be TRUE):
            1. Two-layer Nunjucks layout chain: `_layouts/base.njk` (chrome-less HTML5 shell) + `_layouts/default.njk` (extends base, adds bee-header chrome) using Eleventy front-matter chain (`{{ content | safe }}`, NOT Nunjucks `{% extends %}`).
            2. Side-effect Vite entry `src/entries/bee-header.ts` triggers Lit `@customElement('bee-header')` registration; Vite produces `_site/assets/bee-header-*.js` automatically (no `rollupOptions.input` config needed — plugin auto-derives entries from emitted HTML).
            3. `_data/build.js` exposes `eleventyVersion`, `pluginVersion`, `viteVersion`, `litVersion`, `nodeVersion`, `builtAt`, `gitSha` to templates as `{{ build.* }}`.
            4. Orphan page `_pages/scaffold-check.njk` (filename without underscore; URL with underscore via `permalink: /_scaffold-check/index.html`) renders bee-header chrome + build-info table; permanent deploy diagnostic.
            5. `npm run build` produces `_site/_scaffold-check/index.html` with hashed `<script src="/assets/bee-header-*.js">` reference (research assumption A5 verified).
            6. SPA at `/` unchanged; 172 Vitest tests still green.

          **Plans**: 2 plans
          Plans:
          - [x] 075-01-PLAN.md — Layout chain + bee-header entry + _data/build.js + orphan diagnostic page; build smoke + A5 probe
          - [x] 075-02-PLAN.md — Manual UAT + milestone-close paperwork (ROADMAP/STATE/phase-summary)
          ```

          The `[x]` checkboxes on the Plans subsection assume both
          plans completed in this execution flow. Use the actual
          completion date from `075-01-SUMMARY.md` (frontmatter
          `completed:`) for any date references.

    2. **Update `.planning/STATE.md`** — top frontmatter and Current
       Position:

       Frontmatter changes (replace the existing values, do not add
       new keys):
       - `status:` → `"v3.1 Eleventy Build Wrapper milestone complete; v3.2 (Species Tab) next"`
       - `last_updated:` → current ISO timestamp (use
         `date -u +"%Y-%m-%dT%H:%M:%S.000Z"` or equivalent)
       - `last_activity:` → `"YYYY-MM-DD — Phase 75 (Authoring Scaffold and Verification) complete; v3.1 milestone shippable; bee-header chrome verified at /_scaffold-check/"`
       - `progress.completed_phases:` → `2`
       - `progress.total_plans:` → `5` (3 from Phase 74 + 2 from Phase 75)
       - `progress.completed_plans:` → `5`
       - `progress.percent:` → `100`

       Current Position section (replace the three-line block under
       `## Current Position`):
       - `Phase: 75 of 75 — authoring-scaffold (complete; v3.1 ready to merge)`
       - `Plan: 075-02 (this plan, complete)`
       - `Status: v3.1 milestone shippable; merge to main is the next user-facing step`
       - `Last activity: YYYY-MM-DD — Phase 75 plans 01 + 02 complete; manual UAT approved; phase summary written`

       Do NOT touch the `## Accumulated Context` section's pending
       todos / blockers / quick tasks — those are independent of
       Phase 75 outcome.

    3. **Write `.planning/phases/075-authoring-scaffold/075-PHASE-SUMMARY.md`**.
       Mirror the structure of
       `.planning/phases/074-eleventy-build-wrapper/074-PHASE-SUMMARY.md`
       (read it first to copy the section order verbatim). The
       frontmatter and headline content for Phase 75:

       ```markdown
       ---
       phase: 075-authoring-scaffold
       plans_completed: [01, 02]
       requirements_completed: [D-01, D-02, D-03, D-04, D-05]
       milestone: v3.1
       completed: YYYY-MM-DD
       ---

       # Phase 75: Authoring Scaffold and Verification — Phase Summary

       **Two-layer Nunjucks layout chain (`base.njk` + `default.njk`),
       side-effect Vite entry for `<bee-header>`, build-time metadata
       data file, and orphan verification page at `/_scaffold-check/`
       all in place. `npm run build` produces a working `_site/`
       artifact through the Eleventy + Vite plugin pipeline; the orphan
       page renders bee-header chrome + build-info table in browser;
       SPA at `/` unchanged; 172 Vitest tests green; v3.1 Eleventy
       Build Wrapper milestone complete.**

       ## Plans

       | # | Title | Commit(s) | Status |
       |---|-------|-----------|--------|
       | 01 | Layout chain + bee-header entry + _data/build.js + orphan diagnostic page | (sha from 075-01-SUMMARY) | ✅ |
       | 02 | Manual UAT + milestone-close paperwork | (sha from this plan's commit) | ✅ |

       ## Requirements Status (CONTEXT.md decisions as the requirements set)

       - ✅ **D-01**: Orphan stub at `/_scaffold-check/` — `_pages/scaffold-check.njk` ships permanently as a deploy diagnostic. (Plan 01)
       - ✅ **D-02**: Nunjucks templating — three new `.njk` files; SPA `index.html` continues as Liquid no-op (Phase 74 invariant). (Plan 01)
       - ✅ **D-03**: Two-layer base+default layout chain via Eleventy front-matter (`{{ content | safe }}`, NOT `{% extends %}`). (Plan 01)
       - ✅ **D-04**: `<bee-header>` Lit component embedded in `default.njk`; chrome appears on the orphan page; UAT confirmed in-browser render. (Plan 01 + Plan 02 UAT)
       - ✅ **D-05**: Multi-entry Vite build — research-confirmed automatic via the plugin's HTML processor. `_site/assets/bee-header-*.js` produced alongside `_site/assets/index-*.js` with no `rollupOptions.input` config. (Plan 01)

       ## Phase Boundary Preserved

       - ✅ No new user-facing content pages added (the orphan diagnostic page is not user-facing per CONTEXT decision #1).
       - ✅ SPA still serves at `/` (no URL changes — deferred to v3.2).
       - ✅ No Lit SSR decisions (deferred to v3.2).
       - ✅ 172 Vitest tests green throughout the phase (no test count changes).

       ## v3.2 Entry Conditions

       - ✅ `_layouts/base.njk` and `_layouts/default.njk` ready for content pages — drop a new `.njk` into `_pages/` declaring `layout: default.njk` and it gets bee-header chrome automatically.
       - ✅ `src/entries/` directory established for additional standalone bundle entries (v3.2 likely adds species-page entries here).
       - ✅ `_data/build.js` pattern established for build-time metadata; v3.2 can add `_data/species.js`, `_data/photos.js`, etc. following the same default-export-an-object shape.

       ## Patterns Established

       - **Eleventy front-matter layout chain via `{{ content | safe }}`**: pnwmoths-pattern verbatim. NEVER mix with Nunjucks `{% extends %}` / `{% block %}` (Pitfall #1).
       - **Side-effect entries in `src/entries/`**: one-line `import` modules whose sole purpose is to be Rollup entries that trigger custom-element registration via Lit decorators. No manual `customElements.define`. v3.2 follows the same pattern.
       - **Underscore-prefix-in-URL via `permalink:`**: filename has no leading underscore (sidesteps Eleventy ambiguity); URL has underscore via `permalink: /_<name>/index.html` (Pitfall #2).
       - **Vite HTML processor auto-derives entries** from every templated HTML output. No `rollupOptions.input` config needed for additional bundle entries — declare the `<script type="module" src="/...">` in the layout, Vite walks every emitted page and produces a hashed bundle (research assumption A5 verified end-to-end).

       ## Phase Metrics

       - **Total plans:** 2 (075-01: 4 tasks; 075-02: 3 tasks including 1 checkpoint)
       - **Net new tracked files:** 5 (base.njk, default.njk, build.js, src/entries/bee-header.ts, scaffold-check.njk) + 1 phase summary
       - **Net deleted:** 0
       - **Tests:** 172 → 172 (unchanged)
       - **Bee-header bundle gzipped size:** (record from 075-01 SUMMARY's measurement; research expected ~20–25 KB; CONTEXT budget was <100 KB)
       - **CI runtime impact:** Negligible — one additional small bundle (~25 KB raw) and one additional 1 KB HTML page

       ## Milestone v3.1 Status

       - ✅ Phase 74 (Eleventy Outer Build Integration) complete (2026-04-30).
       - ✅ Phase 75 (this phase) complete (YYYY-MM-DD).
       - ✅ v3.1 Eleventy Build Wrapper milestone shippable. Next user-facing step: merge `gsd/phase-074-eleventy-build-wrapper` (or whatever the milestone branch is named) to `main`. The deploy job will publish `_site/` (with the new orphan page included) to the production CloudFront distribution.

       ## Next Milestone

       v3.2 Species Tab — see `.planning/seeds/species-tab.md`. Phase 75's scaffold makes v3.2 content pages a drop-in (declare `layout: default.njk`, write the body, optionally add a `_data/<topic>.js` for the data feed).

       ---
       *Phase: 075-authoring-scaffold*
       *Phase summary completed: YYYY-MM-DD*
       ```

       Replace each `YYYY-MM-DD` and `(sha from ...)` placeholder with
       the actual values at write time. Read 075-01-SUMMARY.md to
       confirm its commit SHA.

       **Bundle-size capture**: if 075-01-SUMMARY.md recorded the
       gzipped bundle size, copy that number into the Phase Metrics
       row. If not (e.g., the executor forgot to capture it), re-run
       `gzip -c _site/assets/bee-header-*.js | wc -c` from a
       post-build state and record the result.
  </action>
  <verify>
    <automated>set -e; \
grep -q '\[x\] Phase 75:' .planning/ROADMAP.md; \
grep -q '## ✅ v3.1 Eleventy Build Wrapper' .planning/ROADMAP.md; \
grep -q '### Phase 75: Authoring Scaffold and Verification' .planning/ROADMAP.md; \
grep -q '075-01-PLAN.md' .planning/ROADMAP.md; \
grep -q '075-02-PLAN.md' .planning/ROADMAP.md; \
grep -q 'completed_phases: 2' .planning/STATE.md; \
grep -q 'completed_plans: 5' .planning/STATE.md; \
grep -q 'percent: 100' .planning/STATE.md; \
grep -q 'milestone complete' .planning/STATE.md; \
! grep -qE '^Phase: 75 of 75 — authoring-scaffold \(context gathered' .planning/STATE.md; \
test -f .planning/phases/075-authoring-scaffold/075-PHASE-SUMMARY.md; \
grep -q 'plans_completed: \[01, 02\]' .planning/phases/075-authoring-scaffold/075-PHASE-SUMMARY.md; \
grep -q 'requirements_completed: \[D-01, D-02, D-03, D-04, D-05\]' .planning/phases/075-authoring-scaffold/075-PHASE-SUMMARY.md; \
grep -q 'milestone: v3.1' .planning/phases/075-authoring-scaffold/075-PHASE-SUMMARY.md; \
grep -q 'v3.1 Eleventy Build Wrapper milestone shippable' .planning/phases/075-authoring-scaffold/075-PHASE-SUMMARY.md</automated>
  </verify>
  <done>
    `.planning/ROADMAP.md` v3.1 block has heading flipped to `✅`,
    Phase 75 checkbox checked with completion date and 2/2 plan count,
    Phase 75 detail block added (mirroring Phase 74's structure) with
    explicit Goal / Depends on / Requirements / Success Criteria /
    Plans subsection, both plan filenames with `[x]` checkboxes.
    `.planning/STATE.md` frontmatter reflects milestone completion
    (status string updated, completed_phases=2, total_plans=5,
    completed_plans=5, percent=100, last_updated/last_activity bumped);
    Current Position section reads "Phase 75 of 75 (complete; v3.1
    ready to merge)". Phase summary file
    `.planning/phases/075-authoring-scaffold/075-PHASE-SUMMARY.md`
    exists with the structure mirrored from
    `.planning/phases/074-eleventy-build-wrapper/074-PHASE-SUMMARY.md`,
    documents all 5 D-XX requirements as ✅, captures plan SHAs +
    bundle size + patterns established, and concludes with the v3.1
    milestone-shippable assertion.
  </done>
</task>

</tasks>

<verification>
**Phase 75 deliverables checklist** (all must hold after this plan):

- [x] D-01: Orphan stub at `/_scaffold-check/` — `_pages/scaffold-check.njk`
      ships, page renders chrome + table in browser. (Plan 01 + Plan 02 UAT)
- [x] D-02: Nunjucks templating — three new `.njk` files; SPA stays Liquid no-op. (Plan 01)
- [x] D-03: Two-layer base+default layout chain via Eleventy
      front-matter content variable. (Plan 01)
- [x] D-04: `<bee-header>` Lit component embedded; UAT confirms
      in-browser chrome rendering. (Plan 01 build + Plan 02 UAT)
- [x] D-05: Multi-entry Vite build — research-confirmed automatic via
      the plugin's HTML processor; A5 probe verified hashed bee-header
      script reference. (Plan 01)

**Phase boundary preserved:**
- No user-facing content pages (orphan diagnostic is not user-facing)
- SPA still serves at `/` unchanged (UAT regression check confirmed)
- No Lit SSR decisions (deferred to v3.2)
- 172 Vitest tests green throughout

**v3.1 milestone status:**
- Phase 74 ✅ (2026-04-30)
- Phase 75 ✅ (this plan's completion date)
- Milestone shippable; next user-facing step is merge-to-main

**What this plan defers:**
- Merge to `main` and the deploy job's actual run — that's a user
  decision after sign-off. Phase 74's plan 03 already ran the
  GitHub Actions UAT for the workflow shape; this plan's UAT covered
  the local rendering only.
</verification>

<success_criteria>
1. Manual UAT (`npm run dev` → `/_scaffold-check/` and `/`) approved:
   bee-header chrome renders at the orphan page, build-info table
   shows resolved versions (not literal placeholders), SPA at `/`
   works as before.
2. CLAUDE.md verified current (no edits expected; if Task 1 surfaced
   doc drift, narrowly scoped edits applied).
3. ROADMAP.md v3.1 milestone block shows `✅` heading and Phase 75
   checked with completion date; Phase 75 detail block present
   mirroring Phase 74's shape.
4. STATE.md frontmatter updated: completed_phases=2, total_plans=5,
   completed_plans=5, percent=100, last_updated and last_activity
   bumped to current date.
5. Phase summary file `075-PHASE-SUMMARY.md` exists, documents all
   five D-XX requirements as ✅, lists both plan SHAs, and concludes
   with the v3.1 milestone-shippable assertion.
6. No source-code or build-config edits in this plan (the only file
   modifications are to planning artifacts).
</success_criteria>

<output>
After completion, create
`.planning/phases/075-authoring-scaffold/075-02-SUMMARY.md` documenting:

- Manual UAT outcome (approved / issues encountered + resolution)
- Actual dev-server port observed (expected 8080)
- Resolved versions visible in the build-info table during UAT
- Whether CLAUDE.md required edits (expected: no)
- The three planning-artifact edits (ROADMAP.md / STATE.md /
  075-PHASE-SUMMARY.md) and their commit SHA
- Note that Phase 75 closes v3.1 milestone; next step is merge-to-main
  (a user decision, not a plan task)
- Bee-header bundle gzipped size (re-recorded if not already in
  075-01-SUMMARY.md)
</output>
</content>
</invoke>