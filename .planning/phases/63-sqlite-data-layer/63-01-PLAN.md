---
phase: 63-sqlite-data-layer
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/sqlite.ts
  - frontend/src/bee-atlas.ts
  - frontend/src/tests/filter.test.ts
  - frontend/src/tests/bee-atlas.test.ts
  - frontend/src/tests/bee-header.test.ts
  - frontend/src/tests/bee-filter-toolbar.test.ts
  - frontend/src/tests/bee-sidebar.test.ts
  - frontend/src/tests/bee-table.test.ts
autonomous: true
requirements:
  - OCC-05

must_haves:
  truths:
    - "SQLite loads a single occurrences table from occurrences.parquet"
    - "No ecdysis or samples tables exist at runtime"
    - "tablesReady resolves after occurrences table is loaded"
    - "All existing tests pass with the renamed function"
  artifacts:
    - path: "frontend/src/sqlite.ts"
      provides: "Single occurrences table loader"
      contains: "loadOccurrencesTable"
      exports: ["getDB", "tablesReady", "loadOccurrencesTable"]
  key_links:
    - from: "frontend/src/bee-atlas.ts"
      to: "frontend/src/sqlite.ts"
      via: "import { loadOccurrencesTable }"
      pattern: "loadOccurrencesTable"
---

<objective>
Replace the dual ecdysis/samples SQLite tables with a single `occurrences` table loaded from `occurrences.parquet`. Rename `loadAllTables` to `loadOccurrencesTable` across all call sites and mocks.

Purpose: OCC-05 requires the frontend to load occurrence data into a single unified SQLite table.
Output: `sqlite.ts` creates one `occurrences` table matching the schema from `validate-schema.mjs`. All imports and mocks updated.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/63-sqlite-data-layer/63-CONTEXT.md
@.planning/phases/63-sqlite-data-layer/63-PATTERNS.md

<interfaces>
<!-- Authoritative column list from scripts/validate-schema.mjs lines 23-35 -->
occurrences.parquet columns:
  - specimen-side (null for sample-only rows): ecdysis_id, catalog_number, scientificName, recordedBy, fieldNumber, genus, family, floralHost, host_observation_id, inat_host, inat_quality_grade, modified, specimen_observation_id, elevation_m, year, month
  - sample-side (null for specimen-only rows): observation_id, observer, specimen_count, sample_id
  - unified (always populated): lat, lon, date, county, ecoregion_l3

<!-- Current exports from sqlite.ts -->
export function getDB(): Promise<{ sqlite3: SQLiteAPI; db: number }>
export async function loadAllTables(baseUrl: string): Promise<void>  // rename to loadOccurrencesTable
export const tablesReady: Promise<void>
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace dual tables with single occurrences table in sqlite.ts</name>
  <files>frontend/src/sqlite.ts</files>
  <read_first>
    - frontend/src/sqlite.ts
    - scripts/validate-schema.mjs
  </read_first>
  <action>
In `frontend/src/sqlite.ts`:

1. Rename `loadAllTables` to `loadOccurrencesTable` (function declaration on line 63).

2. Replace the two CREATE TABLE statements (lines 66-101) with a single CREATE TABLE:
```sql
CREATE TABLE occurrences (
  ecdysis_id INTEGER,
  catalog_number TEXT,
  scientificName TEXT,
  recordedBy TEXT,
  fieldNumber TEXT,
  genus TEXT,
  family TEXT,
  floralHost TEXT,
  host_observation_id INTEGER,
  inat_host TEXT,
  inat_quality_grade TEXT,
  modified TEXT,
  specimen_observation_id INTEGER,
  elevation_m REAL,
  year INTEGER,
  month INTEGER,
  observation_id INTEGER,
  observer TEXT,
  specimen_count INTEGER,
  sample_id INTEGER,
  lat REAL,
  lon REAL,
  date TEXT,
  county TEXT,
  ecoregion_l3 TEXT
)
```
Column order and types must exactly match the `EXPECTED['occurrences.parquet']` array in `validate-schema.mjs`.

3. Replace the two-file parquet load (lines 103-110) with single file load:
```typescript
const occFile = await asyncBufferFromUrl({ url: `${baseUrl}/occurrences.parquet` });
const occRows = await parquetReadObjects({ file: occFile });
await _insertRows(sqlite3, db, 'occurrences', occRows);
```

4. Update the debug log (line 112) to:
```typescript
console.debug('SQLite table count: occurrences:', occRows.length);
```

5. Update the first-query benchmark (line 120) from `SELECT COUNT(*) FROM ecdysis` to `SELECT COUNT(*) FROM occurrences`.

6. Keep `_serializedExec`, `_init`, `getDB`, `tablesReady`, `_tablesReadyResolve`, `_insertRows`, `_heapMB`, `_benchmarkT0` all unchanged.
  </action>
  <verify>
    <automated>cd /Users/rainhead/dev/beeatlas/frontend && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - sqlite.ts contains `export async function loadOccurrencesTable(`
    - sqlite.ts contains `CREATE TABLE occurrences (`
    - sqlite.ts does NOT contain `CREATE TABLE ecdysis` or `CREATE TABLE samples`
    - sqlite.ts does NOT contain `ecdysis.parquet` or `samples.parquet`
    - sqlite.ts contains `occurrences.parquet`
    - sqlite.ts contains `SELECT COUNT(*) FROM occurrences`
    - sqlite.ts contains `_serializedExec` (preserved)
    - sqlite.ts contains `tablesReady` export (preserved)
  </acceptance_criteria>
  <done>sqlite.ts creates a single occurrences table from occurrences.parquet with all 25 columns matching validate-schema.mjs</done>
</task>

<task type="auto">
  <name>Task 2: Rename loadAllTables to loadOccurrencesTable across all call sites and mocks</name>
  <files>
    frontend/src/bee-atlas.ts,
    frontend/src/tests/filter.test.ts,
    frontend/src/tests/bee-atlas.test.ts,
    frontend/src/tests/bee-header.test.ts,
    frontend/src/tests/bee-filter-toolbar.test.ts,
    frontend/src/tests/bee-sidebar.test.ts,
    frontend/src/tests/bee-table.test.ts
  </files>
  <read_first>
    - frontend/src/bee-atlas.ts
    - frontend/src/tests/filter.test.ts
    - frontend/src/tests/bee-atlas.test.ts
    - frontend/src/tests/bee-header.test.ts
    - frontend/src/tests/bee-filter-toolbar.test.ts
    - frontend/src/tests/bee-sidebar.test.ts
    - frontend/src/tests/bee-table.test.ts
  </read_first>
  <action>
Update every reference from `loadAllTables` to `loadOccurrencesTable`:

**bee-atlas.ts:**
- Line 5: change `import { getDB, loadAllTables, tablesReady }` to `import { getDB, loadOccurrencesTable, tablesReady }`
- Line 267: change `loadAllTables(DATA_BASE_URL)` to `loadOccurrencesTable(DATA_BASE_URL)`

**All 6 test files** (filter.test.ts, bee-atlas.test.ts, bee-header.test.ts, bee-filter-toolbar.test.ts, bee-sidebar.test.ts, bee-table.test.ts):
- In each `vi.mock('../sqlite.ts', ...)` block, change the mock key from `loadAllTables` to `loadOccurrencesTable`:
```typescript
loadOccurrencesTable: vi.fn(() => Promise.resolve()),
```
  </action>
  <verify>
    <automated>cd /Users/rainhead/dev/beeatlas/frontend && npm test -- --run 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - bee-atlas.ts contains `import { getDB, loadOccurrencesTable, tablesReady }`
    - bee-atlas.ts contains `loadOccurrencesTable(DATA_BASE_URL)`
    - bee-atlas.ts does NOT contain `loadAllTables`
    - All 6 test files contain `loadOccurrencesTable:` in their mock blocks
    - No file in frontend/src/ contains the string `loadAllTables`
    - All tests pass (npm test exits 0)
  </acceptance_criteria>
  <done>loadAllTables renamed to loadOccurrencesTable everywhere; all tests pass; no references to old name remain</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| network -> sqlite | Parquet file fetched from CDN loaded into in-memory SQLite |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-63-01 | T (Tampering) | occurrences.parquet fetch | accept | Static file served from CloudFront with TLS; no user-controlled input in URL; same trust model as before (two files -> one file) |
| T-63-02 | I (Info Disclosure) | SQLite in-memory table | accept | All data is public occurrence records; no PII beyond collector names already public on iNaturalist/Ecdysis |
</threat_model>

<verification>
- `cd frontend && npx tsc --noEmit` passes with no errors
- `cd frontend && npm test -- --run` passes all tests
- `grep -r 'loadAllTables' frontend/src/` returns no matches
- `grep 'CREATE TABLE occurrences' frontend/src/sqlite.ts` returns a match
</verification>

<success_criteria>
sqlite.ts creates a single occurrences table with 25 columns; loadOccurrencesTable is the only exported loader function; all existing tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/63-sqlite-data-layer/63-01-SUMMARY.md`
</output>
