---
phase: 157-regions-dropdown-obscured-by-filter-button
plan: 02
type: execute
wave: 2
depends_on: ["157-01"]
files_modified:
  - .planning/phases/157-regions-dropdown-obscured-by-filter-button/157-HUMAN-UAT.md
autonomous: false
auto_advance: false
requirements: [SC-1, SC-2, SC-4]
must_haves:
  truths:
    - "Opening Regions in the COLLAPSED map shows all four options fully visible/clickable; the filter button sits beside (not under) the menu — SC-1, SC-4"
    - "Opening Regions while the LIST pane is expanded shows the menu fully above the list column — SC-1"
    - "Opening Regions while the TABLE pane is expanded shows the menu fully above the table; Mapbox bottom-right attribution remains visible and does NOT bleed over the table pane — SC-1, SC-2 (Phase 108 regression guard)"
    - "All three scenarios verified in BOTH the wide (side pane) and narrow (`max-aspect-ratio: 1`, bottom pane) layouts"
    - "Selecting Off/Counties/Ecoregions/Places from the relocated menu still toggles the corresponding boundary overlay (no behavior regression)"
  artifacts:
    - path: .planning/phases/157-regions-dropdown-obscured-by-filter-button/157-HUMAN-UAT.md
      provides: "operator UAT checklist — three pane states x two layouts, plus the attribution-not-bleeding regression check and boundary-toggle behavior check"
      contains: "attribution"
  key_links:
    - from: "operator"
      to: "regions dropdown stacking + collapsed layout"
      via: "browser + DevTools responsive (narrow aspect ratio) + table-mode attribution inspection"
      pattern: "narrow"
---

<objective>
Author the operator UAT checklist for the regions-dropdown fix and run the blocking human-verify checkpoint. The phase must NOT auto-advance past UAT (UI hint: yes — `autonomous: false` / `auto_advance: false`, per project convention `feedback_uat_ui_phases`).

Source-analysis tests (plan 157-01) prove the relocation and the retained `z-index: 0`, but they cannot prove pixels. This checklist confirms the dropdown actually paints above the pane in all three pane states and both layouts, and — critically — that retaining `bee-map { z-index: 0 }` still keeps the Mapbox bottom-right attribution from bleeding over the table pane (the Phase 108 regression we must not reintroduce).

Purpose: prove the user-visible stacking + layout fix before the phase advances.
Output: `157-HUMAN-UAT.md` checklist + a blocking human-verify checkpoint.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/157-regions-dropdown-obscured-by-filter-button/157-RESEARCH.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write 157-HUMAN-UAT.md</name>
  <files>.planning/phases/157-regions-dropdown-obscured-by-filter-button/157-HUMAN-UAT.md</files>
  <read_first>
    - .planning/phases/153-occurrences-near-me/153-HUMAN-UAT.md — the format to mirror (scenario table: exact steps, expected result, pass/fail). Reuse its structure.
    - .planning/phases/157-regions-dropdown-obscured-by-filter-button/157-RESEARCH.md — the Validation Architecture "Responsive / manual coverage" list (the exact scenarios) and §2 (why the attribution-bleed check matters).
    - .planning/ROADMAP.md — Phase 157 success criteria SC-1..SC-4.
  </read_first>
  <action>
    Write `157-HUMAN-UAT.md` as an operator checklist mirroring the 153 format. Cover this scenario matrix, each in BOTH the wide (side pane) and narrow (DevTools device toolbar or window resized to portrait so `@media (max-aspect-ratio: 1)` triggers the bottom pane) layouts:

    1) Collapsed map: click the Regions button → all four options (Off / Counties / Ecoregions / Places) are fully visible and clickable; the filter (toggle) button sits BESIDE the regions button, not under the opened menu. (SC-1, SC-4)
    2) List pane expanded: open the list pane, then click Regions → the dropdown is fully above the list column, no clipping/obscuring. (SC-1)
    3) Table pane expanded: open the table pane, then click Regions → the dropdown is fully above the table; AND the Mapbox bottom-right attribution/logo is still visible and is NOT painting over the table pane. (SC-1, SC-2 — Phase 108 regression guard)
    4) Behavior sanity: selecting each of Counties / Ecoregions / Places toggles the corresponding boundary overlay on the map, and Off clears it (no behavior regression from the relocation).

    Each row: steps, expected result, [ ] pass / [ ] fail + notes. Add a short header noting `npm run dev` (or the built `/app`) as the test target and how to trigger the narrow layout.

    Then present the blocking human-verify checkpoint and STOP — do not auto-advance (`autonomous: false`, `auto_advance: false`).
  </action>
  <verify>
    <manual>Operator runs the checklist in a browser; records pass/fail per row.</manual>
  </verify>
  <acceptance_criteria>
    - `157-HUMAN-UAT.md` exists with the four scenarios above, each tested in both wide and narrow layouts, with explicit pass/fail checkboxes.
    - The table-mode row explicitly includes the "attribution not bleeding over the table pane" check (SC-2 regression guard).
    - The plan presents a blocking checkpoint and does not auto-advance.
  </acceptance_criteria>
  <done>
    The operator has a complete UAT checklist and the phase halts for human verification before advancing.
  </done>
</task>

</tasks>

<artifacts_produced>
## Artifacts this phase produces

- **`157-HUMAN-UAT.md`** — operator UAT checklist (3 pane states × 2 layouts + attribution regression check + boundary-toggle behavior check).

No source/code changes in this plan. No new packages, symbols, or modules.
</artifacts_produced>

<threat_model>
## Trust Boundaries

None applicable. This plan only authors a markdown checklist and runs a manual verification checkpoint. No code, input, network, or data handling.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-157-UAT-NONE | (none) | UAT checklist doc | accept | No applicable threats — documentation + manual verification only. |
</threat_model>

<verification>
- `157-HUMAN-UAT.md` exists and covers all four scenarios in both layouts.
- Operator records PASS on every row (or files issues for any FAIL) before the phase advances.
</verification>

<success_criteria>
- SC-1: Dropdown fully visible/clickable above the pane in all three pane states and both layouts — operator-confirmed.
- SC-2: Mapbox attribution remains contained below the table pane — operator-confirmed.
- SC-4: Collapsed filter button beside the regions button — operator-confirmed.
- Blocking checkpoint enforced (no auto-advance).
</success_criteria>

<output>
Create `.planning/phases/157-regions-dropdown-obscured-by-filter-button/157-02-SUMMARY.md` when done.
</output>
