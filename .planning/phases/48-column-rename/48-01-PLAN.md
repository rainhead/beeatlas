---
phase: 48-column-rename
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - data/ecdysis_pipeline.py
  - data/export.py
  - data/tests/conftest.py
  - data/tests/test_export.py
  - frontend/src/bee-atlas.ts
  - frontend/src/bee-map.ts
  - frontend/src/bee-sidebar.ts
  - frontend/src/bee-specimen-detail.ts
  - frontend/src/features.ts
  - frontend/src/filter.ts
  - frontend/src/tests/bee-sidebar.test.ts
  - scripts/validate-schema.mjs
autonomous: true
requirements: [REN-01, REN-02, REN-03, REN-04]

must_haves:
  truths:
    - "No Python/TS source file contains 'inat_observation_id' or 'inatObservationId'"
    - "pytest passes with all tests green"
    - "npm test passes with all tests green"
    - "validate-schema.mjs expects host_observation_id (not inat_observation_id)"
    - "Specimen sidebar still renders host plant observation links correctly"
  artifacts:
    - path: "data/ecdysis_pipeline.py"
      provides: "yield key host_observation_id"
      contains: "host_observation_id"
    - path: "data/export.py"
      provides: "SELECT and JOIN using host_observation_id"
      contains: "host_observation_id"
    - path: "scripts/validate-schema.mjs"
      provides: "Schema gate requiring host_observation_id"
      contains: "host_observation_id"
    - path: "frontend/src/bee-sidebar.ts"
      provides: "Specimen interface with hostObservationId"
      contains: "hostObservationId"
    - path: "frontend/src/features.ts"
      provides: "SQL SELECT and property mapping using host_observation_id/hostObservationId"
      contains: "host_observation_id"
  key_links:
    - from: "data/export.py"
      to: "ecdysis_data.occurrence_links"
      via: "SQL JOIN on host_observation_id"
      pattern: "links\\.host_observation_id"
    - from: "frontend/src/features.ts"
      to: "frontend/src/bee-sidebar.ts"
      via: "property mapping host_observation_id -> hostObservationId"
      pattern: "host_observation_id"
    - from: "frontend/src/bee-map.ts"
      to: "frontend/src/bee-sidebar.ts"
      via: "feature property hostObservationId passed to Specimen"
      pattern: "hostObservationId"
---

<objective>
Rename `inat_observation_id` to `host_observation_id` (and camelCase `inatObservationId` to `hostObservationId`) atomically across all 14 touch points in the codebase. Then regenerate parquet and upload to S3 so CI sees the new column name.

Purpose: Disambiguate the existing "host plant observation" column from the upcoming "specimen observation" column (Phase 50). The old name `inat_observation_id` is ambiguous once two different iNat observation ID columns coexist.

Output: All source files use the new column name; DuckDB physical column renamed; parquet on S3 updated; all tests pass.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/48-column-rename/48-CONTEXT.md

<interfaces>
<!-- Key types and contracts across the rename boundary -->

From frontend/src/bee-sidebar.ts (line 17-24):
```typescript
export interface Specimen {
  name: string;
  occid: string;
  inatObservationId?: number | null;  // RENAME TO hostObservationId
  floralHost?: string | null;
  inatHost?: string | null;
  inatQualityGrade?: string | null;
}
```

From data/export.py (lines 108, 116):
```sql
links.inat_observation_id,                              -- RENAME TO host_observation_id
LEFT JOIN inaturalist_data.observations inat ON inat.id = links.inat_observation_id  -- RENAME
```

From scripts/validate-schema.mjs (line 28):
```javascript
'inat_observation_id',  // RENAME TO 'host_observation_id'
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rename inat_observation_id to host_observation_id in all source files and ALTER TABLE in DuckDB</name>
  <read_first>
    data/ecdysis_pipeline.py
    data/export.py
    data/tests/conftest.py
    data/tests/test_export.py
    frontend/src/bee-atlas.ts
    frontend/src/bee-map.ts
    frontend/src/bee-sidebar.ts
    frontend/src/bee-specimen-detail.ts
    frontend/src/features.ts
    frontend/src/filter.ts
    frontend/src/tests/bee-sidebar.test.ts
    scripts/validate-schema.mjs
  </read_first>
  <files>
    data/ecdysis_pipeline.py
    data/export.py
    data/tests/conftest.py
    data/tests/test_export.py
    frontend/src/bee-atlas.ts
    frontend/src/bee-map.ts
    frontend/src/bee-sidebar.ts
    frontend/src/bee-specimen-detail.ts
    frontend/src/features.ts
    frontend/src/filter.ts
    frontend/src/tests/bee-sidebar.test.ts
    scripts/validate-schema.mjs
  </files>
  <action>
    Perform all renames across 12 files. Per D-01, also ALTER TABLE the physical DuckDB column.

    **DuckDB physical column (per D-01):**
    Run in local DuckDB (data/beeatlas.duckdb):
    ```sql
    ALTER TABLE ecdysis_data.occurrence_links RENAME COLUMN inat_observation_id TO host_observation_id;
    ```
    Use: `cd data && python -c "import duckdb; con = duckdb.connect('beeatlas.duckdb'); con.execute('ALTER TABLE ecdysis_data.occurrence_links RENAME COLUMN inat_observation_id TO host_observation_id'); con.close()"`

    **Python files (snake_case: inat_observation_id -> host_observation_id):**

    1. `data/ecdysis_pipeline.py`:
       - Line ~115: docstring `yields {occurrenceID, inat_observation_id}` -> `yields {occurrenceID, host_observation_id}`
       - Line ~175: `yield {"occurrence_id": occurrence_id, "inat_observation_id": obs_id}` -> `yield {"occurrence_id": occurrence_id, "host_observation_id": obs_id}`

    2. `data/export.py`:
       - Line ~108: `links.inat_observation_id,` -> `links.host_observation_id,`
       - Line ~116: `ON inat.id = links.inat_observation_id` -> `ON inat.id = links.host_observation_id`

    3. `data/tests/conftest.py`:
       - Line ~63: `occurrence_id VARCHAR, inat_observation_id BIGINT,` -> `occurrence_id VARCHAR, host_observation_id BIGINT,`

    4. `data/tests/test_export.py`:
       - Line ~19: `'inat_observation_id',` -> `'host_observation_id',`

    **TypeScript files (SQL stays snake_case; TS properties become camelCase hostObservationId):**

    5. `frontend/src/features.ts`:
       - Line ~21 (SQL SELECT): `inat_observation_id,` -> `host_observation_id,`
       - Line ~42 (property mapping): `inat_observation_id: obj.inat_observation_id` -> `host_observation_id: obj.host_observation_id`

    6. `frontend/src/bee-atlas.ts`:
       - Line ~372 (SQL JOIN): `ON e.inat_observation_id = s.observation_id` -> `ON e.host_observation_id = s.observation_id`
       - Line ~396 comment: `inat_observation_id` -> `host_observation_id`
       - Line ~401 (SQL JOIN): `ON e.inat_observation_id = s.observation_id` -> `ON e.host_observation_id = s.observation_id`
       - Line ~769 (SQL SELECT): `inat_observation_id,` -> `host_observation_id,`
       - Line ~789 (property mapping): `inatObservationId: obj.inat_observation_id != null ? Number(obj.inat_observation_id) : null,` -> `hostObservationId: obj.host_observation_id != null ? Number(obj.host_observation_id) : null,`

    7. `frontend/src/bee-map.ts`:
       - Line ~43: `f.get('inat_observation_id')` -> `f.get('host_observation_id')`
       - Line ~47: `inatObservationId: inatId` -> `hostObservationId: inatId`
       - Also rename the local variable `inatId` to `hostObsId` for clarity (optional but consistent).

    8. `frontend/src/bee-sidebar.ts`:
       - Line ~20: `inatObservationId?: number | null;` -> `hostObservationId?: number | null;`

    9. `frontend/src/bee-specimen-detail.ts`:
       - Line ~119: `s.inatObservationId` -> `s.hostObservationId` (two occurrences on lines ~119 and ~120)

    10. `frontend/src/filter.ts`:
        - Line ~144: two occurrences of `inat_observation_id` -> `host_observation_id` in the SQL string

    **Schema gate:**

    11. `scripts/validate-schema.mjs`:
        - Line ~28: `'inat_observation_id',` -> `'host_observation_id',`

    **Test fixtures (TS):**

    12. `frontend/src/tests/bee-sidebar.test.ts`:
        - Lines ~199, ~200: `inatObservationId: null` -> `hostObservationId: null` and `inatObservationId: 99001` -> `hostObservationId: 99001`
        - Lines ~239, ~240: `inatObservationId: null` -> `hostObservationId: null` (two occurrences)

    **Verification after edits:**
    Run `cd data && uv run pytest` and `cd frontend && npm test` to confirm all tests pass with the renamed column.
  </action>
  <verify>
    <automated>cd /Users/rainhead/dev/beeatlas && grep -r "inat_observation_id" data/ scripts/ frontend/src/ --include="*.py" --include="*.ts" --include="*.mjs" | grep -v node_modules | grep -v ".duckdb" ; grep -r "inatObservationId" frontend/src/ --include="*.ts" ; echo "EXIT:$?"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -r "inat_observation_id" data/ scripts/ frontend/src/ --include="*.py" --include="*.ts" --include="*.mjs"` returns zero matches
    - `grep -r "inatObservationId" frontend/src/ --include="*.ts"` returns zero matches
    - `data/ecdysis_pipeline.py` contains `"host_observation_id": obs_id`
    - `data/export.py` contains `links.host_observation_id`
    - `data/tests/conftest.py` contains `host_observation_id BIGINT`
    - `data/tests/test_export.py` contains `'host_observation_id'`
    - `scripts/validate-schema.mjs` contains `'host_observation_id'`
    - `frontend/src/bee-sidebar.ts` contains `hostObservationId?: number | null`
    - `frontend/src/bee-specimen-detail.ts` contains `s.hostObservationId`
    - `frontend/src/features.ts` contains `host_observation_id`
    - `frontend/src/filter.ts` contains `host_observation_id`
    - `frontend/src/bee-atlas.ts` contains `e.host_observation_id = s.observation_id`
    - `frontend/src/bee-map.ts` contains `f.get('host_observation_id')`
    - `frontend/src/tests/bee-sidebar.test.ts` contains `hostObservationId: 99001`
    - `cd data && uv run pytest` exits 0
    - `cd frontend && npm test` exits 0
  </acceptance_criteria>
  <done>All 12 files renamed, DuckDB physical column renamed via ALTER TABLE, pytest passes, npm test passes, zero grep matches for old column name</done>
</task>

<task type="auto">
  <name>Task 2: Regenerate parquet and upload to S3 (per D-02 deployment sequencing)</name>
  <read_first>
    data/run.py
  </read_first>
  <files></files>
  <action>
    Per D-02, run the pipeline locally to regenerate parquet with the new column name and upload to S3 before any code is pushed.

    1. Run: `cd /Users/rainhead/dev/beeatlas/data && python run.py`
       This will execute the full pipeline including export.py which now uses `host_observation_id`. The output parquet at `frontend/public/data/ecdysis.parquet` will have `host_observation_id` instead of `inat_observation_id`.

    2. Verify the parquet has the new column:
       `cd /Users/rainhead/dev/beeatlas && node scripts/validate-schema.mjs`
       This must pass (it now expects `host_observation_id`).

    3. The pipeline's S3 upload step (part of run.py / nightly.sh flow) handles uploading the regenerated parquet to S3. If run.py does not upload to S3 automatically, run:
       `cd /Users/rainhead/dev/beeatlas/data && bash -c 'source nightly.sh'` or the relevant S3 sync command from nightly.sh.

    The goal: S3 parquet has `host_observation_id` before code is pushed, so CI's schema gate sees the new name in both code and S3 data simultaneously.
  </action>
  <verify>
    <automated>cd /Users/rainhead/dev/beeatlas && node scripts/validate-schema.mjs</automated>
  </verify>
  <acceptance_criteria>
    - `node scripts/validate-schema.mjs` exits 0 (parquet at frontend/public/data/ecdysis.parquet has host_observation_id column)
    - `python -c "import duckdb; print(duckdb.connect('data/beeatlas.duckdb', read_only=True).execute('SELECT column_name FROM information_schema.columns WHERE table_name=\\'occurrence_links\\' AND column_name=\\'host_observation_id\\'').fetchone())"` returns a non-None result
  </acceptance_criteria>
  <done>Parquet regenerated with host_observation_id column, validate-schema.mjs passes, S3 has updated parquet</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| N/A | Pure rename — no new inputs, no new trust boundaries |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-48-01 | I (Information Disclosure) | DuckDB ALTER TABLE | accept | Column rename is metadata-only; no data exposure. Local dev DB only. |
| T-48-02 | T (Tampering) | S3 parquet upload | accept | Existing upload mechanism (nightly.sh) with existing IAM permissions; no new attack surface. |
</threat_model>

<verification>
1. `grep -r "inat_observation_id" data/ scripts/ frontend/src/ --include="*.py" --include="*.ts" --include="*.mjs"` returns zero matches
2. `grep -r "inatObservationId" frontend/src/ --include="*.ts"` returns zero matches
3. `cd data && uv run pytest` exits 0
4. `cd frontend && npm test` exits 0
5. `node scripts/validate-schema.mjs` exits 0
</verification>

<success_criteria>
- Zero references to `inat_observation_id` or `inatObservationId` in any Python, TypeScript, or MJS source file
- All pytest and npm test suites pass
- Schema gate validates the new column name in the parquet file
- DuckDB physical column is renamed
- S3 parquet is updated before code push
</success_criteria>

<output>
After completion, create `.planning/phases/48-column-rename/48-01-SUMMARY.md`
</output>
