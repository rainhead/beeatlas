---
phase: 170-source-provenance-facets-rebuild
plan: 02
type: execute
wave: 2
depends_on: ["170-01"]
files_modified:
  - src/filter.ts
  - src/url-state.ts
  - src/features.ts
  - src/bee-map.ts
  - src/bee-pane.ts
  - src/bee-atlas.ts
  - src/style.ts
  - src/bee-occurrence-detail.ts
  - src/tests/occurrence.test.ts
  - src/tests/url-state.test.ts
  - src/tests/filter.test.ts
  - src/tests/build-geojson.test.ts
  - src/tests/bee-pane.test.ts
  - src/tests/bee-map.test.ts
  - src/tests/bee-atlas.test.ts
  - src/tests/bee-occurrence-detail.test.ts
  - docs/domain-model.md
autonomous: true
requirements: [PROV-01, PROV-02, PROV-03]

must_haves:
  truths:
    - "The map filter, map symbology, and the map's hidden-set are driven by `tier`, not the raw `source` string (PROV-02)"
    - "The detail card is driven by `record_type` (orthogonal to tier, D-09/D-10) — 5 variants from 5 record_type values + the isProvisional predicate"
    - "FilterState carries `hiddenTiers` (Set<TierKey>) replacing `hiddenSources`; every FilterState literal updated; `tsc --noEmit` is green (PROV-02)"
    - "`tier=` URL param round-trips (visible-subset encoding with a `none` sentinel); legacy `src=` parses and maps 5→2 to tier vocabulary (back-compat, lossy by design)"
    - "Garbage/hostile `tier=`/`src=` tokens fall back to no-filter, never all-hidden, never crash (anti-blank guard preserved)"
    - "A Vitest assertion compares the occ_id CASE branch priority order across occIdFromRow (occurrence.ts), OCC_ID_SQL_CASE (filter.ts), and occurrence_places.sql — all three equal `ecdysis → inat → inat_obs → checklist` (PROV-03)"
    - "The whole frontend leg ships as ONE atomic commit (PROV-03)"
    - "The occ_id prefix `inat_obs:` and parseOccId().source are UNTOUCHED (D-07)"
  artifacts:
    - path: "src/filter.ts"
      provides: "hiddenTiers FilterState field, tier-driven SQL, exported OCC_ID_SQL_CASE"
      contains: "hiddenTiers"
    - path: "src/url-state.ts"
      provides: "TierKey type, tier= serialize/parse, src= back-compat 5→2 fold"
      contains: "tier"
    - path: "src/tests/occurrence.test.ts"
      provides: "PROV-03 occ_id-coupling cross-file assertion"
      contains: "extractCaseOrder"
    - path: "docs/domain-model.md"
      provides: "social-tier reframe + inat_expert rename documented"
      contains: "inat_expert"
  key_links:
    - from: "src/features.ts"
      to: "data/sqlite_export.py _GEO_COLS (Plan 01)"
      via: "positional geo_blob index 6 (tier)"
      pattern: "row\\[6\\]"
    - from: "src/style.ts"
      to: "feature properties.tier"
      via: "mapbox match expression on ['get','tier']"
      pattern: "'tier'"
    - from: "src/bee-occurrence-detail.ts"
      to: "OccurrenceRow.record_type"
      via: "variant dispatch on row.record_type"
      pattern: "record_type"
    - from: "src/tests/occurrence.test.ts"
      to: "src/filter.ts OCC_ID_SQL_CASE + occurrence_places.sql"
      via: "imported export + readFileSync, asserted equal order"
      pattern: "OCC_ID_SQL_CASE"
---

<objective>
Wave B — the FRONTEND LEG, shipped as ONE ATOMIC COMMIT (PROV-03). Decompose the three coupled
`source` consumers (filter, symbology, detail card) onto the new `tier` + `record_type` facets
that Plan 01 materialized on `marts/occurrences` and published to S3. The filter's organizing
primitive becomes `tier` (`hiddenSources` → `hiddenTiers`); map symbology is tier-driven (Atlas
keeps the recency gradient, Other renders muted, D-08); the detail card stays `record_type`-driven
(D-09/D-10, orthogonal — a 2-value tier cannot pick 5 card variants). Add the PROV-03 occ_id-
coupling assertion. Update the legacy `src=` URL to a 5→2 tier back-compat parse and add a `tier=`
param.

Purpose: Deliver the UI-facing half of the facet decomposition the milestone is built around —
Atlas work pops (recency-graded, the liveness/togetherness signal), external records recede
(muted). The orthogonal facets let a user filter by `tier` without the card losing its 5-way
record-type granularity.

Output: All `src/*.ts` consumers updated, the PROV-03 coupling test, fixture updates across the
test suite, and `docs/domain-model.md` (social reframe + inat_expert). `tsc --noEmit` is the
post-merge gate (the required-field `hiddenTiers` rename fails tsc on any missed literal).

PRECONDITION (do not start the deploy until confirmed): Plan 01's data leg has PUBLISHED to S3
(tier + record_type columns live; checkpoint "published"). Shipping this before the data publishes
deadlocks the deploy validate-db gate against stale S3.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/170-source-provenance-facets-rebuild/170-CONTEXT.md
@.planning/phases/170-source-provenance-facets-rebuild/170-RESEARCH.md
@.planning/phases/170-source-provenance-facets-rebuild/170-PATTERNS.md
@.planning/phases/170-source-provenance-facets-rebuild/170-01-SUMMARY.md
@docs/domain-model.md
</context>

<artifacts_this_phase_produces>
Symbols this plan creates (consumed by the rest of the frontend and Phase 171):
- **`TierKey`** — `'atlas' | 'other'` type, exported from `src/url-state.ts`.
- **`VALID_TIERS`** — `Set<TierKey>(['atlas','other'])` allowlist (the SQL/URL injection guard
  source-of-truth, mirroring `VALID_SOURCES`).
- **`hiddenTiers`** — required `FilterState` field (`Set<TierKey>`), replacing `hiddenSources`.
  Also the `UiState.hiddenTiers` field in url-state.ts.
- **`tier=` URL param** — visible-subset serialization with a `none` all-hidden sentinel.
- **`tier-filter-changed`** — the CustomEvent (renamed from `source-filter-changed`); handler
  `_onTierFilterChanged` on bee-atlas.
- **`record_type`** on `OccurrenceRow` + `OCCURRENCE_COLUMNS` (the wa-sqlite SELECT projection).
- **`tier`** on `OccurrenceProperties` / map feature properties (replaces `source`).
- **`inat_expert`** — the record_type literal the card dispatches on (was `inat_obs`).
- **PROV-03 coupling test** — `extractCaseOrder` + cross-file equality assertions in
  `src/tests/occurrence.test.ts`.
- **`OCC_ID_SQL_CASE` (now exported)** from `src/filter.ts` — the test imports it.

The `record_type` value spellings are fixed by Plan 01's SUMMARY: `ecdysis`→`specimen`,
`waba_specimen`→`waba_specimen`, `waba_sample`→`provisional_sample`, `inat_obs`→`inat_expert`,
`checklist`→`checklist`. Read 170-01-SUMMARY.md to confirm before dispatching the card.
</artifacts_this_phase_produces>

<do_not_touch>
- `src/occurrence.ts` `occIdFromRow` / `parseOccId` / its CASE order — D-07. The test ASSERTS it; it does not change it.
- `data/dbt/models/marts/occurrence_places.sql` CASE — D-07. The test reads it read-only.
- `parseOccId().source` and `bee-atlas.ts:982-984,1013-1015` — this is the occ_id PREFIX vocabulary (`'ecdysis'|'inat'|'inat_obs'|'checklist'`), NOT the column. DO NOT rename.
- The occ_id prefix literal `inat_obs:` everywhere — only the `record_type` VALUE `inat_obs`→`inat_expert` changes (D-06/D-07).
- `style.ts` cluster paint (:40-85) — recency-aggregate-only, does not read source/tier.
- The style-cache bypass logic — this plan changes only the paint EXPRESSION (CLAUDE.md style-cache invariant).
</do_not_touch>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: hiddenSources→hiddenTiers plumbing + tier= URL serialize/parse + src= back-compat</name>
  <files>src/filter.ts, src/url-state.ts, src/bee-atlas.ts, src/bee-pane.ts, src/features.ts</files>
  <behavior>
    - tier= round-trip: `buildParams({ui:{hiddenTiers: new Set(['other'])}})` emits `tier=atlas`; parseParams reads it back to `hiddenTiers={other}`.
    - All-hidden sentinel: hiddenTiers={atlas,other} emits `tier=none`; parseParams('tier=none') → hiddenTiers={atlas,other}.
    - Anti-blank guard: `tier=garbage` (no valid tokens, not 'none') → no filter (hiddenTiers undefined/empty), NEVER all-hidden.
    - src= back-compat 5→2 fold: `src=ecdysis` (one atlas arm) → visibleTiers={atlas} → hiddenTiers={other}; `src=ecdysis,inat_obs` → both tiers visible → hiddenTiers=∅ (no filter); `src=none` → hiddenTiers={atlas,other}. tier= takes precedence when both present.
    - tier filter SQL: hiddenTiers={other} → `o.tier IN ('atlas')`; hiddenTiers={atlas,other} → `1 = 0`.
  </behavior>
  <read_first>
    - src/filter.ts (:27 FilterState.hiddenSources; :33 OccurrenceProperties.source; :76 OccurrenceRow.source union; :98 OCCURRENCE_COLUMNS 'source'; :108 OCC_ID_SQL_CASE — add `export`; :264 isFilterActive; :397-407 source-filter SQL; :443 queryVisibleGeoJSON properties.source)
    - src/url-state.ts (:31 SourceKey; :33 VALID_SOURCES; :38 UiState.hiddenSources; :94-100 buildParams src=; :236-254 parseParams src=; :276 result.filter.hiddenSources; :319-320 result.ui.hiddenSources)
    - src/bee-atlas.ts (:95, :503, :558, :560, :644-645, :691, :1096, :1304, :1541-1542, :1702-1704 — hiddenSources plumbing + @source-filter-changed; AND :980-984, :1011-1015 — parseOccId().source which is DO-NOT-TOUCH)
    - src/bee-pane.ts (:89 hiddenSources prop, :124-125 _hiddenSources state, :545-546 changed handler, :658-666 _onSourceToggle + source-filter-changed dispatch)
    - src/features.ts (:14-17 header comment, :32 `row[6] as source`, :45 properties.source)
    - .planning/phases/170-source-provenance-facets-rebuild/170-RESEARCH.md ("URL Contract" — the full src= back-compat fold + tierOf mapping; "Pitfall 5"; "Code Examples")
    - .planning/phases/170-source-provenance-facets-rebuild/170-PATTERNS.md (the filter.ts + url-state.ts template sections with the exact code shapes to copy)
    - .planning/phases/170-source-provenance-facets-rebuild/170-01-SUMMARY.md (confirmed record_type spellings)
  </read_first>
  <action>
    In url-state.ts: add `export type TierKey = 'atlas' | 'other'` and
    `const VALID_TIERS = new Set<TierKey>(['atlas','other'])`. Rename `UiState.hiddenSources` →
    `hiddenTiers: Set<TierKey>`. In buildParams, emit `tier=` over VALID_TIERS using the existing
    visible-subset + `none` sentinel shape (mirror the current `src=` block at :94-100); STOP
    emitting `src=`. In parseParams, add `tier=` parse copying the anti-blank guard verbatim
    (garbage→no filter, `none`→all-hidden). Add a `src=` BACK-COMPAT branch that runs only when
    `tier=` is absent: parse legacy src tokens against the retained VALID_SOURCES/SourceKey
    allowlist, fold each through `tierOf` (ecdysis/waba_sample/waba_specimen→atlas;
    inat_obs/checklist→other), compute `hiddenTiers = {atlas,other} \ visibleTiers`; `src=none`→
    both hidden. Keep SourceKey/VALID_SOURCES for this back-compat parse ONLY. In filter.ts: rename
    `FilterState.hiddenSources`→`hiddenTiers: Set<TierKey>`; change `OccurrenceProperties.source`→
    `tier: string`; in OccurrenceRow add `tier` and `record_type` (drop the `source` union or keep
    it only if still read — it is not); in OCCURRENCE_COLUMNS replace `'source'` with `'tier',
    'record_type'`; `export` the OCC_ID_SQL_CASE const (for the Task 3 test import) WITHOUT
    changing its branch order (D-07); rewrite the source-filter SQL (:397-407) to the VALID_TIERS
    2-value form with the `o.tier IN (...)` / `1 = 0` shape, preserving the T-164-SQL allowlist
    security comment; in isFilterActive replace `hiddenSources.size`→`hiddenTiers.size`; in
    queryVisibleGeoJSON SELECT `tier` not `source` and set `properties.tier`. In features.ts: rename
    the index-6 decode `const source = row[6]`→`const tier = row[6]`, set `properties.tier`, and
    update the header positional comment to read `tier` at index 6 (positionally coupled to Plan 01's
    _GEO_COLS — they must agree). In bee-atlas.ts: rename every `hiddenSources` literal→`hiddenTiers`,
    `@source-filter-changed`→`@tier-filter-changed`, `_onSourceFilterChanged`→`_onTierFilterChanged`,
    and the event detail type `Set<SourceKey>`→`Set<TierKey>`; DO NOT touch the `parseOccId().source`
    occ_id dispatch at :980-984/:1011-1015 (occ_id PREFIX, not the column). In bee-pane.ts: rename
    the `hiddenSources` prop + `_hiddenSources` state→`hiddenTiers`/`_hiddenTiers`, rename
    `_onSourceToggle`→`_onTierToggle` dispatching `tier-filter-changed` with `{hiddenTiers}` (the
    5→2 toggle render is in Task 2). Run `npx tsc --noEmit` — it is the gate that catches any missed
    FilterState literal (Pitfall 5).
  </action>
  <verify>
    <automated>npm test src/tests/url-state.test.ts src/tests/filter.test.ts && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `grep -rc "hiddenSources" src/` returns 0 (every literal renamed)
    - `grep -c "hiddenTiers" src/filter.ts` ≥ 1 and `grep -c "hiddenTiers" src/bee-atlas.ts` ≥ 1 and `grep -c "hiddenTiers" src/url-state.ts` ≥ 1
    - `grep -c "export const OCC_ID_SQL_CASE\|export.*OCC_ID_SQL_CASE" src/filter.ts` returns 1 (now exported)
    - The OCC_ID_SQL_CASE branch order is byte-unchanged (still ecdysis→inat→inat_obs→checklist; D-07)
    - `grep -c "parseOccId" src/bee-atlas.ts` unchanged and `parsed.source` at :980-984/:1011-1015 still present (occ_id prefix untouched)
    - `src/features.ts` decodes `tier` at row[6]; `properties.tier` set; header comment says tier at index 6
    - `npx tsc --noEmit` exits 0
    - url-state.test.ts + filter.test.ts pass (after Task 3 fixture updates; if run before, the new behavior tests in those files for tier=/src= back-compat pass)
  </acceptance_criteria>
  <done>FilterState carries hiddenTiers; tier= round-trips; src= folds 5→2; tsc green; occ_id prefix untouched.</done>
</task>

<task type="auto">
  <name>Task 2: tier-driven symbology (style.ts) + record_type card dispatch + 2-tier pane toggles + map hidden-set</name>
  <files>src/style.ts, src/bee-occurrence-detail.ts, src/bee-pane.ts, src/bee-map.ts</files>
  <read_first>
    - src/style.ts (:87-102 `_occurrencePointPaint` match on `['get','source']` with checklist→green; AND :40-85 cluster paint which is DO-NOT-TOUCH)
    - src/bee-occurrence-detail.ts (:454-475 variant dispatch: isProvisional → _renderProvisional, then row.source === 'checklist'/'waba_specimen'/'inat_obs' → _renderChecklist/_renderWabaSpecimen/_renderInatObs, else _renderSampleOnly; isSpecimenBacked/isProvisional read ecdysis_id/is_provisional — unchanged)
    - src/bee-pane.ts (:1155-1209 `_renderSources` 5-checkbox list; :1227 call site; :1239 all-hidden hint `=== 5`)
    - src/bee-map.ts (:44-58 filterState default literal with hiddenSources:new Set() — must become hiddenTiers; :60 hiddenSources prop; :273-278 changedProperties handler; :468-469 init filter; :588-592 `_visibleBySource` filtering `f.properties.source`; :610/:614/:625/:659/:669 call sites)
    - .planning/phases/170-source-provenance-facets-rebuild/170-RESEARCH.md ("Symbology (D-08)", "record_type → card variant mapping (D-09)", "Code Examples" style.ts + detail card)
    - .planning/phases/170-source-provenance-facets-rebuild/170-PATTERNS.md (style.ts + bee-occurrence-detail.ts template sections)
    - .planning/phases/170-source-provenance-facets-rebuild/170-01-SUMMARY.md (confirmed record_type spellings — the card dispatches on these)
  </read_first>
  <action>
    In style.ts `_occurrencePointPaint`: change the match from `['get','source']` to
    `['get','tier']`; replace the `'checklist', '#2c7a2c'` branch with `'other', '<MUTED_COLOR>'`
    (D-08 — checklist loses its green and folds into the muted Other treatment); keep the nested
    recency-tier match as the default/fallback (Atlas keeps the recency gradient). Pick a muted
    neutral color/opacity (Claude's discretion — a desaturated grey-blue distinct from the recency
    palette so Atlas pops). Do NOT touch the cluster paint (:40-85) or the style-cache logic. In
    bee-occurrence-detail.ts: change the variant dispatch (:466-474) from `row.source ===` literals
    to `row.record_type ===` literals per the confirmed spellings — `'checklist'`→_renderChecklist,
    `'waba_specimen'`→_renderWabaSpecimen, `'inat_expert'`→_renderInatObs (was `'inat_obs'`), else
    _renderSampleOnly; isProvisional still fires first. Do NOT change the renderer bodies, only the
    dispatch conditions. In bee-pane.ts `_renderSources`: collapse the 5-checkbox list into 2 tier
    toggles ("Atlas work" for `atlas`, "Other records" for `other`) wired to `_onTierToggle`
    (Task 1); rename the method `_renderSources`→`_renderTiers` and its call site; change the
    all-hidden hint check from `=== 5` to `=== 2`. Pick toggle copy that reflects the social framing
    (Atlas = community work; Other = expert observations + literature). In bee-map.ts: change the
    `filterState` default literal field `hiddenSources: new Set()` → `hiddenTiers: new Set()`,
    rename the `hiddenSources` prop → `hiddenTiers`, rename `_visibleBySource` → `_visibleByTier`
    filtering `f.properties.tier` (not `.source`), and update all call sites + the changedProperties
    handler + init filter. The map filter MECHANISM (clustering, ghost, _filterQueryGeneration race
    guard) is unchanged — only the property it reads changes.
  </action>
  <verify>
    <automated>npm test src/tests/bee-pane.test.ts src/tests/bee-map.test.ts src/tests/bee-occurrence-detail.test.ts && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "'get', 'source'\|'get','source'" src/style.ts` returns 0; `grep -c "'tier'" src/style.ts` ≥ 1
    - style.ts cluster paint (:40-85) byte-unchanged; style-cache logic byte-unchanged
    - bee-occurrence-detail.ts dispatch reads `row.record_type` (not `row.source`); `grep -c "record_type === 'inat_expert'" src/bee-occurrence-detail.ts` returns 1
    - bee-pane renders exactly 2 tier toggles (not 5 source checkboxes); all-hidden hint checks `=== 2`
    - bee-map filters on `f.properties.tier` via `_visibleByTier`; `grep -c "_visibleBySource\|properties.source" src/bee-map.ts` returns 0
    - `grep -rc "hiddenSources" src/` returns 0 (incl. bee-map default literal — the required-field gate)
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>Symbology tier-driven (Atlas recency / Other muted); card record_type-driven; 2 tier toggles; map hidden-set on tier.</done>
</task>

<task type="auto">
  <name>Task 3: PROV-03 occ_id-coupling test + fixture updates + docs/domain-model.md</name>
  <files>src/tests/occurrence.test.ts, src/tests/url-state.test.ts, src/tests/filter.test.ts, src/tests/build-geojson.test.ts, src/tests/bee-pane.test.ts, src/tests/bee-map.test.ts, src/tests/bee-atlas.test.ts, src/tests/bee-occurrence-detail.test.ts, docs/domain-model.md</files>
  <read_first>
    - src/tests/occurrence.test.ts (:6 BASE_ROW literal with `source: null`; existing describe/test/expect structure; :60-87 existing occIdFromRow behavioral tests)
    - src/occurrence.ts (:23-30 occIdFromRow — the canonical CASE order the test asserts; DO NOT change)
    - src/filter.ts (the now-exported OCC_ID_SQL_CASE)
    - data/dbt/models/marts/occurrence_places.sql (:43-48 the occ_id CASE block — read-only)
    - src/tests/url-state.test.ts, filter.test.ts, build-geojson.test.ts, bee-pane.test.ts, bee-map.test.ts, bee-atlas.test.ts, bee-occurrence-detail.test.ts (fixtures referencing `source` as a column / hiddenSources — grep each)
    - docs/domain-model.md ("provenance" framing, the five int_combined categories, `inat_obs` references, occ_id prefix vocabulary)
    - .planning/phases/170-source-provenance-facets-rebuild/170-RESEARCH.md ("Proposed: PROV-03 occ_id-coupling Vitest assertion" — the exact extractCaseOrder shape; "record_type → card variant mapping")
    - .planning/phases/170-source-provenance-facets-rebuild/170-PATTERNS.md (the occurrence.test.ts NEW assertion section with the three CASE blocks)
    - .planning/phases/170-source-provenance-facets-rebuild/170-CONTEXT.md (D-01 social reframe, D-06 inat_expert, D-10 PROV-02 wording-drift note)
  </read_first>
  <action>
    Add the PROV-03 coupling test to src/tests/occurrence.test.ts: define
    `extractCaseOrder(sql)` using the regex `/THEN\s+'([a-z_]+):'/g` → array; define the canonical
    `TS_ORDER = ['ecdysis','inat','inat_obs','checklist']`; import `OCC_ID_SQL_CASE` from
    `../filter.ts` and assert `extractCaseOrder(OCC_ID_SQL_CASE).toEqual(TS_ORDER)`; `readFileSync`
    `data/dbt/models/marts/occurrence_places.sql`, slice the block between `CASE` and
    `END AS occ_id`, and assert its `extractCaseOrder` equals TS_ORDER. This asserts coupling
    WITHOUT changing any CASE (D-07/D-11 — the occ_id prefix `inat_obs:` stays). Update fixtures
    across all listed test files: replace `source: '<value>'` row fields with `tier: '<atlas|other>'`
    and `record_type: '<spelling>'` per the confirmed mapping; replace `hiddenSources` Set literals
    with `hiddenTiers` over {atlas,other}; update any `src=` URL-param test expectations to `tier=`
    (keep at least one `src=` back-compat test asserting the 5→2 fold per the Task 1 behavior); in
    build-geojson.test.ts update the geo_blob fixture index 6 from a source value to a tier value;
    extend BASE_ROW in occurrence.test.ts (`source: null` → `tier`/`record_type`). In
    docs/domain-model.md: replace the "provenance" framing with the social framing (D-01 — "whose
    work is this": Atlas / Other, with "Mine" as design intent); rename `inat_obs` record_type
    references to `inat_expert` (D-06) while NOTING the occ_id prefix `inat_obs:` is unchanged
    (D-07); document the tier↔record_type mapping table and the orthogonality (tier drives
    filter/symbology, record_type drives the card, D-09/D-10).
  </action>
  <verify>
    <automated>npm test && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `npm test src/tests/occurrence.test.ts` exits 0 (the 3-way coupling assertion passes)
    - The coupling test imports OCC_ID_SQL_CASE from filter.ts AND readFileSync's occurrence_places.sql AND asserts both equal `['ecdysis','inat','inat_obs','checklist']`
    - `grep -rc "hiddenSources" src/tests/` returns 0; `grep -rc "\.source =\|source:" src/tests/build-geojson.test.ts` reflects tier/record_type fixtures (no stale `source` column fixtures)
    - At least one url-state.test.ts case asserts legacy `src=` back-compat folds 5→2 to hiddenTiers
    - docs/domain-model.md contains `inat_expert` and the social/Atlas-Other framing; still documents the occ_id prefix `inat_obs:` as unchanged
    - `npm test` (full suite) exits 0 AND `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>PROV-03 coupling asserted; all fixtures on tier/record_type; docs reframed; full suite + tsc green.</done>
</task>

<task type="auto">
  <name>Task 4: Ship Wave B as one atomic commit (PROV-03)</name>
  <files>src/filter.ts, src/url-state.ts, src/features.ts, src/bee-map.ts, src/bee-pane.ts, src/bee-atlas.ts, src/style.ts, src/bee-occurrence-detail.ts, src/tests/*, docs/domain-model.md</files>
  <read_first>
    - .planning/phases/170-source-provenance-facets-rebuild/170-01-SUMMARY.md (confirm the data leg PUBLISHED to S3 — this commit's deploy reads occurrences.db from S3; shipping before publish deadlocks the deploy validate-db gate)
    - .planning/phases/170-source-provenance-facets-rebuild/170-CONTEXT.md (D-11 — one atomic commit)
    - CLAUDE.md (§ Deploy paths — code ships via .github/workflows/deploy.yml on push; feedback_ci_is_verification_surface — confirm CI builds on a clean checkout)
  </read_first>
  <action>
    Confirm Plan 01's checkpoint reported "published" (S3 carries tier+record_type, no source).
    Run the full gate one final time: `npm test && npx tsc --noEmit`. Stage ALL Wave B files
    (all src/*.ts consumers + every updated test + docs/domain-model.md) and commit them as ONE
    atomic commit (PROV-03 / D-11) — the three coupled consumers and the coupling test must not
    land in separate commits. Use a message like
    `feat(170): decompose source enum into tier + record_type facets (PROV-01/02/03)`. Do NOT push
    until the data publish is confirmed; per feedback_ci_is_verification_surface, the post-push
    deploy build must be green on a clean checkout (it reads the freshly-published S3 occurrences.db).
  </action>
  <verify>
    <automated>npm test && npx tsc --noEmit && git log --oneline -1</automated>
  </verify>
  <acceptance_criteria>
    - All Wave B files are in a SINGLE commit (git show --stat HEAD lists all src/*.ts consumers + tests + docs/domain-model.md; PROV-03 atomicity)
    - `npm test` exits 0 AND `npx tsc --noEmit` exits 0 at the commit
    - The commit message references PROV-01/02/03
    - Plan 01's S3 publish is confirmed before any push/deploy (data-before-code)
  </acceptance_criteria>
  <done>Frontend leg shipped as one atomic commit; full suite + tsc green; data already published.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| URL params → client SQL | `tier=` / `src=` tokens from the address bar are the ONLY untrusted input; they reach the wa-sqlite filter SQL via string interpolation. |
| S3 occurrences.db → client | Trusted build output (Plan 01); columns are schema-validated by the dbt contract. |

Static-hosted, no server runtime, no auth (CONTEXT: "Mine"/auth explicitly deferred). The single
injection-adjacent surface is the URL-param parser.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-170B-01 | Tampering | SQL injection via `tier=`/`src=` URL param | mitigate | Tokens are allowlist-filtered against the compile-time `VALID_TIERS` (and legacy `VALID_SOURCES`) sets BEFORE any string interpolation — the SQL only ever contains hardcoded literals (preserve the existing T-164-SQL/IV comment at filter.ts:394). Verified by the tier-filter SQL acceptance criteria. |
| T-170B-02 | Denial of Service | Crafted `tier=`/`src=` blanking every row | mitigate | The anti-blank guard is preserved: garbage tokens (visible set empty AND not the explicit `none` sentinel) → treated as NO filter, never all-hidden, never crash. Asserted by Task 1 behavior + a url-state.test.ts case. |
| T-170B-03 | Tampering | Missed FilterState literal → stale `hiddenSources` ships | mitigate | `npx tsc --noEmit` is a blocking gate on every task (required-field contract, Pitfall 5) — a missed literal fails compilation. |
| T-170B-04 | Information disclosure | geo_blob index drift mis-maps tier (features.ts row[6] vs Plan 01 _GEO_COLS) | mitigate | features.ts row[6] decode + Plan 01 _GEO_COLS ship lockstep (data published before this deploys); build-geojson.test.ts asserts the decode. No new data exposed (all public). |
| T-170B-SC | Tampering | npm/pip/cargo installs | accept | This phase installs NO external packages (RESEARCH "Package Legitimacy Audit": N/A) — no supply-chain surface. |

No HIGH-severity threats. The back-compat parser is robust to hostile/garbage tokens by the
preserved anti-blank guard (falls back to no-filter, never all-hidden, never throws).
</threat_model>

<verification>
- `npm test` (full Vitest suite, incl. the PROV-03 coupling test) exits 0
- `npx tsc --noEmit` exits 0 (the required-field hiddenTiers gate — catches any missed literal)
- `grep -rc "hiddenSources" src/` returns 0 (every FilterState literal renamed)
- The occ_id prefix `inat_obs:` and `parseOccId().source` are untouched (D-07)
- OCC_ID_SQL_CASE / occurrence_places.sql / occIdFromRow share order `ecdysis→inat→inat_obs→checklist` (PROV-03 test)
- Symbology reads `tier`; card reads `record_type`; map hidden-set reads `tier`
- `tier=` round-trips; legacy `src=` folds 5→2 (lossy by design); garbage→no filter
- Wave B is ONE atomic commit (PROV-03 / D-11)
- Plan 01's data published to S3 before this deploys; deploy build green on a clean checkout (feedback_ci_is_verification_surface)

## Manual-only (deferred to UAT — visual symbology, D-08)
- Tier filter visually splits Atlas (recency-graded) vs Other (muted) on the map; no green checklist
- A legacy `?src=ecdysis,waba_sample` link still restores the correct visible tier set on reload
</verification>

<success_criteria>
- Map filter, symbology, and the map hidden-set are tier-driven (not raw source); detail card is record_type-driven (D-09/D-10)
- FilterState carries `hiddenTiers` replacing `hiddenSources`; `tsc --noEmit` green
- `tier=` round-trips; `src=` back-compat folds 5→2; garbage tokens → no filter (never all-hidden)
- PROV-03 coupling test asserts the 3-site occ_id CASE order equality; occ_id prefix untouched (D-07)
- Whole frontend leg ships as ONE atomic commit (D-11)
- docs/domain-model.md reframed (social tiers + inat_expert)

> NOTE on PROV-02 wording (D-10): PROV-02 literally says the detail card is "driven by provenance
> tier." Per the locked decision D-09/D-10 the card is **record_type-driven** (orthogonal to tier),
> because a 2-value tier cannot select the 5 card variants. This is the intended decomposition, not
> a gap — the verifier should read PROV-02 as "filter + URL + symbology are tier-driven; the card
> is record_type-driven." The "provenance" naming is superseded by the social framing (D-01).
</success_criteria>

<output>
Create `.planning/phases/170-source-provenance-facets-rebuild/170-02-SUMMARY.md` when done.
Record: the chosen muted color, the tier toggle copy, the atomic commit hash, confirmation the
PROV-03 test passes, and the D-10 card-is-record_type deviation (for the verifier).
</output>
