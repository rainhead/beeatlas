---
phase: 119
plan: 07
type: execute
wave: 4
depends_on: [119-06]
files_modified: []
autonomous: false
requirements: [MAP-01, MAP-02, MAP-03, DET-01]

must_haves:
  truths:
    - "Human has visually confirmed iNat obs render in amber on the map (MAP-01)"
    - "Human has toggled each source checkbox and observed points appearing/disappearing without page reload (MAP-02)"
    - "Human has copied a URL with src= param into a new tab and observed source visibility preserved (MAP-03)"
    - "Human has clicked an amber iNat obs point and seen the dedicated detail card with observer / date / host / image / iNat link (DET-01)"
  artifacts: []
  key_links: []
---

<objective>
Visual UAT for Phase 119: confirm at runtime that all four requirements (MAP-01, MAP-02, MAP-03, DET-01) behave correctly in the dev server. The automated tests already cover the source-level structure; this checkpoint catches Mapbox runtime behavior, cluster-rendering surprises, and any image rendering issues that cannot be asserted via source inspection.

Purpose: UI phases (UI hint: yes per ROADMAP) must NOT auto-advance past UAT — this is the gate that blocks `/gsd-verify-work` until the operator approves visually. The `public/data/occurrences.parquet` is still the pre-Phase-118 31-column schema (per planning_context constraint #8), so iNat obs may not yet appear at all — graceful degradation is the expected behavior.

Output: Operator approval string ("approved" or specific issue report). No file changes.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/119-map-display-source-filter-detail-view/119-UI-SPEC.md
@.planning/phases/119-map-display-source-filter-detail-view/119-VALIDATION.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Visual UAT for MAP-01 / MAP-02 / MAP-03 / DET-01</name>
  <what-built>
    Phase 119 frontend changes:
    - bee-map.ts: amber `#e8a020` paint for `source === 'inat_obs'` features; `_applySourceFilter()` uses synchronous Mapbox `setFilter` driven by `hiddenSources` property.
    - bee-pane.ts: new "Sources" filter row with three checkboxes (Ecdysis specimens / WABA samples / iNat expert obs); emits `source-filter-changed` events; shows "No sources selected. Enable at least one source above." when all three sources hidden.
    - bee-occurrence-detail.ts: new `_renderInatObs(row)` branch renders observer / date / floral host / CC-licensed image / iNat link for `source === 'inat_obs'` rows.
    - bee-atlas.ts: owns `_hiddenSources` state; round-trips through URL `src=` param via `_buildCurrentParams`, `firstUpdated`, and `_onPopState`.
    - url-state.ts: `UiState.hiddenSources` + `buildParams`/`parseParams` for `src=` param with allowlist validation.
    - filter.ts: `OccurrenceRow` extended with `source`, `image_url`, `obs_url`, `user_login`, `license` (all nullable); `OCCURRENCE_COLUMNS` includes the new columns so wa-sqlite SELECTs and GeoJSON spreads propagate them automatically.

    Caveat: `public/data/occurrences.parquet` is still the pre-Phase-118 31-column schema. The new columns will be `null` for every row until the nightly pipeline regenerates the parquet. This is graceful degradation — points still render in their existing recency colors; no iNat-amber points until the new parquet ships.
  </what-built>

  <how-to-verify>
    1. Run `npm test` — full suite must be green before starting visual UAT.
    2. Start the dev server: `npm run dev`. Open the URL it prints (typically `http://localhost:8080`).
    3. **Source filter UI (MAP-02 UI half):**
       - Open the filter panel (magnifying-glass overlay button at top of map).
       - Confirm the new "Sources" row appears with three checkboxes: "Ecdysis specimens", "WABA samples", "iNat expert obs". All three are checked by default.
       - Confirm the row has the layers SVG icon (two stacked polygons) to its left, styled like the other filter-row icons (0.6 opacity).
    4. **Source toggle behavior (MAP-02 map half):**
       - Uncheck "Ecdysis specimens". Map points should disappear within the same frame (no fetch indicator, no reload). Re-check it; points return.
       - Uncheck all three. The occurrence list should show "No sources selected. Enable at least one source above." (NOT the default click prompt).
       - Re-check one. Empty-state message is replaced by the normal click prompt.
       - Note: if `public/data/occurrences.parquet` is still 31 cols, every row has `source = null` and ALL points belong to the "default" branch — toggling Ecdysis off may or may not hide them depending on Mapbox's `null` handling. This is the documented degradation path. Expected behavior in this case: no visible change when toggling sources. Acceptable for this UAT — flag as informational only.
    5. **URL state (MAP-03):**
       - Uncheck "Ecdysis specimens". Inspect the URL: `src=ecdysis` should be present.
       - Uncheck "iNat expert obs" too. URL: `src=ecdysis,inat_obs` (alphabetical).
       - Copy the URL, open in a new tab. The two checkboxes should be unchecked on load (same as the source tab); the map state should match.
       - Browser back button: previous source filter state should restore (popstate).
    6. **iNat obs amber + detail (MAP-01 + DET-01):**
       - This requires data: either run `cd data && uv run python run.py` to regenerate the parquet locally with the Phase 118 schema, OR manually substitute a 36-col parquet at `public/data/occurrences.parquet`. If neither is available, MARK MAP-01 / DET-01 AS "deferred to data-pipeline runtime" and approve based on automated source-inspection only.
       - With data available: locate an amber point on the map. Click it. The sidebar should open showing the iNat detail card with: observed date (in serif italic-ish style), observer login, floral host (italic, only if present), an image (only if license starts with CC), and a "View on iNaturalist" link (target=_blank).
       - Confirm the card style matches the existing `_renderSampleOnly` / `_renderProvisional` cards (`.panel-content.sample-dot-detail` layout).
    7. **No regressions:**
       - Existing click flow on Ecdysis specimen clusters still works.
       - Filter panel taxon / year / month controls still work.
       - Table drawer (table mode) still works.
  </how-to-verify>

  <resume-signal>
    Type "approved" to mark Phase 119 verified.
    If issues: describe each issue clearly. Specify which requirement (MAP-01 / MAP-02 / MAP-03 / DET-01) it falls under and whether it is a blocking defect or informational (graceful degradation).
    If MAP-01 / DET-01 cannot be visually verified because `public/data/occurrences.parquet` lacks `source` data, type "approved with deferred MAP-01/DET-01 (parquet regen pending)" — this acknowledges the automated source-inspection tests carry the contract.
  </resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Operator inspection → approval signal | Human gate; no automated trust assumption |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-119-12 | Repudiation | Visual approval is non-replayable | accept | Approval recorded in SUMMARY; downstream `/gsd-verify-work` regenerates automated assertions independently |
| T-119-SC | Tampering | npm/pip/cargo installs | mitigate | No package installs in this plan |
</threat_model>

<verification>
- Operator types approval signal (with or without deferred-MAP-01/DET-01 caveat)
- No file changes made by this plan
- `npm test` was green at the time of approval (re-run if more than 24h since Plan 119-06 completion)
</verification>

<success_criteria>
- All four requirements visually verified OR explicitly deferred-with-rationale by the operator
- No regressions to existing UI flows (filter panel, table drawer, sidebar overlay)
- URL share-and-restore works for the source filter
</success_criteria>

<output>
Create `.planning/phases/119-map-display-source-filter-detail-view/119-07-SUMMARY.md` when done. Record: the exact operator approval signal; any deferred items; any informational issues observed (e.g., null source column behavior in pre-Phase-118 parquet).
</output>
