---
phase: 119
plan: 06
type: execute
wave: 3
depends_on: [119-02, 119-03, 119-04, 119-05]
files_modified:
  - src/bee-atlas.ts
autonomous: true
requirements: [MAP-02, MAP-03]

must_haves:
  truths:
    - "bee-atlas owns `_hiddenSources: Set<string>` @state (the single source of truth — CLAUDE.md state-ownership invariant)"
    - "bee-pane receives `.hiddenSources` and emits `source-filter-changed`; bee-atlas listener updates state + replaces URL"
    - "bee-map receives `.hiddenSources` and applies the synchronous Mapbox setFilter"
    - "On initial load, _hiddenSources is restored from URL `src=` param via initialParams.ui?.hiddenSources"
    - "On popstate (back/forward), _hiddenSources is restored from parsed.ui?.hiddenSources alongside other UI state"
    - "_buildCurrentParams includes hiddenSources in the ui object literal so the URL stays in sync"
  artifacts:
    - path: "src/bee-atlas.ts"
      provides: "_hiddenSources state + event wiring + URL round-trip"
      contains: "_hiddenSources"
  key_links:
    - from: "bee-atlas _onSourceFilterChanged handler"
      to: "_replaceUrlState()"
      via: "state mutation + URL replace"
      pattern: "_onSourceFilterChanged"
    - from: "bee-atlas render() bindings"
      to: "bee-pane .hiddenSources / bee-map .hiddenSources"
      via: "Lit property bindings + @source-filter-changed listener"
      pattern: "\\.hiddenSources=\\$\\{this\\._hiddenSources\\}"
---

<objective>
Wire the `hiddenSources` reactive state in `bee-atlas` so it (1) owns the Set, (2) receives `source-filter-changed` from bee-pane, (3) passes the Set to both bee-pane and bee-map as properties, (4) round-trips through URL `src=` via `_buildCurrentParams` + `firstUpdated` + `_onPopState`.

Purpose: Close the MAP-02 loop (UI ↔ map ↔ state) and the MAP-03 loop (state ↔ URL). bee-atlas is the coordinator per CLAUDE.md invariant — bee-pane and bee-map remain pure presenters.

Output: bee-atlas.ts has a new `_hiddenSources` @state, a `_onSourceFilterChanged` handler, property bindings to bee-pane and bee-map, and URL restore in both `firstUpdated` and `_onPopState`. All Phase 119 tests green; full suite green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/119-map-display-source-filter-detail-view/119-RESEARCH.md
@.planning/phases/119-map-display-source-filter-detail-view/119-PATTERNS.md
@.planning/phases/119-map-display-source-filter-detail-view/119-UI-SPEC.md

<interfaces>
<!-- Anchors in bee-atlas.ts the executor will extend. -->

From src/bee-atlas.ts (existing _checklistVisible state declaration around line 232 — analog pattern):
  @state() private _checklistVisible = false;

From src/bee-atlas.ts (existing _buildCurrentParams around lines 528-539 — `ui` object literal where hiddenSources must be added):
  return buildParams(view, filter, selection, {
    boundaryMode: this._boundaryMode,
    paneState: this._paneState,
    checklistVisible: this._checklistVisible,
  });

From src/bee-atlas.ts (existing firstUpdated URL restore around lines 227-232):
  this._checklistVisible = initialParams.ui?.checklistVisible ?? false;

From src/bee-atlas.ts (existing _onPopState UI restore around lines 587-589):
  this._boundaryMode = parsed.ui?.boundaryMode ?? 'off';
  this._checklistVisible = parsed.ui?.checklistVisible ?? false;

From src/bee-atlas.ts (existing _onChecklistLayerChanged event handler around lines 965-968 — analog pattern):
  private _onChecklistLayerChanged(e: CustomEvent<{ visible: boolean }>) {
    this._checklistVisible = e.detail.visible;
    this._replaceUrlState();
  }

From src/bee-atlas.ts (render() — locate the existing <bee-pane ...> and <bee-map ...> bindings around line 200 to know where to add the new property/listener bindings).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Declare _hiddenSources state + _onSourceFilterChanged handler + render() bindings</name>
  <files>src/bee-atlas.ts</files>
  <read_first>
    - src/bee-atlas.ts (entire file is large but well-organized; focus on the `@state` declarations around lines 220-240, `_onChecklistLayerChanged` around lines 965-968, and `render()` around line 200 where bee-pane and bee-map are instantiated)
    - .planning/phases/119-map-display-source-filter-detail-view/119-PATTERNS.md (`src/bee-atlas.ts — own _hiddenSources state, wire event, URL round-trip` section has all four wire-in points)
  </read_first>
  <behavior>
    - `bee-atlas` declares `@state() private _hiddenSources: Set<string> = new Set();` next to other UI-state fields
    - `_onSourceFilterChanged` accepts `CustomEvent<{ hiddenSources: Set<string> }>`, assigns `this._hiddenSources = e.detail.hiddenSources`, and calls `this._replaceUrlState()`
    - The `<bee-pane>` element in render() gets `.hiddenSources=${this._hiddenSources}` and `@source-filter-changed=${this._onSourceFilterChanged}`
    - The `<bee-map>` element in render() gets `.hiddenSources=${this._hiddenSources}`
  </behavior>
  <action>
    1. Add `@state() private _hiddenSources: Set<string> = new Set();` next to the existing `@state() private _checklistVisible = false;` declaration.
    2. Add a new private method `_onSourceFilterChanged` mirroring `_onChecklistLayerChanged` structure:
       ```
       private _onSourceFilterChanged(e: CustomEvent<{ hiddenSources: Set<string> }>) {
         this._hiddenSources = e.detail.hiddenSources;
         this._replaceUrlState();
       }
       ```
       Place it near `_onChecklistLayerChanged` to keep related handlers grouped.
    3. In `render()`, on the `<bee-pane>` element (alongside existing bindings like `.checklistVisible=${this._checklistVisible}` and `@checklist-layer-changed=${this._onChecklistLayerChanged}`), add:
       ```
       .hiddenSources=${this._hiddenSources}
       @source-filter-changed=${this._onSourceFilterChanged}
       ```
    4. On the `<bee-map>` element (alongside its existing properties), add:
       ```
       .hiddenSources=${this._hiddenSources}
       ```

    Do NOT modify any other state, handler, or binding. Do NOT touch `isFilterActive`, `_filterQueryGeneration`, `_visibleIds`, or the SQLite query path (RESEARCH confirms source filtering is orthogonal to filterState). Do NOT pass hiddenSources to other components.
  </action>
  <verify>
    <automated>npm test -- src/tests/bee-atlas.test.ts -t "MAP-02" 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `grep -E "@state\(\) private _hiddenSources" src/bee-atlas.ts` matches exactly one line
    - `grep -c "_onSourceFilterChanged" src/bee-atlas.ts` returns at least 2 (definition + render() listener binding)
    - `grep -F ".hiddenSources=${this._hiddenSources}" src/bee-atlas.ts` matches at least 2 lines (bee-pane + bee-map bindings)
    - `grep -F "@source-filter-changed=${this._onSourceFilterChanged}" src/bee-atlas.ts` matches exactly one line
    - `grep -F "this._replaceUrlState()" src/bee-atlas.ts` count is at least 1 greater than pre-plan (the new handler adds one call)
    - `npm test -- src/tests/bee-atlas.test.ts -t "MAP-02"` passes both MAP-02 tests in bee-atlas.test.ts
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>State + handler + bindings in place; bee-atlas MAP-02 tests green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: URL round-trip — _buildCurrentParams + firstUpdated + _onPopState</name>
  <files>src/bee-atlas.ts</files>
  <read_first>
    - src/bee-atlas.ts (`_buildCurrentParams` around lines 528-539; `firstUpdated` URL restore around lines 227-232; `_onPopState` around lines 587-589 — all three sites need symmetric edits)
    - src/url-state.ts (confirm the `UiState.hiddenSources` field is present from Plan 119-02 so the assignments type-check)
    - .planning/phases/119-map-display-source-filter-detail-view/119-RESEARCH.md (Pitfall 3 — popstate restore is the most-forgotten site; Pitfalls 4 & 5 — buildParams + parseParams `hasFilter` conditions are already handled in Plan 119-02)
  </read_first>
  <behavior>
    - `_buildCurrentParams` passes `hiddenSources: this._hiddenSources` inside the `ui` object literal handed to `buildParams`
    - `firstUpdated` restores `this._hiddenSources = initialParams.ui?.hiddenSources ?? new Set()` so URL `?src=...` is honored on initial load
    - `_onPopState` restores `this._hiddenSources = parsed.ui?.hiddenSources ?? new Set()` so back/forward navigation preserves source visibility
  </behavior>
  <action>
    1. In `_buildCurrentParams` (the `ui` object literal passed to `buildParams`), add `hiddenSources: this._hiddenSources` next to `checklistVisible: this._checklistVisible`. Preserve the order of other fields; add hiddenSources at the tail.
    2. In `firstUpdated`, immediately after the line `this._checklistVisible = initialParams.ui?.checklistVisible ?? false;`, add:
       `this._hiddenSources = initialParams.ui?.hiddenSources ?? new Set();`
    3. In `_onPopState`, immediately after the line `this._checklistVisible = parsed.ui?.checklistVisible ?? false;`, add:
       `this._hiddenSources = parsed.ui?.hiddenSources ?? new Set();`

    Do NOT touch other restore lines. Do NOT add a new state-replace or pushState call (existing `_replaceUrlState` flow covers it).
  </action>
  <verify>
    <automated>npm test 2>&1 | tail -40</automated>
  </verify>
  <acceptance_criteria>
    - `grep -F "hiddenSources: this._hiddenSources" src/bee-atlas.ts` matches at least one line (the buildParams ui literal)
    - `grep -F "initialParams.ui?.hiddenSources ?? new Set()" src/bee-atlas.ts` matches at least one line
    - `grep -F "parsed.ui?.hiddenSources ?? new Set()" src/bee-atlas.ts` matches at least one line
    - `grep -c "_hiddenSources" src/bee-atlas.ts` returns at least 5 (declaration + handler assignment + buildParams + firstUpdated + popstate)
    - `npx tsc --noEmit` exits 0
    - `npm test` — full suite green: all four phase requirements satisfied (MAP-01 from Plan 03, MAP-02 from Plans 04/06, MAP-03 from Plans 02/06, DET-01 from Plan 05); no pre-existing tests regress
  </acceptance_criteria>
  <done>URL round-trip closed at all three sites; full suite green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `initialParams.ui?.hiddenSources` / `parsed.ui?.hiddenSources` → `_hiddenSources` | URL-derived Set crosses into bee-atlas state, then propagates to bee-pane and bee-map as a property |
| CustomEvent `source-filter-changed` from bee-pane | DOM-level event crosses the Shadow DOM boundary (bubbles+composed) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-119-01 | Tampering | URL-derived hiddenSources Set | mitigate | `parseParams` in url-state.ts (Plan 119-02) enforces the `VALID_SOURCES` allowlist before constructing the Set; bee-atlas trusts the typed Set and propagates it without further validation |
| T-119-11 | Repudiation | Source filter changes update URL via replaceState (not pushState) | accept | Intentional: avoids polluting history with every checkbox click. UI-SPEC documents this behavior implicitly via the existing `_replaceUrlState` convention used by checklist toggle |
| T-119-SC | Tampering | npm/pip/cargo installs | mitigate | No package installs in this plan |
</threat_model>

<verification>
- All four Phase 119 requirements pass their automated tests
- `npm test` full suite green (no regressions)
- `npx tsc --noEmit` clean
- Manual sanity (deferred to Plan 119-07 human-verify): on page load with `?src=ecdysis`, Ecdysis points are hidden and the corresponding checkbox is unchecked
</verification>

<success_criteria>
- bee-atlas owns _hiddenSources; bee-pane and bee-map remain pure presenters
- URL src= round-trips through firstUpdated and _onPopState
- _replaceUrlState fires on every checkbox toggle
</success_criteria>

<output>
Create `.planning/phases/119-map-display-source-filter-detail-view/119-06-SUMMARY.md` when done. Confirm: all 4 phase requirements have passing automated tests; `npm test` is fully green; `_hiddenSources` is referenced at 5 distinct sites in bee-atlas.ts (declaration, handler, _buildCurrentParams, firstUpdated, _onPopState).
</output>
