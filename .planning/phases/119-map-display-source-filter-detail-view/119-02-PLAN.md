---
phase: 119
plan: 02
type: execute
wave: 1
depends_on: [119-01]
files_modified:
  - src/url-state.ts
autonomous: true
requirements: [MAP-03]

must_haves:
  truths:
    - "buildParams emits `src=` (alphabetically sorted, comma-joined) when hiddenSources is non-empty"
    - "buildParams omits `src` when hiddenSources is empty or undefined"
    - "parseParams populates ui.hiddenSources as a typed Set when src= is present"
    - "parseParams ignores unknown tokens (allowlist filtered)"
    - "An `src`-only URL triggers result.ui (hasFilter condition extended)"
  artifacts:
    - path: "src/url-state.ts"
      provides: "UiState.hiddenSources + src= round-trip"
      contains: "hiddenSources"
  key_links:
    - from: "src/url-state.ts UiState"
      to: "src/bee-atlas.ts _hiddenSources state (Plan 119-06)"
      via: "buildParams/parseParams + initialParams.ui.hiddenSources"
      pattern: "hiddenSources"
---

<objective>
Turn the Wave 0 MAP-03 RED tests green by extending `UiState`, `buildParams`, and `parseParams` with the `src=` URL parameter for hidden source filter state.

Purpose: URL state ownership stays in pure functions; `bee-atlas` (Plan 119-06) reads/writes through these without needing its own serialization logic.

Output: `url-state.ts` with `hiddenSources?: Set<'ecdysis' | 'waba_sample' | 'inat_obs'>` round-tripping cleanly via `src=` comma-joined param. All 6 MAP-03 tests from Plan 119-01 pass.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/119-map-display-source-filter-detail-view/119-RESEARCH.md
@.planning/phases/119-map-display-source-filter-detail-view/119-PATTERNS.md
@.planning/phases/119-map-display-source-filter-detail-view/119-UI-SPEC.md

<interfaces>
<!-- Anchors from url-state.ts the executor will extend. -->

From src/url-state.ts (current UiState):
  export interface UiState {
    boundaryMode: 'off' | 'counties' | 'ecoregions' | 'places';
    paneState: 'list' | 'table' | 'collapsed';
    checklistVisible?: boolean;
  }

From src/url-state.ts (analog patterns — the `cl=1` precedent):
  // buildParams (line ~76):
  if (ui.checklistVisible) params.set('cl', '1');

  // parseParams (line ~234-238):
  const checklistVisible = p.get('cl') === '1';
  if (boundaryMode !== 'off' || paneState !== 'collapsed' || checklistVisible) {
    result.ui = { boundaryMode, paneState, checklistVisible };
  }
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend UiState with hiddenSources and add src= round-trip to buildParams/parseParams</name>
  <files>src/url-state.ts</files>
  <read_first>
    - src/url-state.ts (entire file — locate `UiState` interface, `buildParams` function body around line 76 where `cl` is set, and `parseParams` function body around lines 234–238 where `checklistVisible` is parsed and the `result.ui` emission condition lives)
    - src/tests/url-state.test.ts (the MAP-03 describe block added in Plan 119-01 — this is the contract to satisfy; read the 6 tests to confirm exact expected behavior)
    - .planning/phases/119-map-display-source-filter-detail-view/119-PATTERNS.md (`src/url-state.ts — add src param to UiState / buildParams / parseParams` section has the exact code to use)
    - .planning/phases/119-map-display-source-filter-detail-view/119-RESEARCH.md (Pattern 2 — confirms the allowlist `VALID_SOURCES` and the `hasFilter` extension)
  </read_first>
  <behavior>
    Verified by the 6 MAP-03 tests from Plan 119-01:
    - `buildParams(..., { hiddenSources: new Set(['ecdysis']) }).get('src') === 'ecdysis'`
    - `buildParams(..., defaultUi).has('src') === false`
    - `parseParams('src=ecdysis').ui?.hiddenSources` deep-equals `new Set(['ecdysis'])`
    - `buildParams(..., { hiddenSources: new Set(['inat_obs','ecdysis']) }).get('src') === 'ecdysis,inat_obs'` (alphabetical)
    - `parseParams('src=ecdysis,bogus_source').ui?.hiddenSources` deep-equals `new Set(['ecdysis'])` (unknown token dropped)
    - `parseParams('src=ecdysis').ui` is defined (hasFilter condition extension)
  </behavior>
  <action>
    1. Add `hiddenSources?: Set<'ecdysis' | 'waba_sample' | 'inat_obs'>` as an optional field on `UiState` (after `checklistVisible`).
    2. In `buildParams`, after the existing `if (ui.checklistVisible) params.set('cl', '1');` block, add: `if (ui.hiddenSources && ui.hiddenSources.size > 0) params.set('src', [...ui.hiddenSources].sort().join(','));`. Sort is lexicographic (default JS `Array.sort()` — produces `ecdysis < inat_obs < waba_sample`).
    3. In `parseParams`, declare a module-level (or function-local) `const VALID_SOURCES = new Set(['ecdysis', 'waba_sample', 'inat_obs'] as const);`. After the existing `checklistVisible` parse line, add:
       ```
       const srcRaw = p.get('src') ?? '';
       const hiddenSources = srcRaw
         ? new Set(srcRaw.split(',').filter(s => VALID_SOURCES.has(s as 'ecdysis'|'waba_sample'|'inat_obs')) as Array<'ecdysis'|'waba_sample'|'inat_obs'>)
         : undefined;
       ```
       Treat `hiddenSources` with size 0 (after filtering) as if absent — i.e., `hiddenSources = (set && set.size > 0) ? set : undefined` to avoid an empty Set polluting `result.ui`.
    4. Extend the `result.ui` emission condition from `boundaryMode !== 'off' || paneState !== 'collapsed' || checklistVisible` to additionally OR `(hiddenSources && hiddenSources.size > 0)`. Include `hiddenSources` in the `result.ui` object literal alongside the existing fields.

    Do NOT touch any other URL params (boundary, pane, filter, view, selection). Do NOT modify call sites.
  </action>
  <verify>
    <automated>npm test -- src/tests/url-state.test.ts 2>&1 | tail -40</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "hiddenSources" src/url-state.ts` returns at least 4 (interface field, buildParams write, parseParams local, result.ui field)
    - `grep -c "VALID_SOURCES" src/url-state.ts` returns at least 2 (declaration + usage)
    - `grep -c "params.set('src'" src/url-state.ts` returns 1
    - `npm test -- src/tests/url-state.test.ts` passes all 6 MAP-03 tests (plus the pre-existing url-state tests stay green)
    - `npx tsc --noEmit` exits 0
    - Empty / unknown-only `src=` does NOT cause `result.ui` to be defined when no other UI dimension differs from default (sanity: `parseParams('src=bogus').ui` should be `undefined`)
  </acceptance_criteria>
  <done>UiState extended, src= round-trips alphabetically, allowlist filters unknown tokens, hasFilter condition extended. All 6 MAP-03 tests green; rest of url-state.test.ts still green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `window.location.search` → `parseParams` | User-controlled query string crosses into typed UiState |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-119-01 | Tampering | URL `src` param parsing | mitigate | `VALID_SOURCES` allowlist filters tokens before constructing the Set; the typed Set ensures downstream consumers can only branch on the three known literals. Test #5 from Plan 119-01 enforces this |
| T-119-03 | DoS | Pathological `src=` value (e.g., 10MB string) | accept | `URLSearchParams` caps practical URL length at the browser level (~2000 chars); the filter+Set construction is O(n) on tokens and bounded by the allowlist of 3 |
| T-119-SC | Tampering | npm/pip/cargo installs | mitigate | No package installs in this plan |
</threat_model>

<verification>
- All 6 MAP-03 tests green
- `npm test` overall — no regressions
- `npx tsc --noEmit` clean
</verification>

<success_criteria>
- MAP-03 fully satisfied at the pure-function layer; bee-atlas wiring in Plan 119-06 will surface it at the UI level
- `src` param sorts alphabetically (deterministic — shareable URLs are stable)
- Empty/invalid `src` does not phantom-trigger `result.ui`
</success_criteria>

<output>
Create `.planning/phases/119-map-display-source-filter-detail-view/119-02-SUMMARY.md` when done. Confirm: 6 MAP-03 tests now green; total url-state.test.ts count unchanged elsewhere; `OCCURRENCE_COLUMNS.length` unchanged (this plan only touches url-state.ts).
</output>
