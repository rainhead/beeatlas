---
phase: 119
plan: 04
type: execute
wave: 2
depends_on: [119-01]
files_modified:
  - src/bee-pane.ts
autonomous: true
requirements: [MAP-02]

must_haves:
  truths:
    - "bee-pane renders a 'Sources' filter row with three checkboxes (Ecdysis specimens / WABA samples / iNat expert obs); all checked by default"
    - "Toggling a checkbox dispatches a `source-filter-changed` CustomEvent with `detail.hiddenSources: Set<string>` bubbling and composed"
    - "The hiddenSources @property received from bee-atlas mirrors into a local @state via updated() so the checkboxes reflect URL-restored state"
    - "When hiddenSources.size === 3 (all sources hidden), the occurrence list shows 'No sources selected. Enable at least one source above.' instead of the click prompt"
  artifacts:
    - path: "src/bee-pane.ts"
      provides: "Sources filter row + source-filter-changed dispatch + empty-state copy"
      contains: "_renderSources"
  key_links:
    - from: "bee-pane.ts dispatch source-filter-changed"
      to: "bee-atlas.ts @source-filter-changed listener (Plan 119-06)"
      via: "bubbles+composed CustomEvent"
      pattern: "new CustomEvent\\('source-filter-changed'"
---

<objective>
Add the "Sources" filter row to `bee-pane` following the existing `_renderShow()` / `_onChecklistChange` pattern. Three checkboxes (one per source) drive a `source-filter-changed` CustomEvent that bubbles up to `bee-atlas`. Add the "No sources selected" empty-state copy in the occurrence list when all three sources are hidden.

Purpose: UI-side half of MAP-02 — user can toggle source visibility from the filter panel. `bee-pane` stays a pure presenter (CLAUDE.md invariant): it does not own filter state, only mirrors `hiddenSources` from `bee-atlas` and emits change events.

Output: `bee-pane.ts` declares `hiddenSources` @property + `_hiddenSources` @state mirror, renders `_renderSources()` after `_renderShow()` in the filter list, dispatches `source-filter-changed` on toggle, and shows the empty-state copy when all sources are hidden.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/119-map-display-source-filter-detail-view/119-RESEARCH.md
@.planning/phases/119-map-display-source-filter-detail-view/119-PATTERNS.md
@.planning/phases/119-map-display-source-filter-detail-view/119-UI-SPEC.md

<interfaces>
<!-- Anchors in bee-pane.ts the executor will extend. -->

From src/bee-pane.ts (existing @property + @state pair for checklist, lines 79 and 109):
  @property({ attribute: false }) checklistVisible = false;
  @state() private _showChecklist = false;

From src/bee-pane.ts (existing updated() at lines 503-506):
  updated(changed: PropertyValues) {
    if (changed.has('checklistVisible') && this._showChecklist !== this.checklistVisible) {
      this._showChecklist = this.checklistVisible;
    }
    // ... (continue with existing logic)
  }

From src/bee-pane.ts (existing _onChecklistChange dispatch at lines 599-606):
  private _onChecklistChange(e: Event) {
    const visible = (e.target as HTMLInputElement).checked;
    this._showChecklist = visible;
    this.dispatchEvent(new CustomEvent('checklist-layer-changed', {
      bubbles: true, composed: true,
      detail: { visible },
    }));
  }

From src/bee-pane.ts (existing _renderShow() at lines 1081-1099 and _renderListContent at line 1118 where _renderShow is called):
  ${this._renderShow()}

From src/bee-pane.ts (existing empty-state branch at lines 1130-1132 — the listRows.length === 0 path).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add hiddenSources @property + @state mirror with updated() sync</name>
  <files>src/bee-pane.ts</files>
  <read_first>
    - src/bee-pane.ts (whole file is large — focus on: the `@property`/`@state` declaration block around lines 70-120, the `updated()` method around lines 503-560, the `_onChecklistChange` handler around lines 599-606, the `_renderShow()` method around lines 1081-1099, and the `_renderListContent()` method around lines 1100-1140 where the empty-state copy lives)
    - .planning/phases/119-map-display-source-filter-detail-view/119-PATTERNS.md (the `src/bee-pane.ts — add Sources filter row` section — has the exact property + state + updated patterns)
    - .planning/phases/119-map-display-source-filter-detail-view/119-UI-SPEC.md (copywriting contract: exact label strings, empty-state copy)
  </read_first>
  <behavior>
    - `bee-pane` accepts `hiddenSources: Set<string>` as a reactive property (default empty Set)
    - A private `_hiddenSources: Set<string>` @state mirrors the property and drives the checkbox `.checked` bindings
    - `updated()` syncs `_hiddenSources` from `hiddenSources` when the property changes (one-way property→state mirror; user changes flow back through events, not direct mutation)
  </behavior>
  <action>
    1. Add `@property({ attribute: false }) hiddenSources: Set<string> = new Set();` next to the existing `checklistVisible` property (line ~79).
    2. Add `@state() private _hiddenSources: Set<string> = new Set();` next to the existing `_showChecklist` state (line ~109).
    3. In `updated(changed)` (line ~503), after the existing `checklistVisible` sync block, add:
       ```
       if (changed.has('hiddenSources')) {
         this._hiddenSources = new Set(this.hiddenSources);
       }
       ```
       Use `new Set(this.hiddenSources)` (shallow copy) so the @state holds an independent Set — this enables the checked-binding to re-render reliably on subsequent toggles.
  </action>
  <verify>
    <automated>npm test -- src/tests/bee-pane.test.ts -t "MAP-02" 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `grep -E "@property\(\{ attribute: false \}\) hiddenSources" src/bee-pane.ts` matches exactly one line
    - `grep -E "@state\(\) private _hiddenSources" src/bee-pane.ts` matches exactly one line
    - `grep -F "changed.has('hiddenSources')" src/bee-pane.ts` matches at least one line
    - `grep -F "new Set(this.hiddenSources)" src/bee-pane.ts` matches at least one line
    - `npx tsc --noEmit` exits 0
    - The first MAP-02 test (`declares hiddenSources @property`) passes; remaining MAP-02 tests still fail until Tasks 2 + 3 land
  </acceptance_criteria>
  <done>hiddenSources property + _hiddenSources state mirror in place; updated() sync wired; tsc clean.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add _renderSources() + _onSourceToggle() and wire into _renderListContent</name>
  <files>src/bee-pane.ts</files>
  <read_first>
    - src/bee-pane.ts (the `_renderShow()` method at lines 1081-1099 is the exact analog; the `_renderListContent()` call site for `${this._renderShow()}` is at line ~1118 — the new `${this._renderSources()}` must land immediately after it)
    - src/bee-pane.ts (existing `_onChecklistChange` at lines 599-606 — exact dispatch pattern to copy)
    - .planning/phases/119-map-display-source-filter-detail-view/119-UI-SPEC.md (Source Toggle Controls section: exact labels, default state, accent-color guidance)
    - .planning/phases/119-map-display-source-filter-detail-view/119-PATTERNS.md (`_renderSources() method` block — has the exact SVG layers icon and label list)
  </read_first>
  <behavior>
    - `_renderSources()` returns a `<div class="filter-row">` containing an SVG icon + a `.year-row` with three `<label>` + `<input type="checkbox">` pairs (one per source: ecdysis, waba_sample, inat_obs)
    - Each checkbox is `.checked=${!this._hiddenSources.has(sourceValue)}` (checked = visible)
    - Toggling a checkbox calls `_onSourceToggle(sourceValue, newChecked)` which (a) mutates `_hiddenSources` (delete on check, add on uncheck), (b) dispatches `source-filter-changed` with `detail: { hiddenSources: next }` (bubbles + composed)
    - `_renderListContent()` renders `${this._renderSources()}` directly after `${this._renderShow()}`
  </behavior>
  <action>
    1. Add a new private method `_onSourceToggle(sourceValue: string, checked: boolean)` next to `_onChecklistChange`:
       ```
       private _onSourceToggle(sourceValue: string, checked: boolean) {
         const next = new Set(this._hiddenSources);
         if (checked) next.delete(sourceValue);
         else next.add(sourceValue);
         this._hiddenSources = next;
         this.dispatchEvent(new CustomEvent('source-filter-changed', {
           bubbles: true, composed: true,
           detail: { hiddenSources: next },
         }));
       }
       ```
    2. Add a new private method `_renderSources()` next to `_renderShow()`. Use the layers SVG and `.filter-row` / `.year-row` / `.year-label` classes (existing classes — do NOT introduce new CSS). The `sources` array literal has three entries: `{ value: 'ecdysis', label: 'Ecdysis specimens' }`, `{ value: 'waba_sample', label: 'WABA samples' }`, `{ value: 'inat_obs', label: 'iNat expert obs' }` — labels per UI-SPEC copywriting contract.
       - Each `<input type="checkbox">` MUST include `aria-label="${s.label}"`.
       - Wire `@change=${(e: Event) => this._onSourceToggle(s.value, (e.target as HTMLInputElement).checked)}`.
       - Use the layers SVG from PATTERNS.md (two stacked polygons, 16×16, `stroke-width="1.5"`, `aria-hidden="true"`).
    3. In `_renderListContent()` at line ~1118, add `${this._renderSources()}` on the line immediately after `${this._renderShow()}`. Do not reorder existing rows.

    Do NOT add new CSS classes; the existing `.filter-row` / `.year-row` / `.year-label` cover the styling (UI-SPEC: "no custom styling beyond existing token use"). Do NOT change `_renderShow()`. Do NOT inline a code block — use `html` tagged template literal per Lit convention.
  </action>
  <verify>
    <automated>npm test -- src/tests/bee-pane.test.ts -t "MAP-02" 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "_renderSources" src/bee-pane.ts` returns at least 2 (definition + call site in `_renderListContent`)
    - `grep -c "_onSourceToggle" src/bee-pane.ts` returns at least 2 (definition + inline @change handler)
    - `grep -F "new CustomEvent('source-filter-changed'" src/bee-pane.ts` matches exactly one line
    - `grep -c "'Ecdysis specimens'" src/bee-pane.ts` returns at least 1
    - `grep -c "'WABA samples'" src/bee-pane.ts` returns at least 1
    - `grep -c "'iNat expert obs'" src/bee-pane.ts` returns at least 1
    - `grep -c "ecdysis" src/bee-pane.ts` returns at least 1
    - `grep -c "inat_obs" src/bee-pane.ts` returns at least 1
    - `grep -c "waba_sample" src/bee-pane.ts` returns at least 1
    - `npm test -- src/tests/bee-pane.test.ts -t "MAP-02"` passes all 6 MAP-02 tests in bee-pane.test.ts
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>Sources filter row renders; toggles dispatch source-filter-changed; all 6 bee-pane MAP-02 tests green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Empty-state copy when all sources hidden</name>
  <files>src/bee-pane.ts</files>
  <read_first>
    - src/bee-pane.ts (the `_renderListContent()` empty-state branch around lines 1130-1132 — the conditional that picks between "Loading…" / "Click a point on the map to see details." / `<bee-occurrence-detail>`)
    - .planning/phases/119-map-display-source-filter-detail-view/119-UI-SPEC.md (Copywriting Contract: empty state copy is exactly "No sources selected. Enable at least one source above.")
    - .planning/phases/119-map-display-source-filter-detail-view/119-RESEARCH.md (Open Question #2 — recommendation: show this message when `hiddenSources.size === 3`)
  </read_first>
  <behavior>
    - When `_hiddenSources.size === 3` (all three sources hidden), the occurrence list shows the empty-state copy "No sources selected. Enable at least one source above." instead of the click prompt
    - When `_hiddenSources.size < 3`, existing behavior is preserved (loading placeholder, click prompt, or occurrence list)
  </behavior>
  <action>
    In `_renderListContent()`, modify the empty-state ternary chain. Insert a new branch checking `this._hiddenSources.size === 3` BEFORE the existing `this.listRows.length === 0` branch. Use the existing `.panel-content` + `.hint` class pair (same as the click prompt):
    ```
    this.listLoading
      ? html`<div class="list-placeholder">Loading…</div>`
      : this._hiddenSources.size === 3
        ? html`<div class="panel-content"><p class="hint">No sources selected. Enable at least one source above.</p></div>`
        : this.listRows.length === 0
          ? html`<div class="panel-content"><p class="hint">Click a point on the map to see details.</p></div>`
          : html`<bee-occurrence-detail .occurrences=${this.listRows}></bee-occurrence-detail>`
    ```
    Do NOT introduce new classes. Do NOT change loading or click-prompt copy. Do NOT add the empty-state copy elsewhere (it lives only in the list).
  </action>
  <verify>
    <automated>npm test 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - `grep -F "No sources selected. Enable at least one source above." src/bee-pane.ts` matches exactly one line
    - `grep -F "this._hiddenSources.size === 3" src/bee-pane.ts` matches at least one line
    - The existing "Click a point on the map to see details." copy is unchanged (grep still matches it)
    - `npx tsc --noEmit` exits 0
    - `npm test` shows MAP-02 tests green; MAP-03 + MAP-01 also green at this point (their plans have or will be completing in parallel); DET-01 still red until Plan 119-05 lands
  </acceptance_criteria>
  <done>Empty-state copy in place for all-sources-hidden; no regression in click-prompt or loading placeholder paths.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User checkbox toggle → `_onSourceToggle` | DOM event input; only branches on input.checked boolean + the hardcoded `sourceValue` literal from the closure |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-119-06 | Spoofing | CustomEvent could be synthesized externally | accept | Shadow DOM scoping (bubbles+composed are required for cross-boundary propagation); event detail carries a typed Set. bee-atlas handler treats the Set as data, no privileged action triggered |
| T-119-07 | Tampering | Empty-state copy could be replaced via DOM injection | accept | Lit `html` tagged templates escape interpolated values; no `unsafeHTML` usage; the copy is a static string literal in the source |
| T-119-SC | Tampering | npm/pip/cargo installs | mitigate | No package installs in this plan |
</threat_model>

<verification>
- All 6 MAP-02 tests in bee-pane.test.ts pass
- The PATTERNS.md exact regexes match the new source
- No CSS regressions (no new classes; reuses `.filter-row`, `.year-row`, `.year-label`, `.panel-content`, `.hint`)
- `npx tsc --noEmit` clean
</verification>

<success_criteria>
- UI half of MAP-02 satisfied at the bee-pane layer; bee-atlas wiring (Plan 119-06) ties this to URL state and bee-map
- Empty-state copy renders only when all 3 sources hidden
- Sources row visually consistent with existing `_renderShow()` row
</success_criteria>

<output>
Create `.planning/phases/119-map-display-source-filter-detail-view/119-04-SUMMARY.md` when done. Confirm: 6 MAP-02 bee-pane tests green; new `_renderSources()` + `_onSourceToggle()` methods; empty-state copy lands at exactly one site.
</output>
