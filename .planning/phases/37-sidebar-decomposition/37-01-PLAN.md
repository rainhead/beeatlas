---
phase: 37-sidebar-decomposition
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/bee-filter-controls.ts
  - frontend/src/bee-specimen-detail.ts
  - frontend/src/bee-sample-detail.ts
  - frontend/src/tests/bee-sidebar.test.ts
autonomous: true
requirements: [DECOMP-01, DECOMP-02, DECOMP-03]

must_haves:
  truths:
    - "bee-filter-controls accepts filterState, taxaOptions, countyOptions, ecoregionOptions, boundaryMode, summary as @property inputs"
    - "bee-filter-controls emits filter-changed with full FilterChangedEvent payload on every interaction"
    - "bee-filter-controls holds only transient text input @state, no filter state @state"
    - "bee-specimen-detail renders specimen samples from a samples property and emits close"
    - "bee-sample-detail renders a sample event from a sampleEvent property and emits close"
  artifacts:
    - path: "frontend/src/bee-filter-controls.ts"
      provides: "Controlled filter input component"
      contains: "@customElement('bee-filter-controls')"
    - path: "frontend/src/bee-specimen-detail.ts"
      provides: "Specimen detail presenter"
      contains: "@customElement('bee-specimen-detail')"
    - path: "frontend/src/bee-sample-detail.ts"
      provides: "Sample event detail presenter"
      contains: "@customElement('bee-sample-detail')"
    - path: "frontend/src/tests/bee-sidebar.test.ts"
      provides: "Structural invariant tests for DECOMP-01 through DECOMP-04"
  key_links:
    - from: "frontend/src/bee-filter-controls.ts"
      to: "FilterChangedEvent"
      via: "import type from bee-sidebar.ts"
      pattern: "import type.*FilterChangedEvent.*from.*bee-sidebar"
    - from: "frontend/src/bee-specimen-detail.ts"
      to: "Sample type"
      via: "import type from bee-sidebar.ts"
      pattern: "import type.*Sample.*from.*bee-sidebar"
    - from: "frontend/src/bee-sample-detail.ts"
      to: "SampleEvent type"
      via: "import type from bee-sidebar.ts"
      pattern: "import type.*SampleEvent.*from.*bee-sidebar"
---

<objective>
Create the three sub-components (bee-filter-controls, bee-specimen-detail, bee-sample-detail) and the structural test file. These components are standalone — they do not yet replace anything in bee-sidebar. Plan 02 wires them in.

Purpose: Establish the new component files with correct interfaces, CSS, and behavior so Plan 02 can swap them into bee-sidebar cleanly.
Output: Three new Lit custom elements and a test file with structural invariant tests (tests will initially fail for DECOMP-04 since bee-sidebar is not yet refactored — that is expected).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/37-sidebar-decomposition/37-RESEARCH.md
@.planning/phases/37-sidebar-decomposition/37-UI-SPEC.md

<interfaces>
<!-- Key types and contracts from the codebase that the executor needs -->

From frontend/src/filter.ts:
```typescript
export interface FilterState {
  taxonName: string | null;
  taxonRank: 'family' | 'genus' | 'species' | null;
  yearFrom: number | null;
  yearTo: number | null;
  months: Set<number>;
  selectedCounties: Set<string>;
  selectedEcoregions: Set<string>;
}
```

From frontend/src/bee-sidebar.ts (types to import):
```typescript
export interface Specimen {
  name: string;
  occid: string;
  inatObservationId?: number | null;
  floralHost?: string | null;
}

export interface Sample {
  year: number;
  month: number;
  recordedBy: string;
  fieldNumber: string;
  species: Specimen[];
}

export interface DataSummary {
  totalSpecimens: number;
  speciesCount: number;
  genusCount: number;
  familyCount: number;
  earliestYear: number;
  latestYear: number;
}

export interface TaxonOption {
  label: string;
  name: string;
  rank: 'family' | 'genus' | 'species';
}

export interface SampleEvent {
  observation_id: number;
  observer: string;
  date: string;
  specimen_count: number;
  sample_id: number | null;
  coordinate: number[];
}

export interface FilterChangedEvent {
  taxonName: string | null;
  taxonRank: 'family' | 'genus' | 'species' | null;
  yearFrom: number | null;
  yearTo: number | null;
  months: Set<number>;
  selectedCounties: Set<string>;
  selectedEcoregions: Set<string>;
  boundaryMode: 'off' | 'counties' | 'ecoregions';
}
```

From frontend/src/tests/bee-atlas.test.ts (established test patterns):
```typescript
// readFileSync source analysis pattern:
const src = readFileSync(resolve(__dirname, '../bee-sidebar.ts'), 'utf-8');
expect(src).not.toMatch(/somePattern/);

// elementProperties check pattern:
const props = (BeeMap as unknown as { elementProperties: Map<string, unknown> }).elementProperties;
expect(props.has('layerMode')).toBe(true);
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create bee-filter-controls, bee-specimen-detail, and bee-sample-detail components</name>
  <files>
    frontend/src/bee-filter-controls.ts
    frontend/src/bee-specimen-detail.ts
    frontend/src/bee-sample-detail.ts
  </files>
  <read_first>
    frontend/src/bee-sidebar.ts
    frontend/src/filter.ts
    frontend/src/bee-atlas.ts
    frontend/src/index.css
    .planning/phases/37-sidebar-decomposition/37-RESEARCH.md
    .planning/phases/37-sidebar-decomposition/37-UI-SPEC.md
  </read_first>
  <behavior>
    - bee-filter-controls has @property for filterState, taxaOptions, countyOptions, ecoregionOptions, boundaryMode, summary
    - bee-filter-controls has only three @state fields: _taxonInputText, _countyInputText, _ecoregionInputText
    - bee-filter-controls emits CustomEvent 'filter-changed' with bubbles:true, composed:true containing full FilterChangedEvent
    - bee-filter-controls updated() syncs _taxonInputText from filterState when filterState changes (for popstate restore)
    - bee-specimen-detail has @property for samples (Sample[])
    - bee-specimen-detail emits CustomEvent 'close' with bubbles:true, composed:true on Back button click
    - bee-sample-detail has @property for sampleEvent (SampleEvent)
    - bee-sample-detail emits CustomEvent 'close' with bubbles:true, composed:true on Back button click
  </behavior>
  <action>
    Create three new Lit custom element files by extracting logic from bee-sidebar.ts. Do NOT modify bee-sidebar.ts yet — that happens in Plan 02.

    **bee-filter-controls.ts:**
    - Import `{ css, html, LitElement, type PropertyValues }` from 'lit', decorators from 'lit/decorators.js'
    - Import types: `type { FilterState }` from './filter.ts', `type { DataSummary, TaxonOption, FilterChangedEvent }` from './bee-sidebar.ts'
    - Register as `@customElement('bee-filter-controls')`
    - @property({ attribute: false }) declarations for: filterState (FilterState), taxaOptions (TaxonOption[]), countyOptions (string[]), ecoregionOptions (string[]), boundaryMode ('off'|'counties'|'ecoregions'), summary (DataSummary|null)
    - @state() for only: _taxonInputText (string), _countyInputText (string), _ecoregionInputText (string)
    - Copy these methods verbatim from bee-sidebar.ts, adapting from @state fields to filterState property reads:
      - _emit(partial) — replaces _dispatchFilterChanged, reads from this.filterState instead of this._taxonName etc.
      - _onTaxonInput, _onTaxonChange — use _emit({taxonName, taxonRank}) instead of setting @state then calling _dispatchFilterChanged
      - _onYearFromChange, _onYearToChange — emit partial with yearFrom/yearTo, read current values from this.filterState
      - _onMonthChange — emit partial with months, read current from this.filterState.months
      - _clearFilters — emit with all fields null/empty, boundaryMode 'off'
      - _clearTaxon — emit with taxonName null, taxonRank null
      - _getMonthName (helper, copy verbatim)
      - _onBoundaryToggle — emit with boundaryMode change
      - _onCountyInput, _onCountyChange — emit with updated selectedCounties from this.filterState.selectedCounties
      - _onEcoregionInput, _onEcoregionChange — emit with updated selectedEcoregions
      - _removeCounty, _removeEcoregion — emit with item removed from respective set
    - updated(changedProperties): sync _taxonInputText from filterState+taxaOptions when filterState changes (same logic as bee-sidebar lines 380-401 but reading from this.filterState). Also sync _countyInputText and _ecoregionInputText to '' when their respective sets change.
    - render(): Combine _renderBoundaryToggle() + _renderFilterControls() + _renderRegionControls() from bee-sidebar. The boundary toggle, taxon input+datalist, year inputs, month grid, county/ecoregion inputs+datalists+chips, and "Clear all filters" button. Do NOT include the "Clear selection" button (that belongs in bee-specimen-detail).
    - All chip remove buttons must have `aria-label="Remove ${name}"` per UI-SPEC accessibility notes.
    - Taxon clear button must have `aria-label="Clear taxon filter"`.
    - static styles: Copy these CSS classes from bee-sidebar.ts: .filter-controls, .filter-controls h3, .filter-row, .filter-row input, .year-row, .year-row input, .month-grid, .month-grid label, .taxon-row, .taxon-row input, .taxon-clear-btn, .taxon-clear-btn:hover, .clear-btn, .region-chips, .chip, .chip-type, .chip-remove, .chip-remove:hover, .layer-toggle, .toggle-btn, .toggle-btn:hover, .toggle-btn.active. Add `:host { display: block; }`.

    **bee-specimen-detail.ts:**
    - Import `{ css, html, LitElement }` from 'lit', decorators
    - Import types: `type { Sample }` from './bee-sidebar.ts'
    - Register as `@customElement('bee-specimen-detail')`
    - @property({ attribute: false }) samples: Sample[] = []
    - No @state fields
    - _formatMonth(year, month) helper — copy verbatim from bee-sidebar
    - _onClose(): dispatch CustomEvent('close', { bubbles: true, composed: true })
    - render(): Copy _renderDetail(samples) logic from bee-sidebar (lines 872-891), replacing `this.samples` reference. Add a Back button at the top: `<button class="back-btn" @click=${this._onClose}>&#8592; Back</button>`. Use `this.samples` as the data source.
    - static styles: Copy .back-btn, .sample, .sample-header, .sample-meta, .species-list, .species-list li, .inat-missing from bee-sidebar.

    **bee-sample-detail.ts:**
    - Import `{ css, html, LitElement }` from 'lit', decorators
    - Import types: `type { SampleEvent }` from './bee-sidebar.ts'
    - Register as `@customElement('bee-sample-detail')`
    - @property({ attribute: false }) sampleEvent!: SampleEvent
    - No @state fields
    - _formatSampleDate(dateStr) — copy verbatim from bee-sidebar
    - _onClose(): dispatch CustomEvent('close', { bubbles: true, composed: true })
    - render(): Copy _renderSampleDotDetail(event) logic from bee-sidebar (lines 853-870), using this.sampleEvent. Replace the inline `this.selectedSampleEvent = null` Back handler with `this._onClose()`.
    - static styles: Copy .back-btn, .panel-content, .sample-dot-detail, .sample-dot-detail-header, .event-date, .event-observer, .event-count, .event-inat, .hint from bee-sidebar. Add dt/dd styles.
  </action>
  <verify>
    <automated>cd /Users/rainhead/dev/beeatlas/frontend && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria>
    - frontend/src/bee-filter-controls.ts contains `@customElement('bee-filter-controls')`
    - frontend/src/bee-filter-controls.ts contains `@property({ attribute: false }) filterState`
    - frontend/src/bee-filter-controls.ts contains `@property({ attribute: false }) taxaOptions`
    - frontend/src/bee-filter-controls.ts contains `@property({ attribute: false }) countyOptions`
    - frontend/src/bee-filter-controls.ts contains `@property({ attribute: false }) ecoregionOptions`
    - frontend/src/bee-filter-controls.ts contains `@property({ attribute: false }) boundaryMode`
    - frontend/src/bee-filter-controls.ts contains `@property({ attribute: false }) summary`
    - frontend/src/bee-filter-controls.ts contains `@state() private _taxonInputText`
    - frontend/src/bee-filter-controls.ts contains `@state() private _countyInputText`
    - frontend/src/bee-filter-controls.ts contains `@state() private _ecoregionInputText`
    - frontend/src/bee-filter-controls.ts contains `'filter-changed'`
    - frontend/src/bee-filter-controls.ts contains `bubbles: true, composed: true`
    - frontend/src/bee-filter-controls.ts contains `aria-label="Clear taxon filter"`
    - frontend/src/bee-filter-controls.ts contains `aria-label="Remove`
    - frontend/src/bee-filter-controls.ts does NOT contain `@state() private _taxonName` or `@state() private _yearFrom` (no filter state as @state)
    - frontend/src/bee-specimen-detail.ts contains `@customElement('bee-specimen-detail')`
    - frontend/src/bee-specimen-detail.ts contains `@property({ attribute: false }) samples`
    - frontend/src/bee-specimen-detail.ts contains `'close'`
    - frontend/src/bee-specimen-detail.ts does NOT contain `@state()`
    - frontend/src/bee-sample-detail.ts contains `@customElement('bee-sample-detail')`
    - frontend/src/bee-sample-detail.ts contains `@property({ attribute: false }) sampleEvent`
    - frontend/src/bee-sample-detail.ts contains `'close'`
    - frontend/src/bee-sample-detail.ts does NOT contain `@state()`
    - frontend/src/bee-sample-detail.ts does NOT contain `this.selectedSampleEvent = null`
    - `cd frontend && npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>Three new Lit custom elements compile without errors, each with correct @property declarations, event emissions, migrated CSS, and no filter state held as @state in bee-filter-controls.</done>
</task>

<task type="auto">
  <name>Task 2: Create structural invariant test file for DECOMP requirements</name>
  <files>
    frontend/src/tests/bee-sidebar.test.ts
  </files>
  <read_first>
    frontend/src/tests/bee-atlas.test.ts
    frontend/src/bee-filter-controls.ts
    frontend/src/bee-specimen-detail.ts
    frontend/src/bee-sample-detail.ts
    frontend/src/bee-sidebar.ts
  </read_first>
  <action>
    Create `frontend/src/tests/bee-sidebar.test.ts` following the established test pattern from bee-atlas.test.ts. Use the same vi.mock setup for duckdb.ts, features.ts, and region-layer.ts (these are needed because importing the component triggers its module graph).

    The file must include these test groups:

    **describe('DECOMP-01: bee-filter-controls property interface'):**
    - test: Import BeeFilterControls, check elementProperties has: filterState, taxaOptions, countyOptions, ecoregionOptions, boundaryMode, summary
    - test: Source analysis — bee-filter-controls.ts contains 'filter-changed' string
    - test: Source analysis — bee-filter-controls.ts does NOT match `@state\(\).*private _taxon(?!InputText)` (no filter state as @state, only _taxonInputText allowed)
    - test: Source analysis — bee-filter-controls.ts does NOT match `@state\(\).*private _yearFrom` or `_yearTo` or `_months` or `_selectedCounties` or `_selectedEcoregions`

    **describe('DECOMP-02: bee-specimen-detail property interface'):**
    - test: Import BeeSpecimenDetail, check elementProperties has: samples
    - test: Source analysis — bee-specimen-detail.ts does NOT contain `@state()`

    **describe('DECOMP-03: bee-sample-detail property interface'):**
    - test: Import BeeSampleDetail, check elementProperties has: sampleEvent
    - test: Source analysis — bee-sample-detail.ts does NOT contain `@state()`
    - test: Source analysis — bee-sample-detail.ts does NOT contain `this.selectedSampleEvent = null`

    **describe('DECOMP-04: bee-sidebar is thin layout shell'):**
    These tests will FAIL until Plan 02 completes. That is expected and correct. Write them now so Plan 02 has a red-to-green target.
    - test: bee-sidebar.ts does NOT match `placeholder.*Filter by taxon` (no filter input markup)
    - test: bee-sidebar.ts does NOT match `placeholder.*Filter by county` (no county input markup)
    - test: bee-sidebar.ts does NOT match `placeholder.*Filter by ecoregion`
    - test: bee-sidebar.ts does NOT match `class="species-list"` (no specimen detail markup)
    - test: bee-sidebar.ts does NOT match `class="sample-dot-detail"` (no sample detail markup)
    - test: bee-sidebar.ts does NOT match `_onTaxonInput|_onYearFromChange|_onMonthChange` (no filter handler methods)
    - test: bee-sidebar.ts does NOT match `_renderDetail|_renderSampleDotDetail` (no detail render methods)
    - test: bee-sidebar.ts contains `bee-filter-controls` (composes the sub-component)
    - test: bee-sidebar.ts contains `bee-specimen-detail` (composes the sub-component)
    - test: bee-sidebar.ts contains `bee-sample-detail` (composes the sub-component)

    Use readFileSync + resolve(__dirname, '../filename.ts') for all source analysis tests (same as bee-atlas.test.ts pattern).

    After creating the file, run `cd frontend && npm test -- --run` to verify. DECOMP-01/02/03 tests should pass. DECOMP-04 tests will fail (bee-sidebar not yet refactored) — that is expected. Note which pass and which fail in the commit message.
  </action>
  <verify>
    <automated>cd /Users/rainhead/dev/beeatlas/frontend && npm test -- --run 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - frontend/src/tests/bee-sidebar.test.ts exists
    - frontend/src/tests/bee-sidebar.test.ts contains `describe('DECOMP-01`
    - frontend/src/tests/bee-sidebar.test.ts contains `describe('DECOMP-02`
    - frontend/src/tests/bee-sidebar.test.ts contains `describe('DECOMP-03`
    - frontend/src/tests/bee-sidebar.test.ts contains `describe('DECOMP-04`
    - frontend/src/tests/bee-sidebar.test.ts contains `readFileSync`
    - frontend/src/tests/bee-sidebar.test.ts contains `elementProperties`
    - DECOMP-01 tests pass (bee-filter-controls has correct properties)
    - DECOMP-02 tests pass (bee-specimen-detail has correct properties)
    - DECOMP-03 tests pass (bee-sample-detail has correct properties)
    - DECOMP-04 tests fail (bee-sidebar not yet refactored — this is expected and correct)
  </acceptance_criteria>
  <done>Test file exists with all DECOMP structural invariant tests. DECOMP-01/02/03 pass. DECOMP-04 fails (red — Plan 02 turns these green).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| none | Pure structural refactor — no new network endpoints, no new input paths, no new data storage |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-37-01 | Tampering | bee-filter-controls SQL injection via filter inputs | accept | No new risk — filter values flow through existing buildFilterSQL() in filter.ts which already escapes single quotes; this phase does not change that path |
| T-37-02 | Information Disclosure | XSS via specimen/sample data rendered in sub-components | accept | No new risk — Lit html template tag auto-escapes interpolated values; this phase only moves existing render logic between components |
</threat_model>

<verification>
- `cd frontend && npx tsc --noEmit` exits 0 (all new files compile)
- `cd frontend && npm test -- --run` runs; DECOMP-01/02/03 tests pass
- Three new .ts files exist in frontend/src/
- bee-sidebar.ts is UNCHANGED in this plan
</verification>

<success_criteria>
- bee-filter-controls.ts is a complete controlled-input component with all filter UI, emitting filter-changed
- bee-specimen-detail.ts is a pure render component accepting samples, emitting close
- bee-sample-detail.ts is a pure render component accepting sampleEvent, emitting close
- All three compile with tsc --noEmit
- Test file covers DECOMP-01/02/03 property interface checks (passing) and DECOMP-04 structural invariants (failing, as expected)
</success_criteria>

<output>
After completion, create `.planning/phases/37-sidebar-decomposition/37-01-SUMMARY.md`
</output>
