---
phase: 103-dbt-inat-field-id-constants-plantae-macro
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - data/dbt/macros/inat_field_ids.sql
  - data/dbt/models/intermediate/int_samples_base.sql
  - data/dbt/models/intermediate/int_waba_link.sql
  - data/dbt/models/intermediate/int_combined.sql
  - data/dbt/models/intermediate/int_ecdysis_base.sql
autonomous: true
requirements:
  - DBT-01
  - DBT-02
tags:
  - dbt
  - macros
  - refactor
  - sql

must_haves:
  truths:
    - "The integer literals 8338, 9963, 18116, 1718 do not appear as anonymous SQL JOIN conditions in any intermediate model"
    - "A single is_plant_taxon macro exists and the CASE WHEN taxon__iconic_taxon_name = 'Plantae' expression appears in exactly one .sql file under data/dbt/"
    - "bash data/dbt/run.sh build exits 0 with all tests PASS after the SQL changes"
    - "Output parity is preserved: test_dbt_diff.py passes (sandbox parquet matches public/data/)"
  artifacts:
    - path: "data/dbt/macros/inat_field_ids.sql"
      provides: "Five dbt macros: inat_ofv_specimen_count, inat_ofv_sample_id, inat_ofv_catalog_suffix, inat_ofv_host_obs_url, is_plant_taxon"
      contains: "{% macro inat_ofv_specimen_count() %}8338{% endmacro %}"
    - path: "data/dbt/models/intermediate/int_samples_base.sql"
      provides: "samples_base projection now using is_plant_taxon('op'), inat_ofv_specimen_count(), inat_ofv_sample_id() macros"
    - path: "data/dbt/models/intermediate/int_waba_link.sql"
      provides: "waba_link projection now using inat_ofv_catalog_suffix() macro"
    - path: "data/dbt/models/intermediate/int_combined.sql"
      provides: "Combined model now using inat_ofv_host_obs_url() macro on ofv1718 join"
    - path: "data/dbt/models/intermediate/int_ecdysis_base.sql"
      provides: "ecdysis_base projection now using is_plant_taxon('inat') macro"
  key_links:
    - from: "data/dbt/models/intermediate/int_samples_base.sql"
      to: "data/dbt/macros/inat_field_ids.sql"
      via: "{{ inat_ofv_specimen_count() }} / {{ inat_ofv_sample_id() }} / {{ is_plant_taxon('op') }}"
      pattern: "\\{\\{ (inat_ofv_specimen_count|inat_ofv_sample_id|is_plant_taxon)"
    - from: "data/dbt/models/intermediate/int_waba_link.sql"
      to: "data/dbt/macros/inat_field_ids.sql"
      via: "{{ inat_ofv_catalog_suffix() }}"
      pattern: "\\{\\{ inat_ofv_catalog_suffix\\(\\) \\}\\}"
    - from: "data/dbt/models/intermediate/int_combined.sql"
      to: "data/dbt/macros/inat_field_ids.sql"
      via: "{{ inat_ofv_host_obs_url() }}"
      pattern: "\\{\\{ inat_ofv_host_obs_url\\(\\) \\}\\}"
    - from: "data/dbt/models/intermediate/int_ecdysis_base.sql"
      to: "data/dbt/macros/inat_field_ids.sql"
      via: "{{ is_plant_taxon('inat') }}"
      pattern: "\\{\\{ is_plant_taxon\\('inat'\\) \\}\\}"
---

<objective>
Replace four anonymous iNat OFV field-ID integer literals (8338, 9963, 18116, 1718) and the
duplicated `CASE WHEN taxon__iconic_taxon_name = 'Plantae' ... END` expression with named
dbt macros, satisfying requirements DBT-01 and DBT-02 of the v3.8 Conceptual Tidying
milestone.

Purpose: Centralize scattered domain intelligence (OFV field IDs and the Plantae predicate)
into a single named home in the dbt macros layer. No new features, no schema changes, no
behavioral change — pure refactoring guarded by `dbt build` and `test_dbt_diff.py`.

Output: One new macro file (`data/dbt/macros/inat_field_ids.sql`) plus in-place edits to
four intermediate models. All edits behavioral-transparent; verified via `bash
data/dbt/run.sh build` (mandatory after any `.sql` change per STATE.md guardrail) and the
existing diff regression suite.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/103-dbt-inat-field-id-constants-plantae-macro/103-RESEARCH.md
@.planning/phases/103-dbt-inat-field-id-constants-plantae-macro/103-PATTERNS.md
@data/dbt/macros/emit_feature_collection.sql
@data/dbt/run.sh
@data/dbt/models/intermediate/int_samples_base.sql
@data/dbt/models/intermediate/int_waba_link.sql
@data/dbt/models/intermediate/int_combined.sql
@data/dbt/models/intermediate/int_ecdysis_base.sql

<interfaces>
<!-- dbt macro skeleton — derived from data/dbt/macros/emit_feature_collection.sql -->

Existing macro form (analog):
```
{% macro emit_feature_collection(model_relation, property_name, out_path) %}
...SQL body...
{% endmacro %}
```

Call-site pattern in models (from data/dbt/models/marts/*.sql and existing usage):
```
{{ macro_name() }}              -- zero-arg
{{ macro_name('alias') }}       -- single string-arg
```

Verification wrapper (data/dbt/run.sh): sets DBT_PROFILES_DIR / DBT_PROJECT_DIR; resolves
profile path `path: ../beeatlas.duckdb` relative to project dir. Never invoke `dbt build`
directly.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author macros and substitute call sites in four intermediate models</name>
  <files>
    data/dbt/macros/inat_field_ids.sql (new),
    data/dbt/models/intermediate/int_samples_base.sql,
    data/dbt/models/intermediate/int_waba_link.sql,
    data/dbt/models/intermediate/int_combined.sql,
    data/dbt/models/intermediate/int_ecdysis_base.sql
  </files>
  <read_first>
    data/dbt/macros/emit_feature_collection.sql,
    data/dbt/models/intermediate/int_samples_base.sql,
    data/dbt/models/intermediate/int_waba_link.sql,
    data/dbt/models/intermediate/int_combined.sql,
    data/dbt/models/intermediate/int_ecdysis_base.sql,
    .planning/phases/103-dbt-inat-field-id-constants-plantae-macro/103-RESEARCH.md,
    .planning/phases/103-dbt-inat-field-id-constants-plantae-macro/103-PATTERNS.md
  </read_first>
  <action>
    Create `data/dbt/macros/inat_field_ids.sql` defining five Jinja2 macros following the
    `{% macro name(args) %}...{% endmacro %}` skeleton from the analog
    `emit_feature_collection.sql`. The five macros are:

    1. `inat_ofv_specimen_count` — zero-arg, body is the bare integer literal `8338` on a
       single line (no surrounding whitespace), per RESEARCH §Code Examples and
       PATTERNS §Integer-constant macro pattern.
    2. `inat_ofv_sample_id` — zero-arg, body `9963`.
    3. `inat_ofv_catalog_suffix` — zero-arg, body `18116`.
    4. `inat_ofv_host_obs_url` — zero-arg, body `1718`.
    5. `is_plant_taxon(alias)` — one-arg macro using trim markers `{%- -%}` /
       `{%- endmacro %}` to suppress surrounding whitespace; body is exactly
       `CASE WHEN {{ alias }}.taxon__iconic_taxon_name = 'Plantae' THEN {{ alias }}.taxon__name ELSE NULL END`
       on a single line.

    Add a file-level header comment (4 lines, before the first `{% macro %}`) naming the
    file's purpose and listing the four consuming intermediate model files. After each
    `{% endmacro %}`, add a one-line `-- field_id = NNNN: "<iNat OFV human label>"`
    comment per RESEARCH §Code Examples (the comments document the iNat OFV titles
    "Bee Collection: Number of bees collected", "Bee Collection: Sample ID", "Bee
    Collection: Ecdysis catalog number suffix", "Observation URL"). Comments live
    BETWEEN macro blocks, never inside a macro body — comment text inside a macro body
    would be injected into rendered SQL.

    Then apply the in-place substitutions documented in PATTERNS:

    - `int_samples_base.sql`:
      - Line ~12: replace the literal `CASE WHEN op.taxon__iconic_taxon_name = 'Plantae' THEN op.taxon__name ELSE NULL END` with `{{ is_plant_taxon('op') }}`. Keep ` AS sample_host`.
      - Line ~15: replace the literal `8338` (in `sc.field_id = 8338`) with `{{ inat_ofv_specimen_count() }}`.
      - Line ~17: replace the literal `9963` (in `sid.field_id = 9963`) with `{{ inat_ofv_sample_id() }}`.
    - `int_waba_link.sql`:
      - Line ~9: replace the literal `18116` (in `ofv.field_id = 18116`) with `{{ inat_ofv_catalog_suffix() }}`.
      - Leave the header comment on line 1 referencing `field_id=18116` unchanged (documentation, not load-bearing SQL — per RESEARCH §Open Questions).
    - `int_combined.sql`:
      - Line ~83: replace the literal `1718` (in `ofv1718.field_id = 1718`) with `{{ inat_ofv_host_obs_url() }}`.
      - Do NOT rename the alias `ofv1718` — cosmetic scope creep is explicitly deferred per RESEARCH §Open Questions.
    - `int_ecdysis_base.sql`:
      - Line ~21: replace the literal `CASE WHEN inat.taxon__iconic_taxon_name = 'Plantae' THEN inat.taxon__name ELSE NULL END` with `{{ is_plant_taxon('inat') }}`. Keep ` AS inat_host,`.

    Do NOT touch staging models (`stg_inat__ofvs.sql`, `stg_waba__ofvs.sql`) — their
    field-ID references are in `-- comments`, not load-bearing SQL. RESEARCH §Open
    Questions notes optional staging-model comment updates as out of scope.

    Do NOT add new pytest tests — RESEARCH §Validation Architecture confirms existing
    `bash data/dbt/run.sh build` plus `data/tests/test_dbt_diff.py` fully cover the
    refactor. No Wave 0 gap.

    Run `bash data/dbt/run.sh build` after the edits and confirm it exits 0 with all
    tests PASS — STATE.md guardrail is mandatory and the macro authoring is the only
    behavioral risk (e.g. typo in a literal, wrong trim marker placement, name
    collision with a built-in dbt macro).
  </action>
  <verify>
    <automated>cd /Users/rainhead/dev/beeatlas &amp;&amp; bash data/dbt/run.sh build &amp;&amp; grep -rEn 'field_id = [0-9]+' data/dbt/models/intermediate/ | grep -v '^[^:]*:[^:]*:[[:space:]]*--' | (! grep -E 'field_id = [0-9]') &amp;&amp; test "$(grep -rln \"taxon__iconic_taxon_name = 'Plantae'\" data/dbt/ | grep -v macros/inat_field_ids.sql | wc -l | tr -d ' ')" = "0"</automated>
  </verify>
  <acceptance_criteria>
    - File `data/dbt/macros/inat_field_ids.sql` exists.
    - `grep -c '{% macro inat_ofv_specimen_count() %}8338{% endmacro %}' data/dbt/macros/inat_field_ids.sql` returns `1`.
    - `grep -c '{% macro inat_ofv_sample_id() %}9963{% endmacro %}' data/dbt/macros/inat_field_ids.sql` returns `1`.
    - `grep -c '{% macro inat_ofv_catalog_suffix() %}18116{% endmacro %}' data/dbt/macros/inat_field_ids.sql` returns `1`.
    - `grep -c '{% macro inat_ofv_host_obs_url() %}1718{% endmacro %}' data/dbt/macros/inat_field_ids.sql` returns `1`.
    - `grep -c 'macro is_plant_taxon(alias)' data/dbt/macros/inat_field_ids.sql` returns `1`.
    - `data/dbt/macros/inat_field_ids.sql` contains the literal string `CASE WHEN {{ alias }}.taxon__iconic_taxon_name = 'Plantae' THEN {{ alias }}.taxon__name ELSE NULL END`.
    - `grep -rEn 'field_id = [0-9]+' data/dbt/models/intermediate/`, filtered to lines that are not pure SQL comments (i.e. content before the first `--` contains `field_id`), returns zero matches.
    - `grep -rln "taxon__iconic_taxon_name = 'Plantae'" data/dbt/`, excluding `data/dbt/macros/inat_field_ids.sql`, returns zero files.
    - `int_samples_base.sql` contains all three substrings `{{ is_plant_taxon('op') }}`, `{{ inat_ofv_specimen_count() }}`, `{{ inat_ofv_sample_id() }}`.
    - `int_waba_link.sql` contains the substring `{{ inat_ofv_catalog_suffix() }}`.
    - `int_combined.sql` contains the substring `{{ inat_ofv_host_obs_url() }}` AND retains the alias name `ofv1718` (no rename).
    - `int_ecdysis_base.sql` contains the substring `{{ is_plant_taxon('inat') }}`.
    - `bash data/dbt/run.sh build` exits with status 0 and reports all dbt tests PASS.
  </acceptance_criteria>
  <done>
    Macros file created with five named macros; four intermediate models updated to
    reference macros instead of literals/CASE; `dbt build` is green; no anonymous
    field-ID literal remains in any intermediate model JOIN condition; the Plantae CASE
    expression lives in exactly one file (`data/dbt/macros/inat_field_ids.sql`).
  </done>
</task>

<task type="auto">
  <name>Task 2: Regression gate — run dbt diff test for output parity</name>
  <files>
    (verification only — no files modified)
  </files>
  <read_first>
    data/tests/test_dbt_diff.py,
    .planning/phases/103-dbt-inat-field-id-constants-plantae-macro/103-RESEARCH.md
  </read_first>
  <action>
    Run the existing dbt output-parity regression suite to confirm Task 1's macro
    substitutions are behaviorally transparent. Use the exact command documented in
    RESEARCH §Validation Architecture: `uv run --project data pytest data/tests/test_dbt_diff.py -x`.
    This test compares the sandbox dbt-produced parquet to the canonical `public/data/`
    outputs; any divergence in row counts or key sets fails the test.

    No code changes in this task. If the test fails, the failure indicates a Task 1
    semantic regression (e.g. a typo such as `8388` instead of `8338` in a macro body,
    or `{{ alias }}` accidentally double-quoted), and Task 1 must be revisited before
    declaring the phase complete.
  </action>
  <verify>
    <automated>cd /Users/rainhead/dev/beeatlas &amp;&amp; uv run --project data pytest data/tests/test_dbt_diff.py -x</automated>
  </verify>
  <acceptance_criteria>
    - `uv run --project data pytest data/tests/test_dbt_diff.py -x` exits 0.
    - pytest reports all test_dbt_diff.py test cases as PASSED (no SKIPPED counted as failure for the regression gate; no FAILED).
    - No new files created or modified by this task.
  </acceptance_criteria>
  <done>
    Diff regression suite green, confirming the macro refactor preserves byte-for-byte
    output parity with the pre-refactor pipeline.
  </done>
</task>

</tasks>

<verification>
Phase-level checks (run after both tasks succeed):

1. `bash data/dbt/run.sh build` exits 0 with all dbt tests PASS.
2. `uv run --project data pytest data/tests/test_dbt_diff.py -x` exits 0.
3. `grep -rEn 'field_id = [0-9]+' data/dbt/models/intermediate/` returns no matches outside SQL comment lines.
4. `grep -rln "taxon__iconic_taxon_name = 'Plantae'" data/dbt/` lists exactly one file: `data/dbt/macros/inat_field_ids.sql`.
5. The four `{{ inat_ofv_* }}` macro calls each appear in their expected consuming model (samples_base x 2, waba_link x 1, combined x 1).
6. `{{ is_plant_taxon('op') }}` appears in `int_samples_base.sql` and `{{ is_plant_taxon('inat') }}` appears in `int_ecdysis_base.sql`.
</verification>

<success_criteria>
Phase 103 is complete when:
- The integer literals 8338, 9963, 18116, 1718 do not appear as anonymous SQL JOIN conditions in any intermediate model — each is referenced via its named macro. (DBT-01)
- A single `is_plant_taxon` macro exists; the `CASE WHEN taxon__iconic_taxon_name = 'Plantae'` expression does not appear in more than one `.sql` file. (DBT-02)
- `bash data/dbt/run.sh build` exits 0 with all tests PASS after every SQL change. (DBT-01, DBT-02)
- The diff regression suite passes, confirming behavioral parity.
</success_criteria>

<output>
Create `.planning/phases/103-dbt-inat-field-id-constants-plantae-macro/103-01-SUMMARY.md` when done.
</output>
