---
phase: 73
plan: 1
title: "OL removal and verification"
status: planned
---

# Plan 073-01: OL removal and verification (sketch)

Detailed planning deferred until Phase 72 is complete. High-level scope:

1. `npm uninstall ol ol-mapbox-style` — remove OL packages
2. Verify no OL imports remain (`grep -r "from 'ol" src/`)
3. Verify `rbush` and `quickselect` (OL transitive deps) are gone from lockfile
4. Measure bundle size — target under 200 KB minified for main chunk
5. Update all Vitest tests — mock Mapbox where needed
6. End-to-end verification: URL sharing, filter, selection, table view, sidebar
