---
phase: 073-ol-removal-and-verification
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/package.json
  - frontend/src/bee-atlas.ts
  - frontend/src/region-layer.ts
  - frontend/src/tests/bee-atlas.test.ts
  - frontend/src/tests/bee-header.test.ts
  - frontend/src/tests/bee-filter-toolbar.test.ts
  - frontend/src/tests/bee-sidebar.test.ts
  - frontend/src/tests/bee-table.test.ts
autonomous: true
requirements: []

must_haves:
  truths:
    - "ol and ol-mapbox-style are not listed in package.json dependencies"
    - "rbush and quickselect do not appear in the lockfile"
    - "No source file imports from ol or ol-mapbox-style"
    - "region-layer.ts does not exist as a file"
    - "The loadBoundaries import and call are removed from bee-atlas.ts"
    - "All 172+ Vitest tests pass after cleanup"
  artifacts:
    - path: "frontend/package.json"
      provides: "Dependencies without ol, ol-mapbox-style"
      contains: "mapbox-gl"
    - path: "frontend/src/bee-atlas.ts"
      provides: "Root component without region-layer import"
  key_links:
    - from: "frontend/src/bee-atlas.ts"
      to: "frontend/src/bee-map.ts"
      via: "direct import and property binding"
      pattern: "import.*bee-map"
---

<objective>
Remove all OpenLayers dependencies from the frontend and clean up dead code left from the Mapbox GL JS migration (Phases 71-72). Delete the region-layer.ts stub, remove its import from bee-atlas.ts, uninstall ol/ol-mapbox-style packages, and update stale test mocks that still reference OL-era exports.

Purpose: Complete the Mapbox migration by eliminating all OL artifacts so the dependency tree is clean, the lockfile shrinks, and future developers see no trace of the old map library.
Output: Clean package.json, clean lockfile, no dead module references, all tests green.
</objective>

<execution_context>
@.claude/get-shit-done/workflows/execute-plan.md
@.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@frontend/package.json
@frontend/src/region-layer.ts
@frontend/src/bee-atlas.ts (lines 1-10 for imports, line 283 for loadBoundaries call)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove OL packages and delete region-layer.ts stub</name>
  <files>frontend/package.json, frontend/src/region-layer.ts, frontend/src/bee-atlas.ts</files>
  <read_first>
    - frontend/package.json (current dependencies)
    - frontend/src/region-layer.ts (stub to delete)
    - frontend/src/bee-atlas.ts (import on line 6 and call on line 283)
  </read_first>
  <action>
1. Run `cd frontend && npm uninstall ol ol-mapbox-style` to remove both packages from package.json and regenerate the lockfile.

2. Delete `frontend/src/region-layer.ts` entirely (it is a 10-line no-op stub with exports `loadBoundaries` and `makeRegionStyleFn` that nothing uses for real behavior).

3. Edit `frontend/src/bee-atlas.ts`:
   - Remove line 6: `import { loadBoundaries } from './region-layer.ts';`
   - Remove line 283: `loadBoundaries();` (inside the `.then()` callback after `console.debug('SQLite tables ready');`)
   - The `.then()` block should still contain the `if (this._viewMode === 'table')` block and anything else after the deleted line.

4. Verify no other source files import from `region-layer.ts`, `ol`, or `ol-mapbox-style`:
   ```bash
   cd frontend && grep -rn "region-layer\|from ['\"]ol[/'\"]\|from ['\"]ol-mapbox-style" src/ --include="*.ts" --exclude-dir=tests
   ```
   Expected: zero matches.

5. Verify `rbush` and `quickselect` are gone from the lockfile:
   ```bash
   grep -c "rbush\|quickselect" frontend/package-lock.json
   ```
   Expected: 0 matches.
  </action>
  <verify>
    <automated>cd /Users/rainhead/dev/beeatlas/frontend && ! grep -q '"ol"' package.json && ! grep -q '"ol-mapbox-style"' package.json && ! test -f src/region-layer.ts && ! grep -q "region-layer" src/bee-atlas.ts && ! grep -q "rbush\|quickselect" package-lock.json && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - `frontend/package.json` does NOT contain `"ol"` or `"ol-mapbox-style"` in dependencies
    - `frontend/package.json` DOES still contain `"mapbox-gl"`, `"lit"`, `"wa-sqlite"`, `"hyparquet"`
    - `frontend/package-lock.json` does NOT contain `rbush` or `quickselect`
    - `frontend/src/region-layer.ts` does not exist
    - `frontend/src/bee-atlas.ts` does not contain the string `region-layer`
    - `frontend/src/bee-atlas.ts` does not contain the string `loadBoundaries`
    - No `.ts` file in `frontend/src/` (excluding tests/) contains `from 'ol` or `from 'ol-mapbox-style`
  </acceptance_criteria>
  <done>OL packages removed from package.json, lockfile regenerated without rbush/quickselect, region-layer.ts deleted, bee-atlas.ts no longer imports or calls loadBoundaries.</done>
</task>

<task type="auto">
  <name>Task 2: Clean up stale OL-era test mocks and verify all tests pass</name>
  <files>frontend/src/tests/bee-header.test.ts, frontend/src/tests/bee-filter-toolbar.test.ts, frontend/src/tests/bee-sidebar.test.ts, frontend/src/tests/bee-table.test.ts, frontend/src/tests/bee-atlas.test.ts</files>
  <read_first>
    - frontend/src/tests/bee-atlas.test.ts (lines 30-33 for current region-layer mock)
    - frontend/src/tests/bee-header.test.ts (lines 15-45 for stale mocks)
    - frontend/src/tests/bee-filter-toolbar.test.ts (lines 12-42 for stale mocks)
    - frontend/src/tests/bee-sidebar.test.ts (lines 15-45 for stale mocks)
    - frontend/src/tests/bee-table.test.ts (lines 18-34 for stale mocks)
    - frontend/src/features.ts (to see actual exports for correct mock shape)
  </read_first>
  <action>
Since `region-layer.ts` was deleted in Task 1, all `vi.mock('../region-layer.ts', ...)` blocks in test files must be removed entirely. Additionally, 4 test files mock `features.ts` with an `OccurrenceSource` export that no longer exists -- update these to match the actual `features.ts` API.

**In `frontend/src/tests/bee-atlas.test.ts`:**
- Remove lines 30-33 (the `vi.mock('../region-layer.ts', ...)` block entirely).

**In `frontend/src/tests/bee-header.test.ts`:**
- Remove the `vi.mock('../region-layer.ts', ...)` block (lines 24-45).
- Replace the `vi.mock('../features.ts', ...)` block (lines 15-22) with the correct mock shape:
  ```typescript
  vi.mock('../features.ts', () => ({
    loadOccurrenceGeoJSON: vi.fn(() => Promise.resolve({
      geojson: { type: 'FeatureCollection', features: [] },
      summary: { totalSpecimens: 0, speciesCount: 0, genusCount: 0, familyCount: 0, earliestYear: 0, latestYear: 0 },
      taxaOptions: [],
    })),
  }));
  ```

**In `frontend/src/tests/bee-filter-toolbar.test.ts`:**
- Remove the `vi.mock('../region-layer.ts', ...)` block (lines 21-42).
- Replace the `vi.mock('../features.ts', ...)` block (lines 12-19) with the same correct mock shape as above.

**In `frontend/src/tests/bee-sidebar.test.ts`:**
- Remove the `vi.mock('../region-layer.ts', ...)` block (lines 24-45).
- Replace the `vi.mock('../features.ts', ...)` block (lines 15-22) with the same correct mock shape as above.

**In `frontend/src/tests/bee-table.test.ts`:**
- Remove the `vi.mock('../region-layer.ts', ...)` block (lines 24-34).
- Replace the `vi.mock('../features.ts', ...)` block (lines 18-22) with the same correct mock shape as above.

After all edits, run the full test suite:
```bash
cd frontend && npx vitest run
```
All 172 tests must pass.
  </action>
  <verify>
    <automated>cd /Users/rainhead/dev/beeatlas/frontend && npx vitest run 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - No test file contains `vi.mock('../region-layer.ts'`
    - No test file contains `OccurrenceSource` (stale OL-era export)
    - No test file contains `regionLayer`, `countySource`, `ecoregionSource`, `boundaryStyle`, `selectedBoundaryStyle` (stale OL-era mock properties)
    - `bee-header.test.ts`, `bee-filter-toolbar.test.ts`, `bee-sidebar.test.ts`, `bee-table.test.ts` each mock `features.ts` with `loadOccurrenceGeoJSON` (matching the actual module export)
    - `bee-atlas.test.ts` does NOT mock `region-layer.ts`
    - `npx vitest run` reports all tests passing (172+ tests, 0 failures)
    - `npx tsc --noEmit` exits 0 (TypeScript compilation clean)
  </acceptance_criteria>
  <done>All stale OL-era mocks removed from 5 test files. features.ts mocks updated to match actual exports. Full test suite passes with 0 failures. TypeScript compiles cleanly.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

No new trust boundaries introduced -- this plan only removes code and updates mocks.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-73-01 | T (Tampering) | package.json | accept | Removing dependencies reduces attack surface; no new deps introduced |
</threat_model>

<verification>
1. `grep -c '"ol"' frontend/package.json` returns 0
2. `grep -c '"ol-mapbox-style"' frontend/package.json` returns 0
3. `grep -c "rbush\|quickselect" frontend/package-lock.json` returns 0
4. `test -f frontend/src/region-layer.ts` exits non-zero (file deleted)
5. `grep -rn "region-layer\|from ['\"]ol" frontend/src/ --include="*.ts"` returns 0 matches
6. `cd frontend && npx vitest run` -- all tests pass
7. `cd frontend && npx tsc --noEmit` -- exits 0
</verification>

<success_criteria>
- ol and ol-mapbox-style removed from package.json
- rbush and quickselect absent from lockfile
- region-layer.ts deleted
- No OL imports in any source file
- No stale OL-era mocks in any test file
- All tests pass, TypeScript compiles cleanly
</success_criteria>

<output>
After completion, create `.planning/phases/073-ol-removal-and-verification/073-01-SUMMARY.md`
</output>
