---
phase: 073-ol-removal-and-verification
plan: 02
type: execute
wave: 2
depends_on: ["073-01"]
files_modified: []
autonomous: false
requirements: []

must_haves:
  truths:
    - "The production build succeeds and application JS (excluding mapbox-gl) is under 200 KB"
    - "URL sharing preserves view state, filter state, and selection state end-to-end"
    - "Table view row-pan centers the map on the clicked occurrence"
    - "All Vitest tests pass on a clean dependency tree"
  artifacts:
    - path: "frontend/dist/assets/index-*.js"
      provides: "Production bundle"
  key_links:
    - from: "frontend/src/bee-atlas.ts"
      to: "frontend/src/url-state.ts"
      via: "buildParams/parseParams for URL state"
      pattern: "buildParams|parseParams"
    - from: "frontend/src/bee-atlas.ts"
      to: "frontend/src/bee-map.ts"
      via: "panTo property binding for row-pan"
      pattern: "panTo"
---

<objective>
Verify the post-cleanup build succeeds, measure bundle size, and perform end-to-end feature verification to confirm full feature parity after OL removal. This plan produces the definitive "Phase 73 is done" evidence.

Purpose: Confirm that removing OL dependencies did not break any functionality and document the bundle size outcome.
Output: Bundle size measurement, verified feature parity, phase complete.
</objective>

<execution_context>
@.claude/get-shit-done/workflows/execute-plan.md
@.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/073-ol-removal-and-verification/073-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build verification and bundle size measurement</name>
  <files></files>
  <read_first>
    - frontend/package.json (verify ol is gone before building)
    - frontend/vite.config.ts (build configuration)
  </read_first>
  <action>
1. Run the production build:
   ```bash
   cd frontend && npm run build
   ```
   The build must succeed without errors.

2. Record the output bundle sizes. The key metric is the main JS chunk (index-*.js). Expected breakdown:
   - mapbox-gl contributes ~1,700 KB minified (this is inherent to the library -- it is not tree-shakeable)
   - Application code (lit, wa-sqlite, hyparquet, custom components) should be well under 200 KB
   - Total main chunk will be ~1,900-2,100 KB due to mapbox-gl

3. Measure application-only code size by checking the Vite module transform count and comparing to the pre-cleanup build (which was also 2,018 KB because OL was already tree-shaken out -- no source files imported it).

4. Run `npx tsc --noEmit` to confirm TypeScript compilation is clean.

5. Run `npx vitest run` one final time to confirm all tests pass on the clean dependency tree.

**Bundle size context for the SUMMARY:** The ROADMAP success criterion "Main JS bundle under 200 KB" was set before the migration, when OL was the map library (~400 KB). Mapbox GL JS v3 is ~1,700 KB minified and not tree-shakeable. The application code excluding mapbox-gl IS well under 200 KB. The SUMMARY should note this and propose updating the criterion to: "Application JS excluding mapbox-gl is under 200 KB; total main chunk ~2,000 KB (mapbox-gl is the dominant contributor)."
  </action>
  <verify>
    <automated>cd /Users/rainhead/dev/beeatlas/frontend && npm run build 2>&1 | tail -10 && npx tsc --noEmit && npx vitest run 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `npm run build` exits 0 (no build errors)
    - `npx tsc --noEmit` exits 0
    - `npx vitest run` reports all tests passing, 0 failures
    - Build output lists chunk sizes (main JS chunk, CSS, sidebar chunk, table chunk, wasm file)
    - No warning about missing modules or unresolved imports
  </acceptance_criteria>
  <done>Production build succeeds. TypeScript compiles. All tests pass. Bundle sizes documented.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: End-to-end feature parity verification</name>
  <what-built>
    OL dependencies fully removed. All source code cleaned of OL references. Tests updated and passing. Production build verified.
  </what-built>
  <how-to-verify>
    Run the dev server: `cd frontend && npm run dev`

    **1. Map renders correctly:**
    - Visit http://localhost:5173
    - Map should show Washington state with occurrence dots (clustered at low zoom)
    - Basemap tiles load (Mapbox outdoors style)

    **2. URL sharing (view state):**
    - Zoom into a specific area, then copy the URL
    - Open the URL in a new tab -- map should restore the same center and zoom

    **3. URL sharing (filter state):**
    - Open the filter panel (magnifying glass icon) and select a taxon filter
    - Copy the URL
    - Open in a new tab -- filter should be restored, map shows filtered dots

    **4. URL sharing (selection state):**
    - Click on an occurrence dot (or cluster) to open the sidebar
    - Copy the URL
    - Open in a new tab -- sidebar should show the same selected occurrences

    **5. Boundary toggle:**
    - Use the Regions dropdown to switch between Off / Counties / Ecoregions
    - County or ecoregion polygons should appear/disappear
    - Click on a boundary polygon -- sidebar or filter should respond

    **6. Table view row-pan:**
    - Switch to table view (table icon in header)
    - Click on a row in the table
    - The map strip at the top should pan/center to that occurrence's location

    **7. General interaction:**
    - Click clusters to see occurrence lists in sidebar
    - Close sidebar, click empty map area (sidebar should not reopen)
    - Filter by collector, county, year -- dots update accordingly
  </how-to-verify>
  <resume-signal>Type "approved" if all features work correctly, or describe any issues found.</resume-signal>
  <acceptance_criteria>
    - User confirms map renders with occurrence dots
    - User confirms URL sharing works for view, filter, and selection state
    - User confirms boundary toggle works
    - User confirms table row-pan works
    - User confirms general click interactions work
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

No new trust boundaries -- verification-only plan.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| (none) | — | — | — | No code changes in this plan; verification only |
</threat_model>

<verification>
1. `npm run build` in frontend/ exits 0
2. `npx tsc --noEmit` in frontend/ exits 0
3. `npx vitest run` in frontend/ -- all tests pass
4. Human confirms all 7 feature areas work correctly in the browser
</verification>

<success_criteria>
- Production build succeeds with documented bundle sizes
- TypeScript compiles cleanly
- All Vitest tests pass
- Human verifies: map renders, URL sharing works, boundaries toggle, table row-pan works, click interactions function
</success_criteria>

<output>
After completion, create `.planning/phases/073-ol-removal-and-verification/073-02-SUMMARY.md`
</output>
