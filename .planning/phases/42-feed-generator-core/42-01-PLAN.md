---
phase: 42-feed-generator-core
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - data/tests/conftest.py
  - data/tests/test_feeds.py
  - data/feeds.py
  - data/run.py
autonomous: true
requirements:
  - FEED-01
  - FEED-02
  - FEED-03
  - FEED-04
  - PIPE-01

must_haves:
  truths:
    - "feeds.py generates valid Atom XML with entries for recent determinations"
    - "Each entry contains taxon name, determiner, ecdysis link, collector, and collection date"
    - "Entries are limited to 90-day window on modified timestamp, sorted newest-first"
    - "Feed-level updated equals most recent entry modified; title is correct"
    - "Output file written to frontend/public/data/feeds/determinations.xml"
    - "run.py calls feeds after export step without error"
  artifacts:
    - path: "data/feeds.py"
      provides: "Atom feed generator module"
      exports: ["main", "write_determinations_feed"]
    - path: "data/tests/test_feeds.py"
      provides: "Unit and integration tests for feeds module"
      min_lines: 80
    - path: "data/run.py"
      provides: "Pipeline orchestrator with feeds step appended"
      contains: "feeds"
  key_links:
    - from: "data/feeds.py"
      to: "ecdysis_data.identifications"
      via: "DuckDB SQL join on coreid = CAST(o.id AS VARCHAR)"
      pattern: "i\\.coreid = CAST\\(o\\.id AS VARCHAR\\)"
    - from: "data/run.py"
      to: "data/feeds.py"
      via: "STEPS list import"
      pattern: "from feeds import"
    - from: "data/feeds.py"
      to: "frontend/public/data/feeds/determinations.xml"
      via: "ET.write output"
      pattern: "feeds.*determinations\\.xml"
---

<objective>
Create `data/feeds.py` that queries `beeatlas.duckdb` for recent determinations and writes a valid Atom XML feed to `frontend/public/data/feeds/determinations.xml`. Wire it into `run.py` as the final pipeline step.

Purpose: Enable RSS/Atom feed consumption of recent bee determination activity for the Washington Bee Atlas project.
Output: `data/feeds.py`, `data/tests/test_feeds.py`, updated `data/tests/conftest.py`, updated `data/run.py`
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/42-feed-generator-core/42-RESEARCH.md
@.planning/phases/42-feed-generator-core/42-VALIDATION.md

@data/export.py
@data/run.py
@data/tests/conftest.py
@data/tests/test_export.py
</context>

<interfaces>
<!-- Key patterns the executor needs from existing codebase -->

From data/export.py (DB connection pattern to follow):
```python
DB_PATH = os.environ.get('DB_PATH', str(Path(__file__).parent / 'beeatlas.duckdb'))
_default_assets = str(Path(__file__).parent.parent / 'frontend' / 'public' / 'data')
ASSETS_DIR = Path(os.environ.get('EXPORT_DIR', _default_assets))

def main() -> None:
    con = duckdb.connect(DB_PATH, read_only=True)
    # ... do work ...
    con.close()
```

From data/run.py (STEPS integration pattern):
```python
STEPS: list[tuple[str, Callable]] = [
    ...
    ("export", export_all),
    # new step appended here
]
```

From data/tests/conftest.py (test fixture pattern):
```python
@pytest.fixture(scope="session")
def fixture_db(tmp_path_factory):
    db_path = str(tmp_path_factory.mktemp("db") / "test.duckdb")
    con = duckdb.connect(db_path)
    # ...
    return db_path

@pytest.fixture
def export_dir(tmp_path):
    return tmp_path
```

From data/tests/test_export.py (monkeypatch pattern):
```python
import export as export_mod
def test_something(fixture_con, export_dir, monkeypatch):
    monkeypatch.setattr(export_mod, 'ASSETS_DIR', export_dir)
    export_mod.some_function(fixture_con)
```
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Test infrastructure and stubs (Wave 0)</name>
  <files>data/tests/conftest.py, data/tests/test_feeds.py</files>
  <behavior>
    - test_entry_fields: Feed entry contains title with taxon name and determiner, summary with collector and date, link to ecdysis URL, id as urn:ecdysis:{uuid}, and updated timestamp
    - test_time_window_and_sort: Only rows within 90 days appear; rows older than 90 days are excluded; entries sorted newest-first
    - test_feed_metadata: Feed title is "Washington Bee Atlas -- All Recent Determinations"; feed updated equals most recent entry modified (UTC ISO format); feed has id and self link
    - test_output_file: write_determinations_feed creates feeds/determinations.xml under output dir; file is parseable XML with Atom namespace
    - test_empty_window: When no rows match 90-day window, no file is written (or empty feed handled gracefully)
    - test_blank_fields_excluded: Rows with empty scientific_name or identified_by are excluded from feed
  </behavior>
  <action>
1. Extend `data/tests/conftest.py`:
   - Add `event_date VARCHAR` column to the existing `ecdysis_data.occurrences` CREATE TABLE statement (it is currently missing but feeds.py needs it). Add it after `associated_taxa` and before `_dlt_load_id`. Update the seed INSERT to include value `'2024-06-15'` for the existing test specimen.
   - Add a new `ecdysis_data.identifications` table with columns: `coreid VARCHAR, scientific_name VARCHAR, identified_by VARCHAR, modified TIMESTAMPTZ, record_id VARCHAR, identification_is_current VARCHAR, date_identified VARCHAR, _dlt_load_id VARCHAR, _dlt_id VARCHAR`.
   - Seed 3 identification rows in `_seed_data`:
     a. Recent valid: coreid='5594569' (matches existing occurrence id), scientific_name='Eucera acerba', identified_by='Test Determiner', modified=now()-10days, record_id='det-uuid-1'
     b. Recent blank: coreid='5594569', scientific_name='', identified_by='', modified=now()-5days, record_id='det-uuid-2' (should be excluded by blank filter)
     c. Old valid: coreid='5594569', scientific_name='Andrena lupinorum', identified_by='Old Determiner', modified=now()-100days, record_id='det-uuid-3' (should be excluded by 90-day window)

2. Create `data/tests/test_feeds.py` with tests per the behavior block above. Import `feeds` module via `import feeds as feeds_mod` and use `monkeypatch.setattr(feeds_mod, 'ASSETS_DIR', export_dir)` and `monkeypatch.setattr(feeds_mod, 'DB_PATH', fixture_db)` to inject test DB and output dir. Parse output XML with `xml.etree.ElementTree` and assert on Atom namespace elements.

   For `test_time_window_and_sort`: call `write_determinations_feed` with fixture_con and export_dir, parse the output XML, assert exactly 1 entry (the recent valid row -- blank and old are excluded).

   For `test_entry_fields`: parse the single entry, assert `<title>` contains 'Eucera acerba', `<summary>` contains 'Test Collector' and '2024-06-15', `<link>` href contains 'occid=5594569', `<id>` text is 'urn:ecdysis:69c258f0-7c62-4da3-b991-130ec3dde645'.

   For `test_feed_metadata`: assert feed `<title>` text equals 'Washington Bee Atlas \u2014 All Recent Determinations', feed `<updated>` text ends with '+00:00' (UTC), feed `<id>` and `<link rel="self">` are present.

   For `test_output_file`: assert `export_dir / 'feeds' / 'determinations.xml'` exists and is valid XML.

   For `test_blank_fields_excluded` and `test_empty_window`: test the blank-string and time-window exclusion edge cases explicitly.

3. All tests should initially fail (RED) since `data/feeds.py` does not exist yet. Verify tests are syntactically correct by running `cd data && uv run pytest tests/test_feeds.py --collect-only` to confirm collection without import errors (will show errors since feeds module does not exist -- that is expected at this stage; confirm the test file itself has no syntax errors by checking the error message mentions only the missing import).
  </action>
  <verify>
    <automated>cd /Users/rainhead/dev/beeatlas/data && uv run pytest tests/test_feeds.py --collect-only 2>&1 | head -20</automated>
  </verify>
  <done>conftest.py extended with identifications table and 3 seed rows (recent valid, recent blank, old valid) plus event_date on occurrences. test_feeds.py has 6 test functions that will fail until feeds.py is implemented. Existing tests still pass: `cd data && uv run pytest tests/test_export.py -q` green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement feeds.py (GREEN)</name>
  <files>data/feeds.py</files>
  <behavior>
    - write_determinations_feed queries identifications joined to occurrences, filters 90-day window and blank fields, returns Atom XML
    - Entry id uses urn:ecdysis:{occurrence_id UUID} for uniqueness (not ecdysis URL which can duplicate)
    - Entry title format: "{taxon_name} -- determined by {determiner}"
    - Entry summary format: "Collected by {collector} on {collection_date}. Specimen: ecdysis:{ecdysis_id}"
    - Entry link href: https://ecdysis.org/collections/individual/index.php?occid={ecdysis_id}
    - Feed updated = max modified timestamp in UTC ISO format
    - Empty result set prints skip message and returns without writing file
    - Output directory created with mkdir(parents=True, exist_ok=True)
  </behavior>
  <action>
Create `data/feeds.py` following the patterns from RESEARCH.md exactly:

1. Module-level constants matching export.py pattern:
   - `DB_PATH = os.environ.get('DB_PATH', str(Path(__file__).parent / 'beeatlas.duckdb'))`
   - `_default_assets = str(Path(__file__).parent.parent / 'frontend' / 'public' / 'data')`
   - `ASSETS_DIR = Path(os.environ.get('EXPORT_DIR', _default_assets))`
   - `ATOM_NS = 'http://www.w3.org/2005/Atom'`
   - `ET.register_namespace('', ATOM_NS)` at module level

2. Helper `_atom(tag: str) -> str` returns `f'{{{ATOM_NS}}}{tag}'`

3. Helper `_build_entry(feed: ET.Element, row: tuple) -> None`:
   - Unpack: modified, taxon_name, determiner, specimen_uuid, ecdysis_id, collector, coll_date
   - Create entry subelement with: id (urn:ecdysis:{specimen_uuid}), title ("{taxon} \u2014 determined by {determiner}"), updated (UTC isoformat), link (ecdysis URL), summary (text type with collector and date)

4. Function `write_determinations_feed(con: duckdb.DuckDBPyConnection, out_dir: Path) -> None`:
   - Execute the SQL query from RESEARCH.md Pattern 5 (join on `i.coreid = CAST(o.id AS VARCHAR)`, filter `!= ''` on scientific_name and identified_by, 90-day window, ORDER BY modified DESC)
   - If no rows: print skip message, return
   - Build feed element with: title ("Washington Bee Atlas \u2014 All Recent Determinations"), id (https://beeatlas.org/data/feeds/determinations.xml), link rel=self, updated (max modified in UTC)
   - Iterate rows calling _build_entry
   - ET.indent(tree, space='  ')
   - Write to `out_dir / 'feeds' / 'determinations.xml'` using `tree.write(str(out_path), xml_declaration=True, encoding='unicode')` -- but since ET.write with encoding='unicode' returns a string to stdout-like, instead use: write the string to file via `out_path.write_text(ET.tostring(feed, xml_declaration=True, encoding='unicode'), encoding='utf-8')`
   - Print summary with entry count and file size

5. Function `main() -> None`:
   - Connect read-only to DB_PATH, call write_determinations_feed(con, ASSETS_DIR), close connection

6. Guard `if __name__ == '__main__': main()`

CRITICAL join key: `i.coreid = CAST(o.id AS VARCHAR)` -- NOT occurrence_id. This is verified in RESEARCH.md.
CRITICAL blank filter: `AND i.scientific_name != '' AND i.identified_by != ''` -- blank strings not NULLs.
CRITICAL entry ID: Use `record_id` or `occurrence_id` UUID as `urn:ecdysis:{uuid}` -- NOT the ecdysis URL (which can duplicate for specimens with multiple identifications). Per RESEARCH.md Pitfall 6, use `occurrence_id` UUID since `record_id` is per-identification but the research recommends occurrence_id.

Note on XML writing: Use `ET.indent(tree)` then `ET.tostring(feed, xml_declaration=True, encoding='unicode')` and write the resulting string with `out_path.write_text(result, encoding='utf-8')`. This avoids the BOM issue noted in RESEARCH.md.
  </action>
  <verify>
    <automated>cd /Users/rainhead/dev/beeatlas/data && uv run pytest tests/test_feeds.py -x -q</automated>
  </verify>
  <done>All 6 tests in test_feeds.py pass. feeds.py generates valid Atom XML with correct entry structure, 90-day filtering, blank-field exclusion, proper sort order, and correct feed metadata. Existing test suite still green: `cd data && uv run pytest -q`.</done>
</task>

<task type="auto">
  <name>Task 3: Wire feeds into run.py</name>
  <files>data/run.py</files>
  <action>
1. Add import at top of run.py: `from feeds import main as generate_feeds`
2. Append to STEPS list after the ("export", export_all) entry: `("feeds", generate_feeds),`
3. Verify the import works: `cd data && uv run python -c "from feeds import main; print('feeds import ok')"`
4. Run full test suite to confirm no regressions.

This follows the exact pattern established by all other pipeline steps in run.py (PIPE-01).
  </action>
  <verify>
    <automated>cd /Users/rainhead/dev/beeatlas/data && uv run python -c "from feeds import main; print('ok')" && uv run pytest -q</automated>
  </verify>
  <done>run.py STEPS list includes ("feeds", generate_feeds) after ("export", export_all). Import succeeds. Full test suite passes with no regressions.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| DB -> XML | Data from DuckDB written to static XML file; no user input, no network |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-42-01 | Tampering | feeds.py DB query | accept | Read-only DuckDB connection; no writes possible. Data integrity owned by upstream pipeline. |
| T-42-02 | Information Disclosure | Atom XML content | accept | All data in feed is already public (Ecdysis is a public database); no PII beyond collector names which are already public record. |
| T-42-03 | Spoofing | XML injection via taxon/collector names | mitigate | `xml.etree.ElementTree` auto-escapes `<`, `>`, `&` in `.text` assignments. No raw string concatenation into XML. Verified in RESEARCH.md. |
| T-42-04 | Denial of Service | Large feed (41K entries) | accept | Static file generation runs in nightly batch; no runtime serving concern. File size ~15-20MB is acceptable for static hosting. |
</threat_model>

<verification>
1. `cd data && uv run pytest tests/test_feeds.py -q` -- all feed-specific tests pass
2. `cd data && uv run pytest -q` -- full suite green (no regressions to export tests)
3. `cd data && uv run python -c "from feeds import main; print('ok')"` -- import succeeds
4. `grep -c 'feeds' data/run.py` -- confirms feeds step present in STEPS
</verification>

<success_criteria>
1. `data/feeds.py` exists with `main()` and `write_determinations_feed()` functions
2. `data/tests/test_feeds.py` has 6 passing tests covering entry fields, time window, sort order, feed metadata, output path, blank exclusion, and empty window
3. `data/run.py` STEPS includes ("feeds", generate_feeds) after export
4. All tests pass: `cd data && uv run pytest -q`
5. conftest.py includes identifications table with seed data and event_date on occurrences
</success_criteria>

<output>
After completion, create `.planning/phases/42-feed-generator-core/42-01-SUMMARY.md`
</output>
