---
phase: 147-app-route-sw-topology
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - infra/test/beeatlas-stack.test.ts
  - infra/package.json
  - infra/lib/beeatlas-stack.ts
autonomous: false
requirements: [ROUTE-03]
user_setup: []

must_haves:
  truths:
    - "ROUTE-03 / D-08: CloudFront has dedicated per-path behaviors for `/app/sw.js` AND `/app/manifest.webmanifest` (NOT an `/app/*` wildcard)"
    - "ROUTE-03 / D-09: both behaviors use a shared zero-TTL `CachePolicy` and a shared `ResponseHeadersPolicy` setting `Cache-Control: no-cache, no-store, must-revalidate` (customHeadersBehavior, override=true)"
    - "ROUTE-03 / D-10: a `cdk synth` template-assertion test (`infra/test/beeatlas-stack.test.ts`) asserts both no-cache behaviors and the Cache-Control header exist, and exits 0"
    - "ROUTE-03 / D-09: the new behaviors mirror the existing `/data/*` `addBehavior` + `ResponseHeadersPolicy` + `CachePolicy` pattern already in `infra/lib/beeatlas-stack.ts`"
    - "The `/app/manifest.webmanifest` behavior is added now even though the manifest file lands in Phase 151 (D-08 — path-pattern behavior is harmless before the file exists)"
  artifacts:
    - path: "infra/lib/beeatlas-stack.ts"
      provides: "Two per-path no-cache CloudFront behaviors + shared zero-TTL CachePolicy + shared no-cache ResponseHeadersPolicy"
      contains: "/app/sw.js"
    - path: "infra/test/beeatlas-stack.test.ts"
      provides: "CDK Template.fromStack assertion test for the no-cache behaviors (ROUTE-03)"
      contains: "/app/manifest.webmanifest"
    - path: "infra/package.json"
      provides: "`test` script running the ts-node CDK assertion"
      contains: "ts-node test/beeatlas-stack.test.ts"
  key_links:
    - from: "infra/lib/beeatlas-stack.ts"
      to: "CloudFront distribution"
      via: "distribution.addBehavior('/app/sw.js', ...) and ('/app/manifest.webmanifest', ...)"
      pattern: "addBehavior\\('/app/(sw\\.js|manifest\\.webmanifest)'"
    - from: "infra/test/beeatlas-stack.test.ts"
      to: "infra/lib/beeatlas-stack.ts"
      via: "Template.fromStack(new BeeAtlasStack(...))"
      pattern: "Template\\.fromStack"
---

<objective>
Add CloudFront `no-cache` behaviors for `/app/sw.js` and `/app/manifest.webmanifest` so SW/manifest updates are never delayed by the default long-TTL (ROUTE-03), and gate the infra against regression with a greenfield `cdk synth` template-assertion test. Both behaviors are per-path (no `/app/*` wildcard — that would break the app-shell caching Phase 148 relies on) and mirror the existing `/data/*` construct pattern.

Purpose: ROUTE-03 is completed in one infra change with no revisit to `beeatlas-stack.ts` in Phase 151 (the manifest behavior is added now even though the file lands later — D-08).

Output: modified `infra/lib/beeatlas-stack.ts`, new `infra/test/beeatlas-stack.test.ts`, new `test` script in `infra/package.json`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/147-app-route-sw-topology/147-CONTEXT.md
@.planning/phases/147-app-route-sw-topology/147-RESEARCH.md
@.planning/phases/147-app-route-sw-topology/147-PATTERNS.md

<interfaces>
<!-- Existing /data/* behavior block (infra/lib/beeatlas-stack.ts lines 53-96) — the direct analog to mirror.
     Construct types used: cloudfront.CachePolicy, cloudfront.ResponseHeadersPolicy,
     distribution.addBehavior(pathPattern, origins.S3BucketOrigin.withOriginAccessControl(siteBucket), {...}).
     Imports already present (lines 1-10): cdk, s3, cloudfront, origins, iam, acm, route53, targets, Construct, GlobalStack.
     No new imports needed. -->
<!-- BeeAtlasStack constructor (lines 16-21): requires props.global: GlobalStack (netZone/comZone/siteCert/redirectCert).
     The test must construct GlobalStack first (env us-east-1, where ACM certs live), then BeeAtlasStack (env us-west-2),
     both with explicit fake account '123456789012' and crossRegionReferences: true (RESEARCH Pitfall 4). -->
<!-- infra/package.json scripts today: cdk/build/watch/synth/diff/deploy. No `test` script exists yet. -->
<!-- infra tsconfig.json: "module": "commonjs" — the test uses node:assert/strict + ts-node, NOT Vitest. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1 (Wave 0): Create the CDK template-assertion test + infra test script</name>
  <read_first>
    - infra/lib/beeatlas-stack.ts lines 1-21 (imports + BeeAtlasStack constructor signature requiring props.global) and lines 53-96 (the /data/* behavior block to understand what structure synth emits)
    - infra/lib/global-stack.ts (GlobalStack constructor signature — what props it needs)
    - infra/package.json (scripts block — where to add `test`)
    - infra/tsconfig.json (confirm "module": "commonjs")
    - 147-RESEARCH.md Pattern 6 + Pitfall 4; 147-PATTERNS.md `infra/test/beeatlas-stack.test.ts` section (full assertion content)
  </read_first>
  <files>infra/test/beeatlas-stack.test.ts, infra/package.json</files>
  <action>Create `infra/test/beeatlas-stack.test.ts` (D-10) as a ts-node script using `node:assert/strict`, `aws-cdk-lib`, and `aws-cdk-lib/assertions` `{ Template, Match }` (all already installed — no new deps). Instantiate a `new cdk.App()`, then `new GlobalStack(app, 'G', { env: { account: '123456789012', region: 'us-east-1' }, crossRegionReferences: true })`, then `new BeeAtlasStack(app, 'S', { env: { account: '123456789012', region: 'us-west-2' }, crossRegionReferences: true, global: globalStack })` — explicit fake account/region per Pitfall 4. Call `Template.fromStack(stack)` and assert via `hasResourceProperties`: (1) `AWS::CloudFront::Distribution` `DistributionConfig.CacheBehaviors` contains an entry with `PathPattern: '/app/sw.js'`; (2) same for `PathPattern: '/app/manifest.webmanifest'`; (3) an `AWS::CloudFront::ResponseHeadersPolicy` whose `CustomHeadersConfig.Items` contains `{ Header: 'Cache-Control', Value: Match.stringLikeRegexp('no-cache'), Override: true }`; (4) an `AWS::CloudFront::CachePolicy` with `CachePolicyConfig.DefaultTTL: 0` and `MaxTTL: 0`. End with `console.log('All CDK assertions passed.')`. Add `"test": "ts-node test/beeatlas-stack.test.ts"` to `infra/package.json` scripts. This test is RED until Task 2 adds the behaviors (the CacheBehaviors/policy assertions will not match).</action>
  <verify>
    <automated>cd infra && grep -q "/app/sw.js" test/beeatlas-stack.test.ts && grep -q "/app/manifest.webmanifest" test/beeatlas-stack.test.ts && grep -q "ts-node test/beeatlas-stack.test.ts" package.json && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `infra/test/beeatlas-stack.test.ts` imports `Template` and `Match` from `aws-cdk-lib/assertions` and `node:assert/strict`
    - The test constructs `GlobalStack` then `BeeAtlasStack` with explicit `account: '123456789012'` and `crossRegionReferences: true`
    - The test asserts `PathPattern: '/app/sw.js'`, `PathPattern: '/app/manifest.webmanifest'`, a `Cache-Control` custom header matching `no-cache`, and a zero-TTL CachePolicy
    - `infra/package.json` scripts contains `ts-node test/beeatlas-stack.test.ts`
  </acceptance_criteria>
  <done>CDK assertion test authored (RED, pre-implementation) and wired into `infra/package.json` test script.</done>
</task>

<task type="auto">
  <name>Task 2: Add the two no-cache CloudFront behaviors to the stack</name>
  <read_first>
    - infra/lib/beeatlas-stack.ts lines 53-96 (the /data/* CachePolicy + ResponseHeadersPolicy + addBehavior block to mirror) and line ~96-98 (insertion point: after the /data/* addBehavior closing `);`, before the Route 53 records block)
    - 147-RESEARCH.md Pattern 5; 147-PATTERNS.md `infra/lib/beeatlas-stack.ts MODIFIED` section (exact construct content + insertion point)
    - 147-RESEARCH.md Pitfall 3 (why per-path, NOT /app/* wildcard)
  </read_first>
  <files>infra/lib/beeatlas-stack.ts</files>
  <action>After the `/data/*` `distribution.addBehavior(...)` block, add (D-08, D-09): a shared zero-TTL `cloudfront.CachePolicy` (construct id e.g. `SwNoCachePolicy`) with `defaultTtl`/`maxTtl`/`minTtl` all `cdk.Duration.seconds(0)`, `queryStringBehavior` none, `cookieBehavior` none, gzip/brotli false; and a shared `cloudfront.ResponseHeadersPolicy` (id e.g. `SwNoCacheHeadersPolicy`) with `customHeadersBehavior.customHeaders` = a single `{ header: 'Cache-Control', value: 'no-cache, no-store, must-revalidate', override: true }`. Then call `distribution.addBehavior('/app/sw.js', origins.S3BucketOrigin.withOriginAccessControl(siteBucket), { viewerProtocolPolicy: REDIRECT_TO_HTTPS, cachePolicy: swNoCachePolicy, responseHeadersPolicy: swNoCacheHeadersPolicy })` and an identical call for `'/app/manifest.webmanifest'`. Use per-path patterns ONLY — do NOT use `/app/*` (Pitfall 3: a wildcard would no-cache the app shell + hashed assets that Phase 148 precaches). Reuse the existing `siteBucket`/`distribution`/`origins` identifiers already in scope; add no new imports. Construct ids are Claude's discretion but must be unique within the stack.</action>
  <verify>
    <automated>cd infra && npx ts-node test/beeatlas-stack.test.ts 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `infra/lib/beeatlas-stack.ts` contains `addBehavior('/app/sw.js'` and `addBehavior('/app/manifest.webmanifest'` and NO `addBehavior('/app/*'`
    - `infra/lib/beeatlas-stack.ts` contains the string `no-cache, no-store, must-revalidate`
    - `cd infra && npx ts-node test/beeatlas-stack.test.ts` exits 0 and prints `All CDK assertions passed.`
  </acceptance_criteria>
  <done>Both per-path no-cache behaviors exist; the CDK assertion test passes (GREEN).</done>
</task>

<task type="auto">
  <name>Task 3: Confirm the stack still synthesizes cleanly</name>
  <read_first>
    - infra/package.json (the `synth` and `build` scripts)
    - infra/lib/beeatlas-stack.ts (the edits from Task 2)
  </read_first>
  <files>infra/lib/beeatlas-stack.ts</files>
  <action>Run `cd infra && npm run build` (tsc, catches type errors in the new constructs) and `cd infra && npm run synth` (full `cdk synth --all`, confirms the template assembles end-to-end including cross-region GlobalStack references). This is the integration gate — no code changes expected if Task 2 is correct. If synth fails on credentials/region, that is environment-only (the assertion test already proved structure with fake env); record the failure mode in the SUMMARY rather than mutating the stack.</action>
  <verify>
    <automated>cd infra && npm run build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `cd infra && npm run build` exits 0 (no TypeScript errors from the new constructs)
    - `cd infra && npm run synth` assembles the template (or fails only on AWS credentials/region, documented as environment-only in the SUMMARY)
    - `cd infra && npx ts-node test/beeatlas-stack.test.ts` still exits 0
  </acceptance_criteria>
  <done>Stack compiles and synthesizes; CDK assertion green.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <what-built>The CloudFront no-cache behaviors for `/app/sw.js` and `/app/manifest.webmanifest` (ROUTE-03). The CDK assertion test proves the synth template structure; a post-deploy `curl -I` confirms the real distribution serves `Cache-Control: no-cache` (D-10). This is a human action because it requires a deploy to the live distribution, which is outside this autonomous plan's scope and gated on the developer's decision to deploy.</what-built>
  <how-to-verify>
    1. Deploy when ready: `cd infra && npm run deploy` (or fold into the normal deploy flow). The `/app/manifest.webmanifest` file does not exist until Phase 151 — that is expected; the behavior is harmless before the file lands (D-08).
    2. After deploy, run `curl -I https://<distribution-domain>/app/sw.js` and confirm the response includes `Cache-Control: no-cache, no-store, must-revalidate`.
    3. Record the `curl -I` output (header line) in `147-HUMAN-UAT.md` (D-10). `/app/manifest.webmanifest` returning 403/404 with the no-cache header present is the expected pre-151 state.
  </how-to-verify>
  <resume-signal>Type "approved" once the live `curl -I` shows `Cache-Control: no-cache` on `/app/sw.js` (recorded in HUMAN-UAT), or "defer" to record the curl check as a pending HUMAN-UAT item to run at the next deploy.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CloudFront edge → browser | Response-header policy applied at the CDN determines whether the browser revalidates the SW script |
| IaC change → distribution config | A mis-scoped path pattern could no-cache more than intended |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-147-04 | Tampering / Denial | CloudFront `/app/sw.js` cache TTL | mitigate | Zero-TTL CachePolicy + `Cache-Control: no-cache, no-store, must-revalidate` ResponseHeadersPolicy force revalidation so a stale SW cannot be pinned at the edge beyond the browser's 24h SW-check window. Asserted by the CDK test (DefaultTTL/MaxTTL == 0; Cache-Control header present) and confirmed live by `curl -I` (D-10). |
| T-147-05 | Denial | over-broad `/app/*` no-cache path | mitigate | Per-path behaviors (`/app/sw.js`, `/app/manifest.webmanifest`) only — never an `/app/*` wildcard (RESEARCH Pitfall 3), so the app shell + hashed assets remain cacheable for Phase 148. Asserted by the CDK test matching exact PathPatterns; `grep` confirms no `/app/*` behavior. |
| T-147-SC | Tampering | npm/pip/cargo installs | n/a | No package installs in this plan (RESEARCH Package Legitimacy Audit not applicable — all tools already in `infra/node_modules`). |
</threat_model>

<verification>
- `cd infra && npx ts-node test/beeatlas-stack.test.ts` exits 0 (`All CDK assertions passed.`).
- `cd infra && npm run build` exits 0; `npm run synth` assembles the template.
- `grep -c "addBehavior('/app/" infra/lib/beeatlas-stack.ts` == 2 with no `/app/*` wildcard.
- Human action (D-10): post-deploy `curl -I` shows `Cache-Control: no-cache` on `/app/sw.js`, recorded in HUMAN-UAT.
</verification>

<success_criteria>
- ROUTE-03: CloudFront serves `/app/sw.js` and `/app/manifest.webmanifest` with `Cache-Control: no-cache, no-store, must-revalidate` via per-path behaviors backed by a zero-TTL cache policy.
- D-10: the `cdk synth` assertion test passes and is wired into `infra/package.json`; the live `curl -I` is recorded in HUMAN-UAT.
- D-08/D-09 honored: manifest behavior added now (file lands in 151); both behaviors share the zero-TTL CachePolicy + no-cache ResponseHeadersPolicy mirroring the `/data/*` pattern.
</success_criteria>

<output>
Create `.planning/phases/147-app-route-sw-topology/147-02-SUMMARY.md` when done.
</output>
