---
phase: 124
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - data/tests/test_dbt_diff.py
  - data/resolve_taxon_ids.py
  - data/run.py
  - data/tests/test_resolve_taxon_ids.py
autonomous: true
requirements:
  - PWK-01
  - PWK-02
  - PWK-03

must_haves:
  truths:
    - "test_occurrences_schema_matches docstring says 36 cols, not 30"
    - "_names_to_resolve() SQL includes inat_obs_data.observations as a third UNION branch"
    - "inat-obs step runs before resolve-taxon-ids step in run.py STEPS"
    - "resolver_db test fixture seeds inat_obs_data.observations table"
    - "a test asserts inat_obs-only names appear in the names-to-resolve set"
    - "inactive taxon ID count is printed when resolve_taxon_ids runs"
  artifacts:
    - path: "data/tests/test_dbt_diff.py"
      provides: "updated docstring reflecting 36-column occurrences contract"
      contains: "36 cols"
    - path: "data/resolve_taxon_ids.py"
      provides: "three-source UNION in _names_to_resolve() + inactive taxon enumeration"
      contains: "inat_obs_data.observations"
    - path: "data/run.py"
      provides: "inat-obs step ordered before resolve-taxon-ids"
      contains: "inat-obs"
    - path: "data/tests/test_resolve_taxon_ids.py"
      provides: "resolver_db fixture with inat_obs_data schema + new union test"
      contains: "inat_obs_data"
  key_links:
    - from: "data/run.py STEPS"
      to: "inat_obs_pipeline.load_inat_obs"
      via: "tuple at index before resolve-taxon-ids"
      pattern: "inat-obs.*resolve-taxon-ids|inat-obs.*\\n.*resolve-taxon-ids"
    - from: "data/resolve_taxon_ids.py:_names_to_resolve"
      to: "inat_obs_data.observations"
      via: "UNION branch in CTE"
      pattern: "inat_obs_data\\.observations"
---

<objective>
Fix stale docstrings, extend taxon ID resolution to cover iNat ARM 3 canonical names,
reorder the pipeline so inat-obs populates before resolve-taxon-ids runs, and enumerate
inactive taxon IDs to scope Phase 127 work.

Purpose: Phase 126 taxon_id completeness depends on resolve_taxon_ids covering all three
occurrence sources (checklist, ecdysis, inat_obs). Without the STEPS reorder, the new
UNION branch queries an empty inat_obs_data.observations on any full-pipeline run.

Output: Four modified files; tests green; inactive taxon count printed to stdout.
</objective>

<execution_context>
@/Users/rainhead/.claude/get-shit-done/workflows/execute-plan.md
@/Users/rainhead/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/rainhead/dev/beeatlas/.planning/PROJECT.md
@/Users/rainhead/dev/beeatlas/.planning/ROADMAP.md
@/Users/rainhead/dev/beeatlas/.planning/STATE.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Fix docstring, extend _names_to_resolve SQL, reorder STEPS, update tests</name>
  <files>
    data/tests/test_dbt_diff.py,
    data/resolve_taxon_ids.py,
    data/run.py,
    data/tests/test_resolve_taxon_ids.py
  </files>

  <read_first>
    - data/tests/test_dbt_diff.py — lines 52–58: test_occurrences_schema_matches docstring containing "30 cols" / "30 columns" (lines 54 and 57)
    - data/resolve_taxon_ids.py — lines 1–6: module docstring mentioning "checklist + ecdysis"; lines 49–71: _names_to_resolve function with SQL CTE containing two UNION branches; line 50: function docstring
    - data/run.py — lines 1–15: module docstring with step order prose; lines 84–106: STEPS list; current ordering has "inat-obs" at index 12 (after "places-load") and "resolve-taxon-ids" at index 7 (after "checklist")
    - data/tests/test_resolve_taxon_ids.py — lines 48–85: resolver_db fixture (currently seeds checklist_data.species and ecdysis_data.occurrences only, no inat_obs_data); lines 135–161: test_names_to_resolve_unions_both_sources (the pattern to mirror for the new test)
  </read_first>

  <behavior>
    - test_dbt_diff.py line 54: docstring line contains "36 cols" (was "30 cols")
    - test_dbt_diff.py line 57: docstring line contains "36 columns" (was "30 columns")
    - resolve_taxon_ids.py module docstring line 3: "checklist + ecdysis + inat_obs" (was "checklist + ecdysis")
    - resolve_taxon_ids.py _names_to_resolve docstring: mentions "(checklist + ecdysis + inat_obs)"
    - resolve_taxon_ids.py _names_to_resolve SQL CTE: third UNION branch "SELECT DISTINCT canonical_name FROM inat_obs_data.observations WHERE canonical_name IS NOT NULL"
    - run.py STEPS: ("inat-obs", load_inat_obs) tuple appears immediately before ("resolve-taxon-ids", ...) and NOT in its old position after ("places-load", load_places_step)
    - run.py module docstring: step order prose updated to show "checklist -> inat-obs -> resolve-taxon-ids -> taxa-download -> ..." (inat-obs no longer appears after places-load)
    - test_resolve_taxon_ids.py resolver_db fixture: two new lines added before con.close() — "CREATE SCHEMA inat_obs_data" and "CREATE TABLE inat_obs_data.observations (canonical_name TEXT)"
    - test_resolve_taxon_ids.py: new test test_names_to_resolve_includes_inat_obs_source seeds a name only in inat_obs_data.observations, calls mod.resolve_taxon_ids() with one mocked response, asserts mock_get.call_count == 1 and the queried name is "ddd species" (or equivalent unique name not present in checklist or ecdysis tables)
    - cd data && uv run pytest tests/test_resolve_taxon_ids.py -x passes (green including new test)
    - cd data && uv run pytest tests/test_dbt_diff.py -x passes (green; @_SANDBOX_GUARD skips the live test but the module parses without error)
  </behavior>

  <action>
    PWK-01 — update test_dbt_diff.py docstring:
    In test_occurrences_schema_matches (lines 53–58), replace "30 cols" with "36 cols" on the first docstring line and "30 columns" with "36 columns" on the "Verified baseline:" line. No logic changes.

    PWK-02 part A — extend _names_to_resolve SQL in resolve_taxon_ids.py:
    In the sql string inside _names_to_resolve (lines 58–71), add a third UNION branch inside the CTE after the ecdysis branch:
      UNION
      SELECT DISTINCT canonical_name FROM inat_obs_data.observations
      WHERE canonical_name IS NOT NULL
    Use UNION (not UNION ALL) — the existing branches use UNION for deduplication and that must be preserved across all three sources.

    PWK-02 part B — update docstrings in resolve_taxon_ids.py:
    Module docstring line 3: change "FULL OUTER union of checklist + ecdysis canonical_name LEFT JOIN bridge." to "FULL OUTER union of checklist + ecdysis + inat_obs canonical_name LEFT JOIN bridge."
    _names_to_resolve function docstring (line 50): change "FULL OUTER union of canonical names LEFT JOIN bridge, filtered by what's missing." to "FULL OUTER union of canonical names (checklist + ecdysis + inat_obs) LEFT JOIN bridge, filtered by what's missing."

    PWK-02 part C — reorder STEPS in run.py:
    Move the ("inat-obs", load_inat_obs) tuple from its current position (index 12, after ("places-load", load_places_step)) to immediately before ("resolve-taxon-ids", lambda: resolve_taxon_ids(refresh=_REFRESH_LINEAGE)). The new sequence in the STEPS list around this area becomes:
      ("checklist", load_checklist),
      ("inat-obs", load_inat_obs),
      ("resolve-taxon-ids", lambda: resolve_taxon_ids(refresh=_REFRESH_LINEAGE)),
      ("taxa-download", download_taxa_csv),
    Also update the module docstring (lines 7–9) step order prose to reflect "checklist -> inat-obs -> resolve-taxon-ids -> taxa-download -> taxon-lineage-extended -> places-validation -> places-load -> dbt-build -> ..." removing inat-obs from its old position after places-load.

    PWK-02 part D — extend resolver_db fixture in test_resolve_taxon_ids.py:
    In the resolver_db fixture, immediately before the existing con.close() call (line 84), add:
      con.execute("CREATE SCHEMA inat_obs_data")
      con.execute("CREATE TABLE inat_obs_data.observations (canonical_name TEXT)")
    This ensures _names_to_resolve() does not raise CatalogError when the new UNION branch is evaluated in tests.

    PWK-02 part E — add new test in test_resolve_taxon_ids.py:
    Add test_names_to_resolve_includes_inat_obs_source after test_names_to_resolve_unions_both_sources (after line 161). The test:
    1. Connects to db_path and inserts one row into inat_obs_data.observations (e.g., "ddd species") — no rows in checklist_data.species or ecdysis_data.occurrences.
    2. Provides one fake response via _fake_taxa_search_response([_matching_taxon(4, "ddd species")]).
    3. Calls mod.resolve_taxon_ids() with patch("inaturalist_pipeline.requests.get", side_effect=responses) as mock_get.
    4. Asserts mock_get.call_count == 1.
    5. Asserts the single queried name is "ddd species": queries = [c.kwargs["params"]["q"] for c in mock_get.call_args_list]; assert queries == ["ddd species"].
    Follow the same patch target and call style as test_names_to_resolve_unions_both_sources exactly.
  </action>

  <verify>
    <automated>cd /Users/rainhead/dev/beeatlas/data && uv run pytest tests/test_resolve_taxon_ids.py tests/test_dbt_diff.py -x -q 2>&1 | tail -10</automated>
  </verify>

  <acceptance_criteria>
    - grep -c "36 cols" /Users/rainhead/dev/beeatlas/data/tests/test_dbt_diff.py returns 1
    - grep -c "30 cols\|30 columns" /Users/rainhead/dev/beeatlas/data/tests/test_dbt_diff.py returns 0
    - grep -c "inat_obs_data\.observations" /Users/rainhead/dev/beeatlas/data/resolve_taxon_ids.py returns 1 (in the SQL CTE)
    - grep -c "checklist + ecdysis + inat_obs" /Users/rainhead/dev/beeatlas/data/resolve_taxon_ids.py returns 2 (module docstring + function docstring)
    - In /Users/rainhead/dev/beeatlas/data/run.py STEPS list, "inat-obs" tuple appears before "resolve-taxon-ids" tuple and NOT after "places-load" tuple: python -c "import ast, sys; src=open('data/run.py').read(); steps=[n.elts[0].s for n in ast.parse(src).body if False] or None" (manual read of run.py is sufficient; grep for ordering: grep -n '"inat-obs"\|"resolve-taxon-ids"\|"places-load"' data/run.py should show inat-obs line number less than resolve-taxon-ids line number)
    - grep -c "inat_obs_data" /Users/rainhead/dev/beeatlas/data/tests/test_resolve_taxon_ids.py returns at least 3 (schema create, table create, test insert)
    - grep -c "test_names_to_resolve_includes_inat_obs_source" /Users/rainhead/dev/beeatlas/data/tests/test_resolve_taxon_ids.py returns 1
    - cd /Users/rainhead/dev/beeatlas/data && uv run pytest tests/test_resolve_taxon_ids.py tests/test_dbt_diff.py -x -q exits 0
  </acceptance_criteria>

  <done>All four files modified; tests green; "inat-obs" precedes "resolve-taxon-ids" in STEPS; docstrings accurate; new test passes for inat_obs UNION branch.</done>
</task>

<task type="auto">
  <name>Task 2: Add inactive taxon enumeration to resolve_taxon_ids.py (PWK-03)</name>
  <files>data/resolve_taxon_ids.py</files>

  <read_first>
    - data/resolve_taxon_ids.py — lines 190–215: resolve_taxon_ids() main function body showing the DuckDB connection pattern (con = duckdb.connect(DB_PATH) / try / finally con.close()) and the print(  # noqa: T201 summary pattern; locate a natural insertion point for the enumeration query (e.g., after the n_resolved/unresolved summary print block, before the function returns)
    - data/raw/taxa.csv.gz — referenced path only; do not read content; confirm the file is at data/raw/taxa.csv.gz relative to the data/ directory (which is Path(__file__).parent / "raw" / "taxa.csv.gz")
  </read_first>

  <behavior>
    - Running cd data && uv run python -c "from resolve_taxon_ids import resolve_taxon_ids" imports without error
    - The enumeration query runs inside the existing resolve_taxon_ids() function using the same con that is already open (no second duckdb.connect call needed)
    - stdout contains a line matching "Inactive taxon IDs in bridge: N" (where N is 0 or an integer) when resolve_taxon_ids() executes
    - The print statement uses the # noqa: T201 comment on the same line as print( per project convention
    - The LEFT JOIN uses read_csv with the path constructed as str(Path(__file__).parent / "raw" / "taxa.csv.gz") and filters WHERE t.active = false (BOOLEAN, not string — no column_types override; DuckDB auto-infers BOOLEAN for this file)
    - The SELECT retrieves b.canonical_name, b.taxon_id, t.name, t.active and the result list length is printed
  </behavior>

  <action>
    Inside resolve_taxon_ids() in resolve_taxon_ids.py, after the existing summary print block (the print(  # noqa: T201 call that reports n_resolved/unresolved), add the following enumeration block using the existing `con` connection object (it is still open at this point in the function):

    Construct the taxa path as: taxa_path = str(Path(__file__).parent / "raw" / "taxa.csv.gz")

    Execute:
      inactive = con.execute(f"""
          SELECT b.canonical_name, b.taxon_id, t.name AS inat_name, t.active
          FROM inaturalist_data.canonical_to_taxon_id b
          LEFT JOIN read_csv('{taxa_path}', header=True) t
              ON CAST(t.taxon_id AS INTEGER) = b.taxon_id
          WHERE t.active = false
          ORDER BY b.canonical_name
      """).fetchall()
      print(  # noqa: T201
          f"resolve-taxon-ids: inactive taxon IDs in bridge: {len(inactive)}"
      )

    If any inactive entries are found (len(inactive) > 0), also print each row so the commit message / log captures them for scoping Phase 127. Follow the same print(  # noqa: T201 pattern for any additional output lines.

    Use the BOOLEAN comparison `active = false` (not `active = 'false'`) because read_csv without a column_types override auto-infers the active column as BOOLEAN in this file.

    Do NOT add a separate duckdb.connect() call; use the `con` that is already open in the resolve_taxon_ids() function body.
  </action>

  <verify>
    <automated>cd /Users/rainhead/dev/beeatlas/data && uv run pytest -x -q 2>&1 | tail -5</automated>
  </verify>

  <acceptance_criteria>
    - grep -c "inactive taxon IDs in bridge" /Users/rainhead/dev/beeatlas/data/resolve_taxon_ids.py returns 1
    - grep -c "active = false" /Users/rainhead/dev/beeatlas/data/resolve_taxon_ids.py returns 1
    - grep -c "raw/taxa.csv.gz" /Users/rainhead/dev/beeatlas/data/resolve_taxon_ids.py returns 1 (in the enumeration query)
    - grep -c "noqa: T201" /Users/rainhead/dev/beeatlas/data/resolve_taxon_ids.py is at least 2 (existing summary print + new enumeration print)
    - cd /Users/rainhead/dev/beeatlas/data && uv run pytest -x -q exits 0
    - cd /Users/rainhead/dev/beeatlas/data && uv run python -c "from resolve_taxon_ids import resolve_taxon_ids" exits 0 (imports cleanly)
  </acceptance_criteria>

  <done>Inactive taxon count printed during resolve-taxon-ids step; full test suite green; count is 0 as of 2026-05-28 taxa.csv.gz.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| pipeline → DuckDB | SQL strings constructed with Python f-strings; only DB_PATH and taxa_path are interpolated — both are filesystem paths set by env or __file__, not user input |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-124-01 | Tampering | taxa.csv.gz path interpolation in f-string SQL | accept | Path is derived from __file__ (module location), not user input; no injection vector |
| T-124-SC | Tampering | npm/pip/cargo installs | accept | No new packages installed in this phase; pyproject.toml unchanged |
</threat_model>

<verification>
After both tasks complete:
  cd /Users/rainhead/dev/beeatlas/data && uv run pytest -x -q

Expected: all tests pass, no failures, no errors.

Ordering sanity check:
  grep -n '"inat-obs"\|"resolve-taxon-ids"' /Users/rainhead/dev/beeatlas/data/run.py

Expected: inat-obs line number is lower than resolve-taxon-ids line number.
</verification>

<success_criteria>
1. test_occurrences_schema_matches docstring says "36 cols" / "36 columns" (not "30")
2. _names_to_resolve() SQL CTE has three UNION branches: checklist_data.species, ecdysis_data.occurrences, inat_obs_data.observations
3. run.py STEPS: ("inat-obs", load_inat_obs) appears immediately before ("resolve-taxon-ids", ...) — inat_obs_data.observations is populated before resolve_taxon_ids runs
4. resolver_db fixture seeds inat_obs_data schema and observations table
5. test_names_to_resolve_includes_inat_obs_source passes
6. resolve_taxon_ids() prints "resolve-taxon-ids: inactive taxon IDs in bridge: N" during execution
7. cd data && uv run pytest exits 0
</success_criteria>

<output>
Create /Users/rainhead/dev/beeatlas/.planning/phases/124-pre-work-contract-cleanup/124-01-SUMMARY.md when done.
</output>
