---
phase: "086"
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - data/tests/test_dbt_diff.py
autonomous: true
requirements:
  - VALIDATE-01
must_haves:
  truths:
    - "Running `uv run --project data pytest data/tests/test_dbt_diff.py -x` PASSES (10 existing tests PASS, 5 new species tests SKIP)"
    - "5 new species diff test functions exist in test_dbt_diff.py"
    - "Each new test is SKIP-guarded so it does not fail before sandbox/species.parquet (or species.json / seasonality.json) exists"
    - "When sandbox/species.parquet later appears, the 3 parquet tests will execute (no SKIP); when sandbox/species.json and sandbox/seasonality.json appear, the JSON tests will execute"
  artifacts:
    - path: "data/tests/test_dbt_diff.py"
      provides: "5 new test functions: test_species_parquet_row_count_matches, test_species_parquet_schema_matches, test_species_canonical_name_key_set_matches, test_species_json_matches, test_seasonality_json_matches"
      contains: "SANDBOX_SPECIES_PARQUET_GUARD"
  key_links:
    - from: "test_dbt_diff.py"
      to: "SANDBOX / 'species.parquet'"
      via: "pytest.mark.skipif on file existence"
      pattern: "SANDBOX_SPECIES_PARQUET_GUARD"
    - from: "test_dbt_diff.py"
      to: "PUBLIC / 'species.json'"
      via: "byte-comparable .read_bytes() comparison"
      pattern: "read_bytes\\(\\) =="
---

<objective>
VALIDATE-01 — extend the diff harness BEFORE any species mart code lands so subsequent waves get green/SKIP signal incrementally. Adds 5 SKIP-guarded species artifact diff tests to `data/tests/test_dbt_diff.py`. No dbt model changes; no new sources; nothing in the harness breaks.

Purpose: research §VALIDATE-01 requires harness extension to land in Wave 0 so that Wave 1+ commits show progressively-failing-then-passing signal as artifacts come online. Without this gate, PORT-01 cannot prove byte-comparability with public/data baselines.

Output: 5 new pytest functions + 1 new SANDBOX guard constant appended to test_dbt_diff.py. All initially SKIP because the sandbox files do not yet exist.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/086-port-remaining-transforms/086-RESEARCH.md
@.planning/phases/086-port-remaining-transforms/086-PATTERNS.md
@./CLAUDE.md

@data/tests/test_dbt_diff.py
</context>

<interfaces>
<!-- Existing patterns in test_dbt_diff.py the executor MUST mirror exactly. -->

From data/tests/test_dbt_diff.py (lines 23-29):
```
SANDBOX = Path(__file__).resolve().parent.parent / "dbt" / "target" / "sandbox"
PUBLIC = Path(__file__).resolve().parent.parent.parent / "public" / "data"

_SANDBOX_GUARD = pytest.mark.skipif(
    not (SANDBOX / "occurrences.parquet").exists(),
    reason="run `bash data/dbt/run.sh build` first to produce sandbox outputs",
)
```

Test function shape (lines 36-48): `@<guard>` decorator, docstring with VERIFIED baseline numbers, `duckdb.execute(...).fetchone()[0]`, `assert s == p, f"...sandbox={s}, public={p}"`.

Anti-join test pattern (lines 101-130): EXCEPT in both directions, two assertions.

Verified production baselines (from RESEARCH §PORT-01):
- public/data/species.parquet: 629 rows, 19 columns
- public/data/species.json: 629 rows
- public/data/seasonality.json: 556 species
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add SANDBOX_SPECIES_PARQUET_GUARD and 3 species.parquet diff tests</name>
  <files>data/tests/test_dbt_diff.py</files>
  <read_first>
    - data/tests/test_dbt_diff.py (full file — must understand existing _SANDBOX_GUARD style, naming, docstring conventions, and EXCEPT anti-join pattern at lines 101-130)
    - .planning/phases/086-port-remaining-transforms/086-PATTERNS.md (§`data/tests/test_dbt_diff.py` for the exact target snippets to append)
    - .planning/phases/086-port-remaining-transforms/086-RESEARCH.md (§VALIDATE-01 for SKIP-guard wording and progression semantics)
  </read_first>
  <behavior>
    - After this task: pytest collects 13 occurrence-domain tests (10 existing + 3 new) and reports 10 PASS + 3 SKIP when sandbox/species.parquet does not exist.
    - SKIP reason for the 3 new tests reads exactly: "run `bash data/dbt/run.sh build` first to produce sandbox species.parquet" (matches PATTERNS.md guidance, do not abbreviate).
    - test_species_parquet_row_count_matches docstring records "Verified baseline: both 629 rows."
    - test_species_parquet_schema_matches docstring records "19-column schema (names + types) identical between sandbox and public (19 cols)."
    - test_species_canonical_name_key_set_matches uses two EXCEPT directions and two distinct assertion messages, mirroring test_occurrences_ecdysis_id_join_full at lines 101-130.
  </behavior>
  <action>
    Append to the END of data/tests/test_dbt_diff.py (after the existing GeoJSON tests, with a section banner comment `# PORT-01: Species artifact diff tests`):

    1. New module-level constant `SANDBOX_SPECIES_PARQUET_GUARD = pytest.mark.skipif(...)` checking `(SANDBOX / "species.parquet").exists()` with the reason string above. Use the public name (no leading underscore) so it differs visibly from `_SANDBOX_GUARD` and the difference is grep-able — see PATTERNS.md §"pytest skipif guard with named constant".

    2. `def test_species_parquet_row_count_matches()` — decorated with `@SANDBOX_SPECIES_PARQUET_GUARD`. Body: two `duckdb.execute(f"SELECT COUNT(*) FROM read_parquet('{SANDBOX}/species.parquet')").fetchone()[0]` and matching PUBLIC query, then `assert s == p, f"Row count mismatch: sandbox={s}, public={p}"`.

    3. `def test_species_parquet_schema_matches()` — decorated. Body: collect `[(r[0], r[1]) for r in duckdb.execute(f"DESCRIBE SELECT * FROM read_parquet('{SANDBOX}/species.parquet')").fetchall()]` for sandbox AND public, assert lists equal with the existing diff message format ("Sandbox only: ...\nPublic only: ...").

    4. `def test_species_canonical_name_key_set_matches()` — decorated. Two `SELECT COUNT(*) FROM (SELECT canonical_name FROM read_parquet(...) EXCEPT SELECT canonical_name FROM read_parquet(...))` queries (one direction each). Two assertions with messages: `"{n} canonical_names in sandbox but not public"` and `"{n} canonical_names in public but not sandbox"`.

    Do NOT modify any existing function. Do NOT change the existing `_SANDBOX_GUARD` import or constant. Do NOT alter the SANDBOX/PUBLIC path constants. The exact code snippets to copy live in 086-PATTERNS.md §`data/tests/test_dbt_diff.py` lines starting at "Apply — add the following 5 functions and 2 guards".
  </action>
  <verify>
    <automated>uv run --project data pytest data/tests/test_dbt_diff.py -x --collect-only 2>&amp;1 | grep -E "test_species_parquet_(row_count|schema)|test_species_canonical_name_key_set" | wc -l | grep -q "^[[:space:]]*3$" &amp;&amp; uv run --project data pytest data/tests/test_dbt_diff.py -x 2>&amp;1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "def test_species_parquet_row_count_matches" data/tests/test_dbt_diff.py` returns exactly 1 match
    - `grep -n "def test_species_parquet_schema_matches" data/tests/test_dbt_diff.py` returns exactly 1 match
    - `grep -n "def test_species_canonical_name_key_set_matches" data/tests/test_dbt_diff.py` returns exactly 1 match
    - `grep -c "SANDBOX_SPECIES_PARQUET_GUARD" data/tests/test_dbt_diff.py` returns &gt;= 4 (1 definition + 3 decorator usages)
    - `uv run --project data pytest data/tests/test_dbt_diff.py -x` exits 0
    - Pytest summary line shows the 3 new tests as SKIP (sandbox/species.parquet does not exist in worktree); existing 10 tests retain their pre-task status
    - `python3 -m py_compile data/tests/test_dbt_diff.py` exits 0 (no syntax errors)
  </acceptance_criteria>
  <done>3 species.parquet diff tests added, all SKIP-guarded, suite green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add 2 species.json / seasonality.json byte-comparable diff tests</name>
  <files>data/tests/test_dbt_diff.py</files>
  <read_first>
    - data/tests/test_dbt_diff.py (state after Task 1)
    - .planning/phases/086-port-remaining-transforms/086-PATTERNS.md (§`data/tests/test_dbt_diff.py` — the JSON post-step test snippets at the bottom)
    - .planning/phases/086-port-remaining-transforms/086-RESEARCH.md (§VALIDATE-01 — note that JSON tests use byte comparison, NOT structural comparison)
  </read_first>
  <behavior>
    - After this task: pytest collects 15 tests (10 existing + 5 new), reports 10 PASS + 5 SKIP when sandbox JSON sidecars do not exist.
    - test_species_json_matches and test_seasonality_json_matches each use independent `@pytest.mark.skipif(...)` checking the specific JSON file (NOT the parquet guard) — they SKIP independently because they may exist independently from the parquet (Plan 086-04 produces parquet, Plan 086-05 produces JSON sidecars).
    - Both use `(SANDBOX / "FILENAME").read_bytes() == (PUBLIC / "FILENAME").read_bytes()` — byte comparison, not json.loads + dict comparison. Per RESEARCH §VALIDATE-01 and Pitfall 3, this is intentional (sort_keys + indent + separators are part of the contract).
  </behavior>
  <action>
    Append below the 3 tests created in Task 1, in the same `# PORT-01: Species artifact diff tests` section:

    1. `@pytest.mark.skipif(not (SANDBOX / "species.json").exists(), reason="run species JSON post-step first")` decorating `def test_species_json_matches()`. Body: read both files with `.read_bytes()`, assert equality with message `"species.json content differs between sandbox and public"`. Docstring: "sandbox/species.json content == public/data/species.json (byte-comparable)."

    2. Same shape for `def test_seasonality_json_matches()` with file path `seasonality.json` and message `"seasonality.json content differs between sandbox and public"`. Docstring: "sandbox/seasonality.json content == public/data/seasonality.json (byte-comparable)."

    Do NOT introduce a JSON-specific guard constant — these two tests have independent existence semantics, so inline `pytest.mark.skipif` is correct (Plan 086-05 fills these JSON files independently of the parquet from Plan 086-04). Do NOT use `json.loads` + dict equality — RESEARCH §Pitfall 3 explicitly forbids it because key ordering and separator formatting are load-bearing.
  </action>
  <verify>
    <automated>uv run --project data pytest data/tests/test_dbt_diff.py -x --collect-only 2>&amp;1 | grep -E "test_species_json_matches|test_seasonality_json_matches" | wc -l | grep -q "^[[:space:]]*2$" &amp;&amp; uv run --project data pytest data/tests/test_dbt_diff.py -x 2>&amp;1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "def test_species_json_matches" data/tests/test_dbt_diff.py` returns exactly 1 match
    - `grep -n "def test_seasonality_json_matches" data/tests/test_dbt_diff.py` returns exactly 1 match
    - `grep -c "read_bytes()" data/tests/test_dbt_diff.py` returns &gt;= 4 (2 tests x 2 files each)
    - `grep -c "json.loads" data/tests/test_dbt_diff.py` does NOT increase relative to pre-task baseline (no structural-comparison usage in these new tests)
    - Pytest summary shows: 10 existing PASS, 5 new SKIP (3 from Task 1 + 2 from this task), 0 FAIL, 0 ERROR
    - `python3 -m py_compile data/tests/test_dbt_diff.py` exits 0
  </acceptance_criteria>
  <done>5 species diff tests in place (3 parquet + 2 JSON), all SKIP-guarded, harness green.</done>
</task>

</tasks>

<verification>
- `uv run --project data pytest data/tests/test_dbt_diff.py -x` exits 0 with summary "10 passed, 5 skipped" (or equivalent given pre-existing state — the 5 new tests must contribute SKIP, not PASS or FAIL).
- `python3 -m py_compile data/tests/test_dbt_diff.py` exits 0.
- `git diff --stat data/tests/test_dbt_diff.py` shows ONLY this file modified.
- `bash data/dbt/run.sh build` (if executor chooses to run it) MUST exit 0 with PASS=33 unchanged — this plan touches no dbt models.
</verification>

<success_criteria>
1. 5 new pytest functions exist in `data/tests/test_dbt_diff.py` matching the names listed in must_haves.artifacts.
2. The diff harness exits 0 (existing 10 tests still pass; new 5 tests SKIP because sandbox species artifacts do not yet exist).
3. No other file in the repo is modified.
4. `dbt build` exit status remains 0 with PASS=33 (this plan does not touch any dbt model or source declaration).
</success_criteria>

<output>
After completion, create `.planning/phases/086-port-remaining-transforms/086-01-SUMMARY.md` summarizing the harness extension, the SKIP/PASS test counts before and after, and the plan's role in the Wave 0 → Wave 3 progression.
</output>
