---
phase: "086"
plan: "03"
type: execute
wave: 2
depends_on:
  - "086-01"
files_modified:
  - .planning/phases/086-port-remaining-transforms/ingestion-boundary.md
autonomous: true
requirements:
  - PORT-02
  - PORT-04
must_haves:
  truths:
    - "A documented decision exists in `.planning/phases/086-port-remaining-transforms/ingestion-boundary.md` covering both PORT-02 (occurrence-links derivation) and PORT-04 (resolve_taxon_ids.py)"
    - "PORT-02 section confirms the join+projection logic is ALREADY in dbt (int_waba_link + int_ecdysis_base via stg_ecdysis__occurrence_links) and identifies the remaining ingestion boundary (data/ecdysis_pipeline.py::load_links — HTML scraping)"
    - "PORT-04 section records the decision to KEEP `data/resolve_taxon_ids.py` in Python with rationale (iNat API HTTP calls, ambiguity-resolution policy, rate-limiting, stateful UPSERT)"
    - "Each script is paired with the dbt source/staging seam it crosses, citing the actual file paths for sources.yml entries and staging views"
  artifacts:
    - path: ".planning/phases/086-port-remaining-transforms/ingestion-boundary.md"
      provides: "Written ingestion-boundary decision record covering PORT-02 + PORT-04"
      min_lines: 60
      contains: "PORT-04"
  key_links:
    - from: "ingestion-boundary.md"
      to: "data/ecdysis_pipeline.py::load_links"
      via: "named reference + line range"
      pattern: "ecdysis_pipeline.*load_links"
    - from: "ingestion-boundary.md"
      to: "data/resolve_taxon_ids.py"
      via: "named reference"
      pattern: "resolve_taxon_ids"
    - from: "ingestion-boundary.md"
      to: "data/dbt/models/sources.yml"
      via: "cites the source() declarations these scripts feed"
      pattern: "sources\\.yml"
---

<objective>
PORT-02 + PORT-04 — record the ingestion-vs-transform boundary decisions in a single documentation file. Both requirements resolve to documentation rather than new SQL: the join logic for occurrence-links is already in dbt (int_waba_link + int_ecdysis_base), and `resolve_taxon_ids.py` is unambiguously ingestion (iNat API caller).

Purpose: REQUIREMENTS.md and RESEARCH §PORT-02/§PORT-04 require a documented decision. Without this artifact, future readers (and Phase 88 cutover) cannot tell whether `data/resolve_taxon_ids.py` and `data/ecdysis_pipeline.py::load_links` are scheduled for deletion or are intentional permanent residents of the Python ingestion path.

Output: 1 new markdown file under the phase directory. No source code modified. No dbt model touched.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/086-port-remaining-transforms/086-RESEARCH.md
@.planning/phases/086-port-remaining-transforms/086-PATTERNS.md
@./CLAUDE.md

@data/resolve_taxon_ids.py
@data/ecdysis_pipeline.py
@data/dbt/models/sources.yml
@data/dbt/models/intermediate/int_waba_link.sql
@data/dbt/models/intermediate/int_ecdysis_base.sql
@data/dbt/models/staging/stg_ecdysis__occurrence_links.sql
</context>

<interfaces>
<!-- The seam to document. -->

PORT-02 dbt-side artifacts already in place (verified — see 086-RESEARCH §PORT-02):
- data/dbt/models/staging/stg_ecdysis__occurrence_links.sql wraps source('ecdysis_data', 'occurrence_links')
- data/dbt/models/intermediate/int_waba_link.sql computes catalog_suffix → MIN(waba.id) (specimen_observation_id)
- data/dbt/models/intermediate/int_ecdysis_base.sql consumes both via ref()
- sources.yml line 8: ecdysis_data.occurrence_links is already declared as a source

PORT-04 ingestion artifact:
- data/resolve_taxon_ids.py — calls iNat /v1/taxa endpoint, applies _pick_match D-02 ambiguity-resolution policy, UPSERTS into inaturalist_data.canonical_to_taxon_id, logs unresolved names to data/lineage_unresolved.csv, has --refresh-lineage CLI flag, paces requests per _INAT_PACE_SECONDS

PORT-02 ingestion artifact:
- data/ecdysis_pipeline.py — `load_links` step (also referenced from data/run.py STEPS as ("ecdysis-links", load_links)) populates ecdysis_data.occurrence_links via Ecdysis HTML scraping

Companion staging view created in Plan 086-02 for PORT-04: stg_inat__canonical_to_taxon_id wraps the table that resolve_taxon_ids.py writes.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Author ingestion-boundary.md covering PORT-02 and PORT-04</name>
  <files>.planning/phases/086-port-remaining-transforms/ingestion-boundary.md</files>
  <read_first>
    - data/resolve_taxon_ids.py (full file — extract: HTTP endpoint, rate-limiting constants, the _pick_match policy, UPSERT target table, CSV output path, --refresh-lineage handling)
    - data/ecdysis_pipeline.py (Grep for `load_links` and `occurrence_links` — extract: what HTML/CSV is scraped, what target table is written, what columns are emitted)
    - data/dbt/models/intermediate/int_waba_link.sql (full file — confirm specimen_observation_id derivation already in dbt)
    - data/dbt/models/intermediate/int_ecdysis_base.sql (Grep for `occurrence_links` — confirm host_observation_id LEFT JOIN already in dbt)
    - data/dbt/models/sources.yml (current state after Plan 086-02 commits — confirm canonical_to_taxon_id and occurrence_links declarations)
    - .planning/phases/086-port-remaining-transforms/086-RESEARCH.md §PORT-02 + §PORT-04 (the analysis the doc must summarize)
    - .planning/phases/086-port-remaining-transforms/086-PATTERNS.md §`ingestion-boundary.md` (greenfield content shape guidance)
  </read_first>
  <action>
    Create `.planning/phases/086-port-remaining-transforms/ingestion-boundary.md` as a single decision-record document with these required sections (markdown headings level 2 unless noted):

    Frontmatter (YAML):
    ```
    ---
    phase: 086-port-remaining-transforms
    requirements: [PORT-02, PORT-04]
    decision: keep both Python ingestion scripts in place; consume their output via dbt source() declarations
    decided: 2026-05-14
    ---
    ```

    `# Ingestion vs. Transform Boundary — Phase 86 Decision Record`

    `## Context` — One paragraph: v3.4 cuts SQL transforms over to dbt. Two scripts (`data/ecdysis_pipeline.py::load_links` and `data/resolve_taxon_ids.py`) raised the question "port to dbt or keep in Python?". This document records the answer for both, plus the criterion used to decide.

    `## Decision Criterion` — One paragraph: SQL-shaped (pure transformation over tables) → port to dbt. Python-shaped (HTTP calls, procedural policy, rate-limiting, stateful side effects) → keep in Python and declare the resulting DB table as a dbt `source()`. Cite that this matches the architectural responsibility map in 086-RESEARCH.md.

    `## PORT-02: Occurrence-Links Derivation` — Cover:
    - What `data/ecdysis_pipeline.py::load_links` does (HTML scraping of Ecdysis links, populates `ecdysis_data.occurrence_links` with occurrence_id → host_observation_id mapping).
    - Decision: KEEP in Python (HTTP scraping is ingestion). The join + projection that REQUIREMENTS.md asked to "express as dbt" is already done — point to:
      - `data/dbt/models/staging/stg_ecdysis__occurrence_links.sql` — source wrapper
      - `data/dbt/models/intermediate/int_ecdysis_base.sql` — host_observation_id LEFT JOIN on `ref('stg_ecdysis__occurrence_links')`
      - `data/dbt/models/intermediate/int_waba_link.sql` — specimen_observation_id derivation
      - `data/dbt/models/sources.yml` line 8 — `ecdysis_data.occurrence_links` already declared
    - The seam: `load_links` writes → `ecdysis_data.occurrence_links` → `source()` → `stg_ecdysis__occurrence_links` → consumed by `int_ecdysis_base`. Phase 88 cutover deletes the Python join logic in `data/export.py` (lines 46-55 waba_link, lines ~80 occurrence_links LEFT JOIN); the SCRAPING (load_links) stays.

    `## PORT-04: resolve_taxon_ids.py` — Cover:
    - What `data/resolve_taxon_ids.py` does (queries unresolved canonical_names from FULL OUTER union of checklist_data.species and ecdysis_data.occurrences; calls iNat /v1/taxa endpoint; applies `_pick_match` D-02 ambiguity-resolution policy; UPSERTs into `inaturalist_data.canonical_to_taxon_id`; logs unresolved to `data/lineage_unresolved.csv`; honors `--refresh-lineage` flag from `data/run.py`).
    - Why it is unambiguously Python-shaped: HTTP requests, procedural policy with multi-step filter ladder, rate-limiting (`_INAT_PACE_SECONDS`, `_inat_get_with_retry`), stateful UPSERT with skip-already-resolved logic, CSV side effect for human review.
    - Decision: KEEP in Python. Declare `inaturalist_data.canonical_to_taxon_id` as a dbt `source()` (done in Plan 086-02 — sources.yml + stg_inat__canonical_to_taxon_id.sql).
    - The seam: `resolve_taxon_ids.py` writes → `inaturalist_data.canonical_to_taxon_id` → `source()` → `stg_inat__canonical_to_taxon_id` → consumed by `int_species_universe` (in Plan 086-04) and `test_lin05_lineage_coverage` (in Plan 086-02).

    `## Companion Note: enrich_taxon_lineage_extended` — Brief paragraph: same shape applies. `data/inaturalist_pipeline.py::enrich_taxon_lineage_extended` calls iNat /v1/taxa for ancestor chains; it stays in Python; the resulting `inaturalist_data.taxon_lineage_extended` table is a dbt `source()` (declared in Plan 086-02). Mention because PORT-03 references this script even though the requirement is closed by source declarations + LIN-05 test.

    `## Consequences for Phase 88 Cutover` — Bullet list:
    - `data/run.py` STEPS list keeps three Python ingestion entries: `ecdysis-links` (load_links), `resolve-taxon-ids`, `taxon-lineage-extended`.
    - `data/run.py` STEPS list LOSES: `export` (replaced by `dbt build`), `species-export` (replaced by dbt mart + Python JSON post-step from Plan 086-05).
    - `data/export.py` becomes deletable EXCEPT for any remaining anti-entropy / one-shot helpers (out of scope for Phase 86 — Phase 88 audits).

    Do NOT speculate about future requirements not in REQUIREMENTS.md (e.g., `is_provisional` enum refactor is explicitly out of scope per ROADMAP). Do NOT propose porting `resolve_taxon_ids.py` to dbt Python models — RESEARCH §PORT-04 and Don't Hand-Roll table both reject that approach for the rate-limiting + pandas-dependency cost.
  </action>
  <verify>
    <automated>test -f .planning/phases/086-port-remaining-transforms/ingestion-boundary.md &amp;&amp; wc -l .planning/phases/086-port-remaining-transforms/ingestion-boundary.md | awk '{ if ($1 < 60) { print "FAIL: doc too short ("$1" lines)"; exit 1 } else { print "OK: "$1" lines" } }' &amp;&amp; grep -c "PORT-02" .planning/phases/086-port-remaining-transforms/ingestion-boundary.md | awk '{ if ($1 < 1) { exit 1 } }' &amp;&amp; grep -c "PORT-04" .planning/phases/086-port-remaining-transforms/ingestion-boundary.md | awk '{ if ($1 < 1) { exit 1 } }' &amp;&amp; grep -q "resolve_taxon_ids" .planning/phases/086-port-remaining-transforms/ingestion-boundary.md &amp;&amp; grep -q "load_links" .planning/phases/086-port-remaining-transforms/ingestion-boundary.md &amp;&amp; grep -q "stg_ecdysis__occurrence_links" .planning/phases/086-port-remaining-transforms/ingestion-boundary.md &amp;&amp; grep -q "stg_inat__canonical_to_taxon_id" .planning/phases/086-port-remaining-transforms/ingestion-boundary.md</automated>
  </verify>
  <acceptance_criteria>
    - File exists at `.planning/phases/086-port-remaining-transforms/ingestion-boundary.md`
    - File contains a `# Ingestion vs. Transform Boundary` H1 heading
    - File contains `## PORT-02:` and `## PORT-04:` H2 headings
    - File length >= 60 lines (substantive enough to record both decisions, not a stub)
    - File names all four anchor identifiers in active prose: `data/ecdysis_pipeline.py` (or `load_links`), `data/resolve_taxon_ids.py`, `int_waba_link`, `stg_inat__canonical_to_taxon_id`
    - File cites `data/dbt/models/sources.yml` (the source declarations across the seam)
    - YAML frontmatter parses: `python3 -c "import yaml; head = open('.planning/phases/086-port-remaining-transforms/ingestion-boundary.md').read().split('---', 2); yaml.safe_load(head[1])"` exits 0
    - File does NOT propose porting either script to a dbt Python model (verify: `grep -c 'dbt python model' .planning/phases/086-port-remaining-transforms/ingestion-boundary.md` returns 0; case-insensitive variants also absent)
  </acceptance_criteria>
  <done>Single decision-record document exists, covers both requirements, cites all five key dbt artifacts (sources.yml + 4 model files), and is internally consistent with RESEARCH §PORT-02 and §PORT-04.</done>
</task>

</tasks>

<verification>
- File `.planning/phases/086-port-remaining-transforms/ingestion-boundary.md` exists and is >= 60 lines.
- No source code (`data/`, `src/`, `data/dbt/`) is modified by this plan; `git diff --stat data/ src/` shows zero changes from this plan.
- `bash data/dbt/run.sh build` continues to exit 0 — this plan touches no SQL.
- `uv run --project data pytest data/tests/test_dbt_diff.py -x` continues to exit 0 — this plan touches no Python.
</verification>

<success_criteria>
1. PORT-02 requirement is closed: the doc records that the join+projection are already expressed in dbt and identifies the Python ingestion boundary (load_links).
2. PORT-04 requirement is closed: the doc records the decision to keep `resolve_taxon_ids.py` in Python with explicit rationale and identifies the dbt seam (`source()` + staging wrapper).
3. The doc is the canonical reference cited from Phase 88 plans for "what's the cutover scope?".
4. Doc cites the actual existing artifacts (sources.yml entries, intermediate model files) so a future reader can navigate from the decision to the implementation in one click.
</success_criteria>

<output>
After completion, create `.planning/phases/086-port-remaining-transforms/086-03-SUMMARY.md` summarizing the documented decisions, file size, and the closed requirements (PORT-02, PORT-04).
</output>
