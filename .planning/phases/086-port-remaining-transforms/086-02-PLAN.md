---
phase: "086"
plan: "02"
type: execute
wave: 2
depends_on:
  - "086-01"
files_modified:
  - data/dbt/models/sources.yml
  - data/dbt/models/staging/schema.yml
  - data/dbt/models/staging/stg_inat__canonical_to_taxon_id.sql
  - data/dbt/models/staging/stg_inat__taxon_lineage_extended.sql
  - data/dbt/models/staging/stg_checklist__species.sql
  - data/dbt/tests/test_lin05_lineage_coverage.sql
autonomous: true
requirements:
  - PORT-03
must_haves:
  truths:
    - "`bash data/dbt/run.sh build` exits 0 with at least PASS=39 (33 prior + 3 not_null + 2 unique + 1 LIN-05 singular = 39 minimum); WARN=0, ERROR=0"
    - "Three new staging views exist as materialized views in dbt_sandbox schema: stg_inat__canonical_to_taxon_id, stg_inat__taxon_lineage_extended, stg_checklist__species"
    - "LIN-05 singular test passes against live data (current coverage 100% — 735/735); the test SQL references staging models via ref(), not source() directly"
    - "sources.yml declares two new tables under inaturalist_data and a new checklist_data source block with species + species_counties tables"
  artifacts:
    - path: "data/dbt/models/sources.yml"
      provides: "Source declarations for canonical_to_taxon_id, taxon_lineage_extended, and checklist_data.species (+ species_counties)"
      contains: "checklist_data"
    - path: "data/dbt/models/staging/stg_inat__canonical_to_taxon_id.sql"
      provides: "Pass-through view wrapping inaturalist_data.canonical_to_taxon_id"
      contains: "source('inaturalist_data', 'canonical_to_taxon_id')"
    - path: "data/dbt/models/staging/stg_inat__taxon_lineage_extended.sql"
      provides: "Pass-through view wrapping inaturalist_data.taxon_lineage_extended"
      contains: "source('inaturalist_data', 'taxon_lineage_extended')"
    - path: "data/dbt/models/staging/stg_checklist__species.sql"
      provides: "Pass-through view wrapping checklist_data.species"
      contains: "source('checklist_data', 'species')"
    - path: "data/dbt/models/staging/schema.yml"
      provides: "not_null + unique data_tests on the new staging primary keys"
      contains: "stg_inat__canonical_to_taxon_id"
    - path: "data/dbt/tests/test_lin05_lineage_coverage.sql"
      provides: "Singular dbt test asserting LIN-05 lineage coverage ratio >= 0.95"
      contains: "ratio < 0.95"
  key_links:
    - from: "data/dbt/tests/test_lin05_lineage_coverage.sql"
      to: "stg_inat__canonical_to_taxon_id, stg_inat__taxon_lineage_extended, stg_checklist__species"
      via: "{{ ref(...) }} (NOT source() directly — Pitfall 8)"
      pattern: "ref\\('stg_(inat|checklist)__"
---

<objective>
PORT-03 — declare the new dbt sources and staging views needed by the species mart, and add the LIN-05 ≥0.95 lineage coverage singular test. After this plan, `dbt build` runs the LIN-05 assertion on every build and the species universe DAG has its three input staging views ready for the int_species_universe model in Plan 086-04.

Purpose: Per RESEARCH §PORT-03, `enrich_taxon_lineage_extended` is ingestion (iNat API calls, stays in Python), but the resulting `inaturalist_data.taxon_lineage_extended` table is consumable by dbt as a `source()`. The same applies to `canonical_to_taxon_id` (written by `resolve_taxon_ids.py`). The deliverable is source declarations + staging wrappers + a singular test that closes LIN-05.

Output: 1 modified config file (sources.yml), 1 modified config (staging/schema.yml), 3 new staging .sql files, 1 new singular test .sql file. No marts touched. `dbt build` PASS count rises by ~6.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/086-port-remaining-transforms/086-RESEARCH.md
@.planning/phases/086-port-remaining-transforms/086-PATTERNS.md
@.planning/phases/085-pre-cutover-groundwork/085-04-SUMMARY.md

@data/dbt/models/sources.yml
@data/dbt/models/staging/schema.yml
@data/dbt/models/staging/stg_waba__observations.sql
@data/dbt/tests/test_ecdysis_id_references_source.sql
</context>

<interfaces>
<!-- Schemas in beeatlas.duckdb that this plan declares as sources. -->

inaturalist_data.canonical_to_taxon_id columns (verified — 735 rows):
  canonical_name VARCHAR (PK), taxon_id BIGINT, resolved_at, source

inaturalist_data.taxon_lineage_extended columns (verified — 2196 rows):
  taxon_id BIGINT (PK), family VARCHAR, subfamily VARCHAR, tribe VARCHAR, genus VARCHAR, subgenus VARCHAR

checklist_data.species columns (relevant to PORT-01):
  scientificName, canonical_name, family, subfamily, tribe, genus, subgenus, specific_epithet, status

Existing staging analog — stg_waba__observations.sql (10 lines): comment header naming source + downstream consumers, then `{{ config(materialized='view') }}` and `SELECT * FROM {{ source(...) }}`. Copy this shape exactly.

Existing singular test analog — data/dbt/tests/test_ecdysis_id_references_source.sql (32 lines): rich comment header with PASS=0 semantics, what it replaces, why it is correct, VERIFIED baseline. Body is a pure SELECT that returns rows iff invariant violated.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Declare three new dbt sources in sources.yml</name>
  <files>data/dbt/models/sources.yml</files>
  <read_first>
    - data/dbt/models/sources.yml (full file — 29 lines; preserve indentation, ordering, and trailing newline)
    - .planning/phases/086-port-remaining-transforms/086-PATTERNS.md (§`data/dbt/models/sources.yml` for the exact target patch)
    - .planning/phases/086-port-remaining-transforms/086-RESEARCH.md (§Pitfall 5 — checklist_data MUST be added before any staging model that references it)
  </read_first>
  <action>
    1. Append two table entries to the existing `inaturalist_data` source block (currently lines 10-14): `- name: canonical_to_taxon_id` and `- name: taxon_lineage_extended`. Add a same-line `# written by resolve_taxon_ids.py (ingestion)` comment on the first and `# written by inaturalist_pipeline.enrich_taxon_lineage_extended (ingestion)` on the second. Preserve 6-space indentation under `tables:` matching existing entries.

    2. Append a new top-level source block AFTER `inaturalist_waba_data` and BEFORE `geographies` (alphabetical-ish ordering, matches PATTERNS.md guidance):
    ```
      - name: checklist_data
        schema: checklist_data
        tables:
          - name: species
          - name: species_counties
    ```
    Both `species` AND `species_counties` are declared even though only `species` is needed in Phase 086 — per Pitfall 5 it is cheaper to declare both now than to amend the file again later.

    Do NOT reorder existing source blocks. Do NOT delete existing entries. Do NOT introduce a `description:` or `meta:` key — the existing pattern is bare `- name:` only.
  </action>
  <verify>
    <automated>grep -c "checklist_data" data/dbt/models/sources.yml | grep -q "^2$" &amp;&amp; grep -c "canonical_to_taxon_id" data/dbt/models/sources.yml | grep -q "^1$" &amp;&amp; grep -c "taxon_lineage_extended" data/dbt/models/sources.yml | grep -q "^1$"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "name: canonical_to_taxon_id" data/dbt/models/sources.yml` returns 1 line under the inaturalist_data block
    - `grep -n "name: taxon_lineage_extended" data/dbt/models/sources.yml` returns 1 line under the inaturalist_data block
    - `grep -n "name: checklist_data" data/dbt/models/sources.yml` returns 1 line (top-level source declaration)
    - `grep -n "name: species_counties" data/dbt/models/sources.yml` returns 1 line under checklist_data
    - YAML syntactically valid: `python3 -c "import yaml; yaml.safe_load(open('data/dbt/models/sources.yml'))"` exits 0
    - All 5 pre-existing source blocks (ecdysis_data, inaturalist_data, inaturalist_waba_data, geographies, NEW checklist_data) remain present
  </acceptance_criteria>
  <done>sources.yml declares all sources required by the species mart DAG; YAML still parses; existing source blocks unchanged.</done>
</task>

<task type="auto">
  <name>Task 2: Create three pass-through staging views and add their tests to staging/schema.yml</name>
  <files>data/dbt/models/staging/stg_inat__canonical_to_taxon_id.sql, data/dbt/models/staging/stg_inat__taxon_lineage_extended.sql, data/dbt/models/staging/stg_checklist__species.sql, data/dbt/models/staging/schema.yml</files>
  <read_first>
    - data/dbt/models/staging/stg_waba__observations.sql (the 10-line analog — copy structure exactly)
    - data/dbt/models/staging/schema.yml (full file — append, do not edit existing entries)
    - .planning/phases/086-port-remaining-transforms/086-PATTERNS.md (§`stg_inat__canonical_to_taxon_id.sql`, §`stg_inat__taxon_lineage_extended.sql`, §`stg_checklist__species.sql`, §`data/dbt/models/staging/schema.yml` — exact target snippets)
  </read_first>
  <action>
    Create three new files with content matching the targets in 086-PATTERNS.md (header comment + `{{ config(materialized='view') }}` + `SELECT * FROM {{ source(...) }}`).

    1. `data/dbt/models/staging/stg_inat__canonical_to_taxon_id.sql` — wraps `source('inaturalist_data', 'canonical_to_taxon_id')`. Header comment names the ingestion writer (resolve_taxon_ids.py), the columns (canonical_name PK, taxon_id, resolved_at, source), and downstream consumers (int_species_universe in Plan 086-04, test_lin05_lineage_coverage in this plan).

    2. `data/dbt/models/staging/stg_inat__taxon_lineage_extended.sql` — wraps `source('inaturalist_data', 'taxon_lineage_extended')`. Header names the ingestion writer (inaturalist_pipeline.enrich_taxon_lineage_extended), columns (taxon_id PK BIGINT, family/subfamily/tribe/genus/subgenus VARCHAR), and consumers.

    3. `data/dbt/models/staging/stg_checklist__species.sql` — wraps `source('checklist_data', 'species')`. Header names the table contents (authoritative bee species checklist) and consumers (int_species_universe FULL OUTER JOIN axis, test_lin05_lineage_coverage species_universe CTE).

    4. Append three model blocks to `data/dbt/models/staging/schema.yml` matching the PATTERNS.md target. Each block declares the primary key column with `data_tests: [not_null, unique]` for stg_inat__canonical_to_taxon_id (canonical_name) and stg_inat__taxon_lineage_extended (taxon_id), and `[not_null]` only for stg_checklist__species (canonical_name). Inline-comment the unique tests with VERIFIED counts: 735 / 2196 / (no count needed for not_null-only). Use 4-space indentation matching existing model blocks (lines 4-29).

    Do NOT add column aliases, filters, or computed columns at the staging layer — pure SELECT * (matches stg_waba__observations.sql convention; the only existing exception is stg_inat__observations which filters a tombstone, justified inline). Do NOT pluralize file names. Use snake_case underscores between source-domain and table-name as in existing files (`stg_inat__`, `stg_checklist__` — DOUBLE underscore separator).
  </action>
  <verify>
    <automated>test -f data/dbt/models/staging/stg_inat__canonical_to_taxon_id.sql &amp;&amp; test -f data/dbt/models/staging/stg_inat__taxon_lineage_extended.sql &amp;&amp; test -f data/dbt/models/staging/stg_checklist__species.sql &amp;&amp; python3 -c "import yaml; m = yaml.safe_load(open('data/dbt/models/staging/schema.yml')); names = [x['name'] for x in m['models']]; assert 'stg_inat__canonical_to_taxon_id' in names; assert 'stg_inat__taxon_lineage_extended' in names; assert 'stg_checklist__species' in names; print('schema.yml OK')"</automated>
  </verify>
  <acceptance_criteria>
    - All three new .sql files exist and each contains a single `{{ source(...) }}` reference matching the source name declared in Task 1
    - `grep -c "{{ config(materialized='view') }}" data/dbt/models/staging/stg_inat__canonical_to_taxon_id.sql` returns 1
    - Same check passes for stg_inat__taxon_lineage_extended.sql and stg_checklist__species.sql
    - schema.yml lists all three new models with their PK column and tests as specified
    - YAML still parses (`python3 -c "import yaml; yaml.safe_load(open('data/dbt/models/staging/schema.yml'))"` exits 0)
    - `bash data/dbt/run.sh parse` exits 0 (compilation succeeds — sources resolved, refs resolvable)
  </acceptance_criteria>
  <done>Three staging views compile; schema.yml validates; not_null/unique tests are queued for next dbt build.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Add LIN-05 singular dbt test asserting lineage coverage ≥ 0.95</name>
  <files>data/dbt/tests/test_lin05_lineage_coverage.sql</files>
  <read_first>
    - data/dbt/tests/test_ecdysis_id_references_source.sql (full file — copy header style and PASS=0 semantics exactly)
    - .planning/phases/086-port-remaining-transforms/086-PATTERNS.md (§`data/dbt/tests/test_lin05_lineage_coverage.sql` — full target SQL)
    - .planning/phases/086-port-remaining-transforms/086-RESEARCH.md (§PORT-03 + Pitfall 8 — MUST use `ref('stg_...')`, not `source()` directly)
    - data/beeatlas.duckdb — confirm `SELECT COUNT(*) FROM inaturalist_data.canonical_to_taxon_id` returns 735 and `SELECT COUNT(*) FROM inaturalist_data.taxon_lineage_extended` returns 2196 before running the test (RESEARCH §Environment Availability — guards against empty-source false failure on fresh checkouts).
  </read_first>
  <behavior>
    - PASS condition: query returns 0 rows (coverage ratio >= 0.95).
    - FAIL condition: query returns 1 row containing (total, resolved, ratio) when ratio < 0.95.
    - Verified live baseline: 735/735 = 100% coverage as of 2026-05-13 (PORT-03 research). Test MUST PASS on current data.
  </behavior>
  <action>
    Create `data/dbt/tests/test_lin05_lineage_coverage.sql` with the exact target SQL in 086-PATTERNS.md §`data/dbt/tests/test_lin05_lineage_coverage.sql`.

    Required contents:
    1. Rich header comment (matching test_ecdysis_id_references_source.sql style) covering: what the test enforces (LIN-05 ≥0.95 ratio), PASS=0 rows semantics, FAIL=1 row with (total, resolved, ratio), Pitfall 8 note about ref-not-source, and the VERIFIED 735/735 baseline.

    2. `WITH species_universe AS (...)` CTE: SELECT DISTINCT COALESCE(c.canonical_name, oa.canonical_name) FROM `{{ ref('stg_checklist__species') }} c` FULL OUTER JOIN `(SELECT DISTINCT canonical_name FROM {{ source('ecdysis_data', 'occurrences') }} WHERE canonical_name IS NOT NULL) oa` ON canonical_name equality. (This particular `source()` reference IS allowed because no staging filter applies to occurrences for this purpose; Pitfall 8's restriction is about the LINEAGE inputs which DO have staging wrappers.)

    3. `coverage AS (...)` CTE: SELECT COUNT(*) AS total, SUM(CASE WHEN tle.taxon_id IS NOT NULL THEN 1 ELSE 0 END) AS resolved, joining species_universe to `{{ ref('stg_inat__canonical_to_taxon_id') }} b` LEFT JOIN `{{ ref('stg_inat__taxon_lineage_extended') }} tle` on the bridge.

    4. Final SELECT: `SELECT total, resolved, CAST(resolved AS DOUBLE) / NULLIF(total, 0) AS ratio FROM coverage WHERE CAST(resolved AS DOUBLE) / NULLIF(total, 0) < 0.95`.

    Do NOT register this as a generic test in any schema.yml — it lives in `data/dbt/tests/` as a singular test (mirrors test_ecdysis_id_references_source.sql). Do NOT use `source('inaturalist_data', 'canonical_to_taxon_id')` or `source('inaturalist_data', 'taxon_lineage_extended')` — must reference via `ref('stg_inat__...')` per Pitfall 8.
  </action>
  <verify>
    <automated>test -f data/dbt/tests/test_lin05_lineage_coverage.sql &amp;&amp; grep -c "ref('stg_inat__canonical_to_taxon_id')" data/dbt/tests/test_lin05_lineage_coverage.sql | grep -q "^1$" &amp;&amp; grep -c "ref('stg_inat__taxon_lineage_extended')" data/dbt/tests/test_lin05_lineage_coverage.sql | grep -q "^1$" &amp;&amp; bash data/dbt/run.sh test --select test_lin05_lineage_coverage 2>&amp;1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - File exists at `data/dbt/tests/test_lin05_lineage_coverage.sql`
    - File grep -v '^--' | grep -c "source('inaturalist_data'" returns 0 (no direct source() to lineage inputs in non-comment lines)
    - File grep -v '^--' | grep -c "ref('stg_inat__canonical_to_taxon_id')" returns 1 (in active SQL)
    - File grep -v '^--' | grep -c "ref('stg_inat__taxon_lineage_extended')" returns 1 (in active SQL)
    - File grep -c "0.95" returns &gt;= 1 (the threshold appears in the WHERE clause)
    - `bash data/dbt/run.sh test --select test_lin05_lineage_coverage` exits 0 with "1 of 1 PASS" (test passes against live data; 735/735 = 100% > 0.95)
    - `bash data/dbt/run.sh build` exits 0 with PASS count >= 39 (33 prior + 3 not_null + 2 unique + 1 LIN-05; exact count may vary if any not_null applies to nullable column — see Task 2 schema.yml entries), WARN=0, ERROR=0
  </acceptance_criteria>
  <done>LIN-05 singular test created and passing; full `dbt build` exits 0; staging views materialized.</done>
</task>

</tasks>

<verification>
- Full `bash data/dbt/run.sh build` exits 0; tail of output shows PASS >= 39, ERROR=0, WARN=0.
- `uv run --project data pytest data/tests/test_dbt_diff.py -x` continues to exit 0 (10 PASS + 5 SKIP from Plan 086-01).
- `git diff --stat` shows 6 files: 1 modified yaml (sources.yml), 1 modified yaml (staging/schema.yml), 3 new staging .sql, 1 new test .sql. No mart, no intermediate, no Python file touched.
- `python3 -c "import yaml; yaml.safe_load(open('data/dbt/models/sources.yml')); yaml.safe_load(open('data/dbt/models/staging/schema.yml'))"` exits 0.
</verification>

<success_criteria>
1. `dbt build` runs the LIN-05 singular test and it PASSES against live data.
2. Three new staging views (canonical_to_taxon_id, taxon_lineage_extended, checklist__species) compile and materialize as views in `dbt_sandbox`.
3. `sources.yml` declares all sources Plan 086-04 will need for `int_species_universe`.
4. The diff harness (Plan 086-01 tests + 10 prior tests) continues to exit 0 — this plan does not produce sandbox/species.parquet, so the new species tests still SKIP, which is the intended state at the end of Wave 1.
5. PORT-03 requirement is closed: lineage enrichment is consumable from dbt via source declarations + staging wrappers, and the ≥0.95 coverage assertion runs on every build.
</success_criteria>

<output>
After completion, create `.planning/phases/086-port-remaining-transforms/086-02-SUMMARY.md` covering: source declarations added, staging views created, LIN-05 baseline result (e.g., 735/735 = 100%), and the unblocked dependencies (Plan 086-04 can now reference all three staging models).
</output>
