---
phase: "086"
plan: "05"
type: execute
wave: 3
depends_on:
  - "086-01"
  - "086-04"
files_modified:
  - data/species_export.py
autonomous: true
requirements:
  - PORT-01
  - VALIDATE-01
must_haves:
  truths:
    - "After running `bash data/dbt/run.sh build` followed by `uv run --project data python data/species_export.py`, three sandbox artifacts exist: target/sandbox/species.parquet (19 cols incl. slug), target/sandbox/species.json, target/sandbox/seasonality.json"
    - "All 5 species diff tests in test_dbt_diff.py PASS: row_count, schema (NOW 19 cols matching public), canonical_name_key_set, species_json byte-comparable, seasonality_json byte-comparable"
    - "species_export.py no longer issues the 200+ line multi-CTE SQL — that aggregation lives in dbt; species_export.py only reads the dbt mart parquet, computes slug via feeds._slugify, writes the 3 sandbox artifacts, and (when run in production mode) overwrites public/data/ targets"
    - "data/run.py STEPS list still calls export_species_parquet (no orchestration change in this plan — the function's INTERNALS change, not its boundary)"
  artifacts:
    - path: "data/species_export.py"
      provides: "Reads sandbox/species.parquet (from dbt), adds slug via feeds._slugify, emits sandbox/species.parquet (overwritten with 19 cols), sandbox/species.json (json.dumps sort_keys=True indent=2), sandbox/seasonality.json (json.dumps sort_keys=True separators=(',',':'))"
      contains: "_slugify"
  key_links:
    - from: "species_export.py"
      to: "data/dbt/target/sandbox/species.parquet"
      via: "pyarrow.parquet.read_table → pyarrow.Table.append_column or rebuild"
      pattern: "target/sandbox/species\\.parquet"
    - from: "species_export.py"
      to: "data/dbt/target/sandbox/occurrences.parquet"
      via: "duckdb.read_parquet for seasonality bucket accumulation (NOT public/data/occurrences.parquet — Pitfall 4)"
      pattern: "sandbox.*occurrences\\.parquet"
    - from: "species_export.py"
      to: "feeds._slugify"
      via: "from feeds import _slugify"
      pattern: "from feeds import _slugify"
---

<objective>
PORT-01 (JSON sidecar half) — close the species mart port by replacing `species_export.py`'s SQL aggregation with a thin Python post-step that reads the sandbox/species.parquet (produced by Plan 086-04), appends the `slug` column via `feeds._slugify`, and writes the three artifacts (`species.parquet`, `species.json`, `seasonality.json`) byte-comparable to `public/data/`. After this plan, all 5 species diff tests PASS and PORT-01 is fully closed.

Purpose: RESEARCH §PORT-01 and §Notes/Surprise 1 establish that `slug` (uses `unicodedata.normalize('NFKD')`) and the sort_keys/indent/separators JSON formatting cannot be byte-replicated in SQL. The Python post-step is the canonical home for these. By keeping `species_export.py` as the entry point (no change to data/run.py STEPS), the Phase 88 cutover scope shrinks: only `data/export.py` needs to be retired (replaced by `dbt build`); `species_export.py` evolves in place from "monolith CTE" to "dbt mart consumer + JSON emitter".

Output: `data/species_export.py` rewritten. The function `export_species_parquet(con)` keeps its signature and call site, but its body changes from "issue multi-CTE SQL against ecdysis_data + checklist_data + inaturalist_data + read_parquet(public)" to "read sandbox/species.parquet (from dbt), enrich with slug, write 3 artifacts". Subsequent `dbt build` + `species_export.py` invocation produces 3 sandbox artifacts that all pass the diff harness.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/086-port-remaining-transforms/086-RESEARCH.md
@.planning/phases/086-port-remaining-transforms/086-PATTERNS.md
@.planning/phases/086-port-remaining-transforms/086-04-PLAN.md
@./CLAUDE.md

@data/species_export.py
@data/feeds.py
@data/run.py
@data/tests/test_dbt_diff.py
</context>

<interfaces>
Inputs from Plan 086-04 (must exist before this plan executes):
- data/dbt/target/sandbox/species.parquet — 18 columns (no slug), 629 rows, contract-enforced types per marts/schema.yml
- data/dbt/target/sandbox/occurrences.parquet — 30 columns from Phase 085-04 contract; the source for seasonality bucket accumulation

Helper to import (existing, do NOT modify):
- from feeds import _slugify  — feeds.py:132-148. Signature: `_slugify(value: str) -> str`. Uses `unicodedata.normalize('NFKD')` + `.encode('ascii','ignore')` + lowercase + 3 regex passes. Returns 'unknown' for empty input. The byte-for-byte invariant: the Python species_export.py at HEAD computes slug via this helper; the rewrite MUST keep using it (Pitfall: do not reinvent slugify).

Outputs (this plan writes them):
- data/dbt/target/sandbox/species.parquet — overwritten with 19 columns (slug appended)
- data/dbt/target/sandbox/species.json — written via `json.dumps(sort_keys=True, indent=2)`
- data/dbt/target/sandbox/seasonality.json — written via `json.dumps(sort_keys=True, separators=(',',':'))`

When ASSETS_DIR / EXPORT_DIR points at the sandbox (test/diff context), all 3 artifacts land in target/sandbox/. When run in production (default ASSETS_DIR = public/data/), the same code writes to public/data/ — the executor MUST make this distinction work via the existing EXPORT_DIR env var pattern (see species_export.py lines 28-30).

Verified production baselines (must remain stable):
- species.parquet: 629 rows, 19 cols (incl. slug)
- species.json: 629 rows, ~150KB, json.dumps(sort_keys=True, indent=2)
- seasonality.json: 556 species, < 6MB, json.dumps(sort_keys=True, separators=(',',':'))

Existing serialization patterns to PRESERVE EXACTLY (PATTERNS.md "Python JSON serialization — byte-comparable output"):
- _jsonify_rows helper (species_export.py:54-73) — converts datetime.date → .isoformat() string; pass through other types
- json.dumps(_jsonify_rows(species_rows), sort_keys=True, indent=2)  for species.json
- json.dumps(out_seas, sort_keys=True, separators=(',', ':'))  for seasonality.json (where out_seas is the doubly-sorted dict)
- 6 MB budget assertion on seasonality.json
- _ZERO_HIST = [0] * 12 for any month_histogram NULL fallback (now redundant since dbt CASE backfills, but defensive — keep)
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite species_export.export_species_parquet to read the dbt sandbox mart and emit the three artifacts</name>
  <files>data/species_export.py</files>
  <read_first>
    - data/species_export.py (full file, 333 lines — must understand the existing function signature `export_species_parquet(con: duckdb.DuckDBPyConnection)`, the EXPORT_DIR/ASSETS_DIR env-var pattern at lines 28-30, the _jsonify_rows helper at lines 54-73, the SPECIES_COLUMNS list at lines 45-51, the seasonality accumulation pattern at lines 286-317, the assertions at lines 266-267 and 315-317)
    - data/feeds.py (lines 132-148 — the _slugify function the post-step MUST use; do NOT reimplement)
    - data/run.py (lines 30-55 — confirm STEPS list calls `export_species_parquet`; this plan does NOT change run.py)
    - .planning/phases/086-port-remaining-transforms/086-RESEARCH.md (Pitfall 3: json.dumps key ordering non-negotiable; Pitfall 4: seasonality.json reads occurrences.parquet, NOT species.parquet; the seasonality source must be the SANDBOX occurrences.parquet not public/data — to keep diff comparison clean)
    - .planning/phases/086-port-remaining-transforms/086-PATTERNS.md ("Python JSON serialization — byte-comparable output")
    - data/tests/test_dbt_diff.py (the 5 species tests added in Plan 086-01 — confirm exact filenames and locations the post-step must produce)
  </read_first>
  <behavior>
    - When EXPORT_DIR is unset (production), writes to public/data/ as today.
    - When EXPORT_DIR points at data/dbt/target/sandbox/, writes the 3 artifacts there for the diff harness.
    - SQL aggregation (the 200+ line multi-CTE between species_export.py:115-208) is GONE; replaced by `con.execute("SELECT * FROM read_parquet(...) ORDER BY canonical_name").fetchall()` against the dbt sandbox parquet.
    - Slug is computed in Python via `feeds._slugify(scientificName)` for every row.
    - month_histogram NULL backfill is a no-op (dbt CASE handles it now); the `_ZERO_HIST` defensive code stays in case any row slips through with NULL.
    - Output parquet has all 19 columns in SPECIES_COLUMNS order; pyarrow schema preserved verbatim.
    - species.json: json.dumps(_jsonify_rows(species_rows), sort_keys=True, indent=2)
    - seasonality.json: json.dumps(out_seas, sort_keys=True, separators=(',', ':')); reads from `${EXPORT_DIR}/occurrences.parquet` (sandbox path when EXPORT_DIR is sandbox; public path when not).
    - All existing print() messages and size assertions preserved.
  </behavior>
  <action>
    Rewrite the body of `export_species_parquet(con)` in `data/species_export.py`. KEEP the function signature, the module-level constants (DB_PATH, ASSETS_DIR, BEE_FAMILIES, SPECIES_COLUMNS, _ZERO_HIST), the `_jsonify_rows` helper, and `main()`. CHANGE the function body.

    Required new structure of `export_species_parquet`:

    1. Resolve the path to the dbt-produced sandbox parquet. Add a module-level constant near ASSETS_DIR:
       ```
       DBT_SANDBOX_DIR = Path(os.environ.get(
           'DBT_SANDBOX_DIR',
           str(Path(__file__).parent / 'dbt' / 'target' / 'sandbox'),
       ))
       ```
       This points at `data/dbt/target/sandbox/` by default, overridable via env var.

    2. Inside `export_species_parquet`, replace lines 88-208 (the file-existence check for occurrences.parquet AND the entire multi-CTE SQL) with:
       - Resolve `species_parquet_in = DBT_SANDBOX_DIR / 'species.parquet'`. If missing, raise FileNotFoundError with message instructing the user to run `bash data/dbt/run.sh build` first.
       - Resolve `occurrences_parquet_in = DBT_SANDBOX_DIR / 'occurrences.parquet'`. Same missing-file behavior.
       - Read the dbt mart: `species_rows = con.execute(f"SELECT {', '.join(SPECIES_COLUMNS[:-1])} FROM read_parquet('{species_parquet_in}') ORDER BY canonical_name").fetchall()`. Note: `SPECIES_COLUMNS[:-1]` excludes 'slug' (the dbt mart has 18 cols). Convert rows to list-of-dict using cursor description, mirroring the existing fetched_cols pattern at species_export.py:211-212.

    3. Add slug + defensive month_histogram backfill (mirrors species_export.py:218-222 unchanged):
       ```
       for r in species_rows:
           r['slug'] = _slugify(r['scientificName'])
           if r.get('month_histogram') is None:
               r['month_histogram'] = list(_ZERO_HIST)
       ```

    4. Build the parquet via pyarrow with the existing 19-col schema (species_export.py:227-256 — KEEP THIS BLOCK VERBATIM). Write to `ASSETS_DIR / 'species.parquet'` as before. The verification block at lines 259-267 also stays.

    5. Write species.json: `json.dumps(_jsonify_rows(species_rows), sort_keys=True, indent=2)` to `ASSETS_DIR / 'species.json'` (KEEP species_export.py:273-281 verbatim).

    6. Build seasonality from the SANDBOX occurrences.parquet (Pitfall 4 — must be sandbox, not public, to keep diff comparison clean):
       - Replace species_export.py:289-295 read of `${occurrences_parquet}` (currently from ASSETS_DIR which IS public/data/) with read of `occurrences_parquet_in` (which is DBT_SANDBOX_DIR / 'occurrences.parquet'). Same SELECT shape: `SELECT canonical_name, county, ecoregion_l3, TRY_CAST(month AS INT) - 1 AS m_idx FROM read_parquet('{occurrences_parquet_in}') WHERE canonical_name IS NOT NULL AND month IS NOT NULL`.
       - The accumulation loop (species_export.py:296-307) and the final json.dumps + size assertion (308-317) stay UNCHANGED.

    7. Update the module docstring (species_export.py:1-14) to reflect the new flow: "Reads dbt-produced sandbox/species.parquet and sandbox/occurrences.parquet, adds slug via feeds._slugify, emits species.parquet (19 cols), species.json, seasonality.json. Run AFTER `bash data/dbt/run.sh build`."

    Do NOT change the function name, the call signature, or the run.py STEPS list (Phase 88 will revisit orchestration). Do NOT change SPECIES_COLUMNS, _jsonify_rows, _ZERO_HIST, BEE_FAMILIES, or the pyarrow schema. Do NOT remove the FileNotFoundError pattern (Pitfall 8 from species_export.py:90-92 style — explicit failure when prerequisite parquet missing). Do NOT change ASSETS_DIR semantics — production callers (data/run.py with no env override) still write to public/data/; the diff harness sets EXPORT_DIR=data/dbt/target/sandbox so the same function writes the 3 artifacts to the sandbox for byte comparison.
  </action>
  <verify>
    <automated>python3 -m py_compile data/species_export.py &amp;&amp; grep -q "DBT_SANDBOX_DIR" data/species_export.py &amp;&amp; grep -q "from feeds import _slugify" data/species_export.py &amp;&amp; ! grep -E "FROM[[:space:]]+ecdysis_data\\.occurrences|FROM[[:space:]]+checklist_data\\.species|FROM[[:space:]]+inaturalist_data" data/species_export.py &amp;&amp; bash data/dbt/run.sh build 2>&amp;1 | tail -5 &amp;&amp; EXPORT_DIR=data/dbt/target/sandbox uv run --project data python data/species_export.py 2>&amp;1 | tail -10 &amp;&amp; uv run --project data pytest data/tests/test_dbt_diff.py -x 2>&amp;1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - `data/species_export.py` syntactically valid (`python3 -m py_compile` exits 0)
    - File contains `DBT_SANDBOX_DIR` constant
    - File contains `from feeds import _slugify` (unchanged from current)
    - File body (excluding comments and docstrings) does NOT contain `FROM ecdysis_data.occurrences`, `FROM checklist_data.species`, or `FROM inaturalist_data.canonical_to_taxon_id`/`taxon_lineage_extended` — the SQL aggregation is fully removed (verify with grep above)
    - File body still contains `json.dumps(` with `sort_keys=True, indent=2` for species.json and `sort_keys=True, separators=(',', ':')` for seasonality.json (Pitfall 3 — byte-comparable invariant)
    - `bash data/dbt/run.sh build` exits 0 (sandbox/species.parquet at 18 cols, sandbox/occurrences.parquet at 30 cols)
    - `EXPORT_DIR=data/dbt/target/sandbox uv run --project data python data/species_export.py` exits 0; produces sandbox/species.parquet (19 cols), sandbox/species.json, sandbox/seasonality.json
    - `DESCRIBE SELECT * FROM read_parquet('data/dbt/target/sandbox/species.parquet')` returns exactly 19 column rows including slug
    - `uv run --project data pytest data/tests/test_dbt_diff.py -x` exits 0 with 5 NEW species tests all PASS (no SKIP), plus the 10 prior tests in their existing state. Total: 15 tests, all green.
    - Specifically: `test_species_parquet_schema_matches`, `test_species_json_matches`, `test_seasonality_json_matches` all PASS — closing the gaps documented in Plan 086-04
  </acceptance_criteria>
  <done>species_export.py rewritten as dbt-mart consumer + JSON emitter; full diff harness green; PORT-01 fully closed.</done>
</task>

</tasks>

<verification>
- `python3 -m py_compile data/species_export.py` exits 0.
- `bash data/dbt/run.sh build` exits 0; sandbox/species.parquet has 18 cols.
- `EXPORT_DIR=data/dbt/target/sandbox uv run --project data python data/species_export.py` exits 0; sandbox/species.parquet now has 19 cols (slug appended); sandbox/species.json and sandbox/seasonality.json exist.
- `uv run --project data pytest data/tests/test_dbt_diff.py -x` exits 0; ALL 5 species diff tests PASS (no SKIP, no FAIL); other tests in their pre-plan state.
- `git diff --stat` shows exactly 1 file changed: `data/species_export.py`.
- The 200+ line multi-CTE SQL is gone from species_export.py; the dbt DAG owns the aggregation.
</verification>

<success_criteria>
1. PORT-01 is fully closed: dbt models produce sandbox/species.parquet (18 cols), Python post-step adds slug to reach 19 cols and emits species.json + seasonality.json byte-comparable to public/data/ baselines.
2. All 5 species diff tests added in Plan 086-01 PASS (no SKIP).
3. The Python file no longer issues SQL against `ecdysis_data.occurrences`, `checklist_data.species`, or `inaturalist_data.*` — the entire transform is in dbt.
4. Phase 88 cutover scope is now precisely scoped to: replace `export.py` with `dbt build` invocation in run.py, retire `_apply_migrations`, retire `validate-schema.mjs`. species_export.py stays as a thin Python post-step.
</success_criteria>

<output>
After completion, create `.planning/phases/086-port-remaining-transforms/086-05-SUMMARY.md` covering: rewritten species_export.py line count delta, all 5 species diff tests now PASS, dbt build PASS count after this plan, and a Phase 86 roll-up confirming all 5 requirements (PORT-01, PORT-02, PORT-03, PORT-04, VALIDATE-01) closed.
</output>
