---
phase: 24-tech-debt-audit
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [.planning/PROJECT.md]
autonomous: true
requirements: [DEBT-01]

must_haves:
  truths:
    - "Every existing tech debt item in PROJECT.md has an explicit disposition"
    - "Items resolved by dlt migration (Phases 20-23) are removed from the list with rationale in the commit message"
    - "Surviving items have updated descriptions reflecting the new dlt-based architecture"
    - "Newly discovered debt items from the dlt migration are added to the list"
  artifacts:
    - path: ".planning/PROJECT.md"
      provides: "Updated Known tech debt section"
      contains: "Known tech debt"
  key_links: []
---

<objective>
Audit all seven known tech debt items in PROJECT.md against the new dlt-based architecture delivered by Phases 20-23. Assign each item a disposition (closed/updated/carried forward), update PROJECT.md in-place, and surface any new debt introduced by the migration.

Purpose: Close out the v1.6 milestone by ensuring the tech debt register reflects reality after the dlt pipeline migration.
Output: Updated PROJECT.md with current tech debt section.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/20-pipeline-migration/20-01-SUMMARY.md
@.planning/phases/21-parquet-and-geojson-export/21-01-SUMMARY.md
@.planning/phases/22-orchestration/22-01-SUMMARY.md
@.planning/phases/23-frontend-simplification/23-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Audit existing tech debt and update PROJECT.md</name>
  <files>.planning/PROJECT.md</files>
  <read_first>
    - .planning/PROJECT.md (current tech debt section -- the seven items to audit)
    - .planning/phases/20-pipeline-migration/20-01-SUMMARY.md (what was deleted/replaced)
    - .planning/phases/21-parquet-and-geojson-export/21-01-SUMMARY.md (export layer changes)
    - .planning/phases/22-orchestration/22-01-SUMMARY.md (build-data.sh replacement)
    - .planning/phases/23-frontend-simplification/23-01-SUMMARY.md (links.parquet removal)
    - data/inaturalist_pipeline.py (check if DEFAULT_FIELDS addresses iNat explicit fields debt)
    - data/ecdysis_pipeline.py (check if field_id=8338 is used for ofv matching)
    - data/export.py (check how specimen_count is sourced -- field_id vs name)
    - data/geographies_pipeline.py (check CRS handling for ecoregion shapefile)
  </read_first>
  <action>
Audit each of the seven existing tech debt items per D-01, D-02, D-03, D-07, D-08. For each item, determine disposition by cross-referencing the pipeline code and phase summaries. Then rewrite the "Known tech debt" section of PROJECT.md in-place.

**Item-by-item dispositions to verify and apply:**

1. "CI build runs `npm run build` which calls `build-data.sh` -- makes live HTTP POST to ecdysis.org and live iNat API calls on every push..."
   - CLOSE (per D-07). Phase 22 deleted `build-data.sh` and replaced `build:data` with `cd data && uv run python run.py`. The live-API-call-on-every-push concern is structurally changed: dlt pipelines use incremental loading with local DuckDB state, and CI integration is deferred (INFRA-06/07/08). The `frontend/src/assets/ecdysis.parquet` fallback concern is also obsolete -- parquet files are now gitignored build artifacts generated by export.py.
   - Commit message should note: "Closed: build-data.sh replaced by data/run.py (Phase 22); CI integration deferred to INFRA-06/07/08"

2. "`speicmenLayer` typo in `bee-map.ts`"
   - CARRY FORWARD unchanged. Explicitly deferred in 24-CONTEXT.md deferred section. Keep the existing description.

3. "Phase 1 SUMMARY references `--db` flag; actual CLI flag is `--datasetid`"
   - CLOSE (per D-07). The old ecdysis pipeline module that used `--datasetid` was deleted in Phase 20. Phase 1 SUMMARY is historical documentation of a now-removed system -- the flag discrepancy is moot.
   - Commit message should note: "Closed: old ecdysis module deleted in Phase 20; Phase 1 SUMMARY is historical"

4. "Match iNat ofvs by field_id=8338 (not name)"
   - Read `data/inaturalist_pipeline.py` and `data/export.py` to confirm field_id=8338 is used. If the dlt pipeline uses field_id=8338: CLOSE with rationale that dlt pipeline correctly uses field_id. If it uses field name: CARRY FORWARD with updated description noting it still needs fixing in the dlt pipeline.
   - Commit message should note disposition accordingly.

5. "observations.ndjson cache stores full observation JSON with download timestamp"
   - CLOSE (per D-07). The old inat/ module that produced observations.ndjson was deleted in Phase 20. dlt uses its own incremental state management (DuckDB _dlt_loads table + incremental cursors), not an ndjson cache.
   - Commit message should note: "Closed: old inat/ module and observations.ndjson deleted in Phase 20; dlt uses incremental cursors"

6. "EPA L3 ecoregion CRS risk: shapefile uses non-EPSG spherical Lambert AEA"
   - Read `data/geographies_pipeline.py` to check CRS handling. If CRS conversion is handled in the dlt pipeline: UPDATE description to note it's now handled in geographies_pipeline.py but remains a concern for any future shapefile ingestion outside that pipeline. If not handled: CARRY FORWARD with same description.

7. "iNat API calls don't specify explicit fields"
   - Read `data/inaturalist_pipeline.py` to confirm DEFAULT_FIELDS is used (per 20-01-SUMMARY: "DEFAULT_FIELDS for explicit field selection"). If confirmed: CLOSE. The dlt pipeline specifies explicit fields via DEFAULT_FIELDS parameter.
   - Also close the corresponding pending todo in STATE.md (todo #1: "Specify explicit fields on iNat API calls") if this item is closed.
   - Commit message should note: "Closed: dlt inaturalist_pipeline.py uses DEFAULT_FIELDS for explicit field selection (Phase 20)"

**New debt discovery (per D-05, D-06, D-08):**
After reading the dlt pipeline code files listed in read_first, identify any obvious new debt items. Common candidates:
- Error handling gaps in pipeline code (dlt pipelines were copied verbatim from prototype per 20-01-SUMMARY)
- Missing test coverage for dlt pipelines (data/tests/ was deleted in Phase 20)
- CI integration gaps (no automated pipeline runs yet -- but this is explicitly deferred, so note only if there's a concrete risk)
- Any hardcoded values that should be in .dlt/config.toml

Add new items to the "Known tech debt" section using the same bullet-point format as existing items (per D-06).

**Final section format:**
Replace the entire "Known tech debt:" section content. Keep surviving and new items as bullet points. Do NOT include closed items in the final list (per D-02 -- remove resolved items; rationale captured in commit message).

Update the "Last updated" line at the bottom of PROJECT.md to reference Phase 24.
  </action>
  <verify>
    <automated>grep -c "Known tech debt" .planning/PROJECT.md && grep "speicmenLayer" .planning/PROJECT.md && grep "Last updated.*Phase 24" .planning/PROJECT.md</automated>
  </verify>
  <acceptance_criteria>
    - .planning/PROJECT.md contains "Known tech debt:" section header
    - .planning/PROJECT.md contains "speicmenLayer" (carried forward item)
    - .planning/PROJECT.md does NOT contain "build-data.sh" in the tech debt section (closed item removed)
    - .planning/PROJECT.md does NOT contain "observations.ndjson" in the tech debt section (closed item removed)
    - .planning/PROJECT.md does NOT contain "Phase 1 SUMMARY references" in the tech debt section (closed item removed)
    - .planning/PROJECT.md "Last updated" line references "Phase 24"
    - The git commit message lists all closed items and their rationale
  </acceptance_criteria>
  <done>PROJECT.md "Known tech debt" section reflects current state: resolved items removed, surviving items have accurate descriptions for the dlt architecture, any new debt from the migration is documented</done>
</task>

</tasks>

<verification>
- grep for "Known tech debt" in PROJECT.md confirms section exists
- grep for closed items (build-data.sh, observations.ndjson, "Phase 1 SUMMARY") in the tech debt section confirms they are removed
- grep for carried-forward items (speicmenLayer) confirms they remain
- "Last updated" line references Phase 24
</verification>

<success_criteria>
- Every one of the seven original tech debt items has a clear disposition (visible in the commit message for closed items, visible in the updated section for carried/updated items)
- No closed items remain in the tech debt section
- Surviving items have descriptions accurate for the dlt-based architecture
- Any new debt from the migration is documented
</success_criteria>

<output>
After completion, create `.planning/phases/24-tech-debt-audit/24-01-SUMMARY.md`
</output>
