---
phase: 94
plan: 03
type: execute
wave: 3
depends_on: [01, 02]
files_modified:
  - src/tests/build-output.test.ts
autonomous: false
requirements:
  - URL-01
  - URL-02
  - SPE-01
  - SPE-02
  - SPE-03
  - SPE-04
  - GEN-01
  - GEN-02
  - GEN-03
  - PIPE-01
must_haves:
  truths:
    - "`npm run build` succeeds end-to-end with the new templates and data layer in place"
    - "`_site/species/Agapostemon/femoratus/index.html` exists, contains `<em>Agapostemon femoratus</em>`, references `/data/species-maps/Agapostemon/femoratus.svg`, contains a `<seasonality-viz` tag, contains the literal copy `View 91 occurrences on the atlas`, and every `<img>` tag carries `loading=\"lazy\"`"
    - "`_site/species/Agapostemon/index.html` exists, contains `<em>Agapostemon</em>`, references `/data/species-maps/genus/Agapostemon.svg`, contains a `class=\"species-list\"` block with at least one swatch using `background: #d92626` (the first alphabetical species per D-01)"
    - "Eleventy produces ~527 species pages and ~42 genus pages (one per entry in speciesList / genusList)"
    - "Vite emits a distinct `taxon-page` JS chunk under `_site/assets/` (flat `taxon-page-*.js` or nested `_site/assets/taxon-page/index-*.js`)"
    - "Human reviewer confirms the rendered species page shows photo (or grey 'No photo available' placeholder), the per-species SVG map, the seasonality bar chart, and the genus page shows colored swatches matching the SVG dot colors (Phase 93 D-02 cross-check)"
  artifacts:
    - path: "src/tests/build-output.test.ts"
      provides: "Vitest assertions that the taxon pages exist post-build and that a taxon-page chunk is emitted"
      contains: "taxon-page"
    - path: "_site/species/Agapostemon/femoratus/index.html"
      provides: "Generated species page (build artifact, asserted by test)"
      contains: "Agapostemon femoratus"
    - path: "_site/species/Agapostemon/index.html"
      provides: "Generated genus page (build artifact, asserted by test)"
      contains: "species-list"
  key_links:
    - from: "src/tests/build-output.test.ts"
      to: "_site/species/{Genus}/{epithet}/index.html"
      via: "readFileSync + grep-style assertions inside describe.skipIf(SKIP_BUILD)"
      pattern: "_site/species/Agapostemon/femoratus/index.html"
    - from: "human reviewer"
      to: "running dev server (`npm run dev`)"
      via: "browser navigation to /species/Andrena/milwaukeensis/ and /species/Andrena/"
      pattern: "checkpoint:human-verify"
---

<objective>
Run the production build with Plan 01's data layer and Plan 02's templates in place, then prove via automated assertions and a single human-verification checkpoint that all 10 of Phase 94's requirements are satisfied end-to-end.

Purpose: Goal-backward verification. Plans 01 and 02 produce code; Plan 03 proves the user-facing truths actually hold (`/species/{Genus}/{epithet}/` and `/species/{Genus}/` resolve to the right HTML with the right content, swatches, and chart). The human checkpoint covers the three behaviors flagged as manual-only in `94-VALIDATION.md` (photo render, seasonality chart in browser, swatch ↔ SVG dot color cross-check).

Output: One file modified (`src/tests/build-output.test.ts` extended with taxon-page assertions). The `_site/` directory is regenerated as a side effect of the build run. No code changes to templates or data.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/phases/94-species-genus-pages/94-UI-SPEC.md
@.planning/phases/94-species-genus-pages/94-VALIDATION.md
@.planning/phases/94-species-genus-pages/94-RESEARCH.md
@.planning/phases/94-species-genus-pages/94-PATTERNS.md
@.planning/phases/94-species-genus-pages/94-01-PLAN.md
@.planning/phases/94-species-genus-pages/94-02-PLAN.md
@src/tests/build-output.test.ts
@./CLAUDE.md

<interfaces>
<!-- Existing build-output test patterns to follow -->

The existing `describe.skipIf(SKIP_BUILD)('build output (PAGE-07, PAGE-09)', ...)`
block already runs `execSync('npm run build', ...)` in `beforeAll(180_000)`.
Add new `test(...)` blocks inside the SAME describe so the build runs only once.

Existing helper `findSpeciesChunk()` (lines 38-48): looks under
`_site/assets/species-*.js` (flat) OR `_site/assets/species/*.js` (nested).
Mirror this exactly for `findTaxonChunk()`.

Existing imports already cover everything needed:
  import { describe, test, expect, beforeAll } from 'vitest';
  import { execSync } from 'node:child_process';
  import { readFileSync, readdirSync } from 'node:fs';
  import { resolve, dirname } from 'node:path';
  import { fileURLToPath } from 'node:url';

ROOT and SKIP_BUILD constants already defined at the top of the file.

<!-- Build output paths (verified by 94-RESEARCH.md "Architecture diagram") -->
_site/species/{Genus}/{epithet}/index.html  e.g. _site/species/Agapostemon/femoratus/index.html
_site/species/{Genus}/index.html            e.g. _site/species/Agapostemon/index.html
_site/assets/taxon-page-*.js                OR _site/assets/taxon-page/*.js
public/data/species-maps/{Genus}/{epithet}.svg  (verified on disk: Agapostemon/femoratus.svg exists)
public/data/species-maps/genus/{Genus}.svg      (verified on disk: genus/Agapostemon.svg exists)

<!-- Verified anchor data for Agapostemon femoratus (from 94-RESEARCH.md) -->
species.json: { scientificName: "Agapostemon femoratus", occurrence_count: 91,
                county_count: 10, ecoregion_count: 3, slug: "Agapostemon/femoratus" }
First species alphabetically in Agapostemon by canonical_name: "agapostemon angelicus"
  → hexColor = #d92626 (hue=0, from Plan 01)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend src/tests/build-output.test.ts with taxon-page assertions and run the build</name>
  <files>src/tests/build-output.test.ts</files>
  <read_first>
    - src/tests/build-output.test.ts (full file — extend the existing describe block; do NOT add a second beforeAll or a second `npm run build` call)
    - .planning/phases/94-species-genus-pages/94-PATTERNS.md (`src/tests/build-output.test.ts` section — copy the three new `test(...)` blocks and the `findTaxonChunk` helper)
    - .planning/phases/94-species-genus-pages/94-RESEARCH.md (Validation Architecture → Phase Requirements → Test Map; Common Pitfalls Pitfall 6 — taxon-page entry split)
    - .planning/phases/94-species-genus-pages/94-VALIDATION.md (Per-Task Verification Map — confirms which behaviors are automated vs manual)
    - .planning/phases/94-species-genus-pages/94-01-PLAN.md and 94-02-PLAN.md (so the executor knows what content the templates promise)
  </read_first>
  <behavior>
    Add five new `test(...)` blocks inside the existing `describe.skipIf(SKIP_BUILD)('build output (PAGE-07, PAGE-09)', () => { ... })` block:

    1. "emits _site/species/Agapostemon/femoratus/index.html (SPE-01, URL-01, PIPE-01)"
       - reads the file; asserts `toContain('<em>Agapostemon femoratus</em>')`, `toContain('<seasonality-viz')`, `toContain('/data/species-maps/Agapostemon/femoratus.svg')`, `toContain('View 91 occurrences on the atlas')` (or, defensively, `toMatch(/View \\d+ occurrences on the atlas/)`).
    2. "every <img> on a species page has loading=\"lazy\" (PAGE-07 carry-forward, SPE-02/SPE-03)"
       - reads `_site/species/Agapostemon/femoratus/index.html`; greps all `<img …>` tags; asserts each contains `loading=\"lazy\"`. Same pattern as the existing line-26 assertion for the index species page.
    3. "emits _site/species/Agapostemon/index.html (GEN-01, URL-02, PIPE-01)"
       - reads the file; asserts `toContain('<em>Agapostemon</em>')`, `toContain('/data/species-maps/genus/Agapostemon.svg')`, `toContain('class=\"species-list\"')`, `toMatch(/background:\\s*#d92626/)` (first species in Agapostemon by canonical_name → hue 0 → #d92626; Plan 01 D-01 anchor).
    4. "genus page links each species to its species page (GEN-03)"
       - reads `_site/species/Agapostemon/index.html`; asserts `toMatch(/href=\"\\/species\\/Agapostemon\\/femoratus\\/\"/)` (the deterministic Agapostemon femoratus species page link).
    5. "emits a taxon-page chunk distinct from species chunk (Pattern 4)"
       - mirrors the existing `findSpeciesChunk()` helper as `findTaxonChunk()` (flat OR nested layout under `_site/assets/`); asserts at least one taxon-page chunk file exists. Use `expect(hasFlatTaxon || hasNestedTaxon, 'no taxon-page chunk emitted').toBe(true)`.

    Existing tests (`emits _site/species/index.html with one <bee-species-card>...`, `every <img> tag has loading="lazy"`, `emits a species-page chunk`, `species chunk does NOT contain mapboxgl symbol`) must remain unchanged and must continue to pass.

    The single shared `beforeAll(() => execSync('npm run build', ...))` covers the new tests at no extra build cost.
  </behavior>
  <action>
    Open `src/tests/build-output.test.ts`. Inside the existing `describe.skipIf(SKIP_BUILD)('build output (PAGE-07, PAGE-09)', () => { … })` block, after the last existing `test(...)` and before the closing `});`, add:

    1. A local helper `function findTaxonChunk(): string | undefined { … }` that mirrors `findSpeciesChunk()` — check `_site/assets` for files matching `/^taxon-page-.*\\.js$/` (flat layout) first, then `_site/assets/taxon-page/` for any `.js` file (nested layout). Return the resolved path or `undefined`. Place this helper next to `findSpeciesChunk` for readability.
    2. The five `test(...)` blocks described in <behavior>, in the order listed. Copy assertion forms from 94-PATTERNS.md `src/tests/build-output.test.ts` section. For the "loading=lazy" species-page test, reuse the existing line-28 idiom: `const imgs = html.match(/<img\\b[^>]*>/g) ?? []; for (const img of imgs) { expect(img, img).toMatch(/loading=\"lazy\"/); }`.

    Do NOT add a second `beforeAll`. Do NOT call `execSync('npm run build', ...)` from inside any individual test — the existing beforeAll runs the build once for all tests in the describe block. Do NOT remove or modify any of the four existing tests. Do NOT change the `SKIP_BUILD` constant or describe name.

    Use the deterministic anchor values from 94-RESEARCH.md and the Plan 01 hexColor anchor (`#d92626` for Agapostemon's first species alphabetically by canonical_name).
  </action>
  <verify>
    <automated>npm test -- src/tests/build-output.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "function findTaxonChunk" src/tests/build-output.test.ts` returns `1`.
    - `grep -c "emits _site/species/Agapostemon/femoratus/index.html" src/tests/build-output.test.ts` returns `1`.
    - `grep -c "emits _site/species/Agapostemon/index.html" src/tests/build-output.test.ts` returns `1`.
    - `grep -c "taxon-page chunk" src/tests/build-output.test.ts` returns at least `1`.
    - `grep -c "#d92626" src/tests/build-output.test.ts` returns `1`.
    - `grep -c "View .* occurrences on the atlas" src/tests/build-output.test.ts` returns `1`.
    - `grep -c "/species/Agapostemon/femoratus/" src/tests/build-output.test.ts` returns at least `1` (genus-page-links-to-species assertion).
    - `grep -cE "^\\s*test\\(" src/tests/build-output.test.ts` returns `9` (4 original + 5 new).
    - `grep -cE "^\\s*beforeAll\\(" src/tests/build-output.test.ts` returns `1` (no duplicate build invocation introduced).
    - `npm test -- src/tests/build-output.test.ts` exits 0; reports 9 passing tests (assumes `SKIP_BUILD` is not set).
    - After the test run, `ls _site/species/Agapostemon/femoratus/index.html` succeeds and `ls _site/species/Agapostemon/index.html` succeeds (the test's `beforeAll` build produced both).
    - `wc -l < <(find _site/species -mindepth 3 -name index.html)` reports > 500 (~527 species pages emitted).
    - `wc -l < <(find _site/species -mindepth 2 -maxdepth 2 -name index.html)` reports > 40 (~42 genus pages emitted).
  </acceptance_criteria>
  <done>
    Build output test file extended with 5 new tests + 1 new helper, all green; the build run inside the test produced 500+ species pages and 40+ genus pages plus a `taxon-page` Vite chunk. Eleventy build succeeds end-to-end; both Agapostemon femoratus (species) and Agapostemon (genus) pages contain the required content (scientific name in `<em>`, correct SVG map path, seasonality tag for species, color swatches + species links for genus). All 10 phase requirements (URL-01, URL-02, SPE-01..04, GEN-01..03, PIPE-01) have at least one automated assertion behind them.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human verification of photo, seasonality, and swatch-to-SVG color match</name>
  <files></files>
  <read_first>
    - .planning/phases/94-species-genus-pages/94-VALIDATION.md (Manual-Only Verifications table)
    - .planning/phases/94-species-genus-pages/94-UI-SPEC.md (Species Page Layout, Genus Page Layout, Photo Display, Seasonality Visualization, Color sections — these are what the reviewer should see)
  </read_first>
  <what-built>
    Plan 01 added `speciesList`, `genusList`, and per-species `hexColor` to `_data/species.js`. Plan 02 added two Nunjucks templates (`_pages/species-detail.njk`, `_pages/genus.njk`), one CSS file (`src/styles/taxon-pages.css`), and one Vite entry (`src/entries/taxon-page.ts`). Task 1 of this plan ran the production build (which generated ~527 species pages and ~42 genus pages under `_site/species/`) and confirmed via automated assertions that the right files exist with the right markup, the `taxon-page` Vite chunk was emitted, and every species-page `<img>` carries `loading="lazy"`.

    What automated tests CANNOT prove: that the photo actually paints, that the LitElement seasonality chart actually renders 12 bars in the browser, and that the color swatches on the genus page visually match the SVG map dot colors (the Phase 93 D-02 contract).
  </what-built>
  <how-to-verify>
    1. Start the dev server: `npm run dev` (Vite middleware + Eleventy hot reload).
    2. Open `http://localhost:8080/species/Andrena/milwaukeensis/` (or another species you know has a photo — `Agapostemon/femoratus` is also a good choice). Confirm:
       - Breadcrumb at top reads `Halictidae / Agapostemon / femoratus` (or `Andrenidae / Andrena / milwaukeensis`), with the genus segment a clickable link.
       - `<h1>` shows the scientific name in italic (e.g. `Agapostemon femoratus`).
       - A photo renders in the upper-left/upper region with attribution text below it in muted grey. If no photo exists for the chosen species, a grey "No photo available" placeholder appears in its place at a 4:3 aspect ratio.
       - The per-species SVG occurrence map renders to the right of (or below) the photo, with dots over Washington counties.
       - The seasonality chart renders below the photo/map region with 12 monthly bars (Jan-Dec) and the existing season-color bands. If the species has <5 records, the chart shows the text fallback ("N records, [month range]") instead — also acceptable.
       - Metadata row reads e.g. `91 records · 10 counties · 3 ecoregions` (exact dot separator).
       - The "View 91 occurrences on the atlas →" link is present at the bottom and, when clicked, navigates to `/?taxon=Agapostemon+femoratus&taxonRank=species` and the main atlas filters to that species.
    3. Open `http://localhost:8080/species/Andrena/` (or `/species/Agapostemon/`). Confirm:
       - Breadcrumb reads `{Family} / {Genus}` (plain text, no link for either segment).
       - `<h1>` shows the genus in italic (e.g. `<em>Agapostemon</em>`).
       - Subheading shows `{N} species · {N} records`.
       - The multi-color genus SVG map renders to the left (desktop) or above (mobile) the species list.
       - The species list shows one row per species with: a small colored square swatch on the left, the italic scientific name as a link, and `{N} records` right-aligned.
       - **D-02 cross-check (the load-bearing visual check):** Pick the top species in the list (e.g. `Agapostemon angelicus`). Note its swatch color. Look at the SVG map and confirm at least one dot of that same color appears on the map. Repeat for one or two more species in the list. Swatch colors MUST match SVG dot colors — if they don't, the alphabetical-by-canonical_name sort key has drifted between the JS (`_data/species.js`) and Python (`data/species_maps.py`) producers.
       - Click one species link; the species page loads.
    4. Resize the browser to mobile width (<768px). Confirm both pages collapse to a single column with the photo/map stacked above the species list (genus page) or above the seasonality chart (species page).
    5. Open DevTools console on both pages. Confirm:
       - No JavaScript errors.
       - The taxon-page chunk loads (a `taxon-page-*.js` or `assets/taxon-page/index-*.js` request in the Network tab).
       - `<bee-species-page>`, `<bee-species-card>`, `<bee-taxon-nav>`, `<bee-species-filter>` are NOT present in the DOM (these belong to `/species/` only).
  </how-to-verify>
  <action>Stop and wait for the human reviewer to perform the steps in &lt;how-to-verify&gt;. Do NOT proceed to the SUMMARY task until the reviewer types "approved". If the reviewer reports issues, capture them verbatim in 94-03-SUMMARY.md and pause for instruction — small visual tweaks (CSS spacing, copy fixes) may be addressed inline with a follow-up commit; structural issues (swatch ↔ SVG color mismatch, seasonality data shape) require revisiting Plan 01 or Plan 02 via /gsd-plan-phase --gaps.</action>
  <verify>Wait for human resume-signal "approved" before completing this task. No automated check possible — this task exists to surface visual/runtime behaviors that the build-output assertions in Task 1 cannot prove.</verify>
  <done>Human reviewer typed "approved" (or follow-up edits were made and re-approved). Any deferred issues are documented in 94-03-SUMMARY.md with disposition (resolved inline / deferred to Plan 04 gap closure / deferred to a later phase).</done>
  <resume-signal>Type "approved" if all checks pass, or describe issues (e.g. "swatch colors don't match SVG dot colors on /species/Andrena/", "seasonality chart blank on /species/Agapostemon/femoratus/", "photo placeholder shows for a species that has a photo in TOML").</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| build process → static HTML in `_site/` | The Eleventy + Vite build reads `_data/`, `_pages/`, `src/`, and `public/data/` (all in-repo or pipeline-produced) and writes the public site under `_site/`. No runtime input. |
| browser → rendered taxon page | A human reviewer interacts with the generated pages in their browser during the checkpoint. No server. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-94-01 | Tampering / Template injection | Generated `_site/species/{Genus}/{epithet}/index.html` inline seasonality `<script>` | mitigate | The build-output test in Task 1 asserts `<seasonality-viz` tag presence on the species page. The `{{ sp.month_histogram | dump | safe }}` rendering (Plan 02 Task 2) was already mitigated; this plan's automated check confirms the chunk loads and no console errors fire during the human checkpoint, providing runtime evidence the JSON-array shape is correct. |
| T-94-02 | Tampering / XSS | Rendered HTML (species + genus pages) | mitigate | The Task 1 assertions read the post-build HTML and confirm the expected escaped content is present (`<em>Agapostemon femoratus</em>` etc.). The human checkpoint Task 2 confirms no console errors during browser load — any unescaped script execution would surface as a CSP-style failure or unexpected DOM manipulation. |
| T-94-03 | Tampering / Path traversal | `<img src>` in generated HTML | mitigate | Task 1 asserts the exact expected SVG paths appear in the HTML and (transitively) that the build did not fail when generating them. Future Eleventy/pipeline changes that introduce `..` into slugs would break the assertion `toContain('/data/species-maps/Agapostemon/femoratus.svg')`. The human checkpoint cross-checks that the SVG maps actually load in the browser. |
| T-94-04 | Information Disclosure | Build artifacts under `_site/` | mitigate | Plan 02 Task 1's verification grep (`process.env|secret|token`) returned 0 matches. This plan adds no new files that could leak secrets. The build runs in CI under the same constraints; CloudFront serves `_site/` publicly by design (the entire site is public). |
</threat_model>

<verification>
- `npm test -- src/tests/build-output.test.ts` exits 0 with 9 passing tests (4 original + 5 new).
- Inspect: `ls _site/species/Agapostemon/femoratus/index.html` and `ls _site/species/Agapostemon/index.html` both succeed after the test run.
- `find _site/species -mindepth 3 -name index.html | wc -l` returns > 500 (species pages).
- `find _site/species -mindepth 2 -maxdepth 2 -name index.html | wc -l` returns > 40 (genus pages).
- `find _site/assets \\( -name "taxon-page-*.js" -o -path "*/taxon-page/*.js" \\) | wc -l` returns at least 1.
- The full Vitest suite (`npm test`) exits 0 — including the original `data-species.test.ts` (8 tests), `build-output.test.ts` (9 tests), and any other existing suites that touch `_data/species.js` shape.
- Human checkpoint completed with "approved" or all reported issues resolved by follow-up edits (which, if non-trivial, become a Plan 04 via `/gsd-plan-phase --gaps`).
</verification>

<success_criteria>
- All 10 phase requirements have automated coverage:
  - URL-01 + SPE-01 + PIPE-01: build emits `_site/species/Agapostemon/femoratus/index.html` (Task 1 test 1).
  - URL-02 + GEN-01 + PIPE-01: build emits `_site/species/Agapostemon/index.html` (Task 1 test 3).
  - SPE-02: photo lookup pattern present in template (Plan 02 Task 2 acceptance) + human verification of photo render (Task 2).
  - SPE-03: SVG map URL appears in species HTML (Task 1 test 1).
  - SPE-04: `<seasonality-viz` tag appears in species HTML (Task 1 test 1); human confirms render (Task 2).
  - GEN-02: genus SVG map URL appears in genus HTML (Task 1 test 3); human confirms swatch-color ↔ SVG-dot-color match (Task 2).
  - GEN-03: species page link present in genus HTML (Task 1 test 4).
- Human reviewer signs off on photo, seasonality, and swatch-to-SVG color match (Task 2 resume-signal = "approved").
- Full test suite green.
</success_criteria>

<output>
After completion, create `.planning/phases/94-species-genus-pages/94-03-SUMMARY.md` describing: the build run result (page counts, chunk emission), the 5 new tests added and which requirements they cover, the human checkpoint disposition, any UAT/visual gaps surfaced (deferred or resolved inline), and confirmation that `npm test` is fully green. Cross-reference the per-task verification map in `94-VALIDATION.md` and mark each row's status.
</output>
