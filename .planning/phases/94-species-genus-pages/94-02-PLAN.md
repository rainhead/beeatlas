---
phase: 94
plan: 02
type: execute
wave: 2
depends_on: [01]
files_modified:
  - _pages/species-detail.njk
  - _pages/genus.njk
  - src/styles/taxon-pages.css
  - src/entries/taxon-page.ts
autonomous: true
requirements:
  - URL-01
  - URL-02
  - SPE-01
  - SPE-02
  - SPE-03
  - SPE-04
  - GEN-01
  - GEN-02
  - GEN-03
  - PIPE-01 (partial — species+genus pages; subgenus+tribe in Phase 95)
must_haves:
  truths:
    - "Eleventy generates `_site/species/{Genus}/{epithet}/index.html` for every species in species.speciesList (~527 pages)"
    - "Eleventy generates `_site/species/{Genus}/index.html` for every genus in species.genusList (~42 pages)"
    - "Species pages render: breadcrumb (Family / linked Genus / epithet), <h1><em>{scientificName}</em></h1>, photo (or `No photo available` placeholder), per-species SVG map iff occurrence_count > 0, <seasonality-viz> + inline-script data injection, metadata row, atlas deep-link"
    - "Genus pages render: breadcrumb (Family / Genus), <h1><em>{Genus}</em></h1>, `{speciesCount} species · {totalOccurrences} records` subheading, multi-color genus SVG <img>, <ul class=species-list> with one <li> per species containing a 12×12 color swatch (background = sp.hexColor), italic linked scientific name, and `{occurrence_count} records` count"
    - "Color swatches on the genus page match the SVG dot colors because both consume the same alphabetical-by-canonical_name order (Phase 93 D-01 / D-02)"
    - "The new Vite entry `/src/entries/taxon-page.ts` is referenced from both templates and imports only `index.css`, `styles/taxon-pages.css`, `bee-header.ts`, `species/seasonality-viz.ts` (strict subset of `entries/species.ts`)"
    - "PIPE-01 partial delivery: this plan satisfies the species+genus page generation portion of PIPE-01 (templates); subgenus+tribe pages are Phase 95 scope"
  artifacts:
    - path: "_pages/species-detail.njk"
      provides: "Eleventy pagination template for /species/{Genus}/{epithet}/"
      contains: "pagination:"
    - path: "_pages/genus.njk"
      provides: "Eleventy pagination template for /species/{Genus}/"
      contains: "species.genusList"
    - path: "src/styles/taxon-pages.css"
      provides: "Layout for both taxon page types (.taxon-page, .species-list, .swatch, .breadcrumb, .photo-placeholder)"
      contains: ".taxon-page"
    - path: "src/entries/taxon-page.ts"
      provides: "Vite MPA entry chunk shared by both taxon templates"
      contains: "seasonality-viz"
  key_links:
    - from: "_pages/species-detail.njk"
      to: "species.speciesList"
      via: "Eleventy pagination data resolution (lodashGet dot-notation)"
      pattern: "data: species\\.speciesList"
    - from: "_pages/genus.njk"
      to: "species.genusList"
      via: "Eleventy pagination data resolution"
      pattern: "data: species\\.genusList"
    - from: "_pages/species-detail.njk"
      to: "_data/photos.js (photos global)"
      via: "Nunjucks variable lookup photos[sp.scientificName]"
      pattern: "photos\\[sp\\.scientificName\\]"
    - from: "_pages/species-detail.njk"
      to: "<seasonality-viz> custom element data property"
      via: "inline <script> + document.getElementById('sviz').data = {{ sp.month_histogram | dump | safe }}"
      pattern: "month_histogram \\| dump \\| safe"
    - from: "Both templates"
      to: "src/entries/taxon-page.ts"
      via: "<script type=\"module\" src=\"/src/entries/taxon-page.ts\"> tag → Vite MPA auto-discovery"
      pattern: "src=\"/src/entries/taxon-page.ts\""
---

<objective>
Create the four new files that render Phase 94's static taxon pages on top of Plan 01's data layer: two Nunjucks pagination templates (species detail + genus), one CSS layout file, and one Vite MPA entry. Together they satisfy URL-01, URL-02, SPE-01..04, GEN-01..03, PIPE-01.

Purpose: All authoring lives in one wave so the templates, the styles they reference, and the JS entry they `<script type="module">`-import are landed together. Plan 03 will then run the build and verify the emitted HTML.

Output: Four new files. Zero modifications to existing files. Zero new npm dependencies.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/phases/94-species-genus-pages/94-UI-SPEC.md
@.planning/phases/94-species-genus-pages/94-RESEARCH.md
@.planning/phases/94-species-genus-pages/94-PATTERNS.md
@.planning/phases/93-multi-color-svg-map-generation/93-CONTEXT.md
@.planning/phases/94-species-genus-pages/94-01-PLAN.md
@_pages/species.njk
@_layouts/default.njk
@_layouts/base.njk
@src/entries/species.ts
@src/entries/bee-header.ts
@src/styles/species.css
@src/index.css
@src/species/seasonality-viz.ts
@./CLAUDE.md

<interfaces>
<!-- Data shape this plan consumes (produced by Plan 01) -->

species.speciesList[i] = {
  canonical_name, county_count, ecoregion_count, family, genus,
  month_histogram: number[12], occurrence_count, scientificName,
  slug: "Genus/epithet", specific_epithet, subfamily, subgenus, tribe
}

species.genusList[i] = {
  genus, family, subfamily,
  species: [{ ...speciesEntry, hexColor: "#rrggbb" | "#cccccc" }, ...],
  speciesCount: number,
  totalOccurrences: number
}

<!-- Photos global from _data/photos.js -->
photos[scientificName] = {
  description: string,
  photos: [{ url, src?, srcset?, caption?, attribution?, ordering? }, ...]
}

<!-- Layout chain: default.njk wraps content in <main>, base.njk supplies <title>{{ title }}</title> -->
_layouts/default.njk emits <bee-header></bee-header> + <script type="module" src="/src/entries/bee-header.ts"></script>
_layouts/base.njk emits <title>{{ title or "BeeAtlas" }}</title>

<!-- Vite entry pattern (mirrors src/entries/species.ts but trimmed) -->
src/entries/species.ts imports:
  '../styles/species.css', '../bee-header.ts', '../species/bee-species-page.ts',
  '../species/bee-species-card.ts', '../species/bee-taxon-nav.ts',
  '../species/bee-species-filter.ts', '../species/seasonality-viz.ts'

src/entries/taxon-page.ts MUST import a strict subset (no bee-species-page, no
bee-species-card, no bee-taxon-nav, no bee-species-filter). See 94-RESEARCH.md
Pattern 4 and 94-PATTERNS.md `src/entries/taxon-page.ts` section.

<!-- <seasonality-viz> contract -->
src/species/seasonality-viz.ts exposes `@property({ attribute: false }) data: number[]`.
Set via inline script: document.getElementById('sviz').data = {{ sp.month_histogram | dump | safe }};

<!-- Nunjucks built-in filters used (no registration needed) -->
| dump      = JSON.stringify
| safe      = disable HTML escaping
| urlencode = URL-encode a string
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create src/styles/taxon-pages.css and src/entries/taxon-page.ts</name>
  <files>src/styles/taxon-pages.css, src/entries/taxon-page.ts</files>
  <read_first>
    - src/entries/species.ts (the import-order pattern: CSS-first, then components; copy structure)
    - src/entries/bee-header.ts (minimal-entry pattern)
    - src/styles/species.css (overall stylesheet shape and existing `bee-species-page` rules — do NOT modify; just understand the analog)
    - src/index.css (design tokens: --text-muted, --text-hint, --surface-subtle, --border, --link, --link-hover)
    - .planning/phases/94-species-genus-pages/94-UI-SPEC.md (Layout Contract, Spacing Scale, Typography, Color sections)
    - .planning/phases/94-species-genus-pages/94-PATTERNS.md (`src/styles/taxon-pages.css` and `src/entries/taxon-page.ts` sections — copy the CSS blocks verbatim)
  </read_first>
  <behavior>
    `src/styles/taxon-pages.css`:
    - `.taxon-page` container: max-width 1100px, margin 0 auto, padding 1rem, box-sizing border-box.
    - `.taxon-page > img:first-of-type, .taxon-page .photo-hero`: aspect-ratio 4/3, max-height 360px, width 100%, object-fit contain, background `var(--surface-subtle, #f5f5f5)`.
    - `.taxon-page img[src*="/species-maps/"]`: aspect-ratio 15/8, width 100%, max-width 600px.
    - `.taxon-page .attribution`: font-size 0.75rem, color `var(--text-muted, #666)`, margin 0.25rem 0 0.5rem.
    - `.taxon-page .metadata`: font-size 0.85rem, color `var(--text-muted, #666)`.
    - Desktop two-column at `min-width: 768px` via `.taxon-page .media-grid` (display grid, 1fr 1fr, gap 1.5rem, align-items start).
    - `.breadcrumb`: font-size 0.85rem, color `var(--text-muted, #666)`, margin-bottom 0.5rem. `.breadcrumb a`: color `var(--link, #646cff)`. `.breadcrumb .sep`: padding 0 4px.
    - `.swatch`: display inline-block, width 12px, height 12px, border-radius 2px, vertical-align middle, margin-right 4px.
    - `.species-list`: list-style none, margin 0, padding 0. `.species-list li`: display flex, align-items baseline, gap 0.5rem, padding 0.25rem 0, border-bottom 1px solid `var(--border, #ddd)`. `.species-list .count`: margin-left auto, font-size 0.85rem, color `var(--text-muted, #666)`.
    - `.photo-placeholder`: aspect-ratio 4/3, max-height 360px, display flex, align-items center, justify-content center, background `var(--surface-subtle, #f5f5f5)`, color `var(--text-hint, #767676)`, font-style italic, font-size 0.85rem.
    - File-leading block comment naming Phase 94 and noting design tokens come from `src/index.css`.

    `src/entries/taxon-page.ts`:
    - Four side-effect imports in this exact order: `'../index.css'`, `'../styles/taxon-pages.css'`, `'../bee-header.ts'`, `'../species/seasonality-viz.ts'`.
    - File-leading block comment naming the two templates that consume this entry (`_pages/species-detail.njk`, `_pages/genus.njk`) and noting the import set is a strict subset of `src/entries/species.ts` (no `bee-species-page`, `bee-species-card`, `bee-taxon-nav`, `bee-species-filter`).
    - Pure side-effect module — no `export` statements, no top-level statements other than the imports and the comment.
  </behavior>
  <action>
    1. Create `src/styles/taxon-pages.css` containing the CSS rules listed in <behavior>. Copy each rule block verbatim from 94-PATTERNS.md `src/styles/taxon-pages.css` section. Use CSS custom properties (`var(--text-muted, #666)` form with literal fallback) so the file degrades gracefully if loaded without index.css. Do not include any `@import` statements — the Vite entry composes the CSS chain.
    2. Create `src/entries/taxon-page.ts` containing exactly the four side-effect imports in the order listed in <behavior>. Use the `.ts` extensions on the two custom-element imports (matches `src/entries/species.ts` style under `verbatimModuleSyntax`). Copy the leading comment from 94-PATTERNS.md `src/entries/taxon-page.ts` section.

    Do NOT modify `vite.config.ts`, `eleventy.config.js`, `src/entries/species.ts`, `src/styles/species.css`, or `src/index.css`.
  </action>
  <verify>
    <automated>npx tsc --noEmit -p tsconfig.json</automated>
  </verify>
  <acceptance_criteria>
    - `test -f src/styles/taxon-pages.css` succeeds.
    - `test -f src/entries/taxon-page.ts` succeeds.
    - `grep -c "^\\.taxon-page" src/styles/taxon-pages.css` returns at least `1`.
    - `grep -c "^\\.species-list" src/styles/taxon-pages.css` returns at least `1`.
    - `grep -c "^\\.swatch" src/styles/taxon-pages.css` returns `1`.
    - `grep -c "^\\.breadcrumb" src/styles/taxon-pages.css` returns at least `1`.
    - `grep -c "^\\.photo-placeholder" src/styles/taxon-pages.css` returns `1`.
    - `grep -c "@media (min-width: 768px)" src/styles/taxon-pages.css` returns at least `1`.
    - `grep -c "import" src/entries/taxon-page.ts` returns `4`.
    - `grep -c "bee-species-page\\|bee-species-card\\|bee-taxon-nav\\|bee-species-filter" src/entries/taxon-page.ts` returns `0` (forbidden imports absent).
    - `grep -c "seasonality-viz" src/entries/taxon-page.ts` returns `1`.
    - `grep -c "../index.css\\|../styles/taxon-pages.css" src/entries/taxon-page.ts` returns `2`.
    - `npx tsc --noEmit -p tsconfig.json` exits 0 (no new TS errors introduced).
  </acceptance_criteria>
  <done>
    Both files exist with the required content; no existing files modified; `tsc --noEmit` passes; CSS file declares the class hooks (`.taxon-page`, `.species-list`, `.swatch`, `.breadcrumb`, `.photo-placeholder`) that the templates in Task 2 and Task 3 will reference.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create _pages/species-detail.njk for per-species pages</name>
  <files>_pages/species-detail.njk</files>
  <read_first>
    - _pages/species.njk (analog: front-matter pattern, photo lookup pattern, SVG map conditional, atlas deep-link, scientificName-based photos lookup)
    - _layouts/default.njk (wraps content in <main>, already loads <bee-header>)
    - _layouts/base.njk (sets <title>{{ title }}</title>)
    - .planning/phases/94-species-genus-pages/94-UI-SPEC.md (Species Page Layout, Copywriting Contract, Breadcrumb Navigation, "View N occurrences" link, Photo Display, SVG Map Dimensions, Seasonality Visualization)
    - .planning/phases/94-species-genus-pages/94-RESEARCH.md (Pattern 1 + Pattern 3 + Pattern 5; Pitfall 2 eleventyComputed; Pitfall 4 occurrence_count=0; Pitfall 5 slug-slash; Pitfall 6 entry split)
    - .planning/phases/94-species-genus-pages/94-PATTERNS.md (`_pages/species-detail.njk` section — copy front-matter, photo, SVG, seasonality, deep-link blocks)
    - src/species/seasonality-viz.ts (confirm `data` is a settable property; element registers as `<seasonality-viz>`)
    - .planning/phases/94-species-genus-pages/94-01-PLAN.md (data shape — what fields are available on `sp`)
  </read_first>
  <behavior>
    - Front matter declares `pagination: { data: species.speciesList, size: 1, alias: sp }`, `permalink: "/species/{{ sp.slug }}/"`, `eleventyComputed: { title: "{{ sp.scientificName }} — BeeAtlas" }`, `layout: default.njk`.
    - First body element: `<nav class="breadcrumb">` rendering `{{ sp.family }} <span class="sep">/</span> <a href="/species/{{ sp.genus }}/">{{ sp.genus }}</a> <span class="sep">/</span> {{ sp.specific_epithet }}` (family plain text; genus a link to the genus page; epithet plain text — current page).
    - Page heading: `<h1><em>{{ sp.scientificName }}</em></h1>` — italic, no escaping needed (scientificName is safe).
    - Wrap page body in `<article class="taxon-page">…</article>` so the CSS rules from Task 1 apply.
    - Photo block: `{% set photoEntry = photos[sp.scientificName] %}`. If `photoEntry and photoEntry.photos and photoEntry.photos.length > 0`, set `p = photoEntry.photos[0]` and render `<img loading="lazy" src="{{ p.src or p.url }}" alt="{{ p.caption or sp.scientificName }}">` followed by `<p class="attribution">{{ p.attribution }}</p>` (only when attribution is truthy). Otherwise render `<div class="photo-placeholder">No photo available</div>`.
    - SVG occurrence map: only when `sp.occurrence_count > 0`, render `<img loading="lazy" src="/data/species-maps/{{ sp.slug }}.svg" alt="Occurrence map for {{ sp.scientificName }}">` — the `.taxon-page img[src*="/species-maps/"]` CSS rule sizes it. Do NOT render any "no map" placeholder when count is 0 (UI-SPEC: omit entirely).
    - Seasonality: `<seasonality-viz id="sviz"></seasonality-viz>` followed by `<script>document.getElementById('sviz').data = {{ sp.month_histogram | dump | safe }};</script>`. The `| dump | safe` chain is load-bearing; `month_histogram` is a 12-element integer array from the controlled pipeline (no XSS surface).
    - Metadata row: `<p class="metadata">{{ sp.occurrence_count }} records · {{ sp.county_count }} counties · {{ sp.ecoregion_count }} ecoregions</p>` — exact dot-separator copy per UI-SPEC Copywriting Contract.
    - Atlas deep-link: `<a href="/?taxon={{ sp.scientificName | urlencode }}&amp;taxonRank=species">View {{ sp.occurrence_count }} occurrences on the atlas →</a>` — the UI-SPEC copy ends with " on the atlas →"; preserve the trailing arrow character.
    - Closing: `<script type="module" src="/src/entries/taxon-page.ts"></script>`. Place this AFTER the closing `</article>` so the static markup is parsed first.
    - No use of `bee-species-card`, `bee-species-page`, `bee-taxon-nav`, `bee-species-filter` — those are for the index page.
  </behavior>
  <action>
    Create `_pages/species-detail.njk` with the front matter and body described in <behavior>. Use the verified Nunjucks excerpts from 94-RESEARCH.md "Code Examples → Verified Eleventy Pagination Template" and 94-PATTERNS.md `_pages/species-detail.njk` section as the source. Wrap the photo + SVG map in a single `<div class="media-grid">` so the desktop two-column rule from `taxon-pages.css` activates; on mobile (<768px) the grid collapses to single column automatically.

    For Pitfall 2 (eleventyComputed): use the YAML form `eleventyComputed: { title: "{{ sp.scientificName }} — BeeAtlas" }` shown above. If a downstream task observes literal `{{` in the rendered `<title>`, switch the title to a JS function form per 94-RESEARCH.md Assumptions Log A2 — but try the YAML form first.

    Order of body elements (top to bottom): `<article class="taxon-page">`, breadcrumb, h1, `<div class="media-grid">` containing the photo block then the SVG map block, attribution paragraph (when photo present), seasonality-viz + inline script, metadata row, atlas deep-link, `</article>`, then the Vite entry `<script type="module">` tag.

    Do NOT include any `<link rel="stylesheet">` tags — the Vite entry imports `index.css` and `taxon-pages.css` as side effects. Do NOT add any custom Nunjucks filters; only use built-in `dump`, `safe`, `urlencode`.

    **Note on srcset (intentional omission):** Unlike the analog `_pages/species.njk` (lines 32–39 in PATTERNS.md), this template's photo `<img>` deliberately omits the `srcset` conditional. Rationale: `_data/photos.js` `deriveSrcset` may produce an empty `srcset` for newly ingested photos, and Phase 94 scope is simplified to use the `src`/`url` fallback only (`src="{{ p.src or p.url }}"`). Phase 95 can reintroduce the `srcset` conditional if responsive image sourcing becomes a priority. Do NOT add `srcset` here; if you find yourself wanting it, defer to Phase 95.
  </action>
  <verify>
    <automated>npx @11ty/eleventy --dryrun --quiet 2>&1 | grep -E "species-detail|species/Andrena/milwaukeensis" | head -5</automated>
  </verify>
  <acceptance_criteria>
    - `test -f _pages/species-detail.njk` succeeds.
    - `grep -c "data: species.speciesList" _pages/species-detail.njk` returns `1`.
    - `grep -c "alias: sp" _pages/species-detail.njk` returns `1`.
    - `grep -c "permalink:.*species.*sp\\.slug" _pages/species-detail.njk` returns `1`.
    - `grep -c "eleventyComputed" _pages/species-detail.njk` returns `1`.
    - `grep -c "layout: default.njk" _pages/species-detail.njk` returns `1`.
    - `grep -c "<article class=\"taxon-page\">" _pages/species-detail.njk` returns `1`.
    - `grep -c "class=\"breadcrumb\"" _pages/species-detail.njk` returns `1`.
    - `grep -c "photos\\[sp\\.scientificName\\]" _pages/species-detail.njk` returns `1`.
    - `grep -c "photo-placeholder" _pages/species-detail.njk` returns `1`.
    - `grep -c "sp\\.occurrence_count > 0" _pages/species-detail.njk` returns `1` (SVG conditional).
    - `grep -c "month_histogram | dump | safe" _pages/species-detail.njk` returns `1`.
    - `grep -c "seasonality-viz" _pages/species-detail.njk` returns at least `2` (tag open + script reference).
    - `grep -c "View .* occurrences on the atlas" _pages/species-detail.njk` returns `1`.
    - `grep -c "src=\"/src/entries/taxon-page.ts\"" _pages/species-detail.njk` returns `1`.
    - `grep -c "loading=\"lazy\"" _pages/species-detail.njk` returns at least `1`.
    - `grep -cE "bee-species-card|bee-species-page|bee-taxon-nav|bee-species-filter" _pages/species-detail.njk` returns `0`.
    - `npx @11ty/eleventy --dryrun --quiet` exits 0 (Eleventy parses the template without errors).
  </acceptance_criteria>
  <done>
    The template parses under Eleventy dry-run; the front-matter declares pagination over `species.speciesList`; the body wires photo, SVG map, seasonality, metadata, and atlas deep-link with the exact copy from the UI-SPEC; the only JS reference is the lean taxon-page Vite entry.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Create _pages/genus.njk for per-genus pages</name>
  <files>_pages/genus.njk</files>
  <read_first>
    - _pages/species.njk (Nunjucks reference patterns, photos block — NOT used here, but the pattern style is the same)
    - _layouts/default.njk
    - .planning/phases/94-species-genus-pages/94-UI-SPEC.md (Genus Page Layout, Copywriting Contract for genus, Species list with color swatches)
    - .planning/phases/94-species-genus-pages/94-RESEARCH.md (Pattern 1 — genus variant, Code Examples → Genus Page with Color Swatches; Pitfall 7 URL collision is fine)
    - .planning/phases/94-species-genus-pages/94-PATTERNS.md (`_pages/genus.njk` section — copy front-matter, SVG map, species list blocks)
    - .planning/phases/93-multi-color-svg-map-generation/93-CONTEXT.md (D-03 — no SVG legend, swatches in HTML; D-02 — alphabetical canonical_name order MUST match SVG color index)
    - .planning/phases/94-species-genus-pages/94-01-PLAN.md (genusList shape — `species[]` already sorted, hexColor already on each species)
  </read_first>
  <behavior>
    - Front matter: `pagination: { data: species.genusList, size: 1, alias: genus }`, `permalink: "/species/{{ genus.genus }}/"`, `eleventyComputed: { title: "{{ genus.genus }} — BeeAtlas" }`, `layout: default.njk`.
    - Breadcrumb: `<nav class="breadcrumb">{{ genus.family }} <span class="sep">/</span> {{ genus.genus }}</nav>` — both segments plain text (genus is current page; family link deferred to Phase 96 per UI-SPEC).
    - Heading: `<h1><em>{{ genus.genus }}</em></h1>`.
    - Subheading: `<p class="metadata">{{ genus.speciesCount }} species · {{ genus.totalOccurrences }} records</p>` — `genus.speciesCount` and `genus.totalOccurrences` are the precomputed fields from Plan 01.
    - Wrap in `<article class="taxon-page">…</article>`.
    - Inside a `<div class="media-grid">`: render the genus SVG map `<img loading="lazy" src="/data/species-maps/genus/{{ genus.genus }}.svg" alt="Occurrence map for genus {{ genus.genus }}">` (no occurrence_count guard — genus always has species). Beside it (in the second grid column on desktop), render the species list.
    - Species list: `<ul class="species-list">` with one `<li>` per `sp in genus.species`. Each `<li>` contains, in order: `<span class="swatch" style="background: {{ sp.hexColor }};" aria-hidden="true"></span>`, `<a href="/species/{{ sp.slug }}/"><em>{{ sp.scientificName }}</em></a>`, `<span class="count">{{ sp.occurrence_count }} records</span>`.
    - Empty-state copy: never emitted, because the genusList only contains genera with at least one species in speciesList. (UI-SPEC mentions an "Empty genus" copy line for completeness but in current data it cannot be reached.)
    - Closing: `<script type="module" src="/src/entries/taxon-page.ts"></script>` after the closing `</article>`. No `<seasonality-viz>` on genus pages (the chart is per-species only).
    - The `sp.hexColor` value comes from Plan 01; it is either `#rrggbb` (lowercase hex from `hslToHex`) or `#cccccc` (zero-occurrence sentinel). Both are safe in inline `style="background: {{ sp.hexColor }};"` because they cannot contain quotes or `<` characters — see <threat_model> T-94-02 below.
  </behavior>
  <action>
    Create `_pages/genus.njk` with the front matter and body described in <behavior>. Copy the front-matter, SVG map, and species-list blocks verbatim from 94-PATTERNS.md `_pages/genus.njk` section, then wrap them in the `<article class="taxon-page">` + `<div class="media-grid">` structure to mirror the species-detail page layout.

    Render the species list as a flex column (the `.species-list li` rule from Task 1 makes count right-align via `margin-left: auto` on `.count`). Add `aria-hidden="true"` on each `.swatch` `<span>` since the color is decorative and the scientific name carries the semantic meaning.

    For the `style="background: {{ sp.hexColor }};"` inline rule: leave the value un-piped (no `| safe` needed). Default Nunjucks autoescape escapes HTML special chars but not the `#` or hex digits, which is exactly what we want.

    Do NOT include `<seasonality-viz>` (no per-genus chart in Phase 94). Do NOT include any reference to `bee-species-card` or the index-page coordinators.
  </action>
  <verify>
    <automated>npx @11ty/eleventy --dryrun --quiet 2>&1 | grep -E "genus\\.njk|species/Agapostemon/index" | head -5</automated>
  </verify>
  <acceptance_criteria>
    - `test -f _pages/genus.njk` succeeds.
    - `grep -c "data: species.genusList" _pages/genus.njk` returns `1`.
    - `grep -c "alias: genus" _pages/genus.njk` returns `1`.
    - `grep -c "permalink:.*species.*genus\\.genus" _pages/genus.njk` returns `1`.
    - `grep -c "<article class=\"taxon-page\">" _pages/genus.njk` returns `1`.
    - `grep -c "class=\"breadcrumb\"" _pages/genus.njk` returns `1`.
    - `grep -c "<em>{{ genus.genus }}</em>" _pages/genus.njk` returns `1`.
    - `grep -c "speciesCount" _pages/genus.njk` returns `1`.
    - `grep -c "totalOccurrences" _pages/genus.njk` returns `1`.
    - `grep -c "/data/species-maps/genus/" _pages/genus.njk` returns `1`.
    - `grep -c "Occurrence map for genus" _pages/genus.njk` returns `1`.
    - `grep -c "class=\"species-list\"" _pages/genus.njk` returns `1`.
    - `grep -c "class=\"swatch\"" _pages/genus.njk` returns `1`.
    - `grep -c "sp\\.hexColor" _pages/genus.njk` returns `1`.
    - `grep -c "/species/{{ sp.slug }}/" _pages/genus.njk` returns `1`.
    - `grep -c "{{ sp.occurrence_count }} records" _pages/genus.njk` returns `1`.
    - `grep -c "src=\"/src/entries/taxon-page.ts\"" _pages/genus.njk` returns `1`.
    - `grep -c "seasonality-viz" _pages/genus.njk` returns `0` (not on genus pages).
    - `grep -cE "bee-species-card|bee-species-page|bee-taxon-nav|bee-species-filter" _pages/genus.njk` returns `0`.
    - `npx @11ty/eleventy --dryrun --quiet` exits 0.
  </acceptance_criteria>
  <done>
    The template parses under Eleventy dry-run; front matter declares pagination over `species.genusList`; body wires the multi-color SVG map and the swatch-decorated species list; each species link points at the corresponding species page; no seasonality viz on the genus page.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| build-time data → static HTML | `species.json` (from the pipeline) and `content/species-photos.toml` (curated in-repo) cross into Nunjucks-rendered HTML. No user input enters at runtime — the output is static. |
| static HTML → browser | Inline `<script>` sets a property on a custom element using JSON-serialized integer arrays. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-94-01 | Tampering / Template injection | `<seasonality-viz>` inline `<script>` in `_pages/species-detail.njk` | mitigate | The expression `{{ sp.month_histogram | dump | safe }}` JSON-serializes `month_histogram` before disabling escaping. `month_histogram` is a 12-element integer array produced by `data/species_export.py` (the project's own pipeline). `dump` (= `JSON.stringify`) guarantees the output is a valid JS array literal even if the data were ever tampered with at the storage layer. Implemented in Task 2. |
| T-94-02 | Tampering / XSS | Nunjucks-rendered species name, photo attribution, hexColor inline style | mitigate | Default Nunjucks autoescape applies to every `{{ … }}` in `_pages/species-detail.njk` and `_pages/genus.njk` — no `| safe` filter is used on any string from the data layer. `sp.hexColor` is constrained by Plan 01 to `#rrggbb` lowercase hex or the literal `#cccccc`, so the inline `style="background: {{ sp.hexColor }};"` cannot break out of the attribute (no quotes possible). `p.attribution` and `p.caption` from `content/species-photos.toml` are HTML-escaped by Nunjucks autoescape on `{{ … }}`. Implemented in Tasks 2 and 3. |
| T-94-03 | Tampering / Path traversal | `<img src="/data/species-maps/{{ sp.slug }}.svg">` and `<img src="/data/species-maps/genus/{{ genus.genus }}.svg">` | mitigate | `sp.slug` and `genus.genus` originate from `data/species_export.py`'s controlled lineage table (Genus is a Latin taxon name, specific_epithet is lowercase Latin); Phase 92 set the slug format to `"Genus/epithet"` with no `..`, no leading `/`, no URL-encoded path segments. The strings flow through Nunjucks autoescape on `{{ … }}` so even if a `<` were ever present it would be HTML-escaped. The build-time test in Plan 03 will assert the generated paths resolve to existing files under `_site/`. Implemented in Tasks 2 and 3. |
| T-94-04 | Information Disclosure | Build-time pipeline secrets leaking into HTML | accept | No env vars, no API keys, no database URLs reach these templates. `_data/species.js` (Plan 01) and `_data/photos.js` read only `public/data/species.json`, `public/data/seasonality.json`, and `content/species-photos.toml` — all already-public artifacts. No `process.env` access in either data module or in the new templates. Verified by `grep -E "process\\.env|secret|token" _pages/species-detail.njk _pages/genus.njk _data/species.js src/entries/taxon-page.ts` returning 0 matches at the end of this plan. |
</threat_model>

<verification>
- `npx @11ty/eleventy --dryrun --quiet` exits 0 (templates parse without errors). A full build is run in Plan 03.
- `npx tsc --noEmit -p tsconfig.json` exits 0 (taxon-page.ts compiles).
- `grep -cE "bee-species-card|bee-species-page|bee-taxon-nav|bee-species-filter" src/entries/taxon-page.ts _pages/species-detail.njk _pages/genus.njk` returns `0` (lean entry preserved; no accidental coupling to the index page).
- `grep -E "process\\.env|secret|token" _pages/species-detail.njk _pages/genus.njk src/entries/taxon-page.ts src/styles/taxon-pages.css` returns 0 matches (T-94-04 mitigation evidence).
</verification>

<success_criteria>
- `_pages/species-detail.njk` exists and declares Eleventy pagination over `species.speciesList` with permalink `/species/{{ sp.slug }}/`.
- `_pages/genus.njk` exists and declares Eleventy pagination over `species.genusList` with permalink `/species/{{ genus.genus }}/`.
- `src/styles/taxon-pages.css` exists with `.taxon-page`, `.species-list`, `.swatch`, `.breadcrumb`, `.photo-placeholder`, and a `@media (min-width: 768px)` block.
- `src/entries/taxon-page.ts` exists and imports exactly four modules: `../index.css`, `../styles/taxon-pages.css`, `../bee-header.ts`, `../species/seasonality-viz.ts` (verified by `grep -c "import"` returning `4` and the forbidden-import check returning `0`).
- Both templates reference `/src/entries/taxon-page.ts` in a `<script type="module">` tag and reference no other JS entry.
- `npx @11ty/eleventy --dryrun --quiet` succeeds; `npx tsc --noEmit` succeeds.
- PIPE-01 requirement coverage: this plan delivers the species + genus template portion of PIPE-01 (REQUIREMENTS.md). Subgenus and tribe templates belong to Phase 95 and are explicitly out of scope here.
</success_criteria>

<output>
After completion, create `.planning/phases/94-species-genus-pages/94-02-SUMMARY.md` describing: the four new files and their roles, the front-matter pagination contract for each template, the exact import set of `taxon-page.ts` (and what was deliberately excluded vs `entries/species.ts`), the CSS class hooks introduced and their UI-SPEC source rows, and confirmation that the eleventyComputed YAML form was used (or flagged if it required the JS-function fallback per RESEARCH.md Assumption A2).
</output>
