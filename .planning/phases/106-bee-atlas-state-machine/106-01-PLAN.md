---
phase: 106-bee-atlas-state-machine
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/bee-atlas.ts
  - src/tests/bee-atlas.test.ts
autonomous: true
requirements: ["internal-refactor"]
must_haves:
  truths:
    - "All existing user interactions (occurrence click, region click, place select, selection draw, close button, view toggle, table mode entry/exit, URL load, browser back/forward) behave identically to before the refactor"
    - "Switching to table view and returning to map view leaves the sidebar collapsed (D-08 preserved)"
    - "Loading a URL with ?pane=list opens the sidebar; ?pane=table opens the table; absence of pane param leaves the pane collapsed"
  artifacts:
    - path: "src/bee-atlas.ts"
      provides: "Single _paneState @state() field replacing _viewMode + _sidebarOpen + _tableFilterOpen"
      contains: "@state() private _paneState"
    - path: "src/tests/bee-atlas.test.ts"
      provides: "Updated VIEW-02/SIDE-01/SEL-06 assertions plus new SM-01 block covering _paneState"
      contains: "SM-01"
  key_links:
    - from: "src/bee-atlas.ts firstUpdated"
      to: "_paneState assignment from initialParams.ui.paneState"
      via: "direct assignment (Phase 105 adapter removed)"
      pattern: "this\\._paneState\\s*=\\s*paneState"
    - from: "src/bee-atlas.ts _pushUrlState"
      to: "buildParams ui.paneState argument"
      via: "direct read of this._paneState"
      pattern: "paneState:\\s*this\\._paneState"
    - from: "src/bee-atlas.ts _onViewChanged"
      to: "_paneState transitions ('table' on enter, 'collapsed' on exit)"
      via: "event handler from bee-header view-changed"
      pattern: "this\\._paneState\\s*=\\s*'table'"
---

<objective>
Replace the three-flag view-mode encoding in `bee-atlas.ts` (`_viewMode`, `_sidebarOpen`, `_tableFilterOpen`) with a single discriminated-union `@state()` field `_paneState: 'collapsed' | 'list' | 'table'`. Remove the temporary Phase 105 adapters in `firstUpdated`, `_onPopState`, and `_pushUrlState`. Update the affected source-scan tests and add an SM-01 test block that locks in the new shape.

Purpose: Phase 106 is a pure internal refactor that prepares for Phase 107 (`bee-pane` component). After this plan, `_paneState` is the authoritative runtime source of truth and there are zero references to `_viewMode` or `_sidebarOpen` in `bee-atlas.ts`. The `_tableFilterOpen` flag is demoted to a non-reactive private field; its `.externalOpen` template binding on `bee-filter-panel` is dropped and the imperative `setOpen()` call in `_onToggleFilter` continues to drive DOM state.

Output: `bee-atlas.ts` and `bee-atlas.test.ts` updated in place; `npm test` and `tsc --noEmit` both green; user-visible behavior identical to Phase 105.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/106-bee-atlas-state-machine/106-RESEARCH.md
@.planning/phases/106-bee-atlas-state-machine/106-PATTERNS.md
@.planning/phases/105-url-state-migration/105-01-SUMMARY.md
@src/bee-atlas.ts
@src/tests/bee-atlas.test.ts
@src/url-state.ts

<interfaces>
<!-- Key contracts the executor needs. Extracted from codebase by the planner. -->

From src/url-state.ts (Phase 105 — already shipped):
```typescript
export interface UiState {
  boundaryMode?: BoundaryMode;
  paneState?: 'collapsed' | 'list' | 'table';
}
// buildParams(view, filter, selection, ui: UiState): URLSearchParams
// parseParams reads ?pane=list|table; legacy ?view=table aliases to pane=table; absence => 'collapsed'
```

From src/bee-atlas.ts (CURRENT state — to be refactored):
```typescript
@state() private _viewMode: 'map' | 'table' = 'map';           // line 36 — DELETE
@state() private _sidebarOpen = false;                          // line 53 — DELETE
@state() private _tableFilterOpen = false;                      // line 54 — DEMOTE to plain field
// 40 read/write sites across firstUpdated, _onPopState, _pushUrlState,
// _onOccurrenceClick, _openSidebarForFilter, _onSelectionDrawn,
// _onMapClickEmpty, _onFilterChanged, _onRegionClick, _onPlaceSelected,
// _onViewChanged, _onToggleFilter, _onClose, _restoreBoundsSelection,
// _runTableQuery, _onDataLoaded, _loadSummaryFromSQLite, render()
```

From src/bee-header.ts (unchanged in this phase):
```typescript
@property() viewMode: 'map' | 'table' = 'map';
// emits CustomEvent<'map' | 'table'> on 'view-changed'
// bee-atlas continues to pass .viewMode=${this._paneState === 'table' ? 'table' : 'map'}
```

From src/bee-filter-panel.ts (relevant bindings):
```typescript
// .hideButton: boolean — driven by paneState === 'table'
// .openUpward: boolean — driven by paneState === 'table'
// .externalOpen: boolean — REMOVED from bee-atlas template; bee-filter-panel's setOpen() continues to be called imperatively
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Update bee-atlas.test.ts source-scan assertions and add SM-01 block (RED)</name>
  <files>src/tests/bee-atlas.test.ts</files>
  <read_first>
    - src/tests/bee-atlas.test.ts (full file — must see SIDE-01 block at ~line 131, VIEW-02 at ~163, SEL-06 at ~392, plus the `_sidebarOpen = true` assertion in the SEL-06 area at line 375)
    - .planning/phases/106-bee-atlas-state-machine/106-PATTERNS.md (sections "Tests to update in SIDE-01 block", "Test to update in VIEW-02 block", "Test to update in SEL-06 block", "Existing test pattern to copy for new SM-01 block")
    - .planning/phases/106-bee-atlas-state-machine/106-RESEARCH.md ("Tests That Will Break and Must Be Updated" + "New tests to add in bee-atlas.test.ts")
    - src/bee-atlas.ts (just enough to confirm current method signatures — `_onClose`, `_onViewChanged`, `firstUpdated`, `_pushUrlState`)
  </read_first>
  <behavior>
    After this task, `npm test` MUST fail because the test file now expects `_paneState` shape but `bee-atlas.ts` still has the three-flag shape. Specifically:
    - SIDE-01 expects no `@state() private _sidebarOpen` declaration and expects `this._paneState = 'collapsed'` in `_onClose`
    - VIEW-02 expects `@state() private _paneState` and no `@state() private _viewMode`
    - SEL-06 expects `this._selectionBounds && this._paneState === 'list'`
    - New SM-01 block (7 tests) asserts the full target shape: _paneState declared with three-state type, no _viewMode/_sidebarOpen/_tableFilterOpen as @state fields, _onClose sets _paneState='collapsed', _onViewChanged sets _paneState='table' on entry, firstUpdated assigns _paneState directly from parsed paneState with no `=== 'table' ? 'table' : 'map'` adapter
  </behavior>
  <action>
    Update the existing source-scan tests in `src/tests/bee-atlas.test.ts` and append a new SM-01 describe block. Make these exact edits:

    1) In the SIDE-01 describe block (`SIDE-01: bee-atlas sidebar visibility wiring` at line 131):
       - Replace the test `'bee-atlas.ts declares _sidebarOpen as @state()'` with one named `'bee-atlas.ts does NOT declare _sidebarOpen as @state()'` whose body uses `expect(src).not.toMatch(/@state\(\)\s+private\s+_sidebarOpen/)`.
       - Replace the test `'bee-atlas.ts sets _sidebarOpen = true in _onSpecimenClick'` with `'bee-atlas.ts sets _paneState = list in occurrence click handler'` whose body uses `expect(src).toMatch(/this\._paneState\s*=\s*'list'/)`.
       - Replace the test `'bee-atlas.ts sets _sidebarOpen = false in _onClose'` with `'bee-atlas.ts sets _paneState = collapsed in _onClose'` that uses the method-body extraction pattern (find `private _onClose()`, slice until next `\n  private `, assert body contains `this._paneState = 'collapsed'`).

    2) In the VIEW-02 describe block at line 163:
       - Replace the test at line 174–176 `'bee-atlas.ts declares _viewMode as @state field'` with `'bee-atlas.ts declares _paneState as @state field (replaces _viewMode)'` that asserts both `expect(src).toMatch(/@state\(\)\s+private\s+_paneState/)` and `expect(src).not.toMatch(/@state\(\)\s+private\s+_viewMode/)`.

    3) In the block containing the line 375 assertion `expect(src).toMatch(/this\._sidebarOpen\s*=\s*true/)` (the `_onSelectionDrawn`-reachable test): update the regex to `/this\._paneState\s*=\s*'list'/` and rename the test to reflect the `_paneState` target.

    4) In the SEL-06 + SEL-07 block at line 392, update the test at line 395–397 `'SEL-06: _pushUrlState gives _selectionBounds precedence over cluster/ids'` so its assertion is `expect(src).toContain("this._selectionBounds && this._paneState === 'list'")` (single quotes around `'list'`, matching the source text exactly).

    5) Append a new describe block at the end of the file (before the final closing brace) named `'SM-01: bee-atlas pane state machine (Phase 106)'`. Inside it declare `const src = readFileSync(resolve(__dirname, '../bee-atlas.ts'), 'utf-8');` at block top. Add these seven tests using the patterns shown in 106-PATTERNS.md ("Existing test pattern to copy for new SM-01 block"):
       a. `'bee-atlas.ts declares _paneState as @state() with three-state type'` — matches `/@state\(\)\s+private\s+_paneState/` AND `/'collapsed'\s*\|\s*'list'\s*\|\s*'table'/`
       b. `'bee-atlas.ts does NOT contain _viewMode field'` — `expect(src).not.toMatch(/@state\(\)\s+private\s+_viewMode/)` AND `expect(src).not.toMatch(/this\._viewMode\s*=/)`
       c. `'bee-atlas.ts does NOT contain _sidebarOpen field'` — `expect(src).not.toMatch(/@state\(\)\s+private\s+_sidebarOpen/)` AND `expect(src).not.toMatch(/this\._sidebarOpen\s*=/)`
       d. `'bee-atlas.ts does NOT declare _tableFilterOpen as @state'` — `expect(src).not.toMatch(/@state\(\)\s+private\s+_tableFilterOpen/)` (the plain non-reactive field is allowed)
       e. `'_onClose sets _paneState = collapsed'` — method-body extraction; assert body contains `"this._paneState = 'collapsed'"`
       f. `'_onViewChanged sets _paneState = table when entering table mode'` — method-body extraction on `private _onViewChanged(`; assert body contains `"this._paneState = 'table'"`
       g. `'firstUpdated assigns _paneState directly from parsed paneState'` — `expect(src).toMatch(/this\._paneState\s*=\s*paneState/)` AND `expect(src).not.toMatch(/paneState\s*===\s*'table'\s*\?\s*'table'\s*:\s*'map'/)`

    Do NOT touch any other tests in the file. Imports (`readFileSync`, `resolve`, `describe`, `test`, `expect`) are already at file top — reuse them.
  </action>
  <verify>
    <automated>npm test -- src/tests/bee-atlas.test.ts 2>&1 | tail -40</automated>
  </verify>
  <acceptance_criteria>
    - `src/tests/bee-atlas.test.ts` contains the literal string `SM-01: bee-atlas pane state machine (Phase 106)`
    - `grep -c "SM-01" src/tests/bee-atlas.test.ts` returns at least 1
    - `grep -c "this._paneState" src/tests/bee-atlas.test.ts` returns at least 5
    - The string `expect(src).toMatch(/@state\(\)\s+private\s+_sidebarOpen/)` is absent from the file (replaced by the `.not.toMatch` form)
    - The string `'bee-atlas.ts declares _viewMode as @state field'` is absent (replaced)
    - SEL-06 test contains `this._selectionBounds && this._paneState === 'list'`
    - `npm test -- src/tests/bee-atlas.test.ts` exits non-zero because at least the new SM-01 assertions and the renamed SIDE-01/VIEW-02 assertions fail against unchanged `bee-atlas.ts` (RED state proven)
    - No other describe blocks in the test file were modified
  </acceptance_criteria>
  <done>Test file expresses the Phase 106 target shape; `npm test` for this file fails with assertions naming `_paneState` (RED phase complete).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Refactor bee-atlas.ts to single _paneState field, remove Phase 105 adapters (GREEN)</name>
  <files>src/bee-atlas.ts</files>
  <read_first>
    - src/bee-atlas.ts (full file — 1067 lines; planner mapped 40 read/write sites — see RESEARCH.md "Event Handler Mapping")
    - .planning/phases/106-bee-atlas-state-machine/106-RESEARCH.md (sections "Architecture Patterns", "Event Handler Mapping", "Common Pitfalls" — especially Pitfall 2 about returning to map → collapsed, and Pitfall 6 about the `_pushUrlState` selectionBounds guard)
    - .planning/phases/106-bee-atlas-state-machine/106-PATTERNS.md (every "TARGET" block — these are the exact substitutions to apply)
    - src/url-state.ts (read just enough to confirm `UiState.paneState` typing and `buildParams` signature — already used in Phase 105)
    - src/bee-filter-panel.ts (confirm `setOpen()` is exposed and `externalOpen` removal is safe — check whether any other call sites still set `.externalOpen`)
  </read_first>
  <behavior>
    After this task, `bee-atlas.ts` exposes a single `@state() private _paneState: 'collapsed' | 'list' | 'table' = 'collapsed';` and contains zero references to `_viewMode` or `_sidebarOpen`. `_tableFilterOpen` remains as a non-reactive `private _tableFilterOpen = false;`. The Phase 105 adapter ternaries in `firstUpdated` and `_onPopState` and the derived-paneState block in `_pushUrlState` are gone. All event handlers that previously set the three flags now set `_paneState` to the correct discriminant. User-visible behavior is unchanged: opening the sidebar via cluster/occurrence click, closing it via the X button, drawing a selection rectangle, navigating regions/places, switching between map and table view, browser back/forward — all match Phase 105 behavior. The test suite goes GREEN: all SM-01 tests pass, all renamed SIDE-01/VIEW-02/SEL-06 tests pass, and no other tests regress.
  </behavior>
  <action>
    Apply the refactor described in `106-PATTERNS.md` and `106-RESEARCH.md` to `src/bee-atlas.ts`. Concrete substitutions, with target identifiers:

    Field declarations (around lines 36, 53–54):
    - Delete `@state() private _viewMode: 'map' | 'table' = 'map';`
    - Delete `@state() private _sidebarOpen = false;`
    - Replace `@state() private _tableFilterOpen = false;` with `private _tableFilterOpen = false;` (no decorator — demoted to plain private field)
    - Add `@state() private _paneState: 'collapsed' | 'list' | 'table' = 'collapsed';` in the same @state cluster

    render() template (around lines 167, 175–176, 196, 218–220, 223):
    - `bee-header .viewMode`: change from `${this._viewMode}` to `${this._paneState === 'table' ? 'table' : 'map'}` (keep bee-header API unchanged — Option A from RESEARCH.md)
    - CSS class expression: replace `this._viewMode === 'table' ? 'table-mode' : ''` with `this._paneState === 'table' ? 'table-mode' : ''`; replace `this._viewMode === 'map' && this._sidebarOpen ? 'sidebar-open' : ''` with `this._paneState === 'list' ? 'sidebar-open' : ''`
    - `bee-table` conditional at line 196: `${this._paneState === 'table' ? html\`<bee-table ...>\` : nothing}`
    - `bee-filter-panel` props: change `.hideButton` and `.openUpward` to read `this._paneState === 'table'`; DELETE the `.externalOpen=${this._tableFilterOpen}` binding entirely
    - `bee-sidebar` conditional at line 223: `${this._paneState === 'list' ? html\`<bee-sidebar ...>\` : nothing}`

    firstUpdated (around lines 244–247):
    - Replace `this._viewMode = paneState === 'table' ? 'table' : 'map';` with `this._paneState = paneState;`
    - Leave the `if (paneState === 'table') import('./bee-table.ts');` line as-is

    All `_sidebarOpen = true` sites (lines 283, 287, 289, 568, 575, 583, 624, 724, 755, 1036):
    - Replace each `this._sidebarOpen = true;` with `this._paneState = 'list';`

    All `_sidebarOpen = false` sites (lines 590, 685, 710, 737, 774, 786, 823, 908) — but NOT line 842:
    - Replace each `this._sidebarOpen = false;` with `this._paneState = 'collapsed';`
    - Line 842 (`this._sidebarOpen = false;` inside `_onViewChanged` table-entry branch) is DELETED because `_paneState = 'table'` already implies sidebar closed

    _onPopState (around lines 554–558):
    - Replace `this._viewMode = paneState === 'table' ? 'table' : 'map';` with `this._paneState = paneState;`
    - Update the guard `if (this._viewMode === 'table')` to `if (this._paneState === 'table')`

    _pushUrlState (around lines 500–514):
    - Delete the derived `const paneState: 'list' | 'table' | 'collapsed' = ...` ternary block
    - Update the selectionBounds guard from `this._selectionBounds && this._sidebarOpen` to `this._selectionBounds && this._paneState === 'list'`
    - Pass `paneState: this._paneState` directly in the `ui` argument to `buildParams` (the third positional arg's `ui` object: `{ boundaryMode: this._boundaryMode, paneState: this._paneState }`)

    _runTableQuery (line 467) and _onDataLoaded (line 929):
    - Replace `if (this._viewMode !== 'table') return;` with `if (this._paneState !== 'table') return;`
    - Replace `if (this._viewMode === 'table')` reads (lines 308, 558, 625) with `if (this._paneState === 'table')`

    _onViewChanged (around lines 832–847) — full rewrite per 106-PATTERNS.md TARGET block:
    - Set `this._paneState = 'table'` (not `this._viewMode = 'table'`) when `e.detail === 'table'`
    - In the else branch (`e.detail === 'map'`), set `this._paneState = 'collapsed'` (Pitfall 2: NOT `'list'`)
    - Keep the dynamic `import('./bee-table.ts')` call inside the table branch
    - Keep `this._tableFilterOpen = false;` inside the table branch (it's now a plain field assignment, still effective)
    - Keep `this._pushUrlState()` at end

    _onToggleFilter (around lines 849–852):
    - Body remains identical: toggle `this._tableFilterOpen` and call `setOpen(this._tableFilterOpen)` on the `bee-filter-panel` query. (The field is no longer `@state`, but imperative `setOpen()` still drives DOM.)

    _onClose (around lines 903–910):
    - Replace `this._sidebarOpen = false;` with `this._paneState = 'collapsed';`

    Pitfall checks before declaring done:
    - `grep -nE "(_viewMode|_sidebarOpen)" src/bee-atlas.ts` MUST return zero lines (the deleted fields and all reads/writes are gone)
    - `grep -nE "@state\(\)\s+private\s+_tableFilterOpen" src/bee-atlas.ts` MUST return zero lines (demoted)
    - `grep -nE "paneState\s*===\s*'table'\s*\?\s*'table'\s*:\s*'map'" src/bee-atlas.ts` MUST return zero lines (Phase 105 adapter removed in firstUpdated and _onPopState)
    - Verify dynamic `import('./bee-table.ts')` exists in BOTH `firstUpdated` (existing line 247) and `_onViewChanged` (Pitfall 3)
  </action>
  <verify>
    <automated>grep -cE "(_viewMode|_sidebarOpen)" src/bee-atlas.ts | grep -q '^0$' && grep -cE "@state\(\)\s+private\s+_tableFilterOpen" src/bee-atlas.ts | grep -q '^0$' && npx tsc --noEmit && npm test 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - `grep -cE "(_viewMode|_sidebarOpen)" src/bee-atlas.ts` returns `0`
    - `grep -c "@state() private _paneState" src/bee-atlas.ts` returns `1`
    - `grep -c "'collapsed' | 'list' | 'table'" src/bee-atlas.ts` returns at least `1`
    - `grep -cE "@state\(\)\s+private\s+_tableFilterOpen" src/bee-atlas.ts` returns `0` (plain field allowed)
    - `grep -c "private _tableFilterOpen = false" src/bee-atlas.ts` returns `1`
    - `grep -cE "paneState\s*===\s*'table'\s*\?\s*'table'\s*:\s*'map'" src/bee-atlas.ts` returns `0` (no Phase 105 adapter remains)
    - `grep -c "paneState: this._paneState" src/bee-atlas.ts` returns at least `1` (direct pass to buildParams)
    - `grep -c "this._selectionBounds && this._paneState === 'list'" src/bee-atlas.ts` returns `1`
    - `grep -c ".externalOpen" src/bee-atlas.ts` returns `0` (binding dropped)
    - `npx tsc --noEmit` exits `0`
    - `npm test` exits `0` with all SM-01 tests passing and the prior 448-baseline test count preserved (no regressions)
    - The SIDE-01, VIEW-02, and SEL-06 tests updated in Task 1 all pass
    - Dynamic `import('./bee-table.ts')` is reachable inside `_onViewChanged` when `e.detail === 'table'` AND inside `firstUpdated` when `paneState === 'table'`
  </acceptance_criteria>
  <done>`bee-atlas.ts` uses `_paneState` exclusively; `npm test` and `tsc --noEmit` both green; user-visible behavior identical to Phase 105.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none introduced this phase) | Pure internal TypeScript refactor; no new network, DOM injection, auth, or URL-parsing surface |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-106-01 | Tampering | `bee-atlas.ts` runtime state | accept | Refactor preserves existing behavior; URL parsing in `url-state.ts` (Phase 105) already validates `paneState` against the `'collapsed' \| 'list' \| 'table'` union; invalid values fall through to `'collapsed'` default — unchanged in this phase |
| T-106-02 | Information Disclosure | URL pane state | accept | `paneState` is already serialized to URL since Phase 105 and contains no PII; no change to disclosure surface |

**Package legitimacy:** No npm/pip/cargo installs occur in this phase. No `[ASSUMED]`/`[SUS]` packages — RESEARCH.md confirms zero new dependencies. T-{phase}-SC not applicable.

ASVS L1 categories applicable: none. This is an internal field-rename refactor with no external inputs.
</threat_model>

<verification>
End-of-phase checks:

1. `grep -cE "(_viewMode|_sidebarOpen)" src/bee-atlas.ts` returns `0`
2. `grep -c "@state() private _paneState" src/bee-atlas.ts` returns `1`
3. `grep -c "SM-01" src/tests/bee-atlas.test.ts` returns at least `1`
4. `npx tsc --noEmit` exits `0`
5. `npm test` exits `0` (baseline was 448 tests at Phase 105 close; SM-01 adds 7; SIDE-01 and VIEW-02 lose 0 net tests — they are renamed/replaced 1:1; SEL-06 unchanged count; expect ≥455 tests passing)
6. `npm run build` succeeds (full Eleventy + Vite production build, in case the executor wants extra confidence — optional)

Behavioral spot-checks (covered by existing tests; no manual UAT required):
- URL with `?pane=list` opens sidebar (covered by SEL-06 and SIDE-01 assertions plus URL-01 tests from Phase 105)
- URL with `?pane=table` renders the table (covered by VIEW-02 and SM-01)
- Clicking the X close button collapses the pane (SM-01 `_onClose sets _paneState = collapsed`)
- Switching from table back to map collapses the pane, not opens the list (SM-01 `_onViewChanged` + Pitfall 2)
</verification>

<success_criteria>
1. `_viewMode`, `_sidebarOpen`, and `_tableFilterOpen` (as @state field) no longer exist in `bee-atlas.ts` (SC-2 from ROADMAP)
2. All event handlers that previously toggled those flags now set `_paneState` to the correct discriminant (SC-3 from ROADMAP)
3. Application behavior is unchanged from the user's perspective — `npm test` passes with ≥455 tests green, `tsc --noEmit` exits 0 (SC-1 from ROADMAP)
4. The Phase 105 temporary adapters in `firstUpdated`, `_onPopState`, and `_pushUrlState` are removed
5. `.externalOpen` binding on `bee-filter-panel` is dropped; imperative `setOpen()` continues to drive its open state
</success_criteria>

<output>
Create `.planning/phases/106-bee-atlas-state-machine/106-01-SUMMARY.md` when done — record:
- Final `_paneState` shape and confirmation that `_viewMode`/`_sidebarOpen` are gone
- Whether `_tableFilterOpen` was kept as a plain field (expected: yes) or removed entirely
- Number of tests passing post-refactor (baseline 448 + 7 SM-01 = expected ≥455)
- Any deviations from the planned substitution table (Pitfalls 1–6) and how they were resolved
</output>
