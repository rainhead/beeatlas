---
phase: 127-inactive-taxon-remapping
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - data/dbt/seeds/auto_synonyms.csv
  - data/dbt/seeds/schema.yml
  - data/dbt/dbt_project.yml
  - data/dbt/models/intermediate/int_synonyms.sql
  - data/dbt/models/intermediate/int_combined.sql
  - data/dbt/models/staging/stg_checklist__species.sql
  - data/dbt/models/intermediate/int_species_universe.sql
autonomous: true
requirements: [ITR-03, ITR-04]
must_haves:
  truths:
    - "int_synonyms compiles as a view UNIONing occurrence_synonyms with auto_synonyms anti-joined on synonym so manual entries win when the same source name appears in both (ITR-04)"
    - "All four synonym-JOIN call sites resolve via ref('int_synonyms') instead of ref('occurrence_synonyms') (D-03 repoint — the same application path ITR-03 requires)"
    - "bash data/dbt/run.sh build exits 0 with the header-only auto_synonyms.csv seed (0-inactive case, D-04)"
    - "The marts/occurrences 37-column NOT NULL taxon_id contract still holds after the repoint"
    - "The existing agapostemon texanus -> subtilior manual synonymy still appears in occurrences.parquet"
  artifacts:
    - path: "data/dbt/models/intermediate/int_synonyms.sql"
      provides: "UNION-with-precedence synonym model (D-02)"
      contains: "int_synonyms"
    - path: "data/dbt/seeds/auto_synonyms.csv"
      provides: "Committed header-only placeholder seed so dbt seed never breaks on fresh checkout (D-04); a separate gitignored seed physically distinct from curated occurrence_synonyms.csv, regenerated nightly (D-01)"
      contains: "synonym,accepted_name,source"
    - path: "data/dbt/seeds/schema.yml"
      provides: "auto_synonyms seed registration with not_null/unique tests"
      contains: "auto_synonyms"
    - path: "data/dbt/dbt_project.yml"
      provides: "auto_synonyms +column_types: all varchar"
      contains: "auto_synonyms"
  key_links:
    - from: "int_combined.sql / stg_checklist__species.sql / int_species_universe.sql"
      to: "int_synonyms"
      via: "ref('int_synonyms') LEFT JOIN"
      pattern: "ref..int_synonyms.."
    - from: "int_synonyms.sql"
      to: "occurrence_synonyms + auto_synonyms seeds"
      via: "UNION ALL with anti-join on synonym"
      pattern: "WHERE m.synonym IS NULL"
---

<objective>
Build the dbt half of the inactive-taxon safety net: the application path (ITR-03) and manual precedence (ITR-04). Create the int_synonyms UNION model (D-02), register the auto_synonyms seed (D-01), repoint ALL FOUR existing synonym-JOIN call sites from ref('occurrence_synonyms') to ref('int_synonyms') (D-03 plus the RESEARCH-discovered 4th site), and commit a header-only auto_synonyms.csv placeholder so dbt seed works on a fresh checkout (D-04).

Purpose: Auto-generated remappings (produced by the Python step in plan 01, written to the gitignored auto_synonyms.csv) must flow through the exact same name-based synonym JOIN mechanism as the curated occurrence_synonyms.csv — no parallel application path. Manual entries must win when the same source name appears in both, enforced declaratively via an ANTI JOIN on the synonym column.

Output: int_synonyms.sql view; updated schema.yml + dbt_project.yml; four repointed JOIN sites; a committed header-only auto_synonyms.csv. bash data/dbt/run.sh build passes with the 37-column marts contract intact.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/127-inactive-taxon-remapping/127-CONTEXT.md
@.planning/phases/127-inactive-taxon-remapping/127-RESEARCH.md
@.planning/phases/127-inactive-taxon-remapping/127-PATTERNS.md

<interfaces>
The four ref('occurrence_synonyms') JOIN sites to repoint to ref('int_synonyms') (verified by grep; JOIN shape unchanged at every site, only the ref source changes):
  int_combined.sql line 53  alias syn_e  ON syn_e.synonym  = e.canonical_name
  int_combined.sql line 169 alias syn_io ON syn_io.synonym = io.canonical_name
  stg_checklist__species.sql line 31 alias syn ON syn.synonym = s.canonical_name
  int_species_universe.sql line 61 (inside inat_obs_count_agg CTE) alias syn ON syn.synonym = io.canonical_name
  The COALESCE(syn.accepted_name, <arm>.canonical_name) expressions are UNCHANGED.

int_synonyms.sql model shape (D-02 manual precedence via anti-join on synonym):
  config materialized view; SELECT synonym, accepted_name, source FROM ref('occurrence_synonyms')
  UNION ALL SELECT a.synonym, a.accepted_name, a.source FROM ref('auto_synonyms') a
  LEFT JOIN ref('occurrence_synonyms') m ON m.synonym = a.synonym WHERE m.synonym IS NULL

Existing seed registration shape:
  schema.yml occurrence_synonyms entry: columns synonym(not_null,unique), accepted_name(not_null), source(description)
  dbt_project.yml lines 22-28: seeds.beeatlas.occurrence_synonyms.+column_types all varchar

Existing manual seed row (occurrence_synonyms.csv): agapostemon texanus -> agapostemon subtilior (precedence + regression anchor).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add auto_synonyms seed (placeholder CSV + registration) and int_synonyms model</name>
  <files>data/dbt/seeds/auto_synonyms.csv, data/dbt/seeds/schema.yml, data/dbt/dbt_project.yml, data/dbt/models/intermediate/int_synonyms.sql</files>
  <read_first>
    - data/dbt/seeds/schema.yml (existing occurrence_synonyms entry, lines 4-14)
    - data/dbt/dbt_project.yml (seeds section, lines 22-28)
    - data/dbt/seeds/occurrence_synonyms.csv (header + the one manual row)
    - data/dbt/models/intermediate/int_species_universe.sql (CTE/UNION style analog)
    - data/dbt/models/staging/stg_checklist__species.sql (materialized=view analog, line 10)
    - .planning/phases/127-inactive-taxon-remapping/127-PATTERNS.md (int_synonyms + schema.yml + dbt_project.yml sections, lines 159-298)
    - .planning/phases/127-inactive-taxon-remapping/127-CONTEXT.md (D-01, D-02, D-04, ITR-04)
  </read_first>
  <action>
    Create data/dbt/seeds/auto_synonyms.csv containing exactly one line: the header synonym,accepted_name,source and no data rows (D-04 — this committed placeholder guarantees dbt seed works on a fresh checkout / local dev run; the Python step in plan 01 overwrites it nightly and plan 01 adds it to .gitignore). The placeholder must exist on disk at build time; force-track it with git add -f data/dbt/seeds/auto_synonyms.csv so the header-only file is committed despite the gitignore entry plan 01 introduces.

    In data/dbt/seeds/schema.yml, add an auto_synonyms seed entry after the occurrence_synonyms block, same structure: synonym (not_null, unique), accepted_name (not_null), source (description "Auto-generated: 'inat-inactive-remap:{inactive_taxon_id}'").

    In data/dbt/dbt_project.yml seeds.beeatlas section, add an auto_synonyms block with +column_types declaring synonym, accepted_name, source all as varchar (prevents DuckDB inferring numeric types from a header-only CSV — Shared Patterns note).

    Create data/dbt/models/intermediate/int_synonyms.sql as a view: SELECT synonym, accepted_name, source FROM ref('occurrence_synonyms') UNION ALL the auto rows that have no manual match via LEFT JOIN ref('occurrence_synonyms') m ON m.synonym = a.synonym WHERE m.synonym IS NULL (D-02 anti-join: manual wins by exclusion — ITR-04). Add a header comment explaining the precedence rule.
  </action>
  <verify>
    <automated>cd data && test "$(cat dbt/seeds/auto_synonyms.csv)" = "synonym,accepted_name,source" && grep -q "auto_synonyms" dbt/seeds/schema.yml && grep -q "auto_synonyms" dbt/dbt_project.yml && grep -q "WHERE m.synonym IS NULL" dbt/models/intermediate/int_synonyms.sql && echo SEED-MODEL-OK</automated>
  </verify>
  <acceptance_criteria>
    - data/dbt/seeds/auto_synonyms.csv exists and its entire content is the single line synonym,accepted_name,source (no data rows).
    - data/dbt/seeds/schema.yml has an auto_synonyms seed entry with not_null+unique on synonym and not_null on accepted_name.
    - data/dbt/dbt_project.yml declares auto_synonyms.+column_types with all three columns as varchar.
    - data/dbt/models/intermediate/int_synonyms.sql is a view that UNIONs both seeds with the anti-join WHERE m.synonym IS NULL for precedence (grep confirms).
    - The placeholder CSV is git-tracked (git ls-files data/dbt/seeds/auto_synonyms.csv returns the path).
  </acceptance_criteria>
  <done>auto_synonyms seed exists, is registered with varchar column types and tests, is git-tracked as header-only, and int_synonyms.sql implements manual-precedence UNION.</done>
</task>

<task type="auto">
  <name>Task 2: Repoint all four synonym-JOIN sites and verify dbt build + contract</name>
  <files>data/dbt/models/intermediate/int_combined.sql, data/dbt/models/staging/stg_checklist__species.sql, data/dbt/models/intermediate/int_species_universe.sql</files>
  <read_first>
    - data/dbt/models/intermediate/int_combined.sql (lines 43-55 ecdysis arm, 161-171 inat_obs arm)
    - data/dbt/models/staging/stg_checklist__species.sql (lines 17, 31)
    - data/dbt/models/intermediate/int_species_universe.sql (lines 58, 61 — inat_obs_count_agg CTE)
    - data/dbt/models/marts/schema.yml (taxon_id not_null contract, lines 81-84 and 134-137)
    - .planning/phases/127-inactive-taxon-remapping/127-PATTERNS.md (the four repoint sections, lines 187-239)
    - .planning/phases/127-inactive-taxon-remapping/127-RESEARCH.md (4 repoint sites table, lines 430-441; Pitfall 1)
  </read_first>
  <action>
    Repoint all FOUR LEFT JOIN call sites from ref('occurrence_synonyms') to ref('int_synonyms') — ref source only; the alias names (syn_e, syn_io, syn) and the JOIN/COALESCE expressions are unchanged (D-03 plus the 4th site found in RESEARCH; Pitfall 1):
    - int_combined.sql line 53 (alias syn_e) and line 169 (alias syn_io)
    - stg_checklist__species.sql line 31 (alias syn)
    - int_species_universe.sql line 61 inside inat_obs_count_agg (alias syn)
    After repointing, confirm zero remaining ref('occurrence_synonyms') references in models/ except inside int_synonyms.sql (occurrence_synonyms is now consumed ONLY by int_synonyms). Run a full bash data/dbt/run.sh build to confirm the seed loads (header-only auto_synonyms), int_synonyms materializes, all four consumers compile and run, and the marts/occurrences 37-column NOT NULL taxon_id contract still passes. The agapostemon texanus -> subtilior manual mapping must still flow through (it now arrives via int_synonyms' manual arm).
  </action>
  <verify>
    <automated>cd data && test "$(grep -rl "ref('occurrence_synonyms')" dbt/models/ | grep -v int_synonyms | wc -l)" = "0" && bash dbt/run.sh build && echo BUILD-OK</automated>
  </verify>
  <acceptance_criteria>
    - No model under data/dbt/models/ references ref('occurrence_synonyms') except int_synonyms.sql (the grep -rl ... | grep -v int_synonyms | wc -l == 0 check).
    - int_combined.sql contains exactly 2 ref('int_synonyms') references (syn_e + syn_io).
    - stg_checklist__species.sql and int_species_universe.sql each contain one ref('int_synonyms') reference.
    - bash data/dbt/run.sh build exits 0.
    - The marts/occurrences taxon_id NOT NULL contract passes (build does not fail on the 37-column contract).
    - agapostemon texanus -> agapostemon subtilior still appears in the built occurrences output (manual arm preserved through int_synonyms).
  </acceptance_criteria>
  <done>All four sites repointed, no stray occurrence_synonyms refs in consumers, dbt build green with the 37-column contract intact and the manual synonymy preserved.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| auto_synonyms.csv -> dbt seed | Gitignored, nightly-overwritten CSV authored by the plan-01 Python step from iNat-sourced names; loaded as a dbt seed and UNIONed into the synonym JOIN path |
| seed -> int_synonyms -> marts | Synonym names flow into COALESCE expressions that rewrite canonical_name across all occurrence sources |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-127-06 | Tampering | CSV-injection via auto_synonyms.csv field content | mitigate | dbt seed loads via DuckDB read_csv with declared varchar +column_types — values are data, never executed; int_synonyms uses parameterless ref() JOINs (no string interpolation of seed values into SQL). A malicious/odd name can at worst produce a wrong synonym mapping, which surfaces as a wrong (still non-null) taxon_id rather than code execution. |
| T-127-07 | Spoofing/Integrity | auto entry overriding a curated manual decision | mitigate | int_synonyms anti-join (WHERE m.synonym IS NULL) guarantees manual occurrence_synonyms always wins on a shared synonym key (ITR-04); manual curation is the authoritative taxonomic judgment per D-07. Verified by the agapostemon texanus regression anchor. |
| T-127-08 | Denial of Service | empty/header-only seed breaks dbt build | accept | D-04 header-only placeholder is committed and verified; RESEARCH A1/A3 (header-only seed -> 0-row table, downstream LEFT JOIN falls through to COALESCE) confirmed by the build task. If dbt errored on the empty seed, this task's build verify would catch it. |
| T-127-SC | Tampering | npm/pip/cargo installs | mitigate | No new packages introduced (RESEARCH Package Legitimacy Audit: not applicable). No install tasks in this plan. |
</threat_model>

<verification>
- bash data/dbt/run.sh build exits 0 with header-only auto_synonyms seed.
- grep -rl "ref('occurrence_synonyms')" data/dbt/models/ returns only int_synonyms.sql.
- The marts/occurrences 37-column NOT NULL taxon_id contract still passes.
- agapostemon texanus -> subtilior manual mapping still present in the built output.
</verification>

<success_criteria>
- ITR-03: auto-generated remappings apply through the same synonym JOIN path as occurrence_synonyms (now via int_synonyms at all four sites).
- ITR-04: manual entries take precedence over auto entries on a shared synonym (anti-join enforced, regression anchor green).
- D-04: dbt seed never breaks on a 0-inactive run (committed header-only placeholder).
</success_criteria>

<output>
Create .planning/phases/127-inactive-taxon-remapping/127-02-SUMMARY.md when done. Record: the four repointed sites; confirmation that occurrence_synonyms is now consumed only by int_synonyms; the dbt build result and contract status; and that the agapostemon texanus regression anchor still passes.
</output>
