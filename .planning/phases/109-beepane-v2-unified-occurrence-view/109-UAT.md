---
status: diagnosed
phase: 109-beepane-v2-unified-occurrence-view
source: 109-01-SUMMARY.md, 109-02-SUMMARY.md, 109-03-SUMMARY.md, 109-04-SUMMARY.md
started: 2026-05-20T19:17:00Z
updated: 2026-05-20T19:28:00Z
---

## Current Test

[testing complete]

## Tests

### 1. Selection banner
expected: Click a cluster on the map — the list pane opens and a selection banner appears showing the count and a Clear button. Clicking Clear removes the banner and resets to the full list.
result: pass
notes: Banner appeared with count and Clear button.
issue: List pane overflows the viewport vertically — the page becomes taller than the viewport. The pane content (filter panel + occurrence list + pager) is not constrained to the pane's 60% height. Severity: major.

### 2. Filter-btn active state
expected: Open the list pane, type a species name (e.g. "Apis") in the Species filter, then click the X to close the pane. The floating filter button should turn green (accent color) to indicate a filter is active. Clearing the filter should return it to its default white/neutral appearance.
result: pass
notes: Filter-btn turns green after genus selected. Two layout bugs observed alongside this test.
issues:
  - Pane closes automatically after selecting the genus from autocomplete — pane should stay open to show filtered results. Severity: major.
  - X close button (.pane-close, absolutely positioned top-right) overlaps the Expand to table view button in the sidebar-header. Severity: major.

### 3. Collapsed filter-btn renders with count
expected: On page load (no pane open), a floating button with a magnifying-glass icon and specimen count ("N specimens") is visible in the lower-right area of the map. It is not green/active.
result: pass
notes: Auto-verified — "Toggle occurrence pane" button shows "46092 specimens"; no active class present.

### 4. Filter-btn opens list pane
expected: Clicking the filter button opens the list pane (URL gets pane=list, pane slides into view with filter controls and occurrence list).
result: pass
notes: Auto-verified — URL changed to ?pane=list; list content loaded.

### 5. X close button closes pane
expected: In list state, an X (✕) button in the top-right of the pane closes it, returning to the collapsed filter-btn. URL drops pane=list.
result: pass
notes: Auto-verified — "Close pane" ✕ button present; clicking it returned URL to collapsed state.

### 6. Occurrence list shows paged records
expected: List pane displays occurrence records (date, collector, plant species, specimen count, iNat link), 100 per page.
result: pass
notes: Auto-verified — 100 records loaded; first page showed recent observations with all expected fields.

### 7. Pagination — Next page
expected: When total results exceed 100, Prev/Next buttons and a page counter ("1 / N") appear. Clicking Next advances to page 2; Prev is disabled on page 1.
result: pass
notes: Auto-verified — showed "1 / 479"; Prev disabled; after clicking Next showed "2 / 479".

### 8. Expand to table view button
expected: In list state, the pane header includes an expand button (⊞) to switch to the full table view.
result: pass
notes: Auto-verified — "Expand to table view" button present in sidebar-header.

### 9. Header cleanup — no table/map toggle icons
expected: The bee-header shows only: BeeAtlas heading, Species index link, Places link, GitHub link. No table icon or map icon toggle buttons.
result: pass
notes: Auto-verified — snapshot confirms exactly: heading + 3 links, no toggle buttons.

## Summary

total: 9
passed: 9
issues: 3
pending: 0
skipped: 0
blocked: 0

## Gaps

- truth: "List pane content is contained within the pane's 60%-height bounds; the page does not scroll"
  status: failed
  reason: "User reported: list overflows the pane vertically, creating a page that is taller than the viewport"
  severity: major
  test: 1
  root_cause: "bee-atlas.ts .content.pane-list bee-pane has no max-height; bee-pane :host has no overflow:hidden; the scroll area (filter panel + rows) has no flex:1 min-height:0 overflow-y:auto so content pushes pane beyond viewport"
  artifacts:
    - path: "src/bee-pane.ts"
      issue: ":host missing overflow:hidden; scroll area missing flex:1 min-height:0 overflow-y:auto"
    - path: "src/bee-atlas.ts"
      issue: ".content.pane-list bee-pane missing max-height"
  missing:
    - "Add max-height: calc(100% - 1em) to .content.pane-list bee-pane in bee-atlas.ts"
    - "Add overflow:hidden to :host in bee-pane.ts"
    - "Add flex:1; min-height:0; overflow-y:auto to the filter-panel + occurrence list scroll area"
  debug_session: ""

- truth: "After selecting a genus from autocomplete, the pane remains open showing filtered results"
  status: failed
  reason: "User reported: the pane hides after selecting the genus"
  severity: major
  test: 2
  root_cause: "_onFilterChanged in bee-atlas.ts unconditionally sets this._paneState = 'collapsed' on every filter-changed event, including those originating from within the pane's own inline filter panel. When pane is already in 'list' state the collapse is wrong."
  artifacts:
    - path: "src/bee-atlas.ts"
      issue: "_onFilterChanged sets _paneState = 'collapsed' unconditionally (lines ~808-810)"
  missing:
    - "Guard the collapse: only set _paneState = 'collapsed' if _paneState !== 'list'"
  debug_session: ""

- truth: "The X close button and the Expand to table view button do not overlap"
  status: failed
  reason: "User reported: the close button rides above the table button"
  severity: major
  test: 2
  root_cause: ".pane-close is position:absolute top:0.4rem right:0.4rem on :host, landing directly over the .expand-btn in .sidebar-header which is also at the top-right. Both buttons occupy the same space; z-index:2 on pane-close hides expand-btn."
  artifacts:
    - path: "src/bee-pane.ts"
      issue: ".pane-close position:absolute top:0.4rem right:0.4rem overlaps .sidebar-header expand-btn; pane-close rendered outside sidebar-header"
  missing:
    - "Move .pane-close inside _renderListContent .sidebar-header as a flex item; remove position:absolute from .pane-close"
  debug_session: ""
