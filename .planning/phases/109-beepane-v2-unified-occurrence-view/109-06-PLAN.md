---
phase: 109-beepane-v2-unified-occurrence-view
plan: "06"
type: execute
wave: 6
depends_on: ["109-05"]
files_modified:
  - src/bee-atlas.ts
autonomous: true
requirements: [TABLE-02]
gap_closure: true
source: 109-VERIFICATION.md

must_haves:
  truths:
    - "After selecting a genus from autocomplete while the list pane is open, the occurrence list immediately updates to show only matching occurrences"
  artifacts:
    - path: "src/bee-atlas.ts"
      provides: "_onFilterChanged calls _runListQuery() and resets _listPage when _paneState === 'list'"
  key_links:
    - from: "src/bee-atlas.ts _onFilterChanged"
      to: "_runListQuery()"
      via: "if (this._paneState === 'list') block"
      pattern: "_paneState === 'list'"
---

<objective>
Fix the single remaining VERIFICATION.md gap: when the user changes a filter while the list pane is open, the occurrence list stays stale because _onFilterChanged never calls _runListQuery().

Plan 05 added a guard that keeps the pane open (if (this._paneState !== 'list') this._paneState = 'collapsed') but did not add the corresponding list refresh. This plan adds that missing call.

Output: src/bee-atlas.ts with _onFilterChanged calling _runListQuery() when pane is in list state.
</objective>

<execution_context>
@/Users/rainhead/.claude/get-shit-done/workflows/execute-plan.md
@/Users/rainhead/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/rainhead/dev/beeatlas/.planning/PROJECT.md
@/Users/rainhead/dev/beeatlas/.planning/ROADMAP.md
@/Users/rainhead/dev/beeatlas/.planning/phases/109-beepane-v2-unified-occurrence-view/109-VERIFICATION.md

<interfaces>
<!-- bee-atlas.ts _onFilterChanged (current state after plan 05) -->
// Clear selections when filter changes
this._selectedOccIds = null;
this._selectedCluster = null;
this._selectionBounds = null;
if (this._paneState !== 'list') this._paneState = 'collapsed';

this._tablePage = 1;
this._runFilterQuery().then(() => {
  this._replaceUrlState();
});
this._runTableQuery();

<!-- _runListQuery signature (src/bee-atlas.ts) -->
private async _runListQuery(): Promise<void>
<!-- _listPage field -->
private _listPage: number = 1;
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add _runListQuery() call to _onFilterChanged when pane is in list state</name>
  <read_first>
    - src/bee-atlas.ts (read _onFilterChanged in full; find current line of the plan-05 guard)
  </read_first>
  <action>
In src/bee-atlas.ts, in _onFilterChanged, immediately after the existing line:

  if (this._paneState !== 'list') this._paneState = 'collapsed';

add:

  if (this._paneState === 'list') { this._listPage = 1; this._runListQuery(); }

The two guards are complementary and together handle all pane states:
- pane is NOT in list state → collapse it (existing line)
- pane IS in list state → stay open and refresh the list (new line)

No other changes. Do not modify the selection-clearing lines above, the _tablePage reset, _runFilterQuery, or _runTableQuery calls.

Commit message: `fix(109-06): refresh list on filter change when pane is open`
  </action>
  <acceptance_criteria>
    - `grep -n "_paneState === 'list'" src/bee-atlas.ts` returns exactly one match inside _onFilterChanged containing `_runListQuery()`
    - `grep -n "_listPage = 1" src/bee-atlas.ts` returns a match in _onFilterChanged (the new reset line)
    - The existing line `if (this._paneState !== 'list') this._paneState = 'collapsed'` is still present immediately before the new block
    - `npm test` passes with 0 failures
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| filter-changed event | bee-pane emits internal FilterState; bee-atlas consumes — no external/user-provided data crosses this path |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-109-06-01 | Tampering | _runListQuery call | accept | Pure internal state mutation; FilterState is already validated by the time _onFilterChanged fires |
</threat_model>

<verification>
```
cd /Users/rainhead/dev/beeatlas && npm test && npx tsc --noEmit
```

Both must exit 0. Additionally verify the fix is in place:
```
grep -n "_paneState === 'list'" src/bee-atlas.ts
```
Should return one match in _onFilterChanged with `_runListQuery()`.
</verification>

<success_criteria>
- `npm test` passes with 0 failures
- `tsc --noEmit` exits 0
- `grep "_paneState === 'list'" src/bee-atlas.ts` returns exactly one match in _onFilterChanged containing `_runListQuery()`
- `grep "_listPage = 1" src/bee-atlas.ts` returns a match in _onFilterChanged (page reset before list refresh)
</success_criteria>

<output>
Create `.planning/phases/109-beepane-v2-unified-occurrence-view/109-06-SUMMARY.md` when done.
</output>
