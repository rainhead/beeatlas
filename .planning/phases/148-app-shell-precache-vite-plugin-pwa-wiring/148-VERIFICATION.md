---
phase: 148-app-shell-precache-vite-plugin-pwa-wiring
verified: 2026-06-13T21:00:00Z
status: passed
score: 12/12 must-haves verified
overrides_applied: 0
---

# Phase 148: App Shell Precache + vite-plugin-pwa Wiring — Verification Report

**Phase Goal:** vite-plugin-pwa is wired via eleventy.config.js viteOptions.plugins (not vite.config.ts) with injectManifest strategy; the hashed JS/CSS for the /app Vite entry point is precached; maximumFileSizeToCacheInBytes is raised to at least 30 MB; and the /app page loads fully offline from the SW cache after a single online visit.
**Verified:** 2026-06-13T21:00:00Z
**Status:** passed
**Re-verification:** No — initial verification

## Goal Achievement

### Observable Truths

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | After one online visit to /app, the /app UI loads fully offline from the SW cache (OFF-01) | VERIFIED | 148-HUMAN-UAT.md records PASSED 2026-06-14; browser confirmed SW activated for /app/, JS/CSS served from (ServiceWorker) offline, no SW registered for / |
| 2 | _site/app/sw.js contains an injected precache manifest with hash-versioned URLs, not the literal self.__WB_MANIFEST placeholder (criterion 1) | VERIFIED | Build-output test "OFF-01, criterion 1" PASSES; grep confirms 11 "url":"..." entries present; self.__WB_MANIFEST absent from built file |
| 3 | DevTools Network (offline) shows /app JS/CSS served from (ServiceWorker) with no network errors; recorded as a manual HUMAN-UAT item (criterion 2, D-09) | VERIFIED | 148-HUMAN-UAT.md: status: complete, 1/1 passed, all criteria confirmed including no SW on / |
| 4 | maximumFileSizeToCacheInBytes is set to >= 30000000 in eleventy.config.js (criterion 3, D-03) | VERIFIED | eleventy.config.js line 112: `maximumFileSizeToCacheInBytes: 30_000_000`; build-output test "OFF-01, criterion 3" PASSES |
| 5 | Every precached URL in _site/app/sw.js exists as a file under _site/ (criterion 4) | VERIFIED | Build-output test "OFF-01, criterion 4" PASSES; 11 precache URLs verified on disk |
| 6 | Criteria 1, 3, 4 are proven by assertions added to src/tests/build-output.test.ts (NOT a standalone script), per D-08 | VERIFIED | Three test() blocks named "(OFF-01, criterion 1)", "(OFF-01, criterion 3)", "(OFF-01, criterion 4)" exist in src/tests/build-output.test.ts (lines 326–352); all pass in the describe.skipIf(SKIP_BUILD) block |
| 7 | The precache manifest covers app/index.html and assets/**/*.{js,css} and excludes data/feeds/db/geojson/parquet/png via globIgnores (D-02) | VERIFIED | eleventy.config.js lines 107–110: globPatterns includes 'app/index.html','assets/**/*.{js,css}'; globIgnores covers data/**, feeds/**, *.db, *.geojson, *.parquet, *.png, **/sw.js; built sw.js shows only /app/index.html and /assets/ URLs |
| 8 | vite-plugin-pwa is wired through eleventy.config.js viteOptions.plugins (NOT vite.config.ts), per D-01 | VERIFIED | eleventy.config.js lines 12–13 import VitePWA and resolve; VitePWA({...}) is in viteOptions.plugins (lines 95–119); no VitePWA reference in vite.config.ts |
| 9 | The Phase 147 stub public/app/sw.js is removed; the plugin generates the SW at the stable unhashed URL /app/sw.js (D-01) | VERIFIED | public/app/ directory does not exist; _site/app/sw.js is generated by the plugin (confirmed by injected manifest present) |
| 10 | src/sw.ts registers a NavigationRoute(createHandlerBoundToURL('/app/index.html')) allowlisted to /^\/app\// only, so it never shadows the main / site (D-05) | VERIFIED | src/sw.ts lines 33–37: `createHandlerBoundToURL('/app/index.html')`, `new NavigationRoute(handler, { allowlist: [/^\/app\//] })`, `registerRoute(navigationRoute)` |
| 11 | The Phase 147 src/sw-registration.ts is kept and the plugin sets injectRegister: null and manifest: false (D-06, D-07) | VERIFIED | sw-registration.ts line 16: `navigator.serviceWorker.register('/app/sw.js', { scope: '/app/' })` unchanged; eleventy.config.js lines 102–103: `injectRegister: null`, `manifest: false` |
| 12 | No skipWaiting and no clientsClaim appear in src/sw.ts (D-04 lifecycle invariant) | VERIFIED | grep for skipWaiting/clientsClaim in src/sw.ts returns only the comment "D-04: NO skipWaiting, NO clients.claim." — no actual calls present |

**Score:** 12/12 truths verified

### Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `src/sw.ts` | injectManifest SW source: precacheAndRoute(self.__WB_MANIFEST) + /app-allowlisted NavigationRoute | VERIFIED | 37 lines; imports precacheAndRoute, createHandlerBoundToURL from workbox-precaching; NavigationRoute, registerRoute from workbox-routing; calls precacheAndRoute(self.__WB_MANIFEST); NavigationRoute with allowlist [/^\/app\//]; no skipWaiting/clientsClaim |
| `eleventy.config.js` | VitePWA injectManifest plugin in viteOptions.plugins with 30MB cap and absolute outDir/globDirectory/swDest | VERIFIED | Lines 95–119: VitePWA({ strategies:'injectManifest', srcDir:'src', filename:'sw.ts', outDir: resolve(process.cwd(),...), injectRegister: null, manifest: false, injectManifest: { globDirectory: resolve(...), swDest: resolve(...), maximumFileSizeToCacheInBytes: 30_000_000, modifyURLPrefix: {'': '/'} } }) |
| `src/tests/build-output.test.ts` | Three build-output assertions for OFF-01 criteria 1, 3, 4 | VERIFIED | Lines 326–352: three test() blocks with "(OFF-01, criterion 1/3/4)" names; all 36 tests in file pass including these three and the pre-existing Phase 147 D-04 assertion |

### Key Link Verification

| From | To | Via | Status | Details |
|------|----|-----|--------|---------|
| eleventy.config.js viteOptions.plugins | src/sw.ts (compiled to _site/app/sw.js) | VitePWA({ strategies:'injectManifest', srcDir:'src', filename:'sw.ts', outDir: resolve(process.cwd(),'_site/app') }) | WIRED | VitePWA call present; build produces _site/app/sw.js with injected manifest; 11 precache entries confirmed |
| src/sw.ts precacheAndRoute(self.__WB_MANIFEST) | _site/ hashed app-shell assets | workbox-build injectManifest scan of globDirectory=_site, globPatterns=['app/index.html','assets/**/*.{js,css}'], modifyURLPrefix:{'':'/'}  | WIRED | Built sw.js contains "url":"/app/index.html" and 10 /assets/ hash-versioned URLs; criterion-4 test confirms all exist on disk |
| src/sw-registration.ts (unchanged) | /app/sw.js (plugin-generated) | navigator.serviceWorker.register('/app/sw.js', { scope: '/app/' }) | WIRED | sw-registration.ts line 16 unchanged; plugin generates the file at that URL; HUMAN-UAT confirms SW activates for /app/ scope |

### Data-Flow Trace (Level 4)

Not applicable — this phase produces a compiled service worker and build-output test assertions, not a React/Vue/Svelte component that renders dynamic data from a state variable. The SW itself is the data-flow artifact; its data flow is verified by the build-output tests (criterion 1: manifest injected, criterion 4: all URLs on disk) and the HUMAN-UAT (criterion 2: offline navigation serves from SW cache).

### Behavioral Spot-Checks

| Behavior | Command | Result | Status |
|----------|---------|--------|--------|
| OFF-01 criterion 1: _site/app/sw.js has injected manifest | `npx vitest run src/tests/build-output.test.ts` | 36/36 passed | PASS |
| OFF-01 criterion 3: maximumFileSizeToCacheInBytes >= 30_000_000 | `npx vitest run src/tests/build-output.test.ts` | 36/36 passed | PASS |
| OFF-01 criterion 4: every precached URL exists in _site/ | `npx vitest run src/tests/build-output.test.ts` | 36/36 passed | PASS |
| D-04: no skipWaiting/clientsClaim in src/sw.ts | `grep -E "skipWaiting\|clientsClaim" src/sw.ts` | Only comment line, no calls | PASS |
| stub removed: public/app/sw.js absent | `ls /home/peter/dev/beeatlas/public/app/ 2>/dev/null` | directory absent | PASS |
| 11 real precache URLs with leading / in built SW | `grep -o '"url":"[^"]*"' _site/app/sw.js \| head -10` | /app/index.html, /assets/... (10 hashed chunks) | PASS |

### Probe Execution

No probe scripts declared for this phase. The CI gate is the build-output test suite.

### Requirements Coverage

| Requirement | Source Plan | Description | Status | Evidence |
|-------------|------------|-------------|--------|----------|
| OFF-01 | 148-01-PLAN.md | App shell (hashed JS/CSS for /app entry) precached via vite-plugin-pwa injectManifest, wired through eleventy.config.js viteOptions.plugins; /app UI loads fully offline | SATISFIED | All four success criteria met: criterion 1 (automated test pass), criterion 2 (HUMAN-UAT PASSED 2026-06-14), criterion 3 (automated test pass, 30_000_000 in config), criterion 4 (automated test pass, all 11 URLs on disk) |

### Anti-Patterns Found

| File | Line | Pattern | Severity | Impact |
|------|------|---------|----------|--------|
| src/sw.ts | 18–20 | `declare const self: ...` ambient declaration | Info | Not a stub — required workaround for tsconfig not including vite-plugin-pwa/client types; functional, documented in SUMMARY.md deviation 3 |

No TBD, FIXME, or XXX markers found in phase-modified files. No stub returns. No empty handlers.

### Human Verification Required

(None — all human verification was completed during phase execution.)

The HUMAN-UAT item for OFF-01 criterion 2 was completed and recorded in 148-HUMAN-UAT.md (status: complete, 1/1 passed, 2026-06-14). The phase is fully verified.

### Out-of-Scope Failure Note

The full `npm test` suite currently has one failing test: `src/tests/data-species.test.ts` "Bombus genus specimen_count >= sum of leaves (D-08)". This is a data-rollup assertion on `public/data/species.json` (generated by the nightly pipeline cron, not checked in). It is entirely unrelated to Phase 148's service-worker and build-output changes. The three OFF-01 build-output assertions in `src/tests/build-output.test.ts` all PASS. This orthogonal data-freshness failure does not affect Phase 148 goal achievement.

### Gaps Summary

No gaps. All 12 must-haves verified. OFF-01 satisfied across all four success criteria (3 automated, 1 HUMAN-UAT). Phase goal achieved.

---

_Verified: 2026-06-13T21:00:00Z_
_Verifier: Claude (gsd-verifier)_
