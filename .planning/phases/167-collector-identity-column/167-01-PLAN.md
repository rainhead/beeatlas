---
phase: 167-collector-identity-column
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - data/dbt/models/intermediate/int_combined.sql
  - data/dbt/models/marts/occurrences.sql
  - data/dbt/models/marts/schema.yml
autonomous: false
requirements: [IDENT-01]

must_haves:
  truths:
    - "occurrences.parquet carries a collector_inat_login VARCHAR column (dbt contract 36→37)"
    - "Every waba_sample and waba_specimen row has a non-null collector_inat_login (D-05 hard-error, 0 violations today)"
    - "The ecdysis NULL collector_inat_login count surfaces as a warn-only drift metric each build (D-06, baseline ~2,767)"
    - "occurrences.db (via sqlite_export.py) carries the collector_inat_login column without any edit to sqlite_export.py"
    - "After the one-time SKIP_INTEGRATION_GATE nightly, the live S3 occurrences.parquet carries collector_inat_login (37 cols)"
  artifacts:
    - path: "data/dbt/models/intermediate/int_combined.sql"
      provides: "collector_inat_login COALESCE derived in all 5 UNION ALL arms"
      contains: "collector_inat_login"
    - path: "data/dbt/models/marts/occurrences.sql"
      provides: "collector_inat_login projected through the final SELECT"
      contains: "j.collector_inat_login"
    - path: "data/dbt/models/marts/schema.yml"
      provides: "collector_inat_login contract entry (37th column) + D-05 error test + D-06 warn test"
      contains: "collector_inat_login"
  key_links:
    - from: "data/dbt/models/intermediate/int_combined.sql"
      to: "data/dbt/models/marts/occurrences.sql"
      via: "joined CTE SELECT * exposes collector_inat_login, projected as j.collector_inat_login"
      pattern: "j\\.collector_inat_login"
    - from: "data/dbt/models/marts/occurrences.sql"
      to: "data/dbt/models/marts/schema.yml"
      via: "enforced contract column-name + data_type match (varchar)"
      pattern: "name: collector_inat_login"
---

<objective>
Add a single unified `collector_inat_login VARCHAR` column to the `occurrences` mart,
derived as `COALESCE(specimen_inat_login, host_inat_login, user_login)` in all five
arms of `int_combined.sql`, projected through `occurrences.sql`, and declared in
`schema.yml` (dbt contract 36→37). Add two dbt `not_null` data tests: a hard-error
test scoped to the WABA-named arms (D-05) and a warn-only drift test on the ecdysis
arm (D-06). Validate locally with `bash data/dbt/run.sh build`, then ship the column
to S3 via the documented data-before-code release sequence (one operator-run
`SKIP_INTEGRATION_GATE=1` nightly on maderas).

Purpose: This column is the keystone for every per-collector query downstream
(Phases 169, 171, 172). Shipping it now, data-before-code, means the column is live
in S3 before any TypeScript reads it — no double-gate deadlock.

Output: `collector_inat_login` column live in `occurrences.parquet` / `occurrences.db`;
two new dbt data tests enforcing it at every `dbt build`.

DATA-LAYER ONLY — no TypeScript / frontend changes in this phase. Implements
IDENT-01 per locked decisions D-01..D-08 in 167-CONTEXT.md.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/167-collector-identity-column/167-CONTEXT.md
@.planning/phases/167-collector-identity-column/167-RESEARCH.md
@.planning/phases/167-collector-identity-column/167-PATTERNS.md
@.planning/phases/167-collector-identity-column/167-VALIDATION.md
@CLAUDE.md

# Files this plan edits (read in full before editing):
@data/dbt/models/intermediate/int_combined.sql
@data/dbt/models/marts/occurrences.sql
@data/dbt/models/marts/schema.yml
</context>

<artifacts_produced>
## Artifacts This Phase Produces

| Symbol | Kind | Location | Notes |
|--------|------|----------|-------|
| `collector_inat_login` | mart column (VARCHAR) | `marts/occurrences` (parquet + sqlite `occurrences.db`) | `COALESCE(specimen_inat_login, host_inat_login, user_login)`; the 37th contract column |
| `collector_inat_login` COALESCE expr (×5) | dbt intermediate SELECT field | `data/dbt/models/intermediate/int_combined.sql` (all 5 UNION ALL arms) | identical expression appended as the last column of each arm |
| `j.collector_inat_login` projection | dbt mart SELECT field | `data/dbt/models/marts/occurrences.sql` (final SELECT, appended last) | projected from the `joined` CTE |
| `collector_inat_login` contract entry | dbt contract column (varchar) | `data/dbt/models/marts/schema.yml` (occurrences model, 37th column) | bumps enforced contract 36→37 |
| D-05 hard-error data test | dbt `not_null` generic test (severity: error) | `schema.yml`, on `collector_inat_login`, `where: "source in ('waba_sample', 'waba_specimen')"` | NEW test; 0 violations today |
| D-06 warn drift test | dbt `not_null` generic test (severity: warn) | `schema.yml`, on `collector_inat_login`, `where: "source = 'ecdysis'"` | NEW test; baseline ~2,767 ecdysis NULLs |

No new files. No seed (`collector_identity.csv` is explicitly NOT created per D-04 — the
stale `.planning/research/` docs propose one; ignore them). No edit to `sqlite_export.py`
(it uses schema-agnostic `SELECT *`, so the column carries through automatically). No
TypeScript / frontend changes.
</artifacts_produced>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Add collector_inat_login to int_combined, occurrences mart, and schema.yml contract (+ D-05/D-06 tests)</name>
  <files>data/dbt/models/intermediate/int_combined.sql, data/dbt/models/marts/occurrences.sql, data/dbt/models/marts/schema.yml</files>

  <read_first>
    - data/dbt/models/intermediate/int_combined.sql (the 5-arm UNION ALL being edited — read all 5 arms; the last column of each arm is `NULL::INTEGER AS collapsed_count` at lines 58, 116, 171, 250, 311)
    - data/dbt/models/marts/occurrences.sql (the mart final SELECT being edited — last column is `j.collapsed_count` at line 91, `FROM joined j` at line 92)
    - data/dbt/models/marts/schema.yml (the contract being edited — occurrences model columns end with the `taxon_id` block at lines 79–91; `- name: occurrence_places` starts at line 93)
    - 167-PATTERNS.md §int_combined.sql and §schema.yml (the exact analog excerpts: the `taxon_id` not_null+severity+where test at schema.yml lines 88–91 is the in-repo pattern to mirror)
    - 167-RESEARCH.md §Exact Mechanics §1–§4 and §Common Pitfalls 1–4 (the D-06 tautology pitfall and the column-order parity requirement)
  </read_first>

  <action>
    Make three coordinated edits that must compile together (the int_combined column is
    structurally required by the UNION ALL before the mart can project it).

    (1) In `data/dbt/models/intermediate/int_combined.sql`, append a new last column to
    EACH of the five arms — immediately after the existing final `collapsed_count`
    column in that arm, with a trailing comma added to the prior `collapsed_count` line.
    The expression is IDENTICAL in every arm (bare alias names, since the aliases have
    already resolved at SELECT time per the PATTERNS.md convention):
    `COALESCE(specimen_inat_login, host_inat_login, user_login) AS collector_inat_login`
    Apply to ARM 1 ecdysis (after line 58), ARM 2 waba_sample (after line 116), ARM 3
    waba_specimen (after line 171), ARM 4 inat_obs (after line 250), ARM 5 checklist
    (after line 311). All five arms already project `specimen_inat_login`,
    `host_inat_login`, and `user_login` (verified per D-03), so the COALESCE inputs
    exist in every arm. Do NOT change any existing column. Column count and order must
    stay identical across all five arms so the UNION ALL typechecks.

    (2) In `data/dbt/models/marts/occurrences.sql`, append `j.collector_inat_login` to
    the final SELECT as the new last column — after `j.collapsed_count` (line 91) and
    before `FROM joined j` (line 92). Add a trailing comma to the `j.collapsed_count`
    line. It is available because the `joined` CTE is `SELECT ROW_NUMBER() OVER () AS
    _row_id, *` over int_combined. Do NOT add `j.specimen_inat_login` (it stays an
    intermediate-only COALESCE input, never projected to the mart — per established
    pattern, confirmed no match in the file today).

    (3) In `data/dbt/models/marts/schema.yml`, append a new column entry for the
    `occurrences` model as the 37th column — after the `taxon_id` block (ends line 91)
    and before `- name: occurrence_places` (line 93). The entry: `- name:
    collector_inat_login` with `data_type: varchar`, and a `data_tests:` list holding
    two `not_null` generic tests mirroring the existing `taxon_id` test shape
    (schema.yml lines 88–91):
      - D-05 (hard error): `not_null` with `config: severity: error` and
        `where: "source in ('waba_sample', 'waba_specimen')"`
      - D-06 (warn drift): `not_null` with `config: severity: warn` and
        `where: "source = 'ecdysis'"`
    CRITICAL (RESEARCH Pitfall 1 / PATTERNS critical note): the D-06 `where` predicate
    is `source = 'ecdysis'` ONLY. Do NOT write `... and collector_inat_login is null` —
    that is a tautology (it filters to the already-null rows so 100% trivially fail; the
    count becomes meaningless as a drift signal). Add a brief comment above each test
    citing D-05 / D-06 and the ~2,767 ecdysis baseline (mirroring the `taxon_id` comment
    style). Append the column LAST in schema.yml to match its append-last position in
    the occurrences.sql SELECT (Pitfall 3 — column-order parity).

    Per D-04: do NOT create a `collector_identity.csv` seed or any seed file. Per D-07:
    do NOT add a `packages.yml` or any `dbt_utils` dependency — these are built-in
    `not_null` generic tests. Do NOT edit `sqlite_export.py` (RESEARCH §5: schema-agnostic
    `SELECT *` carries the column through automatically).
  </action>

  <verify>
    <automated>bash data/dbt/run.sh build</automated>
  </verify>

  <acceptance_criteria>
    - `data/dbt/models/intermediate/int_combined.sql` contains exactly 5 occurrences of the literal `AS collector_inat_login` (one per arm): `grep -v '^--' data/dbt/models/intermediate/int_combined.sql | grep -c 'AS collector_inat_login'` returns 5.
    - `data/dbt/models/marts/occurrences.sql` contains `j.collector_inat_login` in the final SELECT and does NOT contain `j.specimen_inat_login`: `grep -c 'j.collector_inat_login' data/dbt/models/marts/occurrences.sql` returns 1 and `grep -c 'j.specimen_inat_login' data/dbt/models/marts/occurrences.sql` returns 0.
    - `data/dbt/models/marts/schema.yml` contains a column entry `name: collector_inat_login` with `data_type: varchar` and exactly two `not_null` tests on it (one `severity: error` with `where: "source in ('waba_sample', 'waba_specimen')"`, one `severity: warn` with `where: "source = 'ecdysis'"`). The D-06 `where` clause is `source = 'ecdysis'` with NO `collector_inat_login is null` suffix: `grep -F "source = 'ecdysis' and collector_inat_login is null" data/dbt/models/marts/schema.yml` returns no match (exit 1).
    - `bash data/dbt/run.sh build` exits 0 (the enforced 37-column contract compiles; a Binder Error here means a column/type mismatch). The D-05 error test reports PASS (0 violations). The D-06 warn test reports a WARN with a count near 2,767 (drift baseline) — NOT a count equal to total ecdysis rows (which would mean the predicate is wrong).
  </acceptance_criteria>

  <done>
    `bash data/dbt/run.sh build` exits 0 with the new 37-column contract; D-05 passes with 0 violations; D-06 warns with ~2,767. `collector_inat_login` is present in all 5 int_combined arms, the occurrences.sql SELECT, and the schema.yml contract.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Validate per-arm NULL baselines and sqlite carry-through</name>
  <files>(no source edits — validation only; regenerates gitignored public/data/occurrences.db)</files>

  <read_first>
    - 167-VALIDATION.md §Observable Baselines (the per-arm NULL signal table: waba_sample 0, waba_specimen 0, ecdysis ~2,767, inat_obs 0, checklist all-NULL) and the read-only validation query
    - 167-RESEARCH.md §Exact Mechanics §5 (sqlite_export.py schema-agnostic SELECT *) and §Validation Architecture §Phase Requirements → Test Map (the PRAGMA spot-check command)
    - Memory note in CLAUDE.md / project_local_uat_stale_occurrences_db: do NOT run `uv run python run.py` locally (Ecdysis auth gate blocks it); validate via run.sh build + sqlite_export.py only
  </read_first>

  <action>
    With the dbt build green from Task 1, confirm the data matches the locked baselines
    and that the column carries through to SQLite.

    (1) Run the read-only per-arm NULL query against the freshly built sandbox parquet
    (from 167-VALIDATION.md), grouping by `source` and counting rows + NULL
    `collector_inat_login` per arm. Confirm: `waba_sample` = 0 NULL, `waba_specimen` = 0
    NULL, `ecdysis` ≈ 2,767 NULL, `inat_obs` = 0 NULL, `checklist` = all NULL (every
    row). Use `duckdb` to read `data/dbt/target/sandbox/occurrences.parquet`.

    (2) Regenerate the local SQLite DB and spot-check the column is present:
    `cd data && uv run python sqlite_export.py`, then a PRAGMA `table_info(occurrences)`
    check that the column list includes `collector_inat_login`. Do NOT run the full
    `run.py` pipeline (the Ecdysis auth gate blocks it locally — this is not a defect).
    Do NOT push to S3 from here; that is the operator step in Task 3.
  </action>

  <verify>
    <automated>cd data && uv run python -c "import duckdb; rows=duckdb.execute(\"SELECT source, COUNT(*) AS n, COUNT(*) FILTER (WHERE collector_inat_login IS NULL) AS nulls FROM read_parquet('dbt/target/sandbox/occurrences.parquet') GROUP BY source ORDER BY source\").fetchall(); print(rows); assert dict((r[0], r[2]) for r in rows)['waba_sample']==0; assert dict((r[0], r[2]) for r in rows)['waba_specimen']==0"</automated>
  </verify>

  <acceptance_criteria>
    - The per-arm NULL query against `data/dbt/target/sandbox/occurrences.parquet` returns: `waba_sample` nulls = 0, `waba_specimen` nulls = 0, `ecdysis` nulls in the 2,600–2,900 band (baseline ~2,767; allow modest data drift), `inat_obs` nulls = 0, `checklist` nulls = total checklist rows. The verify command asserts the two WABA arms are exactly 0 and prints all five rows for inspection.
    - After `cd data && uv run python sqlite_export.py`, `PRAGMA table_info(occurrences)` on `public/data/occurrences.db` includes `collector_inat_login`: `cd data && uv run python3 -c "import sqlite3; c=sqlite3.connect('../public/data/occurrences.db'); cols=[d[1] for d in c.execute('PRAGMA table_info(occurrences)')]; print(cols); assert 'collector_inat_login' in cols"` exits 0.
    - No edit was made to `sqlite_export.py`: `git diff --quiet data/sqlite_export.py` exits 0.
  </acceptance_criteria>

  <done>
    Per-arm NULL counts match the 167-VALIDATION.md baselines (WABA arms 0, ecdysis ~2,767). `occurrences.db` carries `collector_inat_login` with no edit to `sqlite_export.py`.
  </done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 3 (OPERATOR, autonomous: false): one-time SKIP_INTEGRATION_GATE nightly + S3 column-presence confirmation</name>
  <files>(no repo edits — operator runs the nightly on the maderas cron host)</files>

  <read_first>
    - 167-RESEARCH.md §Exact Mechanics §6 (the two deadlocking gates; for this phase only gate 1 — nightly `test_dbt_diff::test_occurrences_schema_matches` — deadlocks; gate 2 deploy `validate-db` is NOT tripped because no new table is added) and the release-sequence steps
    - 167-CONTEXT.md D-08 (data-before-code; one-time SKIP_INTEGRATION_GATE nightly)
    - Memory `project_occurrences_contract_release_sequence` and STATE.md Blockers (if Phase 163 Ecdysis auth is still active, prefix `ECDYSIS_CACHE_TTL_SECONDS=99999999` to reuse the cached ZIP)
    - 167-VALIDATION.md §Manual-Only Verifications
  </read_first>

  <action>
    OPERATOR-ONLY STEP — autonomous: false. The executor MUST NOT run this and MUST NOT
    push to S3. This step runs on the maderas cron host (it needs AWS credentials, the
    dbt-producing pipeline, S3 write, and CloudFront invalidation — none available in the
    executor sandbox). The executor's job here is to PAUSE and surface the operator
    instruction below, then wait for the resume signal.

    The operator, after Task 1's dbt change is committed and pulled onto maderas, runs ONE
    publish with the nightly integration gate bypassed:
    `SKIP_INTEGRATION_GATE=1 bash data/nightly.sh`
    (prefix `ECDYSIS_CACHE_TTL_SECONDS=99999999` if the Phase 163 Ecdysis auth issue is
    still active, to reuse the cached ZIP). This breaks the one-time schema-equality
    deadlock — the fresh sandbox has 37 columns but live S3 still has 36, so the normal
    nightly `test_occurrences_schema_matches` gate would otherwise abort the very publish
    that refreshes S3. After this run lands the 37-column parquet in S3, every subsequent
    normal nightly compares 37-vs-37 and passes unaided. No deploy/frontend action is
    needed in this phase (no TypeScript consumes the column yet — deferred to Phases 169+);
    the deploy `validate-db` gate is unaffected because no new TABLE was added (it checks
    table names only, not columns — RESEARCH §6).
  </action>

  <what-built>
    Task 1 added the `collector_inat_login` column (dbt contract 36→37) and Task 2 confirmed
    it locally. The new column is NOT yet in the live S3 `occurrences.parquet` — S3 still
    carries the 36-column schema until a nightly publishes the rebuild. The first normal
    nightly would abort at the integration gate (`test_occurrences_schema_matches` compares
    37-col sandbox vs 36-col S3). This step breaks that one-time deadlock.

    This is an operator-only step: it runs on the maderas cron host (not the executor
    sandbox), needs AWS credentials and the dbt-producing pipeline, and pushes to S3 +
    invalidates CloudFront. The executor must NOT attempt this.
  </what-built>

  <how-to-verify>
    On the maderas cron host, after Task 1's dbt change is committed and pulled:
    1. Run ONE publish with the integration gate bypassed:
       `SKIP_INTEGRATION_GATE=1 bash data/nightly.sh`
       If the Phase 163 Ecdysis auth issue is still active, prefix the cache-TTL override:
       `ECDYSIS_CACHE_TTL_SECONDS=99999999 SKIP_INTEGRATION_GATE=1 bash data/nightly.sh`
    2. Confirm the live S3 `occurrences.parquet` now carries the column — e.g. DESCRIBE the
       published parquet (or pull it and run `PRAGMA`/`DESCRIBE`) and verify
       `collector_inat_login` is in the column list (37 columns total).
    3. Confirm the NEXT normal nightly's integration gate passes unaided (37-vs-37 schema
       parity) — i.e. no SKIP_INTEGRATION_GATE needed on subsequent runs.
    No deploy/frontend action is required in this phase — no TypeScript consumes the column
    yet (deferred to Phases 169+). The deploy `validate-db` gate is unaffected (it checks
    table names only, not columns — RESEARCH §6).
  </how-to-verify>

  <verify>
    <human-check>Operator confirms (a) the live S3 occurrences.parquet column list includes collector_inat_login (37 cols), and (b) the next normal nightly passes test_dbt_diff::test_occurrences_schema_matches with no SKIP_INTEGRATION_GATE flag.</human-check>
  </verify>

  <acceptance_criteria>
    - The live S3 `occurrences.parquet` column list includes `collector_inat_login` (37 columns).
    - A subsequent normal nightly run passes `test_dbt_diff::test_occurrences_schema_matches` with no SKIP_INTEGRATION_GATE flag (37-vs-37 parity).
  </acceptance_criteria>

  <done>
    Live S3 `occurrences.parquet` carries `collector_inat_login` (37 cols); the next normal nightly's schema-parity gate passes without the bypass flag.
  </done>

  <resume-signal>Reply "shipped" once the S3 occurrences.parquet carries collector_inat_login (37 cols) and the next normal nightly's schema gate passes, or describe what failed.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | This phase adds a derived column inside an existing data pipeline. No new ingress, no new external input, no new auth surface, no server runtime (static-hosting-only). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-167-01 | Information Disclosure | `collector_inat_login` exposed in `occurrences.parquet` / `.db` | accept | `collector_inat_login` is a public iNaturalist handle already shown on the BeeAtlas site (host/specimen/user logins are surfaced today); deriving it into the mart exposes no new data and introduces no new data-sensitivity risk. No PII beyond already-public handles; checklist rows resolve NULL (no free-text `recordedBy` reconciliation). |
| T-167-SC | Tampering | npm/pip/cargo installs | accept | No packages installed this phase (D-07: built-in dbt `not_null` tests, no `packages.yml`, no `dbt_utils`). No supply-chain surface; slopcheck N/A. |

Realistic surface is none above the existing pipeline. Block-on severity is `high`; there
are no high (or any) threats introduced. The one consideration worth stating explicitly:
`collector_inat_login` is a public iNat handle already visible on the site, so the new
column adds no data-sensitivity risk.
</threat_model>

<verification>
Phase-level checks (all run from repo root unless noted):

1. `bash data/dbt/run.sh build` exits 0 — the enforced 37-column contract compiles; D-05 passes (0 violations); D-06 warns (~2,767 ecdysis NULLs).
2. Per-arm NULL parity against `data/dbt/target/sandbox/occurrences.parquet`: waba_sample 0, waba_specimen 0, ecdysis ~2,767, inat_obs 0, checklist all-NULL (167-VALIDATION.md baselines).
3. `occurrences.db` (after `cd data && uv run python sqlite_export.py`) carries `collector_inat_login` via PRAGMA table_info — no edit to `sqlite_export.py`.
4. (operator, post-ship) live S3 `occurrences.parquet` carries `collector_inat_login` (37 cols); the next normal nightly passes `test_dbt_diff::test_occurrences_schema_matches` unaided.
</verification>

<success_criteria>
- [ ] `collector_inat_login VARCHAR` column exists in `occurrences.parquet` (dbt contract 36→37) — IDENT-01.
- [ ] D-05 hard-error dbt test asserts non-null `collector_inat_login` for `source in ('waba_sample','waba_specimen')` and passes (0 violations).
- [ ] D-06 warn dbt test surfaces the ecdysis NULL drift (`where: source = 'ecdysis'`, baseline ~2,767) without blocking the build, with the correct (non-tautological) predicate.
- [ ] `collector_inat_login` carries through to `occurrences.db` with no edit to `sqlite_export.py`; column live in S3 after the one-time SKIP_INTEGRATION_GATE nightly.
- [ ] No `collector_identity.csv` seed created (D-04); no `packages.yml`/`dbt_utils` added (D-07); no TypeScript/frontend changes.
</success_criteria>

<output>
Create `.planning/phases/167-collector-identity-column/167-01-SUMMARY.md` when done.
</output>
