---
phase: 46-basemap-tile-provider-upgrade
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/bee-map.ts
autonomous: false
requirements: []
user_setup:
  - service: stadiamaps
    why: "Tile provider for production basemap — free tier, no credit card"
    env_vars: []
    dashboard_config:
      - task: "Register free account at stadiamaps.com"
        location: "https://client.stadiamaps.com/signup/"
      - task: "Add production domain to allowlist"
        location: "Stadia Maps Dashboard -> Authentication -> Domain Allowlist"

must_haves:
  truths:
    - "Map displays terrain contours, roads, and trails from Stadia Maps outdoors tiles"
    - "Map supports zoom levels up to 20 (previously capped around 15)"
    - "Attribution is displayed automatically by the StadiaMaps source"
    - "No API key or secret is committed to the repository"
  artifacts:
    - path: "frontend/src/bee-map.ts"
      provides: "Single StadiaMaps outdoors tile layer replacing two Esri Ocean layers"
      contains: "StadiaMaps"
  key_links:
    - from: "frontend/src/bee-map.ts"
      to: "ol/source/StadiaMaps.js"
      via: "import and instantiation"
      pattern: "new StadiaMaps"
---

<objective>
Replace the two stacked Esri Ocean basemap tile layers (World_Ocean_Base + World_Ocean_Reference) with a single Stadia Maps `outdoors` tile layer. This gives the map terrain contours, roads, hiking trails, and zoom support up to level 20 — all directly relevant to bee collecting fieldwork.

Purpose: Current Ocean basemap lacks terrain, roads, and trails; zoom caps at ~15. Fieldwork planning needs higher detail.
Output: Updated `bee-map.ts` with StadiaMaps source, build passing, visual confirmation.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/46-basemap-tile-provider-upgrade/46-RESEARCH.md

<interfaces>
<!-- Key types and imports the executor needs from bee-map.ts -->

From frontend/src/bee-map.ts (current imports, lines 12-13):
```typescript
import TileLayer from "ol/layer/Tile.js";
import XYZ from "ol/source/XYZ.js";
```

Current basemap layers (lines 357-371):
```typescript
new TileLayer({
  source: new XYZ({
    attributions: 'Base map by Esri and its data providers',
    urls: [
      'https://services.arcgisonline.com/arcgis/rest/services/Ocean/World_Ocean_Base/MapServer/tile/{z}/{y}/{x}',
      'https://server.arcgisonline.com/arcgis/rest/services/Ocean/World_Ocean_Base/MapServer/tile/{z}/{y}/{x}',
    ]
  }),
}),
new TileLayer({
  source: new XYZ({
    // NB: this source is unmaintained
    url: "https://server.arcgisonline.com/ArcGIS/rest/services/Ocean/World_Ocean_Reference/MapServer/tile/{z}/{y}/{x}",
  }),
}),
```

After these two TileLayers, the layers array continues with:
```typescript
new LayerGroup(),
this.specimenLayer,
this.sampleLayer,
regionLayer,
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace Esri Ocean tile layers with Stadia Maps outdoors</name>
  <files>frontend/src/bee-map.ts</files>
  <read_first>
    - frontend/src/bee-map.ts (full file — understand current imports and layer stack)
  </read_first>
  <action>
In `frontend/src/bee-map.ts`:

1. **Add import** — Add this import near the existing `ol/layer/Tile.js` import (line 12):
   ```typescript
   import StadiaMaps from "ol/source/StadiaMaps.js";
   ```

2. **Remove XYZ import** — Delete line 13 (`import XYZ from "ol/source/XYZ.js";`). XYZ is used only for the two basemap layers being removed. Confirm by checking that XYZ appears nowhere else in the file except the import and the two `new XYZ(` calls at lines 358 and 367.

3. **Replace the two TileLayer blocks** (lines 357-371) with a single TileLayer:
   ```typescript
   new TileLayer({
     source: new StadiaMaps({
       layer: 'outdoors',
       retina: true,
     }),
   }),
   ```
   This single layer replaces both `World_Ocean_Base` and `World_Ocean_Reference`. The rest of the layers array (`new LayerGroup()`, `this.specimenLayer`, `this.sampleLayer`, `regionLayer`) stays exactly as-is.

4. **Do NOT** set any `attributions` property on the TileLayer or source — the StadiaMaps source sets OSM/Stadia attributions automatically.

5. **Do NOT** add any API key, token, or credentials. Domain-based authentication is handled outside the codebase via Stadia Maps dashboard.
  </action>
  <verify>
    <automated>cd /Users/rainhead/dev/beeatlas/frontend && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - bee-map.ts contains `import StadiaMaps from "ol/source/StadiaMaps.js"`
    - bee-map.ts contains `new StadiaMaps({` with `layer: 'outdoors'` and `retina: true`
    - bee-map.ts does NOT contain `World_Ocean_Base`
    - bee-map.ts does NOT contain `World_Ocean_Reference`
    - bee-map.ts does NOT contain `import XYZ from "ol/source/XYZ.js"`
    - bee-map.ts does NOT contain any API key or token string
    - `npm run build` exits 0
    - `cd frontend && npm test -- --run` exits 0
  </acceptance_criteria>
  <done>Build passes, XYZ import removed, two Esri Ocean layers replaced with single StadiaMaps outdoors layer, no API keys in code</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Visual verification of new basemap</name>
  <files>frontend/src/bee-map.ts</files>
  <action>
Human verifies the visual result of Task 1. No code changes in this task — purely visual/functional confirmation that the new Stadia Maps outdoors basemap renders correctly in the browser.
  </action>
  <read_first>
    - frontend/src/bee-map.ts (confirm the StadiaMaps source is in place before verifying)
  </read_first>
  <what-built>Replaced Esri Ocean basemap with Stadia Maps outdoors tiles showing terrain, roads, and trails up to zoom 20</what-built>
  <how-to-verify>
    1. Run `cd frontend && npm run dev` to start the dev server
    2. Open the app in a browser (typically http://localhost:5173)
    3. Verify the basemap shows terrain contours, roads, and natural features (NOT the blue ocean-themed basemap)
    4. Zoom in to level 16+ on a known area (e.g., a town in Washington state) — verify street-level detail appears
    5. Zoom further to level 18-20 — verify tiles continue loading (no blank tiles)
    6. Check the bottom-right corner of the map for Stadia Maps / OpenStreetMap attribution text
    7. Verify specimen/sample dots still render correctly on top of the new basemap
    8. Verify the region overlay still renders correctly
  </how-to-verify>
  <verify>
    <automated>cd /Users/rainhead/dev/beeatlas/frontend && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - Human confirms terrain, roads, and trails are visible on the basemap
    - Human confirms zoom level 16+ shows street-level detail
    - Human confirms attribution text is visible in map corner
    - Human confirms specimen/sample dots render on top of basemap
  </acceptance_criteria>
  <done>User has visually confirmed the new basemap renders terrain, roads, trails, and supports high zoom levels</done>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser to Stadia Maps CDN | Tile requests sent to external tile provider; no user data transmitted |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-46-01 | Information Disclosure | API key in source | mitigate | Use domain-based auth (no credentials in code); user_setup documents account registration as a human task |
| T-46-02 | Denial of Service | Stadia Maps rate limiting | accept | Free tier (200K tiles/month) is sufficient for niche scientific tool; fallback would be switching provider, not a security incident |
| T-46-03 | Tampering | Tile content from CDN | accept | Standard risk for any third-party tile provider; tiles are visual-only, no executable content; same trust level as previous Esri tiles |
</threat_model>

<verification>
- `cd frontend && npm run build` exits 0
- `cd frontend && npm test -- --run` exits 0
- `grep -c "StadiaMaps" frontend/src/bee-map.ts` returns >= 2 (import + usage)
- `grep -c "World_Ocean" frontend/src/bee-map.ts` returns 0
- Visual check confirms terrain/road/trail basemap in browser
</verification>

<success_criteria>
- Map displays Stadia Maps outdoors tiles with terrain, roads, and trails
- Zoom level 20 loads tiles (previously capped at ~15)
- Attribution displays automatically
- No API keys or credentials in the codebase
- All existing tests pass
- Build compiles cleanly
</success_criteria>

<output>
After completion, create `.planning/phases/46-basemap-tile-provider-upgrade/46-01-SUMMARY.md`
</output>
