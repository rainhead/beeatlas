---
phase: 138-frontend-points-detail-card
plan: 04
type: execute
wave: 2
depends_on: [138-01, 138-02]
files_modified:
  - src/filter.ts
  - src/bee-occurrence-detail.ts
autonomous: false
requirements: [UIX-03]

must_haves:
  truths:
    - "OccurrenceRow + OCCURRENCE_COLUMNS carry verbatim_name, locality, collapsed_count so the card receives them off the occurrence row (D-10 consumer side)"
    - "Clicking a checklist point opens a card showing accepted name with inline (det. as {verbatim}) only when they differ (D-05)"
    - "The card shows collector, precision-aware Roman date, locality (omitted when null), 'Represents N collapsed records' when N>1, and a muted 'Bartholomew et al. 2024' line (D-06, D-07, D-08, D-09)"
    - "formatRomanDate handles null/length-4/length-7 inputs (D-08)"
    - "render() dispatch routes source==='checklist' rows to _renderChecklist (D-09)"
  artifacts:
    - path: "src/filter.ts"
      provides: "3 promoted columns in OccurrenceRow interface + OCCURRENCE_COLUMNS select list"
      contains: "verbatim_name"
    - path: "src/bee-occurrence-detail.ts"
      provides: "_renderChecklist method, extended formatRomanDate, render() dispatch branch"
      contains: "_renderChecklist"
  key_links:
    - from: "src/bee-occurrence-detail.ts"
      to: "occurrence row columns"
      via: "row.verbatim_name / row.locality / row.collapsed_count read"
      pattern: "row\\.(verbatim_name|locality|collapsed_count)"
    - from: "src/bee-occurrence-detail.ts render()"
      to: "_renderChecklist"
      via: "source === 'checklist' dispatch"
      pattern: "source === 'checklist'"
---

<objective>
Render the checklist detail card. Add the three promoted columns to the frontend row contract (`OccurrenceRow` + `OCCURRENCE_COLUMNS`) so the card receives them, extend `formatRomanDate` for precision-aware/null dates, and add a `_renderChecklist` branch that shows the accepted name with an inline `(det. as {verbatim})` annotation, collector, Roman date, locality, collapsed-count, and the muted Bartholomew attribution.

Purpose: Satisfies UIX-03. Consumes the 37-column contract from Plan 02 (the columns must exist in occurrences before filter.ts can SELECT them) and the formatRomanDate test scaffold from Plan 01. Wave 2.
Output: A working checklist detail card driven straight off the occurrence row — no new fetch path.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/138-frontend-points-detail-card/138-UI-SPEC.md
@.planning/phases/138-frontend-points-detail-card/138-PATTERNS.md
@.planning/phases/138-frontend-points-detail-card/138-CONTEXT.md
@.planning/phases/138-frontend-points-detail-card/138-02-SUMMARY.md

<interfaces>
<!-- Extracted from codebase. -->
src/filter.ts: `OccurrenceRow` interface (tail at lines 68–78 incl. `checklist_id: number | null`,
`source`, `display_name`, `display_rank`); `OCCURRENCE_COLUMNS` const array (lines 81–90) is the SQL
SELECT list used by queryTablePage/queryListPage. Phase 137 added `checklist_id` to BOTH — mirror that
pattern for the 3 new columns. The promoted columns now exist on the occurrences mart (Plan 02).

src/bee-occurrence-detail.ts:
- `ROMAN_MONTHS` array + `formatRomanDate` (lines 1–13). Plan 01 exported it and wrote tests targeting
  the extended signature `(dateStr: string | null) => string` with branches for length 10/7/4/null.
- `render()` dispatch (lines 287–305): non-specimen rows route to _renderProvisional / _renderInatObs /
  _renderSampleOnly. Add a `row.source === 'checklist' ? this._renderChecklist(row) :` branch before the
  _renderSampleOnly fallback.
- `_renderInatObs` (256–285) and `_renderSampleOnly` (216–233) are the templates to mirror; both use
  `<div class="panel-content sample-dot-detail">` and omit null fields without placeholders.
- `this.taxonCache?.get(row.taxon_id)` resolves the accepted name (line 258 pattern).

CSS classes (already defined, lines 51–162): `.panel-content.sample-dot-detail` (container),
`.inat-id-label` (taxon line), `.event-date` (serif date), `.event-observer` (collector, muted),
`.event-host` (locality, hint), `.event-count` (collapsed-count, hint), `.hint` (muted italic — det.
annotation span + attribution). No new CSS custom properties (UI-SPEC: token set is fixed).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add the 3 promoted columns to OccurrenceRow + OCCURRENCE_COLUMNS</name>
  <files>src/filter.ts</files>
  <read_first>
    - src/filter.ts lines 40–78 (OccurrenceRow interface, esp. the Phase 137 checklist_id additions at 68–69), 81–90 (OCCURRENCE_COLUMNS), ~165/209/419 (selectCols usage in queryTablePage/queryListPage)
    - .planning/phases/138-frontend-points-detail-card/138-PATTERNS.md §"src/filter.ts"
    - .planning/phases/138-frontend-points-detail-card/138-02-SUMMARY.md (confirms the 3 columns now exist on the occurrences mart)
  </read_first>
  <behavior>
    - OccurrenceRow.verbatim_name is typed `string | null`; locality `string | null`; collapsed_count `number | null`.
    - OCCURRENCE_COLUMNS includes the three column names, so the generated SQL SELECT pulls them.
    - A row queried for a checklist occurrence carries non-null values; non-checklist rows carry null.
  </behavior>
  <action>In `filter.ts`, after the `checklist_id: number | null;` field in `OccurrenceRow`, add (mirroring Phase 137's comment style) `// Phase 138 (D-10): checklist detail fields; null for all other sources.` then `verbatim_name: string | null;`, `locality: string | null;`, `collapsed_count: number | null;`. Append `'verbatim_name', 'locality', 'collapsed_count'` to the `OCCURRENCE_COLUMNS` const array (order is irrelevant for SQL SELECT). No change needed to the selectCols call sites — they spread OCCURRENCE_COLUMNS.</action>
  <verify>
    <automated>cd /home/peter/dev/beeatlas && npx tsc --noEmit 2>&1 | tail -15 && npm test -- --run src/tests/filter.test.ts 2>&1 | tail -12</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: `grep -c "verbatim_name\|locality\|collapsed_count" src/filter.ts` returns at least 6 (interface + columns list).
    - Source assertion: OCCURRENCE_COLUMNS array literally contains `'verbatim_name'`, `'locality'`, `'collapsed_count'`.
    - Behavior assertion: `npx tsc --noEmit` clean; `filter.test.ts` passes.
  </acceptance_criteria>
  <done>OccurrenceRow and OCCURRENCE_COLUMNS carry the 3 promoted columns; project typechecks; filter tests green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Extend formatRomanDate + add _renderChecklist + render() dispatch</name>
  <files>src/bee-occurrence-detail.ts</files>
  <read_first>
    - src/bee-occurrence-detail.ts lines 1–13 (ROMAN_MONTHS + formatRomanDate), 216–233 (_renderSampleOnly), 235–253 (_renderProvisional), 256–285 (_renderInatObs), 287–305 (render() dispatch), 51–162 (CSS classes)
    - .planning/phases/138-frontend-points-detail-card/138-UI-SPEC.md §"Detail Card: _renderChecklist" (field order, null states, copywriting) + §"formatRomanDate Extension"
    - .planning/phases/138-frontend-points-detail-card/138-PATTERNS.md §"src/bee-occurrence-detail.ts" (CSS class vocabulary + null-omit patterns)
    - .planning/phases/138-frontend-points-detail-card/138-CONTEXT.md §D-05..D-09
    - src/tests/bee-occurrence-detail.test.ts (the Plan 01 formatRomanDate tests this turns green)
  </read_first>
  <behavior>
    - formatRomanDate(null) === '' ; formatRomanDate('') === '' (D-08 null guard).
    - formatRomanDate('2019-06-15') === '15 VI 2019' ; formatRomanDate('2019-06') === 'VI 2019' ; formatRomanDate('2019') === '2019'.
    - _renderChecklist shows `{accepted} (det. as {verbatim})` only when accepted and verbatim differ; accepted alone when equal/no-verbatim; verbatim alone when no accepted; "No determination" when both null (D-05).
    - Collapsed-count line appears only when collapsed_count > 1 (D-07); locality and collector lines are omitted when null (D-09); attribution "Bartholomew et al. 2024" always renders muted at the bottom (D-06).
    - render() routes a source==='checklist' row to _renderChecklist (D-09).
  </behavior>
  <action>Extend `formatRomanDate` to the UI-SPEC signature `(dateStr: string | null): string`: return `''` for falsy input; length-10 → existing full-date path; length-7 → `${ROMAN_MONTHS[month-1]} ${year}` from the split parts; length-4 → return the year string; else return `dateStr` as-is. Add `_renderChecklist(row: OccurrenceRow)` returning a `<div class="panel-content sample-dot-detail">` with the UI-SPEC field order: (1) `.inat-id-label` taxon line — resolve accepted via `this.taxonCache?.get(row.taxon_id)`; if accepted and `row.verbatim_name` differ, render `<em>{accepted}</em> <span class="hint">(det. as ${row.verbatim_name})</span>`; if equal or no verbatim, `<em>{accepted}</em>` alone; if no accepted but verbatim present, `<em>{verbatim}</em>`; if both null, `<span class="hint">No determination</span>` (D-05); (2) `.event-observer` collector — omit when `row.recordedBy == null`; (3) `.event-date` — `formatRomanDate(row.date)`; (4) `.event-host` locality — omit when `row.locality == null || row.locality === ''`; (5) `.event-count` `Represents ${row.collapsed_count} collapsed records` — only when `row.collapsed_count > 1`; (6) `.hint` `Bartholomew et al. 2024` always. In `render()`, add `row.source === 'checklist' ? this._renderChecklist(row) :` before the `_renderSampleOnly(row)` fallback in the non-specimen map. Use only existing CSS classes/tokens (no new custom properties).</action>
  <verify>
    <automated>cd /home/peter/dev/beeatlas && npm test -- --run src/tests/bee-occurrence-detail.test.ts 2>&1 | tail -15 && npx tsc --noEmit 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: `bee-occurrence-detail.ts` contains `_renderChecklist`, the string `Bartholomew et al. 2024`, the substring `(det. as`, and `Represents`.
    - Source assertion: render() dispatch contains `row.source === 'checklist'`.
    - Behavior assertion: the Plan 01 `formatRomanDate` tests (null, length-4, length-7, full) all PASS.
    - Behavior assertion: `npx tsc --noEmit` clean.
  </acceptance_criteria>
  <done>formatRomanDate handles all precision/null cases; _renderChecklist renders the D-05..D-09 card; render() dispatches checklist rows; date tests green; typecheck clean.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human-verify checklist points + detail card end to end</name>
  <action>Pause for human visual verification per the steps below; do not auto-advance. This checkpoint confirms the UIX-01/UIX-03 manual items from 138-VALIDATION.md (green points, clustering, county-fill absence, card layout, det. annotation, toggle behavior).</action>
  <what-built>The full checklist detail card and green point layer, end to end: Plan 03's green points + this plan's card. Run `npm run dev`.</what-built>
  <how-to-verify>
    1. Run `npm run dev` and open the local URL.
    2. Zoom into Washington until checklist points uncluster — confirm they are flat green (#2c7a2c), solid (not translucent), same radius as other points. Zoom out — confirm they merge into recency-colored clusters (green only at unclustered level).
    3. Confirm NO translucent green county polygons remain at any zoom (county-fill gone).
    4. Click a checklist point. Confirm the card shows, top-to-bottom: accepted name with `(det. as {verbatim})` ONLY when the verbatim differs; collector; a Roman-numeral date (e.g. `15 VI 2019`) or no date line for none-quality records; locality (or no line if absent); `Represents N collapsed records` only when N>1; a muted `Bartholomew et al. 2024` line at the bottom.
    5. Confirm the "Checklist records" source toggle hides/shows the green points, and toggling all four sources off reports zero selected.
  </how-to-verify>
  <resume-signal>Type "approved" or describe any visual/layout issue.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| occurrence-row string fields → rendered detail card | verbatim_name / locality / recordedBy are already-published Bartholomew et al. 2024 strings, rendered through Lit templates. |

## STRIDE Threat Register

Static-hosted, read-only public frontend; no server, no auth, no PII, no persistence.

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-138-06 | Tampering (XSS) | _renderChecklist interpolating verbatim_name/locality/recordedBy | mitigate | Lit auto-escapes interpolated `${...}` values in `html` templates; these fields are bound as text, never via `unsafeHTML`. No `unsafeHTML` is introduced in this plan. |
| T-138-07 | Information Disclosure | Detail card content | accept | All displayed fields are already-public published checklist data; no PII or secret. |
| T-138-SC | Tampering | npm/pip/cargo installs | mitigate | No packages installed (RESEARCH.md: zero new packages). N/A. |

Spoofing / Repudiation / DoS / Elevation: N/A — no endpoint, no auth, no privileged operation. The card is a pure function of an occurrence row already loaded client-side.
</threat_model>

<verification>
- `npm test -- --run src/tests/bee-occurrence-detail.test.ts src/tests/filter.test.ts` green.
- `npx tsc --noEmit` clean.
- Full suite `npm test -- --run` green before /gsd:verify-work (wave-merge gate).
- Human-verify checkpoint: card layout + det. annotation + green points confirmed visually (UIX-03 manual items per VALIDATION.md).
</verification>

<success_criteria>
- OccurrenceRow/OCCURRENCE_COLUMNS expose the 3 promoted columns; card reads them directly.
- _renderChecklist renders accepted/(det. as verbatim)/collector/date/locality/collapsed-count/attribution per D-05..D-09.
- formatRomanDate is null- and precision-safe (D-08).
- Visual checkpoint approved.
</success_criteria>

<output>
Create `.planning/phases/138-frontend-points-detail-card/138-04-SUMMARY.md` when done.
</output>
