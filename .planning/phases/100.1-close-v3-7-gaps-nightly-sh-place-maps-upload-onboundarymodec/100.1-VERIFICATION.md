---
phase: 100.1-close-v3-7-gaps-nightly-sh-place-maps-upload-onboundarymodec
verified: 2026-05-18T00:00:00Z
status: passed
score: 4/4
overrides_applied: 0
---

# Phase 100.1: Close v3.7 Gaps Verification Report

**Phase Goal:** Close v3.7 gaps: nightly.sh place-maps upload + _onBoundaryModeChanged selectedPlace clear
**Verified:** 2026-05-18
**Status:** PASSED
**Re-verification:** No — initial verification

## Goal Achievement

### Observable Truths

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | nightly.sh uploads place-maps SVGs to s3://$BUCKET/data/place-maps/ on every run | VERIFIED | Line 151: `aws --profile "$AWS_PROFILE" s3 cp --recursive --no-progress "$EXPORT_DIR/place-maps/" "s3://$BUCKET/data/place-maps/"` |
| 2 | nightly.sh invalidates /data/place-maps/* in CloudFront alongside species-maps | VERIFIED | Line 160: `--paths "/data/manifest.json" "/data/feeds/*" "/data/species-maps/*" "/data/place-maps/*"` |
| 3 | Switching boundary mode away from 'places' clears _filterState.selectedPlace and re-runs the filter query | VERIFIED | Lines 1013-1020: `leavingPlaces` guard clears `selectedPlace: null`, calls `_runFilterQuery().then(() => _pushUrlState())` and `_runTableQuery()` |
| 4 | Switching between non-places boundary modes does not trigger a redundant filter query | VERIFIED | Lines 1021-1023: `else` branch calls only `_pushUrlState()` — no filter or table query |

**Score:** 4/4 truths verified

### Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `data/nightly.sh` | S3 upload + CloudFront invalidation for place-maps | VERIFIED | Upload line at 151, invalidation path at 160, comment updated at 148 |
| `src/bee-atlas.ts` | _onBoundaryModeChanged clears selectedPlace when leaving places mode | VERIFIED | Full conditional implementation at lines 1010-1024 |

### Key Link Verification

| From | To | Via | Status | Details |
|------|----|-----|--------|---------|
| nightly.sh aws s3 cp --recursive place-maps/ | s3://$BUCKET/data/place-maps/ | aws s3 cp --recursive | VERIFIED | Line 151 exact match |
| nightly.sh cloudfront create-invalidation | /data/place-maps/* | --paths argument | VERIFIED | Line 160 exact match |
| bee-atlas.ts _onBoundaryModeChanged | _runFilterQuery + _pushUrlState | clearing selectedPlace then re-running filter | VERIFIED | leavingPlaces branch at lines 1014-1020 |

### Behavioral Spot-Checks

| Behavior | Command | Result | Status |
|----------|---------|--------|--------|
| nightly.sh syntax valid | `bash -n data/nightly.sh` | exit 0 | PASS |
| TypeScript compiles clean | `npx tsc --noEmit` | exit 0 (no output) | PASS |
| Exactly 3 recursive S3 copy calls (feeds, species-maps, place-maps) | `grep -c 'aws .* s3 cp --recursive' data/nightly.sh` | 3 | PASS — plan's acceptance criteria claimed 2 but that undercounted the pre-existing feeds line; 3 is correct and expected |

### Anti-Patterns Found

None. No TODO/TBD/FIXME markers in modified files. No stubs. No empty implementations.

### Selection State Preservation

`_onBoundaryModeChanged` does NOT mutate any of: `_selectedOccurrences`, `_selectedOccIds`, `_selectedCluster`, `_selectionBounds`, `_sidebarOpen`. Confirmed by reading lines 1010-1024 — only `_filterState`, `_boundaryMode`, and `_tablePage` are touched.

### Requirements Coverage

| Requirement | Description | Status | Evidence |
|-------------|-------------|--------|----------|
| GAP-100-01 | nightly.sh place-maps upload + CloudFront invalidation | SATISFIED | Lines 151 and 160 of data/nightly.sh |
| GAP-100-02 | _onBoundaryModeChanged clears selectedPlace when leaving places mode | SATISFIED | Lines 1010-1024 of src/bee-atlas.ts |

### Human Verification Required

None. Both changes are fully verifiable via source inspection and CLI checks.

---

_Verified: 2026-05-18T00:00:00Z_
_Verifier: Claude (gsd-verifier)_
