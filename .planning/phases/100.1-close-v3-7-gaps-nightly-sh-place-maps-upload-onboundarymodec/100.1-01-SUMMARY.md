---
phase: 100.1-close-v3-7-gaps-nightly-sh-place-maps-upload-onboundarymodec
plan: "01"
subsystem: pipeline + frontend
tags: [gap-closure, nightly-pipeline, filter-state, s3, cloudfront]
dependency_graph:
  requires: []
  provides: [place-maps-s3-upload, place-maps-cloudfront-invalidation, boundary-mode-place-clear]
  affects: [data/nightly.sh, src/bee-atlas.ts]
tech_stack:
  added: []
  patterns: [immutable-filterState-spread, runFilterQuery-then-pushUrlState]
key_files:
  created: []
  modified:
    - data/nightly.sh
    - src/bee-atlas.ts
decisions:
  - "leavingPlaces conditional avoids redundant filter query when switching between non-places modes"
  - "selection state (_selectedOccurrences, _selectedOccIds, etc.) intentionally preserved across boundary-mode toggles — orthogonal to place filter"
metrics:
  duration: "~10 minutes"
  completed: "2026-05-18"
  tasks_completed: 2
  tasks_total: 2
---

# Phase 100.1 Plan 01: Close v3.7 Gaps (nightly.sh place-maps upload + _onBoundaryModeChanged selectedPlace clear) Summary

Closed two v3.7 gaps: nightly.sh now uploads place-maps SVGs to S3 and invalidates CloudFront, and _onBoundaryModeChanged clears the selectedPlace filter when leaving places boundary mode.

## Tasks Completed

| # | Name | Commit | Files |
|---|------|--------|-------|
| 1 | Upload place-maps to S3 and invalidate CloudFront in nightly.sh | c7d7a31 | data/nightly.sh |
| 2 | Clear selectedPlace in _onBoundaryModeChanged when leaving places mode | 1ce1e40 | src/bee-atlas.ts |

## What Was Built

**Task 1 (data/nightly.sh):**
- Added `aws s3 cp --recursive --no-progress "$EXPORT_DIR/place-maps/" "s3://$BUCKET/data/place-maps/"` immediately after the existing species-maps upload line.
- Extended the CloudFront `--paths` argument to include `/data/place-maps/*` alongside the existing manifest, feeds, and species-maps paths.
- Updated the stable-URL comment to mention place-maps.

**Task 2 (src/bee-atlas.ts):**
- Extended `_onBoundaryModeChanged` from a 2-line stub to a proper conditional: computes `leavingPlaces = newMode !== 'places' && selectedPlace !== null`, and when true clears `selectedPlace`, resets `_tablePage`, re-runs `_runFilterQuery().then(() => _pushUrlState())` and `_runTableQuery()`.
- When `leavingPlaces` is false (switching between non-places modes, or no place selected), preserves the original single-line `_pushUrlState()` behavior — no redundant filter query.
- Does NOT clear selection state (`_selectedOccurrences`, `_selectedOccIds`, `_selectedCluster`, `_selectionBounds`, `_sidebarOpen`) — boundary-mode toggles are UI-chrome changes, not map-click selections.

## Verification Results

- `bash -n data/nightly.sh` — PASS (syntax check)
- `grep -F '/data/place-maps/*' data/nightly.sh` — found in CloudFront --paths
- `npx tsc --noEmit` — PASS (no type errors)
- `npm test -- --run src/tests/bee-atlas.test.ts` — 70/70 tests pass
- Manual review: `_onBoundaryModeChanged` contains no selection state mutations

## Deviations from Plan

### Minor Plan Inaccuracy (not a code deviation)

The plan's acceptance criteria stated `grep -c 'aws .* s3 cp --recursive' data/nightly.sh` should return 2. The actual count is 3 (feeds, species-maps, place-maps) because the feeds upload was already using `--recursive` before this plan. The plan's annotation "one for species-maps, one for place-maps" failed to count the pre-existing feeds line. The code is correct — three recursive uploads is the intended behavior; the plan's count was off by one.

### Pre-existing Test Failures (out of scope)

`src/tests/data-species.test.ts` and `src/tests/build-output.test.ts` fail in the worktree because pipeline-generated artifacts (`public/data/species.json`, etc.) are not present. These failures are pre-existing and unrelated to this plan's changes.

## Known Stubs

None.

## Threat Flags

None — no new network endpoints, auth paths, or schema changes introduced.

## Self-Check: PASSED

- `data/nightly.sh` — modified and committed (c7d7a31)
- `src/bee-atlas.ts` — modified and committed (1ce1e40)
- Both commits present in git log
