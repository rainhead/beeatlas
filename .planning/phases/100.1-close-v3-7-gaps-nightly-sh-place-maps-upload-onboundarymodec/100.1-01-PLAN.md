---
phase: 100.1-close-v3-7-gaps-nightly-sh-place-maps-upload-onboundarymodec
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - data/nightly.sh
  - src/bee-atlas.ts
autonomous: true
gap_closure: true
requirements:
  - GAP-100-01
  - GAP-100-02

must_haves:
  truths:
    - "nightly.sh uploads place-maps SVGs to s3://$BUCKET/data/place-maps/ on every run"
    - "nightly.sh invalidates /data/place-maps/* in CloudFront alongside species-maps"
    - "Switching boundary mode away from 'places' clears _filterState.selectedPlace and re-runs the filter query"
    - "Switching between non-places boundary modes (off ↔ counties ↔ ecoregions) does not trigger a redundant filter query"
  artifacts:
    - path: data/nightly.sh
      provides: "S3 upload + CloudFront invalidation for place-maps"
      contains: "$EXPORT_DIR/place-maps/"
    - path: src/bee-atlas.ts
      provides: "_onBoundaryModeChanged clears selectedPlace when leaving places mode"
      contains: "_onBoundaryModeChanged"
  key_links:
    - from: "data/nightly.sh aws s3 cp --recursive place-maps/"
      to: "s3://$BUCKET/data/place-maps/"
      via: "aws s3 cp --recursive"
      pattern: "s3://\\$BUCKET/data/place-maps/"
    - from: "data/nightly.sh cloudfront create-invalidation"
      to: "/data/place-maps/*"
      via: "--paths argument"
      pattern: "/data/place-maps/\\*"
    - from: "bee-atlas.ts _onBoundaryModeChanged"
      to: "_runFilterQuery + _pushUrlState"
      via: "clearing selectedPlace then re-running filter"
      pattern: "_onBoundaryModeChanged"
---

<objective>
Close two gaps left open at the end of v3.7 (Phase 100): (1) the nightly pipeline generates per-place SVG maps but never uploads them to S3 or invalidates them in CloudFront, so production never sees them; (2) when the user toggles the map boundary mode away from `'places'`, the place filter chip silently remains active even though place boundaries are no longer rendered, producing a confusing filter-without-visual state.

Purpose: Bring v3.7 to a coherent shipped state — the pipeline outputs reach the CDN, and the UI state stays internally consistent when the user changes boundary modes.

Output: Two small, surgical edits to `data/nightly.sh` and `src/bee-atlas.ts`. No new files. No new tests required beyond verification commands.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/100-map-filter-integration/100-VERIFICATION.md
@CLAUDE.md

<interfaces>
<!-- Current state of the two functions being modified. Executor should match these patterns. -->

From data/nightly.sh (current upload block, lines 148-160):

The species-maps upload uses:
  aws --profile "$AWS_PROFILE" s3 cp --recursive --no-progress "$EXPORT_DIR/species-maps/" "s3://$BUCKET/data/species-maps/"

The CloudFront invalidation includes:
  --paths "/data/manifest.json" "/data/feeds/*" "/data/species-maps/*"

Place-maps SVGs are written by data/places_maps.py to: $EXPORT_DIR/place-maps/{slug}.svg
(mkdir is idempotent; pipeline always materializes the directory, even when empty.)

From src/bee-atlas.ts (current _onBoundaryModeChanged, lines 1010-1013):

  private _onBoundaryModeChanged(e: CustomEvent<'off' | 'counties' | 'ecoregions' | 'places'>) {
    this._boundaryMode = e.detail;
    this._pushUrlState();
  }

For reference, _onPlaceSelected (lines 678-697) is the canonical pattern for "clear selectedPlace then re-run filter":
  - mutates _filterState immutably (spread + selectedPlace: null)
  - resets sidebar/selection state
  - resets _tablePage to 1
  - calls _runFilterQuery().then(() => this._pushUrlState())
  - calls _runTableQuery()

Type of _filterState.selectedPlace: string | null
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Upload place-maps to S3 and invalidate CloudFront in nightly.sh</name>
  <files>data/nightly.sh</files>
  <read_first>
    - data/nightly.sh (the entire script — pattern matching against the species-maps upload block on lines 148-160 is load-bearing)
    - data/places_maps.py (confirms output path is $EXPORT_DIR/place-maps/{slug}.svg and that the directory is always created via mkdir(parents=True, exist_ok=True))
    - CLAUDE.md (project conventions: static hosting, no server runtime, deploy via CDK)
  </read_first>
  <action>
    Add S3 upload of place-maps and extend the CloudFront invalidation to cover them.

    Step 1 — Insert an `aws s3 cp --recursive` call for place-maps immediately after the existing species-maps upload on line 150. Match the species-maps invocation exactly: same `--profile "$AWS_PROFILE"`, same `--recursive --no-progress` flags, same `s3://$BUCKET/data/place-maps/` destination convention. Source path must be `"$EXPORT_DIR/place-maps/"` (trailing slash, matching species-maps). Do NOT use hashed filenames — these are stable URLs, same as species-maps (the existing comment on line 148 "Feeds and species-maps use stable (non-hashed) URLs for external consumers" applies; extend the comment to include place-maps).

    Step 2 — Extend the CloudFront invalidation `--paths` argument on line 159 to include `/data/place-maps/*` as an additional path. The final --paths list must be: `"/data/manifest.json" "/data/feeds/*" "/data/species-maps/*" "/data/place-maps/*"` (in that order, preserving existing entries verbatim).

    Step 3 — No other changes. Do NOT add hashed-URL helpers for place-maps. Do NOT touch the manifest HEREDOC (place-maps are not enumerated in manifest.json; they are looked up by slug client-side). Do NOT modify run.py, places_maps.py, or any other file.

    Per CLAUDE.md: place-maps are static assets — no server runtime is introduced.
  </action>
  <verify>
    <automated>grep -n '$EXPORT_DIR/place-maps/' data/nightly.sh | grep -v '^#' | grep -c 'aws.*s3 cp --recursive' | grep -q '^1$' &amp;&amp; grep -F '/data/place-maps/*' data/nightly.sh | grep -v '^#' | grep -q 'cloudfront\|--paths\|/data/place-maps/\*' &amp;&amp; bash -n data/nightly.sh</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: `data/nightly.sh` contains exactly one non-comment line invoking `aws ... s3 cp --recursive ... "$EXPORT_DIR/place-maps/" "s3://$BUCKET/data/place-maps/"` (with `--profile "$AWS_PROFILE"` and `--no-progress`).
    - Source assertion: the new upload line appears AFTER the existing species-maps upload line (line ordering matters for log readability).
    - Source assertion: the `cloudfront create-invalidation --paths` list contains `/data/place-maps/*` AND retains all four existing paths (`/data/manifest.json`, `/data/feeds/*`, `/data/species-maps/*`).
    - CLI assertion: `bash -n data/nightly.sh` exits 0 (syntax check passes).
    - CLI assertion: `grep -c 'aws .* s3 cp --recursive' data/nightly.sh` returns 2 (one for species-maps, one for place-maps — and no more).
    - Behavior assertion: no changes to the manifest HEREDOC, the hashed upload helper, or run.py.
  </acceptance_criteria>
  <done>
    Two lines added to nightly.sh: one S3 upload mirroring the species-maps pattern, one new path in the CloudFront --paths list. Syntax check passes. Comment on stable-URLs (line 148) extended to mention place-maps.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Clear selectedPlace in _onBoundaryModeChanged when leaving places mode</name>
  <files>src/bee-atlas.ts</files>
  <read_first>
    - src/bee-atlas.ts (specifically lines 678-697 for the _onPlaceSelected pattern, and lines 1010-1013 for the current _onBoundaryModeChanged stub)
    - CLAUDE.md (Architecture Invariants: state ownership in &lt;bee-atlas&gt;, filter race guard via _filterQueryGeneration)
    - .planning/phases/100-map-filter-integration/100-VERIFICATION.md (confirms _onPlaceSelected is the canonical place-clear pattern)
  </read_first>
  <action>
    Extend `_onBoundaryModeChanged` (currently at src/bee-atlas.ts line 1010) so that when the user switches the boundary mode away from `'places'`, the active place filter is cleared and the filter/table queries are re-run.

    Concrete behavior:
    - Capture `const newMode = e.detail;` at the top of the method.
    - Always set `this._boundaryMode = newMode;` (preserve existing behavior).
    - Compute `const leavingPlaces = newMode !== 'places' && this._filterState.selectedPlace !== null;`.
    - If `leavingPlaces` is true: set `this._filterState = { ...this._filterState, selectedPlace: null };` then reset `this._tablePage = 1;` then call `this._runFilterQuery().then(() => this._pushUrlState());` and `this._runTableQuery();`. Do NOT call `_pushUrlState` directly in this branch — the `.then()` handler covers it (matches the _onPlaceSelected pattern exactly).
    - If `leavingPlaces` is false (either staying in places mode, or no place was selected to begin with): preserve the existing single-line behavior — just call `this._pushUrlState();`. No filter query, no table query.
    - Do NOT clear `_selectedOccurrences`, `_selectedOccIds`, `_selectedCluster`, `_selectionBounds`, or `_sidebarOpen` in this method. Selection state is orthogonal to place-filter state; only `_onPlaceSelected` clears selection because it represents a deliberate map click. Boundary-mode toggles are UI-chrome changes and should not nuke an open sidebar.

    Concrete signature unchanged: `private _onBoundaryModeChanged(e: CustomEvent<'off' | 'counties' | 'ecoregions' | 'places'>)`.

    Per CLAUDE.md filter race guard: `_runFilterQuery` already increments `_filterQueryGeneration` internally (line 334), so no additional guard is needed in this caller.
  </action>
  <verify>
    <automated>npx tsc --noEmit &amp;&amp; npm test -- --run 2>&amp;1 | tail -5 | grep -q 'passed\|Test Files.*passed'</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: `grep -n 'leavingPlaces\|selectedPlace: null' src/bee-atlas.ts` returns at least one match inside `_onBoundaryModeChanged` (lines 1010-1040 range after edit).
    - Source assertion: `_onBoundaryModeChanged` calls `this._runFilterQuery()` exactly once, inside a conditional branch (not unconditionally).
    - Source assertion: `_onBoundaryModeChanged` does NOT mutate `_selectedOccurrences`, `_selectedOccIds`, `_selectedCluster`, `_selectionBounds`, or `_sidebarOpen` (selection state is preserved across boundary-mode toggles).
    - Source assertion: `_onBoundaryModeChanged` retains the `this._boundaryMode = e.detail;` (or equivalent `= newMode`) assignment unconditionally.
    - CLI assertion: `npx tsc --noEmit` exits 0 (no type errors).
    - CLI assertion: `npm test -- --run` reports all tests passing (413+ tests; the existing v3.7 verification baseline must hold).
    - Behavior assertion: switching boundary mode from `'places'` to any other value while `selectedPlace` is set triggers exactly one `_runFilterQuery` and one `_runTableQuery` call; switching between non-places modes triggers neither.
  </acceptance_criteria>
  <done>
    `_onBoundaryModeChanged` clears `selectedPlace` and re-runs the filter/table queries when leaving places mode while a place is selected; preserves single-line behavior otherwise; typecheck and test suite green.
  </done>
</task>

</tasks>

<verification>
After both tasks complete:

1. `bash -n data/nightly.sh` — syntax check passes.
2. `grep -c 'aws .* s3 cp --recursive' data/nightly.sh` — returns 2.
3. `grep -F '/data/place-maps/*' data/nightly.sh` — finds the path in the CloudFront --paths invocation.
4. `npx tsc --noEmit` — exits 0.
5. `npm test -- --run` — all tests pass (regression check: 413+ tests, baseline from Phase 100 verification).
6. Manual code review: `_onBoundaryModeChanged` does not clear selection state (sidebar, occurrences, cluster, bounds).
</verification>

<success_criteria>
- nightly.sh uploads `$EXPORT_DIR/place-maps/` to `s3://$BUCKET/data/place-maps/` once per run.
- nightly.sh invalidates `/data/place-maps/*` in the same CloudFront call as species-maps.
- _onBoundaryModeChanged clears `_filterState.selectedPlace` and re-runs the filter when the new mode is not `'places'` AND a place was selected.
- _onBoundaryModeChanged preserves all other selection/sidebar state.
- TypeScript typecheck and full vitest suite remain green.
- No changes outside the two files listed in files_modified.
</success_criteria>

<output>
Create `.planning/phases/100.1-close-v3-7-gaps-nightly-sh-place-maps-upload-onboundarymodec/100.1-01-SUMMARY.md` when done. Note that both gaps were closed in this single plan, and reference the two files modified.
</output>
