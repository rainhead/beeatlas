---
phase: 141-built-asset-fixtures-red-test-fixes-silent-skip-elimination
plan: 03
type: execute
wave: 2
depends_on:
  - 141-01
files_modified:
  - data/tests/test_species_export.py
  - data/tests/test_dbt_synonymy.py
autonomous: true
requirements:
  - TFIXTURE-03
  - TFIX-04
  - TTIER-02

must_haves:
  truths:
    - "D-01: test_species_export.py has a sandbox_parquet fixture that COPYs species_fixture.csv + higher_taxa_fixture.csv to a tmp sandbox dir and redirects se_mod.DBT_SANDBOX_DIR and se_mod.ASSETS_DIR via monkeypatch.setattr; the sandbox-dependent tests consume it instead of the guard decorators"
    - "sandbox_parquet also builds a minimal occurrences.parquet (export_species_parquet reads DBT_SANDBOX_DIR/occurrences.parquet for seasonality) so export_species_parquet() runs to completion on the fixture"
    - "D-01: test_dbt_synonymy.py has a synonymy_sandbox fixture that builds occurrences.parquet (canonical_name only, agapostemon subtilior present / texanus absent) + species.parquet from the committed CSVs and monkeypatches the test-module SANDBOX constant; the 3 tests consume it instead of the guard decorators"
    - "TTIER-02 / open questions: test_higher_taxa_json_written_and_12_subfamilies (== 12 subfamilies real-dataset property) and test_taxon_id (reads public/data/species.json downstream artifact) are tagged @pytest.mark.integration — not given fast-tier fixtures"
    - "TFIX-04: after this plan, test_species_export.py and test_dbt_synonymy.py have 0 asset-driven skips in the fast tier (the D-05 guard from Plan 01 would otherwise fail them)"
  artifacts:
    - path: "data/tests/test_species_export.py"
      provides: "sandbox_parquet builder fixture; guard decorators removed from fixturizable tests; @integration on the 12-subfamily and species.json tests"
      contains: "sandbox_parquet"
    - path: "data/tests/test_dbt_synonymy.py"
      provides: "synonymy_sandbox builder fixture; guard decorators removed; SANDBOX monkeypatched"
      contains: "synonymy_sandbox"
  key_links:
    - from: "data/tests/test_species_export.py:sandbox_parquet"
      to: "data/tests/fixtures/species_fixture.csv + higher_taxa_fixture.csv"
      via: "duckdb COPY read_csv(...) TO tmp sandbox; monkeypatch.setattr(se_mod, DBT_SANDBOX_DIR, sandbox)"
      pattern: "DBT_SANDBOX_DIR"
    - from: "data/tests/test_dbt_synonymy.py:synonymy_sandbox"
      to: "test_dbt_synonymy.SANDBOX constant"
      via: "monkeypatch.setattr on the test module SANDBOX"
      pattern: "synonymy_sandbox"
---

<objective>
Make the sandbox-parquet-dependent tests in test_species_export.py and test_dbt_synonymy.py run on a clean checkout off committed CSV fixtures, eliminating their asset-driven skips, and tag the two real-dataset assertions @integration (TFIXTURE-03 / TFIX-04 / TTIER-02 / D-01).

Two deliverables:
1. **test_species_export.py** — add a sandbox_parquet builder fixture (COPYs species_fixture.csv + higher_taxa_fixture.csv + a minimal occurrences.parquet into a tmp sandbox; redirects se_mod.DBT_SANDBOX_DIR and se_mod.ASSETS_DIR via monkeypatch.setattr). Remove the guard decorators from the fixturizable tests and have them consume the fixture. Tag test_higher_taxa_json_written_and_12_subfamilies (==12 real-data property) and test_taxon_id (reads public/data/species.json) @pytest.mark.integration.
2. **test_dbt_synonymy.py** — add a synonymy_sandbox builder fixture (builds occurrences.parquet with canonical_name only + species.parquet from the committed CSV; monkeypatches the test-module SANDBOX). Remove the guard decorators and have the 3 tests consume the fixture.

Depends on Plan 01 (the three fixture CSVs and the D-05 guard must exist). No file overlap with Plan 04.

Output: 2 modified test files.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/141-built-asset-fixtures-red-test-fixes-silent-skip-elimination/141-CONTEXT.md
@.planning/phases/141-built-asset-fixtures-red-test-fixes-silent-skip-elimination/141-RESEARCH.md
@.planning/phases/141-built-asset-fixtures-red-test-fixes-silent-skip-elimination/141-PATTERNS.md
@.planning/REQUIREMENTS.md
@./CLAUDE.md

<scope_fence>
STRICTLY OUT OF SCOPE here:
- Authoring the fixture CSVs or the conftest guard (Plan 01); resolver_db fix (Plan 02); test_dbt_diff / test_species_maps / fuzzy / WR (Plan 04).
- Any change to data/species_export.py (production) — use only the existing DBT_SANDBOX_DIR / ASSETS_DIR module constants as monkeypatch seams.
- Any change to run.py / nightly.sh / dbt models / the 33-column contract.
This plan touches ONLY data/tests/test_species_export.py and data/tests/test_dbt_synonymy.py.
</scope_fence>

<interfaces>
Verified from codebase 2026-06-06.

data/species_export.py module constants (the monkeypatch seams):
  line 39: ASSETS_DIR = Path(os.environ.get('EXPORT_DIR', _default_assets))
  line 45: DBT_SANDBOX_DIR = Path(os.environ.get('DBT_SANDBOX_DIR', str(Path(__file__).parent / 'dbt' / 'target' / 'sandbox')))
  RESEARCH Pitfall 1: monkeypatch.setenv is INSUFFICIENT (constant read once at import) — must monkeypatch.setattr(se_mod, DBT_SANDBOX_DIR, sandbox).

data/species_export.py:export_species_parquet (verified lines 195-207) reads BOTH:
  DBT_SANDBOX_DIR/species.parquet AND DBT_SANDBOX_DIR/occurrences.parquet (seasonality);
  raises FileNotFoundError if either is absent. => sandbox_parquet MUST also create occurrences.parquet.

data/tests/test_species_export.py structure (verified):
  line 26: import species_export as se_mod
  line 29: SANDBOX = Path(__file__).resolve().parent.parent / dbt / target / sandbox
  line 32-35: _SANDBOX_GUARD (skipif species.parquet missing; reason: run bash data/dbt/run.sh build)
  line 37-40: _HIGHER_TAXA_GUARD (skipif higher_taxa.parquet missing; same reason)
  line 42-45: _SPECIES_JSON_GUARD (skipif species.json missing; reason: run uv run python data/species_export.py)
  Tests:
    @_SANDBOX_GUARD only: test_slug_hierarchical(49), test_no_old_slug_format(67), test_inat_obs_count_in_species(90)  -> FIXTURIZE (read species.parquet directly)
    @_SANDBOX_GUARD + @_HIGHER_TAXA_GUARD: test_check_slug_collisions_clean_real_data(153), test_higher_rank_taxon_ids_not_written(213), test_export_runs_collision_check_clean(227) -> FIXTURIZE (call export_species_parquet; need all 3 parquets)
    @_SANDBOX_GUARD + @_HIGHER_TAXA_GUARD: test_higher_taxa_json_written_and_12_subfamilies(192) -> @integration (==12 real-dataset property; RESEARCH open Q#2)
    @_SPECIES_JSON_GUARD: test_taxon_id(105) -> @integration (reads public/data/species.json downstream artifact; RESEARCH open Q#1)
    no guard (pure unit, keep as-is): test_check_slug_collisions_raises_on_collision(123), test_check_slug_collisions_bombus_no_false_alarm(138)
  Existing fixturizable bodies do monkeypatch.setattr(se_mod, ASSETS_DIR, tmp_path) + monkeypatch.setenv(DBT_SANDBOX_DIR, str(SANDBOX)); after migration the fixture supplies both so those two lines are dropped.

data/tests/test_dbt_synonymy.py structure (verified):
  line 21: SANDBOX = ... (test-file constant defined IN this module, NOT a production constant)
  line 23-27: _SANDBOX_GUARD ; line 28: _SPECIES_GUARD
  Tests read via duckdb.execute(f... read_parquet({SANDBOX}/occurrences.parquet) / species.parquet):
    test_occurrences_has_agapostemon_subtilior(40): COUNT canonical_name=agapostemon subtilior >= 1
    test_occurrences_has_no_agapostemon_texanus(57): COUNT canonical_name=agapostemon texanus == 0
    test_inat_obs_count_uses_synonymized_canonical_name(79): reads species.parquet inat_obs_count
  RESEARCH Pitfall 2 (EXPLICIT FORM): monkeypatch the TEST-MODULE SANDBOX by importing the test module as a module object and setattr-ing on it:
      import tests.test_dbt_synonymy as m
      monkeypatch.setattr(m, "SANDBOX", sandbox)
  Do NOT use sys.modules[__name__] from a fixture (the fixture's __name__ is not the test module). The SANDBOX constant lives in the test module itself, not in any production module, so the target of setattr is the imported test-module object `m`.

Plan-01 fixtures available: data/tests/fixtures/species_fixture.csv (21 cols, 2 rows), higher_taxa_fixture.csv (12 cols), occurrences_fixture.csv (canonical_name; subtilior present, texanus absent).
FIXTURES_DIR = Path(__file__).parent / fixtures (add to each test file if not present).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add sandbox_parquet fixture to test_species_export.py; migrate fixturizable tests; tag the two real-data tests @integration</name>
  <files>data/tests/test_species_export.py</files>
  <read_first>
    - data/tests/test_species_export.py (the file being modified — guards lines 32-45, all test bodies)
    - data/species_export.py (lines 39, 45 constants; lines 171-235 export_species_parquet — confirm it reads species.parquet + occurrences.parquet)
    - .planning/phases/141-built-asset-fixtures-red-test-fixes-silent-skip-elimination/141-PATTERNS.md (test_species_export.py section — sandbox_parquet builder pattern, BOOLEAN CAST, guard removal; Pattern A/B/D)
    - .planning/phases/141-built-asset-fixtures-red-test-fixes-silent-skip-elimination/141-RESEARCH.md (1.1 affected tests + columns; 7 builder pattern; Pitfall 1 setattr-not-setenv; Pitfall 3 BOOLEAN; open Q#1/#2)
  </read_first>
  <action>
    In data/tests/test_species_export.py (D-01, TFIXTURE-03, TFIX-04, TTIER-02):

    (A) Add a module-level FIXTURES_DIR = Path(__file__).parent / fixtures (if not present) and a function-scoped sandbox_parquet(tmp_path, monkeypatch) fixture. It must: create tmp_path/sandbox; via a duckdb connection, COPY species_fixture.csv to sandbox/species.parquet using SELECT * REPLACE(CAST(on_checklist AS BOOLEAN) AS on_checklist) over read_csv(..., header=True, auto_detect=True) (RESEARCH Pitfall 3 — keep on_checklist BOOLEAN); COPY higher_taxa_fixture.csv to sandbox/higher_taxa.parquet; build a minimal sandbox/occurrences.parquet (export_species_parquet reads it for seasonality — create a tiny table with at least a canonical_name column, e.g. one agapostemon subtilior row, and COPY it). Then monkeypatch.setattr(se_mod, DBT_SANDBOX_DIR, sandbox) and monkeypatch.setattr(se_mod, ASSETS_DIR, tmp_path) (RESEARCH Pitfall 1 — setattr, not setenv). Return sandbox.

    (B) Migrate the six fixturizable tests to consume sandbox_parquet: remove @_SANDBOX_GUARD and @_HIGHER_TAXA_GUARD from test_slug_hierarchical, test_no_old_slug_format, test_inat_obs_count_in_species, test_check_slug_collisions_clean_real_data, test_higher_rank_taxon_ids_not_written, test_export_runs_collision_check_clean. Add sandbox_parquet to each signature; tests that previously did monkeypatch.setattr(se_mod, ASSETS_DIR, tmp_path) + monkeypatch.setenv(DBT_SANDBOX_DIR, ...) drop those two lines (the fixture handles both) and read from the fixture-provided sandbox (use the returned sandbox path or se_mod.DBT_SANDBOX_DIR for any direct read_parquet). Keep each assertion intact.

    (C) Tag the two real-dataset tests @pytest.mark.integration (RESEARCH open Q#1/#2): test_higher_taxa_json_written_and_12_subfamilies and test_taxon_id. For these two, keep a loud guard whose skip reason begins with the literal prefix integration-in-brackets so the D-05 conftest guard does NOT flag them; they are deselected from the fast tier by addopts.

    (D) Leave the two pure-unit tests (test_check_slug_collisions_raises_on_collision, test_check_slug_collisions_bombus_no_false_alarm) untouched. Do NOT modify data/species_export.py.
  </action>
  <acceptance_criteria>
    - test_species_export.py defines a sandbox_parquet fixture that monkeypatch.setattr's se_mod.DBT_SANDBOX_DIR and se_mod.ASSETS_DIR and COPYs species_fixture.csv (with CAST(on_checklist AS BOOLEAN)), higher_taxa_fixture.csv, and a minimal occurrences.parquet into tmp sandbox
    - The 6 fixturizable tests no longer carry _SANDBOX_GUARD / _HIGHER_TAXA_GUARD and take sandbox_parquet
    - test_higher_taxa_json_written_and_12_subfamilies and test_taxon_id are decorated @pytest.mark.integration
    - The fast-tier run of test_species_export.py reports 0 skips and 0 failures, with the @integration tests deselected (the D-05 guard from Plan 01 does not trip)
    - data/species_export.py is NOT modified
  </acceptance_criteria>
  <verify>
    <automated>cd data && grep -q "def sandbox_parquet" tests/test_species_export.py && grep -q "CAST(on_checklist AS BOOLEAN)" tests/test_species_export.py && git diff --quiet species_export.py && uv run pytest tests/test_species_export.py -m "not integration" -q -rs</automated>
  </verify>
  <done>test_species_export.py builds species/higher_taxa/occurrences parquet from committed CSVs via sandbox_parquet; the 6 fixturizable tests pass with 0 skips in the fast tier; the 12-subfamily and species.json tests are @integration; production unchanged.</done>
</task>

<task type="auto">
  <name>Task 2: Add synonymy_sandbox fixture to test_dbt_synonymy.py; migrate the 3 tests off the guard decorators</name>
  <files>data/tests/test_dbt_synonymy.py</files>
  <read_first>
    - data/tests/test_dbt_synonymy.py (the file being modified — SANDBOX line 21, guards 23-28, the 3 test bodies)
    - .planning/phases/141-built-asset-fixtures-red-test-fixes-silent-skip-elimination/141-PATTERNS.md (test_dbt_synonymy.py section — synonymy_sandbox builder, test-module SANDBOX monkeypatch, occurrences inline-build)
    - .planning/phases/141-built-asset-fixtures-red-test-fixes-silent-skip-elimination/141-RESEARCH.md (1.2 affected tests + minimal occurrences; Pitfall 2 test-module SANDBOX; Pitfall 3 BOOLEAN)
  </read_first>
  <action>
    In data/tests/test_dbt_synonymy.py (D-01, TFIXTURE-03, TFIX-04):

    (A) Add FIXTURES_DIR = Path(__file__).parent / fixtures (if not present) and a function-scoped synonymy_sandbox(tmp_path, monkeypatch) fixture. It must: create tmp_path/sandbox; via a duckdb connection, build sandbox/occurrences.parquet with a minimal schema — only canonical_name is asserted on — by building a table from occurrences_fixture.csv (one agapostemon subtilior row, zero agapostemon texanus) and COPYing it (or read occurrences_fixture.csv directly via read_csv then COPY); COPY species_fixture.csv to sandbox/species.parquet with SELECT * REPLACE(CAST(on_checklist AS BOOLEAN) AS on_checklist) (RESEARCH Pitfall 3). Then monkeypatch the TEST-MODULE SANDBOX constant using the EXPLICIT RESEARCH Pitfall 2 form: import the test module as a module object and setattr SANDBOX on it —
        import tests.test_dbt_synonymy as m
        monkeypatch.setattr(m, "SANDBOX", sandbox)
    Do NOT use sys.modules[__name__] from inside the fixture (the fixture's __name__ is the conftest/module it is defined in, not the test module). The SANDBOX constant is defined in test_dbt_synonymy.py itself, so the setattr target is the imported test-module object `m`, not any production module. Return sandbox.

    (B) Remove @_SANDBOX_GUARD and @_SPECIES_GUARD from all 3 tests (test_occurrences_has_agapostemon_subtilior, test_occurrences_has_no_agapostemon_texanus, test_inat_obs_count_uses_synonymized_canonical_name) and add synonymy_sandbox to each signature. The test bodies read read_parquet(f{SANDBOX}/...) — because SANDBOX is monkeypatched to the tmp sandbox, the existing f-string reads now hit the fixture parquet. Keep all 3 assertions intact (subtilior COUNT >= 1; texanus COUNT == 0; species inat_obs_count read).

    Do NOT modify data/species_export.py or any production module.
  </action>
  <acceptance_criteria>
    - test_dbt_synonymy.py defines a synonymy_sandbox fixture that builds occurrences.parquet (canonical_name; agapostemon subtilior present, texanus absent) and species.parquet, and monkeypatches the test-module SANDBOX constant via the imported module object (monkeypatch.setattr(m, "SANDBOX", sandbox) where m is the imported test module) — NOT via sys.modules[__name__]
    - The 3 tests no longer carry _SANDBOX_GUARD / _SPECIES_GUARD and take synonymy_sandbox
    - The fast-tier run of test_dbt_synonymy.py reports 0 skips and 0 failures (D-05 guard does not trip)
    - No production module is modified
  </acceptance_criteria>
  <verify>
    <automated>cd data && grep -q "def synonymy_sandbox" tests/test_dbt_synonymy.py && grep -Eq 'monkeypatch\.setattr\(\s*m\s*,\s*["'"'"']SANDBOX["'"'"']' tests/test_dbt_synonymy.py && ! grep -q "sys.modules\[__name__\]" tests/test_dbt_synonymy.py && uv run pytest tests/test_dbt_synonymy.py -m "not integration" -q -rs</automated>
  </verify>
  <done>test_dbt_synonymy.py builds occurrences/species parquet from committed CSVs via synonymy_sandbox (test-module SANDBOX monkeypatched via the imported module object, not sys.modules[__name__]); the 3 tests pass with 0 skips in the fast tier; no production module changed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none) | This plan introduces no new trust boundary. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-141-03 | (n/a) | two test-fixture builder functions that COPY committed CSVs to tmp parquet + monkeypatch module path constants (test infrastructure only) | accept | No security-relevant attack surface. The builders read static committed test CSVs and write to pytest tmp_path; SANDBOX/DBT_SANDBOX_DIR redirection is test-only. No new network, secrets, auth, or untrusted-input parsing. ASVS L1: nothing to mitigate. (Mirrors Phase 140 T-140-01.) |

No HIGH findings. No new package installs (no Package Legitimacy Gate needed).
</threat_model>

<verification>
Cheap, seconds-scale, per touched file (the host SIGKILLs long runs — never run the whole suite or -m integration here):

```
cd data && uv run pytest tests/test_species_export.py -m "not integration" -q -rs   # 0 skips, 0 failures
cd data && uv run pytest tests/test_dbt_synonymy.py -m "not integration" -q -rs     # 0 skips, 0 failures
cd data && git diff --quiet species_export.py                                        # production untouched
```
</verification>

<success_criteria>
- test_species_export.py: sandbox_parquet builds species/higher_taxa/occurrences parquet from committed CSVs; 6 fixturizable tests pass with 0 fast-tier skips; the ==12-subfamily and species.json tests are @integration (TFIXTURE-03, TFIX-04, TTIER-02)
- test_dbt_synonymy.py: synonymy_sandbox builds occurrences/species parquet and monkeypatches the test-module SANDBOX via the imported module object; 3 tests pass with 0 fast-tier skips (TFIXTURE-03, TFIX-04)
- The Plan-01 D-05 guard does not trip on either file in the fast tier (no asset-driven skip remains)
- Scope fence honored: only the two test files modified; no production / run.py / nightly.sh / dbt changes
</success_criteria>

<output>
Create `.planning/phases/141-built-asset-fixtures-red-test-fixes-silent-skip-elimination/141-03-SUMMARY.md` when done.
</output>
