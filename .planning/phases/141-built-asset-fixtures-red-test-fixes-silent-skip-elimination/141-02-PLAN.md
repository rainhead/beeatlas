---
phase: 141-built-asset-fixtures-red-test-fixes-silent-skip-elimination
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - data/tests/test_resolve_taxon_ids.py
autonomous: true
requirements:
  - TFIX-01

must_haves:
  truths:
    - "D-06: the resolver_db fixture creates schema dbt_sandbox with table occurrence_synonyms(synonym TEXT, accepted_name TEXT, source TEXT) and schema inaturalist_waba_data with table observations(taxon__name TEXT) — the two UNION-arm tables _names_to_resolve queries that were missing"
    - "The ~16 test_resolve_taxon_ids.py tests no longer raise CatalogException; they run and assert real resolution behavior (cold-start union shape, pacing/retry, cache idempotency, CSV reasons, _pick_match, rank-ladder, bridge source)"
    - "The fix is purely additive to the resolver_db fixture — no test body assertion is weakened, no production code (resolve_taxon_ids.py) is touched"
  artifacts:
    - path: "data/tests/test_resolve_taxon_ids.py"
      provides: "resolver_db fixture extended with dbt_sandbox.occurrence_synonyms + inaturalist_waba_data.observations stubs"
      contains: "dbt_sandbox.occurrence_synonyms"
  key_links:
    - from: "data/tests/test_resolve_taxon_ids.py:resolver_db"
      to: "data/resolve_taxon_ids.py:_names_to_resolve UNION arms"
      via: "CREATE SCHEMA dbt_sandbox + CREATE TABLE dbt_sandbox.occurrence_synonyms / inaturalist_waba_data.observations"
      pattern: "CREATE SCHEMA dbt_sandbox"
---

<objective>
Fix the ~16 failing tests in test_resolve_taxon_ids.py (TFIX-01 / D-06). All 16 currently crash with `CatalogException: schema "dbt_sandbox" does not exist` because the `resolver_db` fixture omits two of the five tables that `resolve_taxon_ids.py:_names_to_resolve` UNIONs over.

Single deliverable: extend the existing `resolver_db` fixture with the two missing schema+table stubs (`dbt_sandbox.occurrence_synonyms` and `inaturalist_waba_data.observations`). The fix is additive and behavior-preserving — empty stub tables correctly contribute 0 rows to the UNION in isolation, which is the expected state for these unit tests. No production code and no test assertions change.

Output: 1 modified test file.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/141-built-asset-fixtures-red-test-fixes-silent-skip-elimination/141-CONTEXT.md
@.planning/phases/141-built-asset-fixtures-red-test-fixes-silent-skip-elimination/141-RESEARCH.md
@.planning/phases/141-built-asset-fixtures-red-test-fixes-silent-skip-elimination/141-PATTERNS.md
@.planning/REQUIREMENTS.md
@./CLAUDE.md

<scope_fence>
STRICTLY OUT OF SCOPE here:
- Any change to data/resolve_taxon_ids.py (the production module) — the fix is in the test fixture only.
- Any change to the 16 test bodies or their assertions (they already assert real behavior — they just need the two tables to exist).
- The parquet fixtures / conftest guard (Plan 01); test_dbt_diff / test_species_maps / fuzzy / WR work (Plan 04); test_species_export / test_dbt_synonymy rewrites (Plan 03).
This plan touches ONLY data/tests/test_resolve_taxon_ids.py. It is independent of Plan 01 (no shared files) and runs in the same wave.
</scope_fence>

<interfaces>
Verified from codebase 2026-06-06.

data/resolve_taxon_ids.py:_names_to_resolve SQL (lines ~340-368) UNIONs 5 arms (RESEARCH 2.1):
  checklist_data.species, ecdysis_data.occurrences, inat_obs_data.observations,
  dbt_sandbox.occurrence_synonyms (SELECT DISTINCT accepted_name),
  inaturalist_waba_data.observations (SELECT DISTINCT lower(trim(...)))

data/tests/test_resolve_taxon_ids.py:resolver_db fixture (verified — lines 49-87):
  line 78: con.execute("CREATE SCHEMA checklist_data")
  line 79: con.execute("CREATE SCHEMA ecdysis_data")
  line 80: con.execute("CREATE SCHEMA inaturalist_data")
  line 81: con.execute("CREATE TABLE checklist_data.species (canonical_name TEXT)")
  line 82: con.execute("CREATE TABLE ecdysis_data.occurrences (canonical_name TEXT)")
  line 83: con.execute("CREATE SCHEMA inat_obs_data")
  line 84: con.execute("CREATE TABLE inat_obs_data.observations (canonical_name TEXT)")
  line 85: # Bridge created lazily by resolve_taxon_ids via CREATE TABLE IF NOT EXISTS.
  line 86: con.close()
  line 87: return db_path, resolve_taxon_ids
  MISSING: dbt_sandbox schema + occurrence_synonyms table; inaturalist_waba_data schema + observations table.

Real seed schema (verified data/dbt/seeds/occurrence_synonyms.csv header): synonym,accepted_name,source
  _names_to_resolve queries only accepted_name from this table; synonym/source are structural completeness.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend resolver_db with the two missing UNION-arm tables</name>
  <files>data/tests/test_resolve_taxon_ids.py</files>
  <read_first>
    - data/tests/test_resolve_taxon_ids.py (the file being modified — esp. resolver_db lines 49-87)
    - data/resolve_taxon_ids.py (_names_to_resolve SQL, the 5 UNION arms — confirm the two table names and that only accepted_name / a name column are queried)
    - .planning/phases/141-built-asset-fixtures-red-test-fixes-silent-skip-elimination/141-PATTERNS.md (test_resolve_taxon_ids.py section — exact insertion point and DDL)
    - .planning/phases/141-built-asset-fixtures-red-test-fixes-silent-skip-elimination/141-RESEARCH.md (2.2 / 2.3 verified-working DDL; 2.4 what the 16 tests assert)
  </read_first>
  <action>
    In data/tests/test_resolve_taxon_ids.py, inside the resolver_db fixture, insert two schema+table creations immediately after the existing `con.execute("CREATE TABLE inat_obs_data.observations (canonical_name TEXT)")` (current line 84) and before `con.close()` (current line 86) (D-06):

      - CREATE SCHEMA dbt_sandbox, then CREATE TABLE dbt_sandbox.occurrence_synonyms with columns (synonym TEXT, accepted_name TEXT, source TEXT) — matching the real seed header; _names_to_resolve queries only accepted_name from it.
      - CREATE SCHEMA inaturalist_waba_data, then CREATE TABLE inaturalist_waba_data.observations with column (taxon__name TEXT) — the column the 5th UNION arm reads.

    Leave both tables empty (no seeded rows): an empty table contributes 0 rows to its UNION arm, which is the correct expected state for these isolated unit tests (RESEARCH 2.4). Add a brief inline comment noting D-06 and that these are the two UNION-arm tables _names_to_resolve queries. Do NOT change any test body, any assertion, or data/resolve_taxon_ids.py.
  </action>
  <acceptance_criteria>
    - data/tests/test_resolve_taxon_ids.py contains `CREATE SCHEMA dbt_sandbox` and a CREATE TABLE for `dbt_sandbox.occurrence_synonyms` with columns synonym, accepted_name, source
    - It contains `CREATE SCHEMA inaturalist_waba_data` and a CREATE TABLE for `inaturalist_waba_data.observations (taxon__name ...)`
    - The two creations sit inside the resolver_db fixture, before con.close()
    - data/resolve_taxon_ids.py is NOT modified; no test assertion in the file is changed
    - All test_resolve_taxon_ids.py fast-tier tests pass (0 failures, 0 CatalogException) — the 16 previously-crashing tests now assert real resolution behavior
  </acceptance_criteria>
  <verify>
    <automated>cd data && grep -q "CREATE SCHEMA dbt_sandbox" tests/test_resolve_taxon_ids.py && grep -q "dbt_sandbox.occurrence_synonyms" tests/test_resolve_taxon_ids.py && grep -q "inaturalist_waba_data.observations" tests/test_resolve_taxon_ids.py && git diff --quiet resolve_taxon_ids.py && uv run pytest tests/test_resolve_taxon_ids.py -m 'not integration' -q</automated>
  </verify>
  <done>resolver_db creates dbt_sandbox.occurrence_synonyms and inaturalist_waba_data.observations; all test_resolve_taxon_ids.py fast-tier tests pass with real-behavior assertions; production resolve_taxon_ids.py unchanged.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none) | This plan introduces no new trust boundary. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-141-02 | (n/a) | additive DDL stubs in the resolver_db test fixture (two empty in-memory tables) | accept | No security-relevant attack surface. The change creates empty in-memory DuckDB tables consumed only by unit tests; no production code, network, secrets, auth, or untrusted input. ASVS L1: nothing to mitigate. (Mirrors Phase 140 T-140-01.) |

No HIGH findings. No new package installs (no Package Legitimacy Gate needed).
</threat_model>

<verification>
Cheap, seconds:

```
cd data && grep -q "CREATE SCHEMA dbt_sandbox" tests/test_resolve_taxon_ids.py
cd data && git diff --quiet resolve_taxon_ids.py   # production module untouched
cd data && uv run pytest tests/test_resolve_taxon_ids.py -m 'not integration' -q   # 0 failures
```
</verification>

<success_criteria>
- resolver_db provides dbt_sandbox.occurrence_synonyms and inaturalist_waba_data.observations (D-06)
- All test_resolve_taxon_ids.py fast-tier tests pass (TFIX-01) — 16 previously-crashing tests assert real behavior
- Fix is additive; production resolve_taxon_ids.py and all test assertions unchanged
- Scope fence honored: only test_resolve_taxon_ids.py modified
</success_criteria>

<output>
Create `.planning/phases/141-built-asset-fixtures-red-test-fixes-silent-skip-elimination/141-02-SUMMARY.md` when done.
</output>
