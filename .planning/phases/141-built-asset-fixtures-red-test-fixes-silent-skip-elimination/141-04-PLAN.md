---
phase: 141-built-asset-fixtures-red-test-fixes-silent-skip-elimination
plan: 04
type: execute
wave: 2
depends_on:
  - 141-01
files_modified:
  - data/tests/test_dbt_diff.py
  - data/tests/test_species_maps.py
  - data/tests/test_resolve_checklist_names.py
  - data/tests/test_checklist_pipeline.py
autonomous: true
requirements:
  - TFIX-02
  - TFIX-03
  - TFIX-04
  - TTIER-02

must_haves:
  truths:
    - "D-04 / TFIX-02 / TTIER-02: test_dbt_diff.py has a module-level pytestmark = pytest.mark.integration so all 15 cross-artifact diff tests are deselected (not skipped) from the fast tier; the _SANDBOX_GUARD reason is hardened with an [integration] prefix as a loud guard for explicit -m integration runs"
    - "D-07 / TFIX-03: test_at_least_13_fuzzy_candidates in test_resolve_checklist_names.py is tagged @pytest.mark.integration (the >=13 threshold is a real-dataset property meaningless against the 4-row fixture bridge); the >=13 assertion is NOT weakened"
    - "TFIX-04: test_species_maps.py::test_generate_group_maps_emits_subfamily_svgs is tagged @pytest.mark.integration and its inline pytest.skip reason is prefixed [integration] (==12 subfamilies is a real-dataset property)"
    - "D-08 / WR-01: the checklist_db fixture in test_checklist_pipeline.py replaces importlib.reload with save/restore of mod.DB_PATH (old = mod.DB_PATH; mod.DB_PATH = db_path; yield; mod.DB_PATH = old) so it no longer clobbers checklist_sample_db's patches under pytest-randomly"
    - "D-09 / WR-02: the two relaxed n >= 1 assertions are pinned to exact counts — 6 distinct species and 8 (species, county) rows — matching the fixture"
    - "TFIX-04: after this plan, none of these four files produce an asset-driven skip in the fast tier; the only skips are [integration]-prefixed loud guards on deselected tests"
  artifacts:
    - path: "data/tests/test_dbt_diff.py"
      provides: "module-level pytestmark = pytest.mark.integration; hardened [integration] guard"
      contains: "pytestmark = pytest.mark.integration"
    - path: "data/tests/test_species_maps.py"
      provides: "@integration on test_generate_group_maps_emits_subfamily_svgs; [integration]-prefixed skip"
      contains: "pytest.mark.integration"
    - path: "data/tests/test_resolve_checklist_names.py"
      provides: "@integration on test_at_least_13_fuzzy_candidates (>=13 retained)"
      contains: "pytest.mark.integration"
    - path: "data/tests/test_checklist_pipeline.py"
      provides: "checklist_db save/restore of DB_PATH (no importlib.reload); exact 6/8 count assertions"
      contains: "mod.DB_PATH = old"
  key_links:
    - from: "data/tests/test_dbt_diff.py:pytestmark"
      to: "data/pyproject.toml addopts -m 'not integration'"
      via: "module-level pytest.mark.integration deselects all 15 tests from the fast tier"
      pattern: "pytestmark = pytest.mark.integration"
    - from: "data/tests/test_checklist_pipeline.py:checklist_db"
      to: "checklist_pipeline.DB_PATH"
      via: "save/restore (old = mod.DB_PATH; ... ; mod.DB_PATH = old) instead of importlib.reload"
      pattern: "importlib.reload"
---

<objective>
Tag the genuine full-data checks into the integration tier and harden the two Phase-140-review fixture hazards (TFIX-02 / TFIX-03 / TFIX-04 / TTIER-02 + WR-01/WR-02 / D-04, D-07, D-08, D-09).

Four deliverables, one per file (no file overlap with Plan 03):
1. **test_dbt_diff.py** (D-04 / TFIX-02 / TTIER-02) — module-level `pytestmark = pytest.mark.integration` deselects all 15 cross-artifact diff tests from the fast tier (a fixture version would be tautological per D-04). Harden `_SANDBOX_GUARD` reason with an `[integration]` prefix as a loud guard for explicit `-m integration` runs without built assets.
2. **test_resolve_checklist_names.py** (D-07 / TFIX-03) — tag `test_at_least_13_fuzzy_candidates` `@pytest.mark.integration`; the `>=13` threshold is correct for the full dataset and stays unweakened.
3. **test_species_maps.py** (TFIX-04 / RESEARCH §1.3) — tag `test_generate_group_maps_emits_subfamily_svgs` `@pytest.mark.integration`; prefix its inline skip reason `[integration]`.
4. **test_checklist_pipeline.py** (D-08/D-09 / WR-01/WR-02) — replace `importlib.reload` in the `checklist_db` fixture with save/restore of `mod.DB_PATH`; pin the two `n >= 1` assertions to exact counts (6 species, 8 county rows).

Depends on Plan 01 (the D-05 guard must exist so the fast tier proves 0 asset-driven skips). No file overlap with Plan 03.

Output: 4 modified test files.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/141-built-asset-fixtures-red-test-fixes-silent-skip-elimination/141-CONTEXT.md
@.planning/phases/141-built-asset-fixtures-red-test-fixes-silent-skip-elimination/141-RESEARCH.md
@.planning/phases/141-built-asset-fixtures-red-test-fixes-silent-skip-elimination/141-PATTERNS.md
@.planning/phases/140-checklist-taxonomy-fixture-distillation/140-REVIEW.md
@.planning/REQUIREMENTS.md
@./CLAUDE.md

<scope_fence>
STRICTLY OUT OF SCOPE here:
- Fixture CSVs / conftest guard (Plan 01); resolver_db (Plan 02); test_species_export / test_dbt_synonymy (Plan 03).
- Rebuilding public/data parquet or regenerating any built asset (the test_dbt_diff row-count/schema failures are real stale-public-parquet drift; they are surfaced in the integration tier in nightly — Phase 142 — NOT fixed here).
- Weakening the >=13 fuzzy threshold or the test_dbt_diff assertions.
- Any change to run.py / nightly.sh / dbt models / production modules.
This plan touches ONLY the four named test files.
</scope_fence>

<interfaces>
Verified from codebase 2026-06-06.

data/tests/test_dbt_diff.py (verified): SANDBOX constant + _SANDBOX_GUARD (lines ~27-37, reason "run `bash data/dbt/run.sh build`"). 15 tests total (RESEARCH §3.2): 2 currently FAIL (test_occurrences_row_count_matches, test_occurrences_schema_matches) due to stale 37-col public parquet vs 33-col sandbox (real schema drift, Phase 131); the rest pass or skip. D-04: tag ALL via module-level pytestmark.

data/tests/test_resolve_checklist_names.py (verified):
  line 66: def checklist_resolver_db(tmp_path, monkeypatch) (function-scoped, seeds 4-row checklist, no bridge table)
  line 267: def test_at_least_13_fuzzy_candidates(checklist_resolver_db, monkeypatch)
  line 277-279: asserts unmatched_path.exists(); line 296: assert len(rows) >= 13
  RESEARCH §4: 0 candidates because the fixture bridge is empty; >=13 is a real-dataset property. TAXA_PATH monkeypatch already merged in Phase 140 (no change).

data/tests/test_species_maps.py (verified): test_generate_group_maps_emits_subfamily_svgs at ~line 347 does
  if not real_parquet.exists(): pytest.skip("species.parquet not found — run species-export first")
  then asserts len(generated) == 12 (real-dataset property). RESEARCH §1.3: tag @integration, prefix skip [integration].

data/tests/test_checklist_pipeline.py (verified):
  line 33: def checklist_db(tmp_path, monkeypatch) — function-scoped; line 51: importlib.reload(checklist_pipeline) (WR-01 hazard)
  line 61: def checklist_sample_db(request) — module-scoped, save/restore discipline (the analog)
  checklist_db is used ONLY by the two @integration tests (test_checklist_records_full_row_count, test_checklist_records_full_schema) which call mod.load_checklist() with no args (must use DB_PATH) — so save/restore of mod.DB_PATH preserves behavior (RESEARCH §6.1).
  line 167: assert n >= 1 (in test_load_checklist_populates_species_rows) -> assert n == 6
  line 209: assert n >= 1 (in test_load_checklist_creates_species_counties_table) -> assert n == 8
  checklist_pipeline.DB_PATH is a module-level constant (verified, 140-01 interfaces).

data/pyproject.toml: addopts = "-m 'not integration'" (integration deselected by default).
The D-05 conftest guard from Plan 01 treats [integration]-prefixed skip reasons (and @integration-marked items) as legitimate, non-fast-tier skips.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Tag test_dbt_diff.py and the two fuzzy/species-maps real-data tests @integration</name>
  <files>data/tests/test_dbt_diff.py, data/tests/test_resolve_checklist_names.py, data/tests/test_species_maps.py</files>
  <read_first>
    - data/tests/test_dbt_diff.py (the file — imports, SANDBOX, _SANDBOX_GUARD lines 27-37, the 15 tests)
    - data/tests/test_resolve_checklist_names.py (lines 267-296 test_at_least_13_fuzzy_candidates)
    - data/tests/test_species_maps.py (the test_generate_group_maps_emits_subfamily_svgs body around line 347)
    - .planning/phases/141-built-asset-fixtures-red-test-fixes-silent-skip-elimination/141-PATTERNS.md (test_dbt_diff.py / test_resolve_checklist_names.py / test_species_maps.py sections; Pattern D)
    - .planning/phases/141-built-asset-fixtures-red-test-fixes-silent-skip-elimination/141-RESEARCH.md (§3 test_dbt_diff disposition + 3.2 inventory; §4 fuzzy diagnosis; §1.3 species_maps)
  </read_first>
  <action>
    Three @integration taggings (D-04, D-07, TFIX-02/03/04, TTIER-02):

    (A) test_dbt_diff.py — add a module-level `pytestmark = pytest.mark.integration` right after the imports (this deselects all 15 tests from the fast tier; a fixture diff would be tautological per D-04). Harden the existing `_SANDBOX_GUARD` skip reason to begin with the literal prefix integration-in-brackets (so the D-05 guard recognizes it as a legitimate non-fast-tier skip when someone runs -m integration without building). Do NOT remove the per-test guards; the pytestmark makes them moot in the fast tier and they remain loud guards in the integration tier. Do NOT touch the assertions — the 2 row-count/schema failures are real stale-public-parquet drift surfaced in nightly (Phase 142), not fixed here.

    (B) test_resolve_checklist_names.py — decorate test_at_least_13_fuzzy_candidates with @pytest.mark.integration. Leave the >=13 assertion and the checklist_resolver_db fixture unchanged (the threshold is correct for the full dataset; D-07 fix-honestly). Optionally relax the unmatched_path.exists() assert only if it would fail on a clean checkout in the integration tier — but do not weaken the >=13 count.

    (C) test_species_maps.py — decorate test_generate_group_maps_emits_subfamily_svgs with @pytest.mark.integration and change its inline pytest.skip reason to begin with the literal prefix integration-in-brackets (the ==12 subfamilies count is a real-dataset property). Keep the ==12 assertion intact.
  </action>
  <acceptance_criteria>
    - test_dbt_diff.py contains a module-level `pytestmark = pytest.mark.integration`; its fast-tier collection yields 0 tests
    - test_at_least_13_fuzzy_candidates is decorated @pytest.mark.integration; the `>= 13` assertion text is still present (not weakened)
    - test_generate_group_maps_emits_subfamily_svgs is decorated @pytest.mark.integration; its skip reason starts with the [integration] prefix; the `== 12` assertion is still present
    - Fast-tier collection of all three files shows the tagged tests deselected; no asset-driven skip reaches the fast-tier summary (the D-05 guard from Plan 01 does not trip)
    - No production module is modified
  </acceptance_criteria>
  <verify>
    <automated>cd data && grep -q "pytestmark = pytest.mark.integration" tests/test_dbt_diff.py && grep -q ">= 13" tests/test_resolve_checklist_names.py && grep -q "== 12" tests/test_species_maps.py && uv run pytest tests/test_dbt_diff.py tests/test_resolve_checklist_names.py tests/test_species_maps.py -m "not integration" -q -rs</automated>
  </verify>
  <done>test_dbt_diff.py is fully @integration via module pytestmark (deselected from fast tier); the fuzzy >=13 test and the species-maps ==12 test are @integration with their assertions intact and [integration]-prefixed loud guards; no production change.</done>
</task>

<task type="auto">
  <name>Task 2: Harden test_checklist_pipeline.py — save/restore DB_PATH (WR-01) and pin exact 6/8 counts (WR-02)</name>
  <files>data/tests/test_checklist_pipeline.py</files>
  <read_first>
    - data/tests/test_checklist_pipeline.py (the file — checklist_db lines 33-57 incl. importlib.reload line 51; checklist_sample_db lines 60-126 the save/restore analog; assertions lines 167 and 209)
    - .planning/phases/140-checklist-taxonomy-fixture-distillation/140-REVIEW.md (WR-01 hazard description, WR-02 exact counts 6 species / 8 county rows)
    - .planning/phases/141-built-asset-fixtures-red-test-fixes-silent-skip-elimination/141-PATTERNS.md (test_checklist_pipeline.py section — save/restore pattern, exact-count fix)
    - .planning/phases/141-built-asset-fixtures-red-test-fixes-silent-skip-elimination/141-RESEARCH.md (§6.1 WR-01 fix; §6.2 WR-02; Pitfall 4)
  </read_first>
  <action>
    In data/tests/test_checklist_pipeline.py (D-08, D-09 / WR-01, WR-02):

    (A) WR-01 — In the function-scoped checklist_db fixture, replace the importlib.reload(checklist_pipeline) approach with save/restore of the module-level DB_PATH constant (mirrors checklist_sample_db's discipline, RESEARCH §6.1): import checklist_pipeline as mod; save old_db_path = mod.DB_PATH; set mod.DB_PATH = db_path (the reload was effectively re-reading DB_PATH from the env var); keep the existing monkeypatch.setenv("DB_PATH", db_path) and the existing CREATE SCHEMA ecdysis_data / CREATE TABLE ecdysis_data.occurrences setup; convert the fixture to a yield fixture (yield db_path, mod) and restore mod.DB_PATH = old_db_path after the yield. Remove the `import importlib` and `importlib.reload(...)` lines. This keeps behavior identical for the two @integration tests that use checklist_db and call mod.load_checklist() with no args (they read mod.DB_PATH). Add the optional autouse integrity guard from D-08 ONLY if save/restore alone leaves a residual ordering risk (your discretion per CONTEXT D-08).

    (B) WR-02 — Pin the two relaxed assertions to the fixture's exact counts: in test_load_checklist_populates_species_rows change the `assert n >= 1` (line 167) to `assert n == 6` (6 distinct species in the sample); in test_load_checklist_creates_species_counties_table change the `assert n >= 1` (line 209) to `assert n == 8` (8 (species, county) rows). Update each assertion message to state the exact expected count.

    Do NOT modify checklist_pipeline.py or the module-scoped checklist_sample_db fixture (other than the WR-02 assertions inside the tests that use it).
  </action>
  <acceptance_criteria>
    - The string `importlib.reload` no longer appears in data/tests/test_checklist_pipeline.py
    - checklist_db saves and restores mod.DB_PATH (contains `mod.DB_PATH = old` or equivalent restore) and is a yield fixture
    - `assert n == 6` and `assert n == 8` are present (the two former `n >= 1` assertions)
    - The full test_checklist_pipeline.py fast tier passes (0 failures, 0 asset-driven skips), including when run repeatedly under pytest-randomly (no ordering-dependent failure)
    - checklist_pipeline.py is NOT modified
  </acceptance_criteria>
  <verify>
    <automated>cd data && ! grep -q "importlib.reload" tests/test_checklist_pipeline.py && grep -q "assert n == 6" tests/test_checklist_pipeline.py && grep -q "assert n == 8" tests/test_checklist_pipeline.py && git diff --quiet checklist_pipeline.py && uv run pytest tests/test_checklist_pipeline.py -m "not integration" -q -p randomly</automated>
  </verify>
  <done>checklist_db uses save/restore of DB_PATH (no importlib.reload), eliminating the WR-01 ordering hazard; the two assertions are pinned to exact 6/8 counts (WR-02); the fast tier passes under pytest-randomly; production unchanged.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none) | This plan introduces no new trust boundary. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-141-04 | (n/a) | @integration markers + a test-fixture save/restore refactor + pinned assertion counts (test infrastructure only) | accept | No security-relevant attack surface. Changes are pytest markers, a fixture state save/restore, and tighter assertions; no production code, network, secrets, auth, or untrusted input. ASVS L1: nothing to mitigate. (Mirrors Phase 140 T-140-01.) |

No HIGH findings. No new package installs (no Package Legitimacy Gate needed).
</threat_model>

<verification>
Cheap, seconds-scale, per touched file (the host SIGKILLs long runs — never run the whole suite or -m integration here):

```
cd data && uv run pytest tests/test_dbt_diff.py -m "not integration" --collect-only -q   # 0 tests collected (all @integration)
cd data && uv run pytest tests/test_resolve_checklist_names.py tests/test_species_maps.py -m "not integration" -q -rs
cd data && uv run pytest tests/test_checklist_pipeline.py -m "not integration" -q -p randomly   # green under randomized order
cd data && git diff --quiet checklist_pipeline.py   # production untouched
```
</verification>

<success_criteria>
- test_dbt_diff.py fully @integration via module pytestmark — deselected from the fast tier, runs against real built data in nightly; the 2 stale-public-parquet failures are surfaced there (TFIX-02, TTIER-02)
- test_at_least_13_fuzzy_candidates @integration with >=13 retained (TFIX-03, D-07)
- test_generate_group_maps_emits_subfamily_svgs @integration with ==12 retained and [integration]-prefixed skip (TFIX-04)
- checklist_db uses save/restore of DB_PATH (no importlib.reload) — WR-01 closed; the two assertions pinned to 6/8 — WR-02 closed (D-08, D-09)
- Fast tier for all four files is green with 0 asset-driven skips; the Plan-01 D-05 guard does not trip (TFIX-04)
- Scope fence honored: only the four test files modified; no production / built-asset / run.py / nightly.sh / dbt changes
</success_criteria>

<output>
Create `.planning/phases/141-built-asset-fixtures-red-test-fixes-silent-skip-elimination/141-04-SUMMARY.md` when done.
</output>
