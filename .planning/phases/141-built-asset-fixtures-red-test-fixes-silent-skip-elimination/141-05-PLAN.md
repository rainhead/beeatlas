---
phase: 141-built-asset-fixtures-red-test-fixes-silent-skip-elimination
plan: 05
type: execute
wave: 3
gap_closure: true
depends_on: []
files_modified:
  - data/tests/test_dbt_scaffold.py
  - data/tests/test_higher_taxa.py
  - data/tests/conftest.py
autonomous: true
requirements:
  - TFIXTURE-03
  - TFIX-04
  - TTIER-02

must_haves:
  truths:
    - "SC-1 / TFIXTURE-03: on a clean checkout (sandbox parquets absent), the formerly-skipped scaffold and higher-taxa assertions no longer ERROR or silently SKIP in the fast tier — the sandbox-gated ones are deselected via @integration and the sandbox-free one (test_profiles_yml_declares_spatial) still runs and passes"
    - "TFIX-04 / SC-5: a clean-checkout fast-tier run over test_dbt_scaffold.py + test_higher_taxa.py reports 0 errors and 0 skips (down from the current 1 passed / 16 skipped / 19 errors with the sandbox hidden)"
    - "test_dbt_scaffold.py: every sandbox-gated test (all the @pytest.mark.skipif / _CHECKLIST_GUARD / _OCCURRENCES_GUARD / _SPECIES_GUARD tests, plus test_taxon_id_consistency) is tagged @pytest.mark.integration; test_profiles_yml_declares_spatial is NOT tagged and stays in the fast tier"
    - "test_higher_taxa.py: every test (all are _SANDBOX_GUARD / _SPECIES_GUARD sandbox-gated, including the two inline pytest.skip tests) is tagged @pytest.mark.integration; the ==12 subfamily assertion stays in an @integration test and is NOT weakened"
    - "D-05 guard hardening: _ASSET_SKIP_SIGNATURES[0] is changed from the back-ticked exact phrase to the stem 'data/dbt/run.sh build' (no trailing backtick) so it matches the --select higher_taxa / --select species reason variants too — closing the guard false-negative WITHOUT turning any fast-tier test red (because the affected tests are now @integration and deselected)"
    - "No production code, dbt model, run.py, or nightly.sh change — test-only (data/tests/*.py and data/tests/conftest.py)"
  artifacts:
    - path: "data/tests/test_dbt_scaffold.py"
      provides: "@integration on all sandbox-gated tests; test_profiles_yml_declares_spatial untagged"
      contains: "pytest.mark.integration"
    - path: "data/tests/test_higher_taxa.py"
      provides: "module-level pytestmark = pytest.mark.integration (all tests sandbox-gated)"
      contains: "pytestmark = pytest.mark.integration"
    - path: "data/tests/conftest.py"
      provides: "_ASSET_SKIP_SIGNATURES[0] stem catches --select variants"
      contains: "data/dbt/run.sh build"
  key_links:
    - from: "data/tests/test_dbt_scaffold.py sandbox-gated tests"
      to: "data/pyproject.toml addopts -m 'not integration'"
      via: "@pytest.mark.integration deselects them from the fast tier (no skip, no error)"
      pattern: "pytest.mark.integration"
    - from: "data/tests/test_higher_taxa.py pytestmark"
      to: "data/pyproject.toml addopts -m 'not integration'"
      via: "module-level pytest.mark.integration deselects all tests from the fast tier"
      pattern: "pytestmark = pytest.mark.integration"
    - from: "data/tests/conftest.py:_ASSET_SKIP_SIGNATURES"
      to: "--select higher_taxa / --select species skip reasons"
      via: "stem substring 'data/dbt/run.sh build' matches the back-tick-after-argument variants"
      pattern: "data/dbt/run.sh build"
---

<objective>
Close the SC-1 / SC-5 / TFIX-04 gap left by Plans 01–04: two files named in the phase goal — `test_dbt_scaffold.py` and `test_higher_taxa.py` — were never migrated. On a clean checkout (sandbox parquets are untracked in git, so absent) the fast tier currently produces **1 passed / 16 skipped / 19 errors** over these two files (verifier-reproduced):
- `test_dbt_scaffold.py` → 19 D-05 ERRORs (its skip reasons match the guard signature, so the guard rewrites them to failures — but the tests were never tagged @integration).
- `test_higher_taxa.py` → 16 silent SKIPs (its `--select higher_taxa` / `--select species` reasons sit a backtick away from the guard signature, so the D-05 guard never fires — the exact false-negative TFIX-04 exists to catch).

Three changes, all test-only:
1. **test_dbt_scaffold.py** — tag every sandbox-gated test `@pytest.mark.integration` (deselected, not skipped). Preserve `test_profiles_yml_declares_spatial` in the fast tier (it needs no sandbox).
2. **test_higher_taxa.py** — tag every test `@pytest.mark.integration` (all are sandbox-gated). Keep the `==12` subfamily assertion intact inside its @integration test.
3. **data/tests/conftest.py** — tighten `_ASSET_SKIP_SIGNATURES[0]` to the stable stem `"data/dbt/run.sh build"` so `--select` variants also match. **Order matters:** tag the tests @integration FIRST (so they are deselected) and tighten the signature SECOND — otherwise broadening the matcher converts the 16 silent skips to errors before they're deselected.

Acceptance bar (clean-checkout, sandbox hidden): the two files together run `-m 'not integration' -q -rs` with **0 errors and 0 skips** — the one sandbox-free scaffold test passes; everything sandbox-gated is deselected.

Output: 2 modified test files + 1 modified conftest.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/141-built-asset-fixtures-red-test-fixes-silent-skip-elimination/141-CONTEXT.md
@.planning/phases/141-built-asset-fixtures-red-test-fixes-silent-skip-elimination/141-VERIFICATION.md
@.planning/phases/141-built-asset-fixtures-red-test-fixes-silent-skip-elimination/141-REVIEW.md
@.planning/ROADMAP.md
@./CLAUDE.md

<scope_fence>
STRICTLY OUT OF SCOPE here:
- Plans 01–04 and ALL files they touched (fixtures CSVs, resolver_db, test_species_export / test_dbt_synonymy / test_dbt_diff / test_species_maps / test_resolve_checklist_names / test_checklist_pipeline). DO NOT edit them.
- The D-05 hookwrapper body itself (`pytest_runtest_makereport`) — only the `_ASSET_SKIP_SIGNATURES` tuple changes.
- Building any parquet / fixture for these two files — the gap-closure disposition is @integration deselection (these are genuine full-data checks: ==12 subfamilies, >=2000 checklist rows, schema/contract assertions on built marts), NOT fixturization.
- Weakening any assertion (==12 subfamilies, >=2000 rows, etc.) — they stay intact in the @integration tier.
- Any change to run.py / nightly.sh / dbt models / production modules.
This plan touches ONLY data/tests/test_dbt_scaffold.py, data/tests/test_higher_taxa.py, data/tests/conftest.py.
</scope_fence>

<interfaces>
Verified from codebase 2026-06-06.

data/tests/test_dbt_scaffold.py (verified, full read):
  - Sandbox path constant: SANDBOX = .../dbt/target/sandbox (line 23).
  - SANDBOX-GATED tests (ALL must be tagged @integration):
      test_occurrences_parquet_exists (skipif, line 30-36)
      test_occurrences_has_rows_and_zero_null_county_or_eco (skipif, line 39-59)
      test_counties_geojson_structural (skipif, line 66-81)
      test_ecoregions_geojson_structural (skipif, line 84-96)
      _CHECKLIST_GUARD tests (line 117 guard): test_checklist_parquet_exists, test_checklist_row_count,
        test_checklist_no_null_canonical_name, test_checklist_no_null_specific_epithet,
        test_checklist_family_trim, test_checklist_source_constant (6 tests)
      test_occurrences_row_count_not_inflated_by_checklist (skipif, line 187-207)
      _OCCURRENCES_GUARD tests (line 214 guard): test_occurrences_source_column, test_inat_obs_rows_in_occurrences,
        test_source_no_nulls, test_occurrences_taxon_id_non_null (4 tests)
      _SPECIES_GUARD tests (line 255 guard): test_off_checklist_species_with_occurrences_have_specific_epithet,
        test_off_checklist_species_scientificname_capitalized, test_species_taxon_id_non_null (3 tests)
      test_taxon_id_consistency (line 333, decorated @_OCCURRENCES_GUARD @_SPECIES_GUARD)
  - SANDBOX-FREE test (MUST STAY in fast tier — do NOT tag): test_profiles_yml_declares_spatial (line 103-110;
    reads dbt/profiles.yml, no sandbox).
  - Their skipif reasons use "run `bash data/dbt/run.sh build` first to produce sandbox outputs" — these MATCH
    the current D-05 signature[0], which is why they currently ERROR (the guard fires) rather than skip.

data/tests/test_higher_taxa.py (verified, full read):
  - Two module-level guards: _SANDBOX_GUARD (line 29, reason "run `bash data/dbt/run.sh build --select higher_taxa` ...")
    and _SPECIES_GUARD (line 60, reason "run `bash data/dbt/run.sh build --select species` ...").
  - ALL tests are sandbox-gated (no sandbox-free test in this file):
      test_genus_rollup_matches_string_group (parametrized x6), test_tribe_rollup_matches_string_group (x3),
      test_subgenus_rollup_matches_string_group (x1), test_exactly_12_subfamilies (the ==12 assertion, line 132-138),
      test_eumeninae_absent, test_genus_rollup_equals_species_sum (has inline pytest.skip line 168-171),
      test_checklist_only_species_in_membership (inline pytest.skip line 218-221, 235),
      test_taxon_id_unique_not_null, test_member_taxon_ids_column_present.
  - The --select reasons do NOT match the current D-05 signature[0] (backtick sits after the argument), so these
    16 currently SKIP silently — the guard false-negative.
  - Because EVERY test is sandbox-gated, a module-level `pytestmark = pytest.mark.integration` is correct and complete
    here (unlike test_dbt_scaffold.py, which has the one sandbox-free test).

data/tests/conftest.py (verified, lines 597-642):
  _ASSET_SKIP_SIGNATURES = (
      "run `bash data/dbt/run.sh build`",                  # <-- signature[0], trailing backtick breaks --select match
      "run species-export first",
      "run `uv run python data/species_export.py`",
  )
  The pytest_runtest_makereport hookwrapper (line 611-641) is correct and exempts @integration / xfail / non-skip
  outcomes — leave its body untouched. Only signature[0] changes to "data/dbt/run.sh build" (drop "run `bash " prefix
  AND the trailing backtick) so it matches both the plain reason and the `--select higher_taxa` / `--select species`
  variants as a substring.

data/pyproject.toml: addopts = "-m 'not integration'" (integration deselected by default). The integration marker is
registered (Phase 139). @integration tests are therefore deselected — not skipped — in the fast tier, so the D-05
guard does not fire on them (iter_markers exemption, conftest line 631).

Host constraint: the host SIGKILLs long commands. NEVER run the whole suite or `-m integration` here. All verifies are
scoped to the two touched files in the fast tier. The definitive gap check is a CLEAN-CHECKOUT simulation that hides
the sandbox dir, runs the two files, then restores (mirrors the Phase-140 D-07 mv/restore pattern). The sandbox dir is
data/dbt/target/sandbox (relative to repo root) i.e. dbt/target/sandbox when cwd=data.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Tag the sandbox-gated tests in test_dbt_scaffold.py and test_higher_taxa.py @integration</name>
  <files>data/tests/test_dbt_scaffold.py, data/tests/test_higher_taxa.py</files>
  <read_first>
    - data/tests/test_dbt_scaffold.py (full file — every test, the skipif guards, and the one sandbox-free test test_profiles_yml_declares_spatial at line 103)
    - data/tests/test_higher_taxa.py (full file — every test is sandbox-gated via _SANDBOX_GUARD / _SPECIES_GUARD / inline skip)
    - data/tests/conftest.py (lines 597-642 — the D-05 guard and signatures, to understand WHY @integration deselection avoids both the ERROR and the SKIP path)
    - .planning/phases/141-built-asset-fixtures-red-test-fixes-silent-skip-elimination/141-VERIFICATION.md (Gaps Detail §Gap 1; CR-01 caveat about test_profiles_yml_declares_spatial)
    - .planning/phases/141-built-asset-fixtures-red-test-fixes-silent-skip-elimination/141-04-PLAN.md (style/structure to mirror for @integration tagging)
  </read_first>
  <action>
    Tag every sandbox-gated test in both files @pytest.mark.integration so they are DESELECTED (not skipped, not errored) in the fast tier. `pytest` is already imported in both files.

    (A) data/tests/test_dbt_scaffold.py — do NOT use a blanket module-level pytestmark (it would wrongly deselect the sandbox-free test_profiles_yml_declares_spatial at line 103, per CR-01). Instead tag the sandbox-gated tests. Two equivalent acceptable mechanisms — pick one and apply consistently:
      - Option (a), per-test: add a `@pytest.mark.integration` decorator to each of the 16 sandbox-gated tests enumerated in <interfaces> (the four standalone @pytest.mark.skipif tests, the 6 _CHECKLIST_GUARD tests, test_occurrences_row_count_not_inflated_by_checklist, the 4 _OCCURRENCES_GUARD tests, the 3 _SPECIES_GUARD tests, and test_taxon_id_consistency). Leave test_profiles_yml_declares_spatial with NO integration marker.
      - Option (b), compose onto the shared guard objects: redefine the three reusable guard variables as composed markers, e.g. `_CHECKLIST_GUARD = pytest.mark.integration` chained with the existing skipif (so every `@_CHECKLIST_GUARD`-decorated test gets both), and add a `@pytest.mark.integration` to the four standalone skipif tests and to test_taxon_id_consistency individually. Whichever option, the net result must be: all 16 sandbox-gated tests carry the integration marker; test_profiles_yml_declares_spatial does not.
      Do NOT remove or weaken the existing skipif guards (they stay as loud guards for explicit `-m integration` runs without a build). Do NOT touch any assertion.

    (B) data/tests/test_higher_taxa.py — every test is sandbox-gated, so add a single module-level `pytestmark = pytest.mark.integration` near the top (after the imports / SANDBOX constants, before the first guard). This deselects all tests from the fast tier. Keep _SANDBOX_GUARD, _SPECIES_GUARD, and the two inline `pytest.skip(...)` calls intact (loud guards in the integration tier). Do NOT weaken the `== 12` subfamily assertion in test_exactly_12_subfamilies (line 138) or any other assertion.

    Do NOT modify conftest.py in this task — the signature tightening is Task 2 (order: tag first, tighten second).
  </action>
  <acceptance_criteria>
    - test_dbt_scaffold.py fast-tier (`-m 'not integration'`) collection includes exactly ONE test: test_profiles_yml_declares_spatial (the sandbox-free one); all 16 sandbox-gated tests are deselected.
    - grep confirms test_profiles_yml_declares_spatial has NO @pytest.mark.integration on its own def line (it must stay fast).
    - test_higher_taxa.py contains a module-level `pytestmark = pytest.mark.integration`; its fast-tier collection yields 0 tests.
    - The `== 12` assertion text is still present in test_higher_taxa.py (not weakened).
    - The existing skipif guards and inline pytest.skip calls are all still present in both files.
    - No production module / dbt model / run.py / nightly.sh modified.
  </acceptance_criteria>
  <verify>
    <automated>cd data && grep -q "pytestmark = pytest.mark.integration" tests/test_higher_taxa.py && grep -q "== 12" tests/test_higher_taxa.py && uv run pytest tests/test_higher_taxa.py -m 'not integration' --collect-only -q 2>&1 | grep -Eq "no tests ran|0 tests collected|collected 0 items" && uv run pytest tests/test_dbt_scaffold.py -m 'not integration' --collect-only -q 2>&1 | grep -q "test_profiles_yml_declares_spatial" && [ "$(uv run pytest tests/test_dbt_scaffold.py -m 'not integration' --collect-only -q 2>&1 | grep -c '::test_')" = "1" ] && uv run pytest tests/test_dbt_scaffold.py tests/test_higher_taxa.py -m 'not integration' -q -rs 2>&1 | tail -3 && git diff --quiet -- run.py nightly.sh dbt/</automated>
  </verify>
  <done>Every sandbox-gated test in both files is @integration (deselected from the fast tier); test_dbt_scaffold.py keeps test_profiles_yml_declares_spatial in the fast tier; test_higher_taxa.py is fully deselected via module pytestmark; all assertions (incl. ==12) intact; no production change.</done>
</task>

<task type="auto">
  <name>Task 2: Tighten the D-05 signature stem and verify the clean-checkout 0-errors-0-skips target</name>
  <files>data/tests/conftest.py</files>
  <read_first>
    - data/tests/conftest.py (lines 597-642 — _ASSET_SKIP_SIGNATURES at 604-608 and the hookwrapper at 611-641)
    - data/tests/test_higher_taxa.py (the _SANDBOX_GUARD / _SPECIES_GUARD reasons with `--select` — confirm the new stem matches them as a substring)
    - .planning/phases/141-built-asset-fixtures-red-test-fixes-silent-skip-elimination/141-VERIFICATION.md (Gaps Detail §Gap 2 — exact old/new signature; the order note: tag @integration FIRST, tighten SECOND)
    - .planning/phases/140-checklist-taxonomy-fixture-distillation/140-02-PLAN.md (the mv-then-restore harness pattern in its <verification> block — mirror it for the sandbox dir)
  </read_first>
  <action>
    In data/tests/conftest.py change the first element of `_ASSET_SKIP_SIGNATURES` (line 605) from the back-ticked exact phrase to the stable stem so `--select` argument variants also match as a substring:

      OLD: "run `bash data/dbt/run.sh build`",
      NEW: "data/dbt/run.sh build",

    (Drop the leading "run `bash " and the trailing backtick.) Leave the other two signatures ("run species-export first", "run `uv run python data/species_export.py`") and the entire `pytest_runtest_makereport` hookwrapper body unchanged. Optionally add a one-line comment noting the stem matches both the plain and `--select higher_taxa` / `--select species` variants.

    This is safe now because Task 1 already tagged every test whose skip reason contains this stem @integration — so in the fast tier those tests are deselected (the hookwrapper exits early via the iter_markers exemption, conftest line 631) and the broadened stem reds nothing. The broadened stem only fires for any FUTURE non-@integration test that asset-skips with a `--select` reason — the intended catch.

    Do NOT change any test file in this task (Task 1 handled tagging).
  </action>
  <acceptance_criteria>
    - data/tests/conftest.py contains the exact stem `"data/dbt/run.sh build"` in _ASSET_SKIP_SIGNATURES, and the back-ticked phrase "run `bash data/dbt/run.sh build`" no longer appears as a signature element.
    - The hookwrapper pytest_runtest_makereport body is unchanged (still a generator: `outcome = yield`; still exempts @integration / xfail / non-skip).
    - CLEAN-CHECKOUT target met: with the sandbox dir hidden, `uv run pytest tests/test_dbt_scaffold.py tests/test_higher_taxa.py -m 'not integration' -q -rs` reports 0 errors AND 0 skips (the sandbox-free test_profiles_yml_declares_spatial passes; all sandbox-gated tests are deselected). The sandbox dir is restored afterward.
    - No production / dbt / run.py / nightly.sh change.
  </acceptance_criteria>
  <verify>
    <automated>cd data && grep -q '"data/dbt/run.sh build"' tests/conftest.py && ! grep -q 'run `bash data/dbt/run.sh build`' tests/conftest.py && grep -q "outcome = yield" tests/conftest.py && SB=dbt/target/sandbox; BK=/tmp/sandbox_backup_141_05; rm -rf "$BK"; if [ -d "$SB" ]; then mv "$SB" "$BK"; fi; uv run pytest tests/test_dbt_scaffold.py tests/test_higher_taxa.py -m 'not integration' -q -rs 2>&1 | tee /tmp/cleanco_141_05.txt | tail -5; RC=${PIPESTATUS[0]}; if [ -d "$BK" ]; then mkdir -p dbt/target; mv "$BK" "$SB"; fi; echo "--- clean-checkout result (sandbox restored) ---"; ! grep -Eq "error|skipped" /tmp/cleanco_141_05.txt && grep -Eq "1 passed" /tmp/cleanco_141_05.txt && git diff --quiet -- run.py nightly.sh dbt/</automated>
  </verify>
  <done>The D-05 signature stem catches `--select` variants (closing the WR-01/Gap-2 false-negative) without reddening the fast tier; the clean-checkout run over both files yields 0 errors / 0 skips (1 passed = test_profiles_yml_declares_spatial, rest deselected) with the sandbox restored afterward; production untouched.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none) | This plan introduces no new trust boundary. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-141-05 | (n/a) | @integration markers on existing tests + a one-token tightening of a pytest skip-signature tuple (test infrastructure only) | accept | No security-relevant attack surface. Changes are pytest markers and a substring constant; no production code, network, secrets, auth, or untrusted input. ASVS L1: nothing to mitigate. (Mirrors Phase 140 T-140-01 and Phase 141 T-141-04.) |

No HIGH findings. No new package installs (no Package Legitimacy Gate needed).
</threat_model>

<verification>
Cheap, seconds-scale, per touched file (the host SIGKILLs long runs — NEVER run the whole suite or `-m integration` here):

```
# Task 1 — tagging
cd data && uv run pytest tests/test_higher_taxa.py -m 'not integration' --collect-only -q     # 0 tests collected
cd data && uv run pytest tests/test_dbt_scaffold.py -m 'not integration' --collect-only -q    # only test_profiles_yml_declares_spatial
cd data && grep -q "pytestmark = pytest.mark.integration" tests/test_higher_taxa.py
cd data && grep -q "== 12" tests/test_higher_taxa.py                                           # ==12 not weakened

# Task 2 — signature stem
cd data && grep -q '"data/dbt/run.sh build"' tests/conftest.py
cd data && ! grep -q 'run `bash data/dbt/run.sh build`' tests/conftest.py
```

DEFINITIVE clean-checkout gap gate (mv-then-restore, mirroring Phase-140 D-07; sandbox dir is dbt/target/sandbox):

```
cd data && SB=dbt/target/sandbox; BK=/tmp/sandbox_backup_141_05; rm -rf "$BK"; \
  [ -d "$SB" ] && mv "$SB" "$BK"; \
  uv run pytest tests/test_dbt_scaffold.py tests/test_higher_taxa.py -m 'not integration' -q -rs 2>&1 | tee /tmp/cleanco_141_05.txt | tail -5; \
  [ -d "$BK" ] && { mkdir -p dbt/target; mv "$BK" "$SB"; }; \
  ! grep -Eq "error|skipped" /tmp/cleanco_141_05.txt && grep -Eq "1 passed" /tmp/cleanco_141_05.txt
# Expect: "1 passed in ..." — 0 errors, 0 skips, sandbox restored. (Baseline before this plan: 1 passed / 16 skipped / 19 errors.)
```
</verification>

<success_criteria>
- test_dbt_scaffold.py: all 16 sandbox-gated tests @integration; test_profiles_yml_declares_spatial stays in the fast tier and passes (SC-1, TFIXTURE-03)
- test_higher_taxa.py: module-level @integration deselects all tests; ==12 subfamily assertion retained intact (SC-1, TTIER-02)
- conftest.py: _ASSET_SKIP_SIGNATURES[0] stem "data/dbt/run.sh build" catches --select variants; the false-negative (Gap 2 / WR-01) is closed (TFIX-04)
- Clean-checkout fast-tier run over both files = 0 errors / 0 skips (down from 1 passed / 16 skipped / 19 errors) (SC-1, SC-5, TFIX-04)
- Scope fence honored: only test_dbt_scaffold.py, test_higher_taxa.py, conftest.py modified; Plans 01–04 files untouched; no production / dbt / run.py / nightly.sh change
</success_criteria>

<output>
Create `.planning/phases/141-built-asset-fixtures-red-test-fixes-silent-skip-elimination/141-05-SUMMARY.md` when done.
</output>
