---
phase: 129-hierarchy-foundation
plan: 03
type: execute
wave: 2
depends_on: [129-02]
files_modified:
  - .planning/phases/129-hierarchy-foundation/129-VERIFICATION.md
autonomous: false
requirements: [HIER-03, HIER-06]

must_haves:
  truths:
    - "The Apidae descendant query has been run in real wa-sqlite/Firefox and its latency recorded"
    - "The structure decision (materialized path vs nested-set) is documented and justified by the benchmark against the ~100ms perceptual bar"
    - "The complex-rank occurrence/species count and bycatch occurrence/species count are recorded"
    - "The PAGE-05 decision (dropped per D-01) is recorded with the complex count as evidence"
    - "The zero-orphan / missing-parent assertion result, the resulting taxa row count, and occurrences.db before/after size are recorded (D-04 scoping noted)"
  artifacts:
    - path: ".planning/phases/129-hierarchy-foundation/129-VERIFICATION.md"
      provides: "HIER-03 benchmark evidence + HIER-06 count report + structure decision"
      contains: "Apidae descendant query"
  key_links:
    - from: "occurrences.db taxa table (Plan 02)"
      to: "Firefox wa-sqlite benchmark"
      via: "npm run dev then DevTools performance.now() around the instr() descendant query"
      pattern: "instr\\(lineage_path"
---

<objective>
Close out the foundation phase's measurement + decision deliverables. Build the real
`occurrences.db` from live data (Plan 02 code), run the Apidae descendant query in actual
wa-sqlite/Firefox to record latency (HIER-03), confirm the materialized-path structure passes
the ~100ms perceptual bar (D-03; only fall back to nested-set on clear sluggishness), and
record the complex-rank + bycatch counts, the resulting taxa row count, and the PAGE-05
decision (HIER-06) in VERIFICATION.md.

Purpose: HIER-03 requires the structure choice be justified by a benchmark of the largest bee
subtree run in the real WASM engine (server-side DuckDB does NOT predict WASM perf - Pitfall 1).
HIER-06 requires the complex/bycatch counts and the PAGE-05 decision on the record. D-01 fixes
PAGE-05 as dropped; the count is reported but does not reopen the decision. Note: because the
shipped Anthophila table is intentionally scoped to observed+checklist+ancestors only (D-04), the
table is small and the benchmark is expected to be comfortably under the ~100ms bar — it is a
sanity check confirming D-03, not a tight gate.
Output: `129-VERIFICATION.md` with the 6 required sections from 129-RESEARCH.md "VERIFICATION.md
Required Content".
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/129-hierarchy-foundation/129-CONTEXT.md
@.planning/phases/129-hierarchy-foundation/129-RESEARCH.md
@data/sqlite_export.py

<interfaces>
<!-- The benchmark query + the count queries the executor runs. -->

Apidae descendant query (run in Firefox wa-sqlite; Apidae taxon_id = 47221):
  SELECT taxon_id FROM taxa WHERE taxon_id = 47221 OR instr(lineage_path, '/47221/') > 0

Count queries (run via DuckDB/sqlite against the built occurrences.db, for HIER-06):
  complex-rank occurrences (expected 0 per RESEARCH.md A4):
    SELECT COUNT(*) FROM occurrences o JOIN taxa t ON o.taxon_id = t.taxon_id WHERE t.rank = 'complex'
  complex-rank taxa resident in hierarchy (record actual; only complex taxa that are observed/checklist
  ancestors are present per D-04, so this is far below the global 148):
    SELECT COUNT(*) FROM taxa WHERE rank = 'complex'
  bycatch taxa (expected ~106) + bycatch occurrence rows (expected ~2020):
    SELECT COUNT(*) FROM taxa WHERE is_anthophila = 0
    SELECT COUNT(*) FROM occurrences WHERE taxon_id IN (SELECT taxon_id FROM taxa WHERE is_anthophila = 0)
  total taxa row count (D-04 scoping evidence — expect hundreds–low-thousands, NOT ~17K):
    SELECT COUNT(*) FROM taxa
  zero-orphan confirmation (expected 0):
    SELECT COUNT(*) FROM occurrences WHERE taxon_id IS NOT NULL AND taxon_id NOT IN (SELECT taxon_id FROM taxa)

Known facts from RESEARCH.md (use as expected values; flag any deviation):
  complex occurrences = 0; bycatch = 106 distinct taxon_ids / 2,020 rows; occurrences.db baseline = 26.5 MB.
  NOTE (D-04): the global active-Anthophila figures (~17,343 taxa; ~4,959 Apidae descendants;
  148 complex taxa) are NOT the shipped figures — the table holds only the observed+checklist
  subtree + ancestors. Record the ACTUAL counts produced by the build; do not hardcode the global numbers.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build real occurrences.db and gather HIER-06 counts</name>
  <files>.planning/phases/129-hierarchy-foundation/129-VERIFICATION.md</files>
  <read_first>
    - data/sqlite_export.py (the built taxa table + generate_sqlite / main entrypoint)
    - .planning/phases/129-hierarchy-foundation/129-RESEARCH.md "VERIFICATION.md Required Content" + "Summary" (expected live counts)
    - CLAUDE.md "Running Locally" (data pipeline invocation: cd data && uv run python run.py)
  </read_first>
  <action>
    Produce the real occurrences.db from current data. If a fresh dbt build is needed, run the
    pipeline per CLAUDE.md (cd data && uv run python run.py); otherwise run the export step alone
    (cd data && uv run python sqlite_export.py) against the existing dbt sandbox. Record the
    occurrences.db file size and compare to the 26.5 MB baseline from RESEARCH.md. Then run the HIER-06
    count queries from the interfaces block (complex-rank occurrences, complex-rank taxa, bycatch taxa,
    bycatch occurrence rows, total taxa row count, zero-orphan confirmation) via DuckDB or stdlib sqlite3
    against the built db, plus the Apidae descendant row count (SELECT COUNT(*) FROM taxa WHERE
    taxon_id = 47221 OR instr(lineage_path,'/47221/') > 0). The Apidae descendant query now returns the
    observed+checklist Apidae subtree count (record ACTUAL; this is far smaller than the global ~4,959
    per D-04 — do NOT expect ~4,959). Begin drafting 129-VERIFICATION.md with these recorded sections:
    (3) complex-rank occurrence count + the actual hierarchy-resident complex-taxa count (note it is the
    observed+checklist subtree only per D-04, far below the global 148); (4) bycatch taxa + row counts and
    "PAGE-05 dropped per D-01"; (5) zero-orphan / missing-parent result; (6) occurrences.db before/after
    size AND the total taxa row count, with a note that the artifact is intentionally scoped to
    observed+checklist+ancestors per D-04 (expect hundreds–low-thousands of taxa rows, not ~17K).
    Flag any count that deviates materially from the RESEARCH.md expected values (other than the
    D-04-scoped Anthophila/Apidae/complex counts, which are expected to be smaller than the global figures).
  </action>
  <verify>
    <automated>cd data && uv run pytest tests/test_sqlite_export.py -q</automated>
  </verify>
  <acceptance_criteria>
    - Behavior: occurrences.db exists at public/data/occurrences.db (or the EXPORT_DIR target) and contains a taxa table.
    - Behavior: the zero-orphan query returns 0 against the real db (the orphan/missing-parent gate did not fire).
    - Source assertion: 129-VERIFICATION.md exists and contains the complex-rank count, bycatch counts, total taxa row count, zero-orphan result, and db size sections (grep for "complex", "bycatch", "orphan", "MB", "taxa rows").
    - Source assertion: 129-VERIFICATION.md notes the D-04 scoping (artifact is observed+checklist+ancestors only, not the full active-Anthophila set).
    - All 14 hierarchy + occurrences tests still green.
  </acceptance_criteria>
  <done>Real occurrences.db built; HIER-06 counts + total taxa row count gathered and drafted into 129-VERIFICATION.md; zero orphans / missing parents confirmed on live data; D-04 scoping recorded.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 2: Run the Apidae descendant benchmark in Firefox and record the structure decision</name>
  <files>.planning/phases/129-hierarchy-foundation/129-VERIFICATION.md</files>
  <read_first>
    - .planning/phases/129-hierarchy-foundation/129-RESEARCH.md "Code Examples" (benchmark JS snippet) + "Common Pitfalls" Pitfall 1 (must run in Firefox, not DuckDB) + Pattern 6 (nested-set fallback)
    - .planning/phases/129-hierarchy-foundation/129-VALIDATION.md "Manual-Only Verifications"
    - src/sqlite-worker.ts (wa-sqlite boot sequence, tablesReady signal, how to issue a query to the worker db)
  </read_first>
  <what-built>
    The taxa table (materialized-path lineage_path, D-02 default) is embedded in occurrences.db and
    indexed on lineage_path. Plan 02 made the descendant query (taxon_id = 47221 OR
    instr(lineage_path,'/47221/') > 0) answerable in the browser's wa-sqlite engine. Per D-04 the
    table holds only the observed+checklist subtree + ancestors, so it is small.
  </what-built>
  <action>
    This is a manual human-verify checkpoint - the human runs the wa-sqlite benchmark in Firefox and
    confirms the structure decision. The executor prepares for it: ensure occurrences.db (with the taxa
    table) is served by `npm run dev`, and if needed add a temporary one-off benchmark call (per the
    RESEARCH.md JS snippet) to issue the Apidae descendant query against the worker db with performance.now()
    timing. After the human records the result, finalize 129-VERIFICATION.md sections (1) structure chosen +
    justification and (2) benchmark result, then remove any temporary benchmark code. Per D-02/D-03: keep
    materialized path unless clearly sluggish; if the ~100ms bar is clearly exceeded, flag a nested-set
    lft/rgt follow-up (RESEARCH.md Pattern 6) rather than silently switching structure here. Note: because
    the table is D-04-scoped (small), the STACK.md ~110ms worst-case (computed for ~17K rows) does NOT
    apply; the benchmark is expected to be comfortably under ~100ms and is a sanity check confirming D-03.
  </action>
  <how-to-verify>
    1. Start the dev server: `npm run dev`.
    2. Open the site in Firefox on a mid-range device; open DevTools console.
    3. Wait for tablesReady (the SQLite db is loaded in the worker).
    4. Run the Apidae descendant query against the worker's wa-sqlite db, wrapping it in
       performance.now() before/after (see 129-RESEARCH.md "Code Examples" - the benchmark JS snippet;
       you may add a temporary one-off benchmark call in bee-atlas.ts or run via the worker API).
       Query: SELECT taxon_id FROM taxa WHERE taxon_id = 47221 OR instr(lineage_path, '/47221/') > 0
    5. Record: elapsed milliseconds, browser + version, device class, and the returned row count
       (this is the observed+checklist Apidae subtree count — record the ACTUAL value; it is far
       smaller than the global ~4,959 per D-04, do NOT expect ~4,959).
    6. Decision per D-02/D-03: if latency is at or below the ~100ms perceptual bar (not clearly sluggish),
       KEEP materialized path. Because the D-04-scoped table is small, this is expected; the STACK.md
       ~110ms math (for ~17K rows) does not apply. Only if clearly sluggish (well above ~100-150ms on a
       mid-range device), record that the nested-set lft/rgt fallback (RESEARCH.md Pattern 6) is required
       and flag it for a follow-up plan - do NOT silently switch structure here.
    7. Finalize 129-VERIFICATION.md sections (1) structure chosen + justification and (2) benchmark
       result (browser, device, elapsed ms, row count). Remove any temporary benchmark code added for the test.
  </how-to-verify>
  <verify>
    <human-check>129-VERIFICATION.md records the Firefox/wa-sqlite Apidae benchmark latency (ms), browser+version, device class, returned row count (the D-04-scoped subtree count), and the justified structure decision.</human-check>
  </verify>
  <acceptance_criteria>
    - 129-VERIFICATION.md records: structure chosen (materialized path unless benchmark clearly failed),
      benchmark latency in ms, browser+version, device class, and returned row count.
    - The benchmark was run in real Firefox/wa-sqlite (NOT server-side DuckDB).
    - The recorded row count is the actual D-04-scoped subtree count (not the global ~4,959), and the
      VERIFICATION.md notes the STACK.md ~110ms (17K-row) math does not apply to the small shipped table.
    - If the bar was exceeded, a nested-set follow-up is explicitly flagged rather than silently switched.
    - Any temporary benchmark code added for the test is removed.
  </acceptance_criteria>
  <done>Apidae benchmark run in Firefox; latency + structure decision recorded in 129-VERIFICATION.md; temporary benchmark code removed.</done>
  <resume-signal>Type "approved" with the recorded latency, or describe issues / the sluggish result requiring nested-set follow-up.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| built occurrences.db -> Firefox wa-sqlite | The shipped artifact; descendant-query latency is a UX property, not a security one |
| live data counts -> VERIFICATION.md | Reported integrity evidence; wrong counts would mislead the phase gate |

Note: Build-time pipeline + a manual browser measurement, no server runtime, no user input.
The threat surface is data-integrity reporting accuracy and shipping a correct artifact, not web auth/injection.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-129-08 | Repudiation/Integrity | benchmark recorded from server-side DuckDB instead of real WASM | mitigate | Checkpoint mandates Firefox/wa-sqlite run (Pitfall 1); acceptance criteria require browser+device recorded |
| T-129-09 | Tampering/Integrity | VERIFICATION.md counts diverge from the shipped db, or global figures recorded instead of D-04-scoped figures | mitigate | Counts queried directly against the built occurrences.db; total taxa row count recorded as D-04 scoping evidence; zero-orphan re-confirmed on live data |
| T-129-SC | Tampering | npm/pip/cargo installs | mitigate | No new packages (RESEARCH.md Package Legitimacy Audit: none); slopcheck N/A |
</threat_model>

<verification>
- 129-VERIFICATION.md contains all 6 required sections (structure decision, benchmark result, complex count, bycatch count, zero-orphan, db size) plus the total taxa row count and D-04 scoping note.
- Benchmark run in real Firefox/wa-sqlite with latency, browser, device, and the actual D-04-scoped row count recorded.
- Materialized-path structure kept unless clearly sluggish; nested-set fallback flagged (not silently applied) if the bar is exceeded.
- PAGE-05 recorded as dropped per D-01 with the complex count as evidence.
</verification>

<success_criteria>
- HIER-03: Apidae descendant-query latency measured in wa-sqlite/Firefox; structure choice justified against the ~100ms bar (small D-04-scoped table; STACK.md 17K-row math does not apply).
- HIER-06: complex-rank and bycatch occurrence/species counts recorded; total taxa row count recorded as D-04 evidence; PAGE-05 decision (dropped) documented.
- Zero-orphan / missing-parent confirmed on live data; occurrences.db before/after size recorded.
</success_criteria>

<output>
Create `.planning/phases/129-hierarchy-foundation/129-03-SUMMARY.md` when done (the VERIFICATION.md is the primary phase artifact).
</output>
</content>
</invoke>
