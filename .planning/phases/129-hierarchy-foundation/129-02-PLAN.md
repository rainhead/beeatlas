---
phase: 129-hierarchy-foundation
plan: 02
type: execute
wave: 1
depends_on: [129-01]
files_modified:
  - data/sqlite_export.py
autonomous: true
requirements: [HIER-01, HIER-02, HIER-04, HIER-05]

must_haves:
  truths:
    - "occurrences.db contains a taxa table keyed on taxon_id (PRIMARY KEY) with rank, name, lineage_path, is_anthophila columns (materialized-path lineage_path default per D-02 — no nested-set bake-off)"
    - "The Anthophila arm of taxa contains ONLY observed (occurrence) + checklist bee taxa AND their ancestors up to the Anthophila root (630955) — the full active-Anthophila set from taxa.csv.gz is NOT loaded (per D-04)"
    - "Every taxon_id appearing in any Anthophila lineage_path segment resolves to a real taxa row (no missing-parent gaps)"
    - "Every non-bee bycatch taxon_id present in occurrences resolves to its own finest-rank row with is_anthophila = 0 (finest available rank, no genus rollup, per D-05)"
    - "Every checklist bee species resolves into the hierarchy via canonical_to_taxon_id (seeded into PASS 1 with its ancestors)"
    - "A post-build assertion raises ValueError (fails the nightly gate) if any occurrence taxon_id has no taxa entry OR any lineage_path segment references a missing-parent taxon_id"
    - "The 9 Wave 0 hierarchy tests are GREEN"
  artifacts:
    - path: "data/sqlite_export.py"
      provides: "_build_taxon_hierarchy (seed+ancestry Anthophila load + bycatch pass), _assert_no_orphan_taxon_ids, generate_sqlite wiring"
      contains: "def _build_taxon_hierarchy"
      min_lines: 120
  key_links:
    - from: "data/sqlite_export.py generate_sqlite"
      to: "_build_taxon_hierarchy + _assert_no_orphan_taxon_ids"
      via: "function calls after DETACH out, before geo_blob block"
      pattern: "_build_taxon_hierarchy\\(|_assert_no_orphan_taxon_ids\\("
    - from: "_build_taxon_hierarchy PASS 1 seed"
      to: "out.occurrences Anthophila taxon_ids + checklist canonical_to_taxon_id"
      via: "seed = distinct Anthophila occurrence taxon_ids ∪ checklist taxon_ids, then ancestry-expansion"
      pattern: "canonical_to_taxon_id"
    - from: "_build_taxon_hierarchy PASS 1 ancestry expansion"
      to: "taxa.csv.gz ancestry → seed ancestors + lineage_path"
      via: "unnest(string_split(ancestry, '/')) to collect ancestors; regexp_extract(ancestry || '/' || taxon_id, '(630955(?:/[0-9]+)*)$', 1) for lineage_path"
      pattern: "regexp_extract.*630955"
---

<objective>
Implement the constrained (seed + ancestry-expansion) taxon hierarchy build inside
`data/sqlite_export.py`, turning the 9 Wave 0 tests from RED to GREEN. Add a new
`_build_taxon_hierarchy(con, dst_db)` function that builds the Anthophila arm of the
`taxa` table from ONLY the observed + checklist bee taxa and their ancestors up to the
Anthophila root (630955) — NOT the full active-Anthophila set (per locked decision D-04) —
then loads non-bee bycatch taxa at their finest available rank with `is_anthophila = 0`
(D-05). Add `_assert_no_orphan_taxon_ids(db_path)` as a hard-fail nightly gate that catches
BOTH orphan occurrence taxon_ids AND missing-parent taxon_ids inside any lineage_path
(HIER-04), create indexes, and wire both into `generate_sqlite`.

Purpose: This is the core HIER-01/02/04/05 deliverable — a query-ready, taxon_id-keyed hierarchy
embedded in occurrences.db. Per D-04 the shipped artifact stays small: only the lineages that
actually contain an observed or checklist taxon are materialized (likely hundreds–low-thousands of
rows for WA, not ~17K). This phase is ADDITIVE: nothing reads the table until Phase 130; do NOT
touch int_combined.sql, drop columns, or change the occurrences schema.
Output: `data/sqlite_export.py` with `_build_taxon_hierarchy`, `_assert_no_orphan_taxon_ids`,
module-level `_TAXA_PATH` + `DB_PATH`, and updated `generate_sqlite` signature/body. All 9 Wave 0 tests GREEN.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/129-hierarchy-foundation/129-CONTEXT.md
@.planning/phases/129-hierarchy-foundation/129-RESEARCH.md
@.planning/phases/129-hierarchy-foundation/129-PATTERNS.md
@data/sqlite_export.py
@data/taxa_pipeline.py

<interfaces>
<!-- Contracts the executor must implement against. From 129-PATTERNS.md + RESEARCH.md. -->

Module-level constants to add to sqlite_export.py (PATTERNS.md §Imports pattern):
- _TAXA_PATH = Path(__file__).parent / "raw" / "taxa.csv.gz"
- DB_PATH = os.environ.get("DB_PATH", str(Path(__file__).parent / "beeatlas.duckdb"))

ANTHOPHILA_ID = 630955 (the Anthophila clade root; the seed-expansion stops here and it is the
lineage_path regex anchor).

taxa table target schema (D-02 materialized-path default):
  taxon_id INTEGER PRIMARY KEY, rank TEXT NOT NULL, name TEXT NOT NULL,
  lineage_path TEXT (NULL for bycatch), is_anthophila INTEGER NOT NULL

Descendant query the index serves (used by Phase 130 + the Plan 03 benchmark):
  SELECT taxon_id FROM taxa WHERE taxon_id = :id OR instr(lineage_path, '/:id/') > 0

Ancestry-walk pattern (the template from taxa_pipeline.py lines 128-133 — REUSE this to collect
each seed taxon's ancestors instead of loading the whole clade):
  SELECT CAST(unnest(string_split(ancestry, '/')) AS BIGINT) AS ancestor_id FROM <seed-rows>

read_csv column spec (taxa.csv.gz, TSV, gzip):
  columns = { taxon_id: BIGINT, ancestry: VARCHAR, rank_level: BIGINT,
              rank: VARCHAR, name: VARCHAR, active: VARCHAR }
  delim = chr(9), header = true, compression = 'gzip'
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement _build_taxon_hierarchy (seed + ancestry-expansion Anthophila load, bycatch pass, indexes)</name>
  <files>data/sqlite_export.py</files>
  <read_first>
    - data/sqlite_export.py (the generate_sqlite body, the ATTACH pattern, the _sqlite3.connect geo_blob block — all to be matched/extended)
    - data/taxa_pipeline.py (the all_active_bees CTE, `active = 'true'` string guard line ~123, the `unnest(string_split(ancestry, '/'))` ancestry walk lines ~128-133, parameterized read_csv with [str(TAXA_PATH)] at line ~161)
    - .planning/phases/129-hierarchy-foundation/129-PATTERNS.md §"Core PASS 1/2/3 pattern" + §"Index creation pattern" (prescriptive SQL templates)
    - .planning/phases/129-hierarchy-foundation/129-RESEARCH.md §"Architecture Patterns" Patterns 1-5 + §"Common Pitfalls" 3,4,5,6
  </read_first>
  <behavior>
    - PASS 1 (Anthophila, constrained per D-04): build a seed set = {distinct Anthophila taxon_ids in
      out.occurrences} ∪ {checklist taxon_ids via canonical_to_taxon_id}. Expand the seed to include
      ALL ancestor taxon_ids by unnesting each seed taxon's `ancestry` string. Load EXACTLY that
      taxon_id set (seed ∪ ancestors) from taxa.csv.gz with is_anthophila=1 and a materialized
      lineage_path. The full active-Anthophila set is NOT loaded — only lineages that contain an
      observed or checklist taxon. The Apidae descendant query (taxon_id=47221 OR
      instr(lineage_path,'/47221/')>0) returns the observed+checklist Apidae subtree (far smaller
      than the global ~4,959).
    - PASS 2 (bycatch): every occurrence taxon_id NOT already in taxa is inserted at its OWN rank/name
      from taxa.csv.gz with is_anthophila=0 and lineage_path NULL (no active filter; no rollup).
    - Indexes idx_taxa_lineage and idx_taxa_is_anthophila exist after build.
    - Every taxon_id referenced inside any Anthophila lineage_path segment exists as a taxa row (no
      missing-parent gaps) — guaranteed by the ancestry-expansion seeding above.
    - Wave 0 tests test_taxa_table_exists, test_apidae_descendant_query, test_is_anthophila_flag,
      test_bycatch_present_in_taxa, test_taxa_name_rank_non_null go GREEN.
  </behavior>
  <action>
    This PASS-1 rescope is per locked decision D-04: load observed + checklist Anthophila taxa AND
    their ancestors ONLY — never the full active-Anthophila clade.

    Add module-level `_TAXA_PATH` and `DB_PATH` constants (per the interfaces block / PATTERNS.md
    §Imports pattern). Define `_build_taxon_hierarchy(con: duckdb.DuckDBPyConnection, dst_db: Path,
    taxa_path: Path, db_path: str) -> None`. The function receives the already-open DuckDB `con`
    with `out` still ATTACHed and `out.occurrences` written.

    PASS 1 — Anthophila, constrained (seed + ancestry-expansion):
    Step (a) SEED: build the bee seed taxon_id set as the UNION of two sources:
      (i) `SELECT DISTINCT taxon_id FROM out.occurrences WHERE taxon_id IS NOT NULL` intersected with
          Anthophila membership (taxon is Anthophila if its taxa.csv.gz row has
          `ancestry LIKE '%/630955/%' OR ancestry LIKE '%/630955' OR taxon_id = 630955`); and
      (ii) checklist taxon_ids resolved via `canonical_to_taxon_id` — open a second connection
          `duckdb.connect(db_path, read_only=True)` and run
          `SELECT DISTINCT c.taxon_id FROM read_parquet(_DBT_SANDBOX/'checklist.parquet') cl JOIN
          inaturalist_data.canonical_to_taxon_id c ON c.canonical_name = cl.canonical_name`.
          Close the second connection in a finally block. Guard PASS-1(ii) so it degrades gracefully
          if checklist.parquet or canonical_to_taxon_id is absent in a test context (try/except or
          existence check) — unit tests without a checklist must still build the occurrence-seeded arm.
    Step (b) EXPAND: from taxa.csv.gz, take the rows whose taxon_id is in the seed and unnest each
      one's `ancestry` via `unnest(string_split(ancestry, '/'))` (the taxa_pipeline.py pattern) to
      collect every ancestor taxon_id. The final Anthophila taxon_id set = seed ∪ ancestors ∪ {630955}.
    Step (c) LOAD: `INSERT OR IGNORE INTO out.taxa SELECT taxon_id, rank, name, <lineage_path>,
      1 AS is_anthophila FROM read_csv(?, ...)` filtered to `taxon_id IN (<seed ∪ ancestors set>)`
      AND `rank IN ('family','subfamily','tribe','genus','subgenus','complex','species')` (include
      'complex' — Pitfall 6). Use `INSERT OR IGNORE` to avoid PK collisions across the seed/ancestor
      union. Do NOT use an unconstrained `WHERE active='true' AND ancestry LIKE '%/630955/%'` full-clade
      load — the load MUST be bounded by the seed-from-occurrences/checklist + ancestry-expansion
      taxon_id set. Compute lineage_path via
      `'/' || regexp_extract(ancestry || '/' || CAST(taxon_id AS VARCHAR), '(630955(?:/[0-9]+)*)$', 1) || '/'`.
      Pass the path as a positional parameter `[str(taxa_path)]` (NOT f-string). Use the
      `active = 'true'` STRING literal where you read taxa.csv.gz Anthophila membership (Pitfall:
      boolean TRUE matches zero rows).

    PASS 2 (bycatch) — `INSERT OR IGNORE INTO out.taxa SELECT taxon_id, rank, name, NULL AS lineage_path,
    0 AS is_anthophila FROM read_csv(?, ...)` WHERE taxon_id IN (SELECT DISTINCT taxon_id FROM
    out.occurrences WHERE taxon_id IS NOT NULL AND taxon_id NOT IN (SELECT taxon_id FROM out.taxa)).
    OMIT the active filter here (Pitfall 5 — bycatch may be inactive but was valid at ingest). Keep
    bycatch unchanged: finest rank, is_anthophila=0, lineage_path NULL, no rollup (D-05).

    INDEXES — in a `with _sqlite3.connect(dst_db) as idx_con:` block, create
    `idx_taxa_lineage ON taxa(lineage_path)` and `idx_taxa_is_anthophila ON taxa(is_anthophila)` (IF NOT EXISTS).
    Do NOT add a `_GEO_COLS` change, do NOT touch int_combined.sql, do NOT drop any occurrences column.
  </action>
  <verify>
    <automated>cd data && uv run pytest tests/test_sqlite_export.py -k "taxa_table_exists or apidae or is_anthophila or bycatch or name_rank" -x -q</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: `grep -c "def _build_taxon_hierarchy" data/sqlite_export.py` == 1.
    - Source assertion: `grep -c "regexp_extract" data/sqlite_export.py` >= 1 and the pattern contains `630955`.
    - Source assertion: PASS 1 is seed-constrained, NOT a full-clade load — the Anthophila INSERT
      filters on a seed-from-occurrences/checklist taxon_id set expanded via `unnest(string_split(ancestry`
      (`grep -c "unnest(string_split(ancestry" data/sqlite_export.py` >= 1), and there is NO
      unconstrained `active = 'true' AND ancestry LIKE '%/630955/%'` clause used as the sole PASS-1
      load filter (the active/ancestry test is used only to test Anthophila membership of the
      occurrence/checklist seed, not to select the entire clade).
    - Source assertion: `grep -c "INSERT OR IGNORE INTO out.taxa" data/sqlite_export.py` >= 2 (PASS 1 load + PASS 2 bycatch).
    - Source assertion: PASS 1 seed uses the string guard for Anthophila membership — `grep -c "active = 'true'" data/sqlite_export.py` >= 1.
    - Source assertion: `grep -c "canonical_to_taxon_id" data/sqlite_export.py` >= 1 (checklist seed source).
    - Source assertion: indexes created — `grep -c "idx_taxa_lineage\|idx_taxa_is_anthophila" data/sqlite_export.py` >= 2.
    - Behavior: the 5 listed Wave 0 tests pass.
    - Guard: `grep -c "int_combined\|DROP COLUMN" data/sqlite_export.py` == 0 (no schema mutation this phase).
  </acceptance_criteria>
  <done>_build_taxon_hierarchy implements the seed+ancestry-expansion Anthophila load (D-04), the bycatch pass, and indexes; the 5 structure/flag/bycatch tests are GREEN.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Orphan + missing-parent assertion hard gate + generate_sqlite wiring</name>
  <files>data/sqlite_export.py</files>
  <read_first>
    - data/sqlite_export.py (now has _build_taxon_hierarchy from Task 1; the generate_sqlite call-site between DETACH out and the geo_blob _sqlite3 block)
    - .planning/phases/129-hierarchy-foundation/129-PATTERNS.md §"Orphan assertion pattern" + §"generate_sqlite call-site integration" (prescriptive templates)
    - .planning/phases/129-hierarchy-foundation/129-RESEARCH.md §"Code Examples" (the _assert_no_orphan_taxon_ids template)
  </read_first>
  <behavior>
    - generate_sqlite gains optional `taxa_path: Path | None = None` and `db_path: str | None = None`
      kwargs (defaulting to _TAXA_PATH / DB_PATH) so tests can inject fixtures.
    - After building occurrences and the taxa hierarchy, _assert_no_orphan_taxon_ids raises ValueError
      (matching "orphan") when any occurrence taxon_id has no taxa entry — failing the pipeline before export.
    - The same assertion (or a companion check it calls) ALSO raises ValueError when any non-root
      taxon_id referenced inside any Anthophila lineage_path segment has no taxa row (missing parent),
      satisfying CONTEXT.md "zero orphan / missing-parent taxon_id".
    - Wave 0 tests test_zero_orphan_taxon_ids, test_orphan_assertion_raises, test_active_taxa_only,
      test_complex_and_bycatch_counts go GREEN.
    - All 5 pre-existing occurrences tests still pass (occurrences-only path unaffected; taxa_path default
      points to the real taxa.csv.gz which exists in the repo).
  </behavior>
  <action>
    Add `_assert_no_orphan_taxon_ids(db_path: Path) -> None` using the PATTERNS.md template. It must
    perform BOTH integrity checks against the built db (both raise ValueError containing the word
    "orphan" plus a count when > 0, mirroring the ValueError hard-fail pattern in taxa_pipeline.py):
      (1) ORPHAN occurrence taxon_ids: open `with _sqlite3.connect(db_path)`, run
          `SELECT COUNT(*) FROM occurrences WHERE taxon_id IS NOT NULL
           AND taxon_id NOT IN (SELECT taxon_id FROM taxa)`; raise if > 0.
      (2) MISSING-PARENT taxon_ids: assert every non-root taxon_id appearing as a segment inside any
          Anthophila `lineage_path` exists as a taxa row. Split each Anthophila row's lineage_path into
          its `/`-delimited integer segments, exclude the Anthophila root 630955, and check each segment
          resolves to a `taxa.taxon_id`; raise ValueError (message containing "orphan"/"missing parent"
          + count) if any segment has no row. Implement via stdlib sqlite3 (e.g. a recursive CTE or a
          Python split over `SELECT taxon_id, lineage_path FROM taxa WHERE lineage_path IS NOT NULL`,
          collecting the segment set and diffing against `SELECT taxon_id FROM taxa`).
    Both checks fail the nightly gate with ValueError.

    Update `generate_sqlite` signature to accept `taxa_path: Path | None = None, db_path: str | None = None`,
    resolving `taxa_path = taxa_path or _TAXA_PATH` and `db_path = db_path or DB_PATH`. Keep the existing
    `con` open through the hierarchy build: move the `con.execute("DETACH out")` to AFTER the
    `_build_taxon_hierarchy(con, dst_db, taxa_path, db_path)` call (the function re-uses the ATTACHed `out`),
    then call `_assert_no_orphan_taxon_ids(dst_db)` before the existing geo_blob `_sqlite3` block. Do NOT
    alter the geo_blob construction or `_GEO_COLS`.
    Update `main()` only if needed to pass through defaults (no behavior change for the production path —
    taxa_path defaults to raw/taxa.csv.gz, db_path to beeatlas.duckdb).
  </action>
  <verify>
    <automated>cd data && uv run pytest tests/test_sqlite_export.py -q</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: `grep -c "def _assert_no_orphan_taxon_ids" data/sqlite_export.py` == 1.
    - Source assertion: the ValueError message contains "orphan" — `grep -c 'raise ValueError' data/sqlite_export.py` >= 1 near the orphan function.
    - Source assertion: the assertion also covers missing-parent lineage_path segments — `grep -c "lineage_path" data/sqlite_export.py` references appear inside _assert_no_orphan_taxon_ids (segment check present, not only the occurrence-orphan check).
    - Source assertion: `generate_sqlite` accepts taxa_path — `grep -c "taxa_path" data/sqlite_export.py` >= 2.
    - Source assertion: call-site present — `grep -c "_build_taxon_hierarchy(con" data/sqlite_export.py` >= 1 and `grep -c "_assert_no_orphan_taxon_ids(dst_db)" data/sqlite_export.py` >= 1.
    - Behavior: ALL tests pass — `cd data && uv run pytest tests/test_sqlite_export.py -q` reports 0 failures (5 original + 9 hierarchy = 14 green).
    - Guard: no occurrences-column drop or int_combined edits introduced.
  </acceptance_criteria>
  <done>Orphan + missing-parent gate raises on any unmapped occurrence taxon_id or missing lineage_path parent; generate_sqlite wired with injectable taxa_path/db_path; all 14 tests GREEN.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| taxa.csv.gz (iNat Open Data) → _build_taxon_hierarchy | Untrusted upstream taxonomy; rows can be added/removed/deactivated between nightly runs |
| occurrences.parquet (dbt) → out.taxa coverage | Every occurrence taxon_id must resolve; a missing one is silent corruption if not gated |
| beeatlas.duckdb (canonical_to_taxon_id) → PASS 1 checklist seed | Internal artifact; read-only connection; absence must degrade gracefully in tests |

Note: Build-time pipeline, no server runtime, no user input. The realistic threat is
data-integrity corruption (orphan / missing-parent taxon shipping to production), not web auth/injection.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-129-03 | Tampering/Integrity | occurrence taxon_id with no taxa entry, OR a lineage_path parent with no taxa row, shipping to prod | mitigate | _assert_no_orphan_taxon_ids raises ValueError on BOTH orphan occurrence ids and missing-parent lineage segments → nightly gate hard-fails before export (HIER-04); covered by test_orphan_assertion_raises + test_zero_orphan_taxon_ids |
| T-129-04 | Tampering/Integrity | silent zero-row PASS 1 from boolean-vs-string `active` bug, or an over-broad full-clade load violating D-04 | mitigate | Use `active = 'true'` string literal for Anthophila membership (Pitfall); PASS 1 is bounded by the occurrence/checklist seed + ancestry expansion, not a full-clade scan; test_active_taxa_only + test_apidae_descendant_query assert a non-empty but bounded bee set |
| T-129-05 | Tampering/Integrity | bycatch leaking is_anthophila=1 into bee surfaces | mitigate | PASS 2 forces is_anthophila=0; test_is_anthophila_flag + test_bycatch_present_in_taxa assert the flag (HIER-05) |
| T-129-06 | Denial of Service | duplicate PK abort when seed/ancestor union or checklist species overlaps | mitigate | INSERT OR IGNORE in PASS 1 load + PASS 2 (Pitfall 3); build cannot abort on overlap |
| T-129-07 | Information Disclosure | second DuckDB connection to beeatlas.duckdb | accept | Opened read_only; local file; no secrets; closed in finally |
| T-129-SC | Tampering | npm/pip/cargo installs | mitigate | No new packages (RESEARCH.md Package Legitimacy Audit: none); slopcheck N/A |
</threat_model>

<verification>
- `cd data && uv run pytest tests/test_sqlite_export.py` — all 14 tests green (5 original + 9 hierarchy).
- `cd data && uv run pytest` — full suite green (per-wave merge gate).
- `_build_taxon_hierarchy` performs the seed+ancestry-expansion Anthophila load (D-04) + bycatch pass + indexes; `_assert_no_orphan_taxon_ids` is wired as a hard gate catching orphan AND missing-parent taxon_ids.
- No occurrences-schema mutation; int_combined.sql untouched (additive phase).
</verification>

<success_criteria>
- taxa table built in occurrences.db keyed on taxon_id with rank/name/lineage_path/is_anthophila.
- Anthophila arm = observed + checklist bee taxa + their ancestors ONLY (D-04); bycatch finest-rank (PASS 2) covered.
- Orphan + missing-parent assertion fails the pipeline on any unmapped occurrence taxon_id or any unresolved lineage_path parent.
- All 9 Wave 0 hierarchy tests GREEN; 5 pre-existing tests still green.
</success_criteria>

<output>
Create `.planning/phases/129-hierarchy-foundation/129-02-SUMMARY.md` when done.
</output>
</content>
</invoke>
