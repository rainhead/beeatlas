---
phase: 999.8-separate-spatial-bounds-filter-from-per-record-selection-bac
plan: 03
type: execute
wave: 3
depends_on: ["999.8-01", "999.8-02"]
files_modified:
  - src/bee-atlas.ts
  - src/bee-pane.ts
  - src/tests/bee-atlas.test.ts
  - src/tests/bee-pane.test.ts
autonomous: true
requirements: [D-01, D-04, D-05, D-06, D-07, D-08]
must_haves:
  truths:
    - "bee-atlas owns bounds inside _filterState (no _selectionBounds field remains anywhere in bee-atlas.ts)"
    - "_applyBoundsFilter writes _filterState.bounds, does NOT force _paneState='list' (D-04), does NOT null record selection (D-05)"
    - "Applying a bounds box no longer force-opens the list pane (D-04)"
    - "Bounds filter and per-record selection coexist (D-05): occurrence click / filter-changed / clear-selection / pane-collapse no longer clear bounds"
    - "Clicking empty map clears record selection only, leaving bounds active (D-06)"
    - "near-me-cleared is the only path that clears bounds (D-07); it does so via _filterState spread and does not touch pane state"
    - "Legacy ?sel= and new ?bbox= both restore bounds into _filterState (firstUpdated + popstate) and re-serialize as bbox="
    - "Full suite npm test is green at the Phase-153 baseline (792 + migrated/added cases); npm run build succeeds"
  artifacts:
    - path: "src/bee-atlas.ts"
      provides: "_filterState.bounds ownership, _applyBoundsFilter (renamed), all _selectionBounds sites resolved per D-04/D-05/D-06/D-07, bbox/sel restore wiring, query-call cleanup"
      contains: "_applyBoundsFilter"
    - path: "src/bee-pane.ts"
      provides: "boundsFilterActive/boundsFilterLabel props (renamed from selectionBounds*), near-me-cleared dispatch unchanged"
      contains: "boundsFilterActive"
    - path: "src/tests/bee-atlas.test.ts"
      provides: "migrated SEL-06/SEL-07 block + D-04/D-05/D-06 structural asserts + coexistence + bbox/legacy-sel restore"
      contains: "_applyBoundsFilter"
    - path: "src/tests/bee-pane.test.ts"
      provides: "renamed-prop assertions + retained near-me-cleared regression guard"
      contains: "boundsFilterActive"
  key_links:
    - from: "src/bee-atlas.ts _applyBoundsFilter"
      to: "_filterState.bounds"
      via: "spread mutation"
      pattern: "_filterState = \\{ \\.\\.\\.this\\._filterState, bounds"
    - from: "src/bee-atlas.ts _onNearMeCleared"
      to: "_filterState.bounds = null"
      via: "the only bounds-clear path (D-07)"
      pattern: "bounds: null"
    - from: "src/bee-atlas.ts bee-pane template"
      to: "src/bee-pane.ts boundsFilterActive/boundsFilterLabel props"
      via: "property binding"
      pattern: "\\.boundsFilterActive="
---

<objective>
Move the bounding box from the legacy `_selectionBounds` field into `_filterState.bounds` inside `<bee-atlas>`, and resolve all 20 `_selectionBounds` sites per the locked behavior decisions: bounds no longer force-opens the pane (D-04), bounds and per-record selection coexist (D-05), an empty-map click clears selection only (D-06), and `near-me-cleared` is the sole bounds-clear path (D-07). Rename the bee-pane presenter props to filter-oriented names, and migrate the affected test blocks. This is the consumer transition; it depends on Plans 01 (`FilterState.bounds`, query signatures) and 02 (`bbox=`/`sel=` URL contract).

Purpose: Make the state model honest — bounds is a filter, owned in `_filterState`, flowing through the same plumbing as taxon/year. Removing the `{ type: 'bounds' }` variant from `SelectionState` (Plan 02) makes `tsc` surface every remaining bounds-as-selection usage, which is the completeness check for this transition.

Output: `_filterState.bounds` ownership in `bee-atlas.ts`, `_applyBoundsFilter` (renamed), all `_selectionBounds` sites removed/converted, bbox/sel restore wiring, renamed `bee-pane` props, migrated `bee-atlas.test.ts`/`bee-pane.test.ts`, and a green 792-baseline suite + successful production build.
</objective>

<artifacts_this_phase_produces>
NEW / RENAMED symbols introduced by THIS phase (exclude from source-drift checks):
- `_filterState.bounds` usage in `bee-atlas.ts` (the field comes from Plan 01's `FilterState`)
- `_applyBoundsFilter` method (renamed from `_applyBoundsSelection`)
- `_boundsFilterLabel` getter (renamed from `_selectionBoundsLabel`)
- `boundsFilterActive` / `boundsFilterLabel` props in `bee-pane.ts` (renamed from `selectionBoundsActive`/`selectionBoundsLabel`)
REMOVED (intentional, verified by grep == 0): the `@state() _selectionBounds` field and all 20 `_selectionBounds` references in `bee-atlas.ts`; `selectionBoundsActive`/`selectionBoundsLabel` in `bee-pane.ts`.
</artifacts_this_phase_produces>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@CLAUDE.md

@.planning/phases/999.8-separate-spatial-bounds-filter-from-per-record-selection-bac/999.8-CONTEXT.md
@.planning/phases/999.8-separate-spatial-bounds-filter-from-per-record-selection-bac/999.8-RESEARCH.md
@.planning/phases/999.8-separate-spatial-bounds-filter-from-per-record-selection-bac/999.8-PATTERNS.md
@.planning/phases/999.8-separate-spatial-bounds-filter-from-per-record-selection-bac/999.8-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace _selectionBounds with _filterState.bounds — state, getters, restore, query wiring</name>
  <files>src/bee-atlas.ts</files>
  <read_first>
    - src/bee-atlas.ts: _filterState initial literal (82–94), _selectionBounds field (122), intendedFilterActive getter (189–193), _selectionBoundsLabel getter (197–201), bee-pane template bindings (455–456), firstUpdated restore (510–576 incl. the initSel?.type==='bounds' branch 555–558), _runFilterQuery call (636), _runListQuery hasSelection + queryListPage call (897–903), _buildCurrentParams (920–931), _onPopState restore (1122–1171), _onDataLoaded filter guard (1525)
    - 999.8-PATTERNS.md §§ "_filterState initial literal", "@state() _selectionBounds field", "intendedFilterActive getter", "_selectionBoundsLabel getter", "bee-pane template bindings", "firstUpdated filter restore", "_runFilterQuery call", "_runListQuery hasSelection + queryListPage call", "_buildCurrentParams", "_onPopState filter restore"
    - CLAUDE.md §"Architecture Invariants" — style-cache bypass + filter race guard (verify both still hold once bounds is in _filterState)
  </read_first>
  <action>
    In src/bee-atlas.ts, make bounds a field of `_filterState` and delete `_selectionBounds`:
    - Add `bounds: null` to the `_filterState` initial literal (82–94).
    - DELETE the `@state() private _selectionBounds …` field (line 122).
    - In `intendedFilterActive` (189–193): remove `|| this._selectionBounds !== null` — `isFilterActive(this._filterState)` now covers bounds (Plan 01). This is also the style-cache-bypass source (`intendedFilterActive` is passed to bee-map); confirm the bypass still fires for bounds-only via the isFilterActive branch.
    - Rename `_selectionBoundsLabel` → `_boundsFilterLabel` (197–201) and read `this._filterState.bounds` instead of `this._selectionBounds`.
    - Update the bee-pane template bindings (455–456) to `.boundsFilterActive=${this._filterState.bounds !== null}` and `.boundsFilterLabel=${this._boundsFilterLabel}` (prop rename lands in bee-pane.ts in Task 3 — both sides change together).
    - In `firstUpdated` (510–558): add `bounds: initFilter?.bounds ?? null` to the `_filterState` restore spread (513–525); DELETE the `else if (initSel?.type === 'bounds') { … _selectionBounds = …; _paneState = 'list' }` branch (555–558) entirely — bounds now arrives via `initFilter.bounds`, and the `_paneState='list'` force is dropped (D-04). The `isFilterActive(this._filterState)` guard (~543) now includes bounds automatically.
    - `_runFilterQuery` (636): change `queryVisibleGeoJSON(this._filterState, this._selectionBounds)` to `queryVisibleGeoJSON(this._filterState)` (Plan 01 removed the second arg).
    - `_runListQuery` (897–903): remove `|| this._selectionBounds !== null` from `hasSelection`; remove the trailing `this._selectionBounds ?? null` argument from the `queryListPage(...)` call (Plan 01 removed that param). NOTE the deliberate UX consequence: with bounds active and no record selection, `selectionCount` is null so the "N selected" banner does not appear — the list still shows only in-bounds rows because `queryListPage` applies `f.bounds` via `buildFilterSQL`.
    - `_buildCurrentParams` (920–931): remove the `this._selectionBounds && this._paneState === 'list' ? { type: 'bounds' as const, ... } :` leading ternary branch; the selection arg becomes `this._selectedCluster ? { type: 'cluster' as const, ...this._selectedCluster } : { type: 'ids' as const, ids: this._selectedOccIds ?? [] }`. `buildParams` writes `bbox=` from `filter.bounds` automatically (Plan 02).
    - `_onPopState` (1122–1171): add `bounds: parsed.filter?.bounds ?? null` to the `_filterState` restore spread (1122–1134); DELETE the `else if (parsedSel?.type === 'bounds') { _selectionBounds = …; _selectedOccIds=null; _selectedCluster=null }` branch (1163–1165); remove the three `this._selectionBounds = null` lines in the ids/cluster/else branches (1158/1162/1170); remove `|| parsedSel?.type === 'bounds'` from the popstate `hasSelection` (1175–1177).
    - `_onDataLoaded` filter guard (1525): simplify `if (isFilterActive(this._filterState) || this._selectionBounds !== null)` to `if (isFilterActive(this._filterState))`.
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - grep: `_filterState` initial literal in src/bee-atlas.ts contains `bounds: null`
    - grep -c (excluding comment lines `grep -v '^[[:space:]]*//'`) of `_selectionBounds` in src/bee-atlas.ts is reduced toward 0 — at minimum the field declaration (line 122) and the sites in this task's scope are gone; remaining sites (the behavior-change handlers) are resolved in Task 2. After Task 2 the count is 0.
    - grep: src/bee-atlas.ts contains `_boundsFilterLabel` (renamed) and NOT `_selectionBoundsLabel`
    - grep: `queryVisibleGeoJSON(this._filterState)` appears with a single argument (no second arg)
    - grep: `_buildCurrentParams` contains NO `type: 'bounds' as const`
    - `npx tsc --noEmit` exits 0 (Plan 02's narrowed SelectionState would error on any remaining `{ type: 'bounds' }` — green tsc confirms those are gone in this task's scope)
  </acceptance_criteria>
  <done>Bounds lives in _filterState; getters/restore/query-wiring no longer reference _selectionBounds; tsc is green for the scope touched here.</done>
</task>

<task type="auto">
  <name>Task 2: Resolve the behavior-change handlers — _applyBoundsFilter, near-me-cleared, map-click-empty, coexistence (D-04/D-05/D-06/D-07)</name>
  <files>src/bee-atlas.ts</files>
  <read_first>
    - src/bee-atlas.ts: _applyBoundsSelection (1325–1340) + its call sites (1056, 1339), _onNearMeCleared (1076–1087), _onMapClickEmpty (1342–1367), _onFilterChanged (1369–1408), _onClearSelection (1434–1442), _onPaneCollapse (1485–1491), _onOccurrenceClick (1215–1227), _onRegionClick else branch (1280), _onPlaceSelected else branch (1304), _openSidebarForFilter (1313–1320)
    - 999.8-PATTERNS.md §§ "_applyBoundsSelection RENAME + REFACTOR", "_onNearMeCleared", "_onMapClickEmpty", "_onFilterChanged", "_onClearSelection", "_onPaneCollapse", "other _selectionBounds = null removal sites", and §"Shared Patterns / bounds filter preserved through FilterChangedEvent"
    - 999.8-CONTEXT.md decisions D-04/D-05/D-06/D-07 and the planning_context <resolved_open_questions> (near-me-clear and pane-collapse must NOT touch _filterState.bounds except where stated)
  </read_first>
  <action>
    Resolve every remaining `_selectionBounds` site in src/bee-atlas.ts per the locked decisions:
    - RENAME `_applyBoundsSelection` → `_applyBoundsFilter` (1325–1340) and update both call sites (`_onSelectionDrawn` at ~1339 and the near-me success path at ~1056). In the body: change `this._selectionBounds = bounds` to `this._filterState = { ...this._filterState, bounds }`; REMOVE `this._selectedOccIds = null` and `this._selectedCluster = null` (D-05); REMOVE `this._paneState = 'list'` (D-04). KEEP `++this._selectionDrawnGeneration`, `this._listPage = 1`, and the `_runFilterQuery()`/`_runListQuery()`/`_runTableQuery()`/`_replaceUrlState()` sequence. This preserves the D-01 guarantee that near-me and shift-drag produce byte-identical `_filterState.bounds`.
    - `_onNearMeCleared` (1076–1087): change `this._selectionBounds = null` to `this._filterState = { ...this._filterState, bounds: null }`; REMOVE `this._selectedOccIds = null` and `this._selectedCluster = null` (D-05 — clearing bounds does not clear record selection); REMOVE `this._paneState = 'collapsed'` (D-04/resolved-open-question 1 — clearing bounds does NOT touch pane state, symmetric with apply). KEEP `_runFilterQuery()`/`_runTableQuery()`/`_replaceUrlState()`. This is the ONLY path that clears bounds (D-07).
    - `_onMapClickEmpty` (1342–1367): REMOVE `this._selectionBounds = null` from BOTH branches (D-06). Bounds is a filter and survives an empty-map click; only `_selectedOccIds`/`_selectedCluster` (and, in the boundary-mode branch, the county/ecoregion sets) are cleared.
    - `_onFilterChanged` (1369–1408): add `bounds: this._filterState.bounds` to the new `_filterState` spread (1373–1385) to PRESERVE active bounds across filter-panel changes (CRITICAL — `FilterChangedEvent` carries no `bounds`; do NOT default it to null). REMOVE `this._selectionBounds = null` (1399, D-05).
    - `_onClearSelection` (1434–1442): REMOVE `this._selectionBounds = null` (1437) — clearing per-record selection leaves bounds active (D-05, resolved-open-question 2).
    - `_onPaneCollapse` (1485–1491): REMOVE `this._selectionBounds = null` (1488) — pane collapse leaves bounds active; bounds is cleared only via near-me-cleared (D-07, resolved-open-question 3). This is a deliberate behavior change from current code; assert it in tests.
    - REMOVE the standalone `this._selectionBounds = null` in `_onOccurrenceClick` (1217), `_onRegionClick` else branch (1280), `_onPlaceSelected` else branch (1304), and `_openSidebarForFilter` (1316) — all D-05 (record selection / region / place changes no longer clear bounds; spread sites inherit bounds automatically).
  </action>
  <verify>
    <automated>npx tsc --noEmit && grep -vc '^[[:space:]]*//' src/bee-atlas.ts >/dev/null && test "$(grep -v '^[[:space:]]*//' src/bee-atlas.ts | grep -c '_selectionBounds')" = "0"</automated>
  </verify>
  <acceptance_criteria>
    - grep: `_applyBoundsFilter` is defined and `_applyBoundsSelection` no longer appears in src/bee-atlas.ts
    - grep (excluding comment lines via `grep -v '^[[:space:]]*//'`): `_selectionBounds` count in src/bee-atlas.ts is exactly 0
    - grep: `_applyBoundsFilter` body contains `_filterState = { ...this._filterState, bounds }` and does NOT contain `_paneState = 'list'` or `_selectedOccIds = null`
    - grep: `_onNearMeCleared` body contains `bounds: null` and does NOT contain `_paneState = 'collapsed'`
    - grep: `_onFilterChanged` `_filterState` spread contains `bounds: this._filterState.bounds`
    - grep: `_onMapClickEmpty` body contains NO `_selectionBounds` and NO assignment clearing bounds
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>All 20 _selectionBounds sites resolved per D-04/D-05/D-06/D-07; _applyBoundsFilter is the renamed shared transition; bounds is preserved through filter changes and cleared only via near-me-cleared; grep confirms zero _selectionBounds remain.</done>
</task>

<task type="auto">
  <name>Task 3: Rename bee-pane props + migrate bee-atlas/bee-pane tests; assert full 792-baseline green build</name>
  <files>src/bee-pane.ts, src/tests/bee-pane.test.ts, src/tests/bee-atlas.test.ts</files>
  <read_first>
    - src/bee-pane.ts: prop declarations (88–90), template bindings + near-me-cleared button (1051–1066)
    - 999.8-PATTERNS.md §§ "src/bee-pane.ts — prop declarations", "src/bee-pane.ts — template bindings"
    - src/tests/bee-pane.test.ts: describe `NEAR-01/D-04/D-05` (299–357) — the prop-name and near-me-cleared assertions
    - src/tests/bee-atlas.test.ts: describe `SEL-06 + SEL-07 wiring` (386–508) — the source-text grep assertions that assert the OLD coupling; the NEAR-01 restore tests (~404–428); the `_onPaneCollapse`/`_onMapClickEmpty`/`_onFilterChanged` clear assertions
    - 999.8-RESEARCH.md §"Test Surface" — the per-test required-update table and the list of new tests to add
  </read_first>
  <action>
    Rename the bee-pane presenter props and migrate the tests so the suite reflects the new model:
    - In src/bee-pane.ts: rename `@property … selectionBoundsActive` → `boundsFilterActive` and `selectionBoundsLabel` → `boundsFilterLabel` (88–90). Update all 4 template references (1051–1066) accordingly. Leave the `near-me-cleared` CustomEvent dispatch and button structure UNCHANGED (D-07 — event name is stable).
    - In src/tests/bee-pane.test.ts (NEAR-01 block, 299–357): update prop-name assertions to `boundsFilterActive`/`boundsFilterLabel`; keep the `near-me-cleared` regression assertions (dispatch present, bubbles+composed, active branch drives it), the no-standalone-chip assertion, and the "does not contain _selectionBounds/nearMeCenter/haversine" guard (now also a guard against the renamed-away symbol).
    - In src/tests/bee-atlas.test.ts (SEL-06/SEL-07 block, 386–508): REMOVE/REWRITE the tests that grep for the deleted coupling — `_pushUrlState gives _selectionBounds precedence`, `_pushUrlState emits type: 'bounds'`, `firstUpdated routes bounds selection to _selectionBounds`, `_onPopState routes bounds selection to _selectionBounds`, the `_onPaneCollapse clears _selectionBounds`, `_onMapClickEmpty clears _selectionBounds in both branches`, `_onFilterChanged clears _selectionBounds`, `_onPopState clears _selectionBounds in 3 branches`, `_onOccurrenceClick clears _selectionBounds`, `_onRegionClick/_onPlaceSelected/_openSidebarForFilter clears _selectionBounds`, and the `SM-01 _onPaneCollapse clears three fields` bounds assertion. UPDATE `_onSelectionDrawn delegates to _applyBoundsSelection…` to assert delegation to `_applyBoundsFilter` and that it sets `_filterState … bounds` (not `_selectionBounds`). UPDATE the NEAR-01 restore/data-loaded/intendedFilterActive tests to the simplified `isFilterActive(this._filterState)` form.
    - ADD new tests in src/tests/bee-atlas.test.ts asserting the new model (source-text grep style, matching the existing block): `_filterState` initial literal includes `bounds: null`; `_applyBoundsFilter` writes `_filterState … bounds` and does NOT set `_paneState = 'list'` (D-04) and does NOT null `_selectedOccIds` (D-05); `_onMapClickEmpty` does not clear bounds (D-06); `_onFilterChanged` preserves `bounds: this._filterState.bounds` (D-05); `_onNearMeCleared` clears bounds via `_filterState … bounds: null` and does not touch `_paneState` (D-07); `intendedFilterActive` no longer references `_selectionBounds`; firstUpdated/popstate restore add `bounds: …?.bounds ?? null` (bbox + legacy sel restore via filter); a coexistence assertion that no handler couples bounds-clear to record-selection-clear. Add a top-level guard test: `bee-atlas.ts source contains no _selectionBounds` (grep == 0).
  </action>
  <verify>
    <automated>npm test && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - grep: src/bee-pane.ts contains `boundsFilterActive` and `boundsFilterLabel`, and NO `selectionBoundsActive`/`selectionBoundsLabel`
    - grep: src/bee-pane.ts still contains the `near-me-cleared` CustomEvent dispatch (unchanged)
    - grep: src/tests/bee-pane.test.ts asserts `boundsFilterActive`/`boundsFilterLabel` and retains the `near-me-cleared` regression test
    - grep: src/tests/bee-atlas.test.ts contains `_applyBoundsFilter` and a test asserting `bee-atlas.ts` has zero `_selectionBounds`
    - `npm test` exits 0 — full suite green at the Phase-153 baseline (792 tests, adjusted for the migrated/added cases; count must not drop below 792)
    - `npm run build` exits 0 (tsc --noEmit → eleventy + Vite production build succeeds)
  </acceptance_criteria>
  <done>bee-pane props renamed and bound on both sides; bee-atlas/bee-pane tests migrated to the new model with D-04/D-05/D-06/D-07 + coexistence + bbox/legacy-sel restore assertions; full suite green at/above 792 and production build succeeds.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| popstate/URL → _filterState.bounds | restored bounds floats (already validated by Plan 02's parseParams) become reactive filter state |
| gesture (shift-drag / near-me) → _filterState.bounds | bounds object produced as numbers by the map/geolocation path |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-9998-04 | Tampering / Injection | bounds restore + apply in bee-atlas.ts | mitigate (by upstream validation) | bee-atlas.ts never parses raw URL text for bounds — it receives an already-validated `bounds` object from Plan 02's `parseParams` (range + finite checks) on restore, and a numeric object from the gesture path on apply. SQL interpolation happens only in Plan 01's `buildFilterSQL` over numeric values. No new string-concatenation of untrusted input into SQL is introduced. |
| T-9998-05 | Tampering | filter race guard (`_filterGuard`) | mitigate | A bounds change IS a filter change: `_applyBoundsFilter` / `_onNearMeCleared` call `_runFilterQuery()`, which increments the existing `makeStaleGuard` generation counter, so stale `queryVisibleIds`/`queryVisibleGeoJSON` results are discarded (CLAUDE.md filter-race-guard invariant preserved). |
| T-9998-06 | Information disclosure | style-cache bypass (`intendedFilterActive`) | accept/mitigate | Folding bounds into `_filterState` means `isFilterActive` is true for bounds-only, so `intendedFilterActive` (passed to bee-map) keeps the style cache bypassed for bounds-only state — no regression to the CLAUDE.md style-cache invariant. Verified in Task 1. |
| T-9998-SC | Tampering | npm/pip/cargo installs | n/a | No package installs in this phase. |
</threat_model>

<verification>
- `npx tsc --noEmit` green after Tasks 1 and 2 (Plan 02's narrowed `SelectionState` surfaces any missed bounds-as-selection usage as a compile error — green tsc is the completeness check).
- `grep -v '^[[:space:]]*//' src/bee-atlas.ts | grep -c _selectionBounds` == 0 after Task 2.
- `npm test` full suite green at/above the 792 Phase-153 baseline after Task 3.
- `npm run build` succeeds (tsc --noEmit → eleventy + Vite) — production build is a regression gate per project conventions.
- Manual UAT (VALIDATION.md): (1) open a saved `?sel=…` link → map shows only in-bounds dots, label in 'where' input, URL rewrites to `bbox=` on next interaction (D-03); (2) shift-drag a box then click a cluster → both bounds filter and record selection remain active (D-05). These are operator-verified, not automated.
- D-08 (global clear-all reset) is DEFERRED — no global-reset affordance exists; bounds-in-FilterState makes a future reset trivial. No task implements it (correctly, per CONTEXT.md Deferred Ideas).
</verification>

<success_criteria>
- No `_selectionBounds` references remain in bee-atlas.ts (grep == 0).
- _applyBoundsFilter writes _filterState.bounds; no pane force-open (D-04); no record-selection clear (D-05).
- Empty-map click clears selection only (D-06); near-me-cleared is the sole bounds-clear (D-07).
- Legacy ?sel= and ?bbox= both restore into _filterState.bounds and re-serialize as bbox=.
- bee-pane props renamed; near-me-cleared dispatch unchanged.
- npm test green at/above 792; npm run build succeeds.
</success_criteria>

<output>
Create `.planning/phases/999.8-separate-spatial-bounds-filter-from-per-record-selection-bac/999.8-03-SUMMARY.md` when done.
</output>
