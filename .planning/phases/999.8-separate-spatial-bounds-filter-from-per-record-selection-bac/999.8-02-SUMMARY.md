---
phase: "999.8"
plan: "02"
subsystem: url-state
tags: [url, filter, bounds, bbox, tdd, D-02, D-03]
dependency_graph:
  requires:
    - FilterState.bounds field (Plan 01)
  provides:
    - SelectionState union narrowed to ids/cluster only (no bounds variant)
    - buildParams writes bbox= from filter.bounds (D-02)
    - parseParams reads bbox= into filter.bounds (canonical)
    - parseParams reads legacy sel= into filter.bounds (D-03 back-compat)
    - boundsResult local in parseParams (bbox= takes precedence over sel=)
    - hasFilter predicate includes boundsResult!==null
    - filter.bounds field in parseParams result.filter object
  affects:
    - src/url-state.ts
    - src/tests/url-state.test.ts
tech_stack:
  added: []
  patterns:
    - bbox= URL param (mirrors counties=/place= filter-param write pattern)
    - boundsResult local for two-source merge (bbox= canonical + sel= legacy)
    - Read-old/write-new migration for sel= legacy URLs (D-03)
key_files:
  created: []
  modified:
    - src/url-state.ts
    - src/tests/url-state.test.ts
decisions:
  - D-02 implemented: buildParams writes bbox=west,south,east,north (toFixed(4)) from filter.bounds; sel= write removed
  - D-03 implemented: parseParams reads both bbox= (canonical) and sel= (legacy) into filter.bounds; neither yields selection.type==='bounds'
  - SelectionState union narrowed: { type:'bounds' } variant removed; only ids/cluster remain
  - bbox= takes precedence over sel= when both present (manually crafted URL edge case)
  - o= reader (ids/cluster selection) is orthogonal; bbox= and o= may coexist (D-05)
metrics:
  duration: "4 minutes"
  completed: "2026-06-21"
  tasks_completed: 2
  files_modified: 2
---

# Phase 999.8 Plan 02: URL bbox= Migration + Legacy sel= Back-Compat Summary

**One-liner:** Migrated bounds URL contract to `bbox=` filter param, removed `{ type: 'bounds' }` from SelectionState, and added `sel=` legacy read-into-filter.bounds back-compat (D-02/D-03).

## What Was Built

**Task 1** (TDD RED→GREEN): Updated `emptyFilter()` in `url-state.test.ts` to include `bounds: null`. Replaced the `bounds selection (SEL-06)` describe block with `bounds filter (D-01/D-02/D-03)` covering buildParams bbox= write, no-sel= assertion, filter.bounds=null emits neither, and coexistence with o=. In `url-state.ts`: removed `{ type: 'bounds'; west; south; east; north }` from the `SelectionState` union (leaving only `ids`/`cluster`); deleted the `else if (selection.type === 'bounds') { params.set('sel', ...) }` branch from `buildParams`; added `if (filter.bounds !== null) { params.set('bbox', [...].join(',')) }` placed with the filter params before the selection cascade.

**Task 2** (TDD RED→GREEN — parseParams-side tests were already RED from Task 1 commit): Added `boundsResult` local (`null` initial). Added a `bbox=` reader using the identical 4-part split + finite/range validation (lon [-180,180], lat [-90,90], south<north) as the former `sel=` reader — populates `boundsResult` on success. Added legacy `sel=` reader that populates `boundsResult` only when `boundsResult === null` (bbox takes precedence); removed the old `result.selection = { type: 'bounds', ... }` assignment. Updated `hasFilter` predicate to include `|| boundsResult !== null`. Added `bounds: boundsResult` to the `result.filter` object literal. The `o=` reader (ids/cluster) is completely orthogonal — coexistence test confirms both may be present simultaneously.

## Commits

| Task | Phase | Commit | Description |
|------|-------|--------|-------------|
| Task 1 | RED | bd1a1925 | test(999.8-02): add failing tests for bbox= write + parseParams bounds-as-filter |
| Task 1 | GREEN | 4d8d9362 | feat(999.8-02): narrow SelectionState; write bbox= from filter.bounds in buildParams |
| Task 2 | GREEN | 6b3235af | feat(999.8-02): parseParams reads bbox= and legacy sel= into filter.bounds (D-03) |

Note: Task 2 RED tests were included in the Task 1 RED commit (the `bounds filter` describe block covers both write-side and parse-side tests; the parse-side tests remained failing until Task 2 GREEN).

## Deviations from Plan

None — plan executed exactly as written.

## TypeScript Consumer Errors (Expected)

`npx tsc --noEmit` produces errors in `bee-atlas.ts`, `bee-map.ts`, and `bee-atlas.test.ts` — all consumers that still reference `_selectionBounds`, the removed `type: 'bounds'` SelectionState variant, or FilterState literals missing `bounds: null`. These are expected and will be resolved in Plan 03. No errors in `url-state.ts` or `filter.ts`.

## Threat Model Coverage

T-9998-02 (Tampering/Injection via bbox=/sel= floats): Mitigated. The `bbox=` reader applies the identical finite + range validation as the former `sel=` reader before setting `boundsResult`. Invalid input yields `null` (no bounds), never a thrown error or unvalidated value. Validated floats reach SQL only via Plan 01's numeric-interpolation `buildFilterSQL` clause.

T-9998-03 (Information disclosure via bbox= URL param): Accepted. `bbox=` encodes a viewport rectangle (same exposure class as retired `sel=` and existing `counties=`/`place=`). No PII.

## Known Stubs

None.

## Self-Check: PASSED

- FOUND: src/url-state.ts
- FOUND: src/tests/url-state.test.ts
- FOUND: 999.8-02-SUMMARY.md
- FOUND commit bd1a1925 (test RED Task 1)
- FOUND commit 4d8d9362 (feat GREEN Task 1)
- FOUND commit 6b3235af (feat GREEN Task 2)
