---
phase: 33-test-infrastructure
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/package.json
  - frontend/vite.config.ts
  - frontend/src/smoke.test.ts
autonomous: true
requirements:
  - TEST-01

must_haves:
  truths:
    - "Running `npm test` in frontend/ executes Vitest and exits 0 when all tests pass"
    - "Running `npm test` in frontend/ exits non-zero when any test fails"
    - "happy-dom is the configured test environment so DOM APIs are available"
    - "A trivial passing test exists that validates the harness without importing any app module"
  artifacts:
    - path: "frontend/vite.config.ts"
      provides: "Vitest test configuration with happy-dom environment"
      contains: "environment: 'happy-dom'"
    - path: "frontend/package.json"
      provides: "test script and devDependencies"
      contains: "vitest run"
    - path: "frontend/src/smoke.test.ts"
      provides: "Trivial passing test proving harness works"
      contains: "expect"
  key_links:
    - from: "frontend/package.json"
      to: "vitest"
      via: "test script invokes vitest run"
      pattern: "\"test\":\\s*\"vitest run\""
    - from: "frontend/vite.config.ts"
      to: "vitest"
      via: "test block configures environment"
      pattern: "test:.*environment"
---

<objective>
Install Vitest and happy-dom in frontend/, configure the test runner via vite.config.ts, and add a trivial smoke test that validates the harness is wired correctly.

Purpose: Establish the test infrastructure that all subsequent v1.9 phases depend on. Phase 34 eliminates module side effects so real tests become possible; Phase 38 writes those tests. This phase ensures `npm test` works.

Output: Working `npm test` command, configured happy-dom environment, one passing smoke test.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/33-test-infrastructure/33-CONTEXT.md
@.planning/phases/33-test-infrastructure/33-RESEARCH.md

<interfaces>
<!-- No existing interfaces consumed. This plan creates new config only. -->

From frontend/vite.config.ts (current state — will be extended):
```typescript
import { defineConfig } from 'vite';

export default defineConfig({
  build: {
    sourcemap: true,
  },
});
```

From frontend/package.json scripts (current state — will be extended):
```json
{
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview"
  }
}
```

From frontend/tsconfig.json (read-only reference — DO NOT modify):
```json
{
  "compilerOptions": {
    "types": ["vite/client"],
    "verbatimModuleSyntax": true,
    "noUncheckedIndexedAccess": true
  }
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install Vitest + happy-dom and configure test runner</name>
  <files>frontend/package.json, frontend/vite.config.ts</files>
  <read_first>frontend/package.json, frontend/vite.config.ts, frontend/tsconfig.json</read_first>
  <action>
1. Install devDependencies from the frontend/ directory:
   ```
   cd frontend && npm install -D vitest happy-dom
   ```

2. Add test script to frontend/package.json scripts object (per D-01, using `vitest run` for non-watch CI-friendly exit):
   ```json
   "test": "vitest run"
   ```
   The scripts block should become:
   ```json
   "scripts": {
     "dev": "vite",
     "build": "tsc && vite build",
     "preview": "vite preview",
     "test": "vitest run"
   }
   ```

3. Extend frontend/vite.config.ts with Vitest test block (per D-01: extend existing config, do not create separate vitest.config.ts; per D-02: set environment to happy-dom). Add the triple-slash directive as the FIRST line (Vitest 4 requires `vitest/config`, NOT the old `vitest` form):
   ```typescript
   /// <reference types="vitest/config" />
   import { defineConfig } from 'vite';

   export default defineConfig({
     build: {
       sourcemap: true,
     },
     test: {
       environment: 'happy-dom',
     },
   });
   ```

DO NOT modify frontend/tsconfig.json. DO NOT add `globals: true` to the test config. DO NOT add `"vitest/globals"` to tsconfig types. Test files will use explicit `import { test, expect } from 'vitest'` imports per research recommendation to avoid type conflicts with the existing `"types": ["vite/client"]`.
  </action>
  <verify>
    <automated>cd /Users/rainhead/dev/beeatlas/frontend && node -e "const p = require('./package.json'); if (!p.devDependencies.vitest) throw 'vitest missing'; if (!p.devDependencies['happy-dom']) throw 'happy-dom missing'; if (p.scripts.test !== 'vitest run') throw 'test script wrong'; console.log('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - frontend/package.json devDependencies contains "vitest"
    - frontend/package.json devDependencies contains "happy-dom"
    - frontend/package.json scripts.test equals "vitest run"
    - frontend/vite.config.ts first line is `/// <reference types="vitest/config" />`
    - frontend/vite.config.ts contains `test:` block with `environment: 'happy-dom'`
    - frontend/tsconfig.json is UNCHANGED (still has only `"types": ["vite/client"]`)
    - node_modules/vitest exists after install
  </acceptance_criteria>
  <done>Vitest and happy-dom are installed, vite.config.ts has test block with happy-dom environment, package.json has "test": "vitest run" script</done>
</task>

<task type="auto">
  <name>Task 2: Add trivial smoke test and verify harness</name>
  <files>frontend/src/smoke.test.ts</files>
  <read_first>frontend/vite.config.ts, frontend/package.json</read_first>
  <action>
1. Create frontend/src/smoke.test.ts with explicit vitest imports (per D-04: pure assertion, NO module imports from the app — DuckDB side effects would crash the test runner):
   ```typescript
   import { expect, test } from 'vitest';

   test('harness is wired', () => {
     expect(1 + 1).toBe(2);
   });
   ```

   Per D-03: co-locate test files alongside source in src/. The file name `smoke.test.ts` signals this is a harness validation test, not a setup/fixture file.

2. Run `cd frontend && npm test` and confirm:
   - Vitest runs and finds 1 test file
   - 1 test passes
   - Exit code is 0

3. Verify non-zero exit on failure by temporarily checking: the `vitest run` command exits non-zero when a test fails. This can be confirmed by reviewing vitest documentation or by noting that `vitest run` is documented to exit 1 on failure. No need to actually break the test.

DO NOT import any module from src/ (filter.ts, bee-map.ts, duckdb.ts, etc.) — they have module-level DuckDB WASM side effects that will crash in Node/happy-dom. Phase 34 removes those side effects.
  </action>
  <verify>
    <automated>cd /Users/rainhead/dev/beeatlas/frontend && npm test</automated>
  </verify>
  <acceptance_criteria>
    - frontend/src/smoke.test.ts exists
    - frontend/src/smoke.test.ts contains `import { expect, test } from 'vitest'`
    - frontend/src/smoke.test.ts contains `expect(1 + 1).toBe(2)`
    - frontend/src/smoke.test.ts does NOT contain any import from a local module (no `import` from `./` or `../`)
    - `cd frontend && npm test` exits with code 0
    - Vitest output shows 1 test passed
  </acceptance_criteria>
  <done>`npm test` in frontend/ runs Vitest, finds smoke.test.ts, passes 1 test, exits 0. Harness is confirmed working.</done>
</task>

</tasks>

<verification>
1. `cd frontend && npm test` exits 0 with 1 passing test
2. `grep -q "vitest/config" frontend/vite.config.ts` confirms triple-slash directive
3. `grep -q "happy-dom" frontend/vite.config.ts` confirms environment config
4. `grep -q '"test": "vitest run"' frontend/package.json` confirms test script
5. `grep -c "import.*from '\./\|from '\.\./" frontend/src/smoke.test.ts` returns 0 (no local imports)
</verification>

<success_criteria>
- `npm test` in frontend/ runs Vitest with happy-dom environment and exits 0
- A single trivial test passes without importing any application module
- The test infrastructure is ready for Phase 38 to add real tests after Phase 34 removes side effects
</success_criteria>

<output>
After completion, create `.planning/phases/33-test-infrastructure/33-01-SUMMARY.md`
</output>
