---
phase: 169-per-collector-static-pages
plan: 02
type: execute
wave: 2
depends_on: ["169-01"]
files_modified:
  - _data/collectors.js
  - _pages/collector-detail.njk
  - _pages/collectors.njk
  - src/tests/data-collectors.test.ts
autonomous: true
requirements: [PAGE-01, PAGE-02, PAGE-03, PAGE-04]
must_haves:
  truths:
    - "A bookmarkable page exists at /collectors/{login}/ for every gated collector (D-01: gate honored via the committed collectors.json)"
    - "Each collector page shows the H1 display name (human name or @login fallback) and headline stats (specimens, samples, species) (D-04: human name w/ @login fallback; D-03: specimen/sample/species counts)"
    - "Each collector page shows the pending-vs-identified split (N identified, N awaiting ID) (D-05/D-06/D-07: over specimens, species-rank = identified, not id_date)"
    - "Each collector page deep-links to the map via the existing ?collectors=<recordedBy>:<host_inat_login> param (D-10: reuse existing collectors= param, no new FilterState field)"
    - "An index roster at /collectors.html lists every generated collector (D-08: index page like /places.html)"
    - "npm test asserts collectors.json is an array of >= 100 records with required fields and the split invariant (D-09: Vitest floor >= 100, parallel to data-places.test.ts)"
  artifacts:
    - path: "_data/collectors.js"
      provides: "Eleventy data loader exposing { collectorsArray } from collectors.json"
      contains: "collectorsArray"
    - path: "_pages/collector-detail.njk"
      provides: "Per-collector detail page (pagination size:1, trailing-slash permalink, deep-link)"
      contains: "/collectors/"
    - path: "_pages/collectors.njk"
      provides: "Collector index roster at /collectors.html"
      contains: "/collectors.html"
    - path: "src/tests/data-collectors.test.ts"
      provides: "Vitest floor (>=100) + shape + split-invariant + no-parquet test"
      contains: "collectorsArray"
  key_links:
    - from: "_pages/collector-detail.njk"
      to: "_data/collectors.js"
      via: "pagination data: collectors.collectorsArray"
      pattern: "collectors\\.collectorsArray"
    - from: "_pages/collector-detail.njk"
      to: "src/url-state.ts collectors= param"
      via: "?collectors={recordedBy}:{host_inat_login} deep-link"
      pattern: "collectors="
    - from: "_data/collectors.js"
      to: "public/data/collectors.json"
      via: "readFileSync of collectors.json (JSON only, never parquet)"
      pattern: "collectors\\.json"
---

<objective>
Build the Eleventy frontend over the committed `collectors.json`: a data loader,
a per-collector detail page at `/collectors/{login}/` (headline stats + status
split + map deep-link), an index roster at `/collectors.html`, and a Vitest test
enforcing the D-09 floor and the record shape.

Purpose: Delivers PAGE-01 (bookmarkable pages), PAGE-02 (headline stats), PAGE-03
(pending-vs-identified split), and PAGE-04 (map deep-link). Mirrors the places
pattern exactly with the documented deviations (trailing-slash permalink, no
land_owner, ?collectors= deep-link).

Output: `_data/collectors.js`, `_pages/collector-detail.njk`, `_pages/collectors.njk`,
`src/tests/data-collectors.test.ts`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/169-per-collector-static-pages/169-CONTEXT.md
@.planning/phases/169-per-collector-static-pages/169-RESEARCH.md
@.planning/phases/169-per-collector-static-pages/169-PATTERNS.md
@.planning/phases/169-per-collector-static-pages/169-VALIDATION.md
@public/data/collectors.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Vitest data-shape + floor test for _data/collectors.js (Wave 0 test-first)</name>
  <files>src/tests/data-collectors.test.ts</files>
  <read_first>
    - src/tests/data-places.test.ts (the exact analog: imports + ROOT at lines 1-10, array-shape test at lines 12-14, field-type loop at lines 17-25, length floor at lines 27-29, no-parquet assertion at lines 31-34)
    - .planning/phases/169-per-collector-static-pages/169-RESEARCH.md §"Vitest Floor Test Skeleton (D-09)" (lines 455-497 — full target test)
    - .planning/phases/169-per-collector-static-pages/169-PATTERNS.md §"src/tests/data-collectors.test.ts" (substitution map)
    - public/data/collectors.json (the committed artifact this test reads via _data/collectors.js — already produced by Plan 01)
  </read_first>
  <action>
    Create `src/tests/data-collectors.test.ts` mirroring `data-places.test.ts`. Import
    `collectors` from `'../../_data/collectors.js'` with the `// @ts-expect-error` comment
    (no .d.ts for _data/*.js). Tests: (a) `(collectors as any).collectorsArray` is an Array;
    (b) `collectorsArray.length >= 100` (D-09 floor — strengthen the places `> 0`);
    (c) every entry has correct field types — login (string), display_name (string),
    specimen_count (number), sample_count (number), species_count (number),
    status_denominator (number), status_identified (number), status_awaiting (number)
    (drop land_owner; recordedBy may be string-or-null so do not assert string on it);
    (d) the PAGE-03 invariant `status_identified + status_awaiting === status_denominator`
    for every record; (e) `_data/collectors.js` source does NOT match `/parquet/i`
    (Pitfall #8). Because Plan 01 already committed collectors.json and Task 2 creates
    _data/collectors.js, this test goes GREEN once Task 2 lands. If Task 2 has not yet run,
    the import of `_data/collectors.js` fails (RED) — acceptable Wave-0 state.
  </action>
  <verify>
    <automated>npm test -- src/tests/data-collectors.test.ts 2>&1 | tail -25</automated>
  </verify>
  <acceptance_criteria>
    - src/tests/data-collectors.test.ts exists and imports from `../../_data/collectors.js`
    - The file asserts `collectorsArray.length` is `>= 100` (uses `toBeGreaterThanOrEqual(100)`)
    - The file asserts `status_identified + status_awaiting === status_denominator`
    - The file asserts `_data/collectors.js` source does not match `/parquet/i`
  </acceptance_criteria>
  <done>The Vitest file encodes the floor, field-type, split-invariant, and no-parquet assertions; it is GREEN after Task 2 creates the loader (RED before).</done>
</task>

<task type="auto">
  <name>Task 2: _data/collectors.js loader</name>
  <files>_data/collectors.js</files>
  <read_first>
    - _data/places.js (the exact template — clone all 20 lines, substituting placesArray→collectorsArray and places.json→collectors.json; the Pitfall #8 header comment at lines 8-9)
    - .planning/phases/169-per-collector-static-pages/169-RESEARCH.md §"Pattern 2: Eleventy Data Loader" (lines 190-209)
    - .planning/phases/169-per-collector-static-pages/169-PATTERNS.md §"_data/collectors.js"
    - public/data/collectors.json (the JSON this loader reads, committed by Plan 01)
  </read_first>
  <action>
    Create `_data/collectors.js` by cloning `_data/places.js`. Keep the imports
    (readFileSync / fileURLToPath / dirname-join) and the `here`/`repoRoot` derivation
    verbatim. Read `public/data/collectors.json` (NOT parquet — Pitfall #8) into
    `collectorsArray` and `export default { collectorsArray };`. Update the header comment to
    describe the collectors contract and reference `src/tests/data-collectors.test.ts` as the
    asserting test. Do not sort — pipeline/JSON order is authoritative (matches the places
    loader's stated contract).
  </action>
  <verify>
    <automated>npm test -- src/tests/data-collectors.test.ts 2>&1 | tail -25</automated>
  </verify>
  <acceptance_criteria>
    - _data/collectors.js exists and contains `export default { collectorsArray }`
    - _data/collectors.js reads `public/data/collectors.json`
    - `grep -ci parquet _data/collectors.js` is 0 (Pitfall #8)
    - `npm test -- src/tests/data-collectors.test.ts` exits 0 (all Task 1 assertions GREEN)
  </acceptance_criteria>
  <done>_data/collectors.js exposes { collectorsArray } from collectors.json (JSON only), and the Task 1 Vitest is green.</done>
</task>

<task type="auto">
  <name>Task 3: collector-detail.njk + collectors.njk pages (stats, status split, deep-link, index)</name>
  <files>_pages/collector-detail.njk, _pages/collectors.njk</files>
  <read_first>
    - _pages/place-detail.njk (the analog: pagination/permalink front-matter at lines 1-10, stats body with quantify at lines 11-23, deep-link `/?place=` at line 22)
    - _pages/places.njk (the index analog: front-matter at lines 1-4, loop at lines 9-17)
    - src/url-state.ts (lines ~110-188 — the `collectors=` param encode/decode; entries encoded as `recordedBy:host_inat_login`, `|`-joined; parseParams decodes via decodeURIComponent on both halves)
    - eleventy.config.js (line 33 — `quantify` filter already registered)
    - src/styles/places.css (the stylesheet to reuse via `<link rel="stylesheet" href="/src/styles/places.css">`)
    - .planning/phases/169-per-collector-static-pages/169-RESEARCH.md §"Pattern 3" (permalink + deep-link njk, lines 211-236) and §"D-10 Research Flag Resolution" (lines 258-301 — coverage proof, sample-host-only `?collectors=:login` form)
    - .planning/phases/169-per-collector-static-pages/169-PATTERNS.md §"_pages/collector-detail.njk" and §"_pages/collectors.njk" (substitution maps + the trailing-slash permalink deviation)
    - .planning/phases/169-per-collector-static-pages/169-CONTEXT.md (D-04 display name, D-08 index)
  </read_first>
  <action>
    Create `_pages/collector-detail.njk` from `place-detail.njk`. Front-matter: pagination
    `data: collectors.collectorsArray`, `size: 1`, `alias: collector`; permalink
    `"/collectors/{{ collector.login | urlencode }}/index.html"` (trailing-slash URL —
    DEVIATION from places' `.html`; per RESEARCH Pitfall 3); eleventyComputed title
    `"{{ collector.display_name }} — BeeAtlas"`; layout default.njk. Body: `<link>` the
    `places.css` stylesheet; H1 = `{{ collector.display_name }}` (already resolved to human
    name or @login in the JSON, D-04); a metadata line using the `quantify` filter for
    `{{ collector.specimen_count | quantify("specimen") }}`,
    `{{ collector.sample_count | quantify("sample") }}`, and
    `{{ collector.species_count | quantify("species") }}` (PAGE-02); a status-split line
    reading e.g. `{{ collector.status_identified }} identified to species,
    {{ collector.status_awaiting }} awaiting ID` (PAGE-03 — render only when
    `collector.status_denominator > 0`); and the map deep-link (PAGE-04, D-10):
    `<a href="/?collectors={{ collector.recordedBy | urlencode }}:{{ collector.host_inat_login | urlencode }}">View on the atlas →</a>`
    — apply `urlencode` to BOTH halves (Pitfall 5). For sample-host-only collectors
    `recordedBy` is null → the encoded value is empty, producing `?collectors=:login`,
    which parseParams accepts (recordedBy = decoded || null). Create `_pages/collectors.njk`
    from `places.njk`: permalink `/collectors.html`, title `Collectors — BeeAtlas`, loop
    `{% for collector in collectors.collectorsArray %}`, each `<li>` linking
    `/collectors/{{ collector.login | urlencode }}/` with text `{{ collector.display_name }}`
    and a headline count (e.g. `{{ collector.specimen_count | quantify("specimen") }}`);
    drop the `span.owner` (no land_owner analog) — planner's discretion on which count to show.
  </action>
  <verify>
    <automated>npm run build 2>&1 | tail -8 && test -f _site/collectors.html && ls _site/collectors/ | head -3 && grep -rl "collectors=" _site/collectors/ | head -1 && echo BUILD-DEEPLINK-OK</automated>
  </verify>
  <acceptance_criteria>
    - _pages/collector-detail.njk has pagination `data: collectors.collectorsArray`, `size: 1`, `alias: collector`
    - _pages/collector-detail.njk permalink is `/collectors/{{ collector.login | urlencode }}/index.html` (trailing-slash, not `.html`)
    - _pages/collector-detail.njk contains an `<h1>{{ collector.display_name }}` and `quantify` calls for specimen/sample/species
    - _pages/collector-detail.njk contains a `/?collectors={{ collector.recordedBy | urlencode }}:{{ collector.host_inat_login | urlencode }}` deep-link
    - _pages/collectors.njk permalink is `/collectors.html` and loops `collectors.collectorsArray`
    - `npm run build` succeeds, produces `_site/collectors.html` and `_site/collectors/<login>/index.html`, and a built collector page contains a `collectors=` deep-link
  </acceptance_criteria>
  <done>The detail and index pages build to /collectors/{login}/ and /collectors.html, render the display name + stats + status split, and deep-link to the map via ?collectors=.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| collectors.json → Eleventy templates | The JSON is a committed build artifact derived from already-public iNat data; rendered into static HTML |
| static page → browser map (?collectors=) | The deep-link URL param is consumed by the existing, already-shipped url-state.ts collector filter (no new surface) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-169-03 | Information Disclosure | /collectors/{login}/ static pages, /collectors.html roster | accept | Pages surface only already-public iNat data; operator decided (REQUIREMENTS "Out of Scope") pages are public with no gating, noindex, or coordinate suppression. Residual SEO/scraping risk acknowledged and accepted. |
| T-169-04 | Tampering / Injection | deep-link URL param built in Nunjucks from recordedBy / host_inat_login | mitigate | Apply the `urlencode` filter to BOTH halves of the `?collectors=` value (Pitfall 5); url-state.ts parseParams decodes via decodeURIComponent. No raw string interpolation into a script or attribute beyond the encoded href. |
| T-169-SC | Tampering | npm/pip/cargo installs | accept | This plan installs NO new packages (RESEARCH §Package Legitimacy Audit: not applicable). No supply-chain surface added. |

No new high-severity surface: static HTML over already-public data, reusing the existing shipped collector filter; no new auth, endpoint, or FilterState dimension.
</threat_model>

<verification>
- `npm test -- src/tests/data-collectors.test.ts` passes (array, floor >=100, field types, split invariant, no-parquet).
- `npm run build` succeeds and produces `_site/collectors.html` and `_site/collectors/<login>/index.html`.
- A built collector page contains a `?collectors=` deep-link (PAGE-04).
- Manual UAT (per VALIDATION.md): one collector page renders name + stats + status split copy; "View on the atlas" applies the collector filter on the live map.
</verification>

<success_criteria>
- Bookmarkable `/collectors/{login}/` pages exist for every gated collector (PAGE-01).
- Headline stats (specimens, samples, species) render via the quantify filter (PAGE-02).
- The pending-vs-identified split renders (PAGE-03).
- The map deep-link uses the existing `?collectors=` param with no new FilterState field (PAGE-04, D-10).
- The `/collectors.html` index roster lists every generated collector (D-08).
- The D-09 floor + shape Vitest is green in `npm test`.
</success_criteria>

<output>
Create `.planning/phases/169-per-collector-static-pages/169-02-SUMMARY.md` when done
</output>
