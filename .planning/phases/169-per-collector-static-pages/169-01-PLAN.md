---
phase: 169-per-collector-static-pages
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - data/collectors_export.py
  - data/tests/test_collectors_export.py
  - data/run.py
  - public/data/collectors.json
  - .gitignore
autonomous: true
requirements: [PAGE-01, PAGE-02, PAGE-03]
must_haves:
  truths:
    - "Running the collectors-export step produces public/data/collectors.json (D-02: derived SQL gate in the export, not a curated seed)"
    - "collectors.json is a JSON array of ~124 gated collectors, >= 100 (D-01: gate = collector_inat_login IS NOT NULL AND (ecdysis_id IS NOT NULL OR source IN ('waba_specimen','waba_sample')))"
    - "Each record carries login, display_name, recordedBy, host_inat_login, specimen_count, sample_count, species_count, status_denominator, status_identified, status_awaiting (D-03: specimen/sample/species counts; D-04: display_name = human name with @login fallback)"
    - "For every record status_identified + status_awaiting == status_denominator (D-05: denominator = the collector's specimens; D-06: identified = species-rank determination via species.parquet specific_epithet IS NOT NULL; D-07: NOT keyed on id_date)"
    - "public/data/collectors.json is git-tracked so a clean checkout has it"
  artifacts:
    - path: "data/collectors_export.py"
      provides: "Per-collector stats export step (export_collectors / export_collectors_step)"
      contains: "def export_collectors_step("
    - path: "data/tests/test_collectors_export.py"
      provides: "Golden-fixture pytest for the gate + sample-count formula + split invariant"
      contains: "def test_"
    - path: "public/data/collectors.json"
      provides: "Committed export artifact consumed by Eleventy + Vitest"
      min_lines: 100
    - path: "data/run.py"
      provides: "STEPS registration of collectors-export after places-export"
      contains: "export_collectors_step"
  key_links:
    - from: "data/run.py"
      to: "data/collectors_export.py"
      via: "from collectors_export import export_collectors_step + STEPS entry"
      pattern: "collectors_export import export_collectors_step"
    - from: "data/collectors_export.py"
      to: "public/data/occurrences.parquet + public/data/species.parquet"
      via: "read_parquet over ASSETS_DIR (EXPORT_DIR)"
      pattern: "read_parquet"
---

<objective>
Build the data foundation for per-collector pages: a `collectors_export.py` step
that aggregates per-collector stats directly off `occurrences.parquet` (LEFT JOIN
`species.parquet`), registers it in `run.py` after `places-export`, and produces a
committed `public/data/collectors.json` of the ~124 gated WABA collectors (per D-01).

Purpose: PAGE-01/02/03 all read from this JSON. The Eleventy pages (Plan 02) and
the Vitest floor test cannot exist without it, and it must be committed so
`npm test` / `npm run build` pass on a clean checkout (same convention as
`places.json`).

Output: `data/collectors_export.py`, `data/tests/test_collectors_export.py`,
the `data/run.py` STEPS entry, and the committed `public/data/collectors.json`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/169-per-collector-static-pages/169-CONTEXT.md
@.planning/phases/169-per-collector-static-pages/169-RESEARCH.md
@.planning/phases/169-per-collector-static-pages/169-PATTERNS.md
@.planning/phases/169-per-collector-static-pages/169-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Golden-fixture pytest for collectors_export (Wave 0 test-first)</name>
  <files>data/tests/test_collectors_export.py</files>
  <read_first>
    - data/tests/test_places_export.py (the analog: fixture-parquet construction with pyarrow at lines 75-136, `_setup_env` monkeypatch of DB_PATH/EXPORT_DIR + importlib.reload at lines 139-152, count-assertion test at lines 199+)
    - data/tests/conftest.py (shared pytest config)
    - .planning/phases/169-per-collector-static-pages/169-RESEARCH.md §"Key Findings" #2/#3/#4 (sample-count formula, species-rank source, status split denominators)
    - .planning/phases/169-per-collector-static-pages/169-VALIDATION.md (Wave 0 Requirements bullet 3: assert gate predicate + sample-count formula)
  </read_first>
  <action>
    Create `data/tests/test_collectors_export.py` mirroring `test_places_export.py`'s
    fixture approach. Write two small in-memory parquets with pyarrow: an
    `occurrences.parquet` fixture carrying the columns the query reads
    (collector_inat_login, recordedBy, host_inat_login, ecdysis_id, source, sample_id,
    observation_id, taxon_id) and a `species.parquet` fixture carrying
    (taxon_id, specific_epithet). Seed at least: one ecdysis-backed collector whose
    specimens split across identified (specific_epithet set) and awaiting (taxon_id absent
    from species or specific_epithet NULL), and one sample-host-only collector whose rows
    are source='waba_sample' with sample_id NULL but observation_id set, and one pure
    inat_obs-only login (must NOT survive the gate). Use a `_setup_env`-style monkeypatch
    of DB_PATH + EXPORT_DIR and importlib.reload of the `collectors_export` module so the
    export reads the fixtures from EXPORT_DIR. Assert: (a) collectors.json is a JSON list;
    (b) the D-01 gate is applied — the inat_obs-only login is absent; (c) the sample-host-only
    collector has a NON-ZERO sample_count (proving the
    `COUNT(DISTINCT sample_id) + COUNT(DISTINCT CASE WHEN source='waba_sample' THEN
    observation_id END)` formula — research finding #3); (d) every record satisfies
    status_identified + status_awaiting == status_denominator; (e) each record has all
    required keys (login, display_name, recordedBy, host_inat_login, specimen_count,
    sample_count, species_count, status_denominator, status_identified, status_awaiting).
    This test is RED until Task 2 lands the module.
  </action>
  <verify>
    <automated>cd data && uv run pytest tests/test_collectors_export.py -x 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - data/tests/test_collectors_export.py exists and contains at least 4 `def test_` functions
    - The file references the gate tokens `waba_sample` and `specific_epithet`
    - The file asserts a sample-host-only collector has a non-zero sample_count
    - The file asserts the `status_identified + status_awaiting == status_denominator` invariant
    - Before Task 2: `cd data && uv run pytest tests/test_collectors_export.py` exits non-zero (RED — module not yet present)
  </acceptance_criteria>
  <done>The pytest file exists, encodes the gate + sample-count + split-invariant assertions, and fails (RED) because collectors_export.py does not yet exist.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: collectors_export.py export step + run.py STEPS registration</name>
  <files>data/collectors_export.py, data/run.py</files>
  <read_first>
    - data/places_export.py (the exact template: module constants DB_PATH/ASSETS_DIR at lines 21-23, file-existence guard at lines 58-65, DuckDB execute pattern at lines 66-92, JSON write at lines 139-140, `export_places` public fn at lines 147-172, `export_places_step` zero-arg wrapper at lines 175-182, `__main__` at lines 185-186)
    - data/run.py (import block line 47 `from places_export import export_places_step`; STEPS list line 125 `("places-export", export_places_step),`; pipeline-order comment lines 12-13)
    - .planning/phases/169-per-collector-static-pages/169-RESEARCH.md §"Pattern 1" (full DuckDB query column list at lines 152-185) and §"Pattern 4" (run.py registration)
    - .planning/phases/169-per-collector-static-pages/169-PATTERNS.md §"data/collectors_export.py" (line-anchored deltas: drop tomllib, drop LOAD spatial, no bridge JOIN) and §"STEPS entry in data/run.py"
    - .planning/phases/169-per-collector-static-pages/169-CONTEXT.md §decisions (D-01 gate, D-03 counts, D-04 display name, D-05/D-06/D-07 status split)
  </read_first>
  <action>
    Clone `places_export.py` as `data/collectors_export.py`. Drop `import tomllib`,
    the `_load_place_metadata` helper, the geojson writer, and every `con.execute("LOAD spatial")`
    call (no geometry). Keep the DB_PATH / ASSETS_DIR (EXPORT_DIR) constants verbatim.
    Implement a single DuckDB aggregation over
    `read_parquet(ASSETS_DIR/'occurrences.parquet') o LEFT JOIN
    read_parquet(ASSETS_DIR/'species.parquet') sp ON sp.taxon_id = o.taxon_id`
    with WHERE `o.collector_inat_login IS NOT NULL AND (o.ecdysis_id IS NOT NULL OR
    o.source IN ('waba_specimen','waba_sample'))` (per D-01; the waba_specimen arm adds 0
    collectors today but future-proofs the gate) GROUP BY `o.collector_inat_login`.
    Select per D-03/D-04/D-05/D-06: login = collector_inat_login; display_name =
    MIN(COALESCE(o.recordedBy, '@'||o.collector_inat_login)); recordedBy = MIN(o.recordedBy)
    (null for sample-host-only collectors); host_inat_login = MIN(o.host_inat_login);
    specimen_count = COUNT(DISTINCT CASE WHEN o.ecdysis_id IS NOT NULL THEN o.ecdysis_id END);
    sample_count = COUNT(DISTINCT o.sample_id) + COUNT(DISTINCT CASE WHEN o.source='waba_sample'
    THEN o.observation_id END); species_count = COUNT(DISTINCT CASE WHEN sp.specific_epithet
    IS NOT NULL THEN o.taxon_id END); status_denominator = SUM(CASE WHEN (o.ecdysis_id IS NOT NULL
    OR o.source='waba_specimen') THEN 1 ELSE 0 END); status_identified = SUM(CASE WHEN
    (o.ecdysis_id IS NOT NULL OR o.source='waba_specimen') AND sp.specific_epithet IS NOT NULL
    THEN 1 ELSE 0 END); status_awaiting = SUM(CASE WHEN (o.ecdysis_id IS NOT NULL OR
    o.source='waba_specimen') AND sp.specific_epithet IS NULL THEN 1 ELSE 0 END). Do NOT use
    id_date in this split (D-07 — taxon rank is the sole gate). Guard both parquet paths with
    FileNotFoundError like the analog. Build a records list of dicts (cast counts to int,
    recordedBy left as str-or-None) and write to ASSETS_DIR/'collectors.json' with
    `json.dumps(records, indent=2)`. Expose `export_collectors(con=None)` and
    `export_collectors_step()` (zero-arg wrapper, no LOAD spatial) plus the
    `if __name__ == "__main__": export_collectors_step()` block. In `data/run.py`, add
    `from collectors_export import export_collectors_step` after the places_export import
    (line 47), add `("collectors-export", export_collectors_step),` to STEPS immediately after
    the `places-export` entry (line 125), and append `collectors-export` to the
    pipeline-order comment (lines 12-13).
  </action>
  <verify>
    <automated>cd data && uv run pytest tests/test_collectors_export.py -x 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - data/collectors_export.py contains `def export_collectors_step(` and `def export_collectors(`
    - data/collectors_export.py contains the gate tokens `waba_sample` and `specific_epithet IS NOT NULL`
    - `grep -ci 'load spatial' data/collectors_export.py` is 0 (no LOAD spatial) and the file has no `import tomllib`
    - data/run.py contains `from collectors_export import export_collectors_step`
    - data/run.py contains `("collectors-export", export_collectors_step)` positioned after `("places-export", export_places_step)`
    - `cd data && uv run pytest tests/test_collectors_export.py` exits 0 (GREEN)
  </acceptance_criteria>
  <done>collectors_export.py produces a correct collectors.json from fixtures; the Task 1 pytest passes; run.py registers the step after places-export.</done>
</task>

<task type="auto">
  <name>Task 3: Run the export against the dbt-built mart, commit collectors.json + gitignore exception</name>
  <files>public/data/collectors.json, .gitignore</files>
  <read_first>
    - .gitignore (lines 140-144: `/public/data/*` blanket ignore at line 141, with `!/public/data/places.geojson` and `!/public/data/places.json` un-ignore exceptions at lines 143-144 — collectors.json IS currently ignored and needs its own `!` exception)
    - data/run.py (`_run_dbt_build` lines 63-87 copies occurrences.parquet + species into EXPORT_DIR; the dbt sandbox path `_DBT_SANDBOX` line 56)
    - .planning/phases/169-per-collector-static-pages/169-RESEARCH.md §"Common Pitfalls" Pitfall 1 (read from EXPORT_DIR, not sandbox) and Pitfall 4 (collectors.json must be committed)
    - CLAUDE.md §"Known State" (committed public/data/occurrences.parquet is STALE — regenerate the mart before export; memory project_local_uat_stale_occurrences_db)
  </read_first>
  <action>
    Generate the real `public/data/collectors.json`. The committed
    `public/data/occurrences.parquet` is stale (lacks collector_inat_login until Phase 167's
    S3 landing), so the export must read a FRESH mart that carries collector_inat_login.
    Build it and copy into EXPORT_DIR: run `bash data/dbt/run.sh build` (writes the sandbox
    parquets), copy `data/dbt/target/sandbox/occurrences.parquet` into `public/data/`, ensure
    `public/data/species.parquet` is present (run `cd data && uv run python species_export.py`
    if missing), then run `cd data && uv run python collectors_export.py` to write
    `public/data/collectors.json`. (Equivalently `cd data && uv run python run.py` runs
    dbt-build + species-export + the new collectors-export step in order — use whichever
    completes; the Ecdysis auth gate may block a full run.py, so the targeted commands above
    are the fallback.) Confirm collectors.json is a JSON array of ~124 records (>= 100). To
    survive a clean checkout, add a `!/public/data/collectors.json` un-ignore line to
    `.gitignore` immediately after the existing `!/public/data/places.json` exception
    (line 144), then `git add -- .gitignore public/data/collectors.json`. Do NOT force-add
    (the negation rule un-ignores it cleanly; per feedback_no_force_add_gitignored). Do NOT
    commit the regenerated occurrences.parquet (it stays gitignored / stale-by-design until
    the S3 landing).
  </action>
  <verify>
    <automated>test -f public/data/collectors.json && node -e "const a=require('./public/data/collectors.json'); if(!Array.isArray(a)) throw new Error('not array'); if(a.length<100) throw new Error('floor '+a.length); const r=a[0]; for(const k of ['login','display_name','host_inat_login','specimen_count','sample_count','species_count','status_denominator','status_identified','status_awaiting']) if(!(k in r)) throw new Error('missing '+k); for(const c of a) if(c.status_identified+c.status_awaiting!==c.status_denominator) throw new Error('invariant '+c.login); console.log('collectors.json OK n='+a.length)" && ! git check-ignore -q public/data/collectors.json && echo "TRACKED-OK"</automated>
  </verify>
  <acceptance_criteria>
    - public/data/collectors.json is a JSON array with length >= 100
    - Each record has login, display_name, recordedBy, host_inat_login, specimen_count, sample_count, species_count, status_denominator, status_identified, status_awaiting
    - For every record status_identified + status_awaiting == status_denominator
    - `.gitignore` contains the line `!/public/data/collectors.json`
    - `git check-ignore public/data/collectors.json` exits non-zero (file is tracked, not ignored)
    - No `git add -f` was used (no force-add of a gitignored artifact)
  </acceptance_criteria>
  <done>public/data/collectors.json exists, is committed via a `.gitignore` negation rule, holds ~124 valid records satisfying the split invariant, and survives a clean checkout.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| dbt mart → export step | `occurrences.parquet` / `species.parquet` are pipeline-internal, already-validated artifacts (not external user input) |
| export step → committed JSON | `collectors.json` is a build artifact derived from already-public iNat data |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-169-01 | Information Disclosure | collectors.json (names, logins, counts) | accept | All values derive from already-public iNaturalist data and Ecdysis `recordedBy`; per REQUIREMENTS "Out of Scope" the operator decided pages are public with no gating. No PII beyond what is already public. |
| T-169-02 | Tampering | data/collectors_export.py SQL | mitigate | Query reads pipeline-internal parquets only; no user input crosses into the SQL (params are fixed file paths). No injection surface. |
| T-169-SC | Tampering | npm/pip/cargo installs | accept | This plan installs NO new packages (RESEARCH §Package Legitimacy Audit: not applicable). No supply-chain surface added. |

No new high-severity surface: this plan adds a derived JSON over already-public data and a deterministic DuckDB aggregation with no user input.
</threat_model>

<verification>
- `cd data && uv run pytest tests/test_collectors_export.py` passes (gate, sample-count formula, split invariant).
- `public/data/collectors.json` is a committed JSON array of >= 100 records with all required fields and the split invariant.
- `git check-ignore public/data/collectors.json` exits non-zero (tracked).
- `data/run.py` STEPS lists `collectors-export` after `places-export`.
</verification>

<success_criteria>
- collectors_export.py produces a correct per-collector JSON from the dbt-built mart with the D-01 gate, D-03 counts, D-04 display name, and D-05/D-06/D-07 status split.
- The golden-fixture pytest is green.
- collectors.json is committed and survives a clean checkout (npm test / build can read it in Plan 02).
- The export step is wired into run.py for the nightly pipeline.
</success_criteria>

<output>
Create `.planning/phases/169-per-collector-static-pages/169-01-SUMMARY.md` when done
</output>
