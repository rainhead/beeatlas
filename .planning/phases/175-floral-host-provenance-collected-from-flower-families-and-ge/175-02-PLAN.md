---
phase: 175-floral-host-provenance
plan: 02
type: execute
wave: 2
depends_on:
  - "175-01"
files_modified:
  - _data/species_hosts.js
  - src/tests/data-species_hosts.test.ts
  - _pages/species-detail.njk
  - src/styles/taxon-pages.css
  - data/nightly.sh
  - .github/workflows/deploy.yml
autonomous: true
requirements:
  - "Floral-host Collected from block on species pages"
  - "no dbt-contract change"
must_haves:
  truths:
    - "On a covered species page (e.g. Bombus mixtus) a Collected from block lists flower families with nested genera"
    - "Families are ordered by sample count; a +N more families cap appears when families exceed the display cap"
    - "Species with no host data omit the Collected from block entirely"
    - "The site builds on a clean checkout with no species_hosts.json present (the loader tolerates absence)"
  artifacts:
    - path: "_data/species_hosts.js"
      provides: "Eleventy build-time loader, default-export Record keyed by canonical_name, absence-tolerant"
      contains: "export default"
    - path: "_pages/species-detail.njk"
      provides: "Collected from block fed by species_hosts[sp.canonical_name]"
      contains: "Collected from"
    - path: "data/nightly.sh"
      provides: "hashed upload + manifest key + baseline map for species_hosts.json"
      contains: "species_hosts"
    - path: ".github/workflows/deploy.yml"
      provides: "build-time fetch of species_hosts.json from S3"
      contains: "species_hosts"
  key_links:
    - from: "public/data/species_hosts.json"
      to: "_data/species_hosts.js"
      via: "build-time readFileSync"
    - from: "species_hosts[sp.canonical_name]"
      to: "_pages/species-detail.njk Collected from block"
      via: "Nunjucks lookup"
    - from: "nightly _upload_hashed species_hosts"
      to: "deploy.yml jq .species_hosts fetch"
      via: "manifest.json species_hosts key"
---

<objective>
Surface the floral-host data built in 175-01 on the species detail page, and wire the build-time release path.
Add an absence-tolerant Eleventy _data loader for species_hosts.json, render a Collected from block (families
with nested genera, sample-count ordered, +N more families cap) near the Traits fact-sheet, and add the
3-touch hashed-upload + manifest + deploy-fetch wiring so the sidecar reaches the build. Because the loader
tolerates absence, the CODE deploy stays green even before the first DATA run publishes species_hosts.json.

Output: _data/species_hosts.js + its test; the Collected from block + CSS in species-detail.njk; nightly.sh
publish + deploy.yml fetch; a clean-checkout CI verification.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/175-floral-host-provenance-collected-from-flower-families-and-ge/175-CONTEXT.md
@.planning/phases/175-floral-host-provenance-collected-from-flower-families-and-ge/175-PATTERNS.md
@.planning/phases/175-floral-host-provenance-collected-from-flower-families-and-ge/175-01-PLAN.md
@CLAUDE.md
</context>

<artifacts_produced>
## Artifacts this phase produces (Plan 175-02)
- _data/species_hosts.js: default-export Record keyed by canonical_name; returns {} when public/data/species_hosts.json is absent
- src/tests/data-species_hosts.test.ts: loader contract + absence-tolerance test
- _pages/species-detail.njk: a section.collected-from block (heading "Collected from"), families with nested genera, sample-count ordered, "+N more families" cap
- src/styles/taxon-pages.css: .collected-from styles (reusing the v7.0 traits visual language)
- data/nightly.sh: species_hosts _upload_hashed + manifest "species_hosts" key + LOCAL_NAMES baseline entry
- .github/workflows/deploy.yml: SPECIES_HOSTS_FILE jq fetch to public/data/species_hosts.json

CONSUMES (from 175-01): public/data/species_hosts.json with the LOCKED shape (canonical_name -> ordered array
of {family, sample_count, genera:[{genus, sample_count}]}).
</artifacts_produced>

<tasks>

<task type="auto">
  <name>Task 1: Absence-tolerant _data/species_hosts.js loader + test</name>
  <files>_data/species_hosts.js, src/tests/data-species_hosts.test.ts</files>
  <read_first>
    - _data/photos.js (default-export-only contract; the no-named-exports Pitfall comment lines 11-15,45)
    - _data/species.js (public/data JSON read via readFileSync + fileURLToPath, lines 12-22)
    - src/tests/data-photos.test.ts (Vitest import-the-loader assertion pattern)
    - 175-PATTERNS.md section _data/species_hosts.js (lines 177-198)
  </read_first>
  <action>
    Create _data/species_hosts.js mirroring _data/photos.js and the species.js public/data read. Resolve
    public/data/species_hosts.json relative to import.meta.url (fileURLToPath + dirname + join, like species.js).
    Guard the read: if the file does not exist (existsSync) or JSON.parse fails, return an empty object — local
    dev and the first code deploy run before any species_hosts.json exists, and npm run dev / npm test must not
    crash. Export the parsed mapping DIRECTLY as the default export (export default result) with NO named exports
    (a named export would hide the table behind species_hosts.default per the photos.js pitfall). The value is the
    175-01 LOCKED shape, keyed by canonical_name, so the detail page looks up species_hosts[sp.canonical_name].
    Create src/tests/data-species_hosts.test.ts modeled on data-photos.test.ts: import the default export; assert
    it is an object; for any present entry assert it is an array whose items have family (string), sample_count
    (number), and genera (array); assert the module imports without throwing even when the JSON is absent
    (absence tolerance).
  </action>
  <verify>
    <automated>npx vitest run src/tests/data-species_hosts.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - _data/species_hosts.js contains export default and has NO named exports.
    - The loader returns {} (no throw) when public/data/species_hosts.json is absent.
    - src/tests/data-species_hosts.test.ts passes under vitest, asserting the keyed-array shape and absence tolerance.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Collected from block in species-detail.njk + CSS + render UAT</name>
  <files>_pages/species-detail.njk, src/styles/taxon-pages.css</files>
  <read_first>
    - _pages/species-detail.njk (the section.traits block lines 28-59 + checklist-attribution lines 21-27; photos[sp.scientificName] lookup line 16; pagination alias sp lines 3-5)
    - src/styles/taxon-pages.css (.traits / .traits-heading / .traits-dl classes lines 314-364)
    - eleventy.config.js (the quantify filter registration line 33)
    - 175-PATTERNS.md section _pages/species-detail.njk (lines 201-232)
  </read_first>
  <action>
    In _pages/species-detail.njk add a Collected from block inside .hero-meta, immediately after the
    section.traits block. Set a local from the global lookup species_hosts[sp.canonical_name] (the species_hosts
    global comes from the Task 1 _data loader; sp is the paginated species row). Guard the whole block with an
    if that checks the value exists and has length, so the 534 covered species render it and the rest omit it
    entirely (same pattern as hasHostBees line 32). Render a section.collected-from with an h2 heading
    "Collected from". Iterate families sliced to a FAMILY_CAP (choose 6 — Claude's discretion per CONTEXT line
    82); for each family render the family name, a middot separator, then the family genera names sliced to a
    GENUS_CAP (choose 8) joined by comma-space. When the family count exceeds FAMILY_CAP, render a final muted
    paragraph "(+N more families)" where N is families length minus FAMILY_CAP. NUNJUCKS CAVEAT (plan-check
    warning): do NOT use the `slice`/`map` filters from the PATTERNS illustration — Nunjucks `slice` partitions
    into N groups (not a Python range slice) and there is no built-in `map` filter. Implement caps with a `for`
    loop guarded by `loop.index0 < FAMILY_CAP` / `loop.index0 < GENUS_CAP` (and compute the "+N more" count via
    `families.length`). The mandated render-UAT will catch a non-rendering block. The target layout is the
    user-approved preview in CONTEXT lines 68-75. Do NOT use the dump/safe filter on any host name (Nunjucks
    autoescape must apply — host names are third-party iNat taxonomy). Reuse the traits visual language: add a
    .collected-from rule set to src/styles/taxon-pages.css consistent with .traits / .traits-heading (do not
    invent a new unrelated UI pattern per feedback_no_unrequested_ui_patterns).
    Render UAT: write a fixture public/data/species_hosts.json containing one entry for an existing covered
    species (Bombus mixtus -> e.g. Asteraceae with Solidago/Grindelia, Rosaceae with Rosa/Rubus, plus enough
    families to trigger the +N cap), run npm run build, confirm _site/species/Bombus/mixtus/index.html contains
    "Collected from" and the family names, then restore public/data/species_hosts.json (git checkout or remove
    the fixture).
  </action>
  <verify>
    <automated>cd /Users/rainhead/dev/beeatlas && grep -q "Collected from" _pages/species-detail.njk && grep -q "collected-from" src/styles/taxon-pages.css
    </automated>
  </verify>
  <acceptance_criteria>
    - _pages/species-detail.njk contains a section.collected-from with the heading "Collected from", gated so species without host data omit it.
    - The block iterates families (capped) with nested genera (capped) and renders "(+N more families)" past the cap; no host name uses the dump/safe filter.
    - src/styles/taxon-pages.css contains a .collected-from rule set consistent with .traits.
    - Render UAT: after injecting a fixture species_hosts.json and running npm run build, _site/species/Bombus/mixtus/index.html contains "Collected from"; the fixture is then restored.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Release wiring (nightly.sh + deploy.yml) + clean-checkout CI verification</name>
  <files>data/nightly.sh, .github/workflows/deploy.yml</files>
  <read_first>
    - data/nightly.sh (the _upload_hashed publish block lines 305-346; the LOCAL_NAMES baseline map lines 153-162)
    - .github/workflows/deploy.yml (the Fetch build-time data step lines 34-57; the build-time files comment lines 35-41)
    - 175-PATTERNS.md section deploy.yml + nightly.sh (lines 236-258)
    - memories feedback_ci_is_verification_surface, feedback_no_committed_data_artifacts
  </read_first>
  <action>
    nightly.sh: after the photos_name _upload_hashed line, add species_hosts_name set from _upload_hashed of
    EXPORT_DIR/species_hosts.json with basename species_hosts; add a species_hosts key referencing
    $species_hosts_name to the manifest.json heredoc; and add a species_hosts -> species_hosts.json entry to the
    LOCAL_NAMES baseline map (so the nightly integration diff gate covers it and the drift WARN does not fire).
    Do NOT commit the generated species_hosts.json (feedback_no_committed_data_artifacts).
    deploy.yml: in the Fetch build-time data step, add SPECIES_HOSTS_FILE from jq .species_hosts of
    /tmp/manifest.json and an aws s3 cp of that hashed object to public/data/species_hosts.json, parallel to the
    higher_taxa fetch. Update the step comment enumerating the build-time files to include species_hosts.json
    (now read by _data/species_hosts.js).
    Clean-checkout CI verification (feedback_ci_is_verification_surface): prove the build is green on a fresh
    checkout BEFORE the first nightly publishes species_hosts.json. With public/data/species_hosts.json absent,
    npm run build must succeed because the Task 1 loader returns an empty object for the absent file
    (data-before-code safety).
  </action>
  <verify>
    <automated>rm -f public/data/species_hosts.json && npm run build && test -d _site</automated>
  </verify>
  <acceptance_criteria>
    - data/nightly.sh contains species_hosts in the _upload_hashed publish, the manifest heredoc, and the LOCAL_NAMES baseline map.
    - .github/workflows/deploy.yml contains a jq .species_hosts fetch writing public/data/species_hosts.json, and the build-time comment lists species_hosts.json.
    - npm run build succeeds with public/data/species_hosts.json ABSENT (clean-checkout gate) and produces _site.
    - No file under data/dbt/models/marts/ is modified (no contract change).
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| species_hosts.json -> Nunjucks render | Third-party iNat family/genus names become page HTML |
| S3 (OIDC) -> build working tree | Build-time data fetch from the project's own bucket |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation |
|-----------|----------|-----------|-------------|------------|
| T-175-04 | Tampering (XSS) | Collected from block render | mitigate | Nunjucks autoescape on all host names; no dump/safe filter applied |
| T-175-05 | Denial of service | first deploy before first data run | mitigate | Loader returns empty object when species_hosts.json absent; clean-checkout build gate |
| T-175-06 | Spoofing | S3 fetch in deploy.yml | accept | Own bucket via GitHub OIDC role; same trust as the existing higher_taxa fetch |
</threat_model>

<verification>
- Loader test passes: npx vitest run src/tests/data-species_hosts.test.ts
- Render UAT: injected fixture species_hosts.json then npm run build then _site/species/Bombus/mixtus/index.html contains "Collected from" then fixture restored.
- Clean-checkout gate: npm run build succeeds with public/data/species_hosts.json absent.
- No data/dbt/models/marts/ file changed.
</verification>

<success_criteria>
- _data/species_hosts.js loads the sidecar, default-export-only, absence-tolerant; its test passes.
- The Collected from block renders for covered species and is omitted otherwise; CSS matches the traits language.
- nightly.sh publishes species_hosts.json (hashed + manifest + baseline) and deploy.yml fetches it; the build is green on a clean checkout.
</success_criteria>

<output>
Create .planning/phases/175-floral-host-provenance-collected-from-flower-families-and-ge/175-02-SUMMARY.md when done.
</output>
