---
phase: 120-species-page-source-counts-photo-list
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - _pages/species-detail.njk
  - _pages/genus.njk
  - _pages/subgenus.njk
  - _pages/tribe.njk
  - _data/species.js
autonomous: true
requirements: [SPE-01, SPE-02]

must_haves:
  truths:
    - "Species-detail pages show 'N specimens · N community observations' where N is specimen_count and inat_obs_count respectively"
    - "Genus and subgenus species lists show the source-aware breakdown for species with occurrence_count > 0; checklist-only and zero branches are unchanged"
    - "Tribe pages show 'N specimens · N community observations' per genus entry"
    - "The atlas link reads 'View N records on the atlas →' where N = occurrence_count + inat_obs_count"
    - "The tribe tribeList genus objects expose specimen_count and inat_obs_count (not just occurrence_count)"
  artifacts:
    - path: "_pages/species-detail.njk"
      provides: "Updated metadata line and atlas link"
      contains: "specimen_count }} specimens · {{ sp.inat_obs_count }} community observations"
    - path: "_pages/genus.njk"
      provides: "Updated per-species count span (occurrence branch)"
      contains: "specimen_count }} specimens · {{ sp.inat_obs_count }} community observations"
    - path: "_pages/subgenus.njk"
      provides: "Updated per-species count span (occurrence branch)"
      contains: "specimen_count }} specimens · {{ sp.inat_obs_count }} community observations"
    - path: "_pages/tribe.njk"
      provides: "Updated per-genus count span"
      contains: "g.specimen_count }} specimens · {{ g.inat_obs_count }} community observations"
    - path: "_data/species.js"
      provides: "tribeMap accumulator extended to object with specimen_count/inat_obs_count"
      contains: "inat_obs_count: 0"
  key_links:
    - from: "_data/species.js"
      to: "_pages/tribe.njk"
      via: "tribeList genus objects via Eleventy data cascade"
      pattern: "g\\.specimen_count"
    - from: "public/data/species.json"
      to: "_pages/species-detail.njk"
      via: "Eleventy _data/species.js reads species.json at build time"
      pattern: "specimen_count"
---

<objective>
Update species-detail, genus, subgenus, and tribe Nunjucks templates to display source-aware occurrence breakdowns ("N specimens · N community observations") in place of the single "N records" label. Extend the _data/species.js tribeMap accumulator so genus objects in tribeList carry specimen_count and inat_obs_count alongside the existing occurrence_count.

Purpose: SPE-01 and SPE-02 — give volunteers and contributors a clear picture of how WABA/Ecdysis specimen records compare to expert iNat community observations.

Output: Five modified files; no new files, no pipeline changes, no new CSS or JS.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/120-species-page-source-counts-photo-list/120-CONTEXT.md
@.planning/phases/120-species-page-source-counts-photo-list/120-PATTERNS.md
@.planning/phases/120-species-page-source-counts-photo-list/120-UI-SPEC.md

<interfaces>
<!-- Key data already available in species.json (confirmed present, no pipeline changes needed) -->
From public/data/species.json (each species entry):
  specimen_count: number    -- Ecdysis specimen rows for this taxon
  inat_obs_count: number    -- expert iNat obs rows for this taxon
  occurrence_count: number  -- WABA + Ecdysis combined (used for atlas link guard + genus/subgenus branch condition)
  checklist_count: number   -- Bartholomew checklist records
  on_checklist: boolean     -- species is on the checklist
  county_count: number      -- distinct counties
  ecoregion_count: number   -- distinct ecoregions
  scientificName: string    -- for atlas link urlencode
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Update species-detail.njk count label and atlas link</name>
  <files>_pages/species-detail.njk</files>
  <read_first>
    - _pages/species-detail.njk (read current lines 39–50 — metadata p, checklist-attribution p, atlas link)
    - .planning/phases/120-species-page-source-counts-photo-list/120-PATTERNS.md §species-detail.njk section — exact before/after for line 41 and lines 45-47
    - .planning/phases/120-species-page-source-counts-photo-list/120-UI-SPEC.md §Copywriting Contract
  </read_first>
  <action>
    Apply two targeted changes to _pages/species-detail.njk (per D-01, D-11, D-12):

    Change 1 — metadata line (line 41):
    Replace the leading segment `{{ sp.occurrence_count }} records` with `{{ sp.specimen_count }} specimens · {{ sp.inat_obs_count }} community observations`. The rest of the line (`· {{ sp.county_count }} counties · {{ sp.ecoregion_count }} ecoregions`) is unchanged. The checklist-attribution block (lines 42-44) is not touched.

    Change 2 — atlas link (line 46):
    Replace link text `View {{ sp.occurrence_count }} occurrences on the atlas →` with `View {{ sp.occurrence_count + sp.inat_obs_count }} records on the atlas →`. The guard condition `{%- if sp.occurrence_count > 0 -%}` on line 45 and the `href` attribute stay unchanged. Nunjucks supports inline arithmetic natively — no helper or filter needed.
  </action>
  <verify>
    <automated>grep -c "specimen_count }} specimens" /Users/rainhead/dev/beeatlas/_pages/species-detail.njk</automated>
  </verify>
  <acceptance_criteria>
    - _pages/species-detail.njk line ~41 contains: `{{ sp.specimen_count }} specimens · {{ sp.inat_obs_count }} community observations`
    - _pages/species-detail.njk line ~46 contains: `View {{ sp.occurrence_count + sp.inat_obs_count }} records on the atlas →`
    - The word "occurrences" no longer appears in the atlas link text
    - The checklist-attribution block (Bartholomew et al. 2024) is unmodified
    - `npm run build` exits 0 (Eleventy produces pages without template errors)
  </acceptance_criteria>
  <done>species-detail.njk shows split counts and updated atlas link; build passes</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Update genus.njk, subgenus.njk, tribe.njk count spans and extend _data/species.js tribeMap</name>
  <files>_pages/genus.njk, _pages/subgenus.njk, _pages/tribe.njk, _data/species.js</files>
  <read_first>
    - _pages/genus.njk (read lines 25–35 — the occurrence_count branch and surrounding context)
    - _pages/subgenus.njk (read lines 26–36 — analogous block)
    - _pages/tribe.njk (read lines 22–30 — the genera loop and count span)
    - _data/species.js (read lines 222–260 — tribeMap accumulation loop and tribeList .map())
    - .planning/phases/120-species-page-source-counts-photo-list/120-PATTERNS.md §genus.njk, §subgenus.njk, §tribe.njk, §_data/species.js sections
  </read_first>
  <action>
    Apply four targeted changes (per D-03, D-04, D-05):

    genus.njk (line ~28): In the `{%- if sp.occurrence_count > 0 -%}` branch, replace the inner span content `{{ sp.occurrence_count }} records` with `{{ sp.specimen_count }} specimens · {{ sp.inat_obs_count }} community observations`. The `{%- elif sp.on_checklist -%}` and `{%- else -%}` branches are untouched.

    subgenus.njk (line ~29): Identical change to the same three-branch structure — replace only the `occurrence_count > 0` branch span content. The checklist and zero branches are untouched.

    tribe.njk (line ~26): Replace `{{ g.occurrence_count }} records` with `{{ g.specimen_count }} specimens · {{ g.inat_obs_count }} community observations`. There is no three-branch structure here — all genera in tribeList already have occurrence_count > 0.

    _data/species.js tribeMap accumulator (lines ~235–238): Change the generaMap value from a bare integer to an object. The existing line:
      `tribeMap[sp.tribe].generaMap[sp.genus] = 0;`
    becomes:
      `tribeMap[sp.tribe].generaMap[sp.genus] = { occurrence_count: 0, specimen_count: 0, inat_obs_count: 0 };`

    The existing single increment line:
      `tribeMap[sp.tribe].generaMap[sp.genus] += sp.occurrence_count;`
    becomes three increment lines:
      `tribeMap[sp.tribe].generaMap[sp.genus].occurrence_count += sp.occurrence_count;`
      `tribeMap[sp.tribe].generaMap[sp.genus].specimen_count += (sp.specimen_count || 0);`
      `tribeMap[sp.tribe].generaMap[sp.genus].inat_obs_count += (sp.inat_obs_count || 0);`

    The tribeList .map() destructor (line ~246) changes from:
      `.map(([genus, occurrence_count]) => ({ genus, occurrence_count }))`
    to:
      `.map(([genus, counts]) => ({ genus, ...counts }))`

    The .filter() line above it changes from:
      `.filter(([, occ]) => occ > 0)`
    to:
      `.filter(([, counts]) => counts.occurrence_count > 0)`

    The `totalOccurrences` reduce below (line ~247) uses `g.occurrence_count` which continues to work after the spread.
  </action>
  <verify>
    <automated>cd /Users/rainhead/dev/beeatlas && npm run build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - genus.njk occurrence branch contains: `{{ sp.specimen_count }} specimens · {{ sp.inat_obs_count }} community observations`
    - subgenus.njk occurrence branch contains the same text
    - tribe.njk genera loop span contains: `{{ g.specimen_count }} specimens · {{ g.inat_obs_count }} community observations`
    - _data/species.js tribeMap block initialises `generaMap[genus]` as `{ occurrence_count: 0, specimen_count: 0, inat_obs_count: 0 }`
    - _data/species.js .filter line uses `counts.occurrence_count > 0`
    - _data/species.js .map line uses `({ genus, ...counts })`
    - `npm run build` exits 0
    - `npm test` exits 0 (Vitest suite passes)
    - genus.njk and subgenus.njk checklist-only and zero branches unchanged (still `{{ sp.checklist_count }} checklist records` and `0 records`)
  </acceptance_criteria>
  <done>All four taxon-list templates show source-aware counts; build and tests pass</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| species.json → Nunjucks templates | Static JSON read at build time — no runtime injection. Data is pipeline-produced, not user-supplied. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-120-01 | Information Disclosure | Nunjucks arithmetic `occurrence_count + inat_obs_count` | accept | Both values are non-sensitive counts. Integer addition in Nunjucks cannot produce XSS; output is inside a text node, not an attribute. |
| T-120-SC | Tampering | npm/pip/cargo installs | accept | No new package installs in this plan — template and JS data-module changes only. |
</threat_model>

<verification>
1. `npm run build` exits 0 — Eleventy produces species, genus, subgenus, and tribe pages without template errors
2. `npm test` exits 0 — Vitest suite passes (no JS regressions)
3. In the built site: a species-detail page (e.g. `_site/species/Andrena/milwaukeensis/index.html`) contains "specimens · " and "community observations" and "records on the atlas"
4. A genus page contains "specimens · " in a `.count` span
5. A tribe page contains "specimens · " in a `.count` span
</verification>

<success_criteria>
- SPE-01: species-detail.njk shows `{{ sp.specimen_count }} specimens · {{ sp.inat_obs_count }} community observations` replacing `{{ sp.occurrence_count }} records`
- SPE-02 (species list pages): genus.njk and subgenus.njk occurrence branch updated; tribe.njk per-genus span updated; _data/species.js tribeMap carries specimen_count and inat_obs_count
- Atlas link text reads "records" (not "occurrences") and sums occurrence_count + inat_obs_count
- Checklist-only branches are untouched
- `npm run build` and `npm test` both pass
</success_criteria>

<output>
Create `.planning/phases/120-species-page-source-counts-photo-list/120-01-SUMMARY.md` when done
</output>
