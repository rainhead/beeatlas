---
phase: 113-species-page-expansion
plan: 02
type: execute
wave: 1
depends_on: [113-01]
files_modified:
  - data/dbt/models/intermediate/int_species_universe.sql
  - data/dbt/models/marts/species.sql
  - data/dbt/models/marts/schema.yml
  - data/species_export.py
autonomous: true
requirements: [SPEC-04, SPEC-05]

must_haves:
  truths:
    - "species.parquet contains a checklist_count column populated with row counts from checklist.parquet per canonical_name (0 for species absent from checklist)"
    - "species.parquet month_histogram values for checklist species reflect WABA + checklist month counts merged element-wise"
    - "species.json (post species_export.py) contains checklist_count integer field for every species record"
    - "dbt build succeeds without contract violations after the new column is added"
  artifacts:
    - path: "data/dbt/models/intermediate/int_species_universe.sql"
      provides: "checklist_month_agg CTE + element-wise histogram merge + checklist_count column"
      contains: "checklist_month_agg"
    - path: "data/dbt/models/marts/species.sql"
      provides: "checklist_count in SELECT, after ecoregion_count and before slug post-step"
      contains: "checklist_count"
    - path: "data/dbt/models/marts/schema.yml"
      provides: "checklist_count contract entry for species model"
      contains: "name: checklist_count"
    - path: "data/species_export.py"
      provides: "checklist_count in SPECIES_COLUMNS and PyArrow schema"
      contains: "'checklist_count'"
  key_links:
    - from: "int_species_universe.sql checklist_month_agg CTE"
      to: "{{ ref('checklist') }}"
      via: "FROM clause"
      pattern: "FROM\\s+\\{\\{\\s*ref\\(['\"]checklist['\"]\\)"
    - from: "species_export.py SPECIES_COLUMNS"
      to: "PyArrow schema"
      via: "must contain the same set of columns"
      pattern: "checklist_count.*pa\\.int64"
---

<objective>
Land the dbt + Python data-layer atomic five-step change that adds `checklist_count` to species.parquet and merges checklist month data into `month_histogram`. After this plan: species.json (consumed by Eleventy/_data and species_maps.py downstream) contains `checklist_count` for all 565 species, and the seasonality histogram already includes checklist month contributions before any frontend changes.

Purpose: Per RESEARCH.md §"dbt Contract: Adding checklist_count to species.parquet", omitting any of the five steps causes silent column drops or dbt contract failures. This plan keeps all five edits in one execution so the contract gate validates them together.

Output: species.parquet has 19 columns (was 18) plus the existing post-step `slug`, totaling 20 columns in species.json. Merged month_histogram covers WABA + checklist months. Vitest assertion `speciesList.length > 560` from Plan 01 turns green after this plan runs (because species.json now contains all 565 checklist species — note: this was already true via FULL OUTER JOIN; the count gate becomes verifiable end-to-end).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/113-species-page-expansion/113-CONTEXT.md
@.planning/phases/113-species-page-expansion/113-RESEARCH.md
@.planning/phases/113-species-page-expansion/113-PATTERNS.md
@.planning/phases/113-species-page-expansion/113-VALIDATION.md
@data/dbt/models/intermediate/int_species_universe.sql
@data/dbt/models/intermediate/int_species_occurrences_agg.sql
@data/dbt/models/marts/species.sql
@data/dbt/models/marts/schema.yml
@data/dbt/models/marts/checklist.sql
@data/species_export.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add checklist_month_agg CTE, merge histograms, and emit checklist_count column in dbt</name>
  <files>data/dbt/models/intermediate/int_species_universe.sql, data/dbt/models/marts/species.sql, data/dbt/models/marts/schema.yml</files>
  <read_first>
    - data/dbt/models/intermediate/int_species_universe.sql (whole file — current CTE list, FULL OUTER JOIN, existing month_histogram CASE backfill at lines ~48-53, DISTINCT ON safety net at file tail)
    - data/dbt/models/intermediate/int_species_occurrences_agg.sql (lines 1-45 — the `list_value(SUM(CASE WHEN TRY_CAST(month AS INT) = N ...))::INTEGER[12]` pattern to mirror verbatim for checklist)
    - data/dbt/models/marts/checklist.sql (whole file — confirm column names `canonical_name`, `month`; confirm month column type before TRY_CAST)
    - data/dbt/models/marts/species.sql (whole file — current SELECT column order; new column must be appended after `ecoregion_count`)
    - data/dbt/models/marts/schema.yml (the `species` model block — current contract column list and data_type values)
    - .planning/phases/113-species-page-expansion/113-PATTERNS.md (sections "data/dbt/models/intermediate/int_species_universe.sql", "data/dbt/models/marts/species.sql", "data/dbt/models/marts/schema.yml" — concrete CTE and merge snippets)
    - .planning/phases/113-species-page-expansion/113-RESEARCH.md (§"Code Examples Pattern 1" for the exact element-wise CASE merge expression; §"Common Pitfalls" #1, #2, #8 — CASE not COALESCE, list_value addition syntax, GROUP BY canonical_name to avoid duplicate join rows)
  </read_first>
  <behavior>
    - After `dbt build`, `species.parquet` contains an integer column `checklist_count` populated as `COUNT(*) over checklist rows per canonical_name`, defaulting to 0 for species not in checklist.
    - `month_histogram` for species in checklist contains element-wise sum of WABA and checklist month counts; for WABA-only species it is unchanged; for checklist-only species it equals the checklist month histogram (or all-zeros if all checklist months are NULL).
    - `dbt build` exits 0; `data/dbt/models/marts/schema.yml` contract is enforced and matches the new column set.
    - Row count of species mart is unchanged from the existing FULL OUTER JOIN result (565 canonical names) — the new CTE introduces no row duplication because `checklist_month_agg` is keyed `GROUP BY canonical_name` before the LEFT JOIN.
  </behavior>
  <action>
    In data/dbt/models/intermediate/int_species_universe.sql, add a new CTE named `checklist_month_agg` immediately after the existing `occ_agg` CTE alias. The CTE selects `canonical_name`, a 12-element `list_value(SUM(CASE WHEN TRY_CAST(month AS INT) = N THEN 1 ELSE 0 END), ...)::INTEGER[12] AS checklist_month_histogram` mirroring `int_species_occurrences_agg.sql` lines 25-38, and `COUNT(*) AS checklist_count`. Source is `{{ ref('checklist') }}` with `WHERE canonical_name IS NOT NULL AND month IS NOT NULL` and `GROUP BY canonical_name`. Important: the COUNT(*) here only counts rows with non-null month — this would undercount `checklist_count`. Therefore split into TWO CTEs: `checklist_month_agg` with the month filter and `checklist_count_agg` (no month filter, `WHERE canonical_name IS NOT NULL` only, `COUNT(*) AS checklist_count`, `GROUP BY canonical_name`). LEFT JOIN both into the existing `species_universe` CTE on `canonical_name = COALESCE(c.canonical_name, oa.canonical_name)` mirroring the existing FULL OUTER JOIN key. Replace the existing month_histogram CASE block (lines ~48-53) with the four-branch merge from .planning/phases/113-species-page-expansion/113-RESEARCH.md §"Code Examples Pattern 1": branches for (both NULL → 12-zero literal), (WABA NULL → checklist), (checklist NULL → WABA), (both present → `list_value(oa.month_histogram[1] + cma.checklist_month_histogram[1], ..., oa.month_histogram[12] + cma.checklist_month_histogram[12])::INTEGER[12]`). Add `COALESCE(cca.checklist_count, 0)::BIGINT AS checklist_count` to the SELECT (matches the `bigint` data_type in schema.yml). Use CASE not COALESCE for INTEGER[] backfills (DuckDB 1.4.x limitation per Pitfall #1). Preserve the existing `DISTINCT ON (canonical_name)` safety net at the file tail. In data/dbt/models/marts/species.sql, append `checklist_count` to the SELECT immediately after `ecoregion_count`. In data/dbt/models/marts/schema.yml, locate the `species` model `columns:` list (after `ecoregion_count`) and insert an entry with `- name: checklist_count` and `data_type: bigint`. The new column must appear in schema.yml in the same position as in species.sql SELECT (contract order). Verify the change with `bash data/dbt/run.sh build` — must exit 0 with no contract errors.
  </action>
  <verify>
    <automated>cd data && bash dbt/run.sh build 2>&1 | tail -30 && uv run python -c "import duckdb; con=duckdb.connect('beeatlas.duckdb', read_only=True); cols=[r[1] for r in con.execute(\"DESCRIBE main_marts.species\").fetchall()]; assert 'checklist_count' in cols, f'missing checklist_count in {cols}'; n=con.execute(\"SELECT COUNT(*) FROM main_marts.species WHERE checklist_count > 0\").fetchone()[0]; print(f'checklist_count > 0 rows: {n}'); assert n > 500, f'expected >500 checklist species, got {n}'"</automated>
  </verify>
  <acceptance_criteria>
    - Source: `grep -c "checklist_month_agg" data/dbt/models/intermediate/int_species_universe.sql` returns at least 2 (CTE definition + JOIN reference)
    - Source: `grep -c "checklist_count_agg" data/dbt/models/intermediate/int_species_universe.sql` returns at least 2
    - Source: `grep -Ec "FROM[[:space:]]+\\{\\{[[:space:]]*ref\\(['\\\"]checklist['\\\"]\\)" data/dbt/models/intermediate/int_species_universe.sql` returns at least 1
    - Source: `grep -c "checklist_count" data/dbt/models/marts/species.sql` returns at least 1
    - Source: `grep -A1 "name: checklist_count" data/dbt/models/marts/schema.yml | grep -c "data_type: bigint"` returns 1
    - Source: `grep -c "COALESCE" data/dbt/models/intermediate/int_species_universe.sql | xargs -I{} sh -c 'test {} -le 5'` (no COALESCE on INTEGER[] — see Pitfall #1; some COALESCE on scalar values is OK)
    - Source: the merged month_histogram block uses CASE — `grep -c "CASE WHEN oa.month_histogram IS NULL" data/dbt/models/intermediate/int_species_universe.sql` returns 1
    - Behavior: `bash data/dbt/run.sh build` exits 0
    - Behavior: DESCRIBE on `main_marts.species` lists `checklist_count` with type BIGINT
    - Behavior: at least 500 rows have `checklist_count > 0` (checklist has ~565 species; allow for canonical_name normalization edge cases)
    - Behavior: row count of `main_marts.species` does NOT exceed 600 (no row duplication from JOIN)
  </acceptance_criteria>
  <done>
    dbt build green; species.parquet has the new column; merged histogram populated; schema.yml contract enforced.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Flow checklist_count through species_export.py into species.json</name>
  <files>data/species_export.py</files>
  <read_first>
    - data/species_export.py (whole file — `SPECIES_COLUMNS` constant at lines ~49-55, `export_species_parquet` function, PyArrow schema at lines ~149-169, mart_cols slice logic at lines ~116-117)
    - .planning/phases/113-species-page-expansion/113-PATTERNS.md (section "data/species_export.py" — concrete column placement)
    - .planning/phases/113-species-page-expansion/113-RESEARCH.md (§"dbt Contract: Adding checklist_count to species.parquet" — five-step checklist)
    - data/tests/test_species_export.py (if present — confirm there is an existing test asserting SPECIES_COLUMNS count or shape that must be updated)
  </read_first>
  <behavior>
    - After running `uv run python species_export.py` (or via `run.py`), `public/data/species.json` contains a `checklist_count` integer field on every record.
    - PyArrow schema includes `('checklist_count', pa.int64())` in the same position as the SPECIES_COLUMNS list to keep the two definitions in lockstep.
    - The `mart_cols` slice that excludes `slug` still excludes only `slug` (the post-step Python-computed column) — `checklist_count` is read from the sandbox parquet.
    - Existing pytest suite passes: `cd data && uv run pytest`.
  </behavior>
  <action>
    Open data/species_export.py. Insert `'checklist_count'` into the `SPECIES_COLUMNS` list immediately before `'slug'` (which is the post-step computed column kept last). The new SPECIES_COLUMNS order must be: scientificName, canonical_name, family, subfamily, tribe, genus, subgenus, specific_epithet, on_checklist, status, occurrence_count, specimen_count, provisional_count, first_occurrence_date, last_occurrence_date, month_histogram, county_count, ecoregion_count, checklist_count, slug. Insert `('checklist_count', pa.int64())` into the PyArrow schema list in the matching position (immediately before `('slug', pa.string())`). Verify the existing `mart_cols = SPECIES_COLUMNS[:-1]` slice still selects all dbt-produced columns (everything except `slug`); no slice change needed — `checklist_count` is now part of the dbt sandbox parquet from Task 1. If a test in data/tests/test_species_export.py asserts a specific column count, update the expected count by +1. Run `cd data && uv run python run.py` (or just the species-export step) and confirm species.json gains the field by inspecting the first record. Do NOT add slug renaming, normalization, or any other behavior change.
  </action>
  <verify>
    <automated>cd data && uv run pytest tests/test_species_export.py -x 2>&1 | tail -20 && uv run python -c "import json, pathlib; p=pathlib.Path('../public/data/species.json'); d=json.loads(p.read_text()); rec=d[0] if isinstance(d, list) else next(iter(d.values())); assert 'checklist_count' in rec, f'checklist_count missing from species.json record: {list(rec.keys())[:25]}'; n=sum(1 for r in (d if isinstance(d, list) else d.values()) if r.get('checklist_count', 0) > 0); print(f'species.json records with checklist_count>0: {n}'); assert n > 500"</automated>
  </verify>
  <acceptance_criteria>
    - Source: `grep -c "'checklist_count'" data/species_export.py` returns at least 1 (in SPECIES_COLUMNS)
    - Source: `grep -c "checklist_count.*pa.int64" data/species_export.py` returns 1 (PyArrow schema entry)
    - Source: the order in SPECIES_COLUMNS is `..., 'ecoregion_count', 'checklist_count', 'slug'` — `grep -A1 "'ecoregion_count'" data/species_export.py | grep -c "checklist_count"` returns at least 1
    - Behavior: `cd data && uv run pytest` exits 0
    - Behavior: `public/data/species.json` records contain `checklist_count` field; > 500 records have value > 0
    - Behavior: `npm test -- src/tests/data-species.test.ts` — speciesList count assertion from Plan 01 (>560) now passes
  </acceptance_criteria>
  <done>
    species.json contains checklist_count; PyArrow schema and SPECIES_COLUMNS stay in lockstep; Python tests green; Plan 01 RED test on speciesList count is now GREEN.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| checklist.parquet → dbt | Trusted local pipeline input (produced by Phase 111 from vetted Bartholomew CSV) |
| dbt → species.parquet → species.json | Trusted static export |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-113-02-01 | Tampering | dbt contract on species mart | mitigate | schema.yml contract `enforced: true` blocks accidental column drift |
| T-113-02-02 | Information Disclosure | species.json | accept | already a public static asset; no PII added |
| T-113-02-03 | Denial of Service | int_species_universe CTE | mitigate | GROUP BY canonical_name in checklist_month_agg + DISTINCT ON safety net prevent row-explosion (Pitfall #8) |
</threat_model>

<verification>
- `bash data/dbt/run.sh build` exits 0
- `cd data && uv run pytest` exits 0
- `npm test -- src/tests/data-species.test.ts` — the `speciesList.length > 560` assertion now passes
- `public/data/species.json` contains `checklist_count` on every record
- Row count of `main_marts.species` is unchanged within ±5 from prior baseline (no JOIN row explosion)
</verification>

<success_criteria>
species.parquet has checklist_count column with bigint type; merged month_histogram reflects WABA + checklist counts; species.json downstream contains the field; dbt build and Python test suite are green.
</success_criteria>

<output>
Create `.planning/phases/113-species-page-expansion/113-02-SUMMARY.md` documenting: column counts before/after, dbt build duration, number of species with checklist_count > 0, any rows where merged month_histogram differs from prior WABA-only histogram.
</output>
