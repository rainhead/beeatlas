---
phase: 113-species-page-expansion
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - src/tests/seasonality-viz.test.ts
  - src/tests/data-species.test.ts
  - src/tests/build-output.test.ts
  - data/tests/test_species_maps.py
autonomous: true
requirements: [SPEC-01, SPEC-02, SPEC-03, SPEC-04, SPEC-05]

must_haves:
  truths:
    - "All five Wave 0 RED tests exist and fail with clear messages naming the missing behavior"
    - "Test failures point at the exact production change required (checklist_count column, onChecklist property, genusList checklist-only species, county fills, checklist-only page generation)"
    - "Existing test count assertion is updated from 527 to 565 expectation"
  artifacts:
    - path: "src/tests/seasonality-viz.test.ts"
      provides: "RED test: total=0 + onChecklist=true renders 'Monthly phenology not recorded'"
      contains: "Monthly phenology not recorded"
    - path: "src/tests/data-species.test.ts"
      provides: "RED tests: speciesList.length >= 565; genusList contains checklist-only species; subgenusList accepts checklist-only groups"
      contains: "checklistCount"
    - path: "src/tests/build-output.test.ts"
      provides: "RED tests: checklist-only species page exists, no atlas link, has Bartholomew attribution, species index has 'checklist only' badge"
      contains: "Bartholomew et al. 2024"
    - path: "data/tests/test_species_maps.py"
      provides: "RED tests: checklist county fill paths render with class='checklist-county'; absent county yields no fill"
      contains: "checklist-county"
  key_links:
    - from: "src/tests/data-species.test.ts"
      to: "_data/species.js"
      via: "import"
      pattern: "from ['\"]../../_data/species"
    - from: "src/tests/seasonality-viz.test.ts"
      to: "src/species/seasonality-viz.ts"
      via: "dynamic import"
      pattern: "species/seasonality-viz"
---

<objective>
Establish Wave 0 RED test gates that fail today and become the acceptance signal for every subsequent plan in Phase 113. No production code changes — only test additions and one count assertion update.

Purpose: Per VALIDATION.md, Phase 113 must close the Nyquist gap from prior milestones (Phase 98 bypassed Wave 0). Every SPEC-01..SPEC-05 outcome needs an automated assertion before its implementation lands.

Output: Failing test cases that name the missing artifacts (`checklist_count`, `onChecklist` property, county fill class, checklist-only badge, attribution line) so executors of Plans 02–05 see green when the corresponding production change is correct.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/113-species-page-expansion/113-CONTEXT.md
@.planning/phases/113-species-page-expansion/113-RESEARCH.md
@.planning/phases/113-species-page-expansion/113-PATTERNS.md
@.planning/phases/113-species-page-expansion/113-VALIDATION.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RED tests for JS data layer and seasonality-viz</name>
  <files>src/tests/data-species.test.ts, src/tests/seasonality-viz.test.ts</files>
  <read_first>
    - src/tests/data-species.test.ts (whole file — preserve existing describe blocks, update one assertion, append new tests)
    - src/tests/seasonality-viz.test.ts (whole file — append two new test cases to existing describe block, mirror existing VIZ-02 fallback test structure)
    - _data/species.js (read lines 95-210 to confirm `speciesList`, `genusList`, `subgenusList` shapes and `flat` data source)
    - src/species/seasonality-viz.ts (read render() method to confirm current VIZ-02 fallback wording — must use exact existing fallback string in the negative test)
    - .planning/phases/113-species-page-expansion/113-PATTERNS.md (sections "src/tests/seasonality-viz.test.ts" and "src/tests/data-species.test.ts" — use the assertion code shown verbatim)
  </read_first>
  <behavior>
    - Test A (`data-species.test.ts`): the existing `expect(list.length).toBeGreaterThan(500)` assertion is updated to `expect(list.length).toBeGreaterThan(560)` with comment "// 565 checklist species (SPEC-01)". FAILS until species.parquet rebuild lands.
    - Test B (`data-species.test.ts`, new test): asserts `genusList.flatMap(g => g.species)` contains at least one species with `occurrence_count === 0 && on_checklist === true`, and that each such species has `hexColor === '#cccccc'`. FAILS today because `genusList` filter at line 115 drops occurrence_count=0 species.
    - Test C (`data-species.test.ts`, replaces existing `subgenusList.every(g => g.totalOccurrences > 0)` test at line ~172): asserts `subgenusList.every(g => g.totalOccurrences > 0 || g.checklistCount > 0)`. PASSES vacuously today (no checklistCount field exists; totalOccurrences>0 still holds) — becomes load-bearing once Plan 04 adds checklist-only subgenus groups.
    - Test D (`seasonality-viz.test.ts`, new VIZ-02 case): mounts `<seasonality-viz>`, sets `el.data = new Array(12).fill(0)`, `el.onChecklist = true`, `await el.updateComplete`, then asserts `el.querySelector('p.viz-fallback')?.textContent === 'Monthly phenology not recorded'`. FAILS today because the `onChecklist` property does not exist.
    - Test E (`seasonality-viz.test.ts`, new VIZ-02 negative case): same setup with `el.onChecklist = false` — asserts the fallback text is `'0 records'` (the existing zero-total fallback string from the current implementation). Passes today.
  </behavior>
  <action>
    Update src/tests/data-species.test.ts: change the `speciesList.length` lower bound on the existing assertion to 560 with the comment "// 565 checklist species (SPEC-01)". Add a new `test()` named `'genusList contains at least one species with occurrence_count === 0 and on_checklist (D-03)'` that flatMaps `genusList` species and asserts at least one checklist-only species exists with `hexColor === '#cccccc'`. Replace the existing test `subgenusList.every(g => g.totalOccurrences > 0)` (around line 172) with a new test asserting `g.totalOccurrences > 0 || g.checklistCount > 0` for every group; keep the original test name updated to reflect the new predicate (per PATTERNS.md). Use the test code shown in .planning/phases/113-species-page-expansion/113-PATTERNS.md section "src/tests/data-species.test.ts" verbatim. Append to src/tests/seasonality-viz.test.ts inside the existing top-level `describe` block two new `test()` cases mirroring PATTERNS.md "src/tests/seasonality-viz.test.ts" verbatim: one with `el.onChecklist = true` asserting `'Monthly phenology not recorded'`, one with `el.onChecklist = false` asserting the existing `'0 records'` fallback. Confirm the negative-case expected string by reading `src/species/seasonality-viz.ts` render() output for total=0 — use whatever literal string the current implementation emits (do not invent text). Do not modify any production source — this task only authors RED tests plus the one count-bump assertion update.
  </action>
  <verify>
    <automated>npm test -- src/tests/data-species.test.ts src/tests/seasonality-viz.test.ts 2>&1 | grep -E "FAIL|✗|×|failed" | head -20</automated>
  </verify>
  <acceptance_criteria>
    - Source: `grep -c "565 checklist species" src/tests/data-species.test.ts` returns 1
    - Source: `grep -c "occurrence_count === 0 and on_checklist (D-03)" src/tests/data-species.test.ts` returns 1
    - Source: `grep -c "checklistCount" src/tests/data-species.test.ts` returns at least 1
    - Source: `grep -c "Monthly phenology not recorded" src/tests/seasonality-viz.test.ts` returns 1
    - Source: `grep -c "onChecklist" src/tests/seasonality-viz.test.ts` returns at least 2 (true and false cases)
    - Behavior: `npm test -- src/tests/seasonality-viz.test.ts` exits non-zero with at least one failing assertion naming "Monthly phenology not recorded"
    - Behavior: `npm test -- src/tests/data-species.test.ts` exits non-zero with failure naming the missing checklist-only species in genusList OR the speciesList count
  </acceptance_criteria>
  <done>
    Both test files committed. JS test run is RED with failures explicitly pointing at: (a) speciesList count <565, (b) absence of checklist-only species in genusList, (c) absence of `onChecklist` property handling in seasonality-viz. Plans 02–04 will turn these green.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: RED tests for Python SVG county fills and Eleventy build output</name>
  <files>data/tests/test_species_maps.py, src/tests/build-output.test.ts</files>
  <read_first>
    - data/tests/test_species_maps.py (whole file — read existing `test_write_species_svg_creates_subdir` and related tests to mirror the `tmp_path` + `ET.Element` pattern, namespace import, and assertion style)
    - data/species_maps.py (whole file — confirm current `_write_species_svg` signature, `STYLE_CSS` constant, `_load_county_geojsons` signature, SVG_NS constant. Tests must be written against the TARGET signature defined in PATTERNS.md, which Plan 03 will implement)
    - src/tests/build-output.test.ts (whole file — preserve the `describe.skipIf(SKIP_BUILD)` block; append new tests inside it mirroring the existing `_site/species/Agapostemon/femoratus/index.html` assertion pattern)
    - .planning/phases/113-species-page-expansion/113-PATTERNS.md (sections "data/tests/test_species_maps.py" and "src/tests/build-output.test.ts" — use the test code verbatim)
    - public/data/species.json (only if it exists; the executor may use it to pick a real checklist-only species slug for the build-output assertion — see Action below)
  </read_first>
  <behavior>
    - Test F (`test_species_maps.py`, new): `test_write_species_svg_renders_checklist_county_fill` — passes a 1-county dict to a county_geojsons_by_name parameter and a checklist_counties set containing that county name; asserts exactly one `<path class="checklist-county">` is written into the output SVG.
    - Test G (`test_species_maps.py`, new): `test_write_species_svg_no_checklist_fill_when_county_absent` — passes a county not in checklist_counties; asserts zero `class="checklist-county"` paths.
    - Test H (`test_species_maps.py`, new): `test_style_css_contains_checklist_county_class` — asserts `'checklist-county' in STYLE_CSS` and that `#b0cfe8` and `fill-opacity` appear inside STYLE_CSS.
    - Test I (`build-output.test.ts`, new — gated by `SKIP_BUILD` like existing tests): for a known checklist-only species slug, asserts `_site/species/{slug}/index.html` exists, does NOT match `/View \d+ occurrences on the atlas/`, contains `'Bartholomew et al. 2024'`, and contains `'/data/species-maps/'`. RED today because species page either does not exist or shows the atlas link.
    - Test J (`build-output.test.ts`, new): asserts `_site/species/index.html` contains the substring `'checklist only'`. RED today.
  </behavior>
  <action>
    Append to data/tests/test_species_maps.py three new test functions named exactly `test_write_species_svg_renders_checklist_county_fill`, `test_write_species_svg_no_checklist_fill_when_county_absent`, `test_style_css_contains_checklist_county_class`. Implement them per the code in .planning/phases/113-species-page-expansion/113-PATTERNS.md section "data/tests/test_species_maps.py" — they call `_write_species_svg` with the EXTENDED signature `(slug, points, checklist_counties, county_geojsons_by_name, backdrop, out_dir)` that Plan 03 will implement. The tests must import the SVG_NS module-level constant. Tests should fail today with TypeError or signature mismatch — that is the desired RED state. For src/tests/build-output.test.ts, append two new `test()` calls inside the existing `describe.skipIf(SKIP_BUILD)` block. To choose a stable known-checklist-only slug: read public/data/species.json if present and pick the first record with `occurrence_count === 0 && on_checklist === true` whose slug is non-null; if species.json is not present at planning time, hardcode a slug guess based on .planning/phases/113-species-page-expansion/113-RESEARCH.md guidance and add a TODO comment that Plan 02 will refresh the slug after the dbt rebuild. If a guess must be made, use a placeholder `const KNOWN_CHECKLIST_ONLY_SLUG = 'TODO/replace_after_plan02';` so the test fails loudly. Assertions: `expect(html).not.toMatch(/View \d+ occurrences on the atlas/)`, `expect(html).toContain('Bartholomew et al. 2024')`, `expect(html).toMatch(/src="\/data\/species-maps\//)`. Second test reads `_site/species/index.html` and asserts `expect(html).toContain('checklist only')`. Do not touch any production source file.
  </action>
  <verify>
    <automated>cd data && uv run pytest tests/test_species_maps.py -x 2>&1 | grep -E "FAILED|PASSED|ERROR" | head -10</automated>
  </verify>
  <acceptance_criteria>
    - Source: `grep -c "checklist-county" data/tests/test_species_maps.py` returns at least 2
    - Source: `grep -c "checklist_counties" data/tests/test_species_maps.py` returns at least 2
    - Source: `grep -c "test_style_css_contains_checklist_county_class" data/tests/test_species_maps.py` returns 1
    - Source: `grep -c "Bartholomew et al. 2024" src/tests/build-output.test.ts` returns 1
    - Source: `grep -c "checklist only" src/tests/build-output.test.ts` returns 1
    - Behavior: `cd data && uv run pytest tests/test_species_maps.py::test_write_species_svg_renders_checklist_county_fill` exits non-zero (TypeError on signature OR assertion failure on missing class)
    - Behavior: `cd data && uv run pytest tests/test_species_maps.py::test_style_css_contains_checklist_county_class` exits non-zero (STYLE_CSS missing checklist-county)
  </acceptance_criteria>
  <done>
    Both files committed. Python pytest is RED on three new checklist tests. JS build-output tests are gated behind SKIP_BUILD env (will be exercised at Plan 05 verification with `VITEST_SKIP_BUILD=0`). Plan 03 turns Python tests green; Plan 05 turns build-output tests green.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| repo → CI/dev test runners | Test files execute under Vitest and pytest; no untrusted input is processed |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-113-01-01 | Tampering | test files | accept | tests are dev/build artifacts only; no production runtime |
| T-113-01-02 | Information Disclosure | test fixtures | accept | fixtures contain synthetic county names and geometries only |
</threat_model>

<verification>
- `npm test -- src/tests/data-species.test.ts src/tests/seasonality-viz.test.ts` exits non-zero with failures matching the patterns named in Task 1 acceptance criteria
- `cd data && uv run pytest tests/test_species_maps.py -x` exits non-zero with three new tests failing on signature/class mismatch
- No production source files have been modified (`git diff --stat data/species_maps.py _data/species.js src/species/seasonality-viz.ts` shows no changes)
</verification>

<success_criteria>
All five RED tests committed and failing as documented. Wave 0 gate passes when subsequent plans cause them to flip green.
</success_criteria>

<output>
Create `.planning/phases/113-species-page-expansion/113-01-SUMMARY.md` when done. Mark `nyquist_compliant: true` and `wave_0_complete: true` in 113-VALIDATION.md frontmatter.
</output>
