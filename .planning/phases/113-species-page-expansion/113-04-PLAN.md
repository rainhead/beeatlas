---
phase: 113-species-page-expansion
plan: 04
type: execute
wave: 2
depends_on: [113-01, 113-02]
files_modified:
  - _data/species.js
  - src/species/seasonality-viz.ts
autonomous: true
requirements: [SPEC-01, SPEC-02, SPEC-05]

must_haves:
  truths:
    - "genusList includes checklist-only species (occurrence_count === 0 && on_checklist === true) with hexColor '#cccccc'"
    - "subgenusList includes checklist-only species and is no longer filtered out when totalOccurrences is 0 but checklistCount > 0"
    - "Color index computation over WABA species (occurrence_count > 0) is unchanged — hue assignments do not drift"
    - "seasonality-viz exposes an onChecklist boolean property"
    - "When total === 0 && this.onChecklist, seasonality-viz renders 'Monthly phenology not recorded' instead of '0 records'"
    - "Plan 01 RED tests for genusList checklist-only species and seasonality-viz onChecklist fallback both turn green"
  artifacts:
    - path: "_data/species.js"
      provides: "genusList and subgenusList accept checklist-only species with neutral color and checklistCount aggregation"
      contains: "checklistCount"
    - path: "src/species/seasonality-viz.ts"
      provides: "onChecklist property and Monthly-phenology-not-recorded fallback branch"
      contains: "onChecklist"
  key_links:
    - from: "genusList map lambda"
      to: "g.allMembers checklist-only filter"
      via: "filter(sp => sp.occurrence_count === 0 && sp.on_checklist)"
      pattern: "occurrence_count === 0 && sp\\.on_checklist"
    - from: "seasonality-viz.render"
      to: "this.onChecklist branch"
      via: "early return inside total < 5 fallback"
      pattern: "this\\.onChecklist"
---

<objective>
Update the build-time JS data layer to surface checklist-only species on genus and subgenus pages, and extend the seasonality-viz Lit component with an `onChecklist` property that triggers the "Monthly phenology not recorded" fallback. After this plan: genus/subgenus list assertions from Plan 01 are green; the seasonality-viz fallback test from Plan 01 is green. No template changes yet (Plan 05).

Purpose: D-01, D-03 (genus/subgenus inclusion), D-13 (seasonality fallback). RESEARCH.md Pitfall #3 warns against color index drift; checklist-only species must be appended AFTER color computation. RESEARCH.md Pitfall #7 requires the subgenusList trailing filter to accept checklist-only groups.

Output: `genusList` and `subgenusList` shapes gain checklist-only species entries with `hexColor: '#cccccc'`; `subgenusList` groups include a `checklistCount` field; `seasonality-viz` accepts `onChecklist` property and renders the new fallback text when total===0 && onChecklist===true. JS unit tests from Plan 01 turn green; subgenusList does not lose any existing groups.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/113-species-page-expansion/113-CONTEXT.md
@.planning/phases/113-species-page-expansion/113-RESEARCH.md
@.planning/phases/113-species-page-expansion/113-PATTERNS.md
@_data/species.js
@src/species/seasonality-viz.ts
@src/tests/data-species.test.ts
@src/tests/seasonality-viz.test.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend genusList and subgenusList in _data/species.js to include checklist-only species</name>
  <files>_data/species.js</files>
  <read_first>
    - _data/species.js (whole file — genusList block at lines 95-146 including withOcc filter at line 115, color index assignment at line 128, unresolved-species `Genus sp.` insert at line 136, totalOccurrences computation at line 144; subgenusList block at lines 148-206 with the trailing `.filter(g => g.totalOccurrences > 0)` at line 206)
    - .planning/phases/113-species-page-expansion/113-PATTERNS.md (section "_data/species.js" — concrete code snippets for genusList and subgenusList extensions, including hexColor choice and checklistCount aggregation)
    - .planning/phases/113-species-page-expansion/113-RESEARCH.md (Pitfall #3 color drift, Pitfall #7 subgenusList trailing filter, §"Common Pitfalls" Note on color value conflict — use '#cccccc' to match the existing test in data-species.test.ts line 105 and Plan 01's new test)
    - src/tests/data-species.test.ts (read Plan 01 additions to confirm the new genusList assertion expects hexColor === '#cccccc' and that subgenusList may include checklist-only groups)
  </read_first>
  <behavior>
    - `genusList` group objects contain a `species` array that includes BOTH WABA species (with their hue-derived hexColor) AND checklist-only species (with `hexColor: '#cccccc'`); checklist-only species are appended AFTER the WABA species and sorted alphabetically by canonical_name.
    - Color index computation (the hue assignment for WABA species) continues to iterate over `withOcc` only — adding checklist-only species does NOT change any existing WABA hexColor.
    - `subgenusList` returned objects gain a `checklistCount` field = sum of `checklist_count` across checklist-only members of the subgenus group; `totalOccurrences` continues to be the sum over WABA species only.
    - The trailing `.filter(g => g.totalOccurrences > 0)` becomes `.filter(g => g.totalOccurrences > 0 || g.checklistCount > 0)`.
    - `tribeList` is unchanged (out of phase scope per CONTEXT.md — no tribe page changes).
    - Plan 01 unit tests `genusList contains at least one species with occurrence_count === 0 and on_checklist (D-03)` and the updated `subgenusList.every(g => g.totalOccurrences > 0 || g.checklistCount > 0)` both pass.
  </behavior>
  <action>
    In the genusList map callback in _data/species.js (lines 110-146): after the existing speciesOnly array is built and before the unresolved `Genus sp.` insert, add a new const `checklistOnly = g.allMembers.filter(sp => sp.occurrence_count === 0 && sp.on_checklist && sp.specific_epithet !== null).sort((a, b) => a.canonical_name.localeCompare(b.canonical_name))`. Map to display objects with `{ ...sp, hexColor: '#cccccc' }`. Replace the existing `const species = [...speciesOnly]` (or equivalent local assignment) with `const species = [...speciesOnly, ...checklistOnlyMapped]`. The unresolved `Genus sp.` push (if `unresolvedOccurrences > 0`) continues to be appended after both. Critically, the color index assignment over withOcc must remain unchanged — checklist-only species receive their color directly in the map step, not from `colorByCanon`. In the subgenusList map callback (lines 168-206): apply the identical extension — build `checklistOnly` per subgenus group, map with `hexColor: '#cccccc'`, append to species. Add a `checklistCount` field to the returned object computed as `checklistOnly.reduce((acc, sp) => acc + (sp.checklist_count || 0), 0)`. Change the trailing `.filter(g => g.totalOccurrences > 0)` at line 206 to `.filter(g => g.totalOccurrences > 0 || g.checklistCount > 0)`. Do NOT touch tribeList. Use literal `'#cccccc'` for hexColor (matches the existing test at data-species.test.ts line 105 AND the new Plan 01 assertion). Do not introduce '#aaaaaa' — that color is reserved for the unresolved `Genus sp.` entry.
  </action>
  <verify>
    <automated>npm test -- src/tests/data-species.test.ts 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - Source: `grep -c "occurrence_count === 0 && sp.on_checklist" _data/species.js` returns at least 2 (genusList + subgenusList)
    - Source: `grep -c "'#cccccc'" _data/species.js` returns at least 2 (genusList map + subgenusList map)
    - Source: `grep -c "checklistCount" _data/species.js` returns at least 2 (subgenusList field set + trailing filter)
    - Source: `grep -E "filter\(g => g\.totalOccurrences > 0 \|\| g\.checklistCount > 0\)" _data/species.js` returns at least 1
    - Source: the existing color-index loop iterates ONLY over withOcc (occurrence_count > 0 species) — `grep -A3 "// Color indices must be computed" _data/species.js` shows withOcc-only iteration unchanged
    - Behavior: `npm test -- src/tests/data-species.test.ts` exits 0
    - Behavior: the speciesList count test, the new genusList checklist-only test, and the updated subgenusList totalOccurrences-or-checklistCount test are all GREEN
    - Behavior: no pre-existing test (e.g., `genusList hexColors match the Python _group_colors algorithm`) regresses to red
  </acceptance_criteria>
  <done>
    genusList and subgenusList include checklist-only species; Plan 01 JS data-species tests green; color index unaffected for WABA species.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add onChecklist property and Monthly-phenology-not-recorded fallback to seasonality-viz</name>
  <files>src/species/seasonality-viz.ts</files>
  <read_first>
    - src/species/seasonality-viz.ts (whole file — existing `@property({ attribute: false }) data: number[]` declaration around line 39; render() method including the `if (total < 5)` fallback branch around lines 60-71; existing fallback text string used when total === 0)
    - src/tests/seasonality-viz.test.ts (Plan 01 additions — the two new tests asserting `onChecklist=true` shows the new fallback and `onChecklist=false` continues to show the existing zero-records fallback)
    - .planning/phases/113-species-page-expansion/113-PATTERNS.md (section "src/species/seasonality-viz.ts" — concrete property declaration and early-return placement inside the total<5 branch)
    - .planning/phases/113-species-page-expansion/113-RESEARCH.md (D-13 wording; Pitfall #9 distinguishing "truly zero records" from "records exist but months unknown")
  </read_first>
  <behavior>
    - `seasonality-viz` declares `@property({ attribute: false }) onChecklist = false;` immediately after the existing `data` property.
    - Inside the `if (total < 5)` fallback branch in `render()`, the very first check is `if (total === 0 && this.onChecklist) return html\`<p class="viz-fallback">Monthly phenology not recorded</p>\`;`. All existing branches for 1-4 records continue unchanged, and the `total === 0 && !this.onChecklist` path continues to render the existing zero-records fallback text.
    - The existing `data` property and its default value are unchanged.
    - Plan 01 tests `VIZ-02 checklist fallback: total=0 + onChecklist=true ...` and `VIZ-02 checklist fallback: total=0 + onChecklist=false ...` both pass.
  </behavior>
  <action>
    In src/species/seasonality-viz.ts, add a new `@property({ attribute: false }) onChecklist = false;` field immediately after the existing `data` property declaration. Inside the `render()` method, locate the `if (total < 5)` (or equivalent) fallback branch. Insert as the FIRST statement inside that branch an early return: `if (total === 0 && this.onChecklist) { return html\`<p class="viz-fallback">Monthly phenology not recorded</p>\`; }`. Do not alter the existing 1-4 records rendering, the existing `total === 0 && !this.onChecklist` zero-records fallback, the bar rendering for total >= 5, or any CSS. The fallback paragraph uses the existing `viz-fallback` CSS class so styling is consistent. Confirm by running `npm test -- src/tests/seasonality-viz.test.ts` that the two Plan 01 fallback tests pass and that no existing VIZ-01..05 tests regress.
  </action>
  <verify>
    <automated>npm test -- src/tests/seasonality-viz.test.ts 2>&1 | tail -25</automated>
  </verify>
  <acceptance_criteria>
    - Source: `grep -c "onChecklist" src/species/seasonality-viz.ts` returns at least 2 (declaration + check)
    - Source: `grep -E "@property\(.*\)\s*onChecklist\s*=\s*false" src/species/seasonality-viz.ts` returns 1 line
    - Source: `grep -c "Monthly phenology not recorded" src/species/seasonality-viz.ts` returns 1
    - Source: the early-return is INSIDE the existing `total < 5` (or equivalent zero/small-total) branch — verify by reading render() and confirming the new conditional precedes the existing zero-records fallback emission
    - Behavior: `npm test -- src/tests/seasonality-viz.test.ts` exits 0
    - Behavior: existing VIZ-01..05 tests pass alongside the two new Plan 01 cases
  </acceptance_criteria>
  <done>
    seasonality-viz exposes onChecklist; new fallback renders when applicable; Plan 01 seasonality-viz tests green; no VIZ test regresses.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| species.json -> _data/species.js | Trusted build-time read of static data |
| Lit component DOM -> browser | Trusted local render; no untrusted input crosses |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-113-04-01 | Tampering | genusList color assignments | mitigate | color index iterates withOcc only; checklist-only species receive literal '#cccccc' — verified by existing colors test |
| T-113-04-02 | Information Disclosure | seasonality-viz fallback | accept | fallback text is a static string; no PII |
| T-113-04-03 | Denial of Service | _data/species.js compile time | accept | additional filter/map over ~565 species adds negligible build time |
</threat_model>

<verification>
- `npm test -- src/tests/data-species.test.ts src/tests/seasonality-viz.test.ts` exits 0
- All five Plan 01 JS RED assertions are now GREEN
- No pre-existing JS test regresses
</verification>

<success_criteria>
genusList/subgenusList surface checklist-only species; seasonality-viz handles the onChecklist case; Plan 01 JS tests green; ready for Plan 05 templates.
</success_criteria>

<output>
Create `.planning/phases/113-species-page-expansion/113-04-SUMMARY.md` documenting: how many checklist-only species now appear in genusList; how many subgenus groups would have been dropped without the checklistCount filter; which existing tests were unaffected.
</output>
