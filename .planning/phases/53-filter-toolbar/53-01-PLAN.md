---
phase: 53-filter-toolbar
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/bee-filter-toolbar.ts
  - frontend/src/tests/bee-filter-toolbar.test.ts
  - frontend/src/bee-atlas.ts
  - frontend/src/bee-sidebar.ts
  - frontend/src/tests/bee-sidebar.test.ts
autonomous: false
requirements:
  - FILT-08
  - FILT-09

must_haves:
  truths:
    - "Filter controls (taxon, year, month, county, ecoregion) are visible in a toolbar below the header on page load"
    - "CSV download button is visible in the toolbar and triggers a file download"
    - "Changing a filter in the toolbar updates the map/table identically to before"
    - "The sidebar no longer contains filter controls or the CSV download button"
  artifacts:
    - path: "frontend/src/bee-filter-toolbar.ts"
      provides: "Filter toolbar Lit component"
      exports: ["BeeFilterToolbar"]
      contains: "@customElement('bee-filter-toolbar')"
    - path: "frontend/src/tests/bee-filter-toolbar.test.ts"
      provides: "Unit tests for bee-filter-toolbar"
      contains: "FILT-08"
  key_links:
    - from: "frontend/src/bee-atlas.ts"
      to: "frontend/src/bee-filter-toolbar.ts"
      via: "<bee-filter-toolbar> element in render()"
      pattern: "bee-filter-toolbar"
    - from: "frontend/src/bee-filter-toolbar.ts"
      to: "frontend/src/bee-filter-controls.ts"
      via: "<bee-filter-controls> in shadow DOM"
      pattern: "bee-filter-controls"
    - from: "frontend/src/bee-atlas.ts"
      to: "bee-filter-toolbar csv-download event"
      via: "@csv-download event listener"
      pattern: "@csv-download=.*_onDownloadCsv"
---

<objective>
Move all filter controls and CSV download button from the sidebar into a new persistent toolbar below the header.

Purpose: Users should see filter controls immediately on page load without needing to interact with the sidebar. This is the second step of the v2.4 header-driven layout reorganization.
Output: `bee-filter-toolbar` component, updated `bee-atlas` wiring, cleaned-up `bee-sidebar`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/53-filter-toolbar/53-RESEARCH.md
@.planning/phases/53-filter-toolbar/53-UI-SPEC.md

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From frontend/src/bee-filter-controls.ts:
```typescript
@customElement('bee-filter-controls')
export class BeeFilterControls extends LitElement {
  @property({ attribute: false }) filterState!: FilterState;
  @property({ attribute: false }) taxaOptions: TaxonOption[] = [];
  @property({ attribute: false }) countyOptions: string[] = [];
  @property({ attribute: false }) ecoregionOptions: string[] = [];
  @property({ attribute: false }) collectorOptions: CollectorEntry[] = [];
  @property({ attribute: false }) summary: DataSummary | null = null;
  // Dispatches: new CustomEvent('filter-changed', { bubbles: true, composed: true, detail: ... })
}
```

From frontend/src/bee-sidebar.ts (types exported and used across components):
```typescript
export interface DataSummary { ... }
export interface TaxonOption { ... }
export interface FilteredSummary { ... }
export interface FilterChangedEvent { ... }
export interface FeedEntry { ... }
export interface SampleEvent { ... }
```

From frontend/src/filter.ts:
```typescript
export type FilterState = { ... };
export type CollectorEntry = { ... };
```

From frontend/src/bee-atlas.ts render() — current bee-sidebar wiring (lines 186-208):
```typescript
<bee-sidebar
  .samples=${this._selectedSamples}
  .summary=${this._summary}
  .taxaOptions=${this._taxaOptions}
  .filteredSummary=${this._filteredSummary}
  .layerMode=${this._layerMode}
  .viewMode=${this._viewMode}
  .recentSampleEvents=${...}
  .sampleDataLoaded=${this._sampleDataLoaded}
  .selectedSampleEvent=${this._selectedSampleEvent}
  .filterState=${this._filterState}
  .countyOptions=${this._countyOptions}
  .ecoregionOptions=${this._ecoregionOptions}
  .collectorOptions=${this._collectorOptions}
  .activeFeedEntries=${this._activeFeedEntries}
  @close=${this._onClose}
  @filter-changed=${this._onFilterChanged}
  @layer-changed=${this._onLayerChanged}
  @view-changed=${this._onViewChanged}
  @sample-event-click=${this._onSampleEventClick}
></bee-sidebar>
```

bee-atlas CSS layout — :host is flex column, .content is flex row:
```css
:host { display: flex; flex-direction: column; flex-grow: 1; overflow: hidden; }
.content { display: flex; flex-direction: row; flex-grow: 1; overflow: auto; position: relative; }
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create bee-filter-toolbar component and tests</name>
  <files>frontend/src/bee-filter-toolbar.ts, frontend/src/tests/bee-filter-toolbar.test.ts</files>
  <read_first>
    - frontend/src/bee-header.ts (pattern to follow for component structure and styles)
    - frontend/src/bee-filter-controls.ts (property interface to pass through)
    - frontend/src/tests/bee-header.test.ts (test pattern to follow)
    - frontend/src/bee-sidebar.ts (type imports: DataSummary, TaxonOption, FilterChangedEvent)
    - frontend/src/filter.ts (type imports: FilterState, CollectorEntry)
    - .planning/phases/53-filter-toolbar/53-UI-SPEC.md (visual contract)
  </read_first>
  <behavior>
    - Test 1 (FILT-08): BeeFilterToolbar has @property declarations for filterState, taxaOptions, countyOptions, ecoregionOptions, collectorOptions, summary, layerMode
    - Test 2 (FILT-08): bee-filter-toolbar.ts source contains 'bee-filter-controls' sub-component tag
    - Test 3 (FILT-09): bee-filter-toolbar.ts source contains 'csv-download' event string
    - Test 4 (FILT-09): bee-filter-toolbar.ts source contains a button with class 'csv-btn'
    - Test 5 (FILT-08): bee-filter-toolbar.ts source contains role="toolbar"
  </behavior>
  <action>
    1. Create `frontend/src/tests/bee-filter-toolbar.test.ts` following the pattern from `bee-header.test.ts`:
       - Same vi.mock blocks for duckdb.ts, features.ts, region-layer.ts (required because importing Lit components pulls in bee-atlas transitively)
       - describe block 'FILT-08: bee-filter-toolbar property interface' with test that imports BeeFilterToolbar and checks elementProperties has: filterState, taxaOptions, countyOptions, ecoregionOptions, collectorOptions, summary, layerMode
       - describe block 'FILT-08: bee-filter-toolbar structure' with source-scan tests: contains 'bee-filter-controls', contains 'role="toolbar"'
       - describe block 'FILT-09: csv-download event' with source-scan test: contains 'csv-download', contains 'csv-btn'

    2. Run tests — they should FAIL (RED).

    3. Create `frontend/src/bee-filter-toolbar.ts`:
       - `import { LitElement, css, html } from 'lit';`
       - `import { customElement, property } from 'lit/decorators.js';`
       - `import type { FilterState, CollectorEntry } from './filter.ts';`
       - `import type { DataSummary, TaxonOption } from './bee-sidebar.ts';`
       - `import './bee-filter-controls.ts';`
       - `@customElement('bee-filter-toolbar')`
       - `export class BeeFilterToolbar extends LitElement`
       - @property({ attribute: false }) for each: filterState (FilterState), taxaOptions (TaxonOption[]), countyOptions (string[]), ecoregionOptions (string[]), collectorOptions (CollectorEntry[]), summary (DataSummary | null), layerMode ('specimens' | 'samples')
       - Default values: taxaOptions=[], countyOptions=[], ecoregionOptions=[], collectorOptions=[], summary=null, layerMode='specimens'
       - filterState uses `!` assertion (no default — required prop)
       - static styles per UI spec:
         ```css
         :host {
           display: flex;
           align-items: center;
           width: 100%;
           padding: 0 8px;
           background: var(--surface);
           border-bottom: 1px solid var(--border);
           min-height: 48px;
           box-sizing: border-box;
         }
         bee-filter-controls { flex-grow: 1; }
         .csv-btn {
           flex-shrink: 0;
           margin-left: 8px;
           background: transparent;
           border: 1px solid var(--border-input);
           border-radius: 4px;
           padding: 6px 12px;
           font-size: 0.875rem;
           font-weight: 500;
           color: var(--text-body);
           cursor: pointer;
           min-height: 44px;
           white-space: nowrap;
         }
         .csv-btn:hover { background: var(--surface-muted); }
         .csv-btn:active { background: var(--surface-pressed); }
         ```
       - private _onCsvClick() dispatches: `new CustomEvent('csv-download', { bubbles: true, composed: true })`
       - render() returns:
         ```html
         <div role="toolbar" aria-label="Filter controls" style="display:contents">
           <bee-filter-controls
             .filterState=${this.filterState}
             .taxaOptions=${this.taxaOptions}
             .countyOptions=${this.countyOptions}
             .ecoregionOptions=${this.ecoregionOptions}
             .collectorOptions=${this.collectorOptions}
             .summary=${this.summary}
           ></bee-filter-controls>
           <button class="csv-btn" @click=${this._onCsvClick}>Download CSV</button>
         </div>
         ```

    4. Run tests — they should PASS (GREEN).
  </action>
  <verify>
    <automated>cd /Users/rainhead/dev/beeatlas/frontend && npm test -- --run src/tests/bee-filter-toolbar.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - frontend/src/bee-filter-toolbar.ts exists and contains `@customElement('bee-filter-toolbar')`
    - frontend/src/bee-filter-toolbar.ts contains `@property({ attribute: false }) filterState`
    - frontend/src/bee-filter-toolbar.ts contains `@property({ attribute: false }) taxaOptions`
    - frontend/src/bee-filter-toolbar.ts contains `@property({ attribute: false }) countyOptions`
    - frontend/src/bee-filter-toolbar.ts contains `@property({ attribute: false }) ecoregionOptions`
    - frontend/src/bee-filter-toolbar.ts contains `@property({ attribute: false }) collectorOptions`
    - frontend/src/bee-filter-toolbar.ts contains `@property({ attribute: false }) summary`
    - frontend/src/bee-filter-toolbar.ts contains `@property({ attribute: false }) layerMode`
    - frontend/src/bee-filter-toolbar.ts contains `csv-download`
    - frontend/src/bee-filter-toolbar.ts contains `role="toolbar"`
    - frontend/src/bee-filter-toolbar.ts contains `bee-filter-controls`
    - frontend/src/bee-filter-toolbar.ts contains `csv-btn`
    - frontend/src/tests/bee-filter-toolbar.test.ts exists and contains `FILT-08` and `FILT-09`
    - `cd frontend && npm test -- --run src/tests/bee-filter-toolbar.test.ts` exits 0
  </acceptance_criteria>
  <done>bee-filter-toolbar component created with all 7 properties, CSV button dispatching csv-download event, toolbar role, and all tests passing</done>
</task>

<task type="auto">
  <name>Task 2: Wire toolbar into bee-atlas, remove filter controls from sidebar, update tests</name>
  <files>frontend/src/bee-atlas.ts, frontend/src/bee-sidebar.ts, frontend/src/tests/bee-sidebar.test.ts</files>
  <read_first>
    - frontend/src/bee-atlas.ts (current render method, CSS, imports)
    - frontend/src/bee-sidebar.ts (current properties and render method — identify what to remove)
    - frontend/src/tests/bee-sidebar.test.ts (DECOMP-01 and DECOMP-04 tests to update)
    - frontend/src/bee-filter-toolbar.ts (just created in Task 1 — verify exports)
  </read_first>
  <action>
    **In bee-atlas.ts:**

    1. Add import at top: `import './bee-filter-toolbar.ts';`

    2. In render(), insert `<bee-filter-toolbar>` between `<bee-header>` (line 144) and the error/loading overlays. The exact insertion:
       ```html
       <bee-header
         .layerMode=${this._layerMode}
         .viewMode=${this._viewMode}
         @layer-changed=${this._onLayerChanged}
         @view-changed=${this._onViewChanged}
       ></bee-header>
       <bee-filter-toolbar
         .filterState=${this._filterState}
         .taxaOptions=${this._taxaOptions}
         .countyOptions=${this._countyOptions}
         .ecoregionOptions=${this._ecoregionOptions}
         .collectorOptions=${this._collectorOptions}
         .summary=${this._summary}
         .layerMode=${this._layerMode}
         @filter-changed=${this._onFilterChanged}
         @csv-download=${this._onDownloadCsv}
       ></bee-filter-toolbar>
       ```

    3. On the `<bee-sidebar>` element (currently lines 186-208), remove these property bindings:
       - `.taxaOptions=${this._taxaOptions}` (line 189)
       - `.filterState=${this._filterState}` (line 198)
       - `.countyOptions=${this._countyOptions}` (line 199)
       - `.ecoregionOptions=${this._ecoregionOptions}` (line 200)
       - `.collectorOptions=${this._collectorOptions}` (line 201)

    4. On `<bee-sidebar>`, remove the event listener:
       - `@filter-changed=${this._onFilterChanged}` (line 204)

    5. Also remove from `<bee-sidebar>`:
       - `@layer-changed=${this._onLayerChanged}` (line 205 — already handled by bee-header)
       - `@view-changed=${this._onViewChanged}` (line 206 — already handled by bee-header)

    **In bee-sidebar.ts:**

    1. Remove the import of `bee-filter-controls.ts` (the `import './bee-filter-controls.ts'` line).

    2. Remove the @property declarations that were only used to pass through to bee-filter-controls:
       - `filterState`
       - `countyOptions`
       - `ecoregionOptions`
       - `collectorOptions`
       Keep: `summary`, `filteredSummary`, `layerMode`, `viewMode`, `samples`, `selectedSampleEvent`, `recentSampleEvents`, `sampleDataLoaded`, `activeFeedEntries`, `taxaOptions` — BUT check if `taxaOptions` is used in sidebar's own rendering. If `taxaOptions` is ONLY used in the `<bee-filter-controls>` passthrough, remove it too.

    3. In the render() method, remove the `<bee-filter-controls>` element and its surrounding markup. Also remove any CSV download button markup if present in the sidebar.

    4. Remove the `@layer-changed` and `@view-changed` event dispatchers if they exist in bee-sidebar (layer toggle and view toggle are now in bee-header). Only remove if the sidebar still has these controls — if they were already removed in Phase 52, skip.

    **In bee-sidebar.test.ts:**

    1. In DECOMP-01 test block (line 53): Remove the test `expect(props.has('boundaryMode')).toBe(true)` at line 61 — this is a pre-existing failure (bee-filter-controls has no boundaryMode property).

    2. In DECOMP-04 test block (line 157): Change the test `'bee-sidebar.ts contains bee-filter-controls sub-component tag'` to assert it does NOT contain it:
       ```typescript
       test('bee-sidebar.ts does NOT contain bee-filter-controls sub-component tag', () => {
         const src = readFileSync(resolve(__dirname, '../bee-sidebar.ts'), 'utf-8');
         expect(src).not.toMatch(/bee-filter-controls/);
       });
       ```

    3. If there are tests checking sidebar has filterState, countyOptions, ecoregionOptions, or collectorOptions properties, update those tests to assert these properties are absent.
  </action>
  <verify>
    <automated>cd /Users/rainhead/dev/beeatlas/frontend && npm test -- --run</automated>
  </verify>
  <acceptance_criteria>
    - bee-atlas.ts contains `import './bee-filter-toolbar.ts'`
    - bee-atlas.ts contains `<bee-filter-toolbar` element in render
    - bee-atlas.ts contains `@filter-changed=` on bee-filter-toolbar (not on bee-sidebar)
    - bee-atlas.ts contains `@csv-download=${this._onDownloadCsv}` on bee-filter-toolbar
    - bee-atlas.ts does NOT contain `.filterState=` on bee-sidebar
    - bee-atlas.ts does NOT contain `.countyOptions=` on bee-sidebar
    - bee-atlas.ts does NOT contain `.ecoregionOptions=` on bee-sidebar
    - bee-atlas.ts does NOT contain `.collectorOptions=` on bee-sidebar
    - bee-atlas.ts does NOT contain `@filter-changed=` on bee-sidebar
    - bee-sidebar.ts does NOT contain `bee-filter-controls`
    - bee-sidebar.ts does NOT contain `filterState` as a @property
    - bee-sidebar.test.ts contains `not.toMatch(/bee-filter-controls/)` (inverted test)
    - bee-sidebar.test.ts does NOT contain `expect(props.has('boundaryMode')).toBe(true)`
    - `cd frontend && npm test -- --run` exits 0 (full suite green)
  </acceptance_criteria>
  <done>bee-filter-toolbar wired into bee-atlas between header and content; filter controls and CSV button removed from sidebar; all sidebar properties cleaned up; all tests passing</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Visual and functional verification of filter toolbar</name>
  <files>none</files>
  <action>
    Human verifies the filter toolbar is visible, functional, and the sidebar no longer contains filter controls.
    Steps:
    1. Run `cd frontend && npm run dev` and open http://localhost:5173
    2. Confirm the filter toolbar is visible below the dark header bar on page load
    3. Type a filter term (e.g., "Bombus") into the filter field in the toolbar — verify typeahead and chip creation
    4. Confirm the map updates to show only matching specimens/samples
    5. Click "Download CSV" button in the toolbar — verify a CSV file downloads
    6. Confirm the sidebar no longer shows filter controls or a CSV button
    7. Verify URL params still work: add a filter, copy the URL, open in new tab
    8. On narrow viewport (< 640px width): confirm toolbar wraps naturally
  </action>
  <verify>
    <automated>cd /Users/rainhead/dev/beeatlas/frontend && npm test -- --run</automated>
  </verify>
  <done>User confirms filter toolbar is visible, functional, CSV download works, sidebar is clean, URL params round-trip correctly</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| None new | This phase is a pure DOM restructuring within the existing frontend. No new data flows, no new inputs, no server communication changes. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-53-01 | Tampering | csv-download event | accept | Event only triggers existing _onDownloadCsv logic which queries local DuckDB WASM — no server-side risk. CSV contains only data already visible to user. |
</threat_model>

<verification>
1. `cd frontend && npm test -- --run` — full test suite passes
2. `cd frontend && npm run build` — TypeScript compilation succeeds with no errors
3. Filter toolbar visible below header on page load
4. CSV download button works from toolbar
5. Sidebar no longer contains filter controls
</verification>

<success_criteria>
- All 5 filter dimensions (taxon, year, month, county, ecoregion) accessible in the toolbar below the header
- CSV download button in toolbar triggers file download
- Sidebar contains only detail panels, recent events, and feeds
- All existing URL param round-tripping continues to work
- Full test suite green
</success_criteria>

<output>
After completion, create `.planning/phases/53-filter-toolbar/53-01-SUMMARY.md`
</output>
