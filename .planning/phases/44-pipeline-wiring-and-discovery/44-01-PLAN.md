---
phase: 44-pipeline-wiring-and-discovery
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - data/nightly.sh
  - frontend/index.html
autonomous: true
requirements:
  - PIPE-02
  - DISC-01

must_haves:
  truths:
    - "nightly.sh delegates all pipeline execution to run.py instead of inline Python"
    - "nightly.sh uploads feed XML files to S3 alongside parquet/geojson exports"
    - "Browsers visiting the site can autodiscover the determinations Atom feed"
  artifacts:
    - path: "data/nightly.sh"
      provides: "Pipeline orchestration via run.py and S3 feeds upload"
      contains: "uv run python run.py"
    - path: "frontend/index.html"
      provides: "Atom feed autodiscovery tag"
      contains: "rel=\"alternate\""
  key_links:
    - from: "data/nightly.sh"
      to: "data/run.py"
      via: "uv run python run.py invocation"
      pattern: "uv run python run\\.py"
    - from: "data/nightly.sh"
      to: "s3://BUCKET/data/feeds/"
      via: "aws s3 sync"
      pattern: "s3 sync.*feeds"
    - from: "frontend/index.html"
      to: "/data/feeds/determinations.xml"
      via: "link rel=alternate href"
      pattern: "href=\"/data/feeds/determinations\\.xml\""
---

<objective>
Wire feed generation into the nightly pipeline and add browser autodiscovery.

Purpose: Feed files generated by phases 42-43 need to reach S3 on every nightly run, and browsers need an autodiscovery tag so feed readers can find the main feed without a manual URL.

Output: Updated nightly.sh with run.py delegation and feeds upload; updated index.html with autodiscovery link tag.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/44-pipeline-wiring-and-discovery/44-CONTEXT.md

@data/nightly.sh
@data/run.py
@frontend/index.html
</context>

<tasks>

<task type="auto">
  <name>Task 1: Refactor nightly.sh to use run.py and add feeds S3 upload</name>
  <files>data/nightly.sh</files>
  <read_first>
    - data/nightly.sh (current script to be modified)
    - data/run.py (the orchestrator nightly.sh will delegate to — confirm it has all 8 steps including feeds)
    - data/feeds.py (confirm it respects EXPORT_DIR env var)
  </read_first>
  <action>
Modify data/nightly.sh as follows (per D-01, D-02, D-03, D-04):

**Step 2 (Run pipelines) — Replace lines 29-48** (the inline Python heredoc) with a single call:

```bash
~/.local/bin/uv run python run.py
```

Keep everything above unchanged: the `mkdir -p "$EXPORT_DIR"`, `export DB_PATH EXPORT_DIR`, and `cd "$SCRIPT_DIR"` lines stay exactly as they are (per D-02). The `cd "$SCRIPT_DIR"` ensures run.py is found in the working directory.

This replaces the 5-step inline Python (ecdysis, ecdysis-links, inaturalist, projects, export) with run.py's 8 steps (geographies, ecdysis, ecdysis-links, inaturalist, projects, anti-entropy, export, feeds). The divergence between nightly.sh and run.py is eliminated.

**Step 3 (Push exports) — Add feeds sync after the existing for loop** (per D-03, D-04):

After the existing `for f in ecdysis.parquet samples.parquet counties.geojson ecoregions.geojson; do ... done` block, add:

```bash
aws --profile "$AWS_PROFILE" s3 sync --no-progress "$EXPORT_DIR/feeds/" "s3://$BUCKET/data/feeds/"
```

This uploads all feed XML files and index.json. The `s3 sync` command only uploads changed files, handling the ~200+ feed files efficiently.

Do NOT modify steps 1, 4, or 5. The existing CloudFront invalidation path `/data/*` already covers `/data/feeds/*` (per D-05).

The final script structure should be:
1. Pull DuckDB from S3 (unchanged)
2. Run pipelines (single `uv run python run.py` replacing heredoc)
3. Push exports (existing parquet/geojson for loop + new feeds sync line)
4. Back up DuckDB (unchanged)
5. Invalidate CloudFront (unchanged)
  </action>
  <verify>
    <automated>grep -q 'uv run python run.py' data/nightly.sh && grep -q 's3 sync.*feeds' data/nightly.sh && ! grep -q 'python -' data/nightly.sh && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - data/nightly.sh contains the line `~/.local/bin/uv run python run.py` (no heredoc, no `python -`)
    - data/nightly.sh does NOT contain `<<'EOF'` or `<<EOF` (inline Python removed)
    - data/nightly.sh contains `export DB_PATH EXPORT_DIR` before the run.py call
    - data/nightly.sh contains `aws --profile "$AWS_PROFILE" s3 sync --no-progress "$EXPORT_DIR/feeds/" "s3://$BUCKET/data/feeds/"`
    - data/nightly.sh still contains the existing for loop with `ecdysis.parquet samples.parquet counties.geojson ecoregions.geojson`
    - data/nightly.sh still contains `cloudfront create-invalidation` with `/data/*` path
    - `bash -n data/nightly.sh` exits 0 (valid shell syntax)
  </acceptance_criteria>
  <done>nightly.sh delegates to run.py for all pipeline steps and uploads feeds to S3 via sync</done>
</task>

<task type="auto">
  <name>Task 2: Add Atom feed autodiscovery tag to index.html</name>
  <files>frontend/index.html</files>
  <read_first>
    - frontend/index.html (current HTML to be modified — see existing head structure)
  </read_first>
  <action>
Add the following line to the `<head>` section of frontend/index.html, after the existing `<link rel="icon" href="data:,">` line (line 7) and before the `<link rel="stylesheet"` line (per D-06):

```html
    <link rel="alternate" type="application/atom+xml" title="Washington Bee Atlas — All Recent Determinations" href="/data/feeds/determinations.xml">
```

This enables feed reader autodiscovery per the Atom specification. Browsers and feed readers that check for `rel="alternate" type="application/atom+xml"` will find the main determinations feed automatically.
  </action>
  <verify>
    <automated>grep -q 'rel="alternate" type="application/atom+xml".*href="/data/feeds/determinations.xml"' frontend/index.html && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - frontend/index.html contains `<link rel="alternate" type="application/atom+xml" title="Washington Bee Atlas — All Recent Determinations" href="/data/feeds/determinations.xml">`
    - The tag is inside the `<head>` element (between `<head>` and `</head>`)
    - The href is exactly `/data/feeds/determinations.xml` (absolute path, no domain)
    - The title is exactly `Washington Bee Atlas — All Recent Determinations`
  </acceptance_criteria>
  <done>index.html contains autodiscovery link tag for the Atom determinations feed</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| nightly.sh -> run.py | Shell delegates to Python orchestrator; both run as same user on maderas |
| nightly.sh -> S3 | Uploads pipeline output to public S3 bucket via AWS CLI with named profile |
| Browser -> index.html | Static HTML served to untrusted clients; link tag references static asset |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-44-01 | Tampering | nightly.sh | accept | Script runs as cron user on trusted server (maderas); no untrusted input. File permissions restrict write access. |
| T-44-02 | Information Disclosure | S3 feeds upload | accept | Feed XML contains only public data (determiner names, taxon names, collection dates) already visible on the site. No PII beyond what is already public. |
| T-44-03 | Spoofing | index.html link tag | accept | The href is a relative path to a static file on the same origin; no external URL injection vector. Content served over HTTPS via CloudFront. |
| T-44-04 | Denial of Service | s3 sync | accept | Sync is bounded by number of feed files (~200); no user-controlled input affects file count. Runs once nightly. |
| T-44-05 | Elevation of Privilege | run.py delegation | accept | run.py executes the same pipeline steps that were previously inline; no new privilege gained. Runs under same user context. |
</threat_model>

<verification>
1. `bash -n data/nightly.sh` — valid shell syntax
2. `grep 'uv run python run.py' data/nightly.sh` — run.py delegation present
3. `grep 's3 sync.*feeds' data/nightly.sh` — feeds upload present
4. `grep 'rel="alternate".*atom+xml' frontend/index.html` — autodiscovery tag present
5. No inline Python heredoc remains in nightly.sh
</verification>

<success_criteria>
- nightly.sh runs all 8 pipeline steps via run.py (geographies, ecdysis, ecdysis-links, inaturalist, projects, anti-entropy, export, feeds) instead of 5 inline steps
- Feed XML files are uploaded to S3 via `aws s3 sync` after parquet/geojson uploads
- index.html has a standards-compliant Atom autodiscovery tag pointing to `/data/feeds/determinations.xml`
- Existing nightly.sh functionality (S3 pull, parquet/geojson upload, DuckDB backup, CloudFront invalidation) is preserved
</success_criteria>

<output>
After completion, create `.planning/phases/44-pipeline-wiring-and-discovery/44-01-SUMMARY.md`
</output>
