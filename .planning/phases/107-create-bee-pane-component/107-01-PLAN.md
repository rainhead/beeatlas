---
phase: 107-create-bee-pane-component
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/bee-pane.ts
  - src/tests/bee-pane.test.ts
autonomous: true
requirements: [PANE-01, PANE-02, PANE-03, PANE-04, PANE-06, TABLE-01]
tags: [lit, web-components, presenter, frontend]

must_haves:
  truths:
    - "bee-pane.ts file exists and defines a Lit custom element registered as 'bee-pane'"
    - "bee-pane exposes paneState as an @property accepting 'collapsed' | 'list' | 'table'"
    - "Toggle button (.toggle-btn) is rendered outside all paneState conditional branches (PANE-01)"
    - "Clicking toggle dispatches pane-expand-list when collapsed, pane-collapse otherwise (PANE-02)"
    - "Expand button (.expand-btn) is rendered in list state and dispatches pane-expand-table (PANE-03)"
    - "Shrink button (.shrink-btn) is rendered in table state and dispatches pane-shrink-list (PANE-04)"
    - "CSS @media (max-aspect-ratio: 1) rule hides .expand-btn on mobile (PANE-06)"
    - "bee-table is embedded in table state with property bindings only — no event listeners that would intercept bubbling (TABLE-01)"
    - "src/tests/bee-pane.test.ts asserts all of the above via source-scan with readFileSync + toMatch"
    - "npm test passes; tsc --noEmit exits 0"
  artifacts:
    - path: "src/bee-pane.ts"
      provides: "BeePane LitElement skeleton with three-state chrome, navigation buttons, and table embedding"
      contains: "@customElement('bee-pane')"
    - path: "src/tests/bee-pane.test.ts"
      provides: "Source-scan tests for PANE-01..04, PANE-06, TABLE-01"
      contains: "readFileSync"
  key_links:
    - from: "src/bee-pane.ts"
      to: "src/bee-table.ts"
      via: "import './bee-table.ts'"
      pattern: "import.*bee-table"
    - from: "src/tests/bee-pane.test.ts"
      to: "src/bee-pane.ts"
      via: "readFileSync source scan"
      pattern: "readFileSync.*bee-pane\\.ts"
---

<objective>
Create `src/bee-pane.ts` as a new Lit custom element with the three-state pane chrome (collapsed / list / table) and the four pane-navigation events that `bee-atlas` will consume in Phase 108. Create `src/tests/bee-pane.test.ts` as a Wave 0 RED→GREEN source-scan test suite covering PANE-01..04, PANE-06, and TABLE-01.

Purpose: Establish the public contract (events, properties, CSS classes) of `bee-pane` so that Plan 02 can merge filter UI and occurrence detail into a working component without revisiting the contract. `bee-atlas` is NOT modified in this phase — `bee-pane` exists but is not yet wired (Phase 108 cutover).

Output: A registered `bee-pane` custom element with the navigation chrome and table embedding wired correctly, plus passing source-scan tests for the requirements addressed by this plan.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/107-create-bee-pane-component/107-RESEARCH.md
@.planning/phases/107-create-bee-pane-component/107-PATTERNS.md
@.planning/phases/107-create-bee-pane-component/107-UI-SPEC.md
@.planning/phases/107-create-bee-pane-component/107-VALIDATION.md

<interfaces>
<!-- Key contracts the executor needs. Extracted from RESEARCH + PATTERNS. -->

From src/bee-sidebar.ts (close event dispatch pattern — analog for pane-navigation events):
```typescript
private _onCloseClick() {
  this.dispatchEvent(new CustomEvent('close', { bubbles: true, composed: true }));
}
```

From src/bee-table.ts (events already bubble naturally — no re-dispatch in bee-pane):
- page-changed, sort-changed, row-pan, download-csv, toggle-filter — all with `bubbles: true, composed: true`

From src/bee-filter-panel.ts (imports template to mirror, lines 1–7):
```typescript
import { LitElement, css, html, nothing, type PropertyValues } from 'lit';
import { customElement, property, state } from 'lit/decorators.js';
import { isFilterActive } from './filter.ts';
import type { FilterState, CollectorEntry } from './filter.ts';
import type { DataSummary, TaxonOption, FilterChangedEvent } from './bee-sidebar.ts';
import { resolveDataUrl } from './manifest.ts';
```

Additional imports required for bee-pane (sub-components + table types):
```typescript
import './bee-occurrence-detail.ts';
import './bee-table.ts';
import type { OccurrenceRow, SpecimenSortBy } from './filter.ts';
```

bee-pane public property surface (from RESEARCH.md Pattern 1):
- paneState: 'collapsed' | 'list' | 'table' (default 'collapsed')
- filterState: FilterState
- taxaOptions, countyOptions, ecoregionOptions, collectorOptions, summary, specimenCount
- occurrences: OccurrenceRow[] | null
- rows: OccurrenceRow[], rowCount, page, loading, sortBy, filterActive, selectedIds

bee-pane events out:
- pane-collapse  — clicked toggle while in list or table
- pane-expand-list  — clicked toggle while collapsed
- pane-expand-table — clicked expand button (list only)
- pane-shrink-list — clicked shrink button (table only)
- filter-changed (Plan 02), close (NOT emitted by bee-pane — pane-collapse replaces it)

CSS breakpoint for PANE-06 (project convention, from bee-atlas.ts line 134):
```css
@media (max-aspect-ratio: 1) {
  .expand-btn { display: none; }
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wave 0 — create failing source-scan test file</name>
  <files>src/tests/bee-pane.test.ts</files>
  <read_first>
    - src/tests/bee-sidebar.test.ts (full file — copy header, vi.mock boilerplate, source-scan pattern)
    - src/tests/bee-atlas.test.ts (lines 70–110 — describe block conventions and shared-src pattern)
    - .planning/phases/107-create-bee-pane-component/107-PATTERNS.md (sections "File header, imports, and vi.mock boilerplate" and "Source-scan test structure")
    - .planning/phases/107-create-bee-pane-component/107-VALIDATION.md (Per-Task Verification Map)
  </read_first>
  <action>
    Create src/tests/bee-pane.test.ts. Header and vi.mock boilerplate are copied verbatim from src/tests/bee-sidebar.test.ts lines 1–21 (imports test/expect/describe/vi from vitest, readFileSync, resolve, dirname from node:path, fileURLToPath from node:url, __dirname computation, vi.mock for ../sqlite.ts and ../features.ts with the existing mock shapes).

    Hoist a shared source read above all describe blocks:
    `const src = readFileSync(resolve(__dirname, '../bee-pane.ts'), 'utf-8');`

    Add the following describe blocks with these exact test names and assertions. Use only source-scan assertions (`expect(src).toMatch(...)` or `expect(src).not.toMatch(...)`). All tests in this file address requirements satisfied by THIS plan (PANE-01..04, PANE-06, TABLE-01); PANE-05 tests are owned by Plan 02.

    describe('PANE-01: persistent toggle button'):
      - test 'bee-pane.ts declares a toggle-btn CSS class': expect src toMatch /\.toggle-btn\b/
      - test 'bee-pane.ts renders toggle button outside paneState conditionals' (regex strategy: assert the literal substring `class="toggle-btn"` appears in source, and that it is NOT immediately preceded within 80 chars by a `paneState === 'list'` or `paneState === 'table'` conditional — use a negative lookbehind via two assertions: first toMatch /class="toggle-btn"/, then toMatch the broader render() outer template containing it before any paneState conditional. Concretely: assert /render\(\)[\s\S]*?class="toggle-btn"[\s\S]*?paneState === 'list'/ — toggle-btn must appear in render() output before the first list-state conditional.)

    describe('PANE-02: toggle button dispatches pane-collapse / pane-expand-list'):
      - test 'bee-pane.ts contains _onToggle handler': expect src toMatch /_onToggle\s*\(/
      - test 'bee-pane.ts dispatches pane-expand-list': expect src toMatch /new CustomEvent\(['"]pane-expand-list['"]/
      - test 'bee-pane.ts dispatches pane-collapse': expect src toMatch /new CustomEvent\(['"]pane-collapse['"]/
      - test 'bee-pane.ts toggle dispatch branches on paneState collapsed': expect src toMatch /paneState === ['"]collapsed['"]/

    describe('PANE-03: expand button in list state'):
      - test 'bee-pane.ts declares expand-btn CSS class': expect src toMatch /\.expand-btn\b/
      - test 'bee-pane.ts dispatches pane-expand-table': expect src toMatch /new CustomEvent\(['"]pane-expand-table['"]/
      - test 'bee-pane.ts contains _onExpand handler': expect src toMatch /_onExpand\s*\(/

    describe('PANE-04: shrink button in table state'):
      - test 'bee-pane.ts declares shrink-btn CSS class': expect src toMatch /\.shrink-btn\b/
      - test 'bee-pane.ts dispatches pane-shrink-list': expect src toMatch /new CustomEvent\(['"]pane-shrink-list['"]/
      - test 'bee-pane.ts contains _onShrink handler': expect src toMatch /_onShrink\s*\(/

    describe('PANE-06: expand button hidden on mobile'):
      - test 'bee-pane.ts contains max-aspect-ratio:1 media query': expect src toMatch /@media[^{]*max-aspect-ratio:\s*1/
      - test 'bee-pane.ts hides expand-btn inside the mobile media query': run `src.match(/@media[^{]*max-aspect-ratio:\s*1[\s\S]*?\}\s*\}/)` (matches the @media block through its closing); on the matched block assert /\.expand-btn[\s\S]*?display:\s*none/

    describe('TABLE-01: bee-table embedded without event interception'):
      - test 'bee-pane.ts imports bee-table': expect src toMatch /import\s+['"]\.\/bee-table\.ts['"]/
      - test 'bee-pane.ts renders bee-table element': expect src toMatch /<bee-table\b/
      - test 'bee-pane.ts has no @page-changed listener on bee-table': run `src.match(/<bee-table[\s\S]*?>/)` then assert NOT /@page-changed/ on the matched element
      - test 'bee-pane.ts has no @sort-changed listener on bee-table': same approach, NOT /@sort-changed/
      - test 'bee-pane.ts has no @row-pan listener on bee-table': same approach, NOT /@row-pan/
      - test 'bee-pane.ts has no @download-csv listener on bee-table': same approach, NOT /@download-csv/
      - test 'bee-pane.ts has no @toggle-filter listener on bee-table': same approach, NOT /@toggle-filter/

    describe('sibling isolation (ARCH-03 equivalent)'):
      - test 'bee-pane.ts does not have a runtime (non-type) import of bee-atlas': expect src not toMatch /^import\s+(?!type\s).*['"]\.\/bee-atlas\.ts['"]/m
      - test 'bee-pane.ts does not have a runtime import of bee-filter-panel': expect src not toMatch /^import\s+(?!type\s).*['"]\.\/bee-filter-panel\.ts['"]/m
      - test 'bee-pane.ts does not have a runtime import of bee-sidebar runtime side-effects': expect src not toMatch /^import\s+['"]\.\/bee-sidebar\.ts['"]/m  (note: importing TYPES from bee-sidebar is allowed; `import type { DataSummary, TaxonOption, FilterChangedEvent } from './bee-sidebar.ts'` must not be flagged — the regex excludes `import type` and bare side-effect imports differ from named-type imports)

    Run `npm test -- --run` after writing the file. Tests MUST fail with "Cannot find module '../bee-pane.ts'" or equivalent — this is the RED state. Commit the failing test file with message: `test(107-01): add failing source-scan tests for bee-pane PANE-01..04, PANE-06, TABLE-01`.
  </action>
  <verify>
    <automated>cd /Users/rainhead/dev/beeatlas && npm test -- --run src/tests/bee-pane.test.ts 2>&1 | grep -E "(FAIL|Cannot find module|ENOENT)" | head -5</automated>
  </verify>
  <acceptance_criteria>
    - src/tests/bee-pane.test.ts exists
    - File contains: `readFileSync(resolve(__dirname, '../bee-pane.ts')`
    - File contains describe blocks named exactly: 'PANE-01: persistent toggle button', 'PANE-02: toggle button dispatches pane-collapse / pane-expand-list', 'PANE-03: expand button in list state', 'PANE-04: shrink button in table state', 'PANE-06: expand button hidden on mobile', 'TABLE-01: bee-table embedded without event interception', 'sibling isolation (ARCH-03 equivalent)'
    - File contains `vi.mock('../sqlite.ts'` and `vi.mock('../features.ts'` boilerplate copied from bee-sidebar.test.ts
    - `npm test -- --run src/tests/bee-pane.test.ts` exits non-zero with module-not-found error (RED state) — bee-pane.ts not yet created
    - Git commit recorded with subject `test(107-01): add failing source-scan tests for bee-pane PANE-01..04, PANE-06, TABLE-01`
  </acceptance_criteria>
  <done>Failing source-scan tests exist and committed; tests fail with module-not-found.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: GREEN — implement bee-pane skeleton (chrome, properties, navigation events, table embedding)</name>
  <files>src/bee-pane.ts</files>
  <read_first>
    - src/bee-filter-panel.ts (full file — imports, @customElement, @property/@state structure, connectedCallback/disconnectedCallback, render() outer pattern)
    - src/bee-sidebar.ts (full file — :host CSS shell, .sidebar-header / .close-btn patterns, CustomEvent dispatch pattern)
    - src/bee-atlas.ts (lines 100–250 — how bee-atlas positions and passes properties to bee-table and bee-sidebar today)
    - src/bee-table.ts (lines 1–80 — property surface and event names that must be bound via `.prop=${...}` and NOT intercepted with `@event=${...}`)
    - .planning/phases/107-create-bee-pane-component/107-PATTERNS.md (sections: Imports pattern, @customElement and @property declarations, Internal @state declarations, static styles — :host and container, connectedCallback/disconnectedCallback, Pane toggle/expand/shrink event dispatch — NEW pattern)
    - .planning/phases/107-create-bee-pane-component/107-UI-SPEC.md (Layout Contract, Interaction Contract, Copywriting Contract)
  </read_first>
  <behavior>
    - When paneState is 'collapsed', clicking the toggle button dispatches a 'pane-expand-list' CustomEvent with bubbles+composed
    - When paneState is 'list' or 'table', clicking the toggle dispatches 'pane-collapse'
    - In list state, the expand button is in the DOM and clicking it dispatches 'pane-expand-table'
    - In table state, the shrink button is in the DOM and clicking it dispatches 'pane-shrink-list'
    - The toggle button is rendered regardless of paneState (always-visible)
    - In table state, bee-table is rendered with property bindings only; no @page-changed / @sort-changed / @row-pan / @download-csv / @toggle-filter listeners are attached on the bee-table element
    - On mobile (CSS @media (max-aspect-ratio: 1)), .expand-btn has display: none
    - All source-scan tests in src/tests/bee-pane.test.ts pass
  </behavior>
  <action>
    Create src/bee-pane.ts. Do NOT yet implement the filter UI rows or occurrence detail — that is Plan 02. This task creates the skeleton chrome, the public property surface, the navigation event handlers, and the bee-table embedding.

    Imports: copy the import block listed in the <interfaces> section of this plan exactly. Also import `import './bee-occurrence-detail.ts'`, `import './bee-table.ts'`, and `import type { OccurrenceRow, SpecimenSortBy } from './filter.ts'`. Do NOT add a runtime (non-type) import of './bee-atlas.ts', './bee-filter-panel.ts', or './bee-sidebar.ts' (only `import type` from bee-sidebar.ts for DataSummary/TaxonOption/FilterChangedEvent is allowed).

    Class declaration: `@customElement('bee-pane') export class BeePane extends LitElement`. Declare all @property fields listed in the <interfaces> section. paneState default is 'collapsed'. taxaOptions, countyOptions, ecoregionOptions, collectorOptions default to empty array; rows defaults to empty array; rowCount/page default to 0/1; loading/filterActive default to false; sortBy default is 'date'; occurrences/selectedIds/summary/specimenCount default to null.

    Declare @state internals required for filter UI sync (so Plan 02 only needs to add the render methods, not re-declare state): _open, _taxonInput, _selectedTaxon, _collectorInput, _selectedCollectors, _whereInput, _selectedCounties, _selectedEcoregions, _selectedPlace, _placeNameBySlug, _placeOptions, _elevMin, _elevMax, _yearThisYear, _yearLastYear, _yearEarlier, _openSection, _suggestions, _highlightIndex. Copy declarations verbatim from bee-filter-panel.ts lines 61–91.

    static styles: declare `:host { position: absolute; z-index: 1; display: flex; flex-direction: column; background: var(--surface); box-shadow: 0 2px 8px rgba(0,0,0,0.15); font-family: system-ui, Avenir, Helvetica, Arial, sans-serif; }`. Add CSS for `.toggle-btn`, `.expand-btn`, `.shrink-btn`, `.pane-chrome`, `.table-header` using only var(--token) colors. Toggle button is 2.5rem × 2.5rem minimum. Add the mobile media query: `@media (max-aspect-ratio: 1) { .expand-btn { display: none; } }`. Filter-panel inputs/chips CSS is deferred to Plan 02 (those declarations require the corresponding render methods, which Plan 02 adds).

    connectedCallback / disconnectedCallback / _onDocumentClick: copy verbatim from bee-filter-panel.ts lines 286–294. (Used by Plan 02; declared here so Plan 02 doesn't have to revisit lifecycle wiring.)

    Navigation event handlers — implement exactly:
    - `private _onToggle()`: if `this.paneState === 'collapsed'` dispatch `new CustomEvent('pane-expand-list', { bubbles: true, composed: true })`; else dispatch `new CustomEvent('pane-collapse', { bubbles: true, composed: true })`
    - `private _onExpand()`: dispatch `new CustomEvent('pane-expand-table', { bubbles: true, composed: true })`
    - `private _onShrink()`: dispatch `new CustomEvent('pane-shrink-list', { bubbles: true, composed: true })`

    render(): outer template
    ```
    return html`
      <div class="pane-chrome">
        <button class="toggle-btn" @click=${this._onToggle}
          aria-label=${this.paneState === 'collapsed' ? 'Open filter pane' : 'Close filter pane'}
        >${this.paneState === 'collapsed' ? '⟩' : '⟨'}</button>
        ${this.paneState === 'list' ? html`
          <button class="expand-btn" @click=${this._onExpand} aria-label="Expand to table view">⊞</button>
        ` : nothing}
      </div>
      ${this.paneState === 'list' ? this._renderListContent() : nothing}
      ${this.paneState === 'table' ? this._renderTableContent() : nothing}
    `;
    ```
    The toggle button MUST appear in render() output BEFORE the first `paneState === 'list'` substring (test PANE-01 enforces this).

    Implement `_renderListContent()` as a STUB for this plan: `return html`<div class="list-placeholder">List content (Plan 02 fills in filter UI + occurrence detail)</div>`;`. Plan 02 replaces this with the merged filter UI and bee-occurrence-detail. The stub satisfies PANE-01..04, PANE-06, TABLE-01 tests; PANE-05 is owned by Plan 02.

    Implement `_renderTableContent()`:
    ```
    return html`
      <div class="table-header">
        <button class="shrink-btn" @click=${this._onShrink} aria-label="Return to list view">⊟</button>
      </div>
      <bee-table
        .rows=${this.rows}
        .rowCount=${this.rowCount}
        .page=${this.page}
        .loading=${this.loading}
        .sortBy=${this.sortBy}
        .filterActive=${this.filterActive}
        .selectedIds=${this.selectedIds}
      ></bee-table>
    `;
    ```
    Do NOT add any `@page-changed`, `@sort-changed`, `@row-pan`, `@download-csv`, or `@toggle-filter` listeners on the `<bee-table>` element — TABLE-01 source-scan tests assert their absence (they bubble naturally to bee-atlas in Phase 108).

    After saving, run `npm test -- --run src/tests/bee-pane.test.ts` and `npx tsc --noEmit`. Both MUST pass. Commit with message: `feat(107-01): create bee-pane skeleton with three-state chrome and navigation events`.
  </action>
  <verify>
    <automated>cd /Users/rainhead/dev/beeatlas && npm test -- --run src/tests/bee-pane.test.ts && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - src/bee-pane.ts exists
    - File contains `@customElement('bee-pane')` and `export class BeePane extends LitElement`
    - File contains `paneState` declared as `@property({ attribute: false })` with type union `'collapsed' | 'list' | 'table'`
    - File contains `_onToggle`, `_onExpand`, `_onShrink` private methods
    - File contains string literals `'pane-collapse'`, `'pane-expand-list'`, `'pane-expand-table'`, `'pane-shrink-list'` exactly once each (as CustomEvent first-arg)
    - File contains `.toggle-btn`, `.expand-btn`, `.shrink-btn` CSS class declarations
    - File contains `@media (max-aspect-ratio: 1)` block with `.expand-btn { display: none; }` inside it
    - File contains `<bee-table` element with property bindings (`.rows=`, `.page=`, etc.) and NO `@page-changed`, `@sort-changed`, `@row-pan`, `@download-csv`, `@toggle-filter` attributes on that element
    - File does NOT contain a runtime import of './bee-atlas.ts', './bee-filter-panel.ts', or a bare side-effect `import './bee-sidebar.ts'`
    - File contains the full list of @state declarations from bee-filter-panel.ts lines 61–91 (Plan 02 depends on these)
    - `npm test -- --run src/tests/bee-pane.test.ts` exits 0 (all tests in this file pass — GREEN)
    - `npx tsc --noEmit` exits 0
    - Git commit recorded with subject `feat(107-01): create bee-pane skeleton with three-state chrome and navigation events`
  </acceptance_criteria>
  <done>bee-pane.ts compiles, all source-scan tests for PANE-01..04, PANE-06, TABLE-01 pass, npm test full suite still green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user → DOM | Click events on toggle/expand/shrink buttons are user-controlled but bound to fixed dispatch logic; no user-provided data crosses |
| bee-atlas → bee-pane (properties) | bee-atlas passes filterState, occurrences, rows etc. as Lit properties. bee-pane is a pure presenter and does not execute or eval any property value |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-107-01-01 | Tampering | bee-table event interception in bee-pane | mitigate | Source-scan tests assert no @page-changed/@sort-changed/@row-pan/@download-csv/@toggle-filter listeners on the `<bee-table>` element — bubbling integrity preserved (TABLE-01) |
| T-107-01-02 | Information disclosure | none | accept | bee-pane skeleton renders no occurrence data in this plan; placeholder stub only |
| T-107-01-03 | Elevation of privilege | none | accept | No code paths execute user-supplied strings; no innerHTML; Lit html`` tagged template auto-escapes interpolations |
| T-107-01-SC | Tampering | npm/pip/cargo installs | accept | This plan installs no new packages — pure source-file creation against existing toolchain (Lit 3.2.1, Vitest 4.1.2, TypeScript 5.8.2 — all verified in package.json) |
</threat_model>

<verification>
- `npm test -- --run src/tests/bee-pane.test.ts` passes (all PANE-01..04, PANE-06, TABLE-01 source-scan tests GREEN)
- `npm test` full suite still passes (no other test file should break)
- `npx tsc --noEmit` exits 0
- Manual smoke: `npm run dev` starts the dev server without compile errors (bee-pane is not yet rendered in the UI; Phase 108 wires it)
- Git log shows two commits with subjects matching `test(107-01):` (RED) and `feat(107-01):` (GREEN)
</verification>

<success_criteria>
- src/bee-pane.ts defines `bee-pane` custom element with three-state chrome and navigation event dispatch
- src/tests/bee-pane.test.ts provides source-scan coverage for PANE-01, PANE-02, PANE-03, PANE-04, PANE-06, TABLE-01
- bee-atlas.ts is unchanged in this phase (Phase 108 cutover)
- bee-filter-panel.ts and bee-sidebar.ts are unchanged (Phase 109 deletion)
- npm test green, tsc --noEmit green
</success_criteria>

<output>
Create `.planning/phases/107-create-bee-pane-component/107-01-SUMMARY.md` when done. Document:
- Final list of @property fields on BeePane
- Final list of @state fields declared (passed forward to Plan 02)
- Confirmation that bee-table is embedded without event interception
- Any deviation from the planned CSS class names (must be reflected in Plan 02 if so)
</output>
