---
phase: 107-create-bee-pane-component
plan: 02
type: execute
wave: 2
depends_on: ["107-01"]
files_modified:
  - src/bee-pane.ts
  - src/tests/bee-pane.test.ts
autonomous: true
requirements: [PANE-05]
tags: [lit, web-components, filter-ui, occurrence-detail, frontend]

must_haves:
  truths:
    - "In list state, bee-pane renders all four filter section rows (What/Who/Where/When) using the same UX as bee-filter-panel"
    - "In list state, when occurrences is non-null, bee-pane renders bee-occurrence-detail below the filter rows"
    - "In list state, when occurrences is null, bee-pane renders a placeholder hint (matching bee-sidebar's empty state)"
    - "bee-pane emits filter-changed CustomEvent with the same FilterChangedEvent detail shape as bee-filter-panel"
    - "updated(changed: PropertyValues) keeps internal @state filter fields in sync with the externally provided filterState"
    - "All bee-filter-panel filter behaviors (taxon suggestion, collector multi-select, where multi-select with counties/ecoregions/places, elevation min/max, year buckets) function inside bee-pane"
    - "_ensurePlaceNamesLoaded() lazy-fetches place metadata from the existing manifest URL"
    - "PANE-05 source-scan tests pass; all earlier PANE-01..04, PANE-06, TABLE-01 tests remain green"
    - "npm test passes; tsc --noEmit exits 0"
  artifacts:
    - path: "src/bee-pane.ts"
      provides: "Complete bee-pane with merged filter UI + occurrence detail + table"
      contains: "_renderWhat"
    - path: "src/tests/bee-pane.test.ts"
      provides: "Source-scan tests extended with PANE-05 coverage"
      contains: "PANE-05"
  key_links:
    - from: "src/bee-pane.ts"
      to: "src/bee-occurrence-detail.ts"
      via: "import './bee-occurrence-detail.ts' (already in Plan 01 imports) and `<bee-occurrence-detail .occurrences=${...}>` in list render"
      pattern: "<bee-occurrence-detail"
    - from: "src/bee-pane.ts"
      to: "src/filter.ts"
      via: "FilterChangedEvent dispatch in _emitFilter"
      pattern: "filter-changed"
    - from: "src/bee-pane.ts"
      to: "src/manifest.ts"
      via: "resolveDataUrl('places_meta') inside _ensurePlaceNamesLoaded"
      pattern: "resolveDataUrl\\(['\"]places_meta"
---

<objective>
Complete `src/bee-pane.ts` by merging the filter UI rows (What/Who/Where/When) from `bee-filter-panel.ts` and the occurrence detail shell from `bee-sidebar.ts` into the list-state content of the pane created in Plan 01. Extend `src/tests/bee-pane.test.ts` with PANE-05 source-scan assertions.

Purpose: Satisfy PANE-05 (list state shows all filter controls and occurrence detail on selection) and finalize bee-pane so Phase 108 can swap it in for the old `bee-filter-panel` + `bee-sidebar` pair. All filter behaviors must function identically to bee-filter-panel.

Output: A complete `bee-pane` component with working filter UI, occurrence detail rendering, and full source-scan + integration test coverage.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/107-create-bee-pane-component/107-RESEARCH.md
@.planning/phases/107-create-bee-pane-component/107-PATTERNS.md
@.planning/phases/107-create-bee-pane-component/107-UI-SPEC.md
@.planning/phases/107-create-bee-pane-component/107-VALIDATION.md
@.planning/phases/107-create-bee-pane-component/107-01-SUMMARY.md

<interfaces>
<!-- Outputs from Plan 01 — bee-pane skeleton already has these in place. -->

Plan 01 already created in src/bee-pane.ts:
- All @property fields including filterState, occurrences, taxaOptions/countyOptions/ecoregionOptions/collectorOptions, summary, specimenCount, rows/rowCount/page/loading/sortBy/filterActive/selectedIds
- All @state internal filter fields (_open, _taxonInput, _selectedTaxon, _collectorInput, _selectedCollectors, _whereInput, _selectedCounties, _selectedEcoregions, _selectedPlace, _placeNameBySlug, _placeOptions, _elevMin, _elevMax, _yearThisYear, _yearLastYear, _yearEarlier, _openSection, _suggestions, _highlightIndex)
- connectedCallback / disconnectedCallback / _onDocumentClick (filter dropdown close-on-outside-click)
- Outer render() with _renderListContent() called for list state — but currently returning the placeholder div

What this plan adds:
- year-bucket helper functions (yearBucketsToFilter, filterToYearBuckets) — copy from bee-filter-panel.ts lines 10–36
- Suggestion type aliases (TaxonSug, CollectorSug, WhereSug, AnyS) — bee-filter-panel.ts lines 40–43
- updated(changed: PropertyValues) — bee-filter-panel.ts lines 314–363
- _emitFilter() — bee-filter-panel.ts lines 365–385
- All input handlers: _handleKeydown (397–424), _pickSuggestion (426–431), _onBlur (433–435)
- Taxon section: _onTaxonInput, _selectTaxon, _clearTaxon (439–475)
- Collector section: _onCollectorInput, _selectCollector, _removeCollector (479–523)
- Where section: _onWhereInput, _selectWhere, _removeCounty, _removeEcoregion, _removePlace (527–595)
- _ensurePlaceNamesLoaded (597–620)
- Elevation handlers: _onElevMinInput, _onElevMaxInput (624–634)
- Render methods: _renderWhat (638–683), _renderWho (686–735), _renderWhere (737–822), _renderWhen (824–853)
- CSS rules for filter inputs, chips, suggestions — bee-filter-panel.ts lines 104–284
- Sidebar header CSS (.sidebar-header, .sidebar-title, .close-btn) — bee-sidebar.ts lines 77–103
- Replaces _renderListContent() stub with: sidebar-header (label "Filters"), filter rows wrapper, divider, bee-occurrence-detail (when occurrences non-null) or hint placeholder

FilterChangedEvent detail shape (must match exactly so bee-atlas handlers work in Phase 108):
```typescript
{
  taxonName: string | null,
  taxonRank: 'family' | 'genus' | 'species' | null,
  yearFrom: number | null,
  yearTo: number | null,
  months: Set<number>,  // always new Set() — months not in pane UI
  selectedCounties: Set<string>,
  selectedEcoregions: Set<string>,
  selectedCollectors: CollectorEntry[],
  elevMin: number | null,
  elevMax: number | null,
  selectedPlace: string | null,
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wave 0 — extend test file with PANE-05 source-scan assertions (RED)</name>
  <files>src/tests/bee-pane.test.ts</files>
  <read_first>
    - src/tests/bee-pane.test.ts (current state from Plan 01 — extend in place, do not rewrite)
    - src/tests/bee-sidebar.test.ts (lines 30–100 — patterns for asserting rendered structure via source scan)
    - src/bee-filter-panel.ts (lines 638–853 — render method signatures and the CSS class names like .filter-row, .filter-section, .chip)
    - src/bee-sidebar.ts (lines 113–125 — sidebar-header / bee-occurrence-detail conditional rendering pattern)
  </read_first>
  <action>
    Append the following describe blocks to src/tests/bee-pane.test.ts. The shared `const src = readFileSync(...)` declaration at the top of the file remains; the new tests reuse it.

    describe('PANE-05: list state filter controls + occurrence detail'):
      - test 'bee-pane.ts defines _renderWhat method': expect src toMatch /_renderWhat\s*\(/
      - test 'bee-pane.ts defines _renderWho method': expect src toMatch /_renderWho\s*\(/
      - test 'bee-pane.ts defines _renderWhere method': expect src toMatch /_renderWhere\s*\(/
      - test 'bee-pane.ts defines _renderWhen method': expect src toMatch /_renderWhen\s*\(/
      - test 'bee-pane.ts calls all four filter render methods inside _renderListContent': match _renderListContent body with `src.match(/_renderListContent\s*\([^)]*\)[^{]*\{[\s\S]*?\n\s{0,4}\}/)` then assert the matched block contains /this\._renderWhat\s*\(\)/, /this\._renderWho\s*\(\)/, /this\._renderWhere\s*\(\)/, /this\._renderWhen\s*\(\)/
      - test 'bee-pane.ts renders bee-occurrence-detail when occurrences non-null in list content': match _renderListContent body as above and assert it contains /<bee-occurrence-detail[\s\S]*?\.occurrences=\$\{this\.occurrences\}/ guarded by /occurrences\s*!==\s*null/ or /occurrences\s*\?/
      - test 'bee-pane.ts dispatches filter-changed event': expect src toMatch /new CustomEvent[^)]*['"]filter-changed['"]/
      - test 'bee-pane.ts implements updated(changed: PropertyValues) sync': expect src toMatch /updated\s*\(\s*changed\s*:\s*PropertyValues/
      - test 'bee-pane.ts contains _ensurePlaceNamesLoaded with resolveDataUrl call': expect src toMatch /_ensurePlaceNamesLoaded/ and src toMatch /resolveDataUrl\(['"]places_meta['"]\)/
      - test 'bee-pane.ts FilterChangedEvent detail contains all required fields': match the _emitFilter body and assert it contains the strings 'taxonName', 'taxonRank', 'yearFrom', 'yearTo', 'months', 'selectedCounties', 'selectedEcoregions', 'selectedCollectors', 'elevMin', 'elevMax', 'selectedPlace' (one assertion per field)
      - test 'bee-pane.ts list content stub is removed': expect src not toMatch /List content \(Plan 02 fills in/

    Run `npm test -- --run src/tests/bee-pane.test.ts`. The new PANE-05 tests MUST fail (RED) because _renderWhat/Who/Where/When and the wired _renderListContent do not yet exist. Earlier PANE-01..04, PANE-06, TABLE-01 tests MUST still pass.

    Commit with message: `test(107-02): add failing PANE-05 source-scan tests for bee-pane filter UI`.
  </action>
  <verify>
    <automated>cd /Users/rainhead/dev/beeatlas && npm test -- --run src/tests/bee-pane.test.ts 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - src/tests/bee-pane.test.ts contains describe block 'PANE-05: list state filter controls + occurrence detail'
    - File contains tests asserting _renderWhat, _renderWho, _renderWhere, _renderWhen presence
    - File contains assertion `resolveDataUrl(['"]places_meta['"])` in source
    - File contains assertion that 'List content (Plan 02 fills in' substring is absent
    - `npm test -- --run src/tests/bee-pane.test.ts` shows: PANE-01..04, PANE-06, TABLE-01 tests passing; PANE-05 tests failing
    - Git commit recorded with subject `test(107-02): add failing PANE-05 source-scan tests for bee-pane filter UI`
  </acceptance_criteria>
  <done>PANE-05 tests are present and failing; prior tests still passing.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: GREEN — merge filter UI rows + occurrence detail into bee-pane list state</name>
  <files>src/bee-pane.ts</files>
  <read_first>
    - src/bee-pane.ts (current state from Plan 01 — extend in place)
    - src/bee-filter-panel.ts (lines 10–36 year-bucket helpers; 40–43 suggestion types; 104–284 CSS; 314–435 updated/_emitFilter/_handleKeydown/_pickSuggestion/_onBlur; 439–634 all section handlers + _ensurePlaceNamesLoaded; 638–853 _renderWhat/Who/Where/When)
    - src/bee-sidebar.ts (lines 77–125 — sidebar-header CSS, render shell with bee-occurrence-detail)
    - .planning/phases/107-create-bee-pane-component/107-PATTERNS.md (sections: Filter section render methods, _ensurePlaceNamesLoaded, render() — three-state template)
    - .planning/phases/107-create-bee-pane-component/107-UI-SPEC.md (Layout Contract → Filter Panel — List State, Occurrence Detail — List State)
  </read_first>
  <behavior>
    - In list state, the rendered DOM contains the four filter rows (What/Who/Where/When) with identical UI to bee-filter-panel
    - Typing in the taxon input filters taxaOptions and shows a suggestion dropdown identical to bee-filter-panel
    - Selecting a taxon/collector/county/ecoregion/place updates internal @state and triggers _emitFilter
    - _emitFilter dispatches CustomEvent('filter-changed', { bubbles: true, composed: true, detail: FilterChangedEvent }) with all 11 detail fields
    - On filterState property change from parent, updated() syncs each internal @state field; no stale chips
    - When occurrences is non-null, bee-occurrence-detail renders below the filter rows
    - When occurrences is null, a hint placeholder ("Click a point on the map to see details.") renders below the filter rows
    - All PANE-05 source-scan tests pass; PANE-01..04, PANE-06, TABLE-01 tests still pass
  </behavior>
  <action>
    Extend src/bee-pane.ts (do not rewrite — append/replace specific blocks). Pattern: copy-merge from bee-filter-panel.ts and bee-sidebar.ts. The PATTERNS.md document lists exact line ranges to copy.

    Add at top-of-file scope (before the @customElement class):
    - Year-bucket helpers `yearBucketsToFilter` and `filterToYearBuckets` (bee-filter-panel.ts lines 10–36) — copy verbatim
    - Suggestion type aliases TaxonSug, CollectorSug, WhereSug, AnyS (lines 40–43) — copy verbatim

    Inside the class:
    - Extend static styles by appending the filter-panel CSS rules from bee-filter-panel.ts lines 104–284 (all `.filter-panel`, `.filter-section`, `.filter-row`, `.taxon-input`, `.collector-input`, `.where-input`, `.chip`, `.suggestion`, `.suggestion-list`, `.elev-input`, `.year-bucket`, etc.). Also append sidebar-header CSS from bee-sidebar.ts lines 77–103 (.sidebar-header, .sidebar-title, .close-btn — though close-btn is NOT used in bee-pane since toggle replaces it; the sidebar-title class is reused for the "Filters" label). Keep all CSS using `var(--token)`.
    - Add `updated(changed: PropertyValues)` — copy verbatim from bee-filter-panel.ts lines 314–363
    - Add `_emitFilter()` — copy verbatim from bee-filter-panel.ts lines 365–385. The FilterChangedEvent detail must contain exactly: taxonName, taxonRank, yearFrom, yearTo, months (always `new Set<number>()`), selectedCounties, selectedEcoregions, selectedCollectors, elevMin, elevMax, selectedPlace
    - Add `_handleKeydown`, `_pickSuggestion`, `_onBlur` (lines 397–435)
    - Add taxon handlers: `_onTaxonInput`, `_selectTaxon`, `_clearTaxon` (lines 439–475)
    - Add collector handlers: `_onCollectorInput`, `_selectCollector`, `_removeCollector` (lines 479–523)
    - Add where handlers: `_onWhereInput`, `_selectWhere`, `_removeCounty`, `_removeEcoregion`, `_removePlace` (lines 527–595)
    - Add `_ensurePlaceNamesLoaded()` (lines 597–620) — copy verbatim
    - Add elevation handlers: `_onElevMinInput`, `_onElevMaxInput` (lines 624–634)
    - Add `_renderWhat()`, `_renderWho()`, `_renderWhere()`, `_renderWhen()` (lines 638–853) — copy verbatim

    Replace the `_renderListContent()` placeholder with:
    ```
    private _renderListContent() {
      return html`
        <div class="sidebar-header">
          <span class="sidebar-title">Filters</span>
        </div>
        <div class="filter-panel">
          ${this._renderWhat()}
          ${this._renderWho()}
          ${this._renderWhere()}
          ${this._renderWhen()}
        </div>
        <div class="divider"></div>
        ${this.occurrences !== null
          ? html`<bee-occurrence-detail .occurrences=${this.occurrences}></bee-occurrence-detail>`
          : html`<div class="panel-content"><p class="hint">Click a point on the map to see details.</p></div>`
        }
      `;
    }
    ```
    Add CSS rules for `.divider` (1px var(--border-subtle)), `.panel-content` and `.hint` (matching bee-sidebar.ts equivalents).

    Notes:
    - Do NOT add a close button or `close` event — the toggle button handles collapse via `pane-collapse`
    - Do NOT add the floating filter trigger button from bee-filter-panel (`.filter-btn`) — the pane itself is the persistent filter surface (UI-SPEC says so); keep `_open` @state and document-click handler because Plan 01 declared them, but the filter-panel rows render unconditionally inside _renderListContent (no `_open` gating). If the analog `_renderWhat/Who/Where/When` methods reference `this._open`, set `this._open = true` once in `connectedCallback` so the existing render logic continues to work without rewiring (this preserves verbatim copies of the render methods).
    - Confirm: no runtime side-effect imports of bee-atlas.ts / bee-filter-panel.ts / bee-sidebar.ts were added

    After saving, run `npm test -- --run` (full suite) and `npx tsc --noEmit`. Both MUST pass. Commit: `feat(107-02): merge filter UI rows and occurrence detail into bee-pane list state`.
  </action>
  <verify>
    <automated>cd /Users/rainhead/dev/beeatlas && npm test -- --run && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - src/bee-pane.ts contains private methods named `_renderWhat`, `_renderWho`, `_renderWhere`, `_renderWhen`, `_emitFilter`, `updated`, `_ensurePlaceNamesLoaded`
    - src/bee-pane.ts contains string literal `'filter-changed'` as a CustomEvent first argument
    - src/bee-pane.ts contains `resolveDataUrl('places_meta')`
    - The `_renderListContent` method body invokes `this._renderWhat()`, `this._renderWho()`, `this._renderWhere()`, `this._renderWhen()`
    - The `_renderListContent` method body contains `<bee-occurrence-detail .occurrences=${this.occurrences}>` guarded by an `occurrences !== null` check
    - The "List content (Plan 02 fills in" placeholder string is no longer present
    - All FilterChangedEvent detail keys present in _emitFilter: taxonName, taxonRank, yearFrom, yearTo, months, selectedCounties, selectedEcoregions, selectedCollectors, elevMin, elevMax, selectedPlace
    - `npm test -- --run` exits 0 (full suite green, including all PANE-01..06 and TABLE-01 tests)
    - `npx tsc --noEmit` exits 0
    - Git commit recorded with subject `feat(107-02): merge filter UI rows and occurrence detail into bee-pane list state`
  </acceptance_criteria>
  <done>bee-pane is feature-complete for Phase 107; full test suite green; tsc clean; Phase 108 can begin cutover.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user → filter inputs | User-typed taxon/collector/where strings are stored in @state and used as suggestion-filter inputs and inside Lit html`` templates (auto-escaped) |
| bee-pane → manifest.ts (network fetch) | `_ensurePlaceNamesLoaded` fetches places_meta JSON over HTTPS via the existing resolveDataUrl path |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-107-02-01 | Tampering | filter-changed event detail shape | mitigate | Source-scan test asserts all 11 detail fields by name; if Phase 108 wiring fails because of a shape mismatch, the test would have caught it pre-cutover |
| T-107-02-02 | Information disclosure | places_meta fetch | accept | Same data source already used by bee-filter-panel.ts (no change in network surface); silent-catch on fetch failure preserved verbatim |
| T-107-02-03 | XSS via filter input | _renderWhat/Who/Where/When | mitigate | Lit html`` tagged templates auto-escape interpolated values; no innerHTML used; all user input flows through `${...}` text interpolation |
| T-107-02-04 | Repudiation | n/a | accept | No logging surface introduced |
| T-107-02-SC | Tampering | npm/pip/cargo installs | accept | No new packages installed; copy-merge from existing files only |
</threat_model>

<verification>
- `npm test` full suite passes (all source-scan tests for PANE-01..06 and TABLE-01)
- `npx tsc --noEmit` exits 0
- `npm run build` completes successfully (Eleventy + Vite)
- Manual dev smoke (deferred to Phase 108 UAT): `npm run dev`; bee-pane is registered but not yet rendered in bee-atlas (expected)
- Git log shows commits: `test(107-02):` (RED) and `feat(107-02):` (GREEN)
</verification>

<success_criteria>
- src/bee-pane.ts is feature-complete: chrome (Plan 01) + filter UI rows + occurrence detail conditional (Plan 02)
- Filter behaviors match bee-filter-panel exactly (verbatim copy of render methods and handlers)
- FilterChangedEvent detail shape matches bee-filter-panel exactly
- npm test green; tsc --noEmit green; npm run build green
- Phase 108 unblocked: bee-atlas can swap bee-filter-panel + bee-sidebar for bee-pane
</success_criteria>

<output>
Create `.planning/phases/107-create-bee-pane-component/107-02-SUMMARY.md` when done. Document:
- Total line count of src/bee-pane.ts
- Confirmation of verbatim-copy provenance for each block (with bee-filter-panel.ts line ranges or bee-sidebar.ts line ranges)
- Confirmation that no runtime side-effect imports of bee-atlas.ts / bee-filter-panel.ts / bee-sidebar.ts were introduced
- FilterChangedEvent detail key list confirmed present
- Open issues for Phase 108 cutover, if any
</output>
