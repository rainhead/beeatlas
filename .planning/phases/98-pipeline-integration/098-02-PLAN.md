---
phase: 98-pipeline-integration
plan: 02
type: execute
wave: 2
depends_on:
  - 098-01
files_modified:
  - data/places_export.py
  - data/run.py
  - data/tests/test_places_export.py
  - .gitignore
  - public/data/places.geojson
  - public/data/places.json
autonomous: false
requirements:
  - PPIPE-04
  - PPIPE-05

must_haves:
  truths:
    - "Running the pipeline produces public/data/places.geojson — a GeoJSON FeatureCollection whose features each have a `slug` property and a GeoJSON geometry"
    - "Running the pipeline produces public/data/places.json — a flat JSON array of {slug, name, land_owner, permits, specimen_count, sample_count} records"
    - "specimen_count and sample_count are derived from public/data/occurrences.parquet via place_slug, NOT from data/dbt/target/sandbox (Pitfall 5)"
    - "places_export step is wired into run.py STEPS AFTER dbt-build (it requires occurrences.parquet with place_slug from plan 01)"
    - ".gitignore allows public/data/places.geojson and public/data/places.json to be tracked despite the blanket /public/data/ ignore"
    - "Both files are committed to git (`git ls-files` shows them) so `npm run build` succeeds on a clean CI checkout without running the pipeline"
    - "pytest passes for places.geojson FeatureCollection structure and places.json record-shape assertions"
  artifacts:
    - path: "data/places_export.py"
      provides: "export_places() main producing places.geojson + places.json; export_places_step() zero-arg wrapper for STEPS"
      exports: ["export_places", "export_places_step"]
    - path: "data/run.py"
      provides: "places-export step wired after species-maps (and after dbt-build) in STEPS"
      contains: "(\"places-export\", export_places_step)"
    - path: ".gitignore"
      provides: "Negation rules for places.geojson and places.json placed AFTER the /public/data/ blanket ignore (Pitfall 4)"
      contains: "!/public/data/places.geojson"
    - path: "public/data/places.geojson"
      provides: "Compact GeoJSON FeatureCollection (slug + geometry only) suitable for Mapbox source with promoteId: 'slug'"
      contains: "FeatureCollection"
    - path: "public/data/places.json"
      provides: "Pretty-printed JSON array with all place metadata + specimen/sample counts (no geometry)"
      contains: "specimen_count"
    - path: "data/tests/test_places_export.py"
      provides: "pytest unit tests for places.geojson structure and places.json record shape"
      exports: ["test_places_geojson_structure", "test_places_json_structure", "test_places_json_counts"]
  key_links:
    - from: "data/run.py"
      to: "data/places_export.py"
      via: "from places_export import export_places_step"
      pattern: "places_export"
    - from: "data/places_export.py"
      to: "public/data/occurrences.parquet"
      via: "read_parquet for specimen_count/sample_count aggregation"
      pattern: "read_parquet"
    - from: "data/places_export.py"
      to: "content/places.toml"
      via: "tomllib.load to source permits + metadata (TOML is single source of truth for permits)"
      pattern: "tomllib"
    - from: ".gitignore"
      to: "public/data/places.geojson + public/data/places.json"
      via: "negation rules below the blanket ignore"
      pattern: "!/public/data/places"
---

<objective>
Produce the two place-data artifacts the frontend needs and commit them to git so CI succeeds
without running the pipeline:
  - `public/data/places.geojson` — slim GeoJSON FeatureCollection (slug + geometry) for Mapbox
  - `public/data/places.json`    — rich JSON array (name, owner, permits, counts) for Eleventy

Wire `places-export` into run.py STEPS after dbt-build so it can read `place_slug` out of
`public/data/occurrences.parquet`. Add the .gitignore negation rules so the artifacts can be
committed. Run the pipeline once to produce them, then commit (PPIPE-05). A human checkpoint
gates the final commit.

Purpose: PPIPE-04 + PPIPE-05 — the data the Eleventy place pages (Phase 99) and Mapbox places
boundary layer (Phase 100) both consume.

Output: data/places_export.py + run.py wiring + test_places_export.py + .gitignore negation rules
+ committed places.geojson/places.json artifacts.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/98-pipeline-integration/98-RESEARCH.md
@.planning/phases/98-pipeline-integration/98-PATTERNS.md
@.planning/phases/98-pipeline-integration/98-VALIDATION.md
@.planning/phases/98-pipeline-integration/098-01-SUMMARY.md
@content/places.toml
@data/species_export.py
@data/run.py
@data/places_validation.py
@.gitignore

<interfaces>
<!-- Key code surfaces. Extracted from codebase. -->

From data/species_export.py:
  Module-level env-var pattern (lines 33-44):
    DB_PATH = os.environ.get('DB_PATH', str(Path(__file__).parent / 'beeatlas.duckdb'))
    _default_assets = str(Path(__file__).parent.parent / 'public' / 'data')
    ASSETS_DIR = Path(os.environ.get('EXPORT_DIR', _default_assets))
  CRITICAL DIVERGENCE: species_export.py reads occurrences from DBT_SANDBOX_DIR.
  places_export.py MUST read from ASSETS_DIR / "occurrences.parquet" (the copy made by
  _run_dbt_build) — RESEARCH.md Pitfall 5. Reading from sandbox risks stale data.

From data/run.py (current STEPS after Plan 01 applies):
  places-validation -> places-load -> dbt-build -> topology-postprocess ->
  species-export -> species-maps -> feeds
  Insert (`places-export`, export_places_step) AFTER species-maps and BEFORE feeds.
  Plan 03 will insert (`places-maps`, ...) AFTER places-export.

From content/places.toml:
  Each [[places]] entry has: slug, name, land_owner, geometry_wkt (multi-line, needs .strip()),
  permits (array of inline tables with issuing_authority, optional permit_number, optional
  expiry_date, type)

From .gitignore:
  Line 140: *.parquet
  Line 141: /public/data/
  -> Negations MUST be appended AFTER line 141 (Pitfall 4 — order matters).

From data/dbt/macros/emit_feature_collection.sql (referenced for byte-stable structure):
  Compact JSON with separators=(',', ':') matches counties.geojson / ecoregions.geojson
  pattern. places.geojson is a Mapbox source — use compact, not pretty.

places.json format choice: pretty-printed indent=2 matches species.json (Eleventy _data
consumer convention).

OCCURRENCES PARQUET column reference (from data/dbt/models/marts/schema.yml after Plan 01):
  Relevant columns for counts: is_provisional BOOLEAN, sample_id INTEGER, place_slug VARCHAR
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write failing pytest stubs for places_export</name>
  <files>data/tests/test_places_export.py</files>
  <read_first>
    - data/tests/test_species_export.py (monkeypatch ASSETS_DIR pattern; tmp_path-based isolation)
    - data/tests/test_places_validation.py (write_toml helper; _VALID_PLACE shape)
    - .planning/phases/98-pipeline-integration/98-PATTERNS.md (Pattern: places_export tests)
    - content/places.toml (real shape — permits inline tables)
  </read_first>
  <behavior>
    - test_places_geojson_structure: monkeypatch `places_export.ASSETS_DIR` to tmp_path; pre-seed an in-memory DuckDB with `geographies.places` containing one row; pre-seed a small parquet at `tmp_path/occurrences.parquet` (or skip counts and just call the geojson writer if it accepts an explicit con); call the export function; assert places.geojson is a FeatureCollection with one feature whose properties has key `slug` and geometry has type Polygon/MultiPolygon
    - test_places_json_structure: same setup; call export; assert places.json is a JSON list, len == 1, record has keys {slug, name, land_owner, permits, specimen_count, sample_count}
    - test_places_json_counts: with a known parquet (one row inside slug='test-place', is_provisional=false, sample_id=42; one row place_slug=NULL), assert specimen_count == 1 and sample_count == 1 for test-place
    - All tests MUST fail with ModuleNotFoundError on `import places_export` initially
  </behavior>
  <action>
Create `data/tests/test_places_export.py`:
1. Module docstring: "Tests for places_export.py — places.geojson FeatureCollection structure and places.json record/count shape (PPIPE-04)."
2. Imports: `import json`, `import pytest`, `import duckdb`, `import pyarrow as pa`, `import pyarrow.parquet as pq`, `from pathlib import Path`.
3. Helper `_seed_places_db(db_path)`: connect to db_path, `LOAD spatial`, create schema `geographies`, create table `geographies.places (slug VARCHAR, name VARCHAR, land_owner VARCHAR, geom GEOMETRY)`, insert one row `('test-place', 'Test Place', 'DNR', ST_GeomFromText('POLYGON((-121.0 47.0, -120.9 47.0, -120.9 47.1, -121.0 47.1, -121.0 47.0))'))`.
4. Helper `_write_test_toml(tmp_path)`: write a content/places.toml-shaped file containing exactly the `test-place` entry with one permit `[{issuing_authority='DNR', type='project-level'}]`. Return path.
5. Helper `_write_test_occurrences_parquet(tmp_path)`: build a pyarrow Table with columns `place_slug` (VARCHAR), `is_provisional` (BOOLEAN), `sample_id` (INT64) and 3 rows: `('test-place', False, 42)`, `('test-place', False, 42)` (same sample), `(None, False, 99)` (outside). Write to `tmp_path / "occurrences.parquet"`.
6. `test_places_geojson_structure(tmp_path, monkeypatch)`:
   - Set `monkeypatch.setenv("DB_PATH", str(tmp_path / "test.duckdb"))`
   - Set `monkeypatch.setenv("EXPORT_DIR", str(tmp_path))`
   - `import places_export` (after env set so module-level constants pick up tmp values; use `importlib.reload(places_export)` if necessary)
   - `_seed_places_db(tmp_path / "test.duckdb")`; `_write_test_occurrences_parquet(tmp_path)`; symlink/copy the TOML via `monkeypatch.setattr(places_export, "_PLACES_TOML_PATH", _write_test_toml(tmp_path))` (Task 2 must expose a module-level overridable path constant for testability)
   - Call `places_export.export_places_step()` (or the underlying main)
   - Read `(tmp_path / "places.geojson").read_text()`, parse as JSON, assert top-level `type == "FeatureCollection"`, `len(features) == 1`, `features[0]["properties"]["slug"] == "test-place"`, `features[0]["geometry"]["type"] == "Polygon"`
7. `test_places_json_structure(tmp_path, monkeypatch)`: same setup; assert places.json is a list, len 1, the record has all 6 required keys, types match (specimen_count int, sample_count int, permits list).
8. `test_places_json_counts(tmp_path, monkeypatch)`: same setup; assert `record["specimen_count"] == 2` (two test-place rows, both non-provisional) and `record["sample_count"] == 1` (DISTINCT sample_id=42).

Initial expected failure: `cd data && uv run pytest tests/test_places_export.py -x` fails with ImportError/ModuleNotFoundError on `places_export`.
  </action>
  <verify>
    <automated>cd data && uv run pytest tests/test_places_export.py -x 2>&1 | grep -E "ModuleNotFoundError|ImportError|No module named 'places_export'"</automated>
  </verify>
  <acceptance_criteria>
    - data/tests/test_places_export.py exists and defines exactly the three tests test_places_geojson_structure, test_places_json_structure, test_places_json_counts
    - `grep -c "import places_export" data/tests/test_places_export.py` returns at least 1
    - `grep -c "FeatureCollection" data/tests/test_places_export.py` returns at least 1
    - `grep -c "specimen_count" data/tests/test_places_export.py` returns at least 2 (assertion + parquet seed)
    - `grep -c "sample_count" data/tests/test_places_export.py` returns at least 2
    - The parquet seed in helper has 2 rows with same `sample_id=42` and 1 row with `place_slug=None` (verify by inspection that the count expectations match: specimen_count==2, sample_count==1)
    - `cd data && uv run pytest tests/test_places_export.py -x` exits non-zero with ModuleNotFoundError (RED proven)
    - The other tests collect fine: `cd data && uv run pytest tests/ --collect-only -q 2>&1 | grep -c "test_places_export"` returns at least 3
  </acceptance_criteria>
  <done>Failing tests committed; behavior contract for places_export.py is captured in code.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement places_export.py + wire into run.py + add .gitignore negations</name>
  <files>data/places_export.py, data/run.py, .gitignore</files>
  <read_first>
    - data/species_export.py (env-var module-level constants; DuckDB usage; JSON write pattern; .stat().st_size noqa print idiom)
    - data/places_validation.py (LOAD spatial pattern; tomllib loader)
    - data/run.py (current STEPS order after Plan 01 — places-load now appears)
    - .gitignore (line 141 `/public/data/` is the boundary; negations go AFTER it)
    - data/tests/test_places_export.py (target test contract — note the module-level constant `_PLACES_TOML_PATH` the tests monkeypatch)
    - content/places.toml (multi-line geometry_wkt needs .strip() before passing to duckdb)
  </read_first>
  <behavior>
    - export_places(con: duckdb.DuckDBPyConnection|None = None) reads `geographies.places` from DuckDB for geometry, reads ASSETS_DIR/occurrences.parquet for counts, reads content/places.toml for permits + metadata; writes ASSETS_DIR/places.geojson (compact) and ASSETS_DIR/places.json (indent=2)
    - export_places_step() zero-arg wrapper that opens a DuckDB connection, LOAD spatial, calls export_places(con), closes the connection
    - run.py imports export_places_step and inserts ("places-export", export_places_step) AFTER ("species-maps", generate_species_maps) and BEFORE ("feeds", generate_feeds)
    - .gitignore has negation rules for both files AFTER line 141 (`/public/data/`) so `git status` no longer treats them as ignored
  </behavior>
  <action>
Create `data/places_export.py`:
1. Module docstring: "Export per-place artifacts for the frontend (PPIPE-04). Writes ASSETS_DIR/places.geojson (slim — Mapbox source) and ASSETS_DIR/places.json (rich — Eleventy _data). Runs AFTER dbt-build because specimen/sample counts come from ASSETS_DIR/occurrences.parquet (Pitfall 5 — NOT from DBT_SANDBOX_DIR)."
2. Imports: `import json`, `import os`, `import tomllib`, `from pathlib import Path`, `import duckdb`.
3. Module-level constants (mirror data/species_export.py lines 33-35):
   - `DB_PATH = os.environ.get('DB_PATH', str(Path(__file__).parent / 'beeatlas.duckdb'))`
   - `_default_assets = str(Path(__file__).parent.parent / 'public' / 'data')`
   - `ASSETS_DIR = Path(os.environ.get('EXPORT_DIR', _default_assets))`
   - `_PLACES_TOML_PATH = Path(__file__).parent.parent / 'content' / 'places.toml'` (the test monkeypatches this constant)
4. `_load_place_metadata(toml_path: Path) -> dict[str, dict]`:
   - `with open(toml_path, "rb") as f: data = tomllib.load(f)`
   - Return `{p["slug"]: p for p in data.get("places", [])}`
5. `_query_counts(con, parquet_path: Path) -> dict[str, dict[str, int]]`:
   - If not parquet_path.exists(), raise `FileNotFoundError(f"{parquet_path} not found — run dbt before places-export")` (mirror species_maps.py existence guard)
   - Run: `SELECT place_slug, COUNT(CASE WHEN is_provisional = false OR is_provisional IS NULL THEN 1 END) AS specimen_count, COUNT(DISTINCT CASE WHEN sample_id IS NOT NULL THEN sample_id END) AS sample_count FROM read_parquet('{parquet_path}') WHERE place_slug IS NOT NULL GROUP BY place_slug`
   - Return `{row[0]: {"specimen_count": int(row[1]), "sample_count": int(row[2])} for row in fetchall}`
6. `_write_places_geojson(con, out_path: Path) -> None`:
   - `rows = con.execute("SELECT slug, ST_AsGeoJSON(geom) FROM geographies.places ORDER BY slug").fetchall()`
   - Build `features = [{"type": "Feature", "properties": {"slug": s}, "geometry": json.loads(g)} for s, g in rows]`
   - `fc = {"type": "FeatureCollection", "features": features}`
   - `out_path.write_text(json.dumps(fc, separators=(',', ':')), encoding="utf-8")` — compact, NOT indent=2 (matches counties.geojson, suitable for Mapbox)
   - `print(f"  places.geojson: {len(features):,} features, {out_path.stat().st_size:,} bytes")  # noqa: T201`
7. `_write_places_json(places_meta: dict, counts: dict, out_path: Path) -> None`:
   - Build `records = []`; iterate `sorted(places_meta.items())`; for each slug, meta in order, append `{"slug": slug, "name": meta["name"], "land_owner": meta["land_owner"], "permits": meta.get("permits", []), "specimen_count": counts.get(slug, {}).get("specimen_count", 0), "sample_count": counts.get(slug, {}).get("sample_count", 0)}`
   - `out_path.write_text(json.dumps(records, indent=2), encoding="utf-8")` — pretty, matches species.json
   - `print(f"  places.json: {len(records):,} places, {out_path.stat().st_size:,} bytes")  # noqa: T201`
8. `export_places(con: duckdb.DuckDBPyConnection | None = None) -> None`:
   - If con is None: `con = duckdb.connect(DB_PATH); con.execute("LOAD spatial"); _owned = True` else `_owned = False`
   - `ASSETS_DIR.mkdir(parents=True, exist_ok=True)`
   - `places_meta = _load_place_metadata(_PLACES_TOML_PATH)`
   - `counts = _query_counts(con, ASSETS_DIR / "occurrences.parquet")`
   - `_write_places_geojson(con, ASSETS_DIR / "places.geojson")`
   - `_write_places_json(places_meta, counts, ASSETS_DIR / "places.json")`
   - If `_owned`: `con.close()`
9. `export_places_step() -> None`: `con = duckdb.connect(DB_PATH); con.execute("LOAD spatial"); export_places(con); con.close()` — zero-arg STEPS wrapper.
10. `if __name__ == "__main__": export_places_step()` block (matches geographies_pipeline.py convention).

Modify `data/run.py`:
1. Add `from places_export import export_places_step` next to the new `from places_load import load_places_step` import.
2. In STEPS, insert `("places-export", export_places_step),` AFTER `("species-maps", generate_species_maps),` and BEFORE `("feeds", generate_feeds),`. Result order: ... species-maps -> places-export -> feeds. (Plan 03 will insert places-maps in this same neighborhood.)

Modify `.gitignore`:
1. Append AFTER line 141 (`/public/data/`) but BEFORE the next section comment (`# Claude Code worktrees`), two lines:
   - `!/public/data/places.geojson`
   - `!/public/data/places.json`
   Pitfall 4 — order matters; negations placed before the blanket ignore are inert.
2. Add a one-line comment above the negations: `# v3.7 PPIPE-05: commit places.geojson + places.json so CI builds without running the pipeline`.
  </action>
  <verify>
    <automated>cd data && uv run pytest tests/test_places_export.py -v</automated>
  </verify>
  <acceptance_criteria>
    - data/places_export.py exports `export_places` and `export_places_step`
    - `grep -c '"LOAD spatial"' data/places_export.py` returns at least 1
    - `grep -c 'INSTALL spatial' data/places_export.py` returns 0
    - `grep -c "ASSETS_DIR / .occurrences.parquet." data/places_export.py` returns at least 1 (Pitfall 5 — not DBT_SANDBOX_DIR)
    - `grep -c "DBT_SANDBOX_DIR" data/places_export.py` returns 0
    - `grep -c "ST_AsGeoJSON" data/places_export.py` returns 1
    - `grep -c "separators=(.,., .:.)" data/places_export.py` returns 1 (compact GeoJSON for Mapbox)
    - `grep -c "indent=2" data/places_export.py` returns 1 (pretty places.json)
    - `grep -c "_PLACES_TOML_PATH" data/places_export.py` returns at least 2 (module constant + reference)
    - `grep -c "from places_export import export_places_step" data/run.py` returns 1
    - In data/run.py STEPS, `places-export` appears AFTER `species-maps` and BEFORE `feeds`: `grep -n '"species-maps"\|"places-export"\|"feeds"' data/run.py` shows them in that order
    - `.gitignore` contains `!/public/data/places.geojson` and `!/public/data/places.json`, and their line numbers are GREATER than the line number of `/public/data/` (verify with `grep -n "/public/data" .gitignore`)
    - `cd data && uv run pytest tests/test_places_export.py -v` exits 0 (all 3 tests GREEN)
    - `cd data && uv run python places_export.py` runs against the real data/beeatlas.duckdb + public/data/occurrences.parquet and produces both files; both files validate as JSON (`python -c "import json; json.load(open('public/data/places.geojson'))"` exits 0; same for places.json)
    - places.geojson parses to a FeatureCollection whose feature count equals the row count of geographies.places (`duckdb data/beeatlas.duckdb -c "SELECT COUNT(*) FROM geographies.places"`)
    - places.json record count equals places.toml `[[places]]` entry count
    - `git check-ignore public/data/places.geojson public/data/places.json` returns NEITHER path (both are now tracked-eligible — negations active)
  </acceptance_criteria>
  <done>Both export artifacts produced and tested; .gitignore allows them through; ready for commit-checkpoint in Task 3.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human verification — confirm artifacts are correct, then commit</name>
  <what-built>
    Task 2 produced public/data/places.geojson and public/data/places.json by running the
    pipeline (or `cd data && uv run python places_export.py`). Both files are now eligible to
    be tracked by git (negation rules in .gitignore). This checkpoint asks a human to inspect
    the artifacts before committing — committing a malformed places.geojson into git would
    propagate downstream to every CI build until reverted (PPIPE-05 risk).
  </what-built>
  <how-to-verify>
    1. Confirm both files exist and are non-empty:
       `ls -la public/data/places.geojson public/data/places.json`
    2. Inspect places.geojson contents — open it in a JSON viewer or run:
       `python -c "import json; fc=json.load(open('public/data/places.geojson')); print(fc['type']); print(len(fc['features']), 'features'); print([f['properties']['slug'] for f in fc['features']])"`
       Expected: `FeatureCollection`, N features (matching `[[places]]` entry count in content/places.toml), slugs match the TOML.
    3. Optional spatial sanity check: paste places.geojson contents into https://geojson.io and visually confirm the polygons land at the expected WA locations (rattlesnake-ledge, tiger-mountain, etc.).
    4. Inspect places.json — every record has slug, name, land_owner, permits, specimen_count, sample_count:
       `python -c "import json; r=json.load(open('public/data/places.json')); print(len(r), 'places'); [print(p['slug'], p['specimen_count'], p['sample_count']) for p in r]"`
    5. Confirm `git status public/data/` shows these two files as untracked/modified (NOT ignored). If they are not visible to git, the .gitignore negations did not take effect — go back to Task 2.
    6. If all four checks pass, type `approved` to allow the commit.

    When approved, the executor will run:
      `git add public/data/places.geojson public/data/places.json .gitignore data/places_export.py data/run.py data/tests/test_places_export.py`
      `git commit -m "feat(98-02): places.geojson + places.json export (PPIPE-04, PPIPE-05)"`

    Then verify CI-equivalent build works without running the pipeline:
      `npm run build` (in a fresh shell, NO `cd data && uv run python run.py` first) — must exit 0
  </how-to-verify>
  <resume-signal>Type "approved" to commit, or describe issues found (geometry wrong, counts wrong, files missing, etc.)</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| content/places.toml -> places_export.py | maintainer-curated TOML; permits/metadata flow into places.json |
| public/data/occurrences.parquet -> places_export.py | dbt-produced parquet (already trusted within pipeline) |
| places_export.py -> public/data/ -> git commit | committed artifacts become part of CI build surface |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-98-04 | Tampering | A malformed places.geojson committed to git silently breaks Mapbox on every CI build | mitigate | Human checkpoint (Task 3) requires inspection of JSON parse + feature count + spatial sanity check before commit; pytest in Task 1/2 enforces structural shape |
| T-98-05 | Information Disclosure | places.json bundles all permit metadata into a publicly-served static file | accept | Permit metadata is intentionally public per v3.7 product decision — places are publicly published collecting locations |
| T-98-06 | Tampering | Stale counts in places.json from reading wrong parquet (Pitfall 5) | mitigate | places_export.py reads from ASSETS_DIR (the EXPORT_DIR-resolved post-copy path), NOT from DBT_SANDBOX_DIR; test_places_json_counts verifies the count math against a known fixture |
| T-98-SC | Tampering | npm/pip/cargo installs | mitigate | No new package installs in this plan — slopcheck N/A |
</threat_model>

<verification>
After all three tasks land:
1. `cd data && uv run pytest tests/test_places_export.py -v` — exits 0
2. `git ls-files public/data/places.geojson public/data/places.json` — both paths printed
3. `python -c "import json; fc=json.load(open('public/data/places.geojson')); assert fc['type']=='FeatureCollection'; assert all('slug' in f['properties'] for f in fc['features'])"`
4. `python -c "import json; r=json.load(open('public/data/places.json')); assert all({'slug','name','land_owner','permits','specimen_count','sample_count'} <= set(p.keys()) for p in r)"`
5. From a fresh shell (no DuckDB invocation), `npm run build` exits 0 — CI-equivalent build works without running the pipeline
6. STEPS order check: `grep -nE '"(places-validation|places-load|dbt-build|species-export|species-maps|places-export|feeds)"' data/run.py` shows them in that order
</verification>

<success_criteria>
- PPIPE-04: places.geojson (FeatureCollection, slug-only properties, compact) and places.json (rich, permits + counts, pretty) both produced by `cd data && uv run python places_export.py` or via `run.py`
- PPIPE-05: Both files tracked by git via .gitignore negation rules; clean checkout + `npm run build` succeeds without running the pipeline
- pytest covers FeatureCollection structure, JSON record shape, and count math
- run.py STEPS wiring keeps places-export AFTER dbt-build (so occurrences.parquet exists with place_slug from Plan 01)
</success_criteria>

<output>
Create `.planning/phases/98-pipeline-integration/098-02-SUMMARY.md` when done.
</output>
