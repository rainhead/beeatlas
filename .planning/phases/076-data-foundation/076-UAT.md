---
status: complete
phase: 076-data-foundation
source:
  - 076-01-SUMMARY.md
  - 076-02-SUMMARY.md
  - 076-03-SUMMARY.md
  - 076-04-SUMMARY.md
  - 076-05-SUMMARY.md
  - 076-06-SUMMARY.md
started: 2026-05-02T00:00:00Z
updated: 2026-05-02T00:00:00Z
mode: backend-data-pipeline
---

## Current Test

[testing complete]

## Tests

### 1. Pytest suite passes (SC5)
expected: |
  `cd data && uv run pytest tests/test_checklist_pipeline.py tests/test_taxon_lineage.py`
  passes including the disagreement fixtures (Lasioglossum (Dialictus) zonulum ↔
  Lasioglossum zonulum) and the authority-bearing variant.
result: pass
evidence: |
  20/20 tests pass in 13.45s. Full data suite (80 tests) passes in 13.86s.
  Key fixtures verified: test_disagreement_fixture_canonical_join,
  test_authority_bearing_canonicalizes_to_binomial, test_trinomial_subspecies_folds_to_binomial.

### 2. Checklist source committed with provenance (SC1 — files)
expected: |
  data/checklists/wa_bee_checklist.tsv exists (1 header + 2,861 data rows = 2,862 lines),
  data/checklists/README.md cites Bartholomew et al. 2024 JHR 97 + DOI 10.3897/jhr.97.129013,
  data/checklist_synonyms.csv ships header-only.
  REQUIREMENTS.md CHECK-01 amended to `.tsv`, CHECK-03 footnote present.
result: pass
evidence: |
  wc -l checklists/wa_bee_checklist.tsv → 2862. README.md present (1919 bytes).
  checklist_synonyms.csv 37 bytes (header only). REQUIREMENTS.md line 12 reads `.tsv`,
  line 14 carries the v3.2/v3.3+ footnote.

### 3. run.py STEPS ordering (SC2)
expected: |
  STEPS contains ('checklist', load_checklist) between anti-entropy and export, and
  ('taxon-lineage-extended', enrich_taxon_lineage_extended) after waba per D-03 ordering.
result: pass
evidence: |
  data/run.py:33-44 STEPS list:
    ecdysis → ecdysis-links → inaturalist → waba → taxon-lineage-extended →
    projects → anti-entropy → checklist → export → feeds.
  Both new steps in correct positions.

### 4. Taxon lineage extension (SC3)
expected: |
  inaturalist_pipeline.enrich_taxon_lineage_extended() walks family/subfamily/tribe/
  genus/subgenus into inaturalist_data.taxon_lineage_extended; emits NULL (not a sentinel)
  for absent ranks; existing waba_pipeline.enrich_taxon_lineage left byte-identical.
result: pass
evidence: |
  inaturalist_pipeline.py:142 TARGET_RANKS = {family, subfamily, tribe, genus, subgenus}.
  inaturalist_pipeline.py:145 enrich_taxon_lineage_extended(db_path) defined.
  test_taxon_lineage.py::test_enrich_emits_null_subgenus_not_sentinel passes
  (TAX-03 phantom-node guard).
  test_enrich_unions_inat_and_waba_taxa confirms UNION source.

### 5. canonical_name + reconcile flow (SC4)
expected: |
  canonical_name materialized on both checklist_data.species and ecdysis_data.occurrences
  via canonicalize() (5-step: authority strip, subgenus paren strip, infraspecific marker
  strip, lowercase, whitespace collapse). reconcile() consults synonyms.csv, writes
  unmatched to checklist_unmatched.csv (warn-only).
result: pass
evidence: |
  data/canonical_name.py + 16-test test_canonical_name.py (idempotence + per-step coverage).
  checklist_pipeline.py:29 _update_occurrences_canonical_name() ALTERs occurrences with
  canonical_name VARCHAR. checklist_pipeline.py:64 reconcile() reads SYNONYMS_PATH,
  writes UNMATCHED_PATH header-only on no-unmatched. test_checklist_reconcile.py 8/8 pass.

### 6. Pipeline end-to-end run on real DB
expected: |
  `python data/run.py` completes all STEPS without error and populates the three new
  tables (checklist_data.species, checklist_data.species_counties,
  inaturalist_data.taxon_lineage_extended) per SC1/SC2/SC3.
result: pass
fix: |
  Added _inat_get_with_retry() in data/inaturalist_pipeline.py with 429/5xx-aware
  exponential backoff (Retry-After honored) and a 1s pacing floor between
  successful batches. Migrated enrich_taxon_lineage_extended only — kept waba's
  enrich_taxon_lineage byte-identical per CONTEXT.md D-03 (consolidation deferred
  to v3.3+, see new seed inat-taxonomy-dwca.md). 4 new tests in
  test_taxon_lineage_extended.py cover 429-then-success, 5xx-then-success, retry
  exhaustion, and Retry-After honoring. conftest.py autouse fixture zeros pacing
  in tests so retries don't real-time-sleep.
evidence: |
  Pipeline run #2 (2026-05-02): 170s end-to-end, all STEPS clean.
    - taxon-lineage-extended: 1688 rows in 98.7s (no 429s observed; pacing absorbed it)
    - checklist: 527 species, 2861 county records
    - reconcile: 0 synonym overrides applied; 179 unmatched (warn-only per D-05)
  Verified post-run:
    - checklist_data.species: 527 rows, all canonical_name populated, all status='verified'
    - inaturalist_data.taxon_lineage_extended: 1688 rows (443 with subgenus, 1324 with tribe)
    - ecdysis_data.occurrences.canonical_name: 26097 of 46090 populated (the rest have NULL scientific_name — expected: sample/host rows, not specimens)
    - Disagreement fixture lives in real data: occurrences side carries 'lasioglossum zonulum' as canonical_name (matches checklist canonical regardless of '(Dialictus)' subgenus parens)

## Summary

total: 6
passed: 6
issues: 0
pending: 0
skipped: 0

## Gaps

[none — all gaps closed inline; v3.3+ DwC-A migration captured in
.planning/seeds/inat-taxonomy-dwca.md to consolidate both lineage enrichers]
