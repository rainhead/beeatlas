---
phase: 076
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - data/checklists/wa_bee_checklist.tsv
  - data/checklists/README.md
  - data/checklist_synonyms.csv
  - .planning/REQUIREMENTS.md
autonomous: true
requirements: [CHECK-01]
requirements_addressed: [CHECK-01]
tags: [data-pipeline, checklist, provenance]
must_haves:
  truths:
    - "data/checklists/wa_bee_checklist.tsv exists and is the verbatim TSV from ~/Downloads/washington_bees(3).tsv (per D-01)"
    - "data/checklists/README.md records Bartholomew et al. 2024 (JHR 97; DOI 10.3897/jhr.97.129013) provenance, supplement format, manual-extraction step, and the two-column TSV shape"
    - "data/checklist_synonyms.csv exists with header-only initial state per D-05 schema (checklist_name,canonical_name,source)"
    - "REQUIREMENTS.md CHECK-01 reads `wa_bee_checklist.tsv` (D-01 amendment); CHECK-03 carries the v3.2-only-`verified` footnote (D-02 amendment)"
  artifacts:
    - path: "data/checklists/wa_bee_checklist.tsv"
      provides: "WA bee checklist source data — 2,862 rows, 2 columns (species\\tcounty)"
    - path: "data/checklists/README.md"
      provides: "Provenance and format documentation for the checklist TSV"
    - path: "data/checklist_synonyms.csv"
      provides: "Empty (header-only) override table for checklist↔occurrence name reconciliation"
    - path: ".planning/REQUIREMENTS.md"
      provides: "Updated CHECK-01 (.csv→.tsv) and CHECK-03 (status footnote)"
  key_links:
    - from: "data/checklist_synonyms.csv"
      to: "future data/checklist_pipeline.py reconcile() loader"
      via: "csv.DictReader on three-column header"
      pattern: "checklist_name,canonical_name,source"
---

<objective>
Land the static data and documentation foundation for Phase 76: commit the checklist TSV verbatim, record its provenance, seed the synonyms override file with a header-only initial state, and amend REQUIREMENTS.md to match the locked decisions D-01 (.csv→.tsv) and D-02 (status footnote).

Purpose: Phase 76's pipeline code (Plans 03–06) reads these files at run time. Without them, every later plan blocks on missing source data. They have no runtime dependencies and can ship in Wave 1 alongside Plan 02.

Output: 4 committed files (1 TSV, 1 README, 1 CSV, 1 REQUIREMENTS amendment).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/076-data-foundation/076-CONTEXT.md
@.planning/phases/076-data-foundation/076-PATTERNS.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Commit checklist TSV verbatim</name>
  <files>data/checklists/wa_bee_checklist.tsv</files>
  <read_first>
    - .planning/phases/076-data-foundation/076-CONTEXT.md (D-01)
    - .planning/phases/076-data-foundation/076-RESEARCH.md (User Constraints; "Source data" notes)
  </read_first>
  <action>
Copy `~/Downloads/washington_bees(3).tsv` verbatim to `data/checklists/wa_bee_checklist.tsv`. DO NOT edit, sort, dedupe, or transform the file — it must be byte-identical to the source. Per D-01 the file has 2,862 rows + 1 header row, two tab-separated columns named `species` and `county`, bare binomials with no authority strings.

Concretely:
1. `mkdir -p data/checklists`
2. `cp ~/Downloads/washington_bees\(3\).tsv data/checklists/wa_bee_checklist.tsv`
3. Verify line count is 2863 (`wc -l` includes the header).
4. Verify the first line is the header `species\tcounty` (literal tab; `head -1 data/checklists/wa_bee_checklist.tsv | od -c | head -2` to confirm `\t`).
5. Verify column count on a sample data row equals 2 (`awk -F'\t' 'NR==2 { print NF }' data/checklists/wa_bee_checklist.tsv` prints `2`).

If `~/Downloads/washington_bees(3).tsv` is not present, ask the user to confirm the source path before proceeding — do NOT regenerate or guess at the contents.
  </action>
  <verify>
    <automated>test -f data/checklists/wa_bee_checklist.tsv &amp;&amp; test "$(wc -l &lt; data/checklists/wa_bee_checklist.tsv)" = "2863" &amp;&amp; head -1 data/checklists/wa_bee_checklist.tsv | grep -qP '^species\tcounty$'</automated>
  </verify>
  <acceptance_criteria>
    - `data/checklists/wa_bee_checklist.tsv` exists
    - File has exactly 2863 lines (1 header + 2862 data rows)
    - First line equals `species\tcounty` (tab-separated, no quotes)
    - `awk -F'\t' 'NR==2 { print NF }'` on the file prints `2`
    - `awk -F'\t' 'NR>1 { print $1 }' data/checklists/wa_bee_checklist.tsv | sort -u | wc -l` returns approximately 527 (unique species)
  </acceptance_criteria>
  <done>TSV committed verbatim, line and column counts verified.</done>
</task>

<task type="auto">
  <name>Task 2: Write data/checklists/README.md (provenance)</name>
  <files>data/checklists/README.md</files>
  <read_first>
    - data/README.md (existing data-dir README — match its tone and structure)
    - .planning/phases/076-data-foundation/076-CONTEXT.md (D-01 — provenance requirements)
    - .planning/phases/076-data-foundation/076-RESEARCH.md ("Runtime State Inventory" → committed assets)
    - data/checklists/wa_bee_checklist.tsv (the file this README documents — read first 3 lines to confirm shape)
  </read_first>
  <action>
Create `data/checklists/README.md` documenting the checklist TSV. Required sections (use exactly these H2 headings):

1. `## Source` — paragraph citing **Bartholomew, C. S., D. R. Smith, B. A. K. Sykes, and S. Reichard. 2024. "An updated checklist of the bees (Hymenoptera: Apoidea: Anthophila) of Washington State." Journal of Hymenoptera Research 97: 129013.** Include the DOI link `https://doi.org/10.3897/jhr.97.129013`.

2. `## File format` — table or bullet list documenting:
   - Filename: `wa_bee_checklist.tsv`
   - Encoding: UTF-8
   - Rows: 1 header + 2,862 data rows
   - Columns: `species` (bare binomial, e.g. `Andrena cressonii`), `county` (Washington county name, e.g. `King`)
   - Unique species: ~527
   - Unique counties: 39
   - Delimiter: tab (`\t`)

3. `## Extraction` — paragraph explaining that the TSV was manually extracted from the published supplement PDF (`~/Downloads/Washington's Bees(1).pdf` retained for reference; not committed). Note that the supplement is a tabular checklist and the manual step transformed the per-row layout into long format `(species, county)` pairs.

4. `## Loaded by` — paragraph noting that `data/checklist_pipeline.py::load_checklist()` (Phase 76 / CHECK-02) reads this file and writes `checklist_data.species` (DISTINCT species) and `checklist_data.species_counties` (raw rows).

5. `## Status convention` — paragraph noting that v3.2 populates `status='verified'` for every row (D-02); the `'likely-to-occur'` enum value is reserved for v3.3+ when a curated expected-but-not-yet-found set is introduced.

Keep prose concise — the README's audience is future maintainers diffing this commit. Cite the DOI inline; do not paraphrase the citation. Use markdown link syntax for the DOI URL.
  </action>
  <verify>
    <automated>test -f data/checklists/README.md &amp;&amp; grep -q "Bartholomew" data/checklists/README.md &amp;&amp; grep -q "10.3897/jhr.97.129013" data/checklists/README.md &amp;&amp; grep -q "2,862" data/checklists/README.md &amp;&amp; grep -q "verified" data/checklists/README.md &amp;&amp; grep -q "likely-to-occur" data/checklists/README.md</automated>
  </verify>
  <acceptance_criteria>
    - `data/checklists/README.md` exists
    - Contains the literal string `Bartholomew`
    - Contains the DOI `10.3897/jhr.97.129013`
    - Contains the row count `2,862`
    - Contains both `verified` and `likely-to-occur` (status convention note per D-02)
    - Contains all five H2 headings: `## Source`, `## File format`, `## Extraction`, `## Loaded by`, `## Status convention`
  </acceptance_criteria>
  <done>README committed with provenance, format, extraction, loader, and status-convention sections.</done>
</task>

<task type="auto">
  <name>Task 3: Seed checklist_synonyms.csv (header-only)</name>
  <files>data/checklist_synonyms.csv</files>
  <read_first>
    - .planning/phases/076-data-foundation/076-CONTEXT.md (D-05 — schema)
    - .planning/phases/076-data-foundation/076-RESEARCH.md (User Constraints D-05)
  </read_first>
  <action>
Create `data/checklist_synonyms.csv` with EXACTLY one line: the header row `checklist_name,canonical_name,source` followed by a single trailing newline. No data rows. Per D-05:
- Column 1 `checklist_name`: the raw binomial as it appears in the checklist TSV.
- Column 2 `canonical_name`: the canonicalize()-transformed name to which the checklist row should map.
- Column 3 `source`: free-text URL or citation explaining the mapping (audit trail). Required (non-empty) when a row is added.

Reviewers add rows over time; Phase 76 ships the file empty. Do NOT add placeholder comment rows or example rows. Do NOT add a BOM.
  </action>
  <verify>
    <automated>test -f data/checklist_synonyms.csv &amp;&amp; test "$(wc -l &lt; data/checklist_synonyms.csv)" = "1" &amp;&amp; head -1 data/checklist_synonyms.csv | grep -qx 'checklist_name,canonical_name,source'</automated>
  </verify>
  <acceptance_criteria>
    - `data/checklist_synonyms.csv` exists
    - File has exactly 1 line
    - First line equals `checklist_name,canonical_name,source` (no leading BOM, no whitespace)
    - `file data/checklist_synonyms.csv` reports ASCII or UTF-8 (not UTF-8-BOM)
  </acceptance_criteria>
  <done>Header-only synonyms.csv committed.</done>
</task>

<task type="auto">
  <name>Task 4: Amend REQUIREMENTS.md (D-01 .csv→.tsv; D-02 status footnote)</name>
  <files>.planning/REQUIREMENTS.md</files>
  <read_first>
    - .planning/REQUIREMENTS.md (lines 12–17 — CHECK section)
    - .planning/phases/076-data-foundation/076-CONTEXT.md (D-01, D-02)
  </read_first>
  <action>
Make exactly two edits to `.planning/REQUIREMENTS.md`:

**Edit 1 (D-01):** On the CHECK-01 line, replace `data/checklists/wa_bee_checklist.csv` with `data/checklists/wa_bee_checklist.tsv` (a single character change in the file extension — keep all other text including the parenthetical note unchanged).

The current line reads:
```
- [ ] **CHECK-01**: WA bee checklist committed at `data/checklists/wa_bee_checklist.csv` with provenance recorded in `data/checklists/README.md` (Bartholomew et al. 2024, JHR 97; supplement format documented and dated)
```

Must become:
```
- [ ] **CHECK-01**: WA bee checklist committed at `data/checklists/wa_bee_checklist.tsv` with provenance recorded in `data/checklists/README.md` (Bartholomew et al. 2024, JHR 97; supplement format documented and dated)
```

**Edit 2 (D-02):** On the CHECK-03 line, append a footnote to the `status` enum so the new line reads:
```
- [ ] **CHECK-03**: Checklist table schema: `scientificName`, `family`, `subfamily`, `tribe`, `genus`, `subgenus`, `specific_epithet`, `status` (verified | likely-to-occur — v3.2 populates only `verified`; `likely-to-occur` reserved for v3.3+ when a curated "expected but not yet found" set is introduced), `source_citation`, `notes`
```

Do NOT touch any other line. Do NOT renumber requirements. Do NOT modify the Traceability table.
  </action>
  <verify>
    <automated>grep -q "wa_bee_checklist.tsv" .planning/REQUIREMENTS.md &amp;&amp; ! grep -q "wa_bee_checklist.csv" .planning/REQUIREMENTS.md &amp;&amp; grep -q "v3.2 populates only \`verified\`" .planning/REQUIREMENTS.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep "wa_bee_checklist.tsv" .planning/REQUIREMENTS.md` matches at least once
    - `grep "wa_bee_checklist.csv" .planning/REQUIREMENTS.md` matches zero times
    - The literal string `v3.2 populates only \`verified\`` appears on the CHECK-03 line
    - The literal string `\`likely-to-occur\` reserved for v3.3+` appears on the CHECK-03 line
    - All other CHECK / TAX / AGG / MAP / PHOTO / PAGE / NAV / FILT / VIZ / LINK / PERF requirement IDs remain present (run `grep -cE '^- \[ \] \*\*[A-Z]+-[0-9]+\*\*' .planning/REQUIREMENTS.md` and confirm count is unchanged from before the edit — should still be 67)
  </acceptance_criteria>
  <done>REQUIREMENTS.md amended; CHECK-01 reflects D-01, CHECK-03 reflects D-02.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Filesystem → git | Committed source files become part of the repo's trust surface; reviewers must validate provenance |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-76-01 | Tampering | `data/checklists/wa_bee_checklist.tsv` | mitigate | File is committed verbatim from a known source; provenance recorded in `README.md` with DOI; reviewer can diff future edits against the citation |
| T-76-02 | N/A | `data/checklist_synonyms.csv` | accept | Header-only initial commit; CSV-injection risk (T-76-02 in security_threat_model) does NOT apply here — synonyms.csv is reviewer-curated input, not a pipeline writeback target |
| T-76-XX | N/A — plan only touches static asset commits and a docs amendment | n/a | n/a | n/a |
</threat_model>

<verification>
After all four tasks complete:
1. `git status` shows the four new/modified files (TSV, README, synonyms.csv, REQUIREMENTS.md)
2. `wc -l data/checklists/wa_bee_checklist.tsv` returns 2863
3. `head -1 data/checklists/wa_bee_checklist.tsv` returns `species\tcounty`
4. `head -1 data/checklist_synonyms.csv` returns `checklist_name,canonical_name,source`
5. `grep -c "wa_bee_checklist.tsv" .planning/REQUIREMENTS.md` returns at least 1; `grep -c "wa_bee_checklist.csv" .planning/REQUIREMENTS.md` returns 0
</verification>

<success_criteria>
- [ ] CHECK-01 satisfied: TSV committed at the locked path with README provenance
- [ ] D-01 amendment landed in REQUIREMENTS.md
- [ ] D-02 status-enum footnote landed in REQUIREMENTS.md
- [ ] D-05 synonyms.csv schema seeded (header-only)
- [ ] No data row in synonyms.csv (must be header-only at Phase 76 commit time)
</success_criteria>

<output>
After completion, create `.planning/phases/076-data-foundation/076-01-SUMMARY.md` documenting:
- Files created (paths + line counts)
- REQUIREMENTS.md edits applied (cite line numbers before/after if shifted)
- Confirmation that no data rows leaked into synonyms.csv
- Confirmation that the TSV was copied verbatim (no edits)
</output>
