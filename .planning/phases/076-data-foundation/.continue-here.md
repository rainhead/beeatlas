---
context: phase
phase: 076-data-foundation
task: null
total_tasks: 15
status: planned_ready_to_execute
last_updated: 2026-05-03T05:25:41.690Z
---

<current_state>
Phase 76 (Data Foundation) is fully planned and ready for execution. No work has begun on the implementation. The pause was triggered by context exhaustion (77%) at the end of `/gsd-plan-phase 76`, NOT by a blocker.

6 plans across 4 waves are committed. The plan checker passed after one revision iteration (started with 2 blockers + 3 warnings + 1 info; ended with 0 blockers + 1 doc-hygiene warning that was resolved inline). All 10 phase requirements (CHECK-01..06, TAX-01..04) and all 5 locked decisions (D-01..D-05) are covered.
</current_state>

<completed_work>

This session (`/gsd-plan-phase 76`):
- Research: 076-RESEARCH.md (commit e428bcc) — verified iNat v2 ancestor walk shape live, queried live ecdysis DB and found 0 authority strings + 6 trinomials + 1 real Lasioglossum zonulus/zonulum synonym; surfaced 4 open questions all resolved by planner
- Validation strategy: 076-VALIDATION.md (commit e428bcc, then revised to wave_0_complete:true with 15-row Per-Task Verification Map)
- Pattern map: 076-PATTERNS.md (commit 57de655) — 13 files classified, 11 with concrete analogs
- Plans: 076-01..06 (commit 99103a4) — 6 plans, 15 tasks, 4 waves
- Plan checker passed after revision (2 blockers fixed: Plan 06 missing taxon__id BIGINT in conftest fix; removed invented fixture_db_path)
- ROADMAP.md SC5 amended to use `tests/`-prefixed pytest paths
- 076-RESEARCH.md Open Questions annotated as RESOLVED
</completed_work>

<remaining_work>

Single next step: execute the 6 plans.

Wave 1 (parallel-safe): 076-01 (TSV/README/synonyms.csv/REQUIREMENTS amendment), 076-02 (canonical_name.py TDD)
Wave 2: 076-03 (checklist_pipeline.load_checklist + run.py STEPS)
Wave 3 (parallel-safe): 076-04 (enrich_taxon_lineage_extended + STEPS), 076-05 (occurrences canonical_name + reconcile)
Wave 4: 076-06 (integration tests with disagreement fixtures)
</remaining_work>

<decisions_made>

- Standalone STEP `("taxon-lineage-extended", enrich_taxon_lineage_extended)` in `data/run.py` between `waba` and `anti-entropy` — not called from inside `inaturalist_pipeline.load_observations()`, because that would put it BEFORE waba and miss WABA taxa in the UNION
- `canonicalize()` lives in new `data/canonical_name.py` module (Plan 076-02), not in `data/checklist_pipeline.py` — single responsibility; Phase 77 will import it
- Tests live at `data/tests/test_*.py` with `tests/`-prefixed pytest invocation (matches `data/pyproject.toml` `testpaths = ["tests"]`); ROADMAP SC5 amended
- `synonyms.csv` override UPDATEs the `canonical_name` column at reconcile time — downstream consumers see no synonyms.csv complexity
- `_INFRA_MARKERS` is exactly 5 markers (ssp./var./aff./cf./nr.) — strict reading of D-04 step 3
- Wave 0 satisfied by Plan 02 (canonicalize TDD) only; DB-write integration tests deferred to Plan 06 by intentional design
- `data/canonical_name.py` is a new file, not an extension of `data/feeds.py` (the closest analog `_slugify` is in feeds.py:132)
</decisions_made>

<blockers>
None.
</blockers>

## Required Reading (in order)

1. `.planning/phases/076-data-foundation/076-CONTEXT.md` — locked decisions D-01..D-05 (do not re-litigate)
2. `.planning/phases/076-data-foundation/076-RESEARCH.md` — empirical findings; especially the 6 trinomials in live ecdysis data and the Lasioglossum zonulus/zonulum synonym
3. `.planning/phases/076-data-foundation/076-PATTERNS.md` — analog file:line ranges for each new/modified file
4. `.planning/phases/076-data-foundation/076-VALIDATION.md` — Per-Task Verification Map (15 rows)
5. `.planning/phases/076-data-foundation/076-01-PLAN.md` through `076-06-PLAN.md` — read in wave order
6. `CLAUDE.md` — Python 3.14+; `cd data && uv run pytest`; static-hosting only; never force-add gitignored files

## Critical Anti-Patterns (do NOT repeat these)

| Pattern | Description | Severity | Prevention |
|---------|-------------|----------|------------|
| Calling `enrich_taxon_lineage_extended` from inside `inaturalist_pipeline.load_observations()` | Would run BEFORE waba in `run.py` STEPS, missing WABA taxa in the UNION query | advisory | Plan 04 wires it as a standalone STEP — do NOT move the call back into `load_observations()` |
| Inventing redundant pytest fixtures | Plan checker iter 1 caught a `fixture_db_path` fixture that duplicated existing `fixture_db` | advisory | Read `data/tests/conftest.py:256-275` BEFORE writing any new fixtures |
| Adding `subsp.` to `_INFRA_MARKERS` | D-04 step 3 lists exactly 5 markers; planner originally added a 6th | advisory | Plan 02's test asserts EXACTLY 5; don't add a 6th without amending CONTEXT.md D-04 first |

## Infrastructure State

- DuckDB at `data/beeatlas.duckdb` (105 MB, 564 distinct scientific_name values verified live this session) — present and queryable
- Source TSV at `~/Downloads/washington_bees(3).tsv` — 2,862 rows verified; needs to be copied to `data/checklists/wa_bee_checklist.tsv` (Plan 076-01 Task 1)
- Bartholomew et al. 2024 PDF at `~/Downloads/Washington's Bees(1).pdf` — provenance source for README
- iNat v2 API verified live (`/v2/taxa/{ids}` with `fields=id,name,rank,ancestors.name,ancestors.rank` returns full ancestor chain)
- pytest 9.0.2+ via `uv` — available
- No running services; no background processes

<context>
Phase 76 lands the data-pipeline foundation that Phase 77 species aggregation depends on:
1. WA bee checklist into `checklist_data.species` via new `data/checklist_pipeline.py`
2. Extended iNat lineage walk into new `inaturalist_data.taxon_lineage_extended`
3. Canonical name reconciliation (authority-stripped, lowercase, single-spaced) materialized as `canonical_name VARCHAR` on BOTH `checklist_data.species` AND `ecdysis_data.occurrences`, with `data/checklist_synonyms.csv` override and `data/checklist_unmatched.csv` warn-only sidecar.

The phase is intentionally backend-only — no frontend, no aggregation, no SVG maps, no photo manifest. Those are Phases 77–81.

The execution path is straightforward because all gray areas are resolved: 6 plans, each with concrete `<read_first>` + `<acceptance_criteria>` blocks, each with a `<threat_model>` block, each with `<verify><automated>` test commands.
</context>

<next_action>
Run `/gsd-execute-phase 76` (in a fresh context — `/clear` first).

The executor will start with Wave 1 (Plans 076-01 and 076-02 in parallel). Plan 076-02 is `type: tdd` — RED → GREEN → REFACTOR sequence. Plan 076-01 is straightforward file additions + REQUIREMENTS.md amendment.
</next_action>
