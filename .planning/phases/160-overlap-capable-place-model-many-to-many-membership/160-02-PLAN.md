---
phase: 160-overlap-capable-place-model-many-to-many-membership
plan: 02
type: execute
wave: 1
depends_on: ["160-01"]
files_modified:
  - data/dbt/models/marts/occurrence_places.sql
  - data/dbt/models/marts/occurrences.sql
  - data/dbt/models/marts/schema.yml
  - data/places_validation.py
  - data/run.py
  - data/sqlite_export.py
  - scripts/make-local-manifest.js
  - scripts/validate-db.mjs
autonomous: true
requirements: [SC-1, SC-2, SC-5]
must_haves:
  truths:
    - "bash data/dbt/run.sh build exits 0: occurrences contract is 32 cols (no place_slug); occurrence_places mart builds under its own contract"
    - "occurrence_places.parquet contains one row per (occ_id, place_slug) membership, occ_id mirroring occIdFromRow, sorted, no DISTINCT ON"
    - "places_validation.py no longer rejects overlapping polygons; WKT/WGS84/slug/permit checks remain"
    - "occurrences.db ships an occurrence_places table; the two hardcoded JS whitelists list it"
  artifacts:
    - path: "data/dbt/models/marts/occurrence_places.sql"
      provides: "many-to-many bridge mart (Option B occ_id)"
      contains: "occ_id"
    - path: "data/dbt/models/marts/schema.yml"
      provides: "occurrences 32-col contract + occurrence_places contract"
      contains: "occurrence_places"
    - path: "data/sqlite_export.py"
      provides: "CREATE TABLE out.occurrence_places + occ_id index"
      contains: "occurrence_places"
  key_links:
    - from: "data/dbt/models/marts/occurrence_places.sql"
      to: "geographies.places via ST_Within"
      via: "INNER JOIN, no DISTINCT ON"
      pattern: "ST_Within"
    - from: "data/run.py"
      to: "EXPORT_DIR/occurrence_places.parquet"
      via: "_run_dbt_build copy loop"
      pattern: "occurrence_places.parquet"
    - from: "data/sqlite_export.py"
      to: "out.occurrence_places SQLite table"
      via: "CREATE TABLE AS SELECT * FROM read_parquet"
      pattern: "occurrence_places"
---

<objective>
Wave 1 (pipeline): convert the one-place-per-occurrence partition into a
many-to-many `occurrence_places` bridge. Create the bridge mart, drop the scalar
`place_slug` from the occurrences mart (contract 33→32), remove the
overlap-rejection guard from validation, copy the bridge parquet through the
pipeline, ship it as a second table inside `occurrences.db` (with an index), and
fix the two hardcoded JS table whitelists.

Purpose: This is the dbt-green GATE for the whole phase — the dbt contract is the
schema validator (CLAUDE.md). Exports (160-03) and frontend (160-04) consume the
bridge and must not start until `bash data/dbt/run.sh build` is green here.
Output: a new bridge mart + parquet + occurrences.db table; a 32-col occurrences
contract; overlap-capable validation; updated copy loop + JS whitelists.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/160-overlap-capable-place-model-many-to-many-membership/160-CONTEXT.md
@.planning/phases/160-overlap-capable-place-model-many-to-many-membership/160-RESEARCH.md
@.planning/phases/160-overlap-capable-place-model-many-to-many-membership/160-01-SUMMARY.md
</context>

<artifacts_this_phase_produces>
- `data/dbt/models/marts/occurrence_places.sql` — NEW external parquet mart; one
  row per `(occ_id, place_slug)` membership.
- `occ_id` VARCHAR — synthetic canonical occurrence identity on the bridge
  (Option B), mirroring `src/occurrence.ts` `occIdFromRow` priority.
- `target/sandbox/occurrence_places.parquet` → copied to
  `public/data/occurrence_places.parquet` — NEW bridge artifact.
- `out.occurrence_places` — NEW SQLite table inside `occurrences.db`, with an
  index on `(place_slug, occ_id)`.
- Contract change: occurrences mart 33→32 cols (`place_slug` dropped);
  `occurrence_places` mart gains its own enforced contract.
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Create the bridge mart, drop place_slug from occurrences, update both contracts</name>
  <files>data/dbt/models/marts/occurrence_places.sql, data/dbt/models/marts/occurrences.sql, data/dbt/models/marts/schema.yml</files>
  <read_first>
    - data/dbt/models/marts/occurrences.sql:12-17 (the `materialized='external'` config block — exact structural analog for the bridge), :72-82 (`wa_places` / `with_place` / `place_dedup` CTEs), :97 (`fp.place_slug,` projection), :105 (`LEFT JOIN place_dedup fp`)
    - data/dbt/models/marts/schema.yml:4-7 (occurrences contract header), :61-62 (the `place_slug` column block to delete), :81-90 (a column block with `data_tests` as a structural example for the new contract)
    - data/dbt/models/intermediate/int_combined.sql (per-arm population of the four identity columns; confirms no single PK)
    - src/occurrence.ts:23-30 (`occIdFromRow` priority the bridge `occ_id` CASE must replicate verbatim)
    - 160-RESEARCH.md "Code Examples → Bridge mart" + "occurrences.sql edits" + "Contract edit" + Open Question 1 (Option B confirmed)
  </read_first>
  <action>
    Create `data/dbt/models/marts/occurrence_places.sql` using Option B (synthetic
    `occ_id`) per the locked discretion resolution and RESEARCH lean. Config:
    `materialized='external'`, `location='target/sandbox/occurrence_places.parquet'`,
    `format='parquet'`, `options={'CODEC': "'SNAPPY'"}` (mirror occurrences.sql:12-17).
    Body: `joined` CTE = `SELECT ROW_NUMBER() OVER () AS _row_id, * FROM
    {{ ref('int_combined') }}`; `occ_pt` adds `ST_Point(lon, lat) AS pt`; `wa_places`
    = `{{ source('geographies', 'places') }}`; `with_place` = INNER JOIN
    `ST_Within(occ_pt.pt, p.geom)` projecting `_row_id, p.slug AS place_slug`
    (INNER JOIN = no row when in no place, per D-discretion "empty membership"; NO
    `DISTINCT ON`). Final SELECT projects the Option-B `occ_id` CASE (ecdysis →
    `'ecdysis:'||ecdysis_id`; else observation → `'inat:'||observation_id`; else
    specimen_observation → `'inat_obs:'||specimen_observation_id`; else checklist →
    `'checklist:'||checklist_id`) and `place_slug`, joining `with_place` back to
    `joined` on `_row_id`, `ORDER BY occ_id, place_slug` (determinism). Add a header
    comment documenting the `occ_id` CASE↔`src/occurrence.ts:23` coupling (matching
    the existing `_GEO_COLS` positional-coupling doc convention). `_row_id` MUST NOT
    appear in the projected output. In `occurrences.sql`: remove the `with_place`
    (:74-78) and `place_dedup` (:79-82) CTEs and the `wa_places` CTE (:73) if no
    longer referenced, remove `fp.place_slug,` from the final SELECT (:97), and
    remove the `LEFT JOIN place_dedup fp …` line (:105) — leaving 32 projected
    columns. In `schema.yml`: delete the `- name: place_slug` / `data_type: varchar`
    block (:61-62) from the `occurrences` model (now 32 cols), and add a new
    contract block `- name: occurrence_places` with `config.contract.enforced: true`
    and columns `occ_id` (varchar, `data_tests: [not_null]`) and `place_slug`
    (varchar, `data_tests: [not_null]`).
  </action>
  <verify>
    <automated>bash data/dbt/run.sh build</automated>
  </verify>
  <acceptance_criteria>
    - `bash data/dbt/run.sh build` exits 0 with both contracts enforced
    - `occurrences.sql` final SELECT projects exactly 32 columns (place_slug gone); no `place_dedup`/`with_place` CTEs remain
    - `occurrence_places.sql` exists, uses INNER JOIN `ST_Within`, NO `DISTINCT ON`, projects only `(occ_id, place_slug)`, `ORDER BY occ_id, place_slug`
    - The bridge `occ_id` CASE branch order is ecdysis → inat → inat_obs → checklist, matching `src/occurrence.ts:23-30`
    - `schema.yml` occurrences model has no `place_slug`; an `occurrence_places` contract block exists with both columns `not_null`
    - `cd data && uv run pytest tests/test_occurrence_places.py -x` still GREEN
  </acceptance_criteria>
  <done>The bridge mart builds under contract, the occurrences contract is 32 cols, and the dbt build (the schema gate) is green.</done>
</task>

<task type="auto">
  <name>Task 2: Remove the overlap-rejection guard from places_validation.py</name>
  <files>data/places_validation.py</files>
  <read_first>
    - data/places_validation.py:6-14 (module docstring listing check #6 "Polygon overlap via ST_Intersects"), :100-133 (the WGS84 check tail + the overlap block at :109-133 using `ST_Overlaps` at :125)
    - data/tests/test_places_validation.py (the inverted `test_overlapping_polygons` from 160-01 + the WKT/WGS84/slug/permit tests that must still pass)
    - 160-CONTEXT.md D-03 (remove ST_Overlaps; KEEP WKT/WGS84/slug/permit)
    - 160-RESEARCH.md "Pitfall 5"
  </read_first>
  <action>
    Delete the entire overlap-check block in `places_validation.py` (:109-133 —
    the `if len(valid_geometries) >= 2:` … `places_geom` table creation, the
    `ST_Overlaps` pairwise query, and the `raise ValueError(… overlaps with …)`).
    Remove now-dead scaffolding only if it becomes unused (e.g. the `valid_geometries`
    accumulation is still used by the WKT/WGS84 checks — keep it; only the overlap
    consumer goes). Update the module docstring (:6-14) to drop check #6 ("Polygon
    overlap …") from the enumerated list. Do NOT touch checks #1–#5 (slug regex,
    duplicate slugs, permit fields, WKT validity, WGS84 bounds) or the
    `validate_places_step` wrapper. Overlapping polygons must now return `None`.
  </action>
  <verify>
    <automated>cd data && uv run pytest tests/test_places_validation.py -x</automated>
  </verify>
  <acceptance_criteria>
    - `cd data && uv run pytest tests/test_places_validation.py -x` GREEN — including the inverted `test_overlapping_polygons` (now asserts no raise) AND the unchanged slug/duplicate/WKT/WGS84/permit tests
    - `grep -n 'ST_Overlaps' data/places_validation.py` returns nothing
    - The docstring no longer lists an overlap check
  </acceptance_criteria>
  <done>Overlapping place polygons load cleanly (SC-2/D-03); all other validation checks still reject their bad inputs.</done>
</task>

<task type="auto">
  <name>Task 3: Ship the bridge — copy loop, occurrences.db table + index, JS whitelists</name>
  <files>data/run.py, data/sqlite_export.py, scripts/make-local-manifest.js, scripts/validate-db.mjs</files>
  <read_first>
    - data/run.py:78-84 (`_run_dbt_build` copy loop — the artifact tuple to extend)
    - data/sqlite_export.py:399-436 (`generate_sqlite` signature + the `CREATE TABLE out.occurrences AS SELECT * FROM read_parquet` block + DETACH ordering), :296-308 (`_create_taxa_indexes` — post-DETACH stdlib-sqlite3 index pattern to follow), :476-484 (`main` locating `occurrences.parquet` from `_DBT_SANDBOX`)
    - scripts/make-local-manifest.js:11-24 (the `occurrences_db_tables: ['geo_blob', 'occurrences']` array)
    - scripts/validate-db.mjs:16 (the `REQUIRED_TABLES = ['geo_blob', 'occurrences']` array)
    - 160-RESEARCH.md "Pitfall 1", "Pitfall 2", "sqlite_export.py" code example, Open Question 3 (index recommended)
  </read_first>
  <action>
    In `data/run.py`: add `"occurrence_places.parquet"` to the `_run_dbt_build`
    copy-loop artifact tuple (:80-81) so it lands in `EXPORT_DIR` alongside
    occurrences.parquet (Pitfall 1). In `data/sqlite_export.py`: after the
    `CREATE TABLE out.occurrences …` statement (:429-431) and BEFORE `DETACH out`,
    add `CREATE TABLE out.occurrence_places AS SELECT * FROM read_parquet('<bridge>')`,
    locating the bridge parquet the same way occurrences is located — derive a
    sibling path from `src_parquet` (e.g. `src_parquet.parent / "occurrence_places.parquet"`)
    or accept it as an injectable arg mirroring the existing `src_parquet` handling
    in `generate_sqlite`/`main` (:399-404, :476-481). After DETACH, extend
    `_create_taxa_indexes` (or add a sibling post-DETACH index step) to
    `CREATE INDEX IF NOT EXISTS idx_occ_places ON occurrence_places(place_slug, occ_id)`
    via the stdlib `_sqlite3` handle (WR-04 ordering — index AFTER DETACH, Open
    Question 3). In BOTH `scripts/make-local-manifest.js:14` and
    `scripts/validate-db.mjs:16`, add `'occurrence_places'` to the table array
    (Pitfall 2 — production nightly derives this dynamically, but local dev +
    CI clean-checkout validation use these hardcoded lists).
  </action>
  <verify>
    <automated>cd data && uv run python run.py 2>&1 | tail -5 && cd .. && node scripts/make-local-manifest.js && node scripts/validate-db.mjs</automated>
  </verify>
  <acceptance_criteria>
    - `cd data && uv run python run.py` completes (through `places-maps`) and produces `public/data/occurrence_places.parquet` and an `occurrences.db` containing an `occurrence_places` table (verify: `node -e` / sqlite query, or `node scripts/validate-db.mjs` accepts it)
    - `node scripts/validate-db.mjs` exits 0 with `occurrence_places` in REQUIRED_TABLES
    - `grep -c "occurrence_places" scripts/make-local-manifest.js` and `scripts/validate-db.mjs` each ≥ 1 (use `grep -v '^[[:space:]]*//' | grep -c` to skip comments)
    - `occurrences.db` has an index on `occurrence_places(place_slug, occ_id)`
    - The bridge parquet is in the `_run_dbt_build` copy loop
  </acceptance_criteria>
  <done>The bridge ships end-to-end: built by dbt, copied to EXPORT_DIR, materialized as an indexed occurrences.db table, and required by both JS whitelists (CI clean-checkout will see it).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| content/places.toml → pipeline | author-controlled polygons; slug regex `[a-z0-9-]` still enforced (check #1 retained) |
| pipeline parquet → occurrences.db | machine-generated; schema derived from parquet, no injection surface |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-160-02 | Tampering | dropping overlap check could admit malformed geometry | accept | Only the OVERLAP check is removed (D-03); WKT-validity (#4) and WGS84-bounds (#5) and slug-regex (#1) are retained — malformed/out-of-range geometry still rejected. Overlap is not a safety property. |
| T-160-03 | Tampering | bridge `occ_id` CASE drift from occIdFromRow | mitigate | Header-comment coupling doc + the 160-01 bridge test pins the CASE order; A2 (occ_id uniqueness) is implicitly checked by the not_null contract + the frontend's existing reliance on these IDs. |
| T-160-SC | Tampering | npm/pip/cargo installs | mitigate | No installs (RESEARCH: zero new packages). Gate N/A. |

No HIGH threats: no user-facing input crosses a trust boundary in the pipeline; all inputs are author-controlled TOML already regex/geometry-validated.
</threat_model>

<verification>
- `bash data/dbt/run.sh build` exits 0 (the schema gate) with occurrences=32 cols + occurrence_places contract
- `cd data && uv run pytest tests/test_occurrence_places.py tests/test_places_validation.py -x` GREEN
- `cd data && uv run python run.py` runs through `places-maps` producing the bridge parquet + occurrences.db table
- `node scripts/validate-db.mjs` accepts the rebuilt occurrences.db
- `npm test` for unaffected suites still green (no frontend change yet)
</verification>

<success_criteria>
SC-1 (occurrences contract no longer carries the scalar place_slug; bridge is source of truth), SC-2 (overlaps load), and SC-5 (deterministic multi-membership) are satisfied at the pipeline layer; the dbt build is green so 160-03/160-04 may proceed.
</success_criteria>

<output>
Create `.planning/phases/160-overlap-capable-place-model-many-to-many-membership/160-02-SUMMARY.md` when done. Record the chosen bridge-parquet path-resolution approach in sqlite_export.py and confirm test_places_validation overlap test is now GREEN.
</output>
