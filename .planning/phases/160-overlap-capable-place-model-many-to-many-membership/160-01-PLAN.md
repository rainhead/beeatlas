---
phase: 160-overlap-capable-place-model-many-to-many-membership
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - data/tests/test_occurrence_places.py
  - data/tests/test_places_validation.py
  - data/tests/test_places_export.py
  - src/tests/filter.test.ts
autonomous: true
requirements: [SC-2, SC-3, SC-5]
must_haves:
  truths:
    - "A test asserts a point in the overlap of two places produces exactly two bridge rows, sorted"
    - "test_places_validation asserts overlapping polygons LOAD (no raise) while WKT/WGS84/slug/permit still reject"
    - "test_places_export fixtures emit identity columns (no place_slug) + a bridge fixture, asserting double-count across two places"
    - "A filter.ts test asserts the place clause is an EXISTS membership subquery against occurrence_places (not place_slug =)"
  artifacts:
    - path: "data/tests/test_occurrence_places.py"
      provides: "DuckDB-level bridge SQL test against seeded overlapping places"
      contains: "def test_"
    - path: "data/tests/test_places_validation.py"
      provides: "inverted overlap test (loads, not rejects)"
      contains: "test_overlapping_polygons"
    - path: "data/tests/test_places_export.py"
      provides: "bridge-keyed double-count fixtures + expectations"
      contains: "occurrence_places"
    - path: "src/tests/filter.test.ts"
      provides: "EXISTS-membership place-clause assertion"
      contains: "occurrence_places"
  key_links:
    - from: "data/tests/test_occurrence_places.py"
      to: "the bridge SQL (occurrence_places.sql, built in 160-02)"
      via: "seeded DuckDB ST_Within join run inline (RED until 160-02)"
      pattern: "ST_Within"
---

<objective>
Wave 0: author the failing tests and fixtures that pin every observable behavior
of the overlap-capable place model BEFORE any production code changes. This plan
creates the Nyquist verification surface for plans 160-02/03/04: a DuckDB-level
bridge-membership test, an inverted overlap-acceptance test, double-counting
export fixtures, and a frontend EXISTS-clause assertion.

Purpose: Per 160-VALIDATION.md, the research flagged test fixtures as the single
biggest gap. These tests must exist and FAIL RED first so later plans turn them
GREEN — that is the proof the model change actually happened.
Output: 1 new test file + 3 updated test files. Most assertions are RED until
their producing plan lands (documented per task); the overlap-inversion and the
filter shape can be partly authored now and completed when filter.ts changes.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/160-overlap-capable-place-model-many-to-many-membership/160-CONTEXT.md
@.planning/phases/160-overlap-capable-place-model-many-to-many-membership/160-RESEARCH.md
@.planning/phases/160-overlap-capable-place-model-many-to-many-membership/160-VALIDATION.md
</context>

<artifacts_this_phase_produces>
This plan (Wave 0) produces test scaffolds only. The symbols they reference,
produced by later plans:
- `occurrence_places` dbt mart / parquet / `occurrences.db` table (160-02)
- synthetic `occ_id` VARCHAR column on the bridge (Option B, 160-02)
- `_query_counts` bridge-JOIN rewrite (160-03)
- `filter.ts` EXISTS membership clause (160-04)
This plan itself produces: `data/tests/test_occurrence_places.py` (new), and
updated fixtures/assertions in the three existing test files.
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Bridge-membership DuckDB test + invert the overlap-acceptance test</name>
  <files>data/tests/test_occurrence_places.py, data/tests/test_places_validation.py</files>
  <read_first>
    - data/tests/test_places_export.py (the `_seed_places_db` pattern at :24 — analog for seeding a DuckDB with WKT polygons + LOAD spatial; reuse its connection/seed style)
    - data/tests/test_places_validation.py:135-156 (current `test_overlapping_polygons` that asserts `pytest.raises(ValueError, match="overlap")`)
    - data/dbt/models/marts/occurrences.sql:72-82 (the `ST_Within` join + `place_dedup` the bridge SQL is sourced from, minus the dedup)
    - src/occurrence.ts:23-30 (`occIdFromRow` priority — the canonical identity the bridge `occ_id` CASE must mirror)
    - 160-RESEARCH.md "Code Examples → Bridge mart" + "Pitfall 5"
  </read_first>
  <action>
    Create `data/tests/test_occurrence_places.py`. Seed an in-memory/temp DuckDB
    (LOAD spatial) with: (a) a tiny occurrences-shaped input table carrying the
    four identity columns `ecdysis_id, observation_id, specimen_observation_id,
    checklist_id` plus `lon, lat` for ~3 points — one inside place A only, one
    inside place B only, one inside the A∩B overlap; (b) a `places` table with
    two partially-overlapping WKT polygons (slugs `place-a`, `place-b`) using the
    same coordinate style as test_places_validation's overlapping fixture
    (lon -121.0..-120.9 / -120.95..-120.85, lat 47.0..47.1). Run the bridge SQL
    body inline (the `ST_Within` JOIN with NO `DISTINCT ON`, projecting the
    Option-B `occ_id` CASE that mirrors `occIdFromRow` — `'ecdysis:'||ecdysis_id`,
    else `'inat:'||observation_id`, else `'inat_obs:'||specimen_observation_id`,
    else `'checklist:'||checklist_id` — and `place_slug`, `ORDER BY occ_id,
    place_slug`). Assert: the overlap point yields EXACTLY two rows
    (`(occ_id,'place-a')` and `(occ_id,'place-b')`) in sorted order (D-05/SC-5);
    the A-only point yields one row; the B-only point yields one row; output is
    byte-stable across two runs (determinism — Pitfall 4). Mark with a module
    docstring noting this is the executable spec for `occurrence_places.sql`
    (160-02) — it is RED-by-design only if the inline SQL diverges; since the SQL
    is run inline here it should PASS once authored (it tests the recipe, not the
    mart file). In `test_places_validation.py`, INVERT `test_overlapping_polygons`
    (:135-156): keep the two overlapping-polygon fixtures, but assert
    `validate_places(toml_path)` returns `None` (no raise) — overlaps now LOAD
    (D-03/SC-2). Update the test name/docstring to "overlapping polygons load".
    Do NOT touch the slug/duplicate/WKT/WGS84/permit tests.
  </action>
  <verify>
    <automated>cd data && uv run pytest tests/test_occurrence_places.py -x</automated>
  </verify>
  <acceptance_criteria>
    - `cd data && uv run pytest tests/test_occurrence_places.py -x` passes (the inline bridge SQL is self-contained, so this is GREEN on authoring)
    - `cd data && uv run pytest tests/test_places_validation.py::test_overlapping_polygons -x` FAILS RED now (the live `ST_Overlaps` guard still raises) — turns GREEN in 160-02
    - The overlap point assertion checks for exactly two rows with both `place-a` and `place-b`, sorted by `(occ_id, place_slug)`
    - test_occurrence_places `occ_id` CASE order matches `src/occurrence.ts:23-30` exactly (ecdysis → inat → inat_obs → checklist)
    - WKT/WGS84/slug/permit tests in test_places_validation are unchanged
  </acceptance_criteria>
  <done>The bridge membership recipe and the overlap-acceptance behavior are both expressed as automated tests; the inverted overlap test is RED pending 160-02.</done>
</task>

<task type="auto">
  <name>Task 2: Rewrite export count fixtures for bridge double-counting + frontend EXISTS-clause test</name>
  <files>data/tests/test_places_export.py, src/tests/filter.test.ts</files>
  <read_first>
    - data/tests/test_places_export.py:64-93 (`_write_test_occurrences_parquet` — has a `place_slug` column to remove), :96-108 (`_setup_env`), :155-170 (`test_places_json_counts`)
    - data/places_export.py:40-66 (`_query_counts` current single-table read — to be rewritten in 160-03)
    - src/tests/filter.test.ts:276-277 (`OCCURRENCE_COLUMNS includes place_slug` — to be inverted), :321-340 (the three `selectedPlace` buildFilterSQL place-clause tests — to be rewritten to EXISTS)
    - 160-RESEARCH.md "Pitfall 3", "Code Examples → _query_counts rewrite", "filter.ts edits"
  </read_first>
  <action>
    In `test_places_export.py`: rewrite `_write_test_occurrences_parquet` to DROP
    the `place_slug` column and instead emit the identity columns the bridge keys
    on (`ecdysis_id, observation_id, specimen_observation_id, checklist_id`,
    plus `is_provisional, sample_id, lon, lat`). Add a new helper
    `_write_test_bridge_parquet` writing `occurrence_places.parquet` with columns
    `(occ_id VARCHAR, place_slug VARCHAR)` such that ONE occurrence is a member of
    BOTH `place-a` AND `place-b` (two bridge rows for the same `occ_id`) and at
    least one occurrence is in a single place. Wire it into `_setup_env` and a
    two-place `_write_test_toml`. Rewrite `test_places_json_counts` expectations to
    assert the shared occurrence increments BOTH places' counts (double-count,
    D-05/SC-3): e.g. an Ecdysis-backed occurrence in A∩B contributes specimen_count
    1 to place-a AND 1 to place-b. These assertions are RED until 160-03 rewrites
    `_query_counts` to JOIN the bridge — document that in the test docstring.
    In `src/tests/filter.test.ts`: change the `OCCURRENCE_COLUMNS includes
    place_slug` test (:276) to assert `OCCURRENCE_COLUMNS` does NOT contain
    `place_slug` (SC-1/D-02). Rewrite the three `selectedPlace` place-clause tests
    (:321-340) so the active-place case asserts `occurrenceWhere` contains
    `EXISTS` and `occurrence_places` and `op.place_slug = 'ebeys-landing'` (NOT
    `place_slug = '…'` as a bare occurrences column); the null case asserts no
    `occurrence_places`/`EXISTS` place clause; the quote-escape case asserts
    `op.place_slug = 'o''brien-ranch'` (escaping preserved per threat model). These
    are RED until 160-04 rewrites filter.ts.
  </action>
  <verify>
    <automated>cd data && uv run pytest tests/test_places_export.py -x ; cd .. && npm test -- filter</automated>
  </verify>
  <acceptance_criteria>
    - `_write_test_occurrences_parquet` no longer references `place_slug`; identity columns present
    - A `_write_test_bridge_parquet` (or equivalent) emits `(occ_id, place_slug)` with one occ_id in two places
    - `test_places_json_counts` asserts double-counting (same occurrence → both place counts); RED now, GREEN after 160-03
    - filter.test.ts asserts `OCCURRENCE_COLUMNS` excludes `place_slug` and that the place clause is an `EXISTS … occurrence_places … op.place_slug = ?` subquery; RED now, GREEN after 160-04
    - The quote-escape assertion keeps `''` doubling (no regression of the injection mitigation)
  </acceptance_criteria>
  <done>Export counts and the frontend filter shape are both pinned as automated tests that go GREEN exactly when their producing plan lands; no test still references the scalar place_slug as the source of truth.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user-selected place slug → client SQL (filter.ts) | the only user-influenced value crossing into a query in this phase |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-160-01 | Tampering (SQLi) | filter.ts place clause | mitigate | This Wave-0 plan's filter.test.ts asserts the rewritten EXISTS clause preserves the `'/''` escaping (`op.place_slug = 'o''brien-ranch'`); slugs are also `[a-z0-9-]`-constrained at validation. No HIGH threat. |
| T-160-SC | Tampering | npm/pip/cargo installs | mitigate | No installs in this phase (RESEARCH "Package Legitimacy Audit": zero new packages). Gate N/A. |

No HIGH threats expected: static hosting, no server runtime, single user-influenced value already escaped.
</threat_model>

<verification>
- `cd data && uv run pytest tests/test_occurrence_places.py -x` GREEN (self-contained recipe test)
- `cd data && uv run pytest tests/test_places_validation.py::test_overlapping_polygons -x` RED (pending 160-02)
- `cd data && uv run pytest tests/test_places_export.py::test_places_json_counts -x` RED (pending 160-03)
- `npm test -- filter` shows the place-clause tests RED (pending 160-04)
- The slug/WKT/WGS84/permit validation tests remain GREEN (no regression)
</verification>

<success_criteria>
All four test files express the target behavior; RED-by-design assertions are documented per task and map 1:1 to 160-02/03/04 producing plans (Nyquist sampling continuity).
</success_criteria>

<output>
Create `.planning/phases/160-overlap-capable-place-model-many-to-many-membership/160-01-SUMMARY.md` when done. Record which assertions are RED-pending-which-plan so executors of 160-02/03/04 know their done-state.
</output>
