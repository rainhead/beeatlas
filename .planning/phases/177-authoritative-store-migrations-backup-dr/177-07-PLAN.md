---
phase: 177
plan: 07
type: execute
wave: 4
depends_on: [177-02, 177-04, 177-06]
files_modified:
  - docs/runbooks/notes-store-dr.md
autonomous: false
requirements: [STORE-03, STORE-04]
user_setup:
  - service: aws-cdk
    why: "Deploy the AuthoritativeBackupBucket (plan 177-02) so backups have a destination"
    dashboard_config:
      - task: "cdk deploy the surgical BeeAtlasStack edit (never cdk destroy)"
        location: "infra/ on an operator machine with AWS credentials"
  - service: maderas
    why: "The store, its backup cron, and the full nightly run all live on maderas"
    env_vars:
      - name: NOTES_DB_PATH
        source: "operator-chosen persistent path outside EXPORT_DIR/DB_PATH/git"
      - name: NOTES_BACKUP_BUCKET
        source: "CDK output BackupBucketName from the cdk deploy"

must_haves:
  truths:
    - "The AuthoritativeBackupBucket is deployed and the store is bootstrapped on maderas (alembic upgrade head + WAL sidecars created)"
    - "A backup snapshot has been demonstrably restored into a scratch DB with matching row count + alembic_version — recorded in the Drill Log (STORE-03 / D-12 launch gate)"
    - "A full green nightly leaves notes.db (sha256/mtime) and the backup bucket object list unchanged (STORE-04 / D-17)"
  artifacts:
    - path: "docs/runbooks/notes-store-dr.md"
      provides: "Drill Log entries recording the demonstrated restore + isolation proof"
      contains: "Drill Log"
  key_links:
    - from: "full nightly on maderas"
      to: "notes.db + backup bucket"
      via: "physical + IAM isolation (unchanged after run)"
      pattern: "unchanged"
---

<objective>
Close the phase's two operator-only gates on maderas — these are the STORE-03 and STORE-04 proofs that cannot run locally (memory `project_local_dbt_build_not_runnable`) and MUST NOT auto-advance (memory `feedback_uat_ui_phases`). This plan is `autonomous: false`; the chain pauses here.

1. **Bootstrap** — deploy the backup bucket (`cd infra && cdk deploy`, never destroy), create the persistent store path outside the pipeline paths, run `alembic upgrade head`, optionally seed, start uvicorn once to create WAL sidecars, and take one real backup to the bucket.
2. **Restore drill (STORE-03 / D-12)** — download the latest snapshot, restore into a scratch DB, confirm row count + `alembic_version` match the live store, and record the run in the runbook Drill Log. This is the launch gate that Phase 178's WRITE-04 depends on: no public write until this restore is demonstrated.
3. **Isolation proof (STORE-04 / D-17)** — run a full `bash data/nightly.sh` on maderas, then confirm the `notes.db` sha256/mtime AND the backup bucket object list are unchanged — proving the derived pipeline (DuckDB rebuild, `s3 cp` to `data/`/`db/`/`raw/`) physically and by IAM cannot reach authoritative data.

Purpose: an untested backup is not a backup, and an unproven isolation boundary is not a boundary. Both must be demonstrated before the phase closes.
Output: two Drill Log entries in `docs/runbooks/notes-store-dr.md`.

All three tasks are blocking checkpoints. Follow `docs/runbooks/notes-store-dr.md` (plan 177-06) for exact commands.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/177-authoritative-store-migrations-backup-dr/177-CONTEXT.md
@.planning/phases/177-authoritative-store-migrations-backup-dr/177-RESEARCH.md
@docs/runbooks/notes-store-dr.md
</context>

<artifacts_produced>
This plan produces no code. It produces two operator records:
- `docs/runbooks/notes-store-dr.md` Drill Log entry: demonstrated restore (date, snapshot key, source vs restored row count + alembic_version) — STORE-03 launch-gate evidence
- `docs/runbooks/notes-store-dr.md` Drill Log entry: isolation proof (date, notes.db sha256 before/after nightly, backup bucket object count before/after) — STORE-04 evidence
</artifacts_produced>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: Deploy backup bucket + bootstrap the store on maderas</name>
  <read_first>
    - docs/runbooks/notes-store-dr.md (Bootstrap + Store placement sections)
    - .planning/phases/177-authoritative-store-migrations-backup-dr/177-RESEARCH.md (Physical File Placement; Alembic run command)
  </read_first>
  <action>Operator, on a machine with AWS credentials: `cd infra && npm run deploy` (cdk deploy — surgical BeeAtlasStack edit; NEVER cdk destroy) and note the `BackupBucketName` output. Then on maderas: create the persistent store directory OUTSIDE `EXPORT_DIR` (`/tmp/beeatlas-export`), `DB_PATH` (`/tmp/beeatlas.duckdb`), and the git checkout (default `/opt/beeatlas-store/`); export `NOTES_DB_PATH=<that path>/notes.db`; run `cd data && NOTES_DB_PATH=$NOTES_DB_PATH uv run alembic -c notes_store/migrations/alembic.ini upgrade head`; optionally `uv run python -m notes_store.seed`; start `uvicorn notes_app.main:app --host 127.0.0.1 --port 8001` once (creating the `-wal`/`-shm` sidecars); export `NOTES_BACKUP_BUCKET=<BackupBucketName>` and run `uv run python backup_notes.py` once to land the first snapshot.</action>
  <what-built>The deployed backup bucket and a bootstrapped, migrated store on maderas with one real snapshot in S3.</what-built>
  <how-to-verify>
    1. `aws --profile beeatlas s3 ls s3://<BackupBucketName>/backups/` shows ≥1 object.
    2. `sqlite3 $NOTES_DB_PATH "SELECT version_num FROM alembic_version;"` returns `0001`.
    3. `$NOTES_DB_PATH` resolves to a path NOT under `/tmp/beeatlas-export`, `/tmp/beeatlas.duckdb`, or the git checkout.
    4. `ls $NOTES_DB_PATH-wal $NOTES_DB_PATH-shm` shows the WAL sidecars exist.
  </how-to-verify>
  <verify>
    <automated>MISSING — operator-only (maderas + AWS credentials); verified via the how-to-verify commands, not local CI</automated>
  </verify>
  <acceptance_criteria>
    - Backup bucket deployed; `alembic_version` == 0001 on the maderas store; store path outside pipeline paths; WAL sidecars present; ≥1 snapshot object in the bucket.
  </acceptance_criteria>
  <resume-signal>Type "bootstrapped" with the store path + bucket name, or describe any blocker.</resume-signal>
  <done>The bucket exists, the store is migrated and running on maderas, and a first snapshot is in S3.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Demonstrated restore drill (STORE-03 / D-12 launch gate)</name>
  <read_first>
    - docs/runbooks/notes-store-dr.md (Restore drill section + Drill Log)
    - .planning/phases/177-authoritative-store-migrations-backup-dr/177-RESEARCH.md (Pattern 5: restore drill)
  </read_first>
  <action>Operator, on maderas: follow the runbook restore drill — `aws --profile beeatlas s3 ls s3://$NOTES_BACKUP_BUCKET/backups/` and pick the latest, `aws s3 cp` it to `/tmp/restore_test.db.gz`, `gunzip -k`, then open `/tmp/restore_test.db` with sqlite3 and read the table list, `SELECT count(*) FROM notes`, and `SELECT version_num FROM alembic_version`. Compare against the live store ($NOTES_DB_PATH) — row count and schema version MUST match. Append a Drill Log entry to `docs/runbooks/notes-store-dr.md` recording: date, snapshot S3 key, live vs restored notes count, live vs restored alembic_version, and PASS/FAIL. This is the WRITE-04 launch gate — no public write may open until this passes.</action>
  <what-built>A demonstrated end-to-end restore from an S3 snapshot into a scratch DB, with a recorded Drill Log entry.</what-built>
  <how-to-verify>
    1. Restored `/tmp/restore_test.db` opens as a valid SQLite DB with the `notes` + `note_revisions` tables.
    2. Restored `notes` count == live store `notes` count.
    3. Restored `alembic_version.version_num` == live `alembic_version.version_num`.
    4. A dated Drill Log entry with these numbers exists in `docs/runbooks/notes-store-dr.md`.
  </how-to-verify>
  <verify>
    <automated>MISSING — operator-only restore drill on maderas (S3 + real store); recorded in the Drill Log, not local CI</automated>
  </verify>
  <acceptance_criteria>
    - Restored DB row count + `alembic_version` match the live store; Drill Log entry recorded with PASS.
  </acceptance_criteria>
  <resume-signal>Type "restore verified" with the recorded counts, or describe the mismatch.</resume-signal>
  <done>A snapshot has been demonstrably restored with matching row count + schema version; the STORE-03 launch gate is satisfied.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Nightly isolation proof (STORE-04 / D-17)</name>
  <read_first>
    - docs/runbooks/notes-store-dr.md (Isolation proof section)
    - .planning/phases/177-authoritative-store-migrations-backup-dr/177-RESEARCH.md (Pitfall 7: isolation demonstration gap)
    - data/nightly.sh (the full pipeline whose s3 cp targets must not reach the store)
  </read_first>
  <action>Operator, on maderas: record the pre-state — `sha256sum $NOTES_DB_PATH` and `aws --profile beeatlas s3 ls s3://$NOTES_BACKUP_BUCKET/backups/ | wc -l`. Run a full green nightly: `bash data/nightly.sh` (the real gate — a subset run does NOT count, Pitfall 7). After it completes, record the post-state: `sha256sum $NOTES_DB_PATH` and the backup bucket object count. Confirm BOTH are unchanged — the derived pipeline (DuckDB rebuild + `s3 cp --recursive` to `data/`/`db/`/`raw/`) physically and by IAM could not reach the store file or the backup bucket. Append an isolation-proof Drill Log entry to `docs/runbooks/notes-store-dr.md`: date, notes.db sha256 before/after, bucket object count before/after, and PASS/FAIL.</action>
  <what-built>A demonstrated full-nightly run proving physical + IAM isolation of the authoritative store and its backups.</what-built>
  <how-to-verify>
    1. `bash data/nightly.sh` completes green (publishes as usual).
    2. `sha256sum $NOTES_DB_PATH` is identical before and after the run.
    3. The backup bucket object count is identical before and after (nightly never writes/deletes there).
    4. A dated isolation-proof Drill Log entry with these values exists in the runbook.
  </how-to-verify>
  <verify>
    <automated>MISSING — requires a full run.py/dbt rebuild + S3 push on maderas (cannot run locally); recorded in the Drill Log</automated>
  </verify>
  <acceptance_criteria>
    - notes.db sha256 unchanged AND backup bucket object count unchanged across a full green nightly; Drill Log entry recorded with PASS.
  </acceptance_criteria>
  <resume-signal>Type "isolation verified" with the before/after hashes + counts, or describe what changed.</resume-signal>
  <done>A full green nightly demonstrably leaves the store file and its backups untouched (STORE-04 satisfied).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Derived nightly pipeline → authoritative store + backups | The green nightly must be proven unable to reach either |
| Backup existence → demonstrated recoverability | Recoverability is only real once a restore is demonstrated |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-177-06 | Repudiation / Integrity | Undemonstrated backup | mitigate | Task 2 demonstrated restore (D-12) — the WRITE-04 launch gate; recorded in the Drill Log |
| T-177-02 | Tampering | Nightly reaching/overwriting the store | mitigate | Task 3 isolation proof — notes.db hash + bucket object count unchanged after a full nightly (D-17, Pitfall 7) |
| T-177-BK2 | Denial of Service | Accidental `cdk destroy` of the backup bucket | mitigate | RemovalPolicy.RETAIN (plan 177-02) + operator instruction "never cdk destroy" (memory `project_cdk_stack_composition`) |
</threat_model>

<verification>
- Bootstrap: alembic_version == 0001 on maderas; ≥1 snapshot in the bucket; store path outside pipeline paths
- Restore drill: restored row count + alembic_version match live; Drill Log PASS entry
- Isolation proof: notes.db sha256 + bucket object count unchanged across a full nightly; Drill Log PASS entry
- These are operator-verified (autonomous: false) — the chain must NOT auto-advance past this plan
</verification>

<success_criteria>
- Backup bucket deployed; store bootstrapped + migrated on maderas
- Demonstrated restore recorded (STORE-03 / D-12 launch gate met)
- Full-nightly isolation proof recorded (STORE-04 / D-17 met)
</success_criteria>

<output>
Create `.planning/phases/177-authoritative-store-migrations-backup-dr/177-07-SUMMARY.md` when done
</output>
