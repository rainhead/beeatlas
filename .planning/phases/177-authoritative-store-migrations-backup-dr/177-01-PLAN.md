---
phase: 177
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - data/pyproject.toml
  - data/notes_store/__init__.py
  - data/notes_app/__init__.py
  - .planning/REQUIREMENTS.md
autonomous: false
requirements: [STORE-01, STORE-02, STORE-03]
user_setup: []

must_haves:
  truths:
    - "alembic, sqlalchemy, fastapi, uvicorn are importable in the data/ venv"
    - "REQUIREMENTS.md STORE-03 describes snapshot-based recovery (not native PITR) with Litestream deferred"
    - "notes_store and notes_app are importable Python packages"
  artifacts:
    - path: "data/pyproject.toml"
      provides: "alembic + sqlalchemy + fastapi + uvicorn dependency pins"
      contains: "alembic"
    - path: "data/notes_store/__init__.py"
      provides: "notes_store package marker"
    - path: "data/notes_app/__init__.py"
      provides: "notes_app package marker"
    - path: ".planning/REQUIREMENTS.md"
      provides: "STORE-03 relaxed wording (snapshot-based now, Litestream later)"
      contains: "consistent snapshot"
  key_links:
    - from: "data/pyproject.toml"
      to: "notes_store / notes_app / backup_notes"
      via: "uv-installed dependencies"
      pattern: "alembic|sqlalchemy|fastapi|uvicorn"
---

<objective>
Lay the phase foundation: add the four new Python dependencies (alembic, sqlalchemy, fastapi, uvicorn) to `data/pyproject.toml`, create the `notes_store` and `notes_app` package markers, and relax the REQUIREMENTS.md STORE-03 wording per D-11 so the phase VERIFICATION does not read the backup gate as unmet.

This plan gates on a package-legitimacy human checkpoint: the RESEARCH Package Legitimacy Audit tagged all four packages `[ASSUMED]` (slopcheck was unavailable at research time), so a blocking human verification against PyPI precedes the install (threat T-177-SC).

Purpose: every downstream plan (schema, migrations, backup) imports these packages; the REQUIREMENTS wording is a phase-hygiene prerequisite for VERIFICATION.
Output: updated `data/pyproject.toml` (+ `uv.lock` refresh), two package `__init__.py` files, corrected STORE-03 requirement text.

NOTE (deferred, do NOT act on here): the ROADMAP.md re-scope for Phases 178/179 (the SQLite-on-maderas pivot away from "API Gateway + Lambda") is a separate future roadmap edit — CONTEXT.md Deferred Ideas. This plan touches ONLY the STORE-03 line in REQUIREMENTS.md, nothing in ROADMAP.md.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/177-authoritative-store-migrations-backup-dr/177-CONTEXT.md
@.planning/phases/177-authoritative-store-migrations-backup-dr/177-RESEARCH.md
</context>

<artifacts_produced>
This plan creates/modifies these symbols:
- `data/pyproject.toml` dependency additions: `alembic>=1.18.5`, `sqlalchemy>=2.0.51,<3`, `fastapi>=0.139.0`, `uvicorn>=0.49.0`
- `data/notes_store/__init__.py` — package marker (new package `notes_store`)
- `data/notes_app/__init__.py` — package marker (new package `notes_app`)
- `.planning/REQUIREMENTS.md` STORE-03 revised wording key phrase: "consistent snapshots"
</artifacts_produced>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: Package legitimacy verification (blocking, T-177-SC)</name>
  <read_first>
    - .planning/phases/177-authoritative-store-migrations-backup-dr/177-RESEARCH.md (## Package Legitimacy Audit)
  </read_first>
  <action>Present the four new packages to the operator for legitimacy verification before any install, because the RESEARCH audit tagged them `[ASSUMED]` (slopcheck unavailable). Ask the operator to confirm each package on PyPI: alembic → pypi.org/project/alembic (repo github.com/sqlalchemy/alembic), sqlalchemy → pypi.org/project/SQLAlchemy (repo github.com/sqlalchemy/sqlalchemy), fastapi → pypi.org/project/fastapi (repo github.com/fastapi/fastapi), uvicorn → pypi.org/project/uvicorn (repo github.com/encode/uvicorn). Do NOT run `uv add`/`uv sync` until the operator approves. This checkpoint is never auto-approvable.</action>
  <what-built>Nothing yet — this is a pre-install supply-chain gate for the four `[ASSUMED]` packages.</what-built>
  <how-to-verify>
    1. Open pypi.org/project/alembic, /SQLAlchemy, /fastapi, /uvicorn.
    2. Confirm each is the flagship project (SQLAlchemy org for alembic+sqlalchemy; fastapi org for fastapi; encode org for uvicorn), with high download counts and a matching source repo.
    3. Confirm no typosquat (exact names: alembic, SQLAlchemy, fastapi, uvicorn).
  </how-to-verify>
  <verify>
    <automated>MISSING — human checkpoint; no automated gate (supply-chain legitimacy per package-legitimacy protocol)</automated>
  </verify>
  <acceptance_criteria>
    - Operator explicitly approves all four packages, or names a specific one to swap/hold.
  </acceptance_criteria>
  <resume-signal>Type "approved" to proceed with install, or name any package to hold.</resume-signal>
  <done>Operator has verified the four packages against PyPI and approved the install.</done>
</task>

<task type="auto">
  <name>Task 2: Add dependencies and create package markers</name>
  <files>data/pyproject.toml, data/notes_store/__init__.py, data/notes_app/__init__.py</files>
  <read_first>
    - data/pyproject.toml (the file being modified — existing `[project].dependencies` array + `requires-python = ">=3.14"`)
    - data/config.py (module-level tomllib load convention the new packages will mirror)
  </read_first>
  <action>Append four entries to the `dependencies` array in `data/pyproject.toml`, matching the RESEARCH Standard Stack pins: `alembic>=1.18.5`, `sqlalchemy>=2.0.51,<3`, `fastapi>=0.139.0`, `uvicorn>=0.49.0`. boto3 is already present (do not re-add). Then run `uv sync` in `data/` to install and refresh `uv.lock`. Create empty package-marker files `data/notes_store/__init__.py` and `data/notes_app/__init__.py` (a one-line module docstring is fine, e.g. describing each package's role: notes_store = authoritative store schema/engine/migrations; notes_app = maderas-hosted FastAPI service). Do NOT create any other files in these packages — later plans own them.</action>
  <verify>
    <automated>cd data && uv sync && uv run python -c "import alembic, sqlalchemy, fastapi, uvicorn, notes_store, notes_app; print('ok')"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c -E '^\s*"(alembic|sqlalchemy|fastapi|uvicorn)' data/pyproject.toml` returns 4
    - `cd data && uv run python -c "import alembic, sqlalchemy, fastapi, uvicorn"` exits 0
    - `data/notes_store/__init__.py` and `data/notes_app/__init__.py` both exist
  </acceptance_criteria>
  <done>The four packages install and import; both new packages are importable.</done>
</task>

<task type="auto">
  <name>Task 3: Relax REQUIREMENTS.md STORE-03 wording (D-11)</name>
  <files>.planning/REQUIREMENTS.md</files>
  <read_first>
    - .planning/REQUIREMENTS.md (the STORE-03 line being rewritten — line ~29)
    - .planning/phases/177-authoritative-store-migrations-backup-dr/177-CONTEXT.md (D-11 decision text)
  </read_first>
  <action>Replace the STORE-03 requirement line. The current text demands "native point-in-time recovery **plus** an independent periodic logical dump". Rewrite it to reflect the D-11 decision: the gate is now frequent **consistent snapshots** (SQLite online-backup API) pushed to a dedicated, IAM-isolated, versioned S3 bucket, and a **test-restore demonstrated before any public write** exists; continuous/near-second point-in-time recovery (Litestream) is explicitly deferred to a later phase, with RPO = snapshot interval matching the stated risk tolerance. Keep the requirement ID `STORE-03` and its checkbox unchanged; edit only the description sentence. Do NOT touch any other requirement or the Traceability table. Do NOT edit ROADMAP.md (the 178/179 re-scope is deferred).</action>
  <verify>
    <automated>grep -q "consistent snapshot" .planning/REQUIREMENTS.md && grep -q "Litestream" .planning/REQUIREMENTS.md && ! grep -q "native point-in-time recovery" .planning/REQUIREMENTS.md && echo ok</automated>
  </verify>
  <acceptance_criteria>
    - STORE-03 line contains "consistent snapshot" and "test-restore demonstrated before any public write"
    - STORE-03 line contains "Litestream" as the deferred continuous-PITR option
    - The phrase "native point-in-time recovery" no longer appears in REQUIREMENTS.md
    - No other requirement lines changed (`git diff --stat` shows only REQUIREMENTS.md among planning files, one logical line changed)
  </acceptance_criteria>
  <done>STORE-03 reads as snapshot-based-now / Litestream-later so VERIFICATION does not read the gate as unmet.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| PyPI → data/ venv | Third-party package supply chain crosses into the pipeline host |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-177-SC | Tampering | `uv sync` install of alembic/sqlalchemy/fastapi/uvicorn | mitigate | Blocking human legitimacy checkpoint (Task 1) verifies each `[ASSUMED]` package on PyPI before install; never auto-approvable |
</threat_model>

<verification>
- `cd data && uv run python -c "import alembic, sqlalchemy, fastapi, uvicorn, notes_store, notes_app"` exits 0
- REQUIREMENTS.md STORE-03 grep assertions pass (see Task 3)
- `cd data && uv run pytest -m "not integration" -q` still green (no regressions from dependency additions)
</verification>

<success_criteria>
- Four packages installed and importable in the data/ venv
- notes_store + notes_app packages exist
- STORE-03 wording relaxed per D-11
- Package-legitimacy checkpoint approved by operator
</success_criteria>

<output>
Create `.planning/phases/177-authoritative-store-migrations-backup-dr/177-01-SUMMARY.md` when done
</output>
