---
phase: 110
plan: 02
type: execute
wave: 2
depends_on: [110-01]
files_modified:
  - data/inaturalist_pipeline.py
  - data/waba_pipeline.py
  - data/run.py
  - data/dbt/models/staging/stg_waba__taxon_lineage.sql
  - data/dbt/models/sources.yml
  - data/tests/test_taxon_lineage_extended.py
  - data/tests/test_taxon_lineage.py
autonomous: true
requirements: [TAX-03]
must_haves:
  truths:
    - "grep for `def enrich_taxon_lineage_extended` in data/inaturalist_pipeline.py returns 0 matches"
    - "grep for `def enrich_taxon_lineage` in data/waba_pipeline.py returns 0 matches"
    - "grep for `enrich_taxon_lineage` anywhere under data/ (excluding .planning/ and target/) returns 0 matches"
    - "data/dbt/models/sources.yml has no `taxon_lineage` entry under `inaturalist_waba_data`"
    - "data/dbt/models/staging/stg_waba__taxon_lineage.sql selects taxon_id, genus, family FROM ref('stg_inat__taxon_lineage_extended')"
    - "run.py STEPS contains ('taxa-download', download_taxa_csv) immediately before ('taxon-lineage-extended', load_taxon_lineage_extended)"
    - "`bash data/dbt/run.sh build` exits 0"
    - "`npm test` exits 0"
    - "`cd data && uv run pytest` exits 0 (full data suite green after dead test removal)"
  artifacts:
    - path: "data/dbt/models/staging/stg_waba__taxon_lineage.sql"
      provides: "View on stg_inat__taxon_lineage_extended (D-01)"
      contains: "ref('stg_inat__taxon_lineage_extended')"
    - path: "data/run.py"
      provides: "STEPS list with taxa-download before taxon-lineage-extended"
      contains: "taxa-download"
    - path: "data/run.py"
      provides: "Updated module imports (no enrich_taxon_lineage_extended)"
      contains: "from taxa_pipeline import download_taxa_csv, load_taxon_lineage_extended"
  key_links:
    - from: "data/run.py STEPS"
      to: "data/taxa_pipeline.py"
      via: "import"
      pattern: "from taxa_pipeline import download_taxa_csv, load_taxon_lineage_extended"
    - from: "data/dbt/models/staging/stg_waba__taxon_lineage.sql"
      to: "data/dbt/models/staging/stg_inat__taxon_lineage_extended.sql"
      via: "dbt ref()"
      pattern: "ref\\(\\s*['\\\"]stg_inat__taxon_lineage_extended['\\\"]\\s*\\)"
    - from: "data/dbt/models/intermediate/int_specimen_obs_base.sql"
      to: "data/dbt/models/staging/stg_waba__taxon_lineage.sql"
      via: "dbt ref() — UNCHANGED in this plan"
      pattern: "stg_waba__taxon_lineage"
---

<objective>
Cut over to the offline taxonomy: delete the two live `/v2/taxa` enricher functions (`enrich_taxon_lineage_extended` in `inaturalist_pipeline.py`, `enrich_taxon_lineage` in `waba_pipeline.py`), rewire `data/run.py` STEPS to call the new `taxa_pipeline` module, rewrite `stg_waba__taxon_lineage.sql` per D-01 to delegate to `stg_inat__taxon_lineage_extended` via `ref()`, remove the `inaturalist_waba_data.taxon_lineage` source entry per D-02, delete the two now-dead test files, and prove the cutover with `bash data/dbt/run.sh build` and `npm test` green.

Purpose: Realize TAX-03 — the live enrichers are gone, the pipeline is offline-first, and the existing dbt models still produce the same `taxon_lineage_extended` schema downstream.

Output: Two deleted functions, one rewritten dbt model, one trimmed sources.yml, two deleted test files, and a green `dbt build` + green `npm test` proving nothing downstream regressed.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/110-offline-taxonomy/110-CONTEXT.md
@.planning/phases/110-offline-taxonomy/110-RESEARCH.md
@.planning/phases/110-offline-taxonomy/110-PATTERNS.md
@.planning/phases/110-offline-taxonomy/110-01-SUMMARY.md

<interfaces>
<!-- Contract surface this plan must preserve -->

inaturalist_data.taxon_lineage_extended schema (must NOT change):
  taxon_id BIGINT PRIMARY KEY, family VARCHAR, subfamily VARCHAR,
  tribe VARCHAR, genus VARCHAR, subgenus VARCHAR
  (Phase 110 Plan 01 produces this same schema via CSV PIVOT.)

stg_waba__taxon_lineage contract consumed by int_specimen_obs_base:
  Columns required: taxon_id, genus, family
  (int_specimen_obs_base.sql JOINs on taxon_id, projects genus + family.)

run.py STEPS ordering invariant (relevant subset):
  ... -> resolve-taxon-ids -> taxa-download -> taxon-lineage-extended -> places-validation -> ...

D-01 (LOCKED): stg_waba__taxon_lineage is rewritten as a dbt view on stg_inat__taxon_lineage_extended, selecting taxon_id, genus, family. No new Python step; int_specimen_obs_base is unchanged.
D-02 (LOCKED): The inaturalist_waba_data.taxon_lineage source declaration in sources.yml must be removed.

Plan 01 produces (already exists when this plan runs):
  data/taxa_pipeline.py: download_taxa_csv(), load_taxon_lineage_extended(db_path: str | None = None)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite dbt model + remove sources.yml entry (D-01, D-02)</name>
  <files>data/dbt/models/staging/stg_waba__taxon_lineage.sql, data/dbt/models/sources.yml</files>
  <read_first>
    - data/dbt/models/staging/stg_waba__taxon_lineage.sql (current file — sources from inaturalist_waba_data.taxon_lineage)
    - data/dbt/models/staging/stg_inat__taxon_lineage_extended.sql (analog for ref() pattern)
    - data/dbt/models/sources.yml (current 18-24 block for inaturalist_waba_data)
    - data/dbt/models/intermediate/int_specimen_obs_base.sql (consumer — JOINs on taxon_id, uses genus + family)
    - .planning/phases/110-offline-taxonomy/110-PATTERNS.md (sections "stg_waba__taxon_lineage.sql" and "sources.yml" — verbatim rewritten file contents and the exact line to remove)
  </read_first>
  <action>
    Rewrite `data/dbt/models/staging/stg_waba__taxon_lineage.sql` so it:
      - Keeps `{{ config(materialized='view') }}`
      - Replaces the `source('inaturalist_waba_data','taxon_lineage')` call with `ref('stg_inat__taxon_lineage_extended')`
      - Projects exactly three columns: `taxon_id, genus, family` (per D-01)
      - Carries a comment noting Phase 110 D-01 rationale

    Edit `data/dbt/models/sources.yml` and remove the `- name: taxon_lineage` line under the `inaturalist_waba_data` source block (lines 18-23 of the current file). The `observations` and `observations__ofvs` entries stay. The `inaturalist_data.taxon_lineage_extended` source entry under `inaturalist_data` (line 16) is UNCHANGED (still a source — populated by taxa_pipeline.load_taxon_lineage_extended into the same schema).
  </action>
  <verify>
    <automated>grep -n "taxon_lineage" data/dbt/models/sources.yml; grep -cE "ref\\(\\s*['\\\"]stg_inat__taxon_lineage_extended['\\\"]" data/dbt/models/staging/stg_waba__taxon_lineage.sql</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "inaturalist_waba_data" data/dbt/models/sources.yml -A 5` does NOT show a `taxon_lineage` table entry.
    - `grep -n "taxon_lineage_extended" data/dbt/models/sources.yml` still shows the `inaturalist_data` entry (unchanged).
    - `data/dbt/models/staging/stg_waba__taxon_lineage.sql` contains the literal `ref('stg_inat__taxon_lineage_extended')` (single or double quotes acceptable).
    - That same file selects exactly `taxon_id, genus, family` (no `SELECT *`).
    - `grep -c "source('inaturalist_waba_data', *'taxon_lineage')" data/dbt/models/staging/stg_waba__taxon_lineage.sql` returns 0.
  </acceptance_criteria>
  <done>
    dbt staging model delegates to the iNat staging view per D-01; the orphan source declaration is removed per D-02.
  </done>
</task>

<task type="auto">
  <name>Task 2: Delete enrichers, rewire run.py, delete dead tests, prove green</name>
  <files>data/inaturalist_pipeline.py, data/waba_pipeline.py, data/run.py, data/tests/test_taxon_lineage_extended.py, data/tests/test_taxon_lineage.py</files>
  <read_first>
    - data/inaturalist_pipeline.py (lines 180-276: TARGET_RANKS constant, enrich_taxon_lineage_extended function body to delete)
    - data/waba_pipeline.py (lines 109-160 enrich_taxon_lineage; line 184 the call inside load_observations)
    - data/run.py (lines 28-44 import block; lines 79-98 STEPS list)
    - data/tests/test_taxon_lineage_extended.py (to confirm it imports the deleted function — pure HTTP-mock suite, no salvageable test)
    - data/tests/test_taxon_lineage.py (same situation — confirm it imports waba's enrich_taxon_lineage)
    - data/tests/conftest.py (around lines 110-114 — seeds inaturalist_waba_data.taxon_lineage in session fixture; that fixture seeding is harmless because it lives in tests only)
    - .planning/phases/110-offline-taxonomy/110-PATTERNS.md (sections inaturalist_pipeline.py / waba_pipeline.py / run.py — exact deletion targets)
    - .planning/phases/110-offline-taxonomy/110-RESEARCH.md (Common Pitfalls 4-6 — drives the call-site cleanup checklist)
  </read_first>
  <action>
    Delete from `data/inaturalist_pipeline.py`:
      - The `TARGET_RANKS = {"family", ...}` module constant immediately above the function
      - The entire `def enrich_taxon_lineage_extended(db_path: str | None = None)` function body (currently lines 184-276)
      - If `enrich_taxon_lineage_extended` is referenced anywhere else in the file (search and confirm), remove that reference too
      Keep the remainder of the module intact (imports, `_inat_get_with_retry`, `_INAT_PACE_SECONDS`, `_transform`, `DEFAULT_FIELDS`, `inaturalist_source`, `load_observations`, the `if __name__ == "__main__"` block).

    Delete from `data/waba_pipeline.py`:
      - The entire `def enrich_taxon_lineage(db_path: str)` function body (currently lines 109-160)
      - The call site `enrich_taxon_lineage(DB_PATH)` at the end of `load_observations` (currently line 184). After deletion, `load_observations` ends with `load_info.raise_on_failed_jobs()`.
      - If the import of `requests` or `duckdb` becomes unused after the deletion, leave them — they are likely still used by the dlt source path; only remove imports if linting flags them as unused.

    Edit `data/run.py`:
      - Remove the line `from inaturalist_pipeline import enrich_taxon_lineage_extended` (currently line 30).
      - Add a new import line in the same import block: `from taxa_pipeline import download_taxa_csv, load_taxon_lineage_extended`.
      - In the STEPS list, replace the single tuple `("taxon-lineage-extended", enrich_taxon_lineage_extended),` with TWO tuples in this order:
          ("taxa-download", download_taxa_csv),
          ("taxon-lineage-extended", load_taxon_lineage_extended),
        These two entries replace the single line at the current position (after `resolve-taxon-ids` and before `places-validation`). The relative order of other STEPS entries is unchanged.
      - Update the module docstring's pipeline-order comment to insert `taxa-download` immediately before `taxon-lineage-extended` in the arrow list.

    Delete files (use `rm` or `git rm`):
      - `data/tests/test_taxon_lineage_extended.py`
      - `data/tests/test_taxon_lineage.py`

    Run the verification commands and ensure they ALL pass before committing:
      1. `cd data && uv run pytest` (full data suite, including the new test_taxa_pipeline.py, must be green)
      2. `bash data/dbt/run.sh build` (must exit 0 — confirms the rewritten stg_waba__taxon_lineage compiles and downstream models still build)
      3. `npm test` (from repo root — confirms frontend tests still pass; should be unaffected but is the TAX-03 acceptance gate)

    Note: `bash data/dbt/run.sh build` requires `inaturalist_data.taxon_lineage_extended` to exist in the local DuckDB. Before running the dbt build, run the new taxa pipeline once locally to populate it: `cd data && DB_PATH=data/beeatlas.duckdb uv run python -c "from taxa_pipeline import download_taxa_csv, load_taxon_lineage_extended; download_taxa_csv(); load_taxon_lineage_extended()"`. If this step is too heavyweight for the executor's sandbox (37MB download), document the skip in the SUMMARY but DO NOT skip `npm test` and DO NOT skip the pytest suite — only the dbt smoke can be deferred to nightly with that explicit note.
  </action>
  <verify>
    <automated>cd data &amp;&amp; uv run pytest 2>&amp;1 | tail -10 &amp;&amp; cd .. &amp;&amp; npm test 2>&amp;1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "def enrich_taxon_lineage_extended" data/inaturalist_pipeline.py` returns 0.
    - `grep -c "def enrich_taxon_lineage" data/waba_pipeline.py` returns 0.
    - `grep -rn "enrich_taxon_lineage" data/ --include='*.py' | grep -v '^Binary'` returns 0 matches (no stragglers in tests or other modules).
    - `grep -c "from inaturalist_pipeline import enrich_taxon_lineage_extended" data/run.py` returns 0.
    - `grep -c "from taxa_pipeline import" data/run.py` returns at least 1, and the import line includes both `download_taxa_csv` and `load_taxon_lineage_extended`.
    - `grep -n "taxa-download\|taxon-lineage-extended" data/run.py` shows the two STEPS tuples in adjacent lines, in that order.
    - `data/tests/test_taxon_lineage_extended.py` does not exist.
    - `data/tests/test_taxon_lineage.py` does not exist.
    - `cd data && uv run pytest` exits 0.
    - `npm test` exits 0.
    - `bash data/dbt/run.sh build` exits 0 (or, if deferred per the action note, SUMMARY records the explicit deferral and the nightly run will exercise it).
  </acceptance_criteria>
  <done>
    Both live enrichers gone, run.py rewired, dead tests deleted; pytest, npm test, and (locally or via documented nightly fallback) dbt build all green. TAX-03 satisfied.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| pipeline runtime → DuckDB local file | All transforms in-process; no cross-process trust boundary introduced or removed |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-110-05 | Repudiation | enricher deletion erases historical data lineage | accept | Git history preserves the deleted code; CHANGELOG via SUMMARY documents the cutover; no audit-trail requirement on the codebase itself |
| T-110-06 | Tampering | dbt source rewrite (D-01) silently changes column contract | mitigate | int_specimen_obs_base JOIN columns enumerated in <interfaces>; dbt build serves as the contract test; npm test smoke confirms frontend still consumes the produced parquet |
| T-110-07 | DoS | dbt build fails because inaturalist_waba_data.taxon_lineage no longer exists | mitigate | Task 1 (sources.yml + stg_waba__taxon_lineage rewrite) lands BEFORE Task 2 verification runs dbt build, so the build sees the new ref() chain, not the removed source |
</threat_model>

<verification>
- `grep -rn "enrich_taxon_lineage" data/ --include='*.py'` returns 0 matches (full cutover)
- `grep -rn "inaturalist_waba_data.*taxon_lineage" data/dbt/` returns 0 matches outside of comments
- `cd data && uv run pytest` → exits 0
- `bash data/dbt/run.sh build` → exits 0 (or deferred per the in-task note, with explicit SUMMARY callout)
- `npm test` → exits 0
- Manual diff review: `git diff data/run.py` shows ONE removed import, ONE added import, and ONE removed STEPS line replaced by TWO added STEPS lines
</verification>

<success_criteria>
- Both enricher functions and the waba call site are deleted
- run.py imports + STEPS reflect the new two-step taxa-download / taxon-lineage-extended flow
- D-01 and D-02 are implemented exactly as written in CONTEXT.md
- Dead HTTP-mock test files are removed
- Full pytest, dbt build, and npm test gates are green (or dbt build has explicit nightly-deferral note in SUMMARY)
</success_criteria>

<output>
Create `.planning/phases/110-offline-taxonomy/110-02-SUMMARY.md` when done — record:
- Exact diff stats per modified file (lines removed, lines added)
- Confirmation that `grep -rn enrich_taxon_lineage data/` is empty
- pytest, dbt build, npm test exit codes and runtimes
- Any deferred verification (with rationale) so verify-work knows what nightly must re-check
</output>
