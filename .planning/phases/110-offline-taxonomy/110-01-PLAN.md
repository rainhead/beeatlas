---
phase: 110
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - data/taxa_pipeline.py
  - data/tests/test_taxa_pipeline.py
  - data/.gitignore
autonomous: true
requirements: [TAX-01, TAX-02]
must_haves:
  truths:
    - "Running `data/taxa_pipeline.py download_taxa_csv()` populates data/raw/taxa.csv.gz and data/raw/taxa_cache.json on first run"
    - "Running download_taxa_csv() a second time sends If-None-Match + If-Modified-Since headers; on HTTP 304 it does NOT rewrite the archive"
    - "Running load_taxon_lineage_extended() on a local taxa.csv.gz produces inaturalist_data.taxon_lineage_extended with column order (taxon_id, family, subfamily, tribe, genus, subgenus)"
    - "Taxa absent from a given rank emit NULL in that column (not a sentinel string)"
    - "Pytest suite cd data && uv run pytest tests/test_taxa_pipeline.py -x passes end-to-end"
  artifacts:
    - path: "data/taxa_pipeline.py"
      provides: "download_taxa_csv() + load_taxon_lineage_extended() + module-level constants"
      contains: "def download_taxa_csv"
    - path: "data/taxa_pipeline.py"
      provides: "load_taxon_lineage_extended definition"
      contains: "def load_taxon_lineage_extended"
    - path: "data/tests/test_taxa_pipeline.py"
      provides: "Wave 0 RED tests for caching + ancestry walk"
      contains: "def test_download_uses_304"
    - path: "data/.gitignore"
      provides: "ignore rules for raw/taxa.csv.gz and raw/taxa_cache.json"
      contains: "raw/taxa.csv.gz"
  key_links:
    - from: "data/taxa_pipeline.py download_taxa_csv"
      to: "https://inaturalist-open-data.s3.amazonaws.com/taxa.csv.gz"
      via: "requests.get with If-None-Match + If-Modified-Since headers"
      pattern: "If-None-Match"
    - from: "data/taxa_pipeline.py load_taxon_lineage_extended"
      to: "inaturalist_data.taxon_lineage_extended"
      via: "DuckDB CREATE OR REPLACE TABLE from read_csv with PIVOT"
      pattern: "CREATE OR REPLACE TABLE inaturalist_data\\.taxon_lineage_extended"
---

<objective>
Create `data/taxa_pipeline.py` — a new pipeline module that downloads `taxa.csv.gz` from iNat AWS Open Data with ETag/Last-Modified caching, and populates `inaturalist_data.taxon_lineage_extended` via a DuckDB ancestry walk on the gzipped archive. Land the Wave 0 RED tests in `data/tests/test_taxa_pipeline.py` BEFORE the implementation per Nyquist protocol, then make them GREEN.

Purpose: Replace the live `/v2/taxa` API enricher with an offline ancestry walk so the pipeline carries no rate-limit risk and Phase 111 (Checklist) can look up lineage for any active Anthophila taxon, not just observed ones.

Output: A working `taxa_pipeline.py` (downloader + DuckDB loader), a passing pytest module that exercises ETag caching and ancestry-walk schema, and `.gitignore` entries so the raw cache files do not enter git.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/110-offline-taxonomy/110-CONTEXT.md
@.planning/phases/110-offline-taxonomy/110-RESEARCH.md
@.planning/phases/110-offline-taxonomy/110-PATTERNS.md
@.planning/phases/110-offline-taxonomy/110-VALIDATION.md
@data/checklist_pipeline.py
@data/tests/test_checklist_pipeline.py

<interfaces>
<!-- Established pipeline-module idioms; the executor must follow these patterns exactly. -->

From data/checklist_pipeline.py (module-level constants pattern, lines 22-26):
  DB_PATH = os.environ.get("DB_PATH", str(Path(__file__).parent / "beeatlas.duckdb"))
  CHECKLIST_PATH = Path(__file__).parent / "checklists" / "wa_bee_checklist.tsv"

From data/inaturalist_pipeline.py (current enricher being replaced):
  def enrich_taxon_lineage_extended(db_path: str | None = None) -> None
  -- writes inaturalist_data.taxon_lineage_extended with columns:
     (taxon_id BIGINT PRIMARY KEY, family VARCHAR, subfamily VARCHAR,
      tribe VARCHAR, genus VARCHAR, subgenus VARCHAR)

Target taxa.csv.gz schema (TSV, gzip compressed, verified 2026-05-23):
  taxon_id BIGINT, ancestry VARCHAR, rank_level INTEGER, rank VARCHAR,
  name VARCHAR, active VARCHAR   -- 'true' / 'false' as string, NOT bool

Anthophila taxon_id: 630955

DuckDB read_csv invocation (verified DuckDB 1.5.2):
  read_csv(?, delim='\t', header=true, compression='gzip',
           columns={'taxon_id':'BIGINT','ancestry':'VARCHAR',
                    'rank_level':'INTEGER','rank':'VARCHAR',
                    'name':'VARCHAR','active':'VARCHAR'})
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wave 0 — Write failing tests for download caching and ancestry walk</name>
  <files>data/tests/test_taxa_pipeline.py</files>
  <read_first>
    - data/tests/test_checklist_pipeline.py (for fixture isolation pattern: tmp_path, monkeypatch.setenv DB_PATH, importlib.reload)
    - data/tests/test_taxon_lineage_extended.py (the soon-to-be-deleted suite — borrow nothing, but understand the HTTP-mock idiom not to repeat)
    - .planning/phases/110-offline-taxonomy/110-PATTERNS.md (sections "data/tests/test_taxa_pipeline.py" — verbatim fixture and test skeletons)
    - .planning/phases/110-offline-taxonomy/110-RESEARCH.md (Pattern 1 + Pattern 2 — defines the exact functions tests must assert against)
  </read_first>
  <behavior>
    - test_download_uses_304: file + sidecar exist with ETag "abc" + Last-Modified "Mon, 27 Apr 2026 12:48:16 GMT". download_taxa_csv() sends If-None-Match: abc and If-Modified-Since: that string; on a mocked 304 response the cached bytes (b"fake") are not overwritten.
    - test_download_writes_sidecar: file + sidecar absent. download_taxa_csv() with a mocked 200 response (ETag "xyz", Last-Modified "Tue, 28 Apr 2026 00:00:00 GMT", iter_content streams b"payload") writes the bytes to TAXA_PATH and writes a JSON sidecar at TAXA_CACHE_PATH containing keys "etag"="xyz" and "last_modified"="Tue, 28 Apr 2026 00:00:00 GMT".
    - test_lineage_schema: given a mini gzipped TSV fixture under Anthophila (taxon 630955, an Apidae family row, a Bombus genus row, a Bombus melanopygus species row), load_taxon_lineage_extended() creates inaturalist_data.taxon_lineage_extended whose information_schema.columns ordered by ordinal_position equals ['taxon_id','family','subfamily','tribe','genus','subgenus'].
    - test_lineage_null_ranks: with the same fixture, the Apidae family row (taxon 52775) materializes as (52775, 'Apidae', NULL, NULL, NULL, NULL). The species row (taxon 52776 'Bombus melanopygus') materializes as (52776, 'Apidae', NULL, NULL, 'Bombus', NULL) — subfamily/tribe/subgenus absent → NULL, not empty string.
    - test_lineage_includes_self: the Bombus genus row (taxon 84734) appears with genus='Bombus' (the ancestry column does not list self, so the implementation must UNION ALL the self_rows arm).
  </behavior>
  <action>
    Create `data/tests/test_taxa_pipeline.py` containing five RED tests covering the behaviors above. Use the fixture skeleton in 110-PATTERNS.md verbatim:
      - module-level MINI_TAXA_TSV string (5+ rows; rows for Anthophila itself 630955 superfamily, Apidae 52775 family, Bombus 84734 genus, Bombus melanopygus 52776 species, plus one non-Anthophila active row that must be filtered out)
      - mini_taxa_gz fixture using gzip.open + bytes write to tmp_path
      - taxa_db fixture using tmp_path, monkeypatch.setenv("DB_PATH", ...), importlib.reload(taxa_pipeline), monkeypatch.setattr for TAXA_PATH and TAXA_CACHE_PATH
    For the HTTP tests, patch `taxa_pipeline.requests.get` with `unittest.mock.patch`. The mock_resp for the 200 case must implement `iter_content(chunk_size=...)` returning an iterable of byte chunks (use a MagicMock with side_effect or a small helper).
    Tests must call `taxa_pipeline.download_taxa_csv()` and `taxa_pipeline.load_taxon_lineage_extended()` — both functions are expected to exist with those names. Initially the module does not exist, so the tests RED with ImportError; commit them anyway with `test(110-01): add failing tests for taxa_pipeline (Wave 0)`.
    Do not patch `time.sleep` or add pacing logic — taxa_pipeline has no rate limiter.
    For the lineage tests, the fixture must pre-create the `inaturalist_data` schema in the tmp DuckDB before calling load_taxon_lineage_extended (mirror the conftest pattern of CREATE SCHEMA IF NOT EXISTS).
  </action>
  <verify>
    <automated>cd data &amp;&amp; uv run pytest tests/test_taxa_pipeline.py -x 2>&amp;1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - File `data/tests/test_taxa_pipeline.py` exists.
    - File defines pytest functions: test_download_uses_304, test_download_writes_sidecar, test_lineage_schema, test_lineage_null_ranks, test_lineage_includes_self.
    - File contains a fixture creating a mini gzipped TSV with at least one row containing taxon_id=630955 (Anthophila) and ranks family / genus / species inside that ancestry.
    - `cd data && uv run pytest tests/test_taxa_pipeline.py -x` exits non-zero with ImportError (module `taxa_pipeline` missing) — this is the RED state.
    - Test file uses `unittest.mock.patch("taxa_pipeline.requests.get", ...)` for HTTP tests, NOT live network.
  </acceptance_criteria>
  <done>
    Five pytest functions land RED against a not-yet-existing taxa_pipeline module; commit lands with message `test(110-01): add failing tests for taxa_pipeline (Wave 0)`.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create taxa_pipeline.py with downloader and DuckDB ancestry-walk loader (GREEN)</name>
  <files>data/taxa_pipeline.py, data/.gitignore</files>
  <read_first>
    - data/checklist_pipeline.py (DuckDB connect/try/finally frame and module-level DB_PATH idiom)
    - data/geographies_pipeline.py (HTTP streaming + atomic tmp-then-rename pattern)
    - data/inaturalist_pipeline.py (existing enrich_taxon_lineage_extended for table contract — same column order and types must be reproduced)
    - data/tests/test_taxa_pipeline.py (the RED tests from Task 1 — implementation must satisfy each one)
    - .planning/phases/110-offline-taxonomy/110-RESEARCH.md (Pattern 1, Pattern 2 — verified SQL and Python skeletons)
    - .planning/phases/110-offline-taxonomy/110-PATTERNS.md ("data/taxa_pipeline.py" section — verbatim module skeleton and PIVOT SQL)
    - data/.gitignore (current ignore rules)
  </read_first>
  <action>
    Create `data/taxa_pipeline.py` with the following surface:

      Module-level constants:
        DB_PATH (env-overridable, defaults to data/beeatlas.duckdb — copy idiom from checklist_pipeline.py)
        RAW_DIR = Path(__file__).parent / "raw"
        TAXA_URL = "https://inaturalist-open-data.s3.amazonaws.com/taxa.csv.gz"
        TAXA_PATH = RAW_DIR / "taxa.csv.gz"
        TAXA_CACHE_PATH = RAW_DIR / "taxa_cache.json"
        ANTHOPHILA_ID = 630955

      def download_taxa_csv() -> None:
        - mkdir RAW_DIR with parents=True, exist_ok=True
        - If both TAXA_PATH and TAXA_CACHE_PATH exist: read sidecar JSON; set If-None-Match from cache["etag"] (if present) and If-Modified-Since from cache["last_modified"] (if present)
        - requests.get(TAXA_URL, headers=headers, stream=True, timeout=60)
        - if status_code == 304: print unchanged message and return (do NOT touch files)
        - else resp.raise_for_status()
        - atomic write: stream resp.iter_content(chunk_size=1024*1024) to TAXA_PATH.with_suffix(".gz.tmp"), then .rename(TAXA_PATH)
        - write sidecar JSON {"etag": resp.headers.get("ETag"), "last_modified": resp.headers.get("Last-Modified")}
        - print downloaded size MB

      def load_taxon_lineage_extended(db_path: str | None = None) -> None:
        - if db_path is None: db_path = DB_PATH
        - duckdb.connect(db_path); try / finally with con.close()
        - con.execute("CREATE SCHEMA IF NOT EXISTS inaturalist_data")
        - CREATE OR REPLACE TABLE inaturalist_data.taxon_lineage_extended AS via the verified PIVOT SQL in RESEARCH.md Pattern 2.
        - The CTE must filter active='true' (string) AND (ancestry LIKE '%/630955/%' OR ancestry LIKE '%/630955' OR taxon_id = 630955).
        - The PIVOT GROUP BY column is target_taxon_id; the final CREATE OR REPLACE TABLE statement must SELECT target_taxon_id AS taxon_id, family, subfamily, tribe, genus, subgenus FROM pivoted (column order: taxon_id, family, subfamily, tribe, genus, subgenus — exactly matching the schema in inaturalist_pipeline.py line 208-216).
        - UNION ALL self_rows arm is mandatory (Anthophila/Apidae/Bombus rows do not list themselves in their own ancestry).
        - Anti-patterns to avoid (from RESEARCH.md): do NOT use active = true (bool); do NOT use ancestry LIKE '%630955%' (false-matches 1630955); do NOT omit the self_rows UNION arm.
        - print(f"taxon_lineage_extended: {count} rows")

      Append to `data/.gitignore`:
        raw/taxa.csv.gz
        raw/taxa.csv.gz.tmp
        raw/taxa_cache.json

      All five tests in test_taxa_pipeline.py must now pass.
  </action>
  <verify>
    <automated>cd data &amp;&amp; uv run pytest tests/test_taxa_pipeline.py -x 2>&amp;1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - File `data/taxa_pipeline.py` exists and defines: DB_PATH, RAW_DIR, TAXA_URL, TAXA_PATH, TAXA_CACHE_PATH, ANTHOPHILA_ID, download_taxa_csv, load_taxon_lineage_extended.
    - `grep -c "active = 'true'" data/taxa_pipeline.py` returns at least 1 (string comparison, not boolean).
    - `grep -c "UNION ALL" data/taxa_pipeline.py` returns at least 1 (self_rows arm).
    - `grep -c "If-None-Match" data/taxa_pipeline.py` returns at least 1.
    - `grep -c "target_taxon_id AS taxon_id" data/taxa_pipeline.py` returns at least 1 (PIVOT key rename).
    - `cd data && uv run pytest tests/test_taxa_pipeline.py -x` exits 0 with five tests passing.
    - `data/.gitignore` contains the literal substrings `raw/taxa.csv.gz` and `raw/taxa_cache.json`.
    - No live HTTP request is made during pytest (tests use unittest.mock.patch on requests.get).
  </acceptance_criteria>
  <done>
    Module exists, all five Wave 0 tests pass, gitignore protects the raw cache. Commit lands with message `feat(110-01): implement taxa_pipeline downloader + ancestry walk`.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| pipeline → inaturalist-open-data.s3.amazonaws.com | Outbound HTTPS GET to a public AWS S3 bucket; iNat-published dataset; trusted source |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-110-01 | Tampering | taxa.csv.gz download | accept | HTTPS confirmed via URL scheme; AWS S3 origin; no PII; corrupt download would fail DuckDB CSV parsing at read time (FAIL CLOSED) |
| T-110-02 | Tampering | DuckDB read_csv input | mitigate | read_csv called with explicit columns={...} type spec — type coercion prevents injection via malformed cells; bound parameter `[str(TAXA_PATH)]` (not f-string) |
| T-110-03 | DoS | 37MB download on every nightly run | mitigate | ETag/Last-Modified conditional GET; on 304 the function returns without writing. Pattern 1 (Plan 03 ensures sidecar round-trips via S3 so 304 fires) |
| T-110-04 | Information Disclosure | taxa.csv.gz cache contents | accept | Public dataset; gitignored locally; no exposure risk |
</threat_model>

<verification>
- `cd data && uv run pytest tests/test_taxa_pipeline.py -x` — all five tests pass
- `cd data && uv run pytest` — entire data suite still green (the new module does not break neighbours; old taxon_lineage tests still pass because we have not yet deleted the enrichers — that is Plan 02)
- `grep -n "def download_taxa_csv\|def load_taxon_lineage_extended" data/taxa_pipeline.py` returns both function definitions
- Manually verify against a smoke fixture: `cd data && uv run python -c "import taxa_pipeline; print(taxa_pipeline.TAXA_URL)"` prints the iNat S3 URL
</verification>

<success_criteria>
- `data/taxa_pipeline.py` exists with both required functions and all module constants
- Five pytest tests for caching + ancestry walk all pass
- `.gitignore` protects raw cache artifacts
- Module has no live-API dependency (download is mockable; ancestry walk runs purely from a local file)
</success_criteria>

<output>
Create `.planning/phases/110-offline-taxonomy/110-01-SUMMARY.md` when done — record:
- Functions added (signatures + line counts)
- Final test count + names
- Any deviations from the RESEARCH.md PIVOT SQL (e.g., column rename pattern chosen)
- Any iNat HTTP header quirks encountered if a live HEAD was run
</output>
