---
phase: 110
plan: 03
type: execute
wave: 2
depends_on: [110-01]
files_modified:
  - data/nightly.sh
autonomous: true
requirements: [TAX-04]
must_haves:
  truths:
    - "Nightly runs pull `taxa.csv.gz` from S3 to data/raw/taxa.csv.gz before the pipeline starts (if it exists in S3)"
    - "Nightly runs pull `taxa_cache.json` (the ETag sidecar) alongside the archive so the next run's conditional GET fires"
    - "On exit (success or failure), nightly.sh pushes both files back to S3 so they persist across nightly invocations"
    - "First-ever run with no S3 cache prints a clear 'first run' message and lets the pipeline download fresh from iNat"
  artifacts:
    - path: "data/nightly.sh"
      provides: "S3 pull + EXIT-trap push for taxa.csv.gz and taxa_cache.json"
      contains: "raw/taxa.csv.gz"
  key_links:
    - from: "data/nightly.sh pull block"
      to: "s3://$BUCKET/raw/taxa.csv.gz"
      via: "aws s3 cp"
      pattern: "raw/taxa\\.csv\\.gz"
    - from: "data/nightly.sh EXIT trap"
      to: "s3://$BUCKET/raw/taxa_cache.json"
      via: "aws s3 cp"
      pattern: "raw/taxa_cache\\.json"
---

<objective>
Extend `data/nightly.sh` so the `taxa.csv.gz` archive and its `taxa_cache.json` ETag sidecar round-trip through S3 alongside the existing DuckDB. Without this, every nightly run would re-download 37MB from iNat Open Data because the sidecar (and therefore the conditional GET headers) would be lost between runs.

Purpose: Realize TAX-04 — the taxa archive persists across nightly invocations. Implements the second half of the TAX-01 caching contract (download_taxa_csv emits the sidecar; nightly.sh ensures the sidecar is the same one across runs).

Output: A `data/nightly.sh` with three new variable declarations, one new pull block, and a widened EXIT trap that also pushes the two taxa files.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/110-offline-taxonomy/110-CONTEXT.md
@.planning/phases/110-offline-taxonomy/110-RESEARCH.md
@.planning/phases/110-offline-taxonomy/110-PATTERNS.md
@.planning/phases/110-offline-taxonomy/110-01-SUMMARY.md

<interfaces>
<!-- nightly.sh contract surface this plan extends -->

Existing variable declarations (lines 32-40):
  SCRIPT_DIR="$(cd "$(dirname "$0")" && pwd)"   # resolves to .../beeatlas/data
  BUCKET="${BUCKET:-beeatlasstack-sitebucket397a1860-h5dtjzkld3yv}"
  DB_S3_KEY="db/beeatlas.duckdb"
  DB_PATH="/tmp/beeatlas.duckdb"
  AWS_PROFILE="${AWS_PROFILE:-beeatlas}"

Important path constraint: SCRIPT_DIR is the data/ directory, so $SCRIPT_DIR/raw/ === data/raw/ in the repo. taxa_pipeline.py also resolves TAXA_PATH = Path(__file__).parent / "raw" / "taxa.csv.gz" — which is the SAME path: data/raw/taxa.csv.gz. The two paths must agree.

Existing EXIT trap (line 81):
  trap 'if [[ -f "$DB_PATH" ]]; then echo "--- backing up DuckDB (trap) --- sha256=$(_hash "$DB_PATH")"; aws --profile "$AWS_PROFILE" s3 cp --no-progress "$DB_PATH" "s3://$BUCKET/$DB_S3_KEY" || true; fi' EXIT

Existing DuckDB pull (lines 84-90) — model for the new taxa pull block.

S3 key convention (TAX-04): raw/taxa.csv.gz and raw/taxa_cache.json under the same bucket as the DuckDB.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add taxa.csv.gz + sidecar S3 pull/push to nightly.sh</name>
  <files>data/nightly.sh</files>
  <read_first>
    - data/nightly.sh (current file in full; especially lines 32-40 variable block, line 81 EXIT trap, lines 83-90 DuckDB pull, lines 92-99 pipeline-run block)
    - data/taxa_pipeline.py (TAXA_PATH and TAXA_CACHE_PATH constants — confirm they resolve to data/raw/taxa.csv.gz and data/raw/taxa_cache.json so nightly.sh keys match)
    - .planning/phases/110-offline-taxonomy/110-PATTERNS.md (section "data/nightly.sh" — verbatim variable declarations, pull block, EXIT trap rewrite)
    - .planning/phases/110-offline-taxonomy/110-RESEARCH.md (Pattern 4 and Pitfall 2 — explains why the sidecar must also be synced)
  </read_first>
  <action>
    Edit `data/nightly.sh`:

    A. In the variable declaration block (currently lines 37-40, after `AWS_PROFILE`), add four new declarations:
       TAXA_S3_KEY="raw/taxa.csv.gz"
       TAXA_CACHE_S3_KEY="raw/taxa_cache.json"
       TAXA_PATH="$SCRIPT_DIR/raw/taxa.csv.gz"
       TAXA_CACHE_PATH="$SCRIPT_DIR/raw/taxa_cache.json"

       SCRIPT_DIR resolves to the data/ directory, so these paths align with taxa_pipeline.py's TAXA_PATH (Path(__file__).parent / "raw" / "taxa.csv.gz"). Confirm by reading the constants in data/taxa_pipeline.py before writing the bash lines — both must end up pointing at data/raw/taxa.csv.gz.

    B. Replace the existing single-condition EXIT trap (currently line 81) with a multi-line trap that backs up all three files. The DuckDB block stays first (it is the most critical), then the taxa archive, then the sidecar. Use a single-quoted multi-line `trap '...' EXIT` so $-expansion happens at trap-fire time. Each `aws s3 cp` line keeps the `|| true` suffix so a single failure does not abort the trap (and other files still get pushed).

    C. After the existing DuckDB pull block (currently lines 84-90, ending with the closing `fi`) and before the `# 2. Run pipelines` section (currently line 92), insert a new pull block:
       - Echo a `--- pulling taxa.csv.gz from S3 ---` header line
       - `mkdir -p "$SCRIPT_DIR/raw"` so the destination directory exists on first run
       - aws s3 cp for `s3://$BUCKET/$TAXA_S3_KEY` to `$TAXA_PATH`. If it fails (no cached copy yet), echo a first-run message and continue (do not `set -e` abort — use the same `if ! aws ... 2>/dev/null; then ... fi` idiom as the DuckDB pull). The pipeline will download from iNat instead — that branch is already implemented in Plan 01's download_taxa_csv.
       - aws s3 cp for `s3://$BUCKET/$TAXA_CACHE_S3_KEY` to `$TAXA_CACHE_PATH`. This one ALWAYS uses `|| true` (no `if ! ... ; then` block) — a missing sidecar simply means the next download will be unconditional, which is correct behavior on first run.

    D. Do NOT alter the section that copies `EXPORT_DIR` artifacts to S3 (manifest/feeds/species-maps/etc.) — that surface is unrelated to the taxa cache. Do NOT add taxa files to the CloudFront invalidation list.

    Sanity check after edit: `bash -n data/nightly.sh` must succeed (syntax check). The DuckDB pull/push behavior must be unchanged for nightly cron compatibility.
  </action>
  <verify>
    <automated>bash -n data/nightly.sh &amp;&amp; grep -c "raw/taxa.csv.gz" data/nightly.sh &amp;&amp; grep -c "raw/taxa_cache.json" data/nightly.sh &amp;&amp; grep -c "TAXA_S3_KEY" data/nightly.sh</automated>
  </verify>
  <acceptance_criteria>
    - `bash -n data/nightly.sh` exits 0 (no syntax errors).
    - `grep -c "TAXA_S3_KEY=" data/nightly.sh` returns exactly 1 (variable defined once).
    - `grep -c "TAXA_CACHE_S3_KEY=" data/nightly.sh` returns exactly 1.
    - `grep -c "TAXA_PATH=" data/nightly.sh` returns exactly 1 (must be `$SCRIPT_DIR/raw/taxa.csv.gz`, NOT `/tmp/...`).
    - `grep -c "TAXA_CACHE_PATH=" data/nightly.sh` returns exactly 1.
    - `grep -cE "aws .* s3 cp .*\\\$TAXA_S3_KEY" data/nightly.sh` returns 2 (one pull, one push in EXIT trap).
    - `grep -cE "aws .* s3 cp .*\\\$TAXA_CACHE_S3_KEY" data/nightly.sh` returns 2 (one pull, one push in EXIT trap).
    - `grep -c "pulling taxa.csv.gz from S3" data/nightly.sh` returns 1 (the echo header on the pull block).
    - `grep -c "DB_S3_KEY" data/nightly.sh` is UNCHANGED from baseline (3 occurrences: 1 declaration + 1 pull + 1 trap push) — confirms DuckDB sync was not accidentally altered.
    - The path `TAXA_PATH` resolves to the same on-disk file as taxa_pipeline.py's TAXA_PATH constant: data/raw/taxa.csv.gz. (Manually verify by reading both files; both must end in `data/raw/taxa.csv.gz` after expansion of $SCRIPT_DIR.)
  </acceptance_criteria>
  <done>
    nightly.sh pulls the cached taxa archive + sidecar from S3 before the pipeline runs, and the EXIT trap pushes both files back. The DuckDB sync surface is unchanged. `bash -n` confirms shell-syntax validity. TAX-04 satisfied.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| nightly.sh ↔ s3://$BUCKET | AWS-authenticated S3 operations via the `beeatlas` profile; same bucket already trusted for DuckDB snapshots |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-110-08 | Tampering | taxa.csv.gz cached in S3 | accept | Same trust model as DB_S3_KEY (private bucket, IAM-scoped role). Even if tampered, DuckDB read_csv with typed columns would fail loudly on schema corruption — fail-closed |
| T-110-09 | DoS | EXIT trap failure aborts subsequent backups | mitigate | Each `aws s3 cp` line carries `|| true` so a failed taxa push does not block DuckDB push; ordering puts DuckDB first because it is the most critical artifact |
| T-110-10 | DoS | Missing sidecar on next run → re-downloads 37MB | mitigate | Sidecar is synced alongside archive (key requirement of TAX-04); first-run absence is handled gracefully (download_taxa_csv falls through to unconditional GET) |
| T-110-11 | Information Disclosure | taxa cache visible in S3 bucket | accept | Public iNat dataset; no confidentiality requirement |
</threat_model>

<verification>
- `bash -n data/nightly.sh` (shell syntax check) exits 0
- Manual diff review: `git diff data/nightly.sh` shows 4 new variable declarations, 1 new pull block (with first-run fallback), and an extended EXIT trap that pushes 3 files instead of 1
- Manual: confirm the resolved $TAXA_PATH (= $SCRIPT_DIR/raw/taxa.csv.gz) matches the path constant in data/taxa_pipeline.py
- Cannot fully exercise without S3 credentials in the executor sandbox — full verification fires on the next nightly cron run on maderas (logged via HEALTHCHECK_URL ping)
</verification>

<success_criteria>
- 4 new shell variables (TAXA_S3_KEY, TAXA_CACHE_S3_KEY, TAXA_PATH, TAXA_CACHE_PATH) are declared
- A pull block exists between the DuckDB pull and the pipeline run, with a graceful first-run fallback for the archive and an unconditional `|| true` for the sidecar
- The EXIT trap is widened to also push both taxa files; DuckDB push behavior is unchanged
- `bash -n` confirms script syntax
- Plan execution does not alter any other section of nightly.sh (CloudFront invalidation, manifest upload, healthcheck ping, npm/uv sync steps all unchanged)
</success_criteria>

<output>
Create `.planning/phases/110-offline-taxonomy/110-03-SUMMARY.md` when done — record:
- Exact line-count delta on nightly.sh
- The literal expanded path that $TAXA_PATH resolves to on a real run
- Notes on any nightly-only verification that cannot be exercised in the executor sandbox (e.g., real S3 round-trip) — verify-work should know the next nightly cron is the live test
</output>
