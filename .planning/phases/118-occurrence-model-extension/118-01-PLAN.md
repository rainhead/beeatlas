---
phase: 118-occurrence-model-extension
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - data/dbt/models/sources.yml
  - data/tests/test_dbt_scaffold.py
  - data/tests/test_species_export.py
autonomous: true
requirements: [OCC-01, OCC-02, OCC-03]

must_haves:
  truths:
    - "Pytest collects four new RED tests that fail (or skip pending build) before Plan 02 begins"
    - "dbt source('inat_obs_data', 'observations') is resolvable in subsequent dbt models"
  artifacts:
    - path: "data/dbt/models/sources.yml"
      provides: "inat_obs_data source declaration with observations table"
      contains: "name: inat_obs_data"
    - path: "data/tests/test_dbt_scaffold.py"
      provides: "Three new test functions: test_occurrences_source_column, test_inat_obs_rows_in_occurrences, test_source_no_nulls"
      contains: "test_occurrences_source_column"
    - path: "data/tests/test_species_export.py"
      provides: "test_inat_obs_count_in_species function"
      contains: "test_inat_obs_count_in_species"
  key_links:
    - from: "data/dbt/models/sources.yml"
      to: "DuckDB inat_obs_data.observations table (created by data/inat_obs_pipeline.py)"
      via: "schema: inat_obs_data + table: observations"
      pattern: "name: inat_obs_data"
    - from: "data/tests/test_dbt_scaffold.py"
      to: "data/dbt/target/sandbox/occurrences.parquet"
      via: "_OCCURRENCES_GUARD pytest.mark.skipif + duckdb.execute(read_parquet(...))"
      pattern: "read_parquet\\('.*occurrences.parquet'\\)"
---

<objective>
Establish the Nyquist RED gate for Phase 118 by adding four pytest assertions
(three for OCC-01 in `test_dbt_scaffold.py`, one for OCC-02/03 in
`test_species_export.py`) and declaring the `inat_obs_data` dbt source that
the downstream models in Plan 02 and Plan 03 will reference.

Purpose: Tests fail (RED) before implementation; source declaration unblocks
ARM 3 and `inat_obs_count_agg` in later plans without requiring re-edits to
sources.yml then. Both edits are mechanical, follow established patterns in
the same files, and have no dependency on each other.

Output: Updated sources.yml + four new test functions, all committed with
the source guard pattern that skips until `bash data/dbt/run.sh build` runs.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/118-occurrence-model-extension/118-RESEARCH.md
@.planning/phases/118-occurrence-model-extension/118-PATTERNS.md
@data/dbt/models/sources.yml
@data/tests/test_dbt_scaffold.py
@data/tests/test_species_export.py
@data/inat_obs_pipeline.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Declare inat_obs_data dbt source</name>
  <files>data/dbt/models/sources.yml</files>
  <read_first>
    - data/dbt/models/sources.yml (lines 1-37 — existing source block structure: ecdysis_data, inaturalist_data, inaturalist_waba_data, checklist_data, geographies)
    - data/inat_obs_pipeline.py (lines 50-95 — confirms DuckDB schema 'inat_obs_data' and table 'observations' with 12 columns)
    - .planning/phases/118-occurrence-model-extension/118-PATTERNS.md (sources.yml self-extension block)
  </read_first>
  <action>
    Append a new source block to data/dbt/models/sources.yml after the existing geographies block (current EOF, line 37). Block name: `inat_obs_data`. Schema: `inat_obs_data` (matches the DuckDB schema created by data/inat_obs_pipeline.py:_load_inat_obs at `CREATE SCHEMA IF NOT EXISTS inat_obs_data`). Single table entry: `observations`. Use the exact two-space indent and `- name:` / `schema:` / `tables:` / `- name:` shape of the existing four blocks. Do not add description fields or data_tests. Maintain the trailing newline at EOF.
  </action>
  <verify>
    <automated>cd data && uv run python -c "import yaml; d=yaml.safe_load(open('dbt/models/sources.yml')); names=[s['name'] for s in d['sources']]; assert 'inat_obs_data' in names, names; src=next(s for s in d['sources'] if s['name']=='inat_obs_data'); assert src['schema']=='inat_obs_data'; assert [t['name'] for t in src['tables']]==['observations']; print('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `data/dbt/models/sources.yml` parses as YAML and contains a top-level `sources` entry with `name: inat_obs_data`
    - The new entry has `schema: inat_obs_data`
    - The new entry has exactly one tables item with `name: observations`
    - The five existing source blocks (`ecdysis_data`, `inaturalist_data`, `inaturalist_waba_data`, `checklist_data`, `geographies`) remain unchanged (same `name` values present in YAML)
    - The total number of `sources` entries goes from 5 to 6
  </acceptance_criteria>
  <done>sources.yml declares the inat_obs_data source so that `{{ source('inat_obs_data', 'observations') }}` resolves in Plan 02 (int_combined ARM 3) and Plan 03 (inat_obs_count_agg CTE).</done>
</task>

<task type="auto">
  <name>Task 2: Add three RED tests for OCC-01 in test_dbt_scaffold.py</name>
  <files>data/tests/test_dbt_scaffold.py</files>
  <read_first>
    - data/tests/test_dbt_scaffold.py (all 205 lines — note _CHECKLIST_GUARD pattern at lines 117-121, duckdb.execute(read_parquet(...)).fetchone() pattern at lines 48-59, and module-level `SANDBOX = Path(__file__).resolve().parent.parent / "dbt" / "target" / "sandbox"` at line 23)
    - .planning/phases/118-occurrence-model-extension/118-PATTERNS.md (test_dbt_scaffold.py self-extension block — exact code for three new functions)
    - .planning/phases/118-occurrence-model-extension/118-RESEARCH.md (Validation Architecture / Phase Requirements → Test Map — function names exactly as listed)
  </read_first>
  <action>
    Append to the end of data/tests/test_dbt_scaffold.py: (1) a module-level guard named `_OCCURRENCES_GUARD` defined as `pytest.mark.skipif(not (SANDBOX / "occurrences.parquet").exists(), reason="run `bash data/dbt/run.sh build` first to produce sandbox outputs")`; (2) three new test functions, each decorated with `@_OCCURRENCES_GUARD`, with the exact names and assertions: (a) `test_occurrences_source_column` — `SELECT COUNT(*) FROM read_parquet('{parquet_path}') WHERE source IS NULL`; assert row[0] == 0. (b) `test_inat_obs_rows_in_occurrences` — `SELECT COUNT(*) ... WHERE source = 'inat_obs'`; assert row[0] > 0. (c) `test_source_no_nulls` — `SELECT COUNT(*) ... WHERE source NOT IN ('ecdysis', 'waba_sample', 'inat_obs')`; assert row[0] == 0. Each function reads `parquet_path = str(SANDBOX / "occurrences.parquet")` (must use `str(...)`, never bare Path inside the f-string per PATTERNS.md shared pattern). Docstrings reference OCC-01. Do not modify existing tests or imports — `pytest`, `duckdb`, and `SANDBOX` are already imported at the top of the file.
  </action>
  <verify>
    <automated>cd data && uv run pytest tests/test_dbt_scaffold.py --collect-only -q 2>&1 | grep -E "test_occurrences_source_column|test_inat_obs_rows_in_occurrences|test_source_no_nulls" | wc -l | tr -d ' ' | grep -q '^3$' && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - Pytest collects exactly three new tests with names `test_occurrences_source_column`, `test_inat_obs_rows_in_occurrences`, `test_source_no_nulls` (verifiable by `pytest --collect-only -q`)
    - Each new test function has the `@_OCCURRENCES_GUARD` decorator (verifiable by grep: `grep -B1 'def test_occurrences_source_column' data/tests/test_dbt_scaffold.py | grep -c _OCCURRENCES_GUARD` returns 1)
    - Module-level identifier `_OCCURRENCES_GUARD` is defined exactly once (`grep -c '^_OCCURRENCES_GUARD = pytest.mark.skipif' data/tests/test_dbt_scaffold.py` returns 1)
    - Existing tests collect without error (`pytest --collect-only -q` exit code 0)
    - When run today (before Plan 02), the three new tests either SKIP (occurrences.parquet missing) or FAIL (source column absent) — they MUST NOT pass: `cd data && uv run pytest tests/test_dbt_scaffold.py::test_inat_obs_rows_in_occurrences -x` returns non-zero or skip
  </acceptance_criteria>
  <done>Three pytest functions exist, are collected, and use the SKIPIF guard so the suite stays green pending Plan 02's dbt build artifacts.</done>
</task>

<task type="auto">
  <name>Task 3: Add RED test for OCC-02/OCC-03 in test_species_export.py</name>
  <files>data/tests/test_species_export.py</files>
  <read_first>
    - data/tests/test_species_export.py (all 69 lines — note _SANDBOX_GUARD at lines 24-27, monkeypatch + export_species_parquet(con) call pattern at lines 31-45, and imports at lines 14-20 including `from species_export import export_species_parquet, SPECIES_COLUMNS`)
    - .planning/phases/118-occurrence-model-extension/118-PATTERNS.md (test_species_export.py self-extension block — exact code for new function)
  </read_first>
  <action>
    Append to the end of data/tests/test_species_export.py a single new test function `test_inat_obs_count_in_species(tmp_path, monkeypatch)` decorated with `@_SANDBOX_GUARD`. Body: monkeypatch `se_mod.ASSETS_DIR` to `tmp_path`; monkeypatch env var `DBT_SANDBOX_DIR` to `str(SANDBOX)`; `con = duckdb.connect()`; call `export_species_parquet(con)`; run `duckdb.execute(f"SELECT COUNT(*) FROM read_parquet('{tmp_path}/species.parquet') WHERE inat_obs_count IS NULL").fetchone()`; assert row[0] == 0 with message including count; then assert `'inat_obs_count' in SPECIES_COLUMNS` (membership check, not position). Docstring references OCC-02/03. Do not add new imports — `pytest`, `duckdb`, `se_mod`, `export_species_parquet`, `SPECIES_COLUMNS`, `SANDBOX`, and `_SANDBOX_GUARD` are all already in scope at the top of the file.
  </action>
  <verify>
    <automated>cd data && uv run pytest tests/test_species_export.py --collect-only -q 2>&1 | grep -c 'test_inat_obs_count_in_species' | grep -q '^1$' && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - Pytest collects exactly one new test with name `test_inat_obs_count_in_species`
    - The new test function has the `@_SANDBOX_GUARD` decorator (`grep -B1 'def test_inat_obs_count_in_species' data/tests/test_species_export.py | grep -c _SANDBOX_GUARD` returns 1)
    - No new top-level import statements added (line count of `^import\|^from` block does not increase)
    - When run today (before Plan 03), test either SKIPs (sandbox absent) or FAILs (inat_obs_count column missing): `cd data && uv run pytest tests/test_species_export.py::test_inat_obs_count_in_species -x` returns non-zero or skip
  </acceptance_criteria>
  <done>One pytest function exists, is collected, and uses the _SANDBOX_GUARD so the suite stays green pending Plan 03's species.parquet inat_obs_count column.</done>
</task>

</tasks>

<verification>
After all three tasks:
1. `cd data && uv run pytest tests/test_dbt_scaffold.py tests/test_species_export.py --collect-only -q` exits 0 with the four new test names listed
2. `cd data && uv run pytest tests/test_dbt_scaffold.py tests/test_species_export.py -x` exits 0 (existing tests pass; new tests skip because Plan 02/03 have not built yet) OR exits non-zero in a clean check-out where sandbox parquets exist from earlier runs (RED gate signalling missing columns is also acceptable)
3. `cd data && uv run python -c "import yaml; print('inat_obs_data' in [s['name'] for s in yaml.safe_load(open('dbt/models/sources.yml'))['sources']])"` prints `True`
</verification>

<success_criteria>
- sources.yml declares the inat_obs_data source so Plan 02/03 dbt refs resolve
- Four new pytest functions collected by name, each behind a SKIPIF guard
- Existing 117 pipeline / dbt scaffold tests still pass
</success_criteria>

<output>
Create `.planning/phases/118-occurrence-model-extension/118-01-SUMMARY.md` when done.
</output>
