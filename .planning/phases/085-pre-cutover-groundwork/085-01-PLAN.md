---
phase: 085
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - data/dbt/models/staging/stg_inat__observations.sql
  - data/dbt/models/staging/schema.yml
autonomous: true
requirements:
  - TEST-01

must_haves:
  truths:
    - "stg_inat__observations no longer emits the null-id dlt tombstone row (10,845 rows post-filter, down from 10,846)"
    - "The not_null generic test on stg_inat__observations.id PASSES under dbt build (no longer awkward-fit)"
    - "The unique generic test on stg_inat__observations.id continues to PASS"
    - "downstream occurrences.parquet row count is unchanged (47,883) because the tombstone never joined to any downstream model"
  artifacts:
    - path: "data/dbt/models/staging/stg_inat__observations.sql"
      provides: "Staging view of inaturalist_data.observations with WHERE id IS NOT NULL filter"
      contains: "WHERE id IS NOT NULL"
    - path: "data/dbt/models/staging/schema.yml"
      provides: "Test declarations for staging models; not_null on stg_inat__observations.id no longer annotated as awkward-fit"
  key_links:
    - from: "data/dbt/models/staging/stg_inat__observations.sql"
      to: "source('inaturalist_data', 'observations')"
      via: "FROM source(...) WHERE id IS NOT NULL"
      pattern: "WHERE id IS NOT NULL"
    - from: "data/dbt/models/staging/schema.yml"
      to: "stg_inat__observations.id not_null test"
      via: "data_tests entry without OBSERVED FAIL annotation"
      pattern: "not_null"
---

<objective>
Resolve the TEST-01 awkward-fit: add a `WHERE id IS NOT NULL` filter to `stg_inat__observations.sql`
so the dlt soft-delete tombstone row (`is_deleted=True`, `id=NULL`, all domain fields NULL) is
excluded from the staging view, and update `staging/schema.yml` to reflect that the `not_null` test
now passes cleanly.

Purpose: Move the iNat `not_null` test from FAIL (1 null id) to PASS without touching the raw dlt
layer (the tombstone is preserved in `inaturalist_data.observations` for dlt merge bookkeeping). This
is one of two awkward-fits Phase 85 closes; together they let `dbt build` exit 0.

Output: A 1-line SQL change + a schema.yml comment cleanup. No downstream row-count change
(`occurrences.parquet` remains 47,883 rows) because the tombstone never joined to any downstream
model.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/085-pre-cutover-groundwork/085-RESEARCH.md
@.planning/phases/085-pre-cutover-groundwork/085-PATTERNS.md
@CLAUDE.md

@data/dbt/models/staging/stg_inat__observations.sql
@data/dbt/models/staging/schema.yml
@data/dbt/models/staging/stg_ecdysis__occurrences.sql

<interfaces>
<!-- The staging filter pattern is established in stg_ecdysis__occurrences.sql:
     inline comment explaining the filter is load-bearing, then SELECT * FROM source WHERE <cond>.
     No column aliasing at the staging layer. -->

Analog (stg_ecdysis__occurrences.sql):
```sql
{{ config(materialized='view') }}

SELECT *
FROM {{ source('ecdysis_data', 'occurrences') }}
WHERE decimal_latitude IS NOT NULL AND decimal_latitude != ''
```

Verified facts from RESEARCH.md (HIGH confidence):
- The null id row in inaturalist_data.observations is a dlt soft-delete tombstone
  (id=None, uuid='75525c08-66cc-40f5-a3ac-cbe6fcac41dd', is_deleted=True; all domain fields NULL).
- The tombstone does NOT join to anything downstream: int_ecdysis_base LEFT JOIN is on
  inat.id = links.host_observation_id; NULL never equi-joins.
- Post-filter row count: 10,845 (from 10,846).
- occurrences.parquet row count: 47,883 unchanged.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add WHERE id IS NOT NULL to stg_inat__observations.sql</name>
  <files>data/dbt/models/staging/stg_inat__observations.sql</files>
  <action>
    Edit data/dbt/models/staging/stg_inat__observations.sql. Add a `WHERE id IS NOT NULL` clause to
    the SELECT and add an inline comment block above `{{ config(...) }}` explaining the filter is
    load-bearing.

    Keep the existing "Used by:" comment block (lines 1-6) — it documents how downstream models
    consume this view and remains accurate. Insert a new block above or below it explaining the
    WHERE clause: the null-id row is a dlt soft-delete tombstone (`is_deleted=True`, all domain
    fields NULL), the tombstone is preserved in the raw schema for dlt merge bookkeeping, and
    filtering here is safe because NULL never equi-joins to any downstream model
    (int_ecdysis_base LEFT JOIN uses `inat.id = links.host_observation_id`).

    The final SELECT block becomes:
    `SELECT * FROM {{ source('inaturalist_data', 'observations') }} WHERE id IS NOT NULL`

    No column renames or casts — those happen in intermediate models. Per TEST-01 decision D-01 the
    staging filter is the chosen option; do NOT modify data/inaturalist_pipeline.py (option b was
    explicitly rejected as invasive).
  </action>
  <verify>
    <automated>grep -c "WHERE id IS NOT NULL" data/dbt/models/staging/stg_inat__observations.sql | grep -q "^1$" || (echo "Filter not present exactly once" && exit 1); bash data/dbt/run.sh run --select stg_inat__observations</automated>
  </verify>
  <done>
    Staging view file contains exactly one `WHERE id IS NOT NULL` clause; running
    `bash data/dbt/run.sh run --select stg_inat__observations` succeeds and produces a view; the
    inline comment block explains why the filter is load-bearing (tombstone tombstone preserved in
    raw, NULL never joins downstream).
  </done>
</task>

<task type="auto">
  <name>Task 2: Update staging/schema.yml to reflect TEST-01 resolution</name>
  <files>data/dbt/models/staging/schema.yml</files>
  <action>
    Edit data/dbt/models/staging/schema.yml lines 19-29 (the stg_inat__observations entry):

    1. Rewrite the multi-line `description:` block under `- name: id`. Remove the OBSERVED FAIL
       language. The description should now state: iNaturalist observation ID. The dlt soft-delete
       tombstone row (id=NULL) is filtered upstream in stg_inat__observations.sql; this view
       contains 10,845 rows post-filter. Both not_null and unique now pass cleanly.

    2. Update the inline comment on the `- not_null` line: replace
       `# OBSERVED FAIL (1 null id) — awkward-fit finding, kept as a tripwire`
       with `# VERIFIED: 10,845 rows post-filter (tombstone excluded in staging view)`.

    3. Update the inline comment on the `- unique` line: change
       `# PASSED: 10,846 distinct / 10,846 rows`
       to `# VERIFIED unique: 10,845 distinct / 10,845 rows`.

    Follow the inline-annotation convention from stg_ecdysis__occurrences (line 10:
    `# VERIFIED unique: 46,090 rows, 46,090 distinct catalog_numbers`). Do NOT touch the
    stg_ecdysis__occurrences or stg_waba__observations blocks. Keep the file's other content
    (version, models list structure) intact.
  </action>
  <verify>
    <automated>grep -c "OBSERVED FAIL" data/dbt/models/staging/schema.yml | grep -q "^0$" || (echo "OBSERVED FAIL annotation still present" && exit 1); grep -c "post-filter" data/dbt/models/staging/schema.yml | grep -q "^[12]$" || (echo "post-filter annotation missing" && exit 1); bash data/dbt/run.sh test --select stg_inat__observations</automated>
  </verify>
  <done>
    schema.yml description no longer mentions "OBSERVED FAIL" or "awkward-fit"; the not_null and
    unique inline comments use the VERIFIED convention with the post-filter row count (10,845);
    `bash data/dbt/run.sh test --select stg_inat__observations` reports both tests PASS with 0 FAIL
    and 0 ERROR.
  </done>
</task>

</tasks>

<verification>
After both tasks land:

1. `bash data/dbt/run.sh run --select stg_inat__observations` succeeds.
2. `bash data/dbt/run.sh test --select stg_inat__observations` reports PASS=2 (not_null, unique),
   FAIL=0, ERROR=0.
3. `bash data/dbt/run.sh build` shows the iNat not_null test has moved from FAIL to PASS.
   (One awkward-fit remains — the TEST-02 ecdysis_id relationships ERROR — until Plan 02 lands.)
4. Spot-check that the inline filter comment correctly explains why the tombstone is preserved in
   raw and why filtering downstream is safe.
</verification>

<success_criteria>
- `WHERE id IS NOT NULL` is present in `stg_inat__observations.sql` with an explanatory comment.
- `staging/schema.yml` no longer carries the OBSERVED FAIL annotation; both tests pass on the
  filtered view.
- `dbt test --select stg_inat__observations` exits 0 with 2 PASS.
- The downstream `occurrences.parquet` row count is unchanged (verified once Plan 04 runs the full
  build, but no row-count change should occur here because the tombstone never joined).
</success_criteria>

<output>
After completion, create `.planning/phases/085-pre-cutover-groundwork/085-01-SUMMARY.md` capturing:
- The exact lines changed in each file.
- Confirmation that `dbt test --select stg_inat__observations` exits 0 with PASS=2.
- The before/after row count of the staging view (10,846 → 10,845).
- A note that the dlt tombstone remains in `inaturalist_data.observations` (raw layer untouched).
</output>
