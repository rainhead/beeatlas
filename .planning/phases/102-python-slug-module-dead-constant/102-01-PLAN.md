---
phase: 102-python-slug-module-dead-constant
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - data/domain.py
  - data/feeds.py
  - data/species_export.py
  - data/dbt/models/intermediate/int_species_universe.sql
  - data/tests/test_domain.py
  - data/tests/test_feeds.py
autonomous: true
requirements: [PY-01, PY-02]
tags: [python, refactor, slug, dbt-comment]

must_haves:
  truths:
    - "data/domain.py exists and exports slugify(text: str) -> str"
    - "feeds.py no longer defines _slugify; it imports slugify from domain"
    - "species_export.py imports slugify from domain (not _slugify from feeds)"
    - "BEE_FAMILIES tuple is absent from species_export.py"
    - "int_species_universe.sql comment names itself as the sole gate for bee family filtering"
    - "uv run pytest data/tests/ exits 0"
    - "Byte-equivalence tests assert new slugify produces identical output to the prior _slugify on a known input corpus"
  artifacts:
    - path: "data/domain.py"
      provides: "Canonical slugify function for Python pipeline"
      exports: ["slugify"]
    - path: "data/tests/test_domain.py"
      provides: "Byte-equivalence test for slugify"
      contains: "def test_slugify"
  key_links:
    - from: "data/feeds.py"
      to: "data/domain.py"
      via: "from domain import slugify"
      pattern: "from domain import slugify"
    - from: "data/species_export.py"
      to: "data/domain.py"
      via: "from domain import slugify"
      pattern: "from domain import slugify"
---

<objective>
Extract slug canonicalization into a named module (`data/domain.py`), remove the dead `BEE_FAMILIES` constant from `species_export.py`, update the dbt SQL comment to acknowledge it is the sole gate, and add byte-equivalence tests to prove the new public `slugify` matches the prior private `_slugify` exactly.

Purpose: Eliminate the awkward `from feeds import _slugify` cross-module private import that has been a code-smell since Phase 78; remove a misleading dead constant that suggests `species_export.py` does family filtering when it does not.

Output:
  - `data/domain.py` — new module with public `slugify`
  - Updated `data/feeds.py` and `data/species_export.py` importing from `domain`
  - Removed `BEE_FAMILIES` from `data/species_export.py`
  - Updated comment in `data/dbt/models/intermediate/int_species_universe.sql`
  - New `data/tests/test_domain.py` asserting byte-equivalence
  - Updated `data/tests/test_feeds.py` (imports `slugify`, not `_slugify`)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md

@data/feeds.py
@data/species_export.py
@data/dbt/models/intermediate/int_species_universe.sql
@data/tests/test_feeds.py

<interfaces>
<!-- Prior _slugify implementation in data/feeds.py lines 132-148. -->
<!-- Behavior to preserve byte-for-byte: -->
<!--   - unicodedata.normalize('NFKD', value) -->
<!--   - .encode('ascii', 'ignore').decode('ascii') -->
<!--   - .lower() -->
<!--   - re.sub(r'[\s_.,]+', '-', value)   # whitespace/_/./, -> hyphen -->
<!--   - re.sub(r'[^a-z0-9-]', '', value)  # strip remaining non-alnum-hyphen -->
<!--   - re.sub(r'-+', '-', value)        # collapse hyphens -->
<!--   - .strip('-') or 'unknown' -->
<!-- Imports needed in domain.py: re, unicodedata -->

<!-- Current call sites of _slugify: -->
<!--   data/feeds.py:292    base_slug = _slugify(filter_value) -->
<!--   data/species_export.py:30   from feeds import _slugify  # Phase 78 D-01 -->
<!--   data/species_export.py:147  r['slug'] = genus if genus else _slugify(r['scientificName']) -->
<!--   data/tests/test_feeds.py:14 from feeds import _slugify, ... -->
<!--   data/tests/test_feeds.py:209-219 test_slugify uses _slugify -->

<!-- Phase 78 D-01 invariant: slug output must be byte-for-byte unchanged. -->
<!-- The Phase 78 D-01 comment in species_export.py:30 may be updated to point to domain.slugify -->
<!-- but the byte-for-byte invariant claim remains true. -->

<!-- BEE_FAMILIES (species_export.py:51-54) is unused — verified by grep: -->
<!--   Only references are the definition itself; the SQL has the family list hardcoded. -->

<!-- int_species_universe.sql:69-75 — current comment says: -->
<!--   "Restrict to the seven Anthophila families (BEE_FAMILIES). Non-bee byproducts -->
<!--    of sample collection have no place in a bee species tree." -->
<!-- After PY-02, the SQL is the sole gate; the comment must say so explicitly -->
<!-- (e.g., note the previous Python BEE_FAMILIES constant was removed and this -->
<!--  WHERE clause is now the only filter). -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create data/domain.py with slugify; migrate imports in feeds.py and species_export.py</name>
  <files>data/domain.py, data/feeds.py, data/species_export.py</files>
  <read_first>
    - data/feeds.py (read in full to see current _slugify body and its internal call at line 292)
    - data/species_export.py (read top of file plus line 147 usage and surrounding docstring/comments)
  </read_first>
  <action>
    1. Create `data/domain.py` with a public `slugify(value: str) -> str` whose body is byte-for-byte identical to the current `_slugify` in `data/feeds.py` (lines 132-148). Imports: `re`, `unicodedata`. Include a docstring stating the function is the canonical slug for the Python pipeline (callers: feeds.py, species_export.py) and that its output is the Phase 78 D-01 byte-for-byte invariant (URL-safe ASCII, path-traversal-safe).
    2. In `data/feeds.py`:
       - Delete the local `_slugify` function definition (lines 132-148).
       - Remove now-unused imports if and only if they have no other use in the file (`re` and `unicodedata` are likely used elsewhere — verify before removing; do not remove if still referenced).
       - Add `from domain import slugify` at the top with other imports.
       - Replace the call at line 292 `base_slug = _slugify(filter_value)` with `base_slug = slugify(filter_value)`.
    3. In `data/species_export.py`:
       - Replace `from feeds import _slugify  # Phase 78 D-01: byte-for-byte slug invariant` with `from domain import slugify  # Phase 78 D-01: byte-for-byte slug invariant (Phase 102 PY-01: extracted from feeds._slugify)`.
       - Replace `_slugify(r['scientificName'])` at line 147 with `slugify(r['scientificName'])`.
       - Update the docstring at line 97 that mentions `feeds._slugify` to instead reference `domain.slugify`.
       - Update the comment at line 134 referencing `_slugify` to reference `slugify`.
    Do not change any other code. Do not refactor unrelated formatting.
  </action>
  <verify>
    <automated>cd data &amp;&amp; uv run python -c "from domain import slugify; assert slugify('Jane Smith') == 'jane-smith'; assert slugify('') == 'unknown'; assert slugify('Müller') == 'muller'; assert '/' not in slugify('../../etc/passwd'); print('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `data/domain.py` exists and contains `def slugify(value: str) -> str`
    - `grep -n "def _slugify" data/feeds.py` produces no output
    - `grep -n "from domain import slugify" data/feeds.py` returns exactly 1 line
    - `grep -n "from domain import slugify" data/species_export.py` returns exactly 1 line
    - `grep -n "from feeds import _slugify" data/species_export.py` produces no output
    - `grep -cE "\b_slugify\b" data/feeds.py` returns 0 (no remaining references)
    - `grep -cE "\b_slugify\b" data/species_export.py` returns 0
    - Running the verify command above prints `OK` and exits 0
  </acceptance_criteria>
  <done>
    domain.py exists with public slugify; feeds.py and species_export.py both import it; no remaining references to private _slugify outside test files updated in Task 3.
  </done>
</task>

<task type="auto">
  <name>Task 2: Remove BEE_FAMILIES from species_export.py; update int_species_universe.sql comment</name>
  <files>data/species_export.py, data/dbt/models/intermediate/int_species_universe.sql</files>
  <read_first>
    - data/species_export.py (specifically lines 46-55 for BEE_FAMILIES block + comment; verify no other references via grep)
    - data/dbt/models/intermediate/int_species_universe.sql (lines 60-76)
  </read_first>
  <action>
    1. In `data/species_export.py`, delete the `BEE_FAMILIES` constant tuple and its preceding 4-line explanatory comment (lines 47-54 inclusive — the comment beginning "# Anthophila — the seven recognized bee families." through the closing `)` of the tuple). Leave a single blank line between the import block and `SPECIES_COLUMNS`. Do not change any other code in the file.
    2. In `data/dbt/models/intermediate/int_species_universe.sql`, replace the comment at lines 69-70 (currently `-- Restrict to the seven Anthophila families (BEE_FAMILIES). Non-bee byproducts` / `-- of sample collection have no place in a bee species tree.`) with a comment that explicitly states this WHERE clause is the sole gate for restricting to the seven Anthophila bee families, that there is no other filter in the pipeline, and that the previous Python `BEE_FAMILIES` constant in `species_export.py` was removed in Phase 102 (PY-02) because it was dead code. Keep the comment concise (3-5 lines). Do not change the `WHERE family IN (...)` clause itself.
  </action>
  <verify>
    <automated>! grep -nE "BEE_FAMILIES" data/species_export.py &amp;&amp; grep -niE "(sole gate|only filter|sole filter)" data/dbt/models/intermediate/int_species_universe.sql</automated>
  </verify>
  <acceptance_criteria>
    - `grep -cE "BEE_FAMILIES" data/species_export.py` returns 0
    - `grep -nE "^WHERE family IN" data/dbt/models/intermediate/int_species_universe.sql` still matches (the WHERE clause is unchanged in semantics — the seven family literals remain)
    - The replacement comment in `int_species_universe.sql` (above the `WHERE family IN` clause) contains a phrase like "sole gate" or "only filter" or "sole filter" naming this WHERE clause as the single point of bee-family restriction
    - The replacement comment references Phase 102 / PY-02 and the removal of the Python `BEE_FAMILIES` constant
    - `cd data &amp;&amp; uv run python -c "import species_export"` exits 0 (module still imports cleanly)
  </acceptance_criteria>
  <done>
    Dead BEE_FAMILIES tuple removed from species_export.py; dbt SQL comment now accurately names itself as the sole bee-family gate and notes the removed Python constant.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Add byte-equivalence tests in test_domain.py; update test_feeds.py to import slugify from domain</name>
  <files>data/tests/test_domain.py, data/tests/test_feeds.py</files>
  <read_first>
    - data/tests/test_feeds.py (current test_slugify at lines 207-219 — preserve its assertions)
    - data/domain.py (the new module from Task 1)
    - data/feeds.py (post-Task-1 — confirm _slugify no longer exists)
  </read_first>
  <behavior>
    - test_slugify_basic: slugify("Jane Smith") == "jane-smith"; slugify("") == "unknown"; slugify("Müller") == "muller"
    - test_slugify_strips_punctuation: slugify("Mucera (subgenus)") contains only chars in [a-z0-9-]
    - test_slugify_path_traversal_safe: '/' and '.' are absent from slugify("../../etc/passwd")
    - test_slugify_collapses_runs: slugify("a  b__c..d,,e") == "a-b-c-d-e"
    - test_slugify_unicode_dash: slugify("foo—bar") strips the em-dash (not in [a-z0-9-])
    - test_slugify_byte_equivalence: for a fixed corpus of 12+ inputs (mix of accents, punctuation, parentheses, slashes, dots, empty, all-special), slugify produces the same byte-string output as the inline prior implementation reproduced literally inside the test (so the test is self-contained and does not depend on feeds._slugify still existing).
  </behavior>
  <action>
    1. Create `data/tests/test_domain.py` with the test cases listed in &lt;behavior&gt;. Import `from domain import slugify`. For `test_slugify_byte_equivalence`, define a private helper inside the test that re-implements the prior `_slugify` body verbatim (the same 7-step sequence: normalize NFKD → encode ascii ignore → lower → re.sub `[\s_.,]+` → re.sub `[^a-z0-9-]` → re.sub `-+` → strip `-` or `unknown`) and assert `slugify(s) == prior_impl(s)` for each input in a corpus list. Corpus inputs to include at minimum: `"Jane Smith"`, `""`, `"Müller"`, `"Mucera (subgenus)"`, `"../../etc/passwd"`, `"a  b__c..d,,e"`, `"foo—bar"`, `"François O'Brien"`, `"hyphen---runs"`, `"   "`, `"!!!"`, `"Andrena (Plastandrena) fenningeri"`.
    2. Update `data/tests/test_feeds.py`:
       - Line 14: change `from feeds import _slugify, write_variant_feed, write_all_variants, write_index_json` to `from feeds import write_variant_feed, write_all_variants, write_index_json` and add a new import line `from domain import slugify`.
       - Replace each occurrence of `_slugify(` with `slugify(` inside `test_slugify` (lines 209, 210, 212, 214, 218, 219) and update the docstring on line 208 to read `"""slugify converts human names to URL-safe ASCII slugs."""` (drop the leading underscore).
       - Do not change any other tests in the file.
  </action>
  <verify>
    <automated>cd data &amp;&amp; uv run pytest tests/test_domain.py tests/test_feeds.py -v</automated>
  </verify>
  <acceptance_criteria>
    - `data/tests/test_domain.py` exists and contains `from domain import slugify`
    - `data/tests/test_domain.py` contains a function named `test_slugify_byte_equivalence`
    - `grep -nE "from feeds import .*_slugify" data/tests/test_feeds.py` returns no output
    - `grep -nE "from domain import slugify" data/tests/test_feeds.py` returns exactly 1 line
    - `cd data && uv run pytest tests/test_domain.py -v` exits 0 and shows at least 6 passing tests
    - `cd data && uv run pytest tests/test_feeds.py::test_slugify -v` exits 0
    - `cd data && uv run pytest tests/` exits 0 (full data test suite still green)
  </acceptance_criteria>
  <done>
    Byte-equivalence proof exists in test_domain.py; test_feeds.py imports slugify from domain; full `uv run pytest data/tests/` is green.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User-provided text → filesystem path | `slugify` output is used in feed filenames (`collector-{slug}.xml`, `genus-{slug}.xml`) and species page slugs. Untrusted strings (collector names from iNat, genus names from dbt) cross this boundary. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-102-01 | Tampering | `domain.slugify` output used in filesystem paths | mitigate | Function strips `/`, `.`, and all non-`[a-z0-9-]` characters; returns `'unknown'` for empty result. Verified by `test_slugify_path_traversal_safe` and existing `test_slugify` assertions. Byte-equivalence with prior implementation means the security posture is unchanged from Phase 78 D-01. |
| T-102-02 | Information Disclosure | Public function rename (`_slugify` → `slugify`) | accept | Rename does not change behavior or expose new data; merely promotes a private helper to a public module API. No exposure delta. |
</threat_model>

<verification>
After all tasks complete, run from the repo root:

```bash
cd data && uv run pytest tests/
```

Must exit 0 with all tests passing. The new `test_domain.py` byte-equivalence test provides the Phase 78 D-01 invariant proof.

Static grep gates (run from repo root):

```bash
# PY-01 gates
test "$(grep -c 'def _slugify' data/feeds.py)" = "0"
test "$(grep -c 'from domain import slugify' data/feeds.py)" = "1"
test "$(grep -c 'from domain import slugify' data/species_export.py)" = "1"
test "$(grep -cE '\b_slugify\b' data/species_export.py)" = "0"

# PY-02 gates
test "$(grep -c 'BEE_FAMILIES' data/species_export.py)" = "0"
grep -qiE '(sole gate|only filter|sole filter)' data/dbt/models/intermediate/int_species_universe.sql
grep -q 'PY-02\|Phase 102' data/dbt/models/intermediate/int_species_universe.sql
```

All commands must exit 0.

Note: dbt does not need to be re-built for this phase — `int_species_universe.sql` only changes a SQL comment; the `WHERE family IN (...)` clause is unchanged. The grep gates above are sufficient.
</verification>

<success_criteria>
1. `data/domain.py` exists and exports `slugify(text: str) -> str`; `feeds.py`'s `_slugify` private function is absent from the file. ✅ PY-01
2. `feeds.py` and `species_export.py` both import `slugify` from `domain`. ✅ PY-01
3. `BEE_FAMILIES` does not appear in `species_export.py`; `int_species_universe.sql` contains a comment naming itself as the sole gate for bee family filtering and noting the dead Python constant was removed in Phase 102. ✅ PY-02
4. `uv run pytest data/tests/` passes; `test_domain.py::test_slugify_byte_equivalence` proves byte-for-byte parity with the prior implementation. ✅
</success_criteria>

<output>
Create `.planning/phases/102-python-slug-module-dead-constant/102-01-SUMMARY.md` when done.
</output>
