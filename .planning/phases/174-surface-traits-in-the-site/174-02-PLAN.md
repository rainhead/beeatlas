---
phase: 174-surface-traits-in-the-site
plan: 02
type: execute
wave: 2
depends_on: ["174-01"]
files_modified:
  - _data/species.js
  - src/styles/taxon-pages.css
  - src/tests/data-species.test.ts
autonomous: true
requirements: [TRAIT-UI-02, TRAIT-UI-03]
must_haves:
  truths:
    - "host_bees comma-strings resolve to typed link targets (species slug / genus / plain text) on every species row (D-05)"
    - "makeSpeciesNode leaf nodes carry sociality, sociality_source, diet_breadth, diet_breadth_source, host_plant_family keys (present, null-safe) for index badges (D-06/D-07)"
    - "genus/subgenus species rows carry trait fields via the existing { ...sp } spread (no builder change)"
    - "the .traits* and .node-badge* CSS classes exist exactly per UI-SPEC"
  artifacts:
    - path: "_data/species.js"
      provides: "resolveHostBees() + per-species resolvedHostBees + makeSpeciesNode trait threading"
      contains: "resolveHostBees"
    - path: "src/styles/taxon-pages.css"
      provides: "trait fact-sheet + badge CSS"
      contains: ".node-badge--specialist"
    - path: "src/tests/data-species.test.ts"
      provides: "trait threading + resolvedHostBees shape assertions"
      contains: "resolvedHostBees"
  key_links:
    - from: "_data/species.js resolveHostBees"
      to: "byScientificName / higherTaxaByRankName['genus']"
      via: "build-time name → slug/genus resolution"
      pattern: "higherTaxaByRankName\\['genus'\\]"
    - from: "_data/species.js makeSpeciesNode"
      to: "sp.sociality / sp.diet_breadth / sp.host_plant_family"
      via: "explicit field list (null-coalesced)"
      pattern: "sociality: sp.sociality"
---

<objective>
Thread the merged trait fields through the Eleventy build-time data layer so the
templates (wave 3) can render them, and add the full trait CSS contract.

Two threading paths (RESEARCH §"makeSpeciesNode Trait Threading"):
1. `makeSpeciesNode` builds `fullTree` species leaves from an EXPLICIT field list —
   it must gain the 5 badge fields (D-06/D-07) or the index badges silently break.
2. `genusList` / `subgenusList` build species entries via `{ ...sp }` spread, which
   already carries all 11 trait fields after Path B — NO builder change needed
   (verify this holds).

Plus the cleptoparasite host-bee resolver (D-05): split each `host_bees` comma-string
and resolve every name to a typed link target (`species` slug, `genus` page, or plain
`text` fallback) at build time using `byScientificName` and
`higherTaxaByRankName['genus']`, stored as `sp.resolvedHostBees`.

Purpose: make trait data scannable (TRAIT-UI-03 threading) and host bees linkable
(TRAIT-UI-02 resolution) before the templates consume them.
Output: `resolveHostBees()`, per-species `resolvedHostBees`, badge fields on tree
leaves, the `.traits*`/`.node-badge*` CSS, and JS test coverage.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/174-surface-traits-in-the-site/174-CONTEXT.md
@.planning/phases/174-surface-traits-in-the-site/174-RESEARCH.md
@.planning/phases/174-surface-traits-in-the-site/174-PATTERNS.md
@.planning/phases/174-surface-traits-in-the-site/174-UI-SPEC.md
@_data/species.js
@_pages/genus.njk
@src/styles/taxon-pages.css
@src/tests/data-species.test.ts
</context>

<artifacts>
## Artifacts this plan produces

**New `_data/species.js` symbols:**
- `resolveHostBees(hostBees)` — splits a comma-joined `host_bees` string and returns
  `Array<{ name, slug?, genusName?, type: 'species'|'genus'|'text' }>` or `null`.
- `sp.resolvedHostBees` — assigned on every `flat` row (null when no host_bees), so it
  is carried by `byScientificName`, `speciesList`, and the `{ ...sp }` genus/subgenus spreads.
- `makeSpeciesNode` gains 5 explicit fields: `sociality`, `sociality_source`,
  `diet_breadth`, `diet_breadth_source`, `host_plant_family` (each `?? null`).

**New CSS classes in `src/styles/taxon-pages.css` (verbatim per UI-SPEC §CSS Contract):**
- `.traits`, `.traits-heading`, `.traits-dl` (+ `.traits-dl dt`, `.traits-dl dt:focus-visible`,
  `.traits-dl dd`, `.traits-dl dd a`, `.traits-dl dd a:hover`)
- `.node-badge`, `.node-badge:focus-visible`, `.node-badge--specialist`
  (`.node-badge` is NOT scoped to `.species-index` — genus/subgenus pages reuse it).

**New tests in `src/tests/data-species.test.ts`:**
- makeSpeciesNode leaf carries `sociality`/`diet_breadth`/`host_plant_family` keys.
- genusList species entries carry trait fields via spread.
- every `flat` row has a `resolvedHostBees` key (null or array of typed entries).
</artifacts>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Host-bee resolver + makeSpeciesNode trait threading + JS tests</name>
  <files>_data/species.js, src/tests/data-species.test.ts</files>
  <behavior>
    - Every element of `species.flat` has a `resolvedHostBees` key — `null` when `host_bees` is absent, otherwise an array whose entries each have a `type` of `'species'`, `'genus'`, or `'text'`.
    - A host name that matches `byScientificName` and has a slug resolves to `{ type: 'species', slug }`; a genus-only name matching `higherTaxaByRankName['genus']` resolves to `{ type: 'genus', genusName }`; anything else resolves to `{ type: 'text' }`.
    - Every `fullTree` species leaf has the keys `sociality`, `diet_breadth`, `host_plant_family` present (null is acceptable, `undefined` is NOT — `'sociality' in leaf` must be true).
    - `genusList` species entries expose trait keys via the `{ ...sp }` spread (no builder change).
  </behavior>
  <read_first>
    - `_data/species.js` lines 47-53 — where `flat` and `byScientificName` are built; the `resolveHostBees` definition + the `for (const sp of flat) sp.resolvedHostBees = resolveHostBees(sp.host_bees)` loop insert AFTER line 53 and BEFORE `speciesList` (line 76), so all downstream builders (speciesList, genusList, subgenusList, fullTree) see `resolvedHostBees`.
    - `_data/species.js` lines 23-29 — `higherTaxaByRankName` shape (`['genus']['Andrena'] -> row`).
    - `_data/species.js` lines 105-107 (`genusList` `{ ...sp, hexColor }` spread) and 188 (`subgenusList` same) — confirm the spread carries trait fields with no change (RESEARCH §"genusList and subgenusList — no code change needed").
    - `_data/species.js` lines 370-382 — current `makeSpeciesNode`; add the 5 badge fields after `scientificName` per 174-PATTERNS.md §"_data/species.js" (verbatim replacement).
    - `174-PATTERNS.md` §"_data/species.js" — verbatim `resolveHostBees`, the flat loop, and the makeSpeciesNode replacement.
    - `174-RESEARCH.md` §"Host-Bee Resolution" (typing + case-sensitivity note) and §"Pitfall 6" (makeSpeciesNode omission breaks index badges).
    - `src/tests/data-species.test.ts` lines 51-63 — the `speciesList` / `genusList` assertion patterns to mirror; 174-PATTERNS.md §"src/tests/data-species.test.ts" gives the two new test bodies; add a third asserting `resolvedHostBees` key presence on `species.flat`.
  </read_first>
  <action>
    In `_data/species.js`, add `resolveHostBees(hostBees)` (returns null for falsy input; otherwise
    `hostBees.split(', ').map(...)` resolving each trimmed name: species match via
    `byScientificName[trimmed]` with a slug → `{ name, slug, type: 'species' }`; else genus match
    via `higherTaxaByRankName['genus']?.[trimmed]` → `{ name, genusName: trimmed, type: 'genus' }`;
    else `{ name, type: 'text' }`). Immediately after, add `for (const sp of flat) { sp.resolvedHostBees = resolveHostBees(sp.host_bees); }`.
    Place both AFTER `byScientificName` (line 53) and BEFORE `speciesList` (line 76). Keep the lookup
    case-sensitive — the plain-text fallback is correct per D-05 (do NOT add a case-insensitive
    lookup; RESEARCH Pitfall 3).

    Replace `makeSpeciesNode` (lines 370-382) to add, after `scientificName`, the five fields:
    `sociality: sp.sociality ?? null`, `sociality_source: sp.sociality_source ?? null`,
    `diet_breadth: sp.diet_breadth ?? null`, `diet_breadth_source: sp.diet_breadth_source ?? null`,
    `host_plant_family: sp.host_plant_family ?? null`. Do NOT touch `genusList`/`subgenusList` — the
    `{ ...sp }` spread already carries trait fields.

    Add three tests to `src/tests/data-species.test.ts` (bodies in 174-PATTERNS.md): (1) every
    `fullTree` species leaf has `sociality`/`diet_breadth`/`host_plant_family` keys present;
    (2) `genusList` species entries carry trait fields via spread (sociality is string-or-null,
    never undefined); (3) every `species.flat` row has a `resolvedHostBees` key that is `null` or an
    array whose entries each have a `type` in {species, genus, text}.
  </action>
  <verify>
    <automated>npm test</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "function resolveHostBees" _data/species.js` returns 1; `grep -c "sp.resolvedHostBees = resolveHostBees" _data/species.js` returns 1, placed before the `speciesList` definition.
    - `makeSpeciesNode` lists `sociality:`, `sociality_source:`, `diet_breadth:`, `diet_breadth_source:`, `host_plant_family:` (5 new fields) — `grep -c "sociality: sp.sociality" _data/species.js` returns 1.
    - `genusList`/`subgenusList` are unchanged except no new code (no `resolveHostBees` call inside them).
    - `npm test` exits 0 with the three new `data-species.test.ts` assertions passing (key presence works even when local species.json lacks trait data, because keys are null-coalesced).
  </acceptance_criteria>
  <done>resolveHostBees produces typed host-bee targets on every species row; makeSpeciesNode carries the 5 badge fields; the JS suite is green.</done>
</task>

<task type="auto">
  <name>Task 2: Add the trait fact-sheet + badge CSS (UI-SPEC CSS Contract)</name>
  <files>src/styles/taxon-pages.css</files>
  <read_first>
    - `174-UI-SPEC.md` §"CSS Contract" (lines 342-437) — the verbatim `.traits*` and `.node-badge*` rules, including exact spacing (4px grid: padding `0.25rem 0.5rem`, column-gap `0.75rem`, row-gap `0.25rem`), `var(--token, fallback)` usage, `cursor: help`, and `:focus-visible` rings.
    - `174-PATTERNS.md` §"src/styles/taxon-pages.css" — the same block + the note that `.node-badge` is NOT scoped to `.species-index` (genus/subgenus pages are `.taxon-page`, not `.species-index`).
    - `src/styles/taxon-pages.css` lines 281-297 (existing `:focus-visible` ring rules + the `@media (max-width: 480px)` block at the end of file, line 297) — append the new rules AFTER line 297, each under a `/* Phase 174: ... */` comment header.
  </read_first>
  <action>
    Append to the end of `src/styles/taxon-pages.css` (after line 297) the two `/* Phase 174: ... */`
    blocks exactly as given in 174-UI-SPEC.md §CSS Contract: (1) the fact-sheet rules `.traits`,
    `.traits-heading`, `.traits-dl` (grid `max-content 1fr`, column-gap `0.75rem`, row-gap `0.25rem`,
    `align-items: baseline`), `.traits-dl dt` (`cursor: help`), `.traits-dl dt:focus-visible`
    (`outline: 2px solid var(--accent, #2c7a2c)`), `.traits-dl dd`, `.traits-dl dd a`,
    `.traits-dl dd a:hover`; (2) the badge rules `.node-badge` (`flex: 0 0 auto`, `font-size: 0.75rem`,
    `padding: 0.25rem 0.5rem`, `cursor: help`, `white-space: nowrap`), `.node-badge:focus-visible`,
    and `.node-badge--specialist` (accent border + text). Use only `var(--token, fallback)` form — no
    bare hex except as fallbacks. Do NOT scope `.node-badge` to `.species-index`.
  </action>
  <verify>
    <automated>grep -c -E '\.traits\b|\.traits-heading|\.traits-dl|\.node-badge\b|\.node-badge--specialist' src/styles/taxon-pages.css && npm test</automated>
  </verify>
  <acceptance_criteria>
    - `src/styles/taxon-pages.css` contains selectors `.traits`, `.traits-heading`, `.traits-dl`, `.traits-dl dt`, `.traits-dl dd`, `.node-badge`, `.node-badge--specialist` (grep for each returns >= 1).
    - `.node-badge` is defined WITHOUT a `.species-index` ancestor selector (`grep -c '.species-index .node-badge' src/styles/taxon-pages.css` returns 0).
    - `.traits-dl dt` and `.node-badge` both carry `cursor: help` and have a `:focus-visible` outline using `var(--accent, #2c7a2c)`.
    - Padding/gap values are on the 4px grid (`0.25rem`, `0.5rem`, `0.75rem`) — no `0.1rem`/`0.35rem`/`0.15rem`.
    - `npm test` exits 0 (no regression).
  </acceptance_criteria>
  <done>The trait fact-sheet and badge CSS classes exist verbatim per UI-SPEC, on-grid, reusable on both the index tree and genus/subgenus pages.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| species.json → species.js | Trait + `host_bees` strings (from the trusted Phase 174-01 pipeline) enter the build-time data layer and are transformed into `resolvedHostBees` link targets consumed by templates (wave 3). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-174-03 | Injection (XSS, stored-from-seed) | `resolveHostBees` host-bee link targets | mitigate | The resolver TYPES every host name: only a `byScientificName` match yields a `slug` (produced by path-safe `domain.slugify`) and only a `higherTaxaByRankName['genus']` match yields a `genusName` (a known atlas genus — a clean single alphabetic token). Unmatched names are typed `'text'` and rendered as escaped text in wave 3. Arbitrary `host_bees` input therefore never reaches `href` construction; it falls through to autoescaped text. |
| T-174-SC | Tampering | npm/pip/cargo installs | accept (N/A) | No package installs this phase (RESEARCH Package Legitimacy Audit: none). |
</threat_model>

<verification>
- `npm test` is green (resolver typing, makeSpeciesNode key presence, genusList spread, resolvedHostBees shape).
- CSS grep gates confirm all `.traits*` and `.node-badge*` selectors exist, on-grid, with `.node-badge` un-scoped.
- The resolver's typed output is the security control that keeps unresolved host names out of href construction (T-174-03).
</verification>

<success_criteria>
- `resolveHostBees` resolves host names to typed targets; `sp.resolvedHostBees` exists on every species row.
- `makeSpeciesNode` carries the 5 badge fields; genus/subgenus rows carry traits via spread (verified by tests).
- `.traits*` and `.node-badge*` CSS exists exactly per UI-SPEC, reusable across index + genus/subgenus.
- `npm test` is green.
</success_criteria>

<output>
Create `.planning/phases/174-surface-traits-in-the-site/174-02-SUMMARY.md` when done.
</output>
