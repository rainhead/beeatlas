---
phase: 93-multi-color-svg-map-generation
plan: 02
type: execute
wave: 2
depends_on:
  - 93-01
files_modified:
  - data/species_maps.py
  - data/tests/test_species_maps.py
autonomous: false
requirements:
  - PIPE-02
tags:
  - python
  - svg
  - pipeline

must_haves:
  truths:
    - "Running `cd data && uv run python species_maps.py` against a populated beeatlas.duckdb produces one SVG per genus, one per (Genus, Subgenus) where subgenus is non-null/non-empty, and one per tribe, in addition to the existing per-species SVGs."
    - "Each group SVG contains one circle element per in-bbox occurrence with a per-species fill color from `_group_colors`."
    - "Group-map generation reuses the single occurrence sweep (occ_by_canon) and county backdrop already built by `generate_species_maps` — no second DB pass."
    - "The single wipe-and-rewrite of `public/data/species-maps/` happens exactly once at the start of `generate_species_maps`; group-map generation only creates subdirectories under the already-wiped tree."
    - "Color assignment for each group is deterministic across runs (same species roster -> same colors), per D-01."
  artifacts:
    - path: data/species_maps.py
      provides: "_generate_group_maps function plus integration into the existing generate_species_maps flow"
      contains: "def _generate_group_maps"
    - path: public/data/species-maps/genus/
      provides: "One <Genus>.svg per genus with occurrence_count > 0"
    - path: public/data/species-maps/subgenus/
      provides: "One <Subgenus>.svg per non-null subgenus, nested under <Genus>/"
    - path: public/data/species-maps/tribe/
      provides: "One <Tribe>.svg per tribe with members having occurrence_count > 0"
  key_links:
    - from: data/species_maps.py (generate_species_maps)
      to: data/species_maps.py (_generate_group_maps)
      via: "shared occ_by_canon dict, shared county backdrop, shared maps_dir Path"
      pattern: "_generate_group_maps\\(.*occ_by_canon.*backdrop.*maps_dir"
    - from: data/species_maps.py (_generate_group_maps)
      to: species.parquet
      via: "DuckDB read_parquet — SELECT canonical_name, genus, subgenus, tribe WHERE occurrence_count > 0"
      pattern: "SELECT canonical_name, genus, subgenus, tribe"
---

<objective>
Implement multi-color group SVG map generation in `data/species_maps.py`: emit one SVG per genus, one per (Genus, Subgenus) where subgenus is non-null, and one per tribe. Wire the new function into `generate_species_maps` so it runs in the same pipeline step, reuses the existing occurrence sweep and county backdrop, and shares the single wipe-and-rewrite of `species-maps/`. Update `data/tests/test_species_maps.py` so the previously-skip-guarded test from Plan 01 now exercises real output and passes.

Purpose: Deliver PIPE-02 — multi-color SVG maps for genus, subgenus, and tribe taxon pages. This is the data prerequisite for Phase 94 (Species & Genus Pages) and Phase 95 (Subgenus & Tribe Pages).

Output: Three new directory trees under `public/data/species-maps/` (`genus/`, `subgenus/<Genus>/`, `tribe/`) populated with deterministic multi-color SVG occurrence maps.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/93-multi-color-svg-map-generation/93-CONTEXT.md
@.planning/phases/93-multi-color-svg-map-generation/93-PATTERNS.md
@.planning/phases/93-multi-color-svg-map-generation/93-01-PLAN.md
@data/species_maps.py
@data/run.py
@data/species_export.py

<interfaces>
<!-- Existing helpers in data/species_maps.py (Plan 01 added _group_colors) -->

  _project(lon: float, lat: float) -> tuple[float, float]
  _in_bbox(lon: float, lat: float) -> bool
  _load_county_geojsons(con) -> list[dict]
  _build_county_backdrop(county_geojsons) -> ET.Element
  _write_species_svg(slug, points, backdrop, out_dir) -> int     # per-species, class="occ"
  _group_colors(canonical_names: list[str]) -> dict[str, str]    # added in Plan 01

<!-- Constants reused unchanged -->

  SVG_NS, VIEWBOX, SVG_WIDTH, SVG_HEIGHT, WA_BBOX, STYLE_CSS, ASSETS_DIR

<!-- New helper signature (THIS PLAN creates this) -->

  def _generate_group_maps(
      con: duckdb.DuckDBPyConnection,
      occ_by_canon: dict[str, list[tuple[float, float]]],
      backdrop: ET.Element,
      maps_dir: Path,
  ) -> None:
      """Emit multi-color SVGs under maps_dir/{genus,subgenus,tribe}/.

      Reads species.parquet for group membership; uses occ_by_canon
      for points (no second DB sweep). MUST NOT wipe maps_dir.
      """

<!-- species.parquet columns relevant here (from data/species_export.py SPECIES_COLUMNS) -->

  canonical_name, genus, subgenus, tribe, occurrence_count, ...
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add _generate_group_maps and a multi-color SVG writer in species_maps.py</name>
  <files>data/species_maps.py</files>
  <read_first>
    - data/species_maps.py (after Plan 01 modifications — _group_colors is already present)
    - .planning/phases/93-multi-color-svg-map-generation/93-PATTERNS.md (county backdrop reuse pattern; sorted-attrib idempotency; FileNotFoundError guard; MAP-04 clipped-count logging)
    - .planning/phases/93-multi-color-svg-map-generation/93-CONTEXT.md (D-01 color rule; D-03 no legend; subgenus null guard; output path shapes)
    - data/species_export.py lines 55–74 (SPECIES_COLUMNS list — confirms genus/subgenus/tribe columns in species.parquet)
  </read_first>
  <behavior>
    - Test 1: With a fixture parquet containing 3 species in genus `Andrena` (canonical_names `A a`, `A b`, `A c`), `_generate_group_maps` writes exactly one file at `<maps_dir>/genus/Andrena.svg`.
    - Test 2: Each circle element in `genus/Andrena.svg` carries a `fill` attribute matching one of the colors returned by `_group_colors(['A a', 'A b', 'A c'])`. No circle uses `class="occ"`.
    - Test 3: Same species set produces byte-identical genus SVG across two consecutive runs (idempotency).
    - Test 4: A species with `subgenus = NULL` or `subgenus = ''` does not produce a subgenus file; a species with non-empty subgenus `Melandrena` under genus `Andrena` produces `subgenus/Andrena/Melandrena.svg`.
    - Test 5: Tribe maps are emitted at `tribe/<Tribe>.svg` for every distinct tribe with at least one member of `occurrence_count > 0`.
    - Test 6: `_generate_group_maps` does NOT call `shutil.rmtree` (verified by absence of `rmtree` in function body via source inspection in the unit test).
    - Test 7: When `species.parquet` is missing, `_generate_group_maps` raises `FileNotFoundError` with a message referencing the missing path.
  </behavior>
  <action>
    Implement two new module-level functions in `data/species_maps.py`, placed AFTER `_write_species_svg` and BEFORE `generate_species_maps`:

    (a) `_write_group_svg(slug_path: str, species_points: dict[str, list[tuple[float, float]]], colors: dict[str, str], backdrop: ET.Element, out_dir: Path) -> int`
        - Deepcopy `backdrop` per call (mirror existing `_write_species_svg`).
        - For each `canon` in `sorted(species_points.keys())` (D-01 alphabetical order, stable across runs): create a `<g fill="{colors[canon]}">` sub-element on the root, then for each `(lon, lat)` in `species_points[canon]`: if `_in_bbox(lon, lat)` is False, increment `clipped` and continue; else append a `<circle>` child to the `<g>` with attribs `{"cx": f"{x:.2f}", "cy": f"{y:.2f}", "r": "2.5"}` (no `class`, no per-circle `fill`).
        - Skip species whose `species_points[canon]` is an empty list (do not emit an empty `<g>`).
        - Apply the sorted-attrib idempotency pass after construction (`for elem in root.iter(): if elem.attrib: elem.attrib = dict(sorted(elem.attrib.items()))`).
        - Compute `out_path = out_dir / f"{slug_path}.svg"`. Call `out_path.parent.mkdir(parents=True, exist_ok=True)`. Write via `ET.tostring(root, xml_declaration=True, encoding="unicode")`.
        - Return total `clipped` count.

    (b) `_generate_group_maps(con, occ_by_canon, backdrop, maps_dir)`:
        - Read `species.parquet` at `ASSETS_DIR / "species.parquet"`. If missing, raise `FileNotFoundError(f"{species_parquet} not found — run species-export STEP first")` (mirror existing guard text).
        - Query: `SELECT canonical_name, genus, subgenus, tribe FROM read_parquet('{species_parquet}') WHERE occurrence_count > 0 ORDER BY canonical_name`.
        - Build three group dicts: `genus_members: dict[str, list[str]]`, `subgenus_members: dict[tuple[str, str], list[str]]` (key = (genus, subgenus)), `tribe_members: dict[str, list[str]]`. Iterate rows: append `canonical_name` to `genus_members[genus]` when `genus` is non-null/non-empty; append to `subgenus_members[(genus, subgenus)]` ONLY when `subgenus` is non-null AND `subgenus.strip() != ''` AND `genus` is non-null/non-empty (CONTEXT subgenus null guard); append to `tribe_members[tribe]` when `tribe` is non-null/non-empty.
        - For each group dict + output subdir, iterate keys in sorted order and call `_write_group_svg`:
            - Genus: `out_dir = maps_dir / "genus"`, `slug_path = genus_name`, `species_points = {c: occ_by_canon.get(c, []) for c in members}`, `colors = _group_colors(members)`.
            - Subgenus: `out_dir = maps_dir / "subgenus"`, `slug_path = f"{genus_name}/{subgenus_name}"` (forward slash creates the nested Genus/ subdir via `_write_group_svg`'s parent.mkdir).
            - Tribe: `out_dir = maps_dir / "tribe"`, `slug_path = tribe_name`.
        - Sum total clipped count across all groups. At the end, print a single summary line in the style of the existing per-species summary, e.g. `f"  species-maps/groups: {n_genus + n_subgenus + n_tribe:,} files ({n_genus} genus, {n_subgenus} subgenus, {n_tribe} tribe), {total_clipped:,} total points clipped"`.
        - MUST NOT call `shutil.rmtree`. MUST NOT call `maps_dir.mkdir(parents=True)` at top level — the parent already exists (created by `generate_species_maps`).

    Then modify `generate_species_maps` (only the final block before `finally`): after the existing per-species loop completes and prints its summary, call `_generate_group_maps(con, occ_by_canon, backdrop, maps_dir)`. Do NOT change the wipe-and-rewrite logic, do NOT change the existing per-species loop, do NOT change the `own_con` connection management.

    Do not add fenced code blocks; do not modify `_group_colors`, `_project`, `_in_bbox`, `_load_county_geojsons`, `_build_county_backdrop`, or `_write_species_svg`. Do not add a new entry to `run.py` STEPS — group-map generation runs inside `generate_species_maps` per the PATTERNS.md integration choice.
  </action>
  <verify>
    <automated>cd data && uv run pytest tests/test_species_maps.py -x -v</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "^def _generate_group_maps" data/species_maps.py` returns 1.
    - `grep -c "^def _write_group_svg" data/species_maps.py` returns 1.
    - `grep -c "_generate_group_maps(con, occ_by_canon, backdrop, maps_dir)" data/species_maps.py` returns 1 (the call site inside `generate_species_maps`).
    - `grep -c "shutil.rmtree" data/species_maps.py` returns 1 (only the existing one in `generate_species_maps`; no new wipe).
    - `grep -E 'class="occ"' data/species_maps.py | wc -l` returns 1 (only the existing per-species writer; group writer uses per-group `<g fill="...">`).
    - `grep -c "SELECT canonical_name, genus, subgenus, tribe" data/species_maps.py` returns 1.
    - `grep -c "subgenus IS NULL\\|subgenus.strip()" data/species_maps.py` returns >= 1 (subgenus null guard present in Python code, not SQL — PATTERNS observation #3).
    - Module import smoke: `cd data && uv run python -c "from species_maps import _generate_group_maps, _write_group_svg, _group_colors; print('ok')"` exits 0 and prints `ok`.
    - Function-shape smoke: `cd data && uv run python -c "import inspect; from species_maps import _generate_group_maps; sig = inspect.signature(_generate_group_maps); assert list(sig.parameters) == ['con', 'occ_by_canon', 'backdrop', 'maps_dir'], sig"` exits 0.
    - Existing tests still pass: `cd data && uv run pytest tests/test_species_maps.py::test_write_species_svg_creates_subdir -v` exits 0.
  </acceptance_criteria>
  <done>
    `_generate_group_maps` and `_write_group_svg` are defined and wired into `generate_species_maps`. No new pipeline STEP is added. The single wipe still happens once per run. Module imports cleanly.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Activate group-output test in test_species_maps.py and add fixture-based end-to-end assertion</name>
  <files>data/tests/test_species_maps.py</files>
  <read_first>
    - data/tests/test_species_maps.py (current state after Plan 01)
    - data/species_maps.py (after Task 1 — confirm _generate_group_maps signature)
    - data/tests/conftest.py (session-scoped DuckDB fixture conventions; do NOT add to conftest)
    - data/tests/test_species_export.py (SANDBOX guard pattern for tests that need a real parquet)
  </read_first>
  <behavior>
    - The skip-guarded `test_group_map_output_paths_skip_guarded` from Plan 01 now runs (no longer skipped) because `_generate_group_maps` exists.
    - The test writes a tiny synthetic species.parquet to `tmp_path / "species.parquet"`, builds a trivial backdrop, calls `_generate_group_maps` with a hand-built `occ_by_canon`, and asserts the expected output files exist with non-empty content.
    - The test asserts that a subgenus file path uses `subgenus/<Genus>/<Subgenus>.svg` (nested), proving the slash-in-slug parent.mkdir behavior works.
    - The test asserts no subgenus file is emitted for a species whose subgenus column is empty string.
    - Determinism: a second call to `_generate_group_maps` against the same inputs produces byte-identical SVG output.
  </behavior>
  <action>
    Update the existing `test_group_map_output_paths_skip_guarded` test in `data/tests/test_species_maps.py`:

    - Remove the `@pytest.mark.skipif` decorator (or change the condition to always-False) now that `_generate_group_maps` exists; rename the test to `test_generate_group_maps_emits_expected_files` for clarity.
    - Build a tiny `species.parquet` in `tmp_path` using `pyarrow` (already a project dep — used by `species_export.py`):
        * 4 rows minimum: 
            - `{canonical_name: 'Andrena milwaukeensis', genus: 'Andrena', subgenus: 'Melandrena', tribe: 'Andrenini', occurrence_count: 2}`
            - `{canonical_name: 'Andrena prunorum', genus: 'Andrena', subgenus: '', tribe: 'Andrenini', occurrence_count: 1}`
            - `{canonical_name: 'Andrena vicina', genus: 'Andrena', subgenus: None, tribe: 'Andrenini', occurrence_count: 1}`
            - `{canonical_name: 'Bombus mixtus', genus: 'Bombus', subgenus: 'Pyrobombus', tribe: 'Bombini', occurrence_count: 3}`
        * Include all `SPECIES_COLUMNS` (import from `species_export`) with placeholder values for unused columns (None/empty) so `read_parquet` works — alternatively, write only the four columns the function actually queries (`canonical_name, genus, subgenus, tribe, occurrence_count`) and rely on DuckDB's tolerant SELECT.
    - `monkeypatch.setattr(species_maps_module, 'ASSETS_DIR', tmp_path)` so the function reads the test parquet.
    - Build an in-memory DuckDB connection (`duckdb.connect()`) — no schema setup needed; only `read_parquet` is used.
    - Build a trivial `backdrop = ET.Element(f"{{{SVG_NS}}}svg")`.
    - Build `occ_by_canon` with at least one in-bbox point per `canonical_name` and at least one out-of-bbox point on one species to exercise the clip path (a point at lon=-100, lat=40 is out of WA_BBOX).
    - `maps_dir = tmp_path / "species-maps"; maps_dir.mkdir()` (parent dir already exists from `tmp_path`); pre-create the species-maps dir to mirror real-world invariant (`_generate_group_maps` MUST NOT wipe).
    - Call `_generate_group_maps(con, occ_by_canon, backdrop, maps_dir)`.
    - Assert:
        * `(maps_dir / "genus" / "Andrena.svg").exists()` is True.
        * `(maps_dir / "genus" / "Bombus.svg").exists()` is True.
        * `(maps_dir / "subgenus" / "Andrena" / "Melandrena.svg").exists()` is True.
        * `(maps_dir / "subgenus" / "Bombus" / "Pyrobombus.svg").exists()` is True.
        * No subgenus file exists for Andrena prunorum/vicina (empty/null subgenus) — verify with `not any(p.name == 'prunorum.svg' for p in (maps_dir / 'subgenus').rglob('*.svg'))`.
        * `(maps_dir / "tribe" / "Andrenini.svg").exists()` and `(maps_dir / "tribe" / "Bombini.svg").exists()`.
        * Each genus SVG contains at least one `<circle>` element with a `fill` attribute (parse via `ET.parse`); no `<circle>` has `class="occ"` in group SVGs.
    - Add a separate test `test_generate_group_maps_deterministic` that calls the helper twice in two distinct tmp_paths with the same inputs and asserts the produced SVGs are byte-identical (`open(file_a, 'rb').read() == open(file_b, 'rb').read()` for each pair). Reuse the parquet-writing logic via a small helper function inside the test module.

    Keep all other tests untouched. Keep imports minimal: add `import pyarrow as pa`, `import pyarrow.parquet as pq`, `import duckdb`. If the imports above are not yet present, add them.
  </action>
  <verify>
    <automated>cd data && uv run pytest tests/test_species_maps.py -x -v</automated>
  </verify>
  <acceptance_criteria>
    - `cd data && uv run pytest tests/test_species_maps.py -x -v` exits 0 with zero SKIPPED tests in this file (the previously-skipped test now runs).
    - `grep -c "def test_generate_group_maps_emits_expected_files" data/tests/test_species_maps.py` returns 1.
    - `grep -c "def test_generate_group_maps_deterministic" data/tests/test_species_maps.py` returns 1.
    - `grep -c "skipif" data/tests/test_species_maps.py` returns 0 (no remaining skip guards in this file).
    - `cd data && uv run pytest tests/test_species_maps.py --collect-only -q | grep -c "::test_"` returns >= 9 (8 from Plan 01 reorganized + 1 new deterministic test, less any rename/replacement).
    - Test runtime < 10 seconds (no full pipeline run; only in-memory DuckDB on a synthetic parquet).
  </acceptance_criteria>
  <done>
    The forward-looking test from Plan 01 is now active and passing; deterministic behavior is locked in. `_generate_group_maps` is verified end-to-end against a synthetic parquet without requiring a full nightly pipeline run.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human verification — run real pipeline and spot-check group SVG outputs</name>
  <files>public/data/species-maps/genus/, public/data/species-maps/subgenus/, public/data/species-maps/tribe/</files>
  <action>Pause for human verification of pipeline outputs. The executor MUST NOT proceed past this checkpoint without explicit "approved" from the user. The work to be verified was completed in Task 1 (code) and Task 2 (tests).</action>
  <verify>Human inspects the output files and visual rendering per the checks below.</verify>
  <done>User responds "approved" after confirming all 8 verification checks pass.</done>
  <what-built>Multi-color group SVG maps emitted under `public/data/species-maps/{genus,subgenus,tribe}/` during the standard `species-maps` pipeline step.</what-built>
  <how-to-verify>
    Run the data pipeline locally (requires `data/beeatlas.duckdb` to be populated — typically pulled by `data/nightly.sh` or already present from a prior local run):

      cd data && uv run python species_maps.py

    Then check the outputs:

      ls public/data/species-maps/genus/ | head -10
      ls public/data/species-maps/subgenus/Andrena/ 2>/dev/null | head -10
      ls public/data/species-maps/tribe/ | head -10

    Expected:
      1. `public/data/species-maps/genus/Andrena.svg` exists and is non-empty.
      2. `public/data/species-maps/genus/Bombus.svg` exists and is non-empty.
      3. At least one file exists under `public/data/species-maps/subgenus/` (e.g. `Bombus/Pyrobombus.svg`).
      4. `public/data/species-maps/tribe/Andrenini.svg` exists.
      5. Open `genus/Andrena.svg` in a browser; visually confirm it shows the WA county outline with multi-colored dots (Andrena has ~72 species — many colors visible).
      6. Open the same SVG in a text editor; confirm it contains multiple `<g fill="#......">` group elements (one per species in the genus) and that no `<circle>` element has `class="occ"`.
      7. Re-run `cd data && uv run python species_maps.py` a second time. Verify byte-identical output for at least one file:
           shasum public/data/species-maps/genus/Andrena.svg
         (record first hash, re-run pipeline, recompute hash — they must match).
      8. Run the full pytest suite: `cd data && uv run pytest -x` — all tests pass.

    Optional: open `subgenus/Andrena/Melandrena.svg` in a browser to confirm nested subgenus paths render correctly.
  </how-to-verify>
  <resume-signal>Type "approved" if all 8 checks pass; otherwise paste the failing observation.</resume-signal>
</task>

</tasks>

<verification>
- `cd data && uv run pytest tests/test_species_maps.py -x -v` exits 0 with no skips.
- `cd data && uv run python species_maps.py` against a populated `beeatlas.duckdb` produces files under `public/data/species-maps/genus/`, `public/data/species-maps/subgenus/<Genus>/`, and `public/data/species-maps/tribe/`.
- Running the pipeline twice produces byte-identical SVG output for the same species roster (D-01 determinism).
- The single wipe-and-rewrite of `species-maps/` still happens exactly once per pipeline run.
- No changes to `data/run.py` STEPS list (group-map generation is internal to `generate_species_maps`).
</verification>

<success_criteria>
- PIPE-02 satisfied: multi-color SVGs exist for every genus, subgenus (where defined), and tribe.
- Each species within a group gets a distinct color via `_group_colors` (D-01).
- Color assignment is deterministic across runs.
- No legend in SVGs (D-03); legends live in HTML pages built in Phase 94/95.
- The existing per-species SVG generation is unchanged.
</success_criteria>

<output>
After completion, create `.planning/phases/93-multi-color-svg-map-generation/93-02-SUMMARY.md` per the template at @$HOME/.claude/get-shit-done/templates/summary.md. The summary MUST note the alphabetical-canonical_name sort key (D-02) so Phase 94's Eleventy template can match it for HTML swatch ordering.
</output>
