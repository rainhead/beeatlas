---
phase: 93-multi-color-svg-map-generation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - data/species_maps.py
  - data/tests/test_species_maps.py
autonomous: true
requirements:
  - PIPE-02
tags:
  - python
  - svg
  - pipeline
  - testing

must_haves:
  truths:
    - "A pure helper assigns a deterministic distinct HSL-derived hex color to each species in a group, sorted alphabetically by canonical_name (D-01)."
    - "Unit tests assert the determinism, sort order, and group-size handling of the color helper without requiring the full pipeline to run."
    - "A failing-first test asserts that genus, subgenus, and tribe SVG output paths exist after a hypothetical group-map run (xfail or skip-guarded until Plan 02 lands)."
  artifacts:
    - path: data/species_maps.py
      provides: "Pure function _group_colors(canonical_names) -> dict[str, str] mapping canonical_name -> '#rrggbb'"
      contains: "def _group_colors"
    - path: data/tests/test_species_maps.py
      provides: "Color helper unit tests + skip-guarded path assertions for genus/subgenus/tribe outputs"
      contains: "def test_group_colors"
  key_links:
    - from: data/tests/test_species_maps.py
      to: data/species_maps.py
      via: "from species_maps import _group_colors"
      pattern: "from species_maps import .*_group_colors"
---

<objective>
Add a small, pure color-assignment helper to `data/species_maps.py` and expand `data/tests/test_species_maps.py` with unit tests that lock in D-01 semantics (alphabetical canonical_name sort, evenly-spaced HSL hues, deterministic hex output, no dependency on group size). Also add skip-guarded path assertions for the genus/subgenus/tribe output directories so Plan 02's implementation is gated by a concrete contract.

Purpose: Establish the color-assignment contract and test scaffolding BEFORE Plan 02 implements `_generate_group_maps`. Plan 02 implements against this helper.

Output: One new pure helper `_group_colors` in `species_maps.py`; expanded test file covering the helper and forward-looking output-path expectations.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/93-multi-color-svg-map-generation/93-CONTEXT.md
@.planning/phases/93-multi-color-svg-map-generation/93-PATTERNS.md
@data/species_maps.py
@data/tests/test_species_maps.py
@data/tests/test_species_export.py

<interfaces>
<!-- Contract to be created in this plan. Plan 02 will import _group_colors. -->

New helper signature (to be added in data/species_maps.py):

  def _group_colors(canonical_names: list[str]) -> dict[str, str]:
      """Return a deterministic canonical_name -> '#rrggbb' mapping.

      D-01: sort input alphabetically, assign evenly-spaced HSL hues
      (lightness=0.5, saturation=0.7) via colorsys.hls_to_rgb, format
      as lowercase '#rrggbb'. Pure: identical input -> identical output.
      """

Existing reusable helpers in data/species_maps.py (Plan 02 will compose with these):
  - _project(lon, lat) -> tuple[float, float]
  - _in_bbox(lon, lat) -> bool
  - _load_county_geojsons(con) -> list[dict]
  - _build_county_backdrop(county_geojsons) -> ET.Element
  - _write_species_svg(slug, points, backdrop, out_dir) -> int  (Plan 02 will NOT reuse for group maps — needs multi-color)

Existing constants reused unchanged:
  - SVG_NS, VIEWBOX, SVG_WIDTH, SVG_HEIGHT, WA_BBOX, STYLE_CSS, ASSETS_DIR
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add _group_colors helper to species_maps.py</name>
  <files>data/species_maps.py</files>
  <read_first>
    - data/species_maps.py (current state — see imports block and existing helper conventions)
    - .planning/phases/93-multi-color-svg-map-generation/93-CONTEXT.md (D-01 specifies hue = i * 360 / n, fixed saturation ~70%, lightness ~50%)
    - .planning/phases/93-multi-color-svg-map-generation/93-PATTERNS.md (HSL→hex section: colorsys.hls_to_rgb(hue/360, 0.5, 0.7) → floats → '#rrggbb')
  </read_first>
  <action>
    Add `import colorsys` next to the existing stdlib imports (alphabetical with `copy`, `json`, `os`, `shutil`, `xml.etree.ElementTree as ET`). Add a new module-level pure helper `_group_colors(canonical_names: list[str]) -> dict[str, str]` placed immediately above `_write_species_svg`. Implementation rules:
      - Sort `canonical_names` alphabetically (D-01) using `sorted(canonical_names)`; do NOT mutate the input.
      - Compute `n = len(sorted_names)`. If `n == 0`, return `{}`. If `n == 1`, the single species receives hue 0.
      - For each `i, canon` in `enumerate(sorted_names)`: compute `hue = i * 360.0 / n` (float division), then `(r, g, b) = colorsys.hls_to_rgb(hue / 360.0, 0.5, 0.7)` — note HLS argument order (hue, lightness, saturation) per PATTERNS observation #5. Convert each channel via `int(round(channel * 255))` and format as `'#{:02x}{:02x}{:02x}'`. Use `round` (not bare `int(channel*255)`) to avoid systematic truncation bias across channels.
      - Return `dict` mapping `canon -> hex_string`. The dict is built in sorted-name iteration order; callers MUST NOT rely on dict ordering for color assignment — color is encoded in the value, ordering is only used for HUE computation.
      - Docstring must reference D-01 by ID and state determinism guarantee.
    Do NOT yet add `_generate_group_maps` — that lands in Plan 02. Do not modify any existing function bodies. Do not add fenced code blocks anywhere outside the helper.
  </action>
  <verify>
    <automated>cd data && uv run python -c "from species_maps import _group_colors; r = _group_colors(['Zebra', 'Apple', 'Mango']); assert list(r.keys()) == sorted(['Zebra','Apple','Mango']) or set(r.keys()) == {'Zebra','Apple','Mango'}; assert all(v.startswith('#') and len(v) == 7 for v in r.values()); print(r)"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "^import colorsys" data/species_maps.py` returns exactly one match.
    - `grep -n "^def _group_colors" data/species_maps.py` returns exactly one match positioned BEFORE the line containing `def _write_species_svg`.
    - `cd data && uv run python -c "from species_maps import _group_colors; assert _group_colors([]) == {}"` exits 0.
    - `cd data && uv run python -c "from species_maps import _group_colors; r = _group_colors(['Bombus mixtus']); assert set(r.keys()) == {'Bombus mixtus'} and r['Bombus mixtus'].startswith('#') and len(r['Bombus mixtus']) == 7"` exits 0.
    - `cd data && uv run python -c "from species_maps import _group_colors; a = _group_colors(['c','a','b']); b = _group_colors(['c','a','b']); assert a == b"` exits 0 (determinism).
    - `cd data && uv run python -c "from species_maps import _group_colors; r = _group_colors(['c','a','b']); s = _group_colors(['b','a','c']); assert r == s"` exits 0 (sort-input invariance).
    - `cd data && uv run python -c "from species_maps import _group_colors; r = _group_colors(['a']*1 + [chr(ord('a')+i) for i in range(1,72)]); assert len({v for v in r.values()}) >= 60"` exits 0 (72-species group like Andrena yields broad color spread, allowing some near-collisions but not more than ~12).
    - No existing function in data/species_maps.py is modified (line count for `def generate_species_maps` body unchanged; verified by `grep -c "^def " data/species_maps.py` increasing by exactly 1).
  </acceptance_criteria>
  <done>
    The new `_group_colors` helper is defined in `data/species_maps.py`, importable, pure, deterministic, and exercised by the verification commands above. Plan 02 can `from species_maps import _group_colors`.
  </done>
</task>

<task type="auto">
  <name>Task 2: Expand test_species_maps.py with color helper tests and forward-looking group-output assertions</name>
  <files>data/tests/test_species_maps.py</files>
  <read_first>
    - data/tests/test_species_maps.py (current single-test file — extend, do not rewrite)
    - data/tests/test_species_export.py (analog for SANDBOX guard pattern + monkeypatch ASSETS_DIR style)
    - .planning/phases/93-multi-color-svg-map-generation/93-CONTEXT.md (D-01 color rule; output paths: species-maps/genus/<Genus>.svg, species-maps/subgenus/<Genus>/<Subgenus>.svg, species-maps/tribe/<Tribe>.svg)
    - data/species_maps.py (to confirm _group_colors signature added in Task 1)
  </read_first>
  <action>
    Append new tests to `data/tests/test_species_maps.py` without removing or modifying the existing `test_write_species_svg_creates_subdir` test. Add these tests:

    1. `test_group_colors_determinism` — calls `_group_colors(['Andrena milwaukeensis', 'Andrena prunorum', 'Andrena vicina'])` twice with shuffled input order; asserts both calls return identical dicts.

    2. `test_group_colors_sort_order_independence` — passes the same names in two different orders; asserts identical result. Asserts that hue assignment is keyed on alphabetical position (the first alphabetical name maps to the same color regardless of input order).

    3. `test_group_colors_hex_format` — calls helper with 5 names; asserts every value matches regex `^#[0-9a-f]{6}$` (lowercase hex, 7 chars).

    4. `test_group_colors_empty` — asserts `_group_colors([]) == {}`.

    5. `test_group_colors_single_species` — asserts a 1-name input yields a 1-entry dict whose value is a valid hex string.

    6. `test_group_colors_large_group_distinct` — uses a 72-name synthetic list (mimicking Andrena scale) and asserts at least 60 distinct hex values (HSL evenly spaced will produce a few visually-near hues but not exact duplicates of int-rounded RGB).

    7. `test_group_map_output_paths_skip_guarded` — declared with `@pytest.mark.skipif(<condition>, reason="Plan 02 implements _generate_group_maps")`. The skip condition checks for the absence of `_generate_group_maps` in `species_maps` module (`not hasattr(species_maps_module, '_generate_group_maps')`). When unskipped (Plan 02 complete), it asserts that calling the function (with a mocked or fixture-backed con) produces output files at `genus/<Genus>.svg`, `subgenus/<Genus>/<Subgenus>.svg`, `tribe/<Tribe>.svg` shapes — but for THIS plan, only the skip guard and path-shape assertions are scaffolded; the test SKIPs in Plan 01 and is wired up to run automatically once Plan 02 lands. The body can be `pytest.skip` early-returns or use marker-based skip — pick the simpler skipif approach.

    Imports at top of file: keep existing `xml.etree.ElementTree as ET`; add `import re`, `import pytest`, `import species_maps as species_maps_module`, and import `_group_colors` directly: `from species_maps import _write_species_svg, _group_colors, SVG_NS`. Do NOT add a `SANDBOX_GUARD` — these tests do not need the dbt sandbox; they exercise pure helpers and module-level shape only.

    Do not introduce fixtures requiring DuckDB or the full pipeline. All tests in this file must run with `cd data && uv run pytest tests/test_species_maps.py -x` in <2 seconds, no DB.
  </action>
  <verify>
    <automated>cd data && uv run pytest tests/test_species_maps.py -x -v</automated>
  </verify>
  <acceptance_criteria>
    - `cd data && uv run pytest tests/test_species_maps.py -x -v` exits 0.
    - Test count: at least 7 new tests added (8 total including existing `test_write_species_svg_creates_subdir`). Verified by `cd data && uv run pytest tests/test_species_maps.py --collect-only -q | grep -c "::test_"` returning >= 8.
    - `grep -n "from species_maps import" data/tests/test_species_maps.py | grep -c "_group_colors"` returns 1.
    - `grep -c "def test_group_colors_determinism" data/tests/test_species_maps.py` returns 1.
    - `grep -c "def test_group_colors_empty" data/tests/test_species_maps.py` returns 1.
    - `grep -c "def test_group_map_output_paths_skip_guarded" data/tests/test_species_maps.py` returns 1.
    - `cd data && uv run pytest tests/test_species_maps.py::test_group_map_output_paths_skip_guarded -v` reports SKIPPED (because `_generate_group_maps` does not yet exist).
    - Existing test `test_write_species_svg_creates_subdir` still passes — `cd data && uv run pytest tests/test_species_maps.py::test_write_species_svg_creates_subdir -v` exits 0.
    - Total runtime under 5 seconds (no DuckDB session-scoped fixture imported).
  </acceptance_criteria>
  <done>
    `data/tests/test_species_maps.py` contains the pure-helper test battery for `_group_colors`, plus a skip-guarded scaffold test that will activate when Plan 02 adds `_generate_group_maps`. All tests run quickly without DuckDB.
  </done>
</task>

</tasks>

<verification>
- `cd data && uv run python -c "from species_maps import _group_colors"` succeeds.
- `cd data && uv run pytest tests/test_species_maps.py -x -v` exits 0 with at least 7 passed + 1 skipped (the forward-looking group-output test).
- `cd data && uv run pytest tests/ -x` continues to pass (no regressions in sibling tests like `test_species_export.py`).
</verification>

<success_criteria>
- `_group_colors` is pure, deterministic, and importable.
- Test file scaffolds the contract that Plan 02 must satisfy (group output paths under `genus/`, `subgenus/<Genus>/`, `tribe/`).
- No DuckDB or pipeline run required to execute these tests.
- Phase 92's `_write_species_svg` behavior unchanged.
</success_criteria>

<output>
After completion, create `.planning/phases/93-multi-color-svg-map-generation/93-01-SUMMARY.md` per the template at @$HOME/.claude/get-shit-done/templates/summary.md.
</output>
