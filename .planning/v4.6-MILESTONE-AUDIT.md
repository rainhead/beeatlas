---
milestone: v4.6
milestone_name: Taxonomy Hierarchy & Normalization
audited: "2026-06-03"
gaps_closed: "2026-06-03 — atlas page→map link added; orphans removed; phase 133 VALIDATION added"
status: passed
scores:
  requirements: 20/20
  phases: 5/5
  integration: 18/18 wired (0 missing, 0 orphaned — both cleaned)
  flows: 4/4 (page→map link added)
gaps:
  requirements: []   # none unsatisfied
  integration: []    # no missing connections
  flows: []          # taxon page→map link added (commit: View on atlas)
resolved_after_audit:
  - "flow taxon page→map: added 'View on atlas' link to genus/tribe/subfamily/subgenus pages (canonical ?taxon=<id>)"
  - "orphan src/lib/spa-link.ts: deleted (file + test + ARCH-04 block; stale url-state comment updated)"
  - "orphan _data/species.js buildTree/tree/TAXON_LEVELS: removed (IN-01)"
  - "phase 133 Nyquist: 133-VALIDATION.md added — nyquist_compliant: true (4/4 TREE)"
tech_debt:   # residual, accepted — deferred to backlog (not v4.6 scope gaps)
  - phase: earlier-phases
    items:
      - "Nyquist partial on 129/131/132 (VALIDATION.md present, nyquist_compliant false / wave_0 incomplete) — accepted; phases shipped + verified"
  - phase: 133-browse-tree
    items:
      - "No SECURITY.md (threats T-133-07/08/09 mitigated + verified in code; /gsd:secure-phase 133 to formalize)"
      - "WR-01/04/05 + IN-02/03 from 133-REVIEW.md (non-blocking polish)"
  - phase: backlog (todos captured during milestone)
    items:
      - "cluster-selection-visual-feedback (medium)"
      - "data-test-suite-environmental-deps (medium)"
      - "genus-page-subgenera-breakout (medium)"
      - "pluralization-sweep-web-copy (low)"
      - "table-rank-column (low)"
nyquist:
  compliant_phases: ["130", "133"]
  partial_phases: ["129", "131", "132"]
  missing_phases: []
  overall: partial (133 now compliant; earlier-phase partials accepted)
deferred_at_close:
  quick_tasks: 22   # legacy scanner cruft (empty-date dirs), pre-existing
  uat_gaps: 0       # the 1 reported is Phase 130 status=passed, 0 open scenarios (false positive)
---

# v4.6 Milestone Audit — Taxonomy Hierarchy & Normalization

**Phases 129–133** | Audited 2026-06-03 | **Status: passed** (audit-flagged gaps closed; residual items are accepted backlog)

> **Gap closure (2026-06-03):** the three gaps the operator chose to address after the
> initial `tech_debt` verdict are resolved — (1) the taxon page→filtered-map E2E warning
> (added "View on atlas" links), (2) both orphaned exports (`spa-link.ts`, `species.js`
> flat tree) removed, and (3) phase 133's missing Nyquist coverage (`133-VALIDATION.md`
> added, compliant). Status upgraded `tech_debt → passed`. Residual tech debt
> (earlier-phase partial Nyquist, 133 SECURITY.md, 5 backlog todos, 22 legacy quick-task
> dirs) is accepted and deferred — none are v4.6 scope gaps.

## Definition of Done
Replace denormalized rank columns with a taxon_id hierarchy; descendant-by-any-rank
map filtering; an expandable browse tree; subfamily/taxon pages. **All met.**

## Requirements Coverage — 20/20 satisfied

3-source cross-reference (REQUIREMENTS.md traceability + phase VERIFICATION.md +
SUMMARY.md frontmatter) agrees for every requirement. No orphans, no unsatisfied.

| Group | IDs | Phase | Verification | Integration |
|-------|-----|-------|--------------|-------------|
| HIER | 01–06 | 129 | complete | wired (lineage_path build + zero-orphan assert) |
| MFILT | 01–03 | 130 | passed | wired (descendant `instr(lineage_path,…)` filter; 8-rank cache; legacy URL decode) |
| NORM | 01–03 | 131 | passed | wired (33-col contract; geo_blob[6]=source matches features.ts; display_name via JOIN) |
| PAGE | 01–04 | 132 | passed | wired (subfamily/tribe/subgenus pages built; slug-collision hard-fail) |
| TREE | 01–04 | 133 | passed | wired (tree → pages + 🗺 map links; gap-closure fixed default view) |

## Cross-Phase Integration — 18 wired, 0 missing

1. **Hierarchy → filter (129→130):** `src/filter.ts` emits `instr(lineage_path, '/${taxonId}/')`; data-verified (Halictinae 11,977; Eucerini 3,134), 2.0 ms on Apidae scope. ✓
2. **Normalization → frontend (131→130/132/133):** geo_blob 7-field layout `sqlite_export.py:_GEO_COLS[6]=source` ↔ `features.ts:row[6]=source` — **verified safe** (the silent-wrong-data risk surface). 4 dropped columns absent from all production reads; `display_name` computed via taxa JOIN. ✓
3. **Browse tree → pages + map (133→132/130):** all node links resolve to built pages; 🗺 links use the Phase 130 `?taxon=…&taxonRank=…` contract, decoded by `_resolveLegacyTaxon` across 8 ranks. ✓
4. **Build integrity:** `dbt build` PASS=61/ERROR=0; 591 JS tests pass; `tsc --noEmit` clean; 33-col occurrences contract enforced. ✓

## Findings (all non-blocking)

**WARNING — E2E "taxon page → filtered map":** genus/tribe/subfamily/subgenus pages
link out to iNaturalist but have no "View on atlas" map link. The filtered-map view is
reachable via the browse tree's 🗺 affordance instead. Pre-existing (predates v4.6);
PAGE-01..04 and TREE-04 are satisfied as written. Candidate backlog item if a
page→map link is desired.

**Orphaned exports:** `src/lib/spa-link.ts` (stale — unused, narrow rank type, doc
invariant in url-state.ts violated) and `_data/species.js` legacy `buildTree`/`tree`
export (Phase 96 leftover). Both harmless.

**Nyquist coverage — partial:** 130 compliant; 129/131/132 partial; **133 has no
VALIDATION.md**. Discovery only — run `/gsd:validate-phase {N}` per phase if formal
Nyquist coverage is wanted (not required to ship).

**Security:** no `*-SECURITY.md` for phase 133 (threats T-133-07/08/09 mitigated in
code + verified). `/gsd:secure-phase 133` to formalize.

## Verdict
All requirements met, all cross-phase wiring present, no broken flows. The milestone is
**shippable**. The tech-debt items above are deferred work to track, not blockers.
