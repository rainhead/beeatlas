---
phase: 112
plan: 02
type: execute
wave: 2
depends_on:
  - 112-01
files_modified:
  - src/url-state.ts
  - src/manifest.ts
  - scripts/make-local-manifest.js
autonomous: true
requirements:
  - MAP-04
must_haves:
  truths:
    - "Calling buildParams(..., { ..., checklistVisible: true }) produces a URLSearchParams with cl=1"
    - "Calling parseParams('cl=1') returns a result with result.ui.checklistVisible === true"
    - "The default `defaultUi = { boundaryMode: 'off', paneState: 'collapsed' }` still type-checks against the extended UiState (checklistVisible is OPTIONAL)"
    - "resolveDataUrl('checklist') is a valid call (TypeScript accepts 'checklist' as a DataKey)"
    - "Local dev manifest at public/data/manifest.json (regenerated by scripts/make-local-manifest.js) contains a checklist key whose value is 'checklist.parquet'"
    - "All MAP-04 tests in src/tests/url-state.test.ts pass GREEN; no other test in the suite regresses"
  artifacts:
    - path: "src/url-state.ts"
      provides: "UiState.checklistVisible field, cl= encode/decode"
      contains: "checklistVisible"
    - path: "src/manifest.ts"
      provides: "checklist key on Manifest interface"
      contains: "checklist:"
    - path: "scripts/make-local-manifest.js"
      provides: "checklist key in local dev manifest output"
      contains: "checklist:"
  key_links:
    - from: "src/url-state.ts buildParams"
      to: "src/bee-atlas.ts _buildCurrentParams() (consumer in Plan 03)"
      via: "UiState argument"
      pattern: "checklistVisible"
    - from: "src/manifest.ts DataKey"
      to: "src/bee-map.ts _loadChecklistData() (consumer in Plan 03)"
      via: "resolveDataUrl('checklist') call"
      pattern: "resolveDataUrl\\(['\"]checklist['\"]"
---

<objective>
Wire the data-layer plumbing that Plan 03 will consume: extend `UiState` with an optional `checklistVisible` flag and the `cl=1` URL param round-trip in `src/url-state.ts`; add `checklist` to the `Manifest` interface in `src/manifest.ts` (which makes `'checklist'` a valid `DataKey`); add the `checklist` key to the local dev manifest generator `scripts/make-local-manifest.js`. This plan exists to keep Plan 03 focused on the UI/map work without entangling the URL+manifest typing.

Purpose: `cl=1` URL persistence and `resolveDataUrl('checklist')` typing must be in place before the bee-map component code in Plan 03 can compile and pass tests.
Output: Three files modified; MAP-04 url-state tests turn GREEN; `resolveDataUrl('checklist')` typechecks; regenerated local manifest contains the checklist key.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/112-checklist-map-layer/112-RESEARCH.md
@.planning/phases/112-checklist-map-layer/112-PATTERNS.md
@.planning/phases/112-checklist-map-layer/112-01-PLAN.md

<interfaces>
<!-- Current UiState shape (src/url-state.ts) — Plan 02 extends this. -->

From src/url-state.ts:
```typescript
export interface UiState {
  boundaryMode: 'off' | 'counties' | 'ecoregions' | 'places';
  paneState: 'list' | 'table' | 'collapsed';
  // NEW (this plan): checklistVisible?: boolean
}
export function buildParams(view, filter, selection, ui: UiState): URLSearchParams;
export function parseParams(query: string): { view, filter, selection, ui?: UiState };
```

From src/manifest.ts (lines 3-24):
```typescript
interface Manifest {
  occurrences: string;
  species: string;
  seasonality: string;
  counties: string;
  ecoregions: string;
  places: string;
  places_meta: string;
  // NEW (this plan): checklist: string;
  generated_at: string;
}
type DataKey = keyof Omit<Manifest, 'generated_at'>;
export async function resolveDataUrl(key: DataKey): Promise<string | null>;
```

From scripts/make-local-manifest.js (lines 11-20): JSON.stringify of an object with 7 data keys plus generated_at; Plan 02 adds an 8th key `checklist: 'checklist.parquet'`.

From .planning/phases/111-checklist-pipeline/111-VERIFICATION.md: confirms `public/data/checklist.parquet` exists locally (32,768 bytes, 2861 rows, 39 distinct counties matching counties.geojson NAME values exactly).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend UiState with optional checklistVisible and cl=1 round-trip in src/url-state.ts</name>
  <files>src/url-state.ts</files>
  <read_first>
    - src/url-state.ts (full file — UiState interface, buildParams body, parseParams body; specifically the boundaryMode / paneState encode/decode lines that are the exact analog)
    - src/tests/url-state.test.ts (the MAP-04 describe block added by Plan 01 — these tests define the contract this task must satisfy GREEN)
    - .planning/phases/112-checklist-map-layer/112-PATTERNS.md (lines 369-414 for the precise diff: interface field, buildParams insertion, parseParams insertion)
  </read_first>
  <behavior>
    - UiState interface gains an optional field `checklistVisible?: boolean`.
    - In buildParams, after the paneState line `if (ui.paneState !== 'collapsed') params.set('pane', ui.paneState)`, insert `if (ui.checklistVisible) params.set('cl', '1')`. When checklistVisible is false or undefined, no cl param is set.
    - In parseParams, in the UI state assembly block (the block where boundaryMode and paneState are read and assigned to result.ui), parse `const checklistVisible = p.get('cl') === '1'`. Extend the conditional that decides whether to set result.ui so it also fires when checklistVisible is true: `if (boundaryMode !== 'off' || paneState !== 'collapsed' || checklistVisible) { result.ui = { boundaryMode, paneState, checklistVisible }; }`. When result.ui is otherwise emitted (e.g. because boundaryMode is non-default), include checklistVisible in the emitted object.
    - Default behavior: no cl param in URL ⇒ checklistVisible is false or absent in the parsed UiState.
    - Round-trip: parseParams(buildParams(..., { boundaryMode: 'off', paneState: 'collapsed', checklistVisible: true }).toString()).ui?.checklistVisible is true.
  </behavior>
  <action>
    Edit `src/url-state.ts`. Make THREE edits:

    1. In the UiState interface, add a new line `checklistVisible?: boolean;` immediately after the `paneState` field. The field MUST be optional (`?`) — this avoids breaking the dozens of existing callers/tests that construct UI objects without it.

    2. In buildParams, locate the line `if (ui.paneState !== 'collapsed') params.set('pane', ui.paneState);` (around line 73 per PATTERNS.md) and insert immediately after it: `if (ui.checklistVisible) params.set('cl', '1');`. The string param name is `cl` and value is the literal string `'1'`.

    3. In parseParams, locate the UI block (around lines 218-235 per PATTERNS.md) where boundaryMode and paneState are computed and conditionally assigned to result.ui. Add `const checklistVisible = p.get('cl') === '1';` before the conditional. Extend the conditional from `if (boundaryMode !== 'off' || paneState !== 'collapsed')` to `if (boundaryMode !== 'off' || paneState !== 'collapsed' || checklistVisible)`. Extend the object literal from `{ boundaryMode, paneState }` to `{ boundaryMode, paneState, checklistVisible }`. The new field is emitted unconditionally inside the object (consumers read with `?.`).

    Do NOT change the function signatures. Do NOT remove or reorder existing params.

    If Plan 01 added any `@ts-expect-error` comments in `src/tests/url-state.test.ts`, REMOVE them now — they will become unused once UiState includes checklistVisible.
  </action>
  <verify>
    <automated>cd /Users/rainhead/dev/beeatlas &amp;&amp; npm test -- src/tests/url-state.test.ts 2>&amp;1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "checklistVisible?: boolean" src/url-state.ts` returns 1 (the new optional field).
    - `grep -c "params.set('cl', '1')" src/url-state.ts` returns 1 (the encode line).
    - `grep -c "p.get('cl') === '1'" src/url-state.ts` returns 1 (the decode line).
    - All 6 MAP-04 tests in `src/tests/url-state.test.ts` PASS (GREEN).
    - All pre-existing url-state tests continue to PASS — no regression in this file.
    - `npm run typecheck` exits 0 (no TypeScript errors introduced; no leftover unused `@ts-expect-error`).
  </acceptance_criteria>
  <done>UiState extended; buildParams writes cl=1; parseParams reads it; MAP-04 tests green; no regressions; typecheck clean.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add checklist key to Manifest interface (src/manifest.ts) and local dev generator (scripts/make-local-manifest.js)</name>
  <files>src/manifest.ts, scripts/make-local-manifest.js</files>
  <read_first>
    - src/manifest.ts (full file — Manifest interface and DataKey type at lines 3-24; resolveDataUrl function below)
    - scripts/make-local-manifest.js (full file — the JSON.stringify({...}) call where existing data keys are listed)
    - public/data/checklist.parquet (confirm file exists locally — `ls -la public/data/checklist.parquet` should show 32768 bytes per Phase 111 VERIFICATION.md)
    - .planning/phases/112-checklist-map-layer/112-PATTERNS.md (lines 418-468 for the precise manifest interface and script edits)
  </read_first>
  <behavior>
    - `src/manifest.ts` Manifest interface gains a new field `checklist: string;` placed before `generated_at: string;`. This is REQUIRED (not optional) because the production manifest already includes the key (Phase 111 wired it via nightly.sh) and the local manifest generator must produce it (next sub-task).
    - Because `DataKey = keyof Omit<Manifest, 'generated_at'>`, adding `checklist` automatically makes `'checklist'` a valid argument to `resolveDataUrl`. No additional change to DataKey or resolveDataUrl is required.
    - `scripts/make-local-manifest.js`: the object literal passed to `JSON.stringify(...)` gains a new key `checklist: 'checklist.parquet',` placed before `generated_at: 'local',`.
    - Re-running `node scripts/make-local-manifest.js` overwrites `public/data/manifest.json` with the 8-key object including `"checklist": "checklist.parquet"`.
  </behavior>
  <action>
    Edit `src/manifest.ts`: locate the Manifest interface. Insert a new line `checklist: string;` immediately before the `generated_at: string;` line. Do NOT change DataKey (it is derived).

    Edit `scripts/make-local-manifest.js`: locate the `JSON.stringify({ ... })` object literal. Insert a new line `checklist: 'checklist.parquet',` immediately before the `generated_at: 'local',` line. Preserve the surrounding indentation and trailing-comma style of adjacent lines.

    After both edits, regenerate the local manifest by running `node scripts/make-local-manifest.js`. Verify `public/data/manifest.json` now contains `"checklist": "checklist.parquet"`.
  </action>
  <verify>
    <automated>cd /Users/rainhead/dev/beeatlas &amp;&amp; node scripts/make-local-manifest.js &amp;&amp; node -e "const m=require('./public/data/manifest.json'); if(m.checklist!=='checklist.parquet'){console.error('FAIL:',m.checklist);process.exit(1)} console.log('OK checklist=',m.checklist)" &amp;&amp; npm run typecheck 2>&amp;1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "checklist: string;" src/manifest.ts` returns 1 (the new field).
    - `grep -c "checklist: 'checklist.parquet'" scripts/make-local-manifest.js` returns 1 (the new key).
    - `public/data/manifest.json` contains `"checklist": "checklist.parquet"` after running the generator (verified by the node -e check in the verify block — exit 0 = pass).
    - `npm run typecheck` exits 0; specifically, no error of the form `Argument of type '"checklist"' is not assignable to parameter of type 'DataKey'` would now occur if `resolveDataUrl('checklist')` were called.
    - `npm test` overall exit status is unchanged from the post-Plan-01 baseline EXCEPT that the 6 MAP-04 tests are now GREEN; no other tests regress.
  </acceptance_criteria>
  <done>Manifest interface and local manifest generator both carry the checklist key; production manifest already had it (Phase 111); typecheck clean; resolveDataUrl('checklist') is callable from Plan 03 code.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → CloudFront CDN | Existing — checklist.parquet served from same origin as occurrences.parquet (Phase 111 already wired the upload). No new trust boundary added by this plan. |
| URL query string → parseParams | Existing — Plan 02 adds one new param (`cl`) parsed via strict equality check `p.get('cl') === '1'`. No path that accepts arbitrary string content as code or HTML. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-112-02-SC | Tampering | npm/yarn/pnpm install | accept | No new packages installed; URL/manifest changes use only built-in browser/Node APIs. |
| T-112-02-01 | Tampering | URL `cl` param | mitigate | parseParams uses strict `=== '1'` comparison — any other value (including SQL/HTML injection attempts) coerces to `false`. No string interpolation into DOM or SQL from this param. |
| T-112-02-02 | Information Disclosure | Manifest exposes checklist.parquet URL | accept | checklist.parquet is intentionally public data (Bartholomew et al. 2024 published dataset); same posture as occurrences.parquet. |
</threat_model>

<verification>
- `npm test` runs: all Plan 01 MAP-04 tests turn GREEN; all other pre-existing tests still pass; Plan 01 MAP-01/02/03 tests remain RED (they belong to Plan 03's scope).
- `npm run typecheck` exits 0.
- `public/data/manifest.json` regenerated and contains `"checklist": "checklist.parquet"`.
- `git diff --stat src/url-state.ts src/manifest.ts scripts/make-local-manifest.js` shows three modified files; no other source file changed.
</verification>

<success_criteria>
- UiState carries `checklistVisible?: boolean`; URL `cl=1` encodes and decodes correctly.
- Manifest interface carries `checklist: string`; `resolveDataUrl('checklist')` typechecks and resolves to `'checklist.parquet'` (or hashed equivalent) in both local dev and production.
- MAP-04 gates are GREEN.
- No regression in pre-existing tests.
- Plan 03 can now begin without typing or URL-plumbing blockers.
</success_criteria>

<output>
Create `.planning/phases/112-checklist-map-layer/112-02-SUMMARY.md` when done with: files modified, exact diffs (3 lines in url-state.ts, 1 line each in manifest.ts and make-local-manifest.js), MAP-04 test count green/total, typecheck status, and a note that Plan 03 can now consume `resolveDataUrl('checklist')` and `UiState.checklistVisible`.
</output>
