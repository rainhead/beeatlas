---
phase: 113-species-page-expansion
plan: 05
type: execute
wave: 3
depends_on: [113-02, 113-03, 113-04]
files_modified:
  - _pages/species.njk
  - _pages/species-detail.njk
  - _pages/genus.njk
  - _pages/subgenus.njk
autonomous: false
requirements: [SPEC-01, SPEC-02, SPEC-03, SPEC-04, SPEC-05]

must_haves:
  truths:
    - "Species index shows 'checklist only' badge for checklist-only species; '0 records' label is reserved for species with neither WABA nor checklist data"
    - "Species detail page renders the augmented SVG when occurrence_count > 0 OR on_checklist is true"
    - "Species detail page wires el.onChecklist = sp.on_checklist on the seasonality-viz custom element"
    - "Species detail page shows attribution line `N checklist records · Bartholomew et al. 2024` only when on_checklist is true"
    - "Species detail page hides the `View N occurrences on the atlas` link when occurrence_count === 0"
    - "Genus and subgenus pages show `N checklist records` count for checklist-only species instead of `0 records`"
    - "Plan 01 build-output tests turn green when run with VITEST_SKIP_BUILD=0"
  artifacts:
    - path: "_pages/species.njk"
      provides: "checklist-only badge in species index"
      contains: "checklist only"
    - path: "_pages/species-detail.njk"
      provides: "SVG conditional, attribution line, atlas-link conditional, onChecklist wiring"
      contains: "Bartholomew et al. 2024"
    - path: "_pages/genus.njk"
      provides: "checklist record count display for checklist-only species"
      contains: "checklist records"
    - path: "_pages/subgenus.njk"
      provides: "checklist record count display for checklist-only species"
      contains: "checklist records"
  key_links:
    - from: "species-detail.njk"
      to: "https://jhr.pensoft.net/article/129013/"
      via: "anchor href"
      pattern: "jhr\\.pensoft\\.net/article/129013"
    - from: "species-detail.njk seasonality-viz script"
      to: "sp.on_checklist"
      via: "el.onChecklist assignment"
      pattern: "el\\.onChecklist\\s*="
---

<objective>
Land the four Nunjucks template changes that surface all the data-layer work from Plans 02-04: checklist-only badge in the species index, augmented SVG visibility on checklist-only species pages, citation attribution line, atlas-link suppression for zero-occurrence species, seasonality-viz onChecklist wiring, and checklist record count display on genus/subgenus pages. After this plan: all five SPEC requirements are satisfied; Plan 01 build-output tests run green with `VITEST_SKIP_BUILD=0`; a human checkpoint verifies visual presentation.

Purpose: D-01, D-06, D-08, D-09, D-13, D-14, D-15. The data is now in place (species.json has checklist_count, SVGs exist for checklist species, JS data layer surfaces them in lists, seasonality-viz handles the onChecklist flag) — templates render that data.

Output: 565 species pages, including checklist-only species pages with augmented SVG + attribution + no atlas link; species index with checklist-only badge; genus/subgenus pages listing checklist-only species with checklist record counts.

Per `feedback_uat_ui_phases.md` (this phase has `UI hint: yes` in ROADMAP), the human-verify checkpoint at the end is mandatory and not auto-advanceable.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/113-species-page-expansion/113-CONTEXT.md
@.planning/phases/113-species-page-expansion/113-RESEARCH.md
@.planning/phases/113-species-page-expansion/113-PATTERNS.md
@_pages/species.njk
@_pages/species-detail.njk
@_pages/genus.njk
@_pages/subgenus.njk
@src/tests/build-output.test.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Update species.njk, genus.njk, subgenus.njk count slots for checklist species</name>
  <files>_pages/species.njk, _pages/genus.njk, _pages/subgenus.njk</files>
  <read_first>
    - _pages/species.njk (whole file — current count slot around line 23 emitting `{{ sp.occurrence_count }} records`)
    - _pages/genus.njk (whole file — current species list loop with count slot)
    - _pages/subgenus.njk (whole file — current species list loop, structurally identical to genus.njk)
    - .planning/phases/113-species-page-expansion/113-PATTERNS.md (sections "_pages/species.njk", "_pages/genus.njk", "_pages/subgenus.njk" — exact Nunjucks conditional block to use, including whitespace control `{%- ... -%}`)
    - .planning/phases/113-species-page-expansion/113-CONTEXT.md (D-14 "checklist only" badge, D-01 "N checklist records" for genus/subgenus)
  </read_first>
  <behavior>
    - In _pages/species.njk, the count slot becomes a three-branch conditional: `occurrence_count > 0` -> `{{ sp.occurrence_count }} records`; else if `sp.on_checklist` -> `<span class="count checklist-badge">checklist only</span>`; else -> `0 records`.
    - In _pages/genus.njk, the count slot inside the species list loop becomes: `occurrence_count > 0` -> `{{ sp.occurrence_count }} records`; else if `sp.on_checklist` -> `{{ sp.checklist_count }} checklist records`; else -> `0 records`.
    - In _pages/subgenus.njk, the identical conditional from genus.njk applies.
    - The unresolved `Genus sp.` entry (which has neither slug nor on_checklist) continues to fall into the final `0 records` branch.
    - Whitespace control (`{%- ... -%}`) preserves the existing inline rendering — no stray newlines around the `<span class="count">` element.
  </behavior>
  <action>
    In _pages/species.njk, locate the line `<span class="count">{{ sp.occurrence_count }} records</span>` (around line 23) and replace with a Nunjucks if/elif/else block: when `sp.occurrence_count > 0` emit `<span class="count">{{ sp.occurrence_count }} records</span>`; when `sp.on_checklist` emit `<span class="count checklist-badge">checklist only</span>`; else emit `<span class="count">0 records</span>`. Use `{%- if ... -%}` whitespace control to keep the markup inline. In _pages/genus.njk, locate the `<span class="count">{{ sp.occurrence_count }} records</span>` inside the `{%- for sp in genus.species -%}` loop and replace with the same conditional, but the on_checklist branch emits `<span class="count">{{ sp.checklist_count }} checklist records</span>` (no badge class — D-01 differs from D-14). In _pages/subgenus.njk, apply the identical change inside its species list loop. Do not modify any swatch, link, scientificName, or surrounding `<li>` markup. The `checklist-badge` CSS class is referenced only as a hook — no new CSS file is created in this phase; if styling is desired, it should be added in a follow-up quick task per CONTEXT.md (no styling decision was locked).
  </action>
  <verify>
    <automated>npm run build 2>&1 | tail -10 && grep -l "checklist only" _site/species/index.html && grep -l "checklist records" _site/species/Andrena/index.html 2>/dev/null || echo "andrena fallback may not be checklist-only"</automated>
  </verify>
  <acceptance_criteria>
    - Source: `grep -c "checklist only" _pages/species.njk` returns 1
    - Source: `grep -c "checklist-badge" _pages/species.njk` returns 1
    - Source: `grep -c "checklist records" _pages/genus.njk` returns 1
    - Source: `grep -c "checklist records" _pages/subgenus.njk` returns 1
    - Source: each of the three templates uses an `{%- if sp.occurrence_count > 0 -%}` opening conditional (verifiable via `grep -c "if sp.occurrence_count > 0" _pages/species.njk _pages/genus.njk _pages/subgenus.njk`)
    - Behavior: after `npm run build`, `_site/species/index.html` contains the substring `checklist only`
    - Behavior: at least one genus page under `_site/species/{Genus}/index.html` contains `checklist records`
  </acceptance_criteria>
  <done>
    Three templates updated; build succeeds; species index shows badge; genus/subgenus pages show checklist record counts.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Update species-detail.njk for SVG condition, attribution, atlas-link suppression, and onChecklist wiring</name>
  <files>_pages/species-detail.njk</files>
  <read_first>
    - _pages/species-detail.njk (whole file — current SVG image slot conditional, current `View N occurrences on the atlas` link, current seasonality-viz `<script>` tag wiring `el.data`, current metadata `<p>` line)
    - .planning/phases/113-species-page-expansion/113-PATTERNS.md (section "_pages/species-detail.njk" — concrete Nunjucks edits for SVG conditional, attribution line, atlas-link wrap, and seasonality-viz script extension)
    - .planning/phases/113-species-page-expansion/113-CONTEXT.md (D-06 SVG condition, D-08/D-09 attribution format and URL, D-13 seasonality-viz wiring, D-15 atlas link hide)
  </read_first>
  <behavior>
    - The SVG `<img>` is rendered when `sp.occurrence_count > 0 or sp.on_checklist` (not just `occurrence_count > 0`).
    - The seasonality-viz inline `<script>` now sets both `el.data` and `el.onChecklist` from `sp.month_histogram` and `sp.on_checklist`.
    - A new attribution `<p class="checklist-attribution">` line appears immediately after the existing metadata `<p>` and only when `sp.on_checklist` is truthy. Content: `{{ sp.checklist_count }} checklist records · <a href="https://jhr.pensoft.net/article/129013/">Bartholomew et al. 2024</a>`.
    - The existing `View N occurrences on the atlas` anchor is wrapped in `{%- if sp.occurrence_count > 0 -%}` and only renders when occurrences exist.
    - All other markup (heading, scientific name, distribution metadata text, photo manifest links if any) is unchanged.
  </behavior>
  <action>
    Edit _pages/species-detail.njk. (1) Locate the existing SVG image conditional (currently `{%- if sp.occurrence_count > 0 -%}` around line 24) and broaden it to `{%- if sp.occurrence_count > 0 or sp.on_checklist -%}`. (2) Locate the seasonality-viz `<script>` block (currently sets `el.data = {{ sp.month_histogram | dump | safe }}`) and extend it to also set `el.onChecklist = {{ sp.on_checklist | dump | safe }}` on the same element handle. Preserve the existing `customElements.whenDefined('seasonality-viz').then(...)` wrapper. (3) Immediately after the existing metadata `<p>` line (the one showing `N records · N counties · N ecoregions`), insert a new conditional block: `{%- if sp.on_checklist -%}<p class="checklist-attribution">{{ sp.checklist_count }} checklist records · <a href="https://jhr.pensoft.net/article/129013/">Bartholomew et al. 2024</a></p>{%- endif -%}`. (4) Locate the existing `<a href="/?taxon={{ sp.scientificName | urlencode }}&amp;taxonRank=species">View {{ sp.occurrence_count }} occurrences on the atlas →</a>` anchor and wrap it in `{%- if sp.occurrence_count > 0 -%}...{%- endif -%}`. Do not change any other lines. Verify by running `npm run build` and inspecting a checklist-only species page (find one with `jq '.[] | select(.occurrence_count == 0 and .on_checklist) | .slug' public/data/species.json | head -1`) — the rendered HTML must contain the attribution line, the SVG `<img>`, and must NOT contain the atlas link.
  </action>
  <verify>
    <automated>npm run build 2>&1 | tail -5 && SLUG=$(node -e "const d=require('./public/data/species.json'); const arr=Array.isArray(d)?d:Object.values(d); const s=arr.find(r=>r.occurrence_count===0 && r.on_checklist && r.slug); process.stdout.write(s?s.slug:'')") && echo "checklist-only slug: $SLUG" && [ -n "$SLUG" ] && HTML="_site/species/$SLUG/index.html" && test -f "$HTML" && grep -q "Bartholomew et al. 2024" "$HTML" && grep -q "/data/species-maps/" "$HTML" && ! grep -q "View .* occurrences on the atlas" "$HTML" && echo "PASS"</automated>
  </verify>
  <acceptance_criteria>
    - Source: `grep -c "or sp.on_checklist" _pages/species-detail.njk` returns at least 1 (SVG conditional)
    - Source: `grep -c "Bartholomew et al. 2024" _pages/species-detail.njk` returns 1
    - Source: `grep -c "jhr.pensoft.net/article/129013" _pages/species-detail.njk` returns 1
    - Source: `grep -c "checklist-attribution" _pages/species-detail.njk` returns 1
    - Source: `grep -c "el.onChecklist" _pages/species-detail.njk` returns 1
    - Source: the `View .* occurrences on the atlas` anchor is wrapped in an `{%- if sp.occurrence_count > 0 -%}` block — verify by reading the surrounding lines
    - Behavior: after `npm run build`, for a checklist-only species (occurrence_count=0, on_checklist=true) selected from species.json, the rendered HTML contains `Bartholomew et al. 2024` and `/data/species-maps/` and does NOT contain the `View N occurrences on the atlas` link
    - Behavior: for a WABA species (occurrence_count > 0), the atlas link continues to render
  </acceptance_criteria>
  <done>
    species-detail.njk renders augmented SVG for checklist species, shows attribution, hides atlas link when no occurrences, wires onChecklist to seasonality-viz.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Run full build + test suite to flip Plan 01 build-output tests green</name>
  <files>src/tests/build-output.test.ts</files>
  <read_first>
    - src/tests/build-output.test.ts (Plan 01 additions — confirm the placeholder slug constant `KNOWN_CHECKLIST_ONLY_SLUG` exists and update it to a verified slug from species.json before running the full suite)
    - public/data/species.json (use jq or node to find a stable checklist-only slug for the test)
    - .planning/phases/113-species-page-expansion/113-VALIDATION.md (full-suite command and expected runtime)
  </read_first>
  <behavior>
    - The placeholder `KNOWN_CHECKLIST_ONLY_SLUG` (or equivalent) in src/tests/build-output.test.ts is replaced with a real slug from a checklist-only species in species.json.
    - `VITEST_SKIP_BUILD=0 npm test` exits 0 with all Plan 01 build-output tests green: checklist-only species page exists, has attribution, lacks atlas link; species index has the "checklist only" badge.
    - The full pytest suite `cd data && uv run pytest` exits 0.
  </behavior>
  <action>
    Pick a stable checklist-only slug by running `node -e "const d=require('./public/data/species.json'); const arr=Array.isArray(d)?d:Object.values(d); const s=arr.find(r=>r.occurrence_count===0 && r.on_checklist && r.slug); console.log(s.slug)"`. Open src/tests/build-output.test.ts and replace the `KNOWN_CHECKLIST_ONLY_SLUG` placeholder with the literal slug string. Remove any TODO comment Plan 01 left behind. Run `VITEST_SKIP_BUILD=0 npm test` and confirm the two Plan 01 build-output tests for `checklist-only species page` and `species index checklist only badge` pass. Run `cd data && uv run pytest` and confirm zero failures. If either fails, do NOT auto-advance — surface the failure for human inspection.
  </action>
  <verify>
    <automated>VITEST_SKIP_BUILD=0 npm test 2>&1 | tail -20 && cd data && uv run pytest 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - Source: `grep -c "KNOWN_CHECKLIST_ONLY_SLUG\|TODO/replace" src/tests/build-output.test.ts` shows no `TODO/replace` placeholder remains (the constant value is a real slug string like `Andrena/something`)
    - Behavior: `VITEST_SKIP_BUILD=0 npm test` exits 0
    - Behavior: `cd data && uv run pytest` exits 0
    - Behavior: all five Plan 01 RED tests (speciesList count, genusList checklist-only, seasonality-viz onChecklist fallback, build-output checklist-only species page, build-output species index badge) are GREEN
  </acceptance_criteria>
  <done>
    Full test suite green; all Plan 01 RED gates flipped; build-output test slug updated to real data.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Human visual UAT of species page expansion</name>
  <what-built>
    - Species index `/species/` shows `checklist only` badge for species with occurrence_count=0 and on_checklist=true (D-14)
    - Checklist-only species pages exist at `/species/{Genus}/{epithet}/` and render the augmented SVG with light-blue county fills (#b0cfe8), no red occurrence dots (D-05, D-06)
    - WABA species with checklist records show the augmented SVG with BOTH light-blue county fills underneath AND red occurrence dots on top (D-04)
    - Species detail pages with `on_checklist=true` show a new line `N checklist records · Bartholomew et al. 2024` linking to https://jhr.pensoft.net/article/129013/ (D-08, D-09)
    - Checklist-only species pages do NOT show the `View N occurrences on the atlas →` link (D-15)
    - Genus and subgenus pages list checklist-only species with `N checklist records` count, swatch in neutral grey #cccccc (D-01)
    - For checklist-only species whose checklist months are all NULL, the seasonality-viz section shows `Monthly phenology not recorded` instead of `0 records` (D-13)
  </what-built>
  <how-to-verify>
    1. Run `npm run dev` and open http://localhost:8080/species/
    2. Find a species with the "checklist only" badge; click into it
    3. Confirm the SVG renders with light-blue county fills and NO red dots
    4. Confirm the line `N checklist records · Bartholomew et al. 2024` is present and the link goes to https://jhr.pensoft.net/article/129013/
    5. Confirm there is NO `View N occurrences on the atlas →` link
    6. If the species has all-NULL checklist months, confirm seasonality-viz shows `Monthly phenology not recorded`
    7. Navigate to the genus page (the breadcrumb or parent link); confirm the checklist-only species appears in the genus species list with a grey swatch and `N checklist records` count
    8. Navigate back to the species index and find a species WITH WABA records that also has on_checklist=true; click into it; confirm BOTH county fills AND red dots render on the same SVG, with dots on top of fills
    9. Confirm a normal WABA-only species (e.g., Agapostemon femoratus) still shows `View N occurrences on the atlas →` and no attribution line
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| species.json -> Nunjucks templates -> static HTML | Trusted build-time render |
| outbound link to jhr.pensoft.net | External destination — hard-coded, no user input |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-113-05-01 | Tampering | rendered HTML | accept | static HTML; no runtime input |
| T-113-05-02 | Information Disclosure | attribution link | accept | hard-coded outbound link to public publication URL |
| T-113-05-03 | Spoofing | citation link | mitigate | link is literal URL in source; no template injection because URL is hard-coded, not from data |
</threat_model>

<verification>
- `VITEST_SKIP_BUILD=0 npm test` exits 0 with all build-output assertions green
- `cd data && uv run pytest` exits 0
- Human checkpoint approved
</verification>

<success_criteria>
All five SPEC requirements satisfied; human UAT approves visual presentation of checklist data on species/genus/subgenus pages and species index.
</success_criteria>

<output>
Create `.planning/phases/113-species-page-expansion/113-05-SUMMARY.md` documenting: count of species pages generated, count showing attribution line, count of checklist-only species in genusList, sample slugs used for verification, and the human-approval signal.
</output>
