---
phase: 22-orchestration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - data/run.py
  - scripts/build-data.sh
  - package.json
autonomous: true
requirements: [ORCH-01, ORCH-02]

must_haves:
  truths:
    - "Running `cd data && uv run python run.py` executes geographies, ecdysis, inat, projects, export in that order"
    - "build-data.sh no longer exists"
    - "package.json build:data invokes the new Python runner"
    - "Each pipeline is independently runnable via `cd data && uv run python <pipeline>.py`"
  artifacts:
    - path: "data/run.py"
      provides: "Pipeline orchestration runner"
      contains: "from geographies_pipeline import load_geographies"
    - path: "package.json"
      provides: "Updated build:data script"
      contains: "cd data && uv run python run.py"
  key_links:
    - from: "data/run.py"
      to: "data/geographies_pipeline.py"
      via: "import load_geographies"
      pattern: "from geographies_pipeline import load_geographies"
    - from: "data/run.py"
      to: "data/export.py"
      via: "import main"
      pattern: "from export import main"
    - from: "package.json"
      to: "data/run.py"
      via: "build:data script"
      pattern: "cd data && uv run python run.py"
---

<objective>
Create data/run.py to replace scripts/build-data.sh as the single-command pipeline orchestrator. The runner imports and calls each pipeline's load function directly (no subprocess), sequences them in the correct order, and is invoked via `npm run build:data`.

Purpose: Consolidate pipeline orchestration into a Python script that provides proper tracebacks, shared memory, and a single uv process.
Output: data/run.py created, build-data.sh deleted, package.json updated.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/22-orchestration/22-CONTEXT.md

@data/geographies_pipeline.py
@data/ecdysis_pipeline.py
@data/inaturalist_pipeline.py
@data/projects_pipeline.py
@data/export.py
@scripts/build-data.sh
@package.json

<interfaces>
<!-- Callable functions the runner must import. Extracted from pipeline modules. -->

From data/geographies_pipeline.py:
```python
def load_geographies() -> None:  # line 132
```

From data/ecdysis_pipeline.py:
```python
def load_ecdysis() -> None:  # line 172
def load_links() -> None:  # line 182
# __main__ calls both: load_ecdysis() then load_links()
```

From data/inaturalist_pipeline.py:
```python
def load_observations(full_reload: bool = False) -> None:  # line 113
# __main__ calls: load_observations(full_reload="--full-reload" in sys.argv)
```

From data/projects_pipeline.py:
```python
def load_projects() -> None:  # line 63
```

From data/export.py:
```python
def main() -> None:  # line 268
# Exports all four frontend asset files from beeatlas.duckdb
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create data/run.py pipeline runner</name>
  <files>data/run.py</files>
  <read_first>
    data/geographies_pipeline.py
    data/ecdysis_pipeline.py
    data/inaturalist_pipeline.py
    data/projects_pipeline.py
    data/export.py
  </read_first>
  <action>
Create `data/run.py` with the following structure (per D-01, D-02, D-03):

1. Imports: Import the callable functions from each pipeline module:
   - `from geographies_pipeline import load_geographies`
   - `from ecdysis_pipeline import load_ecdysis, load_links`
   - `from inaturalist_pipeline import load_observations`
   - `from projects_pipeline import load_projects`
   - `from export import main as export_all`

2. Define a `STEPS` list of tuples: `(name: str, callable: Callable)` in this exact order (per D-03):
   - `("geographies", load_geographies)`
   - `("ecdysis", load_ecdysis)`
   - `("ecdysis-links", load_links)`
   - `("inaturalist", load_observations)`  (calls with no args, so full_reload=False default)
   - `("projects", load_projects)`
   - `("export", export_all)`

3. Define `def main() -> None:` that:
   - Prints a banner with the step name before each step (Claude's discretion on formatting — use simple `print(f"--- {name} ---")`)
   - Times each step with `time.monotonic()` and prints elapsed seconds after each step
   - Calls each step function in STEPS order
   - Fails fast on first error (no try/except around steps — let the exception propagate with full traceback, per Claude's discretion favoring fail-fast)
   - Prints total elapsed time at the end

4. Add `if __name__ == "__main__": main()` at the bottom (per D-04, D-05 — this also makes run.py independently runnable).

Do NOT use subprocess. Do NOT include anti_entropy_pipeline (per D-03). Do NOT add a `--step` flag (per deferred items).
  </action>
  <verify>
    <automated>cd /Users/rainhead/dev/beeatlas/data && python -c "import run; print([s[0] for s in run.STEPS])" 2>&1 | grep -q "geographies.*ecdysis.*ecdysis-links.*inaturalist.*projects.*export"</automated>
  </verify>
  <acceptance_criteria>
    - File data/run.py exists
    - Contains `from geographies_pipeline import load_geographies`
    - Contains `from ecdysis_pipeline import load_ecdysis, load_links`
    - Contains `from inaturalist_pipeline import load_observations`
    - Contains `from projects_pipeline import load_projects`
    - Contains `from export import main as export_all`
    - STEPS list has exactly 6 entries in order: geographies, ecdysis, ecdysis-links, inaturalist, projects, export
    - Contains `def main() -> None:`
    - Contains `if __name__ == "__main__":`
    - Does NOT contain `subprocess`
    - Does NOT contain `anti_entropy`
  </acceptance_criteria>
  <done>data/run.py imports all five pipeline load functions plus export, sequences them in the correct order, and is importable without error</done>
</task>

<task type="auto">
  <name>Task 2: Delete build-data.sh and update package.json</name>
  <files>scripts/build-data.sh, package.json</files>
  <read_first>
    scripts/build-data.sh
    package.json
  </read_first>
  <action>
1. Delete `scripts/build-data.sh` (per D-06): `git rm scripts/build-data.sh`

2. Edit `package.json` (per D-06): Change line 18 from:
   `"build:data": "bash scripts/build-data.sh",`
   to:
   `"build:data": "cd data && uv run python run.py",`

   Leave the `"build"` script on line 19 unchanged — it already chains `npm run build:data && npm run build --workspace=frontend` which is correct.

   Leave all other scripts untouched (per D-06 — old npm scripts like `fetch-inat`, `fetch-links`, `cache-restore-links` are Phase 24 cleanup).
  </action>
  <verify>
    <automated>cd /Users/rainhead/dev/beeatlas && ! test -f scripts/build-data.sh && grep -q '"build:data": "cd data && uv run python run.py"' package.json && echo "PASS"</automated>
  </verify>
  <acceptance_criteria>
    - scripts/build-data.sh does not exist
    - package.json contains exactly: `"build:data": "cd data && uv run python run.py"`
    - package.json `"build"` script still contains `npm run build:data && npm run build --workspace=frontend`
    - package.json still contains `fetch-inat`, `cache-restore-links`, `fetch-links` scripts (untouched per D-06)
  </acceptance_criteria>
  <done>build-data.sh is deleted, package.json build:data points to the new Python runner, all other scripts unchanged</done>
</task>

</tasks>

<verification>
1. `cd /Users/rainhead/dev/beeatlas/data && python -c "import run; print('import OK')"` — verifies run.py is importable
2. `! test -f /Users/rainhead/dev/beeatlas/scripts/build-data.sh` — verifies old script deleted
3. `grep 'uv run python run.py' /Users/rainhead/dev/beeatlas/package.json` — verifies package.json updated
4. ORCH-02 verification: `cd /Users/rainhead/dev/beeatlas/data && python -c "import geographies_pipeline; import ecdysis_pipeline; import inaturalist_pipeline; import projects_pipeline; import export; print('all importable')"` — each pipeline remains independently importable
</verification>

<success_criteria>
- data/run.py exists and imports without error
- STEPS list sequences: geographies -> ecdysis -> ecdysis-links -> inaturalist -> projects -> export
- scripts/build-data.sh is deleted
- package.json build:data is `cd data && uv run python run.py`
- Each pipeline .py file retains its `if __name__ == "__main__"` block for isolated execution
</success_criteria>

<output>
After completion, create `.planning/phases/22-orchestration/22-01-SUMMARY.md`
</output>
