---
phase: 20-pipeline-migration
plan: 02
type: execute
wave: 2
depends_on: ["20-01"]
files_modified: []
autonomous: false
requirements: [PIPE-10]

must_haves:
  truths:
    - "inat pipeline runs and writes rows to inaturalist_data.observations"
    - "ecdysis pipeline runs and writes rows to ecdysis_data.occurrences"
    - "geographies pipeline runs and writes rows to geographies.ecoregions"
    - "projects pipeline runs and writes rows to inaturalist_data.projects"
    - "anti-entropy pipeline runs without error"
  artifacts:
    - path: "data/beeatlas.duckdb"
      provides: "DuckDB database with all pipeline data"
  key_links:
    - from: "data/inaturalist_pipeline.py"
      to: "data/beeatlas.duckdb"
      via: "dlt.destinations.duckdb('beeatlas.duckdb')"
      pattern: "duckdb.*beeatlas"
    - from: "data/.dlt/config.toml"
      to: "data/inaturalist_pipeline.py"
      via: "dlt config injection (project_id)"
      pattern: "project_id"
---

<objective>
Run all five dlt pipelines to verify they write data to DuckDB without error, satisfying PIPE-10.

Purpose: Proves the ported pipeline files actually work end-to-end with live data sources, not just that they exist and import.
Output: A populated data/beeatlas.duckdb with rows from all five pipelines.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/20-pipeline-migration/20-RESEARCH.md
@.planning/phases/20-pipeline-migration/20-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run all five pipelines in dependency order</name>
  <files>data/beeatlas.duckdb</files>
  <read_first>
    data/.dlt/config.toml,
    .planning/phases/20-pipeline-migration/20-RESEARCH.md
  </read_first>
  <action>
    Run all five pipelines from data/ directory in the correct dependency order. Each pipeline must complete without error.

    Execution order (from RESEARCH.md):
    1. `cd data && uv run python geographies_pipeline.py` — no data deps; downloads shapefiles (~8-9 min first run); writes to geographies.ecoregions, geographies.us_states, geographies.us_counties, geographies.ca_provinces, geographies.ca_census_divisions
    2. `cd data && uv run python inaturalist_pipeline.py` — no data deps; fetches from iNat v2 API; writes to inaturalist_data.observations
    3. `cd data && uv run python ecdysis_pipeline.py` — no data deps; fetches from ecdysis.org; writes to ecdysis_data.occurrences, ecdysis_data.occurrence_links, etc.
    4. `cd data && uv run python projects_pipeline.py` — depends on inat (reads observations__observation_projects); writes to inaturalist_data.projects
    5. `cd data && uv run python anti_entropy_pipeline.py` — depends on inat (reads inaturalist_data.observations); samples and checks for deletions

    Steps 1-3 can run in any order relative to each other. Steps 4-5 must run after step 2.

    After all pipelines complete, verify row counts:
    ```bash
    cd data
    uv run duckdb beeatlas.duckdb "SELECT 'observations' as tbl, COUNT(*) as n FROM inaturalist_data.observations UNION ALL SELECT 'occurrences', COUNT(*) FROM ecdysis_data.occurrences UNION ALL SELECT 'ecoregions', COUNT(*) FROM geographies.ecoregions UNION ALL SELECT 'projects', COUNT(*) FROM inaturalist_data.projects"
    ```

    All row counts must be > 0.

    NOTE: This task requires live internet access to iNat API, ecdysis.org, and geography shapefile hosts. The geographies pipeline may take 8-9 minutes on first run due to shapefile downloads. Total time for all five: expect 10-30+ minutes.
  </action>
  <verify>
    <automated>cd /Users/rainhead/dev/beeatlas/data && uv run duckdb beeatlas.duckdb "SELECT 'observations' as tbl, COUNT(*) as n FROM inaturalist_data.observations UNION ALL SELECT 'occurrences', COUNT(*) FROM ecdysis_data.occurrences UNION ALL SELECT 'ecoregions', COUNT(*) FROM geographies.ecoregions UNION ALL SELECT 'projects', COUNT(*) FROM inaturalist_data.projects"</automated>
  </verify>
  <acceptance_criteria>
    - data/beeatlas.duckdb exists and is > 0 bytes
    - Query `SELECT COUNT(*) FROM inaturalist_data.observations` returns > 0
    - Query `SELECT COUNT(*) FROM ecdysis_data.occurrences` returns > 0
    - Query `SELECT COUNT(*) FROM geographies.ecoregions` returns > 0
    - Query `SELECT COUNT(*) FROM inaturalist_data.projects` returns > 0
    - anti_entropy_pipeline.py exited with code 0
  </acceptance_criteria>
  <done>All five pipelines ran successfully; DuckDB contains rows from each pipeline</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify pipeline outputs</name>
  <what-built>All five dlt pipelines ported from prototype and run against live data sources. DuckDB database at data/beeatlas.duckdb now contains observation, specimen, geographic, and project data.</what-built>
  <how-to-verify>
    1. Check the pipeline run output above — all five should have completed without errors
    2. Review the row count query output — all tables should have > 0 rows
    3. Optionally inspect a few rows: `cd data && uv run duckdb beeatlas.duckdb "SELECT id, taxon__name FROM inaturalist_data.observations LIMIT 5"`
    4. Confirm beeatlas.duckdb is NOT tracked by git: `git status data/beeatlas.duckdb` should show nothing (ignored)
  </how-to-verify>
  <resume-signal>Type "approved" if all pipelines ran correctly, or describe any issues</resume-signal>
</task>

</tasks>

<verification>
All five pipelines write rows to data/beeatlas.duckdb:
1. inaturalist_data.observations has > 0 rows
2. ecdysis_data.occurrences has > 0 rows
3. geographies.ecoregions has > 0 rows
4. inaturalist_data.projects has > 0 rows
5. anti_entropy_pipeline.py exited cleanly
</verification>

<success_criteria>
- All five pipelines run without error from data/ directory
- DuckDB query shows non-zero row counts for all four main tables
- beeatlas.duckdb is gitignored (not tracked)
</success_criteria>

<output>
After completion, create `.planning/phases/20-pipeline-migration/20-02-SUMMARY.md`
</output>
