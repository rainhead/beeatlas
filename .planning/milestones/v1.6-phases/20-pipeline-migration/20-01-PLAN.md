---
phase: 20-pipeline-migration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - data/inaturalist_pipeline.py
  - data/ecdysis_pipeline.py
  - data/geographies_pipeline.py
  - data/projects_pipeline.py
  - data/anti_entropy_pipeline.py
  - data/.dlt/config.toml
  - data/pyproject.toml
  - data/uv.lock
  - data/.gitignore
  - data/README.md
  - data/Makefile
  - data/ecdysis/
  - data/inat/
  - data/links/
  - data/scripts/
  - data/spatial.py
  - data/tests/
  - .planning/PROJECT.md
  - .planning/todos/pending/2026-03-12-specify-explicit-fields-on-inat-api-calls.md
autonomous: true
requirements: [PIPE-08, PIPE-09]

must_haves:
  truths:
    - "Five dlt pipeline .py files exist in data/ and are importable"
    - "Old pipeline modules (ecdysis/, inat/, links/, scripts/, spatial.py, tests/) are gone from data/"
    - ".dlt/config.toml has all four config keys with correct values including relative html_cache_dir"
    - "pyproject.toml has exactly the prototype's five deps and no old deps"
    - "uv sync succeeds and all pipeline imports resolve"
  artifacts:
    - path: "data/inaturalist_pipeline.py"
      provides: "iNat observation pipeline"
    - path: "data/ecdysis_pipeline.py"
      provides: "Ecdysis specimen + links pipeline"
    - path: "data/geographies_pipeline.py"
      provides: "Geographic boundary pipeline"
    - path: "data/projects_pipeline.py"
      provides: "iNat projects pipeline"
    - path: "data/anti_entropy_pipeline.py"
      provides: "Anti-entropy soft-delete pipeline"
    - path: "data/.dlt/config.toml"
      provides: "Centralized pipeline configuration"
    - path: "data/pyproject.toml"
      provides: "Consolidated dependency manifest"
    - path: "data/README.md"
      provides: "Pipeline documentation"
  key_links:
    - from: "data/anti_entropy_pipeline.py"
      to: "data/inaturalist_pipeline.py"
      via: "from inaturalist_pipeline import DEFAULT_FIELDS, _transform"
      pattern: "from inaturalist_pipeline import"
    - from: "data/.dlt/config.toml"
      to: "all pipeline files"
      via: "dlt config injection at runtime"
      pattern: "sources\\.inaturalist|sources\\.ecdysis"
---

<objective>
Port the five dlt pipeline files from the prototype into data/, create .dlt/config.toml, replace pyproject.toml, delete old pipeline modules, and update supporting files.

Purpose: Establishes the new dlt-based pipeline architecture in the project, replacing the old custom pipeline modules with the proven prototype code.
Output: Five runnable pipeline files, centralized config, clean dependency manifest, updated documentation.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/20-pipeline-migration/20-CONTEXT.md
@.planning/phases/20-pipeline-migration/20-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Copy pipeline files and create config</name>
  <files>
    data/inaturalist_pipeline.py,
    data/ecdysis_pipeline.py,
    data/geographies_pipeline.py,
    data/projects_pipeline.py,
    data/anti_entropy_pipeline.py,
    data/.dlt/config.toml
  </files>
  <read_first>
    ~/dlt-inat-test/inaturalist_pipeline.py,
    ~/dlt-inat-test/ecdysis_pipeline.py,
    ~/dlt-inat-test/geographies_pipeline.py,
    ~/dlt-inat-test/projects_pipeline.py,
    ~/dlt-inat-test/anti_entropy_pipeline.py,
    ~/dlt-inat-test/.dlt/config.toml
  </read_first>
  <action>
    Copy five pipeline files VERBATIM from ~/dlt-inat-test/ into data/:
    - inaturalist_pipeline.py
    - ecdysis_pipeline.py
    - geographies_pipeline.py
    - projects_pipeline.py
    - anti_entropy_pipeline.py

    These files are copied exactly as-is. Do NOT modify any code in them.

    Create data/.dlt/config.toml with this EXACT content:

    ```toml
    [runtime]
    log_level="WARNING"
    dlthub_telemetry = true

    [sources.inaturalist]
    project_id = 166376

    [sources.ecdysis]
    dataset_id = 44

    [sources.ecdysis_links]
    db_path = "beeatlas.duckdb"
    html_cache_dir = "raw/ecdysis_cache"
    ```

    The ONLY change from the prototype config is html_cache_dir: replace the absolute path `/Users/rainhead/dev/beeatlas/data/raw/ecdysis_cache` with the relative path `raw/ecdysis_cache` (per locked decision on html_cache_dir config).
  </action>
  <verify>
    <automated>ls data/inaturalist_pipeline.py data/ecdysis_pipeline.py data/geographies_pipeline.py data/projects_pipeline.py data/anti_entropy_pipeline.py data/.dlt/config.toml && echo "all files present"</automated>
  </verify>
  <acceptance_criteria>
    - data/inaturalist_pipeline.py exists and is byte-identical to ~/dlt-inat-test/inaturalist_pipeline.py
    - data/ecdysis_pipeline.py exists and is byte-identical to ~/dlt-inat-test/ecdysis_pipeline.py
    - data/geographies_pipeline.py exists and is byte-identical to ~/dlt-inat-test/geographies_pipeline.py
    - data/projects_pipeline.py exists and is byte-identical to ~/dlt-inat-test/projects_pipeline.py
    - data/anti_entropy_pipeline.py exists and is byte-identical to ~/dlt-inat-test/anti_entropy_pipeline.py
    - data/.dlt/config.toml contains `project_id = 166376`
    - data/.dlt/config.toml contains `dataset_id = 44`
    - data/.dlt/config.toml contains `html_cache_dir = "raw/ecdysis_cache"` (NOT an absolute path)
    - data/.dlt/config.toml contains `db_path = "beeatlas.duckdb"`
  </acceptance_criteria>
  <done>Five pipeline files copied verbatim; .dlt/config.toml created with relative html_cache_dir path</done>
</task>

<task type="auto">
  <name>Task 2: Replace pyproject.toml, delete old modules, update .gitignore</name>
  <files>
    data/pyproject.toml,
    data/uv.lock,
    data/.gitignore,
    data/Makefile,
    data/ecdysis/,
    data/inat/,
    data/links/,
    data/scripts/,
    data/spatial.py,
    data/tests/
  </files>
  <read_first>
    data/pyproject.toml,
    data/.gitignore,
    ~/dlt-inat-test/pyproject.toml
  </read_first>
  <action>
    1. REPLACE data/pyproject.toml with this EXACT content (per locked decision on dep pruning):

    ```toml
    [project]
    name = "beeatlas-data"
    version = "0.1.0"
    description = "dlt pipelines for Washington Bee Atlas data ingestion"
    readme = "README.md"
    requires-python = ">=3.14"
    dependencies = [
        "dlt[duckdb]>=1.23.0",
        "duckdb",
        "requests",
        "beautifulsoup4",
        "geopandas",
    ]
    ```

    No [build-system], no [tool.hatch.*], no [dependency-groups] — these are all removed.

    2. Run `cd data && uv sync` to regenerate uv.lock and install deps.

    3. DELETE old module directories and files:
    - `rm -rf data/ecdysis/`
    - `rm -rf data/inat/`
    - `rm -rf data/links/`
    - `rm -rf data/scripts/`
    - `rm data/spatial.py`
    - `rm -rf data/tests/`
    - `rm data/Makefile`

    4. UPDATE data/.gitignore — append these lines to the existing content:

    ```
    beeatlas.duckdb
    beeatlas.duckdb.wal
    .geography_cache/
    raw/ecdysis_cache/
    ```

    The existing entries (*.zip, *.pyc, __pycache__, last_fetch.txt, *.ndjson) remain unchanged.
  </action>
  <verify>
    <automated>cd /Users/rainhead/dev/beeatlas && ! ls data/ecdysis/ data/inat/ data/links/ data/scripts/ data/spatial.py data/tests/ data/Makefile 2>/dev/null && echo "old modules gone" && cd data && python3 -c "import tomllib; t=tomllib.loads(open('pyproject.toml').read()); deps=' '.join(t['project']['dependencies']); assert 'pyinaturalist' not in deps and 'dlt' in deps and 'build-system' not in open('pyproject.toml').read(), deps" && echo "pyproject ok" && uv run python -c "import dlt, duckdb, requests, bs4, geopandas; print('imports ok')" && grep 'beeatlas.duckdb' .gitignore && echo "gitignore ok"</automated>
  </verify>
  <acceptance_criteria>
    - data/pyproject.toml contains `name = "beeatlas-data"`
    - data/pyproject.toml contains `"dlt[duckdb]>=1.23.0"` in dependencies
    - data/pyproject.toml does NOT contain `pyinaturalist`, `pydwca`, `sqlalchemy`, `pyogrio`, `pandas`, `pyarrow`, `pydantic`
    - data/pyproject.toml does NOT contain `[build-system]` or `[tool.hatch`
    - data/pyproject.toml does NOT contain `[dependency-groups]`
    - `cd data && uv sync` completes without error
    - `cd data && uv run python -c "import dlt, duckdb, requests, bs4, geopandas"` exits 0
    - data/ecdysis/ directory does NOT exist
    - data/inat/ directory does NOT exist
    - data/links/ directory does NOT exist
    - data/scripts/ directory does NOT exist
    - data/spatial.py does NOT exist
    - data/tests/ directory does NOT exist
    - data/Makefile does NOT exist
    - data/.gitignore contains `beeatlas.duckdb`
    - data/.gitignore contains `.geography_cache/`
    - data/.gitignore contains `raw/ecdysis_cache/`
  </acceptance_criteria>
  <done>pyproject.toml replaced with prototype deps only; uv.lock regenerated; all old module directories and Makefile deleted; .gitignore updated for dlt artifacts</done>
</task>

<task type="auto">
  <name>Task 3: Write README, update PROJECT.md, close pending todo</name>
  <files>
    data/README.md,
    .planning/PROJECT.md,
    .planning/todos/pending/2026-03-12-specify-explicit-fields-on-inat-api-calls.md,
    .planning/todos/done/2026-03-12-specify-explicit-fields-on-inat-api-calls.md
  </files>
  <read_first>
    ~/dlt-inat-test/README.md,
    .planning/PROJECT.md,
    .planning/todos/pending/2026-03-12-specify-explicit-fields-on-inat-api-calls.md
  </read_first>
  <action>
    1. WRITE data/README.md based on the prototype README at ~/dlt-inat-test/README.md:
    - Replace project name "dlt-inat-test" with "beeatlas-data"
    - Replace title line with: `# beeatlas-data`
    - Replace description with: `Ingestion pipelines that load observation, specimen, and geographic data into beeatlas.duckdb for export as Parquet files.`
    - In the Ecdysis section, change `html_cache_dir = "/path/to/cache"` example to `html_cache_dir = "raw/ecdysis_cache"`
    - Keep ALL other content identical to the prototype README (pipeline descriptions, loading instructions, full reload section)
    - Add a note at the top after the description: `All commands below assume `data/` is your working directory.`

    2. UPDATE .planning/PROJECT.md line 133 — replace the key decision:
    - OLD: `| Use iNat API v1 (pyinaturalist default), not v2 | v2 has project observation count discrepancies; coordinate order also differs | ✓ Good — v1 returned correct counts and lat/lon order |`
    - NEW: `| Use iNat v2 REST API directly (not pyinaturalist) | dlt prototype uses v2 with explicit DEFAULT_FIELDS and geojson.coordinates for correct lat/lon; original v1 decision was about pyinaturalist's v2 wrapper which had different issues | ✓ Updated — Phase 20 migration; direct REST usage avoids v2 wrapper issues |`

    3. CLOSE the pending todo — move the file:
    `mv .planning/todos/pending/2026-03-12-specify-explicit-fields-on-inat-api-calls.md .planning/todos/done/2026-03-12-specify-explicit-fields-on-inat-api-calls.md`
    Per CONTEXT.md: the prototype's DEFAULT_FIELDS in inaturalist_pipeline.py resolves this todo by design.
  </action>
  <verify>
    <automated>cd /Users/rainhead/dev/beeatlas && test -s data/README.md && grep 'beeatlas-data' data/README.md && grep 'raw/ecdysis_cache' data/README.md && grep 'iNat v2 REST API' .planning/PROJECT.md && test -f .planning/todos/done/2026-03-12-specify-explicit-fields-on-inat-api-calls.md && ! test -f .planning/todos/pending/2026-03-12-specify-explicit-fields-on-inat-api-calls.md && echo "all checks pass"</automated>
  </verify>
  <acceptance_criteria>
    - data/README.md contains `beeatlas-data` (not `dlt-inat-test`)
    - data/README.md contains `raw/ecdysis_cache` (not `/path/to/cache` or absolute path)
    - data/README.md contains `inaturalist_pipeline.py` (pipeline docs present)
    - data/README.md contains `uv sync` (install instructions present)
    - .planning/PROJECT.md line 133 contains `iNat v2 REST API` (not `iNat API v1`)
    - .planning/todos/done/2026-03-12-specify-explicit-fields-on-inat-api-calls.md exists
    - .planning/todos/pending/2026-03-12-specify-explicit-fields-on-inat-api-calls.md does NOT exist
  </acceptance_criteria>
  <done>README documents new pipeline layout; PROJECT.md key decision updated for iNat v2; pending todo closed</done>
</task>

</tasks>

<verification>
Structural verification (no network required):
1. All five pipeline .py files present in data/
2. No old module directories remain (ecdysis/, inat/, links/, scripts/, spatial.py, tests/)
3. .dlt/config.toml has all four config keys with correct values
4. pyproject.toml has exactly the five required deps and no old deps
5. uv sync succeeds and imports resolve
6. .gitignore covers dlt artifacts
7. README.md documents pipeline usage
8. PROJECT.md key decision updated
</verification>

<success_criteria>
- `ls data/inaturalist_pipeline.py data/ecdysis_pipeline.py data/geographies_pipeline.py data/projects_pipeline.py data/anti_entropy_pipeline.py data/.dlt/config.toml` succeeds
- `! ls data/ecdysis/ data/inat/ data/links/ data/scripts/ data/spatial.py data/tests/ data/Makefile 2>/dev/null` returns true
- `cd data && uv run python -c "import dlt, duckdb, requests, bs4, geopandas; print('ok')"` exits 0
- `cd data && python3 -c "import tomllib; t=tomllib.loads(open('.dlt/config.toml').read()); assert t['sources']['ecdysis_links']['html_cache_dir']=='raw/ecdysis_cache'"` exits 0
</success_criteria>

<output>
After completion, create `.planning/phases/20-pipeline-migration/20-01-SUMMARY.md`
</output>
