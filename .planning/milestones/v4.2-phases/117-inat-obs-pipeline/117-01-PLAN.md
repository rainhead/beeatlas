---
phase: 117
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - data/raw/inat_expert_obs.csv
  - data/tests/test_inat_obs_pipeline.py
autonomous: true
requirements:
  - PIPE-01
  - PIPE-02
  - PIPE-03
  - PIPE-04
tags:
  - pipeline
  - csv
  - tests
  - wave-0
must_haves:
  truths:
    - "data/raw/inat_expert_obs.csv exists in git and matches the expected iNat export schema"
    - "data/tests/test_inat_obs_pipeline.py exists with 4 RED test stubs (PIPE-01..04) that fail with a clear pending message until Plan 02 lands"
    - "Running `cd data && uv run pytest tests/test_inat_obs_pipeline.py -x` exits non-zero (RED) — collection fails with ModuleNotFoundError because inat_obs_pipeline is not yet a module; this is the intended Nyquist Wave 0 RED gate signal for Plan 02"
  artifacts:
    - path: "data/raw/inat_expert_obs.csv"
      provides: "Committed iNat expert observation CSV export — the sole input to load_inat_obs()"
      contains: "id,observed_on,latitude,longitude,scientific_name,user_login,image_url,license,quality_grade"
    - path: "data/tests/test_inat_obs_pipeline.py"
      provides: "Integration test stubs for inat_obs pipeline (4 tests)"
      contains: "def test_schema_has_12_columns"
  key_links:
    - from: "data/tests/test_inat_obs_pipeline.py"
      to: "data/inat_obs_pipeline (module not yet created)"
      via: "import inat_obs_pipeline (lazy / skipped in Wave 0)"
      pattern: "import inat_obs_pipeline"
---

<objective>
Wave 0 Nyquist gate for Phase 117. Commit the iNat expert-observation CSV export and create failing test stubs against the not-yet-built pipeline module, so Plans 02 and 03 have a green/red signal to drive against.

Purpose: nothing else in Phase 117 can run before the CSV exists at `data/raw/inat_expert_obs.csv`; integration tests need the CSV to validate dedup, schema, canonicalization, and floral_host mapping.

Output: a committed CSV (~45,354 data rows including the 821 Ecdysis-overlap rows) and `data/tests/test_inat_obs_pipeline.py` containing four parameterized-but-skipping integration test stubs that go RED with a clear "pending Plan 02" message until the pipeline module lands.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/117-inat-obs-pipeline/117-CONTEXT.md
@.planning/phases/117-inat-obs-pipeline/117-RESEARCH.md
@.planning/phases/117-inat-obs-pipeline/117-PATTERNS.md
@.planning/phases/117-inat-obs-pipeline/117-VALIDATION.md

@data/tests/test_checklist_pipeline.py
@data/tests/conftest.py

<interfaces>
<!-- Expected CSV header columns (D-02 + RESEARCH Pattern 4). Wave 0 verifies the
     committed file's header matches; subsequent plans depend on these names. -->

Required CSV columns (must appear in the committed CSV header row):
- id                                                  (BIGINT obs_id)
- observed_on                                         (YYYY-MM-DD)
- latitude
- longitude
- scientific_name
- user_login
- image_url
- license
- quality_grade
- field:associated species with names lookup          (floral_host source; per CONTEXT.md D-04 / PIPE-04)

Excluded-from-output (informational): any other iNat export columns are ignored
by load_inat_obs(). The pipeline reads only the columns above.

Test stub contract (these names are load-bearing — Plan 02 fills them in):
- test_schema_has_12_columns        (PIPE-01)
- test_canonical_name_non_null      (PIPE-02)
- test_dedup_excludes_specimen_obs  (PIPE-03)
- test_floral_host_mapping          (PIPE-04)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Commit the iNat expert observation CSV export</name>
  <files>data/raw/inat_expert_obs.csv</files>
  <read_first>
    - data/raw/ (list existing files — confirm directory exists; taxa_cache.json and taxa.csv.gz are already there)
    - .planning/phases/117-inat-obs-pipeline/117-CONTEXT.md (D-01, D-02 — fixed filename, 12 output columns)
    - .planning/phases/117-inat-obs-pipeline/117-RESEARCH.md (Pattern 4 CSV column mapping; A1/A2 assumptions to verify against the actual export)
  </read_first>
  <action>
    Place the iNaturalist expert-observation CSV export at `data/raw/inat_expert_obs.csv` (per D-01: fixed filename, committed to git, no S3 management). The export is expected to contain ~45,354 data rows spanning 2011–2026 (PIPE-01..04 source data).

    If the file is not already staged on disk: the file is supplied by the user out-of-band — abort this task with a clear message listing the expected absolute path so the operator can copy it in. Do NOT synthesize sample data and do NOT pull from the iNat API.

    Once the file is in place, verify the header row contains all 10 source columns required by `<interfaces>` above (`id`, `observed_on`, `latitude`, `longitude`, `scientific_name`, `user_login`, `image_url`, `license`, `quality_grade`, `field:associated species with names lookup`). Implementation note: `head -1 data/raw/inat_expert_obs.csv | tr ',' '\n' | grep -Fx '<col>'` per column, or a one-liner Python script using `csv.reader`. If any column is missing, stop and surface the discrepancy — do not commit a CSV with a non-conforming header.

    Confirm row count is in the expected order of magnitude: `wc -l data/raw/inat_expert_obs.csv` should report ~45,355 lines (45,354 data rows + 1 header). A count off by more than ±10% from 45,354 should be flagged in the SUMMARY but not block the plan; the CSV is the source of truth.

    Note: the file is large (~tens of MB). Confirm `.gitignore` does not exclude `data/raw/*.csv` (only `taxa.csv.gz` and `ecdysis_cache` are gitignored under `data/raw/`); if it does, surface the conflict — do NOT use `git add -f` (per global instruction `feedback_no_force_add_gitignored`).
  </action>
  <verify>
    <automated>test -f data/raw/inat_expert_obs.csv &amp;&amp; head -1 data/raw/inat_expert_obs.csv | tr ',' '\n' | grep -qFx 'id' &amp;&amp; head -1 data/raw/inat_expert_obs.csv | tr ',' '\n' | grep -qFx 'scientific_name' &amp;&amp; head -1 data/raw/inat_expert_obs.csv | tr ',' '\n' | grep -qFx 'field:associated species with names lookup' &amp;&amp; head -1 data/raw/inat_expert_obs.csv | tr ',' '\n' | grep -qFx 'quality_grade'</automated>
  </verify>
  <acceptance_criteria>
    - `data/raw/inat_expert_obs.csv` exists and is tracked by git (`git ls-files --error-unmatch data/raw/inat_expert_obs.csv` exits 0)
    - Header row contains exactly the column names: `id`, `observed_on`, `latitude`, `longitude`, `scientific_name`, `user_login`, `image_url`, `license`, `quality_grade`, `field:associated species with names lookup` (extras allowed but each of these must be present verbatim)
    - `wc -l data/raw/inat_expert_obs.csv` reports between 40,000 and 50,000 lines (sanity gate around the ~45,354-row expectation)
    - File is NOT added with `git add -f`; if `.gitignore` blocks it, the conflict is surfaced before commit
  </acceptance_criteria>
  <done>The CSV is committed to git at the canonical path with a verified header.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create RED test stubs for PIPE-01..PIPE-04</name>
  <files>data/tests/test_inat_obs_pipeline.py</files>
  <read_first>
    - data/tests/test_checklist_pipeline.py (analog — fixture pattern, isolated DB, importlib.reload, schema-assertion pattern)
    - data/tests/conftest.py (waba seed pattern lines 244-258 referenced in 117-PATTERNS.md for dedup test setup)
    - .planning/phases/117-inat-obs-pipeline/117-PATTERNS.md (`Isolated DB fixture pattern`, `Schema assertion pattern`, `Non-null assertion pattern`, `Dedup test approach`)
    - .planning/phases/117-inat-obs-pipeline/117-RESEARCH.md (`Test Skeleton` section)
    - data/canonical_name.py (signature of `canonicalize()` used by the canonical_name test)
  </read_first>
  <behavior>
    - test_schema_has_12_columns: after running load_inat_obs() against a fixture CSV, `inat_obs_data.observations` exposes exactly the 12 columns `obs_id, observed_on, lat, lon, canonical_name, scientific_name, user_login, image_url, license, floral_host, quality_grade, obs_url` in that order, AND `EXPORT_DIR/inat_obs.parquet` has the same 12 columns when read back via duckdb.
    - test_canonical_name_non_null: after load_inat_obs(), `SELECT count(*) FROM inat_obs_data.observations WHERE canonical_name IS NULL AND scientific_name IS NOT NULL` returns 0.
    - test_dedup_excludes_specimen_obs: pre-seed `inaturalist_waba_data.observations__ofvs` with field_id=18116 and value=<known obs_id>, plus a matching row in `inaturalist_waba_data.observations`. Include the same obs_id in the fixture CSV. After load_inat_obs(), the obs_id is absent from `inat_obs_data.observations`.
    - test_floral_host_mapping: fixture CSV has one row with `field:associated species with names lookup` populated and one row with it empty. After load_inat_obs(), the first row's `floral_host` equals the source value; the second row's `floral_host` IS NULL.

    All four tests MUST FAIL in this plan because `inat_obs_pipeline` does not exist yet. Use a module-level guard: `pytest.importorskip("inat_obs_pipeline", reason="Pending Plan 02: load_inat_obs() not yet implemented")` ONLY IF you also assert at file end that the marker raises — i.e., do not silently skip. Preferred: do NOT use importorskip; let the `import inat_obs_pipeline` at the top of the file raise ModuleNotFoundError, which pytest reports as a test-collection error / failure. That is the RED signal Plan 02 will turn GREEN.
  </behavior>
  <action>
    Create `data/tests/test_inat_obs_pipeline.py` with the four test stubs named verbatim in `<interfaces>`: `test_schema_has_12_columns`, `test_canonical_name_non_null`, `test_dedup_excludes_specimen_obs`, `test_floral_host_mapping`.

    Structure per the `test_checklist_pipeline.py` analog (117-PATTERNS.md `Isolated DB fixture pattern`):
    - Top imports: `csv`, `duckdb`, `pytest`, `importlib`, and `import inat_obs_pipeline` (this import deliberately fails until Plan 02 lands — that is the RED gate)
    - Fixture `inat_obs_db(tmp_path, monkeypatch)`: monkeypatch `DB_PATH` and `EXPORT_DIR` to `tmp_path`; reload `inat_obs_pipeline`; pre-create schemas `inaturalist_waba_data` with tables `observations(id BIGINT)` and `observations__ofvs(_dlt_root_id VARCHAR, field_id BIGINT, name VARCHAR, value VARCHAR, _dlt_id VARCHAR)`; monkeypatch the module's `CSV_PATH` to point at a tmp CSV the test writes per-case
    - Each test writes a minimal CSV with the same header columns as the committed file (10 source columns), then asserts the behavior described in `<behavior>`
    - Use `duckdb.connect(db_path, read_only=True)` for all assertion blocks (117-PATTERNS.md `read_only=True Connection`)
    - Column-order assertion uses `information_schema.columns` ORDER BY ordinal_position, exact list match against the 12-column tuple in `<behavior>` (PIPE-01)
    - Dedup test seed values: pick obs_id = 999000001 for the duplicate row and a second obs_id = 999000002 for the keep row; insert duplicate via `INSERT INTO inaturalist_waba_data.observations VALUES (999000001); INSERT INTO inaturalist_waba_data.observations__ofvs VALUES ('root1', 18116, 'observation', '999000001', 'dltid1');` (field_id=18116 is the WABA specimen_observation_id OFV per 117-RESEARCH.md Pattern 2 fallback query)

    Do NOT use `pytest.importorskip` or `pytest.mark.skip`. The intent is that this file fails to collect / fails to import in Wave 0 — that is the RED Nyquist gate. Plan 02 will deliver `data/inat_obs_pipeline.py` and turn this file green.
  </action>
  <verify>
    <automated>cd data &amp;&amp; uv run pytest tests/test_inat_obs_pipeline.py --collect-only 2>&amp;1 | grep -E 'ModuleNotFoundError|ImportError|inat_obs_pipeline'</automated>
  </verify>
  <acceptance_criteria>
    - `data/tests/test_inat_obs_pipeline.py` exists
    - File contains four `def test_…` functions with names exactly: `test_schema_has_12_columns`, `test_canonical_name_non_null`, `test_dedup_excludes_specimen_obs`, `test_floral_host_mapping`
    - `cd data && uv run pytest tests/test_inat_obs_pipeline.py -x` exits non-zero (RED) — collection failure due to missing `inat_obs_pipeline` module is the intended outcome
    - File does NOT use `pytest.importorskip`, `pytest.mark.skip`, or `pytest.mark.xfail` against the missing module — the failure must be a hard ImportError so Plan 02 has a clear GREEN target
    - `cd data && uv run pytest tests/ -x --ignore=tests/test_inat_obs_pipeline.py` exits 0 (the rest of the suite still passes — the new file does not poison other tests)
    - Fixture `inat_obs_db` pre-creates `inaturalist_waba_data.observations` and `inaturalist_waba_data.observations__ofvs` (grep `tests/test_inat_obs_pipeline.py` for `observations__ofvs` returns at least 1 match)
    - Dedup test references `field_id = 18116` (grep returns at least 1 match)
  </acceptance_criteria>
  <done>RED test stubs exist; pytest collection fails on the new file because `inat_obs_pipeline` is not yet a module, and the rest of the data test suite is unaffected.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| committed CSV → pipeline | The CSV is a trusted git-tracked input; no untrusted-user data crosses this boundary. STRIDE focus is on schema correctness, not injection. |
| dev disk → repo | Wave 0 file movement; no network or auth surface. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-117-01-IV | Tampering / Input Validation (V5) | `data/raw/inat_expert_obs.csv` header | mitigate | Task 1 verifies the header contains all 10 required column names (PIPE-01..04). A non-conforming header aborts the commit. |
| T-117-01-IV2 | Tampering / Input Validation (V5) | `obs_id` integer cast | accept (deferred to Plan 02) | Plan 02 casts via `int(row["id"])` per 117-RESEARCH.md Pitfall 3. Wave 0 is the file commit; no parsing yet. |
| T-117-01-SC | Tampering / Supply Chain | new packages | n/a — no installs | No new packages added in this phase. 117-RESEARCH.md Package Legitimacy Audit confirms zero new dependencies. |

No high-severity threats. ASVS L1 V5 (input validation) is mitigated at task level.
</threat_model>

<verification>
- `git ls-files data/raw/inat_expert_obs.csv data/tests/test_inat_obs_pipeline.py` returns both paths
- `cd data && uv run pytest tests/test_inat_obs_pipeline.py -x` exits non-zero (RED — expected)
- `cd data && uv run pytest tests/ -x --ignore=tests/test_inat_obs_pipeline.py` exits 0 (rest of suite unaffected)
- Header row of CSV contains all 10 required columns
</verification>

<success_criteria>
1. `data/raw/inat_expert_obs.csv` is committed to git with a header containing the 10 expected source columns (verified by Task 1 acceptance check)
2. `data/tests/test_inat_obs_pipeline.py` contains the 4 named test stubs (`test_schema_has_12_columns`, `test_canonical_name_non_null`, `test_dedup_excludes_specimen_obs`, `test_floral_host_mapping`)
3. Running the new test file is RED (fails to import `inat_obs_pipeline`) — this is the Nyquist Wave 0 signal that Plan 02 must satisfy
4. The rest of the data test suite still passes when the new file is excluded
</success_criteria>

<output>
Create `.planning/phases/117-inat-obs-pipeline/117-01-SUMMARY.md` when done. Record:
- Actual row count of the committed CSV
- Any header columns that were present but not in the expected list (informational; the pipeline ignores them)
- Confirmation that `data/raw/inat_expert_obs.csv` is NOT in .gitignore
- The exact pytest failure mode for the new test file (e.g., `ModuleNotFoundError: No module named 'inat_obs_pipeline'`)
</output>
</content>
</invoke>