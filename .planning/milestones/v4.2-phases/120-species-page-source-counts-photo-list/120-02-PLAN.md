---
phase: 120-species-page-source-counts-photo-list
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - data/species_export.py
  - data/nightly.sh
autonomous: true
requirements: [SPE-03]

must_haves:
  truths:
    - "Running species_export.py produces public/data/photos.json keyed by canonical_name with lists of { url, license } objects"
    - "Only CC-licensed photos (license IS NOT NULL AND license != 'all rights reserved') appear in photos.json"
    - "photos.json is uploaded to S3 via the _upload_hashed pattern and its manifest key 'photos' appears in manifest.json"
    - "photos.json uses sort_keys=True, indent=2 for idempotency"
  artifacts:
    - path: "data/species_export.py"
      provides: "AGG-06 photos.json write block inside export_species_parquet"
      contains: "inat_obs_data.observations"
    - path: "data/nightly.sh"
      provides: "photos_name upload call and manifest entry"
      contains: "photos_name=$(_upload_hashed"
  key_links:
    - from: "data/species_export.py"
      to: "public/data/photos.json"
      via: "ASSETS_DIR / 'photos.json' write in export_species_parquet"
      pattern: "photos_out = ASSETS_DIR"
    - from: "data/nightly.sh"
      to: "s3://$BUCKET/data/photos-<hash>.json"
      via: "_upload_hashed function"
      pattern: "_upload_hashed.*photos"
---

<objective>
Add the AGG-06 photos.json artifact to species_export.py (query inat_obs_data.observations for CC-licensed photos, write per-species list of { url, license } objects) and wire the new file into nightly.sh via the hashed-upload pattern and manifest.json entry.

Purpose: SPE-03 — store per-species CC-licensed iNat photo metadata for the future photo carousel milestone; no UI consumer this milestone.

Output: photos.json at public/data/photos.json (local dev) and as a hashed S3 object in production.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/120-species-page-source-counts-photo-list/120-CONTEXT.md
@.planning/phases/120-species-page-source-counts-photo-list/120-PATTERNS.md

<interfaces>
<!-- inat_obs_data.observations schema (Phase 117, data/inat_obs_pipeline.py) -->
Table: inat_obs_data.observations
Columns (all from the same beeatlas.duckdb connection used by export_species_parquet):
  obs_id          VARCHAR
  observed_on     DATE
  lat             DOUBLE
  lon             DOUBLE
  canonical_name  VARCHAR  (non-null; D-04 canonicalization applied)
  scientific_name VARCHAR
  user_login      VARCHAR
  image_url       VARCHAR  (nullable)
  license         VARCHAR  (nullable; 'all rights reserved' for non-CC; NULL for no license)
  floral_host     VARCHAR  (nullable)
  quality_grade   VARCHAR
  obs_url         VARCHAR

<!-- species_export.py key identifiers (read before editing) -->
Function: export_species_parquet(con: duckdb.DuckDBPyConnection)
  DB_PATH: module-level — path to beeatlas.duckdb
  ASSETS_DIR: module-level Path — defaults to public/data/
  Writes end-of-function after seasonality.json (line ~236-238)

<!-- nightly.sh upload pattern (read before editing) -->
Function: _upload_hashed(src, basename, [extra aws flags...])
  Returns: hashed filename string (e.g. "photos-abc123def456.json")
Existing upload lines end at line ~157 (checklist_name)
manifest.json heredoc: lines ~159–171; "checklist" key at line ~168, "generated_at" is last data key
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Add AGG-06 photos.json block to species_export.py</name>
  <files>data/species_export.py</files>
  <read_first>
    - data/species_export.py (read lines 195–250 — seasonality.json block and main() function; establishes insertion point and con object usage)
    - data/species_export.py (read lines 1–40 — module header, DB_PATH, ASSETS_DIR, imports)
    - .planning/phases/120-species-page-source-counts-photo-list/120-PATTERNS.md §species_export.py section — exact code pattern for the photos block
    - data/inat_obs_pipeline.py (read lines 70–100 — inat_obs_data.observations CREATE TABLE to confirm column names)
  </read_first>
  <action>
    Add the AGG-06 photos.json block inside `export_species_parquet(con)`, immediately after the seasonality.json assert (after line ~238) and before the closing of the function (before `def main()`). Per D-06, D-07, D-08, D-09.

    The block:
    1. Executes a DuckDB query on the existing `con` object (same connection to beeatlas.duckdb):
       SELECT canonical_name, image_url, license
       FROM inat_obs_data.observations
       WHERE license IS NOT NULL AND license != 'all rights reserved'
         AND image_url IS NOT NULL
       ORDER BY canonical_name

    2. Accumulates results into a dict[str, list[dict]] named `photos`, keyed by canonical_name, values are lists of {"license": license_, "url": url} dicts.

    3. Writes to ASSETS_DIR / "photos.json" using json.dumps with sort_keys=True and indent=2 (matching species.json convention per D-07 — human-inspectable, not tight-packed like seasonality.json).

    4. Prints a summary line: `photos.json: {len(photos):,} species, {photos_out.stat().st_size:,} bytes`

    5. Update the module docstring at lines 1–7 to add `photos.json` to the artifact list ("Per-species CC-licensed iNat photo list — D-07/D-08").

    6. Update the `export_species_parquet` function docstring (lines 83–95) to add `- photos.json (CC-licensed iNat obs photos, keyed by canonical_name)` to the "Writes three artifacts" list (which becomes "four artifacts").

    Note: `con` is the connection passed into `export_species_parquet`. The `inat_obs_data` schema lives in the same beeatlas.duckdb file that `DB_PATH` points to. No ATTACH call is needed — the schema is already present after `inat_obs_pipeline.load_inat_obs(con)` runs earlier in the pipeline.
  </action>
  <verify>
    <automated>cd /Users/rainhead/dev/beeatlas/data && grep -c "inat_obs_data.observations" species_export.py</automated>
  </verify>
  <acceptance_criteria>
    - data/species_export.py contains a block querying `inat_obs_data.observations` with filter `license IS NOT NULL AND license != 'all rights reserved' AND image_url IS NOT NULL`
    - data/species_export.py contains `photos_out = ASSETS_DIR / "photos.json"`
    - data/species_export.py contains `json.dumps(photos, sort_keys=True, indent=2)`
    - The module docstring and export_species_parquet docstring mention photos.json
    - `cd /Users/rainhead/dev/beeatlas/data && uv run python -c "import species_export"` exits 0 (module imports without error)
    - Running `cd /Users/rainhead/dev/beeatlas/data && uv run pytest tests/test_species_export.py -x` exits 0 (existing tests still pass)
  </acceptance_criteria>
  <done>species_export.py produces photos.json alongside existing artifacts; existing tests green</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Wire photos.json into nightly.sh upload and manifest</name>
  <files>data/nightly.sh</files>
  <read_first>
    - data/nightly.sh (read lines 138–180 — _upload_hashed function definition, upload block, manifest heredoc, and s3 cp for manifest)
    - .planning/phases/120-species-page-source-counts-photo-list/120-PATTERNS.md §nightly.sh section — exact before/after for the upload line and manifest entry
  </read_first>
  <action>
    Two targeted additions to data/nightly.sh (per D-10):

    Addition 1 — upload line: After the `checklist_name=$(_upload_hashed ...)` line (currently line ~157), add:
      `photos_name=$(_upload_hashed "$EXPORT_DIR/photos.json" "photos")`
    No extra --content-type flag needed (.json files default to application/json in the _upload_hashed function's aws s3 cp call — confirm by checking the function definition).

    Addition 2 — manifest entry: In the manifest.json heredoc (lines ~159–171), add a new key after the "checklist" entry and before "generated_at":
      `"photos": "$photos_name",`

    The manifest key name is "photos" — lowercase stem of the filename, following the established convention (e.g., "checklist", "species", "seasonality").
  </action>
  <verify>
    <automated>grep -c "photos_name" /Users/rainhead/dev/beeatlas/data/nightly.sh</automated>
  </verify>
  <acceptance_criteria>
    - data/nightly.sh contains `photos_name=$(_upload_hashed "$EXPORT_DIR/photos.json" "photos")`
    - data/nightly.sh manifest heredoc contains `"photos": "$photos_name",`
    - The "photos" entry appears before the "generated_at" entry in the manifest heredoc
    - `bash -n /Users/rainhead/dev/beeatlas/data/nightly.sh` exits 0 (shell syntax valid)
    - The checklist_name line is unmodified and still present immediately before the new photos_name line
  </acceptance_criteria>
  <done>nightly.sh uploads photos.json with a content-addressed key and records it in manifest.json</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| inat_obs_data.observations → photos.json | Pipeline-internal read; data entered via trusted CSV export. No user-supplied data at write time. |
| photos.json → S3 CloudFront | Static file upload; no server-side execution. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-120-02 | Information Disclosure | photos.json image_url / license values | accept | Data is sourced from iNat's public CSV export; URLs are already public. No PII beyond observer login (not included in photos.json). |
| T-120-03 | Tampering | nightly.sh _upload_hashed content-addressing | accept | SHA-256 hash in the filename ensures stale CDN responses are impossible; manifest.json points to the correct hash. |
| T-120-SC | Tampering | npm/pip/cargo installs | accept | No new package installs in this plan — DuckDB and json are already available in the data environment. |
</threat_model>

<verification>
1. `bash -n /Users/rainhead/dev/beeatlas/data/nightly.sh` exits 0
2. `cd /Users/rainhead/dev/beeatlas/data && uv run pytest tests/test_species_export.py -x` exits 0
3. data/species_export.py grep: `grep -c "inat_obs_data.observations" data/species_export.py` returns >= 1
4. data/nightly.sh grep: `grep "photos_name" data/nightly.sh` shows both the upload call and the manifest entry
5. When run against a populated beeatlas.duckdb (after inat_obs_pipeline has run): public/data/photos.json exists, is valid JSON, contains a dict keyed by canonical_name strings with list-of-dict values containing "url" and "license" keys.
</verification>

<success_criteria>
- SPE-03: species_export.py writes photos.json (CC-licensed iNat obs photos per species) to ASSETS_DIR
- nightly.sh uploads photos.json via _upload_hashed and records key "photos" in manifest.json
- Shell and Python syntax valid; existing tests unbroken
- photos.json structure: `{ "Canonical Name": [{ "url": "...", "license": "..." }, ...] }` with sort_keys=True, indent=2
</success_criteria>

<output>
Create `.planning/phases/120-species-page-source-counts-photo-list/120-02-SUMMARY.md` when done
</output>
