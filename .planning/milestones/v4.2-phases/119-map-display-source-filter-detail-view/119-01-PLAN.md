---
phase: 119
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - src/tests/url-state.test.ts
  - src/tests/bee-atlas.test.ts
  - src/tests/bee-pane.test.ts
  - src/filter.ts
autonomous: true
requirements: [MAP-01, MAP-02, MAP-03, DET-01]

must_haves:
  truths:
    - "Vitest reports failing assertions for MAP-01, MAP-02, MAP-03, DET-01 (RED state) before any implementation lands"
    - "OccurrenceRow gains source/image_url/obs_url/user_login/license fields without breaking existing rows"
    - "OCCURRENCE_COLUMNS includes the five new column names so wa-sqlite SELECTs and features.ts spreads propagate them automatically"
  artifacts:
    - path: "src/tests/url-state.test.ts"
      provides: "MAP-03 describe block exercising src param round-trip"
      contains: "MAP-03: source filter URL param (src=)"
    - path: "src/tests/bee-atlas.test.ts"
      provides: "MAP-01, MAP-02, DET-01 source-inspection assertions"
      contains: "MAP-01: iNat obs amber color"
    - path: "src/tests/bee-pane.test.ts"
      provides: "MAP-02 hiddenSources + checkbox assertions"
      contains: "MAP-02: source filter row"
    - path: "src/filter.ts"
      provides: "Extended OccurrenceRow + OCCURRENCE_COLUMNS"
      contains: "source: 'ecdysis' | 'waba_sample' | 'inat_obs' | null"
  key_links:
    - from: "src/filter.ts OCCURRENCE_COLUMNS"
      to: "src/features.ts featureToOccurrenceRow"
      via: "as const tuple drives ...obj spread on GeoJSON properties"
      pattern: "OCCURRENCE_COLUMNS"
---

<objective>
Establish the Nyquist Wave 0 RED gates for all four phase requirements and extend `OccurrenceRow` / `OCCURRENCE_COLUMNS` so downstream waves have a stable type and column projection to build against.

Purpose: Lock the behavior contract (MAP-01/02/03 + DET-01) into the test suite before any implementation exists; expose the five new occurrence columns to the rest of the codebase via the `as const` tuple that drives SQL projection and GeoJSON property spread.

Output: Failing Vitest assertions for every requirement; a typed `OccurrenceRow` with `source` / `image_url` / `obs_url` / `user_login` / `license` (all nullable) and matching column names appended to `OCCURRENCE_COLUMNS`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/119-map-display-source-filter-detail-view/119-RESEARCH.md
@.planning/phases/119-map-display-source-filter-detail-view/119-PATTERNS.md
@.planning/phases/119-map-display-source-filter-detail-view/119-UI-SPEC.md
@.planning/phases/119-map-display-source-filter-detail-view/119-VALIDATION.md

<interfaces>
<!-- Anchors from current codebase the executor will be matching/extending. -->

From src/filter.ts (existing OccurrenceRow tail and OCCURRENCE_COLUMNS):
  interface OccurrenceRow { ...; sample_id: number | null; sample_host: string | null; is_provisional: boolean | null; specimen_inat_taxon_name: string | null; specimen_inat_quality_grade: string | null; }
  export const OCCURRENCE_COLUMNS = [ ..., 'is_provisional', 'specimen_inat_taxon_name', 'specimen_inat_quality_grade' ] as const;

From src/tests/url-state.test.ts (existing analog for `cl` param):
  describe('MAP-04: checklist layer URL param (cl=1)', () => { ... })  // lines 266-300, the analog to copy

From src/tests/bee-atlas.test.ts (ARCH-02/ARCH-03 source-inspection pattern):
  const src = readFileSync(resolve(__dirname, '../bee-map.ts'), 'utf-8');
  test('...', () => { expect(src).toMatch(/.../); });

From src/tests/bee-pane.test.ts (top of file already has `src` loaded via readFileSync):
  Existing PANE-01 / PANE-02 describe blocks use the same source-inspection pattern.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add MAP-03 src= round-trip tests to url-state.test.ts</name>
  <files>src/tests/url-state.test.ts</files>
  <read_first>
    - src/tests/url-state.test.ts (whole file — locate the existing `MAP-04: checklist layer URL param (cl=1)` describe block; copy its structure including the `defaultView` / `emptyFilter()` / `defaultSelection` / `defaultUi` helpers it uses)
    - src/url-state.ts (current UiState interface, buildParams signature, parseParams return shape — needed so the tests reference real exports)
    - .planning/phases/119-map-display-source-filter-detail-view/119-PATTERNS.md (the `src/tests/url-state.test.ts — MAP-03 tests for src param` section is the exact spec for the tests to add)
  </read_first>
  <action>
    Append a new `describe('MAP-03: source filter URL param (src=)', ...)` block to `src/tests/url-state.test.ts`. Reuse the same fixture helpers used by the existing `MAP-04` block (`defaultView`, `emptyFilter()`, `defaultSelection`, `defaultUi`). Add six tests:
    1. single hidden source serializes: `ui.hiddenSources = new Set(['ecdysis'])` → `buildParams(...).get('src') === 'ecdysis'`
    2. default (empty hiddenSources): `buildParams(..., defaultUi).has('src') === false`
    3. parse round-trip: `parseParams('src=ecdysis').ui?.hiddenSources` equals `new Set(['ecdysis'])`
    4. multiple hidden sources are emitted alphabetically sorted: `new Set(['inat_obs','ecdysis'])` → `'ecdysis,inat_obs'`
    5. invalid token filtered out: `parseParams('src=ecdysis,bogus_source').ui?.hiddenSources` equals `new Set(['ecdysis'])`
    6. `src=ecdysis` alone triggers `result.ui` being defined (hasFilter condition extension): `parseParams('src=ecdysis').ui` is defined.
    Use `Set` equality via `expect(set).toEqual(new Set([...]))`. Tests MUST be RED (the implementation in Plan 02 has not landed yet); do not write any url-state.ts changes here.
  </action>
  <verify>
    <automated>npm test -- src/tests/url-state.test.ts 2>&1 | grep -E "(MAP-03|FAIL|PASS|✓|✗)" | head -30</automated>
  </verify>
  <acceptance_criteria>
    - `src/tests/url-state.test.ts` contains the literal string `MAP-03: source filter URL param (src=)`
    - `src/tests/url-state.test.ts` contains at least 6 `test(` (or `it(`) entries inside that describe block
    - `npm test -- src/tests/url-state.test.ts` reports 6 failing tests under the `MAP-03` describe (RED state — `hiddenSources` field does not yet exist on `UiState`)
    - The test file still compiles (no TypeScript errors); use `as const` casts on the `Set` literals so they fit `Set<'ecdysis'|'waba_sample'|'inat_obs'>`
  </acceptance_criteria>
  <done>Six new MAP-03 tests committed; all RED; tsc passes (test file still compiles even though the assertions fail at runtime).</done>
</task>

<task type="auto">
  <name>Task 2: Add MAP-01, MAP-02, DET-01 source-inspection tests to bee-atlas.test.ts and bee-pane.test.ts</name>
  <files>src/tests/bee-atlas.test.ts, src/tests/bee-pane.test.ts</files>
  <read_first>
    - src/tests/bee-atlas.test.ts (locate the ARCH-02/ARCH-03 source-inspection blocks around lines 89–123 — they use `readFileSync(resolve(__dirname, '../FILE.ts'), 'utf-8')` then `expect(src).toMatch(/.../)`. Copy that structure)
    - src/tests/bee-pane.test.ts (locate the top-of-file `readFileSync` of `bee-pane.ts` plus the PANE-01 / PANE-02 describe blocks around lines 23–52)
    - .planning/phases/119-map-display-source-filter-detail-view/119-PATTERNS.md (`src/tests/bee-atlas.test.ts — MAP-01 and DET-01 source-inspection tests` and `src/tests/bee-pane.test.ts — MAP-02 source toggle tests` sections specify the exact regexes)
    - src/bee-map.ts (so the test patterns reference real layer/paint identifiers — confirm the `unclustered-point` layer name)
  </read_first>
  <action>
    In `src/tests/bee-atlas.test.ts`, append three describe blocks:
    1. `MAP-01: iNat obs amber color in unclustered-point paint` — reads `../bee-map.ts` source, asserts `/#e8a020/`, asserts `/'case'/`, asserts `/\['==', \['get', 'source'\], 'inat_obs'\]/`
    2. `DET-01: _renderInatObs dispatched for source=inat_obs` — reads `../bee-occurrence-detail.ts` source, asserts `/_renderInatObs\s*\(/`, asserts `/row\.source\s*===\s*['"]inat_obs['"]/`
    3. `MAP-02: source-filter-changed event in bee-atlas` — reads `../bee-atlas.ts` source, asserts `/source-filter-changed/` and `/_hiddenSources/`

    In `src/tests/bee-pane.test.ts`, append a `MAP-02: source filter row in bee-pane` describe block (reuse the `src` constant already loaded at top of file). Add six tests with the exact assertions from PATTERNS.md:
    - declares `hiddenSources @property` (regex `/@property[\s\S]{0,50}hiddenSources/`)
    - dispatches `source-filter-changed` (regex `/new CustomEvent\(['"]source-filter-changed['"]/`)
    - contains `_renderSources` method (regex `/_renderSources\s*\(/`)
    - contains literal `ecdysis`
    - contains literal `inat_obs`
    - contains literal `waba_sample`

    Tests MUST be RED — none of the asserted strings exist in source files yet.
  </action>
  <verify>
    <automated>npm test -- src/tests/bee-atlas.test.ts src/tests/bee-pane.test.ts 2>&1 | grep -E "(MAP-0[12]|DET-01|FAIL|PASS|✓|✗)" | head -40</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "MAP-01: iNat obs amber color" src/tests/bee-atlas.test.ts` returns 1
    - `grep -c "DET-01: _renderInatObs" src/tests/bee-atlas.test.ts` returns 1
    - `grep -c "MAP-02: source-filter-changed event in bee-atlas" src/tests/bee-atlas.test.ts` returns 1
    - `grep -c "MAP-02: source filter row in bee-pane" src/tests/bee-pane.test.ts` returns 1
    - `npm test -- src/tests/bee-atlas.test.ts src/tests/bee-pane.test.ts` reports red assertions under those describe blocks (RED state)
    - Test files still compile under tsc (no broken imports — the `readFileSync` / `resolve` imports follow the existing ARCH-02 pattern)
  </acceptance_criteria>
  <done>All four new describe blocks committed; assertions fail (RED) because implementations have not landed.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Extend OccurrenceRow and OCCURRENCE_COLUMNS in filter.ts</name>
  <files>src/filter.ts</files>
  <read_first>
    - src/filter.ts (entire file — focus on lines around 26–66 where `OccurrenceRow` and `OCCURRENCE_COLUMNS` are declared; identify the exact tail of each so the additions land at the right place per PATTERNS.md `src/filter.ts — extend OccurrenceRow and OCCURRENCE_COLUMNS` section)
    - src/features.ts (verify the `...obj` spread in `loadOccurrenceGeoJSON` / `featureToOccurrenceRow` — confirm that adding column names to `OCCURRENCE_COLUMNS` automatically threads them through; no edit needed)
    - .planning/phases/119-map-display-source-filter-detail-view/119-RESEARCH.md (Pattern 1 — confirms that source is nullable because public/data/occurrences.parquet still has 31 cols)
  </read_first>
  <behavior>
    - `OccurrenceRow` has new fields: `source: 'ecdysis' | 'waba_sample' | 'inat_obs' | null`, `image_url: string | null`, `obs_url: string | null`, `user_login: string | null`, `license: string | null`
    - `OCCURRENCE_COLUMNS` (the `as const` tuple) includes the five new column names appended after `specimen_inat_quality_grade`
    - `OCCURRENCE_COLUMNS.length` grows by exactly 5
    - Existing call sites that do `OCCURRENCE_COLUMNS.join(', ')` still compile (no signature changes)
  </behavior>
  <action>
    In `src/filter.ts`:
    1. Extend the `OccurrenceRow` interface — after the existing `specimen_inat_quality_grade: string | null;` tail, add five new fields (literal types as above; all five nullable). Order: `source`, `image_url`, `obs_url`, `user_login`, `license`.
    2. Extend the `OCCURRENCE_COLUMNS` `as const` tuple — append the five string literals in the same order after `'specimen_inat_quality_grade'`. Do not reorder existing entries.

    Do not modify `features.ts`; the spread already propagates new columns. Do not touch SQL projection callers — they read from the tuple. Use the existing 4-space (or 2-space, match the file) indentation. Keep the `as const` assertion intact.
  </action>
  <verify>
    <automated>npx tsc --noEmit && npm test 2>&1 | tail -50</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "source: 'ecdysis' | 'waba_sample' | 'inat_obs' | null" src/filter.ts` returns 1
    - `grep -c "image_url: string | null" src/filter.ts` returns 1
    - `grep -c "obs_url: string | null" src/filter.ts` returns 1
    - `grep -c "user_login: string | null" src/filter.ts` returns 1
    - `grep -c "license: string | null" src/filter.ts` returns 1
    - `grep -E "^\s*'source',\s*'image_url',\s*'obs_url',\s*'user_login',\s*'license',?\s*$" src/filter.ts` matches at least one line
    - `npx tsc --noEmit` exits 0
    - `npm test` shows pre-existing tests still passing; only the new Wave 0 RED tests added in Tasks 1–2 are failing
  </acceptance_criteria>
  <done>filter.ts extended; tsc clean; Wave 0 RED state preserved (only the new MAP-01/02/03/DET-01 assertions fail).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| URL → frontend `parseParams` | User-controlled `src` query param crosses into UiState |
| Parquet → wa-sqlite → OccurrenceRow | New columns must coerce null safely when older parquet is served |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-119-01 | Tampering | URL `src` param parsing in url-state.ts (planned Wave 1) | mitigate | Wave 0 test in Task 1 #5 asserts unknown tokens are filtered via `VALID_SOURCES` allowlist; the Wave 1 implementation MUST satisfy this RED test before merge |
| T-119-02 | Information disclosure | `OccurrenceRow.image_url` / `user_login` propagation | accept | All four iNat-specific columns are public iNat data (CC-licensed or ARR images, public user logins); no PII beyond what iNaturalist.org already exposes |
| T-119-SC | Tampering | npm/pip/cargo installs | mitigate | No package installs in this plan (audit confirms `Section not applicable` per RESEARCH.md) |
</threat_model>

<verification>
- All Wave 0 RED tests committed and failing for the right reason (asserted strings/structures don't yet exist)
- `npx tsc --noEmit` passes
- `OCCURRENCE_COLUMNS.length` is exactly 5 greater than its pre-Plan-01 value
- No test that was green pre-plan turns red (only the new MAP-01/02/03/DET-01 tests are red)
</verification>

<success_criteria>
- Tasks 1 + 2: All four phase requirements (MAP-01, MAP-02, MAP-03, DET-01) have at least one failing test asserting a concrete code-level shape (event name, regex on paint expression, regex on render dispatch, set round-trip)
- Task 3: OccurrenceRow + OCCURRENCE_COLUMNS extended with five nullable fields; tsc clean; downstream plans can import `OccurrenceRow` with the new fields
</success_criteria>

<output>
Create `.planning/phases/119-map-display-source-filter-detail-view/119-01-SUMMARY.md` when done. Note in the summary: count of new failing tests (expected: 6 url-state + 8 bee-atlas + 6 bee-pane = 20 RED assertions), confirmation that pre-existing tests still pass, and the final `OCCURRENCE_COLUMNS` length.
</output>
