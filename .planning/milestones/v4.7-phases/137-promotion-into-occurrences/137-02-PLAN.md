---
phase: 137-promotion-into-occurrences
plan: 02
type: execute
wave: 2
depends_on: ["137-01"]
files_modified:
  - data/sqlite_export.py
  - src/features.ts
  - src/tests/build-geojson.test.ts
autonomous: true
requirements: [PRO-04]
nyquist_compliant: true

must_haves:
  truths:
    - "PRO-04: occurrences.db geo_blob carries checklist identity — _GEO_COLS gains checklist_id at index 7 (after source)"
    - "PRO-04: src/features.ts _buildGeoJSONFromRaw reads row[7] and decodes a non-null checklist_id to occId = 'checklist:<N>'"
    - "PRO-04: a checklist row (NULL ecdysis/observation/specimen ids, non-null checklist_id) is NOT dropped by _buildGeoJSONFromRaw"
    - "PRO-04: the _GEO_COLS change and the features.ts decode change land in ONE atomic commit, accompanied by the Vitest test"
  artifacts:
    - path: "data/sqlite_export.py"
      provides: "_GEO_COLS with checklist_id appended at index 7 + updated column-order comment"
      contains: "checklist_id"
    - path: "src/features.ts"
      provides: "checklist_id = row[7] decode + `else if (checklist_id != null) occId = checklist:${checklist_id}` branch"
      contains: "checklist:"
    - path: "src/tests/build-geojson.test.ts"
      provides: "8-field toRow/RowOverride + makeChecklistRow factory + checklist decode/no-drop/null-drop cases"
      contains: "checklist:"
  key_links:
    - from: "data/sqlite_export.py _GEO_COLS index 7"
      to: "src/features.ts row[7]"
      via: "positional geo_blob tuple encoding (untyped coupling — must change atomically)"
      pattern: "row\\[7\\]"
    - from: "src/features.ts checklist branch"
      to: "occId 'checklist:<N>'"
      via: "else if (checklist_id != null) appended to the ecdysis/inat/inat_obs chain"
      pattern: "checklist:\\$\\{checklist_id\\}"
---

<objective>
In a SINGLE atomic commit, append `checklist_id` to `sqlite_export._GEO_COLS` (new index 7, after `source`) and add a `checklist:<N>` decode branch to `_buildGeoJSONFromRaw` in `src/features.ts`, with a Vitest test in `src/tests/build-geojson.test.ts` proving a checklist row decodes to `occId === 'checklist:<N>'` and is not dropped.

Purpose: The `_GEO_COLS` list and the positional `row[i]` decode in `features.ts` are coupled by index with NO shared type contract — a one-index slip silently drops every checklist point (or corrupts existing rows) with no runtime error. PRO-04 exists to enforce that these two files change together. This plan depends on Plan 01 having added `checklist_id` to `occurrences.parquet`/the occurrences SQLite table.

Output:
- `_GEO_COLS` = `[lat, lon, ecdysis_id, observation_id, specimen_observation_id, year, source, checklist_id]` (8 fields; checklist_id at index 7) + updated column-order comment documenting the Phase 137 atomic coupling.
- `features.ts`: `const checklist_id = row[7]` + `else if (checklist_id != null) occId = \`checklist:${checklist_id}\`` appended to the occId chain + updated layout comment.
- `build-geojson.test.ts`: `RowOverride`/`toRow` migrated 7→8 fields (checklist_id optional, null default), `makeChecklistRow` factory, three new `it` cases.
- One commit; `npm test` (Vitest) green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/137-promotion-into-occurrences/137-CONTEXT.md
@.planning/phases/137-promotion-into-occurrences/137-RESEARCH.md
@CLAUDE.md

<interfaces>
<!-- Current state (verified live): -->

data/sqlite_export.py lines 455-462 (7-field _GEO_COLS):
  Column order comment: [lat, lon, ecdysis_id, observation_id, specimen_observation_id, year, source]
  _GEO_COLS = ["lat", "lon", "ecdysis_id", "observation_id", "specimen_observation_id", "year", "source"]
  select_expr at line 465: `c if c in actual else f"NULL AS {c}"` — handles missing cols; checklist_id will be a real occurrences column (Plan 01) so it is selected directly.

src/features.ts _buildGeoJSONFromRaw (lines 14-48) current decode:
  const ecdysis_id = row[2]; const observation_id = row[3]; const specimen_observation_id = row[4];
  const year = Number(row[5]); const source = row[6] as string | null;
  if (ecdysis_id != null) occId = `ecdysis:${ecdysis_id}`;
  else if (observation_id != null) occId = `inat:${observation_id}`;
  else if (specimen_observation_id != null) occId = `inat_obs:${specimen_observation_id}`;
  if (occId == null) continue;

src/tests/build-geojson.test.ts: existing toRow factory is 7-field `Required<RowOverride>`;
  makeEcdysisRow / makeInatRow / makeSpecimenObsRow all call toRow with 7 explicit fields.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Atomic geo_blob ↔ features.ts checklist_id append + Vitest checklist:<N> decode (single commit)</name>
  <files>data/sqlite_export.py, src/features.ts, src/tests/build-geojson.test.ts</files>
  <read_first>
    - data/sqlite_export.py lines 454-471 (the `_GEO_COLS` list, its column-order comment, and the `select_expr` / geo_blob construction — confirm the 7-field current state matches the &lt;interfaces&gt; block)
    - src/features.ts lines 14-48 (`_buildGeoJSONFromRaw`: the row[2-6] decode, the occId if/else chain, and the `if (occId == null) continue` drop; confirm the layout comment on line 14)
    - src/tests/build-geojson.test.ts (ENTIRE file — the `RowOverride` interface, the `toRow(r: Required<RowOverride>)` factory, and EVERY existing factory callsite: makeEcdysisRow/makeInatRow/makeSpecimenObsRow and any inline toRow calls — all must migrate to the 8-field layout)
    - .planning/phases/137-promotion-into-occurrences/137-RESEARCH.md §"Pattern 4" + §"Pattern 5" + §"Verified build-geojson.test.ts extension" (8-field factory + 3 test cases) + §"Pitfall 4" (silent-drop failure mode) + §"Open Questions #2" (make checklist_id optional with null default — lowest churn)
    - STATE.md §Decisions ("Phase 137 atomic commit": positional coupling, one commit)
  </read_first>
  <behavior>
    - A checklist row `[lat, lon, null, null, null, year, 'checklist', <N>]` → `occId === 'checklist:<N>'` and emits exactly one feature.
    - A checklist row with all three legacy IDs null but non-null checklist_id is NOT dropped (feature count 1).
    - A row with all four IDs null (checklist_id null too) IS dropped (feature count 0) — the existing drop behavior is preserved.
    - Existing ecdysis/inat/inat_obs decode behavior is unchanged (their factory callsites now pass checklist_id: null by default).
  </behavior>
  <action>
    This is ONE task producing ONE atomic commit (D-08 / STATE.md "Phase 137 atomic commit" — the `_GEO_COLS` ↔ `features.ts` positional coupling is untyped; splitting risks silent data loss for every checklist point).
    (a) `data/sqlite_export.py`: append `"checklist_id"` to `_GEO_COLS` as the LAST element (index 7, after `"source"`). Update the column-order comment (lines 455-456) to list the 8 columns `[lat, lon, ecdysis_id, observation_id, specimen_observation_id, year, source, checklist_id]` and add a line: "Phase 137: checklist_id appended at index 7; features.ts _buildGeoJSONFromRaw updated in same commit (positional coupling)." Leave the `select_expr` logic untouched — `checklist_id` is now a real occurrences column (Plan 01) so it is selected directly; the `NULL AS` fallback remains harmless.
    (b) `src/features.ts`: add `const checklist_id = row[7];` after the `source = row[6]` line. Append `else if (checklist_id != null) occId = \`checklist:${checklist_id}\`;` to the END of the existing if/else occId chain (after the `inat_obs` branch; chain position is immaterial since checklist rows have NULL ecdysis/observation/specimen ids). Update the layout comment on line 14 to include `checklist_id` at index 7.
    (c) `src/tests/build-geojson.test.ts`: add `checklist_id?: number | null` to `RowOverride` (OPTIONAL, per research Open Q#2 / lowest churn). Migrate `toRow` to the 8-field layout returning `[..., r.source, r.checklist_id]`; give it a `null` default for checklist_id so existing 7-field factory callsites do not all need editing (provide the default in toRow's spread/destructure, not at every callsite). Add a `makeChecklistRow` factory (lat 47.4, lon -120.6, all three legacy IDs null, year 1998, source 'checklist', checklist_id 7777, spread overrides). Add three `it` cases inside the `_buildGeoJSONFromRaw` describe: (1) `checklist_id: 42` → `occId === 'checklist:42'`, one feature; (2) default checklist row (legacy IDs null) → NOT dropped, one feature; (3) `checklist_id: null` → dropped, zero features.
    Run `npm test` (Vitest run, NOT watch) and ensure all existing + new cases pass. Stage all three files and commit together (one atomic commit).
  </action>
  <verify>
    <automated>npx vitest run src/tests/build-geojson.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run src/tests/build-geojson.test.ts` exits 0 with all tests passing, including the three new `checklist:` cases.
    - `grep -q '"checklist_id"' data/sqlite_export.py` succeeds AND `checklist_id` is the last element of `_GEO_COLS` (8 entries total).
    - `grep -n 'index 7\|Phase 137' data/sqlite_export.py` shows the updated atomic-coupling comment.
    - `grep -q 'checklist:${checklist_id}' src/features.ts` succeeds AND `grep -q 'const checklist_id = row\[7\]' src/features.ts` succeeds.
    - `grep -q 'makeChecklistRow' src/tests/build-geojson.test.ts` succeeds AND `grep -q "checklist:42" src/tests/build-geojson.test.ts` succeeds.
    - `git show --stat HEAD` (the commit produced by this task) lists all three files — `data/sqlite_export.py`, `src/features.ts`, `src/tests/build-geojson.test.ts` — in ONE commit (atomic boundary; not split).
    - No watch-mode flag used (vitest `run`, not `--watch`).
  </acceptance_criteria>
  <done>`_GEO_COLS` carries `checklist_id` at index 7, `features.ts` decodes `checklist:<N>`, and the Vitest suite proves decode + no-drop — all in one atomic commit; `npx vitest run src/tests/build-geojson.test.ts` green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | The geo_blob is a build-time-serialized blob from the trusted occurrences SQLite DB; `features.ts` decodes it client-side. No untrusted external input, no auth, no network boundary. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-137-03 | Tampering | _GEO_COLS ↔ features.ts positional index drift | mitigate | D-08 mandates one atomic commit + a Vitest test in the SAME commit; CI fails if features.ts is updated without the test, or the decode breaks — the test is the type-contract substitute for the untyped positional coupling. |
| T-137-04 | Denial of Service | silent checklist-point drop (occId == null → continue) | mitigate | The "checklist row not dropped" Vitest case fails if features.ts isn't updated, surfacing the silent-drop failure mode (research Pitfall 4) at test time rather than in production. |
| T-137-SC | Tampering | npm/pip/cargo installs | accept | No new packages installed this phase (RESEARCH §"Package Legitimacy Audit": not applicable). No install task exists. |

No new threat surfaces are introduced — this is a positional-decode extension of an existing static export path.
</threat_model>

<verification>
- `npx vitest run src/tests/build-geojson.test.ts` green (covers PRO-04 decode + no-drop).
- `git show --stat HEAD` confirms the three files landed in one atomic commit.
- (Manual, Phase 138) checklist points render on the live map — out of scope here; this plan ends at decode correctness.
</verification>

<success_criteria>
- PRO-04: `occurrences.db` geo_blob carries checklist identity (`_GEO_COLS` index 7); `features.ts` decodes `checklist:<N>`; `_buildGeoJSONFromRaw` drops no checklist point; `_GEO_COLS` + `features.ts` changed in one atomic commit with a Vitest decode test.
</success_criteria>

<output>
Create `.planning/phases/137-promotion-into-occurrences/137-02-SUMMARY.md` when done.
</output>
