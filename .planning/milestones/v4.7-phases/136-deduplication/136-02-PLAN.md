---
phase: 136-deduplication
plan: 02
type: execute
wave: 2
depends_on: ["136-01"]
files_modified:
  - data/dbt/models/intermediate/int_checklist_collapsed.sql
  - data/checklist_dedup.py
files_modified_note: "Also adds isolated-DuckDB collapse-SQL execution to tests/test_checklist_dedup.py via shared helper (no new file)."
autonomous: true
requirements: [DUP-01]

must_haves:
  truths:
    - "Exact internal duplicate groups (canonical_name, lat, lon, year, month, day, recordedBy) collapse to one survivor"
    - "The survivor's ObjectID is the lowest ObjectID in its group (D-03)"
    - "The survivor carries collapsed_count = group size, 1 if unique (D-04)"
    - "NULL recordedBy rows do NOT collapse together — each forms its own group"
    - "_collectors_match implements token-set + initials-awareness with no fuzzy scoring (D-05)"
  artifacts:
    - path: "data/dbt/models/intermediate/int_checklist_collapsed.sql"
      provides: "DUP-01 internal collapse: MIN(ObjectID) survivor + COUNT(*) collapsed_count over stg_checklist__records_full"
      contains: "collapsed_count"
    - path: "data/checklist_dedup.py"
      provides: "_normalize_collector + _collectors_match token-set helpers (D-05)"
      contains: "def _collectors_match"
  key_links:
    - from: "data/dbt/models/intermediate/int_checklist_collapsed.sql"
      to: "stg_checklist__records_full"
      via: "ref() GROUP BY collapse"
      pattern: "ref\\('stg_checklist__records_full'\\)"
---

<objective>
Implement DUP-01 internal collapse as the `int_checklist_collapsed` dbt model (lowest-ObjectID survivor + collapsed_count over the five exact-match keys), and implement the net-new token-set collector normalization helpers (`_normalize_collector` / `_collectors_match`) in `checklist_dedup.py`. This greens the three DUP-01 tests plus the collector-normalization test.

Purpose: Collapse must land BEFORE candidate generation (136-03) so the surviving ObjectID is the stable key a curator confirms against (pair_key stability invariant, D-02/D-03). The collector helpers are needed by the candidate writer in 136-03; building them here keeps that plan focused on the spatial join.
Output: One implemented dbt model, two implemented Python helpers, four tests turned green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/136-deduplication/136-CONTEXT.md
@.planning/phases/136-deduplication/136-RESEARCH.md
@.planning/phases/136-deduplication/136-PATTERNS.md
@.planning/phases/136-deduplication/136-01-SUMMARY.md

<interfaces>
<!-- Column shape int_checklist_collapsed must emit (from stg_checklist__records_full) -->
Input columns (stg_checklist__records_full):
  ObjectID, verbatim_name, canonical_name, lat, lon,
  year, month, day, date_quality, recordedBy, locality, family, coord_flag, taxon_id

Output (int_checklist_collapsed):
  ObjectID (= MIN per group), canonical_name, lat, lon, year, month, day,
  date_quality, recordedBy, verbatim_name, locality, family, coord_flag, taxon_id,
  collapsed_count (= COUNT(*) per group)

Collector helper signatures (from 136-01 stubs):
  _normalize_collector(name: str | None) -> frozenset[str]
  _collectors_match(a: str | None, b: str | None) -> bool   # token-set + initials, no fuzzy
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement int_checklist_collapsed (DUP-01 collapse, D-03 lowest-ObjectID, D-04 collapsed_count)</name>
  <files>data/dbt/models/intermediate/int_checklist_collapsed.sql</files>
  <read_first>
    - data/dbt/models/intermediate/int_checklist_collapsed.sql (the placeholder from 136-01 being implemented)
    - data/dbt/models/intermediate/stg_checklist__records_full.sql (input columns + the coord_flag='valid' filter already applied — confirm NULL-coord rows are already excluded; do not double-handle)
    - data/dbt/models/intermediate/int_combined.sql (materialized='table' config line 11 — the materialization analog for an expensive base)
    - .planning/phases/136-deduplication/136-PATTERNS.md (lines 45-73 — the verified GROUP BY survivor + COALESCE(recordedBy, ...) NULL-handling pattern)
    - .planning/phases/136-deduplication/136-RESEARCH.md (lines 177-237, 573-599 — collapse SQL + Anti-Pattern 3 NULL recordedBy + Pitfall 5 the 5,184 figure is a hint not the assertion)
  </read_first>
  <behavior>
    - A group of N rows identical on (canonical_name, lat, lon, year, month, day, recordedBy) collapses to one row.
    - That survivor's ObjectID = MIN(ObjectID) of the group (D-03).
    - That survivor's collapsed_count = N; a unique row gets collapsed_count = 1 (D-04).
    - Two rows with NULL recordedBy at the same site/date/species do NOT collapse — each is its own group.
    - Zero (canonical_name, lat, lon, year, month, day, recordedBy) tuples remain duplicated post-collapse.
  </behavior>
  <action>
    Implement `int_checklist_collapsed.sql` with `{{ config(materialized='table') }}` (expensive base, materialize once). GROUP BY the five-key tuple `canonical_name, lat, lon, year, month, day, date_quality` plus a NULL-safe collector key `COALESCE(recordedBy, CAST(ObjectID AS VARCHAR))` so NULL-collector rows each form their own group (D-03 / Anti-Pattern 3). Use a CTE to compute the coalesced grouping key once, then `MIN(ObjectID) AS ObjectID`, carry the non-key survivor fields via `MIN(...)` (verbatim_name, locality, family, coord_flag, taxon_id, recordedBy), and `COUNT(*) AS collapsed_count` (D-04). Source is `{{ ref('stg_checklist__records_full') }}`. Replace the 136-01 placeholder body entirely.

    Do NOT use ST_Distance_Sphere here (no spatial in the collapse). Do NOT filter out year_only/none rows from the collapse — they can still be internal dupes among themselves (RESEARCH lines 580-582); the date_quality='full' filter belongs to candidate generation (136-03), not collapse. Do NOT touch int_combined / occurrences mart / the 33-column contract.
  </action>
  <verify>
    <automated>cd data && uv run pytest tests/test_checklist_dedup.py::test_no_exact_duplicates_after_collapse tests/test_checklist_dedup.py::test_collapsed_count_correct tests/test_checklist_dedup.py::test_lowest_objectid_survives -x</automated>
  </verify>
  <acceptance_criteria>
    - `pytest tests/test_checklist_dedup.py::test_no_exact_duplicates_after_collapse -x` passes (zero remaining exact-duplicate tuples).
    - `pytest tests/test_checklist_dedup.py::test_collapsed_count_correct -x` passes (collapsed_count = group size).
    - `pytest tests/test_checklist_dedup.py::test_lowest_objectid_survives -x` passes (MIN(ObjectID) survives).
    - The model begins with `{{ config(materialized='table') }}` and refs `stg_checklist__records_full`.
    - The GROUP BY uses a COALESCE collector key matching the SELECT expression (NULL-collector rows not collapsed together).
    - `grep -c "ST_Distance_Sphere" data/dbt/models/intermediate/int_checklist_collapsed.sql` returns 0.
  </acceptance_criteria>
  <done>The three DUP-01 collapse tests are green; lowest-ObjectID survivor carries collapsed_count; NULL collectors are not merged.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement token-set collector normalization (_normalize_collector / _collectors_match, D-05)</name>
  <files>data/checklist_dedup.py</files>
  <read_first>
    - data/checklist_dedup.py (the 136-01 stubs being implemented)
    - .planning/phases/136-deduplication/136-RESEARCH.md (lines 312-367 — the _normalize_collector / _collectors_match reference algorithm and the [ASSUMED] initials rule)
    - .planning/phases/136-deduplication/136-PATTERNS.md (lines 349-351, 501-505 — "No Analog Found": net-new, no existing collector normalizer; canonical_name.py normalize_scientific_name is for taxa not people)
    - data/canonical_name.py (lines 73-100 — confirm normalize_scientific_name is taxon-only; do NOT reuse it for collectors)
  </read_first>
  <behavior>
    - _normalize_collector(None) -> frozenset() (empty).
    - _normalize_collector lowercases, strips punctuation, collapses whitespace, returns a frozenset of tokens.
    - _collectors_match('J Smith', 'John Smith') -> True (initials awareness).
    - _collectors_match('Smith, J.', 'J. Smith') -> True (token-set equality, order-insensitive).
    - _collectors_match('A Jones', 'B Jones') -> False (different initial).
    - _collectors_match(None, 'John Smith') -> False (NULL is never a match — D-08 NULL ineligible).
  </behavior>
  <action>
    Implement `_normalize_collector(name)` per RESEARCH lines 322-339: return `frozenset()` for None; else lowercase, `re.sub(r'[^\w\s]', ' ', ...)` to strip punctuation, collapse whitespace, split into a token frozenset. Implement `_collectors_match(a, b)` per RESEARCH lines 341-364: return False if either is None (D-08: NULL never matches); normalize both; True if token-sets equal; else apply the initials rule — for the smaller set, every token must either be in the larger set OR be a single char that is the initial (startswith) of some token in the larger set. This is exact set logic, NO fuzzy scoring (D-05 explicitly rejects rapidfuzz scoring). Do not import rapidfuzz for scoring; stdlib `re` is sufficient.
  </action>
  <verify>
    <automated>cd data && uv run pytest tests/test_checklist_dedup.py::test_collector_normalization -x</automated>
  </verify>
  <acceptance_criteria>
    - `pytest tests/test_checklist_dedup.py::test_collector_normalization -x` passes.
    - `_collectors_match('J Smith','John Smith')` is True; `_collectors_match('A Jones','B Jones')` is False; `_collectors_match(None, 'x')` is False.
    - `grep -c "rapidfuzz" data/checklist_dedup.py` returns 0 (no fuzzy scoring import — D-05).
  </acceptance_criteria>
  <done>Collector helpers implemented with token-set + initials logic; no fuzzy scoring; collector test green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| source CSV → collapsed dbt table | Bartholomew collector strings flow into the GROUP BY collector key |

Static-hosted offline pipeline; no runtime server, no network input, no auth surface. Threat surface is data-integrity.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-136-03 | Information Disclosure / Integrity | NULL-collector rows wrongly merged | mitigate | COALESCE(recordedBy, CAST(ObjectID AS VARCHAR)) grouping key keeps NULL-collector rows distinct (D-03, Anti-Pattern 3); test_lowest_objectid_survives + collapse tests guard it |
| T-136-04 | Integrity | Over-merged collectors via fuzzy logic | accept | D-05 mandates exact token-set (no fuzzy scoring); false-split bias means a missed match renders two points (recoverable), never a silent merge |
| T-136-SC | Tampering | npm/pip/cargo installs | mitigate | No new packages this plan; stdlib `re` only |
</threat_model>

<verification>
- `cd data && uv run pytest tests/test_checklist_dedup.py -x` — DUP-01 + collector tests green (candidate/status/gate tests still red until 136-03/04).
- `cd data && uv run pytest -m 'not integration'` — no regressions in the fast tier.
- No change to int_combined / occurrences mart / contract.
</verification>

<success_criteria>
- int_checklist_collapsed emits lowest-ObjectID survivor + collapsed_count; NULL collectors not merged.
- _normalize_collector / _collectors_match implement token-set + initials with no fuzzy scoring.
- The three DUP-01 tests and the collector test are green.
</success_criteria>

<output>
Create `.planning/phases/136-deduplication/136-02-SUMMARY.md` when done
</output>
