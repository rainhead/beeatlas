---
phase: 138-frontend-points-detail-card
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/tests/url-state.test.ts
  - src/tests/bee-occurrence-detail.test.ts
  - src/tests/build-geojson.test.ts
  - data/tests/test_species_checklist_count.py
autonomous: true
requirements: [UIX-01, UIX-02, UIX-03, UIX-04]

must_haves:
  truths:
    - "Wave 0 test scaffolds exist so every UIX-01..04 implementation task has an automated verify command (Nyquist)"
    - "url-state.test.ts encodes the 4-source VALID_SOURCES expectation and a src=checklist round-trip (D-11)"
    - "bee-occurrence-detail.test.ts asserts formatRomanDate null / length-4 / length-7 behavior (D-08)"
    - "test_species_checklist_count.py asserts species checklist_count equals the deduped int_checklist_dedup_status count (UIX-04)"
  artifacts:
    - path: "src/tests/url-state.test.ts"
      provides: "Updated MAP-03 expectations for 4 sources + src=checklist round-trip"
    - path: "src/tests/bee-occurrence-detail.test.ts"
      provides: "formatRomanDate precision/null unit tests"
      min_lines: 20
    - path: "src/tests/build-geojson.test.ts"
      provides: "Assertion that checklist point GeoJSON carries source='checklist'"
    - path: "data/tests/test_species_checklist_count.py"
      provides: "UIX-04 deduped-count integration assertion"
      min_lines: 20
  key_links:
    - from: "src/tests/url-state.test.ts"
      to: "VALID_SOURCES"
      via: "parseParams('src=ecdysis') complement assertion"
      pattern: "checklist"
---

<objective>
Create the Wave 0 test scaffolds named in 138-VALIDATION.md so every downstream implementation task (Plans 02–04) has an automated verify command. These tests are written to the TARGET behavior (4-source VALID_SOURCES, date precision, deduped count) and therefore start RED until the implementation plans land — that is the intended Nyquist gate.

Purpose: Without these scaffolds, three of the five per-requirement verifications in VALIDATION.md have no file to run. This plan closes the Wave 0 gaps first.
Output: Updated/created test files; running them now demonstrates the expected RED state with messages that name the missing behavior.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/138-frontend-points-detail-card/138-VALIDATION.md
@.planning/phases/138-frontend-points-detail-card/138-RESEARCH.md
@.planning/phases/138-frontend-points-detail-card/138-CONTEXT.md

<interfaces>
<!-- Extracted from codebase — executor should use these directly. -->
src/url-state.ts (current): `export type SourceKey = 'ecdysis' | 'waba_sample' | 'inat_obs'` and
`const VALID_SOURCES = new Set<SourceKey>(['ecdysis', 'waba_sample', 'inat_obs'])` (lines 32–34).
`parseParams(search: string)` returns `{ ui?: { hiddenSources?: Set<SourceKey> } }`; `hiddenSources` is the COMPLEMENT of the `src=` visible set against VALID_SOURCES.

src/features.ts line 45 emits `properties: { occId, recencyTier, source: source ?? '' }`; `source` is decoded from `row[6]`. Checklist rows have `source === 'checklist'`.

VALIDATION.md names `src/tests/features.test.ts`, but that file does NOT exist. The actual GeoJSON-building test is `src/tests/build-geojson.test.ts`, which already imports the build function and references `source`. Add the checklist-source assertion THERE — do not create a new features.test.ts.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update url-state tests for 4-source VALID_SOURCES + src=checklist round-trip</name>
  <files>src/tests/url-state.test.ts</files>
  <read_first>
    - src/tests/url-state.test.ts (the file being modified — read the MAP-03 describe block, esp. lines ~410–425 where 'src=ecdysis' produces hiddenSources)
    - src/url-state.ts lines 32–34 (VALID_SOURCES), ~90–100 (buildParams src=/cl=), ~265–305 (parseParams)
    - .planning/phases/138-frontend-points-detail-card/138-RESEARCH.md §"Pitfall 3" and §"VALID_SOURCES extension"
    - .planning/phases/138-frontend-points-detail-card/138-PATTERNS.md §"src/url-state.ts"
  </read_first>
  <action>In the existing `describe('MAP-03: source filter URL param (src=)')` block: update the assertion where `parseParams('src=ecdysis')` is expected to yield `hiddenSources` — the expected set becomes `new Set(['inat_obs', 'waba_sample', 'checklist'])` (was the 2-member set). Update the "two hidden sources" case similarly so the complement accounts for `checklist` (per D-11). Add a NEW test asserting a `src=checklist` round-trip: `buildParams` for a UiState whose only visible source is `checklist` produces a `src=` containing `checklist`, and `parseParams` of that string yields `hiddenSources` = the other three sources. Add an assertion that the parsed VALID_SOURCES universe has 4 members by checking `parseParams('src=ecdysis')` hides exactly 3. These tests target post-implementation behavior and will be RED until Plan 03 ships.</action>
  <verify>
    <automated>cd /home/peter/dev/beeatlas && npm test -- --run src/tests/url-state.test.ts 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: the test file contains the literal `'checklist'` inside the MAP-03 describe block's expected `hiddenSources` set.
    - Behavior assertion: a new test named for `src=checklist` round-trip exists and references both `buildParams` and `parseParams`.
    - Expected RED: running the command shows the new/updated assertions failing (VALID_SOURCES still has 3 members), with failure messages naming `checklist`. RED here is correct for Wave 0.
  </acceptance_criteria>
  <done>url-state.test.ts encodes the 4-source complement + src=checklist round-trip; runs RED against current 3-source code.</done>
</task>

<task type="auto">
  <name>Task 2: Create formatRomanDate precision/null unit tests</name>
  <files>src/tests/bee-occurrence-detail.test.ts, src/bee-occurrence-detail.ts</files>
  <read_first>
    - src/bee-occurrence-detail.ts lines 1–14 (ROMAN_MONTHS array + current formatRomanDate at line 9)
    - .planning/phases/138-frontend-points-detail-card/138-UI-SPEC.md §"formatRomanDate Extension"
    - .planning/phases/138-frontend-points-detail-card/138-PATTERNS.md §"src/bee-occurrence-detail.ts" (formatRomanDate current form)
    - An existing component test for import/setup style, e.g. src/tests/occurrence.test.ts
  </read_first>
  <action>Create `src/tests/bee-occurrence-detail.test.ts`. `formatRomanDate` is currently module-private (not exported). Export it from `bee-occurrence-detail.ts` as part of this task (add `export` to the function declaration) so it is unit-testable, then import it in the test. Write a `describe('formatRomanDate', ...)` with cases per D-08: full date `'2019-06-15'` → `'15 VI 2019'`; year-only `'2019'` (length 4) → `'2019'`; month-precision `'2019-06'` (length 7) → `'VI 2019'`; null → `''`; empty string → `''`. These target the UI-SPEC-extended signature `(dateStr: string | null) => string` and will be RED until Plan 04 extends the function.</action>
  <verify>
    <automated>cd /home/peter/dev/beeatlas && npm test -- --run src/tests/bee-occurrence-detail.test.ts 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: `src/tests/bee-occurrence-detail.test.ts` exists and imports `formatRomanDate` from `../bee-occurrence-detail`.
    - Behavior assertion: file contains the five cases (full, year-only, month, null, empty) with the exact expected strings `'15 VI 2019'`, `'2019'`, `'VI 2019'`, `''`.
    - Expected RED: null/length-4/length-7 cases fail against the current implementation (which throws or mishandles them).
  </acceptance_criteria>
  <done>bee-occurrence-detail.test.ts asserts the five formatRomanDate cases; formatRomanDate is exported; tests run RED.</done>
</task>

<task type="auto">
  <name>Task 3: Add checklist source-property GeoJSON assertion + UIX-04 deduped-count pytest</name>
  <files>src/tests/build-geojson.test.ts, data/tests/test_species_checklist_count.py</files>
  <read_first>
    - src/tests/build-geojson.test.ts (existing GeoJSON test that already references `source`)
    - src/features.ts lines 14–46 (_GEO_COLS layout, source at row[6], properties emit at line 45)
    - data/tests/test_checklist_pipeline.py (analog data integration test — connection/fixture setup pattern)
    - data/tests/conftest.py (pytest fixtures / DuckDB connection helpers)
    - .planning/phases/138-frontend-points-detail-card/138-RESEARCH.md §1 (UIX-04 deduped count: 19,929 rows; the dedup_status / lat / lon filter)
    - .planning/phases/138-frontend-points-detail-card/138-PATTERNS.md §"int_species_universe.sql"
  </read_first>
  <action>(a) In `src/tests/build-geojson.test.ts`, add a test that builds GeoJSON from a raw row whose `source` (index 6) is `'checklist'` and asserts the resulting feature's `properties.source === 'checklist'` (UIX-01 — the paint expression keys on this). This assertion describes behavior that already exists in features.ts, so it may pass immediately; that is fine — it is the regression guard for the paint contract. (b) Create `data/tests/test_species_checklist_count.py` following the connection/fixture pattern in test_checklist_pipeline.py. Assert that for each `canonical_name`, the `checklist_count` materialized in `species.parquet` (or the `species` mart) equals `COUNT(*)` from `int_checklist_dedup_status WHERE canonical_name IS NOT NULL AND dedup_status IS DISTINCT FROM 'confirmed' AND lat IS NOT NULL AND lon IS NOT NULL` grouped by canonical_name. Keep the query scoped to a single file (host kills long Bash runs — never run the whole data suite). This targets the Plan 02 CTE fix and will be RED until then (current count reads the 42k county mart).</action>
  <verify>
    <automated>cd /home/peter/dev/beeatlas && npm test -- --run src/tests/build-geojson.test.ts 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: build-geojson.test.ts contains a test asserting `properties.source` equals `'checklist'` for a checklist-source input row.
    - Source assertion: `data/tests/test_species_checklist_count.py` exists and contains the literal filter `dedup_status IS DISTINCT FROM 'confirmed'` and reads `int_checklist_dedup_status`.
    - Behavior assertion (build-geojson): the new GeoJSON assertion passes (source already emitted).
    - The pytest file is structurally valid (collectable): `cd data && uv run pytest tests/test_species_checklist_count.py --collect-only -q` lists at least one test without import errors. It may be RED on assertion until Plan 02.
  </acceptance_criteria>
  <done>build-geojson.test.ts guards source emission (green); test_species_checklist_count.py is collectable and asserts the deduped-count equality (RED until Plan 02).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none in this plan) | Test-only changes; no runtime surface, no new inputs, no dependencies. |

## STRIDE Threat Register

BeeAtlas is a static-hosted, read-only public frontend plus an offline nightly dbt pipeline: no server runtime, no auth, no user-input persistence, no PII. This plan adds only test files.

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-138-01 | Tampering | Test scaffolds asserting wrong contract | accept | Tests are reviewed in plan; assertions trace to D-08/D-11/UIX-04 with exact expected strings. |
| T-138-SC | Tampering | npm/pip/cargo installs | mitigate | No packages installed in this phase (RESEARCH.md: "zero new packages"). Nothing to verify. |

Spoofing / Repudiation / Information-Disclosure / DoS / Elevation: N/A — no network endpoint, no auth, no secret, no privileged operation introduced.
</threat_model>

<verification>
- `npm test -- --run src/tests/url-state.test.ts src/tests/bee-occurrence-detail.test.ts src/tests/build-geojson.test.ts` runs; the build-geojson source assertion is green; url-state and detail assertions are RED in the ways named above (expected pre-implementation).
- `cd data && uv run pytest tests/test_species_checklist_count.py --collect-only -q` collects without import error.
</verification>

<success_criteria>
- All four Wave 0 test artifacts exist (one updated, one created, one extended, one created).
- formatRomanDate is exported.
- Each downstream requirement (UIX-01..04) now has a named automated verify command per VALIDATION.md.
</success_criteria>

<output>
Create `.planning/phases/138-frontend-points-detail-card/138-01-SUMMARY.md` when done.
</output>
