---
phase: 138-frontend-points-detail-card
plan: 03
type: execute
wave: 1
depends_on: [138-01]
files_modified:
  - src/style.ts
  - src/url-state.ts
  - src/bee-pane.ts
  - src/bee-atlas.ts
  - src/bee-map.ts
autonomous: true
requirements: [UIX-01, UIX-02]

must_haves:
  truths:
    - "Unclustered checklist points render flat green #2c7a2c overriding the recency scheme (D-01, D-02), keyed on the source feature property"
    - "Checklist points cluster with the other three sources; clusters stay recency-colored (D-03)"
    - "Selected checklist points keep green fill + standard white stroke (D-04)"
    - "The checklist-county-fill layer and its _checklistAllRows/county-aggregation/_showChecklist plumbing are fully removed across style/bee-map/bee-pane/bee-atlas/url-state (UIX-02)"
    - "checklist is a real entry in VALID_SOURCES so src=checklist round-trips and the no-sources guard counts 4 sources (D-11, D-12)"
    - "The checklist source flows through the existing hiddenSources path, not a new ad-hoc property chain (architecture invariant)"
  artifacts:
    - path: "src/style.ts"
      provides: "_occurrencePointPaint source-keyed green override; checklistCountyFillLayerSpec removed"
      contains: "#2c7a2c"
    - path: "src/url-state.ts"
      provides: "VALID_SOURCES with 4 members incl. checklist; cl= param removed"
      contains: "checklist"
    - path: "src/bee-pane.ts"
      provides: "checklist source toggle via _onSourceToggle; no-sources threshold 4"
    - path: "src/bee-map.ts"
      provides: "county-fill plumbing removed; checklist via standard source filter"
  key_links:
    - from: "src/style.ts"
      to: "features.ts source property"
      via: "match on ['get','source']"
      pattern: "'get', 'source'"
    - from: "src/bee-pane.ts"
      to: "src/bee-atlas.ts"
      via: "source-filter-changed event → hiddenSources"
      pattern: "source-filter-changed"
---

<objective>
Make checklist render as real green map points and fold the checklist source into the standard source-selection machinery: extend the point paint with a source-keyed green override, add `checklist` to `VALID_SOURCES`, and delete the entire retired county-fill / `_showChecklist` property chain so checklist flows through `hiddenSources` exactly like the other three sources.

Purpose: Satisfies UIX-01 (distinct green points that cluster + obey the taxon filter) and UIX-02 (county-fill gone; checklist a real source in the toggle set, the no-sources guard, and the URL). This is the frontend integration slice — independent of the data-pipeline slice (Plan 02) and the detail card (Plan 04).
Output: Green checklist points, simplified source-state, src=checklist round-trip.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/138-frontend-points-detail-card/138-UI-SPEC.md
@.planning/phases/138-frontend-points-detail-card/138-PATTERNS.md
@.planning/phases/138-frontend-points-detail-card/138-RESEARCH.md
@CLAUDE.md

<interfaces>
<!-- Extracted from codebase. -->
src/style.ts: `_occurrencePointPaint(colors)` (lines 87–99) returns the circle paint; both
`unclusteredPointLayerSpec` (line 101) and `selectedOccurrencesLayerSpec` (line 111) call it, so a
single edit to the circle-color expression covers points AND selection (D-04). `checklistCountyFillLayerSpec()`
(lines 224–236) returns the fill layer to delete.

src/url-state.ts: `SourceKey` union + `VALID_SOURCES` set (lines 32–34); `buildParams` serializes `src=`
(and the `cl=` checklist flag at ~line 95); `parseParams` computes `hiddenSources` as the complement of
the visible `src=` set against VALID_SOURCES (~lines 267–305); `UiState.checklistVisible?` field to remove.

src/bee-pane.ts: `_onSourceToggle(sourceValue, checked)` (lines 627–635) dispatches
`source-filter-changed` with `{ hiddenSources }`. `_renderSources` (lines 1111–1158) builds toggle rows;
the checklist row currently uses `_showChecklist`/`_onChecklistChange`. No-sources guard at line 1189
checks `=== 3`. State to delete: `_showChecklist` (115), `checklistVisible` property (84), `_onChecklistChange` (618–625), the `checklistVisible` updated() branch (513–514).

src/bee-atlas.ts: owns reactive state (architecture invariant). `_onSourceFilterChanged` (1059–1062)
sets `_hiddenSources` + `_replaceUrlState()`. To delete: `_checklistVisible` state (41),
`_onChecklistLayerChanged` (1054–1057), template bindings `.showChecklist=`/`.checklistVisible=`/
`@checklist-layer-changed=`/`.checklistTaxon`/`.checklistTaxonRank` (178, 180–181, 214, 218),
and `_checklistVisible` usages (252, 608, 670).

src/bee-map.ts: `hiddenSources` prop (60) is the path checklist transitions to. To delete:
`showChecklist`/`checklistTaxon`/`checklistTaxonRank` props (59, 61–62), `_checklistCounties`/
`_checklistAllRows`/`_checklistGeneration` (67–69), updated() guard (345), county-fill layer add in
_initMap (436–441), private methods `_applyChecklistLayer`/`_applyChecklistVisibility`/
`_applyChecklistFilter`/`_loadChecklistData` (704–765), the `checklistCountyFillLayerSpec` import, and any
`'checklist-county-fill'` reference in the map click handler.

features.ts already emits `source` in feature properties (line 45) — no change; the paint match keys on it.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add checklist to VALID_SOURCES + green point paint override; remove county-fill spec</name>
  <files>src/url-state.ts, src/style.ts</files>
  <read_first>
    - src/url-state.ts lines 32–34 (SourceKey/VALID_SOURCES), ~39 (UiState.checklistVisible?), ~95 (cl= serialize), ~265–305 (parseParams complement + cl= parse)
    - src/style.ts lines 87–99 (_occurrencePointPaint), 101–115 (unclustered + selected specs that call it), 224–236 (checklistCountyFillLayerSpec to delete)
    - src/features.ts lines 14–46 (source emitted at row[6], property at line 45)
    - .planning/phases/138-frontend-points-detail-card/138-UI-SPEC.md §"Mapbox Paint Contract" and §"VALID_SOURCES"
    - .planning/phases/138-frontend-points-detail-card/138-PATTERNS.md §"src/style.ts" and §"src/url-state.ts"
    - src/tests/url-state.test.ts (the Plan 01 assertions this turns green)
  </read_first>
  <action>In `url-state.ts`: add `'checklist'` to the `SourceKey` union and to the `VALID_SOURCES` set (D-11). Remove the `cl=` serialization line in `buildParams` and the `cl=` parse in `parseParams`, and drop `checklistVisible?` from the `UiState` interface (per RESEARCH.md Open Question 1 recommendation: remove entirely; stale `cl=` bookmarks harmlessly no-op since checklist is now a default-on source). In `style.ts`: replace the `'circle-color'` entry of `_occurrencePointPaint` with a source-keyed outer `match` — `['match', ['get','source'], 'checklist', '#2c7a2c', <existing recencyTier match>]` (D-01/D-02; exact hex from UI-SPEC). Leave `circle-radius: 6`, `circle-stroke-width: 1`, `circle-stroke-color: '#ffffff'`, opacity solid (D-04). Both unclustered and selected specs inherit this automatically. Delete `checklistCountyFillLayerSpec` (body + export). Honor the style-cache invariant: this is a static expression, so no cache-key change is required, but do NOT introduce a cached branch that ignores filterState/selectedOccIds.</action>
  <verify>
    <automated>cd /home/peter/dev/beeatlas && npm test -- --run src/tests/url-state.test.ts 2>&1 | tail -15 && npx tsc --noEmit 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: `grep -n "'checklist'" src/url-state.ts` shows checklist in both SourceKey and VALID_SOURCES.
    - Source assertion: `style.ts` contains `'#2c7a2c'` inside a `match` on `['get', 'source']`; `checklistCountyFillLayerSpec` no longer appears (`grep -c checklistCountyFillLayerSpec src/style.ts` returns 0).
    - Source assertion: no `cl=` / `checklistVisible` remains in url-state.ts.
    - Behavior assertion: the Plan 01 url-state MAP-03 + src=checklist round-trip tests now PASS (4-source complement correct).
    - Behavior assertion: `npx tsc --noEmit` reports no new errors from this file (SourceKey union widened — Pitfall 2).
  </acceptance_criteria>
  <done>checklist is a real VALID_SOURCES member with src= round-trip; point paint emits green for source='checklist'; county-fill spec deleted; url-state tests green.</done>
</task>

<task type="auto">
  <name>Task 2: Migrate bee-pane checklist toggle to hiddenSources + update no-sources threshold</name>
  <files>src/bee-pane.ts</files>
  <read_first>
    - src/bee-pane.ts lines 84 (checklistVisible property), 115 (_showChecklist), 513–514 (updated() checklist branch), 618–625 (_onChecklistChange), 627–635 (_onSourceToggle pattern), 1111–1158 (_renderSources), 1131–1136 (checklist toggle entry), 1189 (no-sources === 3)
    - .planning/phases/138-frontend-points-detail-card/138-UI-SPEC.md §"Source Toggle Integration" + §"Copywriting Contract" (tooltip text)
    - .planning/phases/138-frontend-points-detail-card/138-PATTERNS.md §"src/bee-pane.ts"
    - src/tests/bee-pane.test.ts (no-sources threshold test to keep green)
  </read_first>
  <action>In `_renderSources`, replace the checklist toggle entry: keep `label: 'Checklist records'` (D-12), set `tooltip: 'Published specimen records from Bartholomew et al. 2024'`, `checked: !this._hiddenSources.has('checklist')`, `onChange: (e) => this._onSourceToggle('checklist', (e.target as HTMLInputElement).checked)`. Order checklist last in the 4-source list per UI-SPEC. Change the no-sources guard from `this._hiddenSources.size === 3` to `=== 4` (D-11). Delete: the `_showChecklist` @state (115), the `checklistVisible` @property (84), the `_onChecklistChange` method (618–625), and the `checklistVisible` branch in `updated()` (513–514). bee-pane is a pure presenter — emit `source-filter-changed`; do not hold checklist state locally beyond reading `_hiddenSources`.</action>
  <verify>
    <automated>cd /home/peter/dev/beeatlas && npm test -- --run src/tests/bee-pane.test.ts 2>&1 | tail -15 && npx tsc --noEmit 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: `grep -c "_showChecklist\|_onChecklistChange\|checklistVisible" src/bee-pane.ts` returns 0.
    - Source assertion: bee-pane.ts contains `_onSourceToggle('checklist'` and the `=== 4` no-sources threshold.
    - Behavior assertion: `bee-pane.test.ts` passes (no-sources fires at 4 hidden sources).
    - Behavior assertion: `npx tsc --noEmit` clean for bee-pane.ts.
  </acceptance_criteria>
  <done>The checklist toggle routes through _onSourceToggle/hiddenSources; no-sources guard counts 4; all _showChecklist plumbing removed from bee-pane; tests green.</done>
</task>

<task type="auto">
  <name>Task 3: Remove checklist county-fill plumbing from bee-atlas + bee-map</name>
  <files>src/bee-atlas.ts, src/bee-map.ts</files>
  <read_first>
    - src/bee-atlas.ts lines 41 (_checklistVisible), 178/180–181/214/218 (template bindings), 252/608/670 (_checklistVisible usages), 1054–1057 (_onChecklistLayerChanged), 1059–1062 (_onSourceFilterChanged — the path to keep)
    - src/bee-map.ts lines 59/61–62 (props), 67–69 (state), 345 (updated guard), 436–441 (layer add), 704–765 (the four _applyChecklist*/_loadChecklistData methods), plus the import of checklistCountyFillLayerSpec and any 'checklist-county-fill' in the click handler
    - .planning/phases/138-frontend-points-detail-card/138-PATTERNS.md §"src/bee-atlas.ts" and §"src/bee-map.ts"
    - CLAUDE.md §"Architecture Invariants" (state ownership, style cache, filter race guard)
    - src/tests/bee-map.test.ts and src/tests/bee-atlas.test.ts
  </read_first>
  <action>In `bee-atlas.ts`: delete the `_checklistVisible` @state (41) and `_onChecklistLayerChanged` (1054–1057); remove the template bindings `.showChecklist=`, `.checklistVisible=`, `@checklist-layer-changed=`, `.checklistTaxon`, `.checklistTaxonRank` (178, 180–181, 214, 218); remove the `_checklistVisible` init from parseParams restore (252), the `checklistVisible:` field from the `_replaceUrlState` UiState literal (608), and the restore at 670. Leave `_onSourceFilterChanged`/`_hiddenSources` untouched — that is the surviving path. In `bee-map.ts`: delete props `showChecklist`/`checklistTaxon`/`checklistTaxonRank` (59, 61–62), state `_checklistCounties`/`_checklistAllRows`/`_checklistGeneration` (67–69), the county-fill clauses in the `updated()` guard (345), the `checklistCountyFillLayerSpec()` add block in `_initMap` (436–441), the four private methods `_applyChecklistLayer`/`_applyChecklistVisibility`/`_applyChecklistFilter`/`_loadChecklistData` (704–765), the `checklistCountyFillLayerSpec` import, and any `'checklist-county-fill'` reference in the map click handler. Checklist now flows through the existing `hiddenSources` → `source IN (...)` filter path; do not add replacement plumbing.</action>
  <verify>
    <automated>cd /home/peter/dev/beeatlas && npx tsc --noEmit 2>&1 | tail -20 && npm test -- --run src/tests/bee-map.test.ts src/tests/bee-atlas.test.ts 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: `grep -c "showChecklist\|_checklistVisible\|_checklistAllRows\|checklist-county-fill\|checklistCountyFillLayerSpec\|_loadChecklistData" src/bee-map.ts src/bee-atlas.ts` returns 0.
    - Source assertion: `_onSourceFilterChanged` and `_hiddenSources` still present in bee-atlas.ts (the surviving path).
    - Behavior assertion: `npx tsc --noEmit` is clean across the whole project (no dangling references to the deleted symbols).
    - Behavior assertion: `bee-map.test.ts` + `bee-atlas.test.ts` pass.
  </acceptance_criteria>
  <done>All checklist county-fill / _showChecklist plumbing removed from bee-atlas and bee-map; project typechecks; checklist routes solely through hiddenSources; component tests green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| URL query params (`src=`) → client state | Bounded enum parse: only members of VALID_SOURCES survive `parseParams`; unknown tokens are dropped. No injection sink. |

## STRIDE Threat Register

Static-hosted, read-only public frontend. No server, no auth, no PII, no persistence.

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-138-04 | Tampering | `src=` URL param parsing | mitigate | `parseParams` filters against the VALID_SOURCES enum (`VALID_SOURCES.has(...)`); arbitrary param values cannot inject sources or sinks. Bounded set of 4 strings. |
| T-138-05 | Information Disclosure | Map paint expression | accept | Paint is a static client-side expression over already-public published data; reveals nothing not already on the map. |
| T-138-SC | Tampering | npm/pip/cargo installs | mitigate | No packages installed (RESEARCH.md: zero new packages). N/A. |

Spoofing / Repudiation / DoS / Elevation: N/A — no endpoint, no auth, no privileged operation. The mapbox `match` expression takes no user input beyond the bounded `source` feature property.
</threat_model>

<verification>
- `npx tsc --noEmit` clean (the SourceKey union widening + symbol deletions all resolve).
- `npm test -- --run src/tests/url-state.test.ts src/tests/bee-pane.test.ts src/tests/bee-map.test.ts src/tests/bee-atlas.test.ts` all green.
- Manual (per VALIDATION.md): `npm run dev` → checklist points are flat green when unclustered, join recency-colored clusters when zoomed out, respond to the taxon filter; no translucent county polygons remain; toggling all four sources off reports zero.
</verification>

<success_criteria>
- Checklist points render green (D-01/D-02), cluster (D-03), select green (D-04).
- County-fill layer + all _showChecklist plumbing gone (UIX-02).
- checklist is a real VALID_SOURCES member; src=checklist round-trips; no-sources guard counts 4 (D-11/D-12).
</success_criteria>

<output>
Create `.planning/phases/138-frontend-points-detail-card/138-03-SUMMARY.md` when done.
</output>
