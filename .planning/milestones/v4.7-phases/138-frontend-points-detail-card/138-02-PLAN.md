---
phase: 138-frontend-points-detail-card
plan: 02
type: execute
wave: 1
depends_on: [138-01]
files_modified:
  - data/dbt/models/intermediate/int_combined.sql
  - data/dbt/models/marts/schema.yml
  - data/dbt/models/intermediate/int_species_universe.sql
autonomous: true
requirements: [UIX-03, UIX-04]

must_haves:
  truths:
    - "verbatim_name, locality, collapsed_count are promoted into the occurrences contract; ARMs 1–3 emit typed NULLs, ARM 4 selects real values (D-10)"
    - "The enforced dbt contract column count is bumped 34 → 37 in marts/schema.yml (D-10)"
    - "checklist_count is re-sourced from int_checklist_dedup_status (deduped/coord-bearing) so it equals the promoted point-record count, not the 42k county mart (UIX-04)"
    - "bash data/dbt/run.sh build passes the 37-column contract gate"
  artifacts:
    - path: "data/dbt/models/intermediate/int_combined.sql"
      provides: "ARM 4 selects verbatim_name/locality/collapsed_count::INTEGER; ARMs 1–3 typed NULL casts"
      contains: "verbatim_name"
    - path: "data/dbt/models/marts/schema.yml"
      provides: "37-column occurrences contract incl. the 3 new entries"
      contains: "collapsed_count"
    - path: "data/dbt/models/intermediate/int_species_universe.sql"
      provides: "checklist_count_agg re-sourced from int_checklist_dedup_status"
      contains: "int_checklist_dedup_status"
  key_links:
    - from: "data/dbt/models/intermediate/int_combined.sql"
      to: "data/dbt/models/marts/schema.yml"
      via: "column count must match contract"
      pattern: "verbatim_name|locality|collapsed_count"
    - from: "data/dbt/models/intermediate/int_species_universe.sql"
      to: "int_checklist_dedup_status"
      via: "checklist_count_agg CTE ref"
      pattern: "ref\\('int_checklist_dedup_status'\\)"
---

<objective>
Promote the three checklist detail-card columns (`verbatim_name`, `locality`, `collapsed_count`) into the `occurrences` contract (34 → 37) and fix `checklist_count` to count the deduped, promoted record set instead of the retired 42k county mart. This is the upstream data slice: the frontend detail card (Plan 04) reads these columns straight off the occurrence row, and species/taxon pages read the corrected count.

Purpose: Satisfies UIX-04 (counts equal deduped record count, no double-count) and supplies the contract columns UIX-03's card needs. Must ship as one wave because the contract gate (`run.sh build`) fails until both `int_combined.sql` and `schema.yml` agree.
Output: 37-column occurrences contract; corrected `checklist_count`; passing dbt build.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/138-frontend-points-detail-card/138-RESEARCH.md
@.planning/phases/138-frontend-points-detail-card/138-PATTERNS.md
@CLAUDE.md

<interfaces>
<!-- Extracted from codebase. -->
int_combined.sql is a 4-ARM UNION ALL. Each ARM's LAST select column today is `checklist_id`
(ARM1 line 47, ARM2 line 103, ARM3 line 182, ARM4 line 240) with NO trailing comma.
To insert 3 new columns before the closing FROM, ADD a trailing comma to the existing `checklist_id`
line and append the 3 new lines after it (so checklist_id is no longer last).

int_checklist_dedup_status confirmed columns (DESCRIBE): `verbatim_name VARCHAR`, `locality VARCHAR`,
`collapsed_count BIGINT` (cast `::INTEGER` for the contract — DuckDB COUNT(*) returns BIGINT).
ARM 4 already aliases this ref as `cl` and selects `cl.canonical_name`, `cl.ObjectID::INTEGER AS checklist_id`.

int_species_universe.sql checklist_count_agg CTE (lines 44–51) currently reads `ref('checklist')`
(county mart, 42,218 rows). The sibling inat_obs_count_agg CTE (lines 56–63) is the analog for
reading an intermediate ref directly to avoid the external-parquet circular DAG.

CLAUDE.md invariant: the dbt contract column count is the SOLE enforcement gate (no JS validator),
checked at `bash data/dbt/run.sh build`. Current baseline = 34 columns → bump to 37.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Promote verbatim_name/locality/collapsed_count in int_combined ARMs + bump schema.yml to 37</name>
  <files>data/dbt/models/intermediate/int_combined.sql, data/dbt/models/marts/schema.yml</files>
  <read_first>
    - data/dbt/models/intermediate/int_combined.sql lines 13–61 (ARM 1, checklist_id at 47), 63–146 (ARM 2, line 103), 148–195 (ARM 3, line 182), 197–244 (ARM 4, checklist_id at 240, FROM ref('int_checklist_dedup_status') cl at 241)
    - data/dbt/models/marts/schema.yml lines 72–75 (checklist_id entry, taxon_id entry that follows)
    - data/dbt/models/intermediate/int_checklist_collapsed.sql (source of verbatim_name/locality/collapsed_count)
    - .planning/phases/138-frontend-points-detail-card/138-PATTERNS.md §"int_combined.sql" and §"schema.yml"
    - .planning/phases/138-frontend-points-detail-card/138-CONTEXT.md §D-10
  </read_first>
  <action>In `int_combined.sql`: for ARMs 1, 2, and 3, add a trailing comma to each ARM's existing `NULL::INTEGER AS checklist_id` line, then append three lines `NULL::VARCHAR AS verbatim_name`, `NULL::VARCHAR AS locality`, `NULL::INTEGER AS collapsed_count` (per D-10's typed-NULL pattern, mirroring how Phase 137 added checklist_id). For ARM 4, add a trailing comma to `cl.ObjectID::INTEGER AS checklist_id`, then append `cl.verbatim_name`, `cl.locality`, `cl.collapsed_count::INTEGER AS collapsed_count` (the `::INTEGER` cast is mandatory — collapsed_count is BIGINT from COUNT(*)). In `schema.yml`, after the `checklist_id` column block (data_type integer) and before `taxon_id`, add three column entries: `verbatim_name` (data_type varchar), `locality` (data_type varchar), `collapsed_count` (data_type integer). No data_tests on these — nullable by design (D-10).</action>
  <verify>
    <automated>cd /home/peter/dev/beeatlas/data && bash dbt/run.sh build 2>&1 | tail -25</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: `grep -c 'verbatim_name' data/dbt/models/intermediate/int_combined.sql` returns at least 4 (one per ARM).
    - Source assertion: schema.yml contains `name: verbatim_name`, `name: locality`, `name: collapsed_count` exactly once each.
    - Behavior assertion: `bash data/dbt/run.sh build` completes without a contract-mismatch error (the 37-column gate passes — proves int_combined and schema.yml agree).
    - Behavior assertion: a DuckDB query `SELECT verbatim_name, locality, collapsed_count FROM occurrences WHERE source='checklist' LIMIT 1` returns non-null example values; the same columns are NULL for a `source='ecdysis'` row.
  </acceptance_criteria>
  <done>occurrences carries 37 contract columns; checklist rows have real verbatim_name/locality/collapsed_count; other sources have NULLs; dbt build green.</done>
</task>

<task type="auto">
  <name>Task 2: Re-source checklist_count_agg from int_checklist_dedup_status (UIX-04)</name>
  <files>data/dbt/models/intermediate/int_species_universe.sql</files>
  <read_first>
    - data/dbt/models/intermediate/int_species_universe.sql lines 19–43 (checklist_month_agg — leave unchanged), 44–51 (checklist_count_agg — the CTE to replace), 56–63 (inat_obs_count_agg — the analog)
    - .planning/phases/138-frontend-points-detail-card/138-RESEARCH.md §1 "UIX-04 Double-Count Source of Truth" (the exact replacement CTE + the circular-DAG pitfall)
    - .planning/phases/138-frontend-points-detail-card/138-PATTERNS.md §"int_species_universe.sql"
    - data/tests/test_species_checklist_count.py (the Plan 01 assertion this satisfies)
  </read_first>
  <action>Replace the body of the `checklist_count_agg` CTE in `int_species_universe.sql` (lines 44–51) to read `FROM {{ ref('int_checklist_dedup_status') }}` with `WHERE canonical_name IS NOT NULL AND dedup_status IS DISTINCT FROM 'confirmed' AND lat IS NOT NULL AND lon IS NOT NULL`, grouping `COUNT(*) AS checklist_count` by `canonical_name` — the same filter ARM 4 uses, per RESEARCH.md §1. Use `ref('int_checklist_dedup_status')` NOT `ref('occurrences')` to avoid the external-parquet circular DAG (Pitfall 1). Keep a comment citing UIX-04 and noting the previous source was the 42k county mart. Do NOT touch the `checklist_month_agg` CTE above it (out of scope — month histogram still reads ref('checklist')).</action>
  <verify>
    <automated>cd /home/peter/dev/beeatlas/data && bash dbt/run.sh build 2>&1 | tail -15 && uv run pytest tests/test_species_checklist_count.py -x 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: `checklist_count_agg` CTE in int_species_universe.sql references `int_checklist_dedup_status` and contains `dedup_status IS DISTINCT FROM 'confirmed'` and `lat IS NOT NULL`.
    - Source assertion: the CTE no longer reads `ref('checklist')` (the `checklist_month_agg` CTE may still — that is a different CTE).
    - Behavior assertion: `test_species_checklist_count.py` (from Plan 01) passes — checklist_count equals the deduped count from int_checklist_dedup_status.
    - Behavior assertion: a spot-check query confirms the count dropped for a high-density species (e.g. Bombus mixtus moves from ~4,095 toward ~1,413 per RESEARCH.md), proving the re-source took effect and is not a no-op.
  </acceptance_criteria>
  <done>checklist_count on species/taxon pages equals the deduped promoted record count; the Wave 0 UIX-04 pytest is green; dbt build remains green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| committed CSV/TSV → nightly dbt build | Build-time-only ingest of already-committed, curator-reviewed checklist data. No live network, no user input. |

## STRIDE Threat Register

BeeAtlas pipeline runs offline (nightly cron on maderas), produces static parquet/sqlite consumed by a read-only public frontend. No server runtime, no auth, no PII.

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-138-02 | Tampering | int_combined column order vs schema.yml positional contract | mitigate | The dbt 37-column contract gate (`run.sh build`) blocks any drift; ARM column counts must match before the mart materializes. Verified in Task 1 acceptance. |
| T-138-03 | Information Disclosure | Promoted columns exposing data | accept | verbatim_name/locality/collector are already-published Bartholomew et al. 2024 public records; no PII or secret. |
| T-138-SC | Tampering | npm/pip/cargo installs | mitigate | No packages installed (RESEARCH.md: zero new packages). N/A. |

Spoofing / Repudiation / DoS / Elevation: N/A — offline build, no endpoint, no auth, no privileged op.
</threat_model>

<verification>
- `bash data/dbt/run.sh build` passes the 37-column contract gate.
- `uv run pytest tests/test_species_checklist_count.py -x` is green.
- Spot DuckDB checks confirm checklist detail columns populate for checklist rows and NULL elsewhere, and the re-sourced count materially differs from the old county-mart count.
</verification>

<success_criteria>
- occurrences contract = 37 columns, enforced and green.
- checklist rows expose verbatim_name/locality/collapsed_count; other sources NULL.
- checklist_count equals the deduped promoted count (UIX-04 satisfied at the data tier).
</success_criteria>

<output>
Create `.planning/phases/138-frontend-points-detail-card/138-02-SUMMARY.md` when done.
</output>
