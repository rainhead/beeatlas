---
phase: 135-name-reconciliation
plan: 04
type: execute
wave: 1
depends_on: ["135-01"]
files_modified:
  - data/dbt/models/sources.yml
  - data/dbt/models/staging/stg_checklist__records_full.sql
  - data/dbt/models/intermediate/int_synonyms.sql
  - data/dbt/seeds/schema.yml
autonomous: true
requirements: [RCN-06, RCN-07]
must_haves:
  truths:
    - "int_synonyms unifies occurrence_synonyms + auto_synonyms + gbif_checklist_synonyms with manual-wins anti-join"
    - "stg_checklist__records_full applies the synonym JOIN + taxon_id bridge over checklist_records_full"
    - "gbif_checklist_synonyms seed has not_null+unique tests on synonym"
    - "checklist_records_full is a declared dbt source"
    - "Honors D-06: the committed gbif_checklist_synonyms seed loads into DuckDB via dbt (the committed cache is a seed-into-DuckDB, not a binary .duckdb), and int_synonyms reads it on the nightly path with zero network calls"
    - "Honors D-07: all checklist synonym resolution is unified through the single int_synonyms subsystem (third UNION arm) — no separate/disjoint synonym path remains"
  decisions_covered: [D-06, D-07]
  artifacts:
    - path: "data/dbt/models/staging/stg_checklist__records_full.sql"
      provides: "Checklist staging: canonical_name + resolved taxon_id via int_synonyms + bridge"
      contains: "int_synonyms"
    - path: "data/dbt/models/intermediate/int_synonyms.sql"
      provides: "Third UNION arm for gbif_checklist_synonyms (single synonym subsystem)"
      contains: "gbif_checklist_synonyms"
    - path: "data/dbt/models/sources.yml"
      provides: "checklist_records_full source declaration"
      contains: "checklist_records_full"
  key_links:
    - from: "data/dbt/models/staging/stg_checklist__records_full.sql"
      to: "int_synonyms"
      via: "LEFT JOIN syn ON syn.synonym = cr.canonical_name"
      pattern: "ref\\('int_synonyms'\\)"
    - from: "data/dbt/models/intermediate/int_synonyms.sql"
      to: "gbif_checklist_synonyms"
      via: "UNION ALL anti-joined against occurrence_synonyms + auto_synonyms"
      pattern: "ref\\('gbif_checklist_synonyms'\\)"
---

<objective>
Wire the dbt synonym subsystem and checklist staging so all checklist name resolution flows through the single `int_synonyms` view: declare `checklist_records_full` as a source, add the GBIF seed as a third anti-joined UNION arm in `int_synonyms`, author `stg_checklist__records_full.sql` to apply the synonym JOIN + iNat taxon_id bridge over the canonical_name column, and register the new seed's tests.

Purpose: RCN-06 (single synonym subsystem; checklist resolution unified into int_synonyms) + RCN-07 prerequisite (stg model produces the canonical_name→taxon_id mapping the homonym guard checks).
Output: 1 modified sources.yml, 1 new stg model, 1 modified int_synonyms, 1 modified seed schema.yml.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/135-name-reconciliation/135-RESEARCH.md
@.planning/phases/135-name-reconciliation/135-PATTERNS.md

<interfaces>
From data/dbt/models/intermediate/int_synonyms.sql (current — two UNION arms):
  SELECT synonym, accepted_name, source FROM occurrence_synonyms
  UNION ALL <auto_synonyms anti-joined on occurrence_synonyms>

From data/dbt/models/staging/stg_checklist__species.sql (analog):
  COALESCE(syn.accepted_name, s.canonical_name) AS canonical_name
  FROM source('checklist_data','species') s LEFT JOIN ref('int_synonyms') syn ON syn.synonym = s.canonical_name

Bridge refs available: ref('stg_inat__canonical_to_taxon_id'), ref('stg_inat__genus_taxon_ids')
Seed ref consumed: ref('gbif_checklist_synonyms')   # produced by Plan 135-02; header `synonym,accepted_name,source,...`
Table source: checklist_data.checklist_records_full  # gains canonical_name col in Plan 135-03
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Declare checklist_records_full source + add int_synonyms third arm (RCN-06)</name>
  <files>data/dbt/models/sources.yml, data/dbt/models/intermediate/int_synonyms.sql, data/dbt/seeds/schema.yml</files>
  <read_first>
    - data/dbt/models/sources.yml checklist_data block (lines ~24–29) — add `- name: checklist_records_full` (RESEARCH Pitfall 8).
    - data/dbt/models/intermediate/int_synonyms.sql — the existing two-arm UNION + anti-join (135-PATTERNS.md §int_synonyms.sql shows the exact third arm: gbif arm anti-joined against BOTH occurrence_synonyms AND auto_synonyms so manual + inactive-remap win).
    - data/dbt/seeds/schema.yml — the per-seed not_null/unique test pattern (135-PATTERNS.md §gbif_checklist_synonyms.csv).
  </read_first>
  <action>In sources.yml, add `- name: checklist_records_full` to the `checklist_data` source tables list. In int_synonyms.sql, append a third `UNION ALL` arm selecting `g.synonym, g.accepted_name, g.source FROM {{ ref('gbif_checklist_synonyms') }} g` LEFT JOIN occurrence_synonyms m ON m.synonym=g.synonym LEFT JOIN auto_synonyms a ON a.synonym=g.synonym WHERE m.synonym IS NULL AND a.synonym IS NULL (manual + inactive-remap win over GBIF). In seeds/schema.yml, register `gbif_checklist_synonyms` with not_null+unique on `synonym` and not_null on `accepted_name`, plus a `source` description. Requires the gbif_checklist_synonyms.csv seed to exist (Plan 135-02 commits it header-bearing); if running before 135-02 lands, create a header-only placeholder so `dbt seed` loads.</action>
  <verify>
    <automated>cd data && bash dbt/run.sh build --select int_synonyms 2>&1 | grep -Eiq "(PASS|success|completed|1 of)" && echo INT-SYNONYMS-OK && grep -c "gbif_checklist_synonyms" data/dbt/models/intermediate/int_synonyms.sql</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "checklist_records_full" data/dbt/models/sources.yml` >= 1.
    - int_synonyms.sql contains a third UNION arm referencing `gbif_checklist_synonyms` anti-joined on both occurrence_synonyms and auto_synonyms.
    - seeds/schema.yml has a `gbif_checklist_synonyms` entry with unique+not_null on synonym.
    - `bash dbt/run.sh build --select int_synonyms` builds the int_synonyms artifact (valid Jinja/YAML + compiles + runs).
  </acceptance_criteria>
  <done>checklist_records_full is a source; int_synonyms is the single 3-arm synonym subsystem; seed tests registered.</done>
</task>

<task type="auto">
  <name>Task 2: Author stg_checklist__records_full.sql (synonym JOIN + taxon_id bridge)</name>
  <files>data/dbt/models/staging/stg_checklist__records_full.sql</files>
  <read_first>
    - data/dbt/models/staging/stg_checklist__species.sql — the COALESCE(syn.accepted_name, ...) + LEFT JOIN int_synonyms pattern (135-PATTERNS.md §stg_checklist__records_full.sql gives the full target SQL).
    - 135-RESEARCH.md §Architecture Patterns "stg_checklist__records_full.sql" — the bridge JOINs (stg_inat__canonical_to_taxon_id + stg_inat__genus_taxon_ids fallback) and `WHERE cr.coord_flag = 'valid'`.
    - Plan 135-03 — confirms checklist_records_full.canonical_name is populated (this model JOINs on cr.canonical_name).
  </read_first>
  <action>Create data/dbt/models/staging/stg_checklist__records_full.sql as a `{{ config(materialized='view') }}` selecting ObjectID, verbatim_name, COALESCE(syn.accepted_name, cr.canonical_name) AS canonical_name, lat/lon/year/month/day/date_quality/recordedBy/locality/family/coord_flag, and COALESCE(ctt.taxon_id, g.taxon_id)::INTEGER AS taxon_id. FROM source('checklist_data','checklist_records_full') cr LEFT JOIN int_synonyms syn ON syn.synonym = cr.canonical_name LEFT JOIN stg_inat__canonical_to_taxon_id ctt ON ctt.canonical_name = COALESCE(syn.accepted_name, cr.canonical_name) LEFT JOIN stg_inat__genus_taxon_ids g ON ctt.taxon_id IS NULL AND position(' ' IN COALESCE(syn.accepted_name, cr.canonical_name))=0 AND g.genus_name = COALESCE(syn.accepted_name, cr.canonical_name) WHERE cr.coord_flag = 'valid'. Do NOT touch int_combined or the occurrences contract — that is Phase 137.</action>
  <verify>
    <automated>cd data && bash dbt/run.sh build --select stg_checklist__records_full 2>&1 | grep -Eiq "(PASS|success|completed|1 of)" && echo STG-OK</automated>
  </verify>
  <acceptance_criteria>
    - Model compiles + builds: `bash dbt/run.sh build --select stg_checklist__records_full` succeeds.
    - It references `int_synonyms` (single synonym source, RCN-06) — `grep -c "int_synonyms" stg_checklist__records_full.sql` >= 1.
    - `WHERE cr.coord_flag = 'valid'` present.
    - No reference to int_combined or the marts contract (Phase 137 boundary respected).
  </acceptance_criteria>
  <done>stg_checklist__records_full builds, resolving canonical_name + taxon_id through int_synonyms and the iNat bridge.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| committed seed → int_synonyms | gbif_checklist_synonyms.csv (auto, promote-only) enters the synonym view but is lowest precedence |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-135-07 | Tampering | GBIF auto synonyms overriding curated mappings | mitigate | Third arm anti-joins on occurrence_synonyms AND auto_synonyms (WHERE both NULL) — manual + inactive-remap always win (135-PATTERNS.md precedence) |
| T-135-08 | Denial (build break) | duplicate synonym key fanning out int_synonyms UNION | mitigate | unique test on gbif_checklist_synonyms.synonym in seeds/schema.yml (RESEARCH Pitfall 6) |
| T-135-SC | Tampering | pip installs | mitigate | No installs in this plan |
</threat_model>

<verification>
- `bash dbt/run.sh build --select int_synonyms` + `bash dbt/run.sh build --select stg_checklist__records_full` succeed.
- int_synonyms carries the third gbif arm with correct anti-join precedence.
</verification>

<success_criteria>
- RCN-06: single synonym subsystem (int_synonyms 3-arm); stg_checklist resolves via it.
- RCN-07 prerequisite: stg model yields the canonical_name→taxon_id mapping the homonym guard checks.
</success_criteria>

<output>
Create `.planning/phases/135-name-reconciliation/135-04-SUMMARY.md` when done.
</output>
