---
phase: 135-name-reconciliation
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - data/tests/test_resolve_checklist_names.py
  - data/tests/test_checklist_pipeline.py
  - data/tests/test_canonical_name.py
  - data/dbt/tests/assert_no_anthophila_homonyms.sql
autonomous: true
requirements: [RCN-01, RCN-02, RCN-03, RCN-04, RCN-05, RCN-06, RCN-07]
must_haves:
  truths:
    - "Every RCN requirement has a named, runnable test that currently fails (RED)"
    - "New tests use isolated :memory:/tmp_path DuckDB fixtures, not the dbt_sandbox fixture"
  artifacts:
    - path: "data/tests/test_resolve_checklist_names.py"
      provides: "RED stubs for RCN-02, RCN-03, RCN-04, RCN-05 + isolated checklist_resolver_db fixture"
      contains: "def test_noop_without_refresh"
    - path: "data/tests/test_checklist_pipeline.py"
      provides: "RCN-06 stubs test_no_active_reconcile_call + test_single_synonym_source"
      contains: "def test_no_active_reconcile_call"
    - path: "data/dbt/tests/assert_no_anthophila_homonyms.sql"
      provides: "RCN-07 homonym-guard singular dbt test"
      contains: "HAVING COUNT(DISTINCT"
  key_links:
    - from: "data/tests/test_resolve_checklist_names.py"
      to: "resolve_checklist_names module"
      via: "importlib.reload under monkeypatched DB_PATH"
      pattern: "import resolve_checklist_names"
---

<objective>
Create the Wave 0 RED test scaffolding named in 135-VALIDATION.md so every implementation task in later waves has an automated verify target that exists before it runs (Nyquist). All tests are written to FAIL now (the modules/columns/dbt test they exercise do not exist yet) and turn GREEN as Waves 1–2 land.

Purpose: No implementation task may run with `<automated>MISSING</automated>`. This plan converts all MISSING verify references into real, named, failing tests.
Output: 3 pytest files (one new, two extended) + 1 dbt singular test file. No production code.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/135-name-reconciliation/135-VALIDATION.md
@.planning/phases/135-name-reconciliation/135-RESEARCH.md
@.planning/phases/135-name-reconciliation/135-PATTERNS.md

<interfaces>
<!-- Contracts the test stubs assert against. These are the to-be-built signatures. -->
From data/resolve_checklist_names.py (built in Plan 135-02 — does NOT exist yet):
  def resolve_checklist_names(refresh: bool = False) -> None   # no-op unless refresh=True
  def compute_lca(name1: str, name2: str, taxa: dict) -> int | None
  def check_checklist_resolution_gate() -> None                # sys.exit on source='unresolved'
  Module constants (monkeypatch targets): DB_PATH, AUDIT_CSV, FUZZY_REVIEW_CSV,
    GBIF_SEED_CSV, _GBIF_PACE_SECONDS

Audit CSV columns (Plan 135-02 writes these):
  verbatim_name, canonical_name, resolved_taxon_id, accepted_canonical_name,
  source, confidence, gbif_match_type, notes
  source vocabulary: exact | synonym_seed | gbif | fuzzy | slash_lca | unresolved

Fuzzy-review CSV columns:
  verbatim_name, canonical_name, fuzzy_candidate, fuzzy_score, fuzzy_candidate_taxon_id

From data/checklist_pipeline.py (Plan 135-03 retires reconcile + adds canonical_name col):
  load_checklist()   # must no longer reference reconcile after Plan 135-03
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create test_resolve_checklist_names.py RED stubs (RCN-02/03/04/05)</name>
  <files>data/tests/test_resolve_checklist_names.py</files>
  <read_first>
    - data/tests/test_resolve_taxon_ids.py — copy the `resolver_db` isolated-DuckDB fixture (monkeypatch.setenv("DB_PATH", ...) + importlib.reload), the `_fake_*_response` mock-helper style, and the `test_second_run_makes_no_api_calls` no-op assertion shape (135-PATTERNS.md §test_resolve_checklist_names.py).
    - 135-VALIDATION.md §Per-Task Verification Map — the exact node-ids: test_audit_csv_covers_all_names, test_noop_without_refresh, test_fuzzy_candidates_written, test_at_least_13_fuzzy_candidates, test_fuzzy_review_gate, test_slash_lca, test_slash_verbatim_retained.
    - 135-RESEARCH.md §RCN-05 — the verified ancestry strings for the compute_lca fixture (angelicus 270393, texanus 1581468, LCA=606634).
  </read_first>
  <behavior>
    - test_noop_without_refresh: patches pygbif.species.name_backbone; calls resolve_checklist_names(refresh=False); asserts call_count == 0 (RCN-03).
    - test_slash_lca: pure-function; compute_lca('agapostemon angelicus','agapostemon texanus', taxa_dict) == 606634, NOT 50086 (RCN-05). Use the verified ancestry strings from RESEARCH inline (no taxa.csv.gz file I/O).
    - test_slash_verbatim_retained: after a refresh run on a fixture row whose verbatim_name contains '/', the audit CSV row keeps the raw slash verbatim_name string while resolved_taxon_id is the LCA (RCN-05).
    - test_audit_csv_covers_all_names: every distinct checklist canonical_name appears exactly once in the audit CSV with a non-empty `source` in the locked vocabulary and a numeric `confidence` in [0,1] (RCN-02).
    - test_fuzzy_candidates_written: fuzzy tier writes rows to FUZZY_REVIEW_CSV with the 5 locked columns (RCN-04).
    - test_at_least_13_fuzzy_candidates: running the fuzzy tier over the unmatched set yields >= 13 candidates at score_cutoff 85 (RCN-04, A1 assumption).
    - test_fuzzy_review_gate: no row whose `source` contains the literal 'fuzzy' is present in occurrence_synonyms / gbif_checklist_synonyms (RCN-04).
  </behavior>
  <action>Create data/tests/test_resolve_checklist_names.py. Add the `checklist_resolver_db` fixture (tmp_path duckdb, monkeypatch DB_PATH, importlib.reload(resolve_checklist_names), monkeypatch _GBIF_PACE_SECONDS=0.0, redirect AUDIT_CSV/FUZZY_REVIEW_CSV/GBIF_SEED_CSV into tmp_path, create `checklist_data` schema + a handful of seeded checklist_records_full rows including one slash-compound and one known-misspelling). Add a `_fake_gbif_response(match_type, canonical_name=None, confidence=99)` helper mirroring the live shape (omit `usage` when match_type=='NONE'). Write the 7 tests above as RED stubs that import the not-yet-existing module via `importlib`. Scope every fixture to tmp_path/:memory: so the 18 pre-existing `dbt_sandbox` failures are never touched (RESEARCH Pitfall 7). Do NOT implement resolve_checklist_names — that is Plan 135-02; tests must FAIL on import/AttributeError now.</action>
  <verify>
    <automated>cd data && uv run pytest tests/test_resolve_checklist_names.py -x 2>&1 | grep -Eq "(ModuleNotFoundError|ImportError|AttributeError|error|failed)" && echo RED-OK</automated>
  </verify>
  <acceptance_criteria>
    - File defines exactly the 7 node-ids listed in 135-VALIDATION.md (collectable by `pytest --collect-only`).
    - The compute_lca assertion literal is `== 606634` (not 50086).
    - No test references `dbt_sandbox`; fixture uses tmp_path + monkeypatch.setenv("DB_PATH", ...).
    - Run currently FAILS (module absent). `pytest --collect-only -q tests/test_resolve_checklist_names.py` either errors on import or lists the 7 tests.
  </acceptance_criteria>
  <done>test_resolve_checklist_names.py exists with 7 named RED tests + isolated fixture; run is red because resolve_checklist_names does not yet exist.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add RCN-06 stubs to test_checklist_pipeline.py + RCN-01 trailing-space case</name>
  <files>data/tests/test_checklist_pipeline.py, data/tests/test_canonical_name.py</files>
  <read_first>
    - data/tests/test_checklist_pipeline.py — current structure/imports (extend, do not rewrite).
    - 135-RESEARCH.md §RCN-06 — the inspect.getsource(load_checklist) assertion and the checklist_synonyms.csv emptiness assertion.
    - data/tests/test_canonical_name.py — the pure-function `test_canonicalize_*` style (135-PATTERNS.md §test_canonical_name.py additions); add a trailing-space case only if not already present.
    - data/canonical_name.py:73 normalize_scientific_name — confirm the trailing-space input `"Agapostemon texanus "` already normalizes (it does per RESEARCH); the new test documents RCN-01.
  </read_first>
  <behavior>
    - test_no_active_reconcile_call: inspect.getsource(checklist_pipeline.load_checklist) does NOT contain 'reconcile' (RCN-06, D-07). Currently FAILS because reconcile() is still called.
    - test_single_synonym_source: checklist_synonyms.csv has zero data rows (header-only or absent) AND no module-level SYNONYMS_PATH reference remains in checklist_pipeline (RCN-06).
    - test trailing-space normalization: normalize_scientific_name("Agapostemon texanus ") == "agapostemon texanus" (RCN-01). This may already pass — keep it as a regression guard; it is the only non-RED test here and that is acceptable for an existing-behavior requirement.
  </behavior>
  <action>Append to data/tests/test_checklist_pipeline.py: `test_no_active_reconcile_call` (uses `import inspect, checklist_pipeline`; asserts 'reconcile' not in source of load_checklist) and `test_single_synonym_source` (reads data/checklist_synonyms.csv via csv.DictReader, asserts len(rows)==0, and asserts `inspect.getsource(checklist_pipeline)` no longer contains 'SYNONYMS_PATH'). Append to data/tests/test_canonical_name.py a `-k trailing`-discoverable test for the trailing-space RCN-01 case if absent. The reconcile/SYNONYMS_PATH assertions FAIL now (both still present) and go GREEN in Plan 135-03.</action>
  <verify>
    <automated>cd data && uv run pytest tests/test_checklist_pipeline.py::test_no_active_reconcile_call tests/test_canonical_name.py -k "reconcile or trailing or synonym_source" 2>&1 | grep -Eq "(failed|passed)" && echo COLLECTED</automated>
  </verify>
  <acceptance_criteria>
    - `pytest --collect-only` lists test_no_active_reconcile_call and test_single_synonym_source.
    - test_no_active_reconcile_call currently FAILS (reconcile still in load_checklist).
    - Trailing-space test passes (documents existing RCN-01 behavior).
    - No new dependency imports beyond stdlib `inspect`/`csv`.
  </acceptance_criteria>
  <done>Two RCN-06 RED tests + one RCN-01 trailing-space regression test exist and are collectable.</done>
</task>

<task type="auto">
  <name>Task 3: Create RCN-07 homonym-guard dbt singular test (authored to fail-on-violation)</name>
  <files>data/dbt/tests/assert_no_anthophila_homonyms.sql</files>
  <read_first>
    - data/dbt/tests/test_higher_rank_taxon_ids_name_rank_unique.sql — the `GROUP BY ... HAVING COUNT(*) > 1` zero-row singular-test pattern (135-PATTERNS.md §assert_no_anthophila_homonyms.sql).
    - 135-RESEARCH.md §RCN-07 + Assumptions A3 — Anthophila ancestor taxon_id assumed 630955; the planner-flagged verification belongs to Plan 135-05, not here. Author the test referencing 630955 with a comment that the id is verified in 135-05's build step.
    - 135-PATTERNS.md §assert_no_anthophila_homonyms.sql — guidance on referencing taxa.csv.gz (choose `read_csv` with the project's taxa path convention; do NOT add `severity='warn'` — RCN-07 demands hard-fail).
  </read_first>
  <action>Create data/dbt/tests/assert_no_anthophila_homonyms.sql as a singular test: a CTE restricting `{{ ref('int_combined') }}` rows to taxon_ids under Anthophila ancestor 630955 (via the project's taxa.csv.gz read convention), then `GROUP BY canonical_name HAVING COUNT(DISTINCT taxon_id) > 1`, selecting the offending rows so dbt fails the build when non-empty. Omit `severity='warn'`. Add a header comment: "RCN-07 — Anthophila ancestor taxon_id=630955 (verify in Plan 135-05 build step per RESEARCH A3)." This test will not actually execute until int_combined carries checklist rows (Phase 137); for Phase 135 it must at minimum compile and run green against the current int_combined (zero offending rows). NOTE: this test references int_combined which Phase 135 does NOT modify, so it returns zero rows today — that is the correct GREEN baseline.</action>
  <verify>
    <automated>cd data && bash dbt/run.sh test --select assert_no_anthophila_homonyms 2>&1 | grep -Eiq "(PASS|1 of 1|success)" && echo DBT-TEST-OK</automated>
  </verify>
  <acceptance_criteria>
    - File compiles under `dbt parse` (valid `{{ ref('int_combined') }}` Jinja).
    - `bash dbt/run.sh test --select assert_no_anthophila_homonyms` runs and PASSES against current int_combined (zero homonyms today).
    - No `severity='warn'` config present (hard-fail required).
    - Header comment names taxon_id 630955 and defers its verification to Plan 135-05.
  </acceptance_criteria>
  <done>assert_no_anthophila_homonyms.sql exists, compiles, and passes green against the unmodified int_combined.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none in this plan) | Test files only; no external input, no curator-facing CSV written here |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-135-01 | Tampering | dbt test referencing taxa.csv.gz path | accept | Read-only committed file; no write; path is repo-local |
| T-135-SC | Tampering | npm/pip/cargo installs | mitigate | No installs in this plan; pygbif/rapidfuzz pre-approved in Phase 134 (RESEARCH §Package Legitimacy Audit) — no `[ASSUMED]`/`[SUS]` packages, no checkpoint required |
</threat_model>

<verification>
- All three test files collectable; `pytest --collect-only` lists every node-id in 135-VALIDATION.md.
- RED tests fail (modules/columns absent); the dbt homonym test passes green against current int_combined.
- No test touches the dbt_sandbox fixture (RESEARCH Pitfall 7).
</verification>

<success_criteria>
- Every RCN-01..RCN-07 verify command in 135-VALIDATION.md resolves to a real, named test.
- No later implementation task carries `<automated>MISSING</automated>`.
</success_criteria>

<output>
Create `.planning/phases/135-name-reconciliation/135-01-SUMMARY.md` when done.
</output>
