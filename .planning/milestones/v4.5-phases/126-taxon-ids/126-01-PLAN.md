---
phase: 126-taxon-ids
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - data/tests/test_dbt_scaffold.py
  - data/tests/test_resolution_gate.py
  - data/resolve_taxon_ids.py
  - data/run.py
  - data/dbt/models/intermediate/int_species_universe.sql
  - data/dbt/models/intermediate/int_combined.sql
  - data/dbt/models/marts/species.sql
  - data/dbt/models/marts/occurrences.sql
  - data/dbt/models/marts/schema.yml
autonomous: true
requirements: [TID-01, TID-02]
must_haves:
  truths:
    - "Every row in species.parquet has a non-null taxon_id INTEGER (TID-01, D-01)"
    - "Every row in occurrences.parquet has a non-null taxon_id INTEGER (TID-02, D-01)"
    - "occurrences.taxon_id equals species.taxon_id for the same canonical_name (D-03)"
    - "The WABA arm derives canonical_name from specimen_inat_taxon_name and resolves through the same bridge, not waba.taxon__id directly (D-04)"
    - "The 4 known non-bee WABA rows are excluded from occurrences.parquet, not silently dropped — the gate reports them (D-09)"
    - "A pre-build resolution gate fails the pipeline non-zero before dbt build when an unresolved bee name is present (D-02)"
    - "The gate's failure path is durably tested: an unresolved BEE name forces SystemExit, while a KNOWN_NON_BEES-only set does not block and is reported (D-02, D-09)"
  artifacts:
    - path: "data/dbt/models/marts/schema.yml"
      provides: "NOT NULL taxon_id constraint on both marts; species=21 entries, occurrences=37 entries (D-01, D-07)"
      contains: "type: not_null"
    - path: "data/resolve_taxon_ids.py"
      provides: "Extended resolution union (occurrence_synonyms.accepted_name + WABA-derived names) and check_resolution_gate() with KNOWN_NON_BEES (D-02, D-09, RD-01)"
      contains: "KNOWN_NON_BEES"
    - path: "data/run.py"
      provides: "resolution-gate STEPS entry ordered between resolve-taxon-ids and taxa-download"
      contains: "resolution-gate"
    - path: "data/tests/test_dbt_scaffold.py"
      provides: "Wave-0 non-null + consistency test scaffolds (TID-01, TID-02, D-03)"
      contains: "test_species_taxon_id_non_null"
    - path: "data/tests/test_resolution_gate.py"
      provides: "Wave-0 gate failure-path tests: blocking bee name forces SystemExit, KNOWN_NON_BEES-only set does not block and is reported (D-02, D-09)"
      contains: "test_gate_blocks_unresolved_bee"
  key_links:
    - from: "data/dbt/models/intermediate/int_combined.sql"
      to: "stg_inat__canonical_to_taxon_id"
      via: "LEFT JOIN on post-synonymy canonical_name per arm (ctt / ctt_w / ctt_io)"
      pattern: "stg_inat__canonical_to_taxon_id"
    - from: "data/dbt/models/marts/species.sql"
      to: "int_species_universe.taxon_id"
      via: "SELECT taxon_id"
      pattern: "taxon_id"
---

<objective>
Thread the already-resolved `taxon_id` through the dbt data layer so both `species.parquet` and `occurrences.parquet` carry a non-null `taxon_id INTEGER` column, and install the pre-build resolution gate that hard-fails the nightly pipeline before dbt build when a bee name is unresolved (D-02). This phase SURFACES an existing resolution — it does not build resolution.

Purpose: TID-01 and TID-02 require the column to exist and be non-null on every row. D-01 makes the dbt contract the backstop; D-02's gate makes failures diagnosable in the nightly cron log.
Output: Modified intermediate + mart SQL, updated schema.yml contract (21/37 columns), extended resolution union + gate in resolve_taxon_ids.py, new run.py STEPS entry, Wave-0 pytest scaffolds, and a Wave-0 gate failure-path test.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/126-taxon-ids/126-CONTEXT.md
@.planning/phases/126-taxon-ids/126-RESEARCH.md
@.planning/phases/126-taxon-ids/126-PATTERNS.md
@.planning/phases/126-taxon-ids/126-VALIDATION.md
@CLAUDE.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wave-0 non-null + consistency test scaffolds</name>
  <files>data/tests/test_dbt_scaffold.py</files>
  <read_first>
    - data/tests/test_dbt_scaffold.py (the file being modified; study `_SPECIES_GUARD` lines 255-258 and `_OCCURRENCES_GUARD` lines 214-217, and the SPV-01 test body at lines 261-289)
    - .planning/phases/126-taxon-ids/126-PATTERNS.md (section "data/tests/test_dbt_scaffold.py" — verbatim test bodies to add)
    - .planning/phases/126-taxon-ids/126-VALIDATION.md (Wave 0 Requirements list)
  </read_first>
  <behavior>
    - test_species_taxon_id_non_null: COUNT of species.parquet rows WHERE taxon_id IS NULL must equal 0 (TID-01). Guarded by `@_SPECIES_GUARD`.
    - test_occurrences_taxon_id_non_null: COUNT of occurrences.parquet rows WHERE taxon_id IS NULL must equal 0 (TID-02). Guarded by `@_OCCURRENCES_GUARD`.
    - test_taxon_id_consistency: JOIN occurrences to species USING (canonical_name); COUNT of rows WHERE o.taxon_id != s.taxon_id must equal 0 (D-03). Guarded by both `@_OCCURRENCES_GUARD` and `@_SPECIES_GUARD`.
    - All three RED before the mart changes land (taxon_id column absent → DuckDB Binder error or skip if parquet absent); GREEN after Tasks 2-4 produce the column.
  </behavior>
  <action>Append three test functions to data/tests/test_dbt_scaffold.py following the existing `_SPECIES_GUARD`/`_OCCURRENCES_GUARD` skipif pattern from Phase 125. Reuse the existing guard markers (do not redefine them). Each test reads `str(SANDBOX / "species.parquet")` or `"occurrences.parquet"` and asserts `n == 0` with a descriptive message. The consistency test stacks both guard decorators. Use the verbatim bodies in 126-PATTERNS.md. Do not run the build in this task — these scaffolds are expected to be RED (or skipped if the sandbox parquet predates the change) until Tasks 2-4 land.</action>
  <verify>
    <automated>uv run --project data pytest data/tests/test_dbt_scaffold.py --collect-only -q 2>&1 | grep -E "test_species_taxon_id_non_null|test_occurrences_taxon_id_non_null|test_taxon_id_consistency"</automated>
  </verify>
  <acceptance_criteria>
    - data/tests/test_dbt_scaffold.py defines test_species_taxon_id_non_null, test_occurrences_taxon_id_non_null, test_taxon_id_consistency
    - Each new test carries the correct guard decorator(s) and does NOT redefine `_SPECIES_GUARD`/`_OCCURRENCES_GUARD`
    - `uv run --project data pytest data/tests/test_dbt_scaffold.py --collect-only -q` lists all three test node IDs
  </acceptance_criteria>
  <done>Three Wave-0 test functions collected by pytest; assertions target null taxon_id counts and cross-mart equality.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 1b: Wave-0 resolution-gate failure-path test (D-02, D-09)</name>
  <files>data/tests/test_resolution_gate.py</files>
  <read_first>
    - data/resolve_taxon_ids.py (the module under test; `UNRESOLVED_CSV = Path(__file__).parent / "lineage_unresolved.csv"` line 20, `_read_unresolved_csv` lines 36-46 for the CSV header/field convention, `import csv` line 8 — `check_resolution_gate()` + `KNOWN_NON_BEES` are added by Task 2 and read `UNRESOLVED_CSV` via `csv.DictReader` keyed on `canonical_name`)
    - .planning/phases/126-taxon-ids/126-PATTERNS.md (section "data/resolve_taxon_ids.py" — the exact `check_resolution_gate()` body and `KNOWN_NON_BEES = {"cicindela pugetana", "cleridae", "encopognathus"}` this test pins behavior for)
    - data/tests/test_dbt_scaffold.py (repo pytest conventions: module-level `import` of the unit under test, plain `def test_*` functions, descriptive assert messages — this gate test is pure-Python and needs NO parquet `skipif` guard)
  </read_first>
  <behavior>
    - test_gate_blocks_unresolved_bee (case A): given a lineage_unresolved.csv whose rows include a single canonical_name that is NOT in KNOWN_NON_BEES (e.g. `agapostemon subtilior`), `check_resolution_gate()` raises `SystemExit` (non-zero exit), and the offending name appears in the SystemExit message.
    - test_gate_allows_known_non_bees_only (case B): given a lineage_unresolved.csv whose rows are ALL members of KNOWN_NON_BEES (e.g. `cicindela pugetana`, `cleridae`, `encopognathus`), `check_resolution_gate()` does NOT raise — it returns normally and emits a report line naming the excluded count (assert via capsys that stdout contains `resolution-gate: OK` and the count of excluded rows), proving the rows are reported not silently dropped (D-09).
    - Both RED until Task 2 adds `check_resolution_gate()` + `KNOWN_NON_BEES` (import/attribute error → RED); GREEN once Task 2 lands.
  </behavior>
  <action>Create data/tests/test_resolution_gate.py. At module scope, `import resolve_taxon_ids as r` (the same import style used elsewhere in data/tests). Write a small helper or inline fixture that writes a header row (`canonical_name`) plus the supplied data rows to a temp CSV under pytest's `tmp_path`, then `monkeypatch.setattr(r, "UNRESOLVED_CSV", tmp_csv_path)` so the gate reads the synthetic file — do NOT touch the real data/lineage_unresolved.csv. The header column name MUST be `canonical_name` to match the gate's `csv.DictReader` keyed access. For case A, write rows containing `agapostemon subtilior` (a real bee not in KNOWN_NON_BEES) and assert with `pytest.raises(SystemExit)` that the gate exits; assert the offending name is in `str(excinfo.value)`. For case B, write rows containing only the three KNOWN_NON_BEES names, call the gate (must NOT raise), and use the `capsys` fixture to assert stdout contains `resolution-gate: OK` and references the excluded-row count. Reference `r.KNOWN_NON_BEES` for the case-B name set rather than hardcoding, so the test stays coupled to the module constant. This test is pure-Python — add no parquet `skipif` guard.</action>
  <verify>
    <automated>uv run --project data pytest data/tests/test_resolution_gate.py --collect-only -q 2>&1 | grep -E "test_gate_blocks_unresolved_bee|test_gate_allows_known_non_bees_only"</automated>
  </verify>
  <acceptance_criteria>
    - data/tests/test_resolution_gate.py defines test_gate_blocks_unresolved_bee and test_gate_allows_known_non_bees_only
    - Both tests monkeypatch `resolve_taxon_ids.UNRESOLVED_CSV` to a tmp CSV (the real lineage_unresolved.csv is never read or mutated)
    - Case A asserts `SystemExit` via `pytest.raises` and that the offending bee name appears in the exit message
    - Case B asserts the gate does NOT raise and that stdout reports the KNOWN_NON_BEES exclusions (`resolution-gate: OK` + count)
    - Until Task 2 lands this is the RED Wave-0 gate; once it lands, `uv run --project data pytest data/tests/test_resolution_gate.py -x` exits 0
  </acceptance_criteria>
  <done>Two Wave-0 tests durably cover the gate's failure path: a blocking bee name forces non-zero exit, a KNOWN_NON_BEES-only set passes and is reported (D-02, D-09).</done>
</task>

<task type="auto">
  <name>Task 2: Extend resolution union + install pre-build gate (D-02, D-09, RD-01)</name>
  <files>data/resolve_taxon_ids.py, data/run.py</files>
  <read_first>
    - data/resolve_taxon_ids.py (the file being modified; `_names_to_resolve` SQL union lines 58-74, `_read_unresolved_csv` lines 36-46, `UNRESOLVED_CSV` line 21)
    - data/run.py (the file being modified; STEPS list lines 84-106, resolve-taxon-ids entry line 93, import block lines 30-46, `_run_dbt_build` lines 59-81)
    - .planning/phases/126-taxon-ids/126-PATTERNS.md (sections "data/resolve_taxon_ids.py" and "data/run.py")
    - .planning/phases/126-taxon-ids/126-RESEARCH.md (RD-01 fix, Pitfall 3, "Pre-build resolution gate" implementation)
  </read_first>
  <action>In data/resolve_taxon_ids.py, extend the `_names_to_resolve` UNION with two new arms: (a) `SELECT DISTINCT accepted_name AS canonical_name FROM main.occurrence_synonyms WHERE accepted_name IS NOT NULL` (the RD-01 fix so synonymized accepted names like `agapostemon subtilior` are verified even absent from raw sources); (b) `SELECT DISTINCT lower(trim(...first_two_tokens of taxon__name...)) AS canonical_name FROM inat_waba_data.observations WHERE taxon__name IS NOT NULL` (Pitfall 3 — so WABA-derived names enter the bridge). Use the exact `split_part(...,' ',1) || ' ' || split_part(...,' ',2)` two-token derivation from 126-PATTERNS.md. Add module constant `KNOWN_NON_BEES = {"cicindela pugetana", "cleridae", "encopognathus"}`. Add `check_resolution_gate()`: read `lineage_unresolved.csv` via `UNRESOLVED_CSV.open(newline="")` + `csv.DictReader` (header field `canonical_name`), compute `blocking = [r for r in rows if r["canonical_name"] not in KNOWN_NON_BEES]`; if blocking is non-empty, `sys.exit(...)` with a message naming the offenders and the fix command `uv run python resolve_taxon_ids.py --refresh-lineage`; otherwise print `resolution-gate: OK (N known non-bee rows excluded)` so excluded rows are REPORTED not silenced (D-09). In data/run.py, import the gate alongside the existing `resolve_taxon_ids` import and insert `("resolution-gate", check_resolution_gate)` into STEPS immediately after the `resolve-taxon-ids` entry (line 93) and before `taxa-download` (line 94). Use `sys.exit(message)` as the failure mode (propagates as SystemExit through main()'s handler). The Wave-0 tests from Task 1b must turn GREEN once this lands — run them as part of verification.</action>
  <verify>
    <automated>cd /home/peter/dev/beeatlas/data && uv run python -c "import resolve_taxon_ids as r; assert hasattr(r,'check_resolution_gate'); assert r.KNOWN_NON_BEES=={'cicindela pugetana','cleridae','encopognathus'}; print('gate ok')" && uv run python -c "import run; names=[s[0] for s in run.STEPS]; i=names.index('resolve-taxon-ids'); assert names[i+1]=='resolution-gate', names; assert names[i+2]=='taxa-download', names; print('order ok')" && cd /home/peter/dev/beeatlas && uv run --project data pytest data/tests/test_resolution_gate.py -x</automated>
  </verify>
  <acceptance_criteria>
    - resolve_taxon_ids.py defines KNOWN_NON_BEES == {"cicindela pugetana", "cleridae", "encopognathus"}
    - resolve_taxon_ids.py defines check_resolution_gate() that sys.exit()s when a non-KNOWN_NON_BEES row is present and prints an OK line reporting the excluded count otherwise
    - The resolution union includes occurrence_synonyms.accepted_name and the WABA two-token derived name arm
    - run.py STEPS contains "resolution-gate" ordered immediately after "resolve-taxon-ids" and before "taxa-download"
    - `uv run python -c "import run; ..."` order assertion exits 0
    - `uv run --project data pytest data/tests/test_resolution_gate.py -x` exits 0 (the Task 1b Wave-0 tests are now GREEN)
  </acceptance_criteria>
  <done>Gate function exists with the correct exclusion set, union covers synonym accepted names + WABA-derived names, STEPS wires the gate in the correct position, and the Task 1b failure-path tests pass.</done>
</task>

<task type="auto">
  <name>Task 3: Surface taxon_id through intermediate models (int_species_universe + int_combined)</name>
  <files>data/dbt/models/intermediate/int_species_universe.sql, data/dbt/models/intermediate/int_combined.sql</files>
  <read_first>
    - data/dbt/models/intermediate/int_species_universe.sql (the file being modified; existing bridge LEFT JOIN lines 130-133, species_universe CTE SELECT ending ~line 127, final SELECT line 151)
    - data/dbt/models/intermediate/int_combined.sql (the file being modified; ARM 1 synonymy COALESCE line 43 + JOIN line 52, ARM 2 WABA `NULL AS canonical_name` line 88 + WHERE ~line 100, ARM 3 COALESCE line 136 + JOIN line 143)
    - data/dbt/models/intermediate/int_specimen_obs_base.sql (source of `specimen_inat_taxon_name`, line 7)
    - .planning/phases/126-taxon-ids/126-PATTERNS.md (sections "int_species_universe.sql" and "int_combined.sql")
    - .planning/phases/126-taxon-ids/126-RESEARCH.md (Pitfall 1 BIGINT cast, Pitfall 2 VARCHAR cast, RD-01)
  </read_first>
  <action>In int_species_universe.sql, add `ctt.taxon_id::INTEGER AS taxon_id` to the `species_universe` CTE SELECT (after `inat_obs_count`); the existing bridge LEFT JOIN (alias `ctt`) already supplies it and the final `SELECT DISTINCT ON (canonical_name) *` picks it up — no other change. In int_combined.sql, add to each occurrence arm a LEFT JOIN to `{{ ref('stg_inat__canonical_to_taxon_id') }}` using a distinct alias per arm and SELECT `<alias>.taxon_id::INTEGER AS taxon_id` at the SAME column position in all three arms (UNION ALL type consistency). ARM 1 (ecdysis): alias `ctt`, join `ON ctt.canonical_name = COALESCE(syn_e.accepted_name, e.canonical_name)`. ARM 3 (inat_obs): alias `ctt_io`, join `ON ctt_io.canonical_name = COALESCE(syn_io.accepted_name, io.canonical_name)`. ARM 2 (WABA): alias `ctt_w` — replace `NULL AS canonical_name` with the `lower(trim(...two-token split_part of sob.specimen_inat_taxon_name...))::VARCHAR AS canonical_name` derivation (D-04), and join `ON ctt_w.canonical_name = <same derivation>`, then SELECT `ctt_w.taxon_id::INTEGER AS taxon_id`. The `::INTEGER` cast at every selection point is mandatory (taxon_lineage_extended is BIGINT; contract requires integer — Pitfall 1). The `::VARCHAR` cast on the WABA canonical_name is mandatory (Pitfall 2). All join keys use POST-synonymy canonical_name to guarantee D-03 (occurrences.taxon_id == species.taxon_id). Do not run dbt in this task.</action>
  <verify>
    <automated>cd /home/peter/dev/beeatlas/data && grep -c 'taxon_id::INTEGER' dbt/models/intermediate/int_combined.sql | grep -qE '^[3-9]|^[1-9][0-9]' && grep -q 'taxon_id::INTEGER AS taxon_id' dbt/models/intermediate/int_species_universe.sql && grep -q 'ctt_w' dbt/models/intermediate/int_combined.sql && grep -q 'ctt_io' dbt/models/intermediate/int_combined.sql && echo "intermediate models ok"</automated>
  </verify>
  <acceptance_criteria>
    - int_species_universe.sql species_universe CTE SELECT includes `ctt.taxon_id::INTEGER AS taxon_id`
    - int_combined.sql contains a bridge LEFT JOIN in all three arms with aliases ctt, ctt_w, ctt_io (no alias collision)
    - int_combined.sql ARM 2 canonical_name is the derived `lower(trim(...))::VARCHAR` expression (no longer `NULL`)
    - Every taxon_id selection casts `::INTEGER`; the WABA canonical_name casts `::VARCHAR`
    - grep verification command exits 0
  </acceptance_criteria>
  <done>Both intermediate models emit a `taxon_id` column; all join keys are post-synonymy canonical_name; casts are present.</done>
</task>

<task type="auto">
  <name>Task 4: Add taxon_id to marts + NOT NULL contract; build and verify (TID-01, TID-02, D-01, D-03, D-07)</name>
  <files>data/dbt/models/marts/species.sql, data/dbt/models/marts/occurrences.sql, data/dbt/models/marts/schema.yml</files>
  <read_first>
    - data/dbt/models/marts/species.sql (the file being modified; header comment line 1, SELECT lines 15-36)
    - data/dbt/models/marts/occurrences.sql (the file being modified; final SELECT lines 83-100, canonical_name line 93)
    - data/dbt/models/marts/schema.yml (the file being modified; contract.enforced block lines 4-8, species mart last column ~125, occurrences mart last column ~78-80)
    - .planning/phases/126-taxon-ids/126-PATTERNS.md (sections "species.sql", "occurrences.sql", "schema.yml")
    - .planning/phases/126-taxon-ids/126-RESEARCH.md ("Contract Column Count Corrections": species 20→21, occurrences 36→37)
  </read_first>
  <action>In species.sql, add `taxon_id` to the SELECT after `inat_obs_count` (no cast — int_species_universe already cast to INTEGER); update the header comment from "19-column"/inconsistent to "21-column". In occurrences.sql, add `j.taxon_id` to the final SELECT after `j.canonical_name` (line 93, no cast — int_combined already cast). In schema.yml, add to BOTH marts a `taxon_id` column entry with `data_type: integer` and `constraints: - type: not_null` (use the `constraints:` style that matches contract enforcement, NOT `data_tests:`); place it as the new last column in each model (species → 21 entries, occurrences → 37 entries) per D-07. Then run the build. The build is the belt-and-suspenders enforcement of D-01: a null taxon_id or BIGINT/INTEGER mismatch fails it. After the build, run the Wave-0 tests from Task 1 — they must now be GREEN. Apply the WABA exclusion concern: if the gate or build surfaces a blocking unresolved bee name, fix per the gate message; the 4 KNOWN_NON_BEES rows must be excluded from occurrences.parquet (verify the gate reported them, D-09).</action>
  <verify>
    <automated>cd /home/peter/dev/beeatlas && bash data/dbt/run.sh build && uv run --project data pytest data/tests/test_dbt_scaffold.py -k "taxon_id" -x</automated>
  </verify>
  <acceptance_criteria>
    - species.sql SELECT includes taxon_id; header comment reads 21-column
    - occurrences.sql final SELECT includes j.taxon_id after j.canonical_name
    - schema.yml has a `taxon_id` entry with `constraints: - type: not_null` in BOTH marts; species mart has 21 column entries, occurrences mart has 37
    - `bash data/dbt/run.sh build` exits 0 with both contracts enforcing the new column counts
    - `uv run --project data pytest data/tests/test_dbt_scaffold.py -k "taxon_id" -x` exits 0 (test_species_taxon_id_non_null, test_occurrences_taxon_id_non_null, test_taxon_id_consistency all GREEN)
  </acceptance_criteria>
  <done>Both marts carry a non-null taxon_id INTEGER; dbt contract enforces it; the three Wave-0 tests pass; D-03 consistency holds; KNOWN_NON_BEES rows excluded and reported.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| iNat `taxa.csv.gz` / iNat API → bridge | External taxonomy data crosses into the pipeline; already-resolved in prior steps, this phase only reads it |
| Nightly cron (maderas) → published parquet | Pipeline output ships to S3/CloudFront with no human in the loop on each run |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-126-01 | Tampering | int_combined WABA arm canonical_name derivation | mitigate | `::VARCHAR` cast + bridge JOIN on POST-synonymy key; KNOWN_NON_BEES excludes the 4 confirmed non-bee bycatch rows so they cannot inject a spurious taxon_id |
| T-126-02 | Denial of Service | resolution-gate coupling nightly freshness to name resolution | accept | Documented operational tradeoff in CONTEXT.md; the gate fails fast with an actionable message rather than shipping a NULL taxon_id silently. Phase 127 adds the auto-remap safety net |
| T-126-03 | Information Disclosure | published taxon_id INTEGER column | accept | taxon_id is a public iNaturalist identifier; no PII, no secret. Static-site read-only data |
| T-126-04 | Tampering | poisoned/unexpected taxon mapping silently shipping | mitigate | dbt NOT NULL contract (D-01) + pre-build gate (D-02) hard-fail before publish; cross-mart D-03 consistency test catches species/occurrence divergence; the Task 1b failure-path test guarantees the gate itself does not regress into a no-op |
| T-126-SC | Tampering | npm/pip/cargo installs | accept | No new packages this phase (RESEARCH Package Legitimacy Audit: N/A); no install task exists |
</threat_model>

<verification>
- `bash data/dbt/run.sh build` exits 0 with species mart enforcing 21 columns and occurrences mart enforcing 37 columns.
- `uv run --project data pytest data/tests/test_dbt_scaffold.py -x` is green (includes the three new taxon_id tests).
- `uv run --project data pytest data/tests/test_resolution_gate.py -x` is green (gate blocks an unresolved bee name and passes/reports a KNOWN_NON_BEES-only set).
- `uv run python -c "import run; ..."` confirms resolution-gate is ordered between resolve-taxon-ids and taxa-download.
- The gate prints a `resolution-gate: OK (N known non-bee rows excluded)` line on a clean run (D-09 reporting).
</verification>

<success_criteria>
- TID-01: species.parquet has a non-null taxon_id INTEGER for every row.
- TID-02: occurrences.parquet has a non-null taxon_id INTEGER for every row.
- D-01: dbt contract enforces NOT NULL on both marts; build hard-fails otherwise.
- D-02: pre-build resolution gate exits non-zero before dbt build on an unresolved bee name, and its failure path is covered by a durable pytest test (test_resolution_gate.py).
- D-03: occurrences.taxon_id == species.taxon_id for matching canonical_name.
- D-04: the WABA arm (int_combined ARM 2) derives canonical_name from specimen_inat_taxon_name (two-token lower/trim) and resolves through the same bridge — not waba.taxon__id directly.
- D-09: the 4 KNOWN_NON_BEES WABA rows are excluded from occurrences.parquet and reported by the gate; the gate's reporting (not silently dropping) behavior is asserted by test_gate_allows_known_non_bees_only.
</success_criteria>

<output>
Create `.planning/phases/126-taxon-ids/126-01-SUMMARY.md` when done.
</output>
