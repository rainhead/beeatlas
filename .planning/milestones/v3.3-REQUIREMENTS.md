# Milestone v3.3 Requirements — dbt Spike

**Status:** ✅ SHIPPED 2026-05-13 — all 18 requirements satisfied (see `.planning/v3.3-MILESTONE-AUDIT.md`).

**Milestone goal:** Learn whether `dbt-duckdb` is the right shape for the BeeAtlas data layer by porting one representative slice end-to-end on a branch. Produce a go/no-go writeup that decides whether v3.4+ pursues a full migration.

**Scope discipline (per `feedback_spike_scope`):** Requirements are framed as *learning outcomes* — diffs, writeups, captured observations. No requirement says "replaces X" or "deletes X." Cutover decisions belong to a follow-up rewrite milestone.

**Out of scope for v3.3:**

| Item | Reason |
|------|--------|
| Changes to `data/run.py`, `data/nightly.sh`, `public/data/` | Spike is exploratory; production paths untouched |
| Changes to `scripts/validate-schema.mjs` or frontend consumers | No schema cutover in spike |
| Replacing or deleting any existing Python pipeline code | Cutover deferred to follow-up rewrite milestone |
| Anti-entropy, ingestion, or artifact-generator restructuring | Out of slice — `species_maps.py`, `feeds.py`, dlt fetchers stay |
| Multi-slice porting | One slice is enough to learn the shape |
| dbt CI integration | Local-only spike |

## v3.3 Requirements

### Scaffolding (SCAFFOLD)

- [x] **SCAFFOLD-01**: `data/dbt/` contains a working `dbt-duckdb` project (`dbt_project.yml`, `profiles.yml`, `sources.yml`) that connects to a copy of `data/beeatlas.duckdb` and lists the upstream raw tables it reads as `source()` declarations. — *Validated; 4 source schemas declared.*
- [x] **SCAFFOLD-02**: `dbt build` runs end-to-end on the spike slice from a clean local checkout and exits 0. — *Validated; `PASS=23 WARN=0 ERROR=0 SKIP=0 NO-OP=0` in ~1.4s.*
- [x] **SCAFFOLD-03**: dbt artifacts (`target/`, logs, profiles) are gitignored where appropriate; the dbt project is not referenced by `data/run.py`, `data/nightly.sh`, or any CI workflow. — *Validated; `git grep` confirms zero production refs.*

### Slice Port (PORT)

- [x] **PORT-01**: One slice is selected and documented in the phase context. — *Validated; chosen slice = `export.py` → occurrences.parquet + counties.geojson + ecoregions.geojson; rationale in `dbt-spike-findings.md` §Slice Choice.*
- [x] **PORT-02**: The slice is expressed as a DAG of dbt models with declared `{{ ref() }}` and `{{ source() }}` dependencies. — *Validated; 23 models, `dbt ls` confirms full DAG.*
- [x] **PORT-03**: Output artifacts materialize to a sandbox directory, NOT `public/data/`. — *Validated; `external_root: target/sandbox` in profiles.yml.*
- [x] **PORT-04**: Spatial joins preserve current semantics; deviations listed in findings. — *Validated; `ST_Within` + `ST_Distance` fallback in `occurrences.sql`; FORMAT CSV macro deviation documented.*

### Tests & Contracts (TEST)

- [x] **TEST-01**: ≥3 classes of dbt generic test attempted with per-test results recorded. — *Validated; 10 tests across `not_null` / `unique` / `relationships`; 8 PASS, 1 FAIL (awkward-fit), 1 ERROR (awkward-fit) documented in `084-TEST-FINDINGS.md`.*
- [x] **TEST-02**: A dbt model `contract` is declared and enforced on at least one output model; drift behavior documented. — *Validated; 33-column contract on `occurrences` with `enforced: true`; intentional column rename produced exit 1 + compile-time mismatch table; revert restored green.*
- [x] **TEST-03**: ≥1 invariant from `validate-schema.mjs` re-expressed as a dbt test or contract; comparison recorded. — *Validated; side-by-side comparison table records type-blind vs type-aware, CI-time vs build-time tradeoffs.*

### Diffing (DIFF)

- [x] **DIFF-01**: Reproducible diff covers row counts, schema, and key-set equality on `ecdysis_id` / `inat:<id>`. — *Validated; `data/tests/test_dbt_diff.py` (11 tests, all green) covers row count (47,883 equal), schema names+types (33 cols identical), ecdysis_id EXCEPT both ways (0 rows), `host_observation_id` EXCEPT both ways (0 rows; added in `f50b9b2` to close the verifier-flagged inat gap).*
- [x] **DIFF-02**: Spatial-join discrepancies enumerated and root-caused. — *Validated; 84 county-boundary rows across 4 boundary pairs; ecoregion 0 rows; root cause = ST_Within nondeterminism in both implementations (LEFT JOIN with no dedup before fallback).*
- [x] **DIFF-03**: All material diffs classified. — *Validated; 8-row classification table covers every material difference; only non-cosmetic divergence is the 84 boundary rows, classified as semantic divergence to investigate.*

### Partial Runs & Lineage (PART)

- [x] **PART-01**: `dbt run --select` exercised on ≥2 subgraphs; parallelism documented. — *Validated; `staging+` (23 models) and `+occurrences` (21 models); Thread-1..Thread-4 evidence captured; DuckDB serializes execution despite scheduler parallelism.*
- [x] **PART-02**: Model-level lineage artifact captured and referenced. — *Validated; `084-lineage-listing.txt` (23 lines); referenced from findings doc §PART-02.*

### Findings (FIND)

- [x] **FIND-01**: `.planning/research/dbt-spike-findings.md` exists with sections covering worked well / awkward / clearer / less clear. — *Validated; all four sections present.*
- [x] **FIND-02**: Concrete go / no-go / go-with-conditions recommendation. — *Validated; verdict = **GO-WITH-CONDITIONS** with 4 evidence citations.*
- [x] **FIND-03**: Explicit prerequisites covering test coverage / schema decisions / ingestion-vs-transform / parallel-run orchestration / frontend impact. — *Validated; 5-subsection prerequisites block at end of findings doc; each section begins with "Before cutover" and lists BeeAtlas-specific bullets.*

## Traceability

| REQ-ID | Phase | Final Status |
|--------|-------|--------------|
| SCAFFOLD-01 | Phase 83 | ✅ satisfied |
| SCAFFOLD-02 | Phase 83 | ✅ satisfied |
| SCAFFOLD-03 | Phase 83 | ✅ satisfied |
| PORT-01 | Phase 83 | ✅ satisfied |
| PORT-02 | Phase 83 | ✅ satisfied |
| PORT-03 | Phase 83 | ✅ satisfied |
| PORT-04 | Phase 83 | ✅ satisfied |
| TEST-01 | Phase 84 | ✅ satisfied |
| TEST-02 | Phase 84 | ✅ satisfied |
| TEST-03 | Phase 84 | ✅ satisfied |
| DIFF-01 | Phase 84 | ✅ satisfied (inat anti-join added post-verification in `f50b9b2`) |
| DIFF-02 | Phase 84 | ✅ satisfied |
| DIFF-03 | Phase 84 | ✅ satisfied |
| PART-01 | Phase 84 | ✅ satisfied |
| PART-02 | Phase 84 | ✅ satisfied |
| FIND-01 | Phase 84 | ✅ satisfied |
| FIND-02 | Phase 84 | ✅ satisfied |
| FIND-03 | Phase 84 | ✅ satisfied |

**Coverage:** 18/18 v3.3 requirements satisfied. No orphans. No unsatisfied.

## Forward-Looking Requirements (v3.4+ candidates)

These were noted as `## Future Requirements` in the milestone's original REQUIREMENTS.md. The spike's findings doc converts them into a 5-prerequisite checklist; v3.4 should formalize them as concrete REQ-IDs:

- Full migration of remaining transform code to dbt models.
- Hard cutover: replace `export.py`/`species_export.py` in `data/run.py`; retire `_apply_migrations()`; replace `validate-schema.mjs` with dbt contracts.
- Test surface expansion: re-express anti-entropy invariants and pipeline post-conditions as dbt singular tests.
- Resolve awkward-fits: NULL `id` in `stg_inat__observations`; cross-type relationships test.
- Decide samples.parquet shape (separate output vs. folded into occurrences).
- Replace FORMAT CSV macro with a clean GeoJSON emission strategy.
- Adapt `data/nightly.sh` to dbt exit-code semantics.
- Test `materialized='incremental'` for incremental-build behavior.
- Publish dbt-generated lineage docs alongside the project.
