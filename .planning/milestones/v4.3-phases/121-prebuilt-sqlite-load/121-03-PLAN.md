---
phase: 121-prebuilt-sqlite-load
plan: 03
type: execute
wave: 2
depends_on: [121-01, 121-02]
files_modified:
  - package.json
  - package-lock.json
  - src/sqlite-worker-db.ts
autonomous: false
requirements: [PERF-03]
must_haves:
  truths:
    - "hyparquet is removed from package.json devDependencies (and therefore from the production worker bundle)"
    - "npm install reconciles package-lock.json and node_modules with no hyparquet entry"
    - "The retired spike worker file src/sqlite-worker-db.ts is deleted (Plan 02 already ported its logic into src/sqlite-worker.ts)"
    - "npm test passes against the new worker"
    - "Browser benchmark confirms worker tablesReady <= 600 ms and loading-screen-lifted <= 1000 ms on a warm CDN connection"
  artifacts:
    - path: "package.json"
      provides: "dependency manifest with hyparquet removed"
      contains: "wa-sqlite"
    - path: "src/sqlite-worker.ts"
      provides: "MemoryVFS-seeding worker (verification artifact — modified in Plan 02, not this plan)"
      contains: "mapNameToFile"
  key_links:
    - from: "package.json devDependencies"
      to: "src/sqlite-worker.ts imports"
      via: "removal of hyparquet from both"
      pattern: "hyparquet"
---

<objective>
Finalize the spike cutover: remove `hyparquet` from `package.json` (and reconcile `package-lock.json`), delete the now-redundant spike worker file `src/sqlite-worker-db.ts`, run the full test suite, and confirm in the browser that the load-time target is met.

Purpose: Closes the loop on PERF-03 (bundle size / no dead code) and provides the human-in-the-loop benchmark check that the spike's projected ~70% load-time reduction holds in production-shaped conditions.

Output: A clean dependency tree (no hyparquet), a deleted spike file, a green test suite, and a recorded benchmark observation in the SUMMARY.

Note: `scripts/make-local-manifest.js` was already updated during the spike to include `occurrences_db: 'occurrences.db'`, and `src/manifest.ts` already declares `occurrences_db?: string`. Both are listed in the spike findings as completed; this plan does NOT re-edit them.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/quick/260527-spike-prebuilt-sqlite-vfs/FINDINGS.md
@package.json
@scripts/make-local-manifest.js
@src/manifest.ts

<interfaces>
<!-- Reference state at the start of this plan -->

From package.json devDependencies (current):
- `"hyparquet": "^1.25.6"` — to be removed
- All other entries to remain

From scripts/make-local-manifest.js (already correct from spike — DO NOT modify):
- Already emits `occurrences_db: 'occurrences.db'`

From src/manifest.ts (already correct from spike — DO NOT modify):
- Already declares `occurrences_db?: string`
- `resolveDataUrl('occurrences_db')` already works against both local-dev and production manifests

Spike target metrics from FINDINGS.md "Benchmark Results":
- Baseline: worker tablesReady ~1750–2003 ms, loading screen lifted ~2125–2559 ms
- Spike: worker tablesReady ~406–462 ms, loading screen lifted ~723–752 ms
- Phase 121 success criterion: worker tablesReady <= 600 ms, loading screen lifted <= 1000 ms (warm CDN)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove hyparquet dependency and delete spike worker file</name>
  <files>package.json, package-lock.json, src/sqlite-worker-db.ts</files>
  <read_first>
    - package.json (current devDependencies block, including hyparquet entry on line 37)
    - src/sqlite-worker-db.ts (the spike file — confirm Plan 02 has already ported its logic into src/sqlite-worker.ts before deletion)
    - src/sqlite-worker.ts (post Plan 02 — confirm it does NOT import hyparquet, so removal is safe)
  </read_first>
  <action>
    Run `npm uninstall hyparquet` to remove the dependency from `package.json` and reconcile `package-lock.json` and `node_modules`. Do not edit `package.json` by hand — npm's removal keeps the lockfile internally consistent and avoids the multi-minute `msgpackr-extract` native rebuild that nightly.sh's `_LOCK_HASH` cache otherwise mitigates only on the cron host.

    Delete `src/sqlite-worker-db.ts` with `rm src/sqlite-worker-db.ts`. The spike worker is no longer referenced anywhere (Plan 02 ported its logic into `src/sqlite-worker.ts`), and leaving it would confuse future maintainers about which worker is canonical.

    Before deletion, verify nothing else imports it: `grep -rn "sqlite-worker-db" src/ scripts/ data/ 2>/dev/null` should return only matches inside `src/sqlite-worker-db.ts` itself (a file's own filename does not appear in its imports, so the expected result is zero hits).

    Do NOT touch:
    - `scripts/make-local-manifest.js` (already correct from spike — re-check via `grep occurrences_db scripts/make-local-manifest.js` to confirm one match)
    - `src/manifest.ts` (already correct from spike — re-check via `grep occurrences_db src/manifest.ts` to confirm at least one match in the `Manifest` interface)
    - `vite.config.ts` (hyparquet was never explicitly listed in `optimizeDeps`, so no change is needed here — only the `wa-sqlite` entry remains)
  </action>
  <verify>
    <automated>grep -c '"hyparquet"' package.json | grep -q "^0$" && grep -c '"hyparquet"' package-lock.json | grep -q "^0$" && [ ! -f src/sqlite-worker-db.ts ]</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '"hyparquet"' package.json` returns `0`
    - `grep -c '"hyparquet"' package-lock.json` returns `0`
    - `test ! -f src/sqlite-worker-db.ts` exits 0 (file deleted)
    - `grep -rn "from 'hyparquet'" src/` returns no matches
    - `grep -rn "hyparquet" src/` returns no matches
    - `grep -rn "sqlite-worker-db" src/ scripts/ data/ 2>/dev/null` returns no matches
    - `grep -c "occurrences_db" scripts/make-local-manifest.js` returns at least `1` (untouched from spike)
    - `grep -c "occurrences_db" src/manifest.ts` returns at least `1` (untouched from spike)
    - `npm ls hyparquet` reports `(empty)` or that hyparquet is not installed
  </acceptance_criteria>
  <done>
    `hyparquet` is gone from the dependency tree; `src/sqlite-worker-db.ts` is deleted; manifest plumbing established in the spike is preserved.
  </done>
</task>

<task type="auto">
  <name>Task 2: Generate occurrences.db locally and run full test suite</name>
  <files></files>
  <read_first>
    - package.json (scripts block — confirm `npm test` runs `vitest run` and `npm run typecheck` runs `tsc --noEmit`)
    - data/sqlite_export.py (created in Plan 01 — confirm `main()` can be invoked standalone)
  </read_first>
  <action>
    1. Ensure the local `public/data/occurrences.db` exists. If `data/dbt/target/sandbox/occurrences.parquet` exists from a recent local dbt run, generate the DB via `cd data && uv run python sqlite_export.py`. If the sandbox parquet is absent, run `cd data && uv run python run.py` to build everything (slow — minutes — but covers the full pipeline including the new step). Either path leaves `public/data/occurrences.db` in place.
    2. Confirm the local manifest is regenerated: `node scripts/make-local-manifest.js` writes `public/data/manifest.json` containing both `occurrences` and `occurrences_db` keys.
    3. Run `npm run typecheck`. Must exit 0.
    4. Run `npm test`. Must exit 0. Investigate and fix any failure — the most likely failure mode is a test that mocked `parquetReadObjects` or imported the spike file; either delete those tests (now dead) or update them to reflect the new worker.
    5. Record the test run summary (test count, pass count) for the SUMMARY artifact.
  </action>
  <verify>
    <automated>npm run typecheck && npm test</automated>
  </verify>
  <acceptance_criteria>
    - `public/data/occurrences.db` exists and is > 1 MB (sanity: real DB, not an empty file). Check via `test $(stat -f%z public/data/occurrences.db 2>/dev/null || stat -c%s public/data/occurrences.db) -gt 1000000`
    - `public/data/manifest.json` contains both keys: `grep -c '"occurrences"' public/data/manifest.json` returns at least `1` AND `grep -c '"occurrences_db"' public/data/manifest.json` returns exactly `1`
    - `npm run typecheck` exits 0
    - `npm test` exits 0
    - No test file under `src/**/*.test.ts` or `tests/**/*.test.ts` references `hyparquet` or `parquetReadObjects` after this task: `grep -rn -E "(hyparquet|parquetReadObjects)" src/ tests/ 2>/dev/null` returns no matches
  </acceptance_criteria>
  <done>
    Local pipeline outputs include `occurrences.db`; manifest exposes it; full vitest suite is green against the new worker.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Browser benchmark verification</name>
  <what-built>
    The runtime worker now fetches a prebuilt SQLite database from `/data/occurrences.db` and seeds it into MemoryVFS instead of parsing parquet and running an INSERT loop. The spike measured worker `tablesReady` at ~406–462 ms and `loading screen lifted` at ~723–752 ms in Chromium against localhost (baseline: ~1750–2003 ms / ~2125–2559 ms in Firefox). Phase 121 success criteria require `tablesReady` <= 600 ms and `loading screen lifted` <= 1000 ms in Firefox against the warm CDN — this checkpoint confirms the spike's projection survived the production cutover and the cross-browser/cross-network jump.
  </what-built>
  <how-to-verify>
    1. Start the dev server: `npm run dev`
    2. Open the site in Firefox (the browser used for the recorded baseline numbers in FINDINGS.md). Open DevTools -> Console BEFORE navigating to the page so the `[BENCHMARK]` log lines are captured from the first load.
    3. Navigate to the site. Wait for the loading screen to lift and the map to render occurrences.
    4. In the console, find the `[BENCHMARK]` lines emitted by the worker. Record:
       - `[BENCHMARK] fetch occurrences.db: <ms> ms | <MB> MB raw`
       - `[BENCHMARK] open_v2 (preloaded VFS): <ms> ms`
       - `[BENCHMARK] SQL geo query: <ms> ms | <n> rows`
       - `[BENCHMARK] GeoJSON build: <ms> ms | features: <n>`
       - `[BENCHMARK] worker tablesReady: <ms> ms total`
       - `[BENCHMARK] worker boot (main-thread wall time): <ms> ms`
       (Note: the dev server is localhost, so this confirms the localhost shape matches the spike. The strict CDN check happens during the next nightly deploy + a follow-up production load.)
    5. Reload the page and capture a second run (warm WASM module cache) for sanity.
    6. Spot-check the map: click an occurrence point; the sidebar should open with the same fields as before the cutover. Apply a filter (e.g. genus); the visible point set should update.
    7. After deploying via nightly.sh, repeat the benchmark capture against production (https://beeatlas.net) on Firefox with a warm CDN (i.e. load the page once first, then capture the second load). Compare against the <= 600 ms tablesReady / <= 1000 ms loading-screen targets.

    Approve if: (a) localhost benchmarks land in the same general envelope as the spike (tablesReady under ~700 ms, loading screen lifted under ~1000 ms), AND (b) click + filter still work, AND (c) production benchmarks (post-deploy) meet the <= 600 ms / <= 1000 ms targets.

    Reject if: tablesReady drifts above 1000 ms locally, the worker errors with `manifest is missing occurrences_db key`, or click/filter behavior regresses.
  </how-to-verify>
  <resume-signal>Type "approved" with the benchmark numbers captured, or describe the issue.</resume-signal>
</task>

</tasks>

<verification>
- `grep -rn "hyparquet" .` (excluding `node_modules`, `_site`, `.planning`) returns no source matches
- Production load on Firefox, warm CDN, shows `[BENCHMARK] worker tablesReady` <= 600 ms and `[BENCHMARK] worker boot` <= 1000 ms in the console
- Existing click-interaction, SQL filter, and sidebar behavior unchanged (manual smoke per checkpoint)
</verification>

<success_criteria>
1. `hyparquet` no longer in `package.json`, `package-lock.json`, or any `src/` import
2. `src/sqlite-worker-db.ts` deleted
3. `npm test` and `npm run typecheck` green
4. Browser benchmark in Firefox shows worker `tablesReady` <= 600 ms and loading screen lifted <= 1000 ms against warm CDN (post-deploy verification step is part of this checkpoint)
5. Click + filter behavior unchanged
</success_criteria>

<output>
Create `.planning/phases/121-prebuilt-sqlite-load/121-03-SUMMARY.md` when done, including the captured benchmark numbers from Task 3.
</output>
