---
phase: 122-worker-geojson-aggregation
plan: 02
type: execute
wave: 2
depends_on: [122-01]
files_modified: []
autonomous: false
requirements: [PERF-GEO-02]

must_haves:
  truths:
    - "Browser benchmark shows SQL geo agg query time well below 150 ms in Firefox (warm cache)"
    - "loadOccurrenceGeoJSON buffer transfer is near-zero ms (ArrayBuffer transfer, not clone)"
    - "decode+build GeoJSON log line appears in console with feature count matching occurrence count"
    - "Map loads with occurrences visible; click interaction and sidebar work correctly"
  artifacts:
    - path: "public/data/manifest.json"
      provides: "Local manifest with occurrences_db key for dev server"
      contains: "occurrences_db"
    - path: "public/data/occurrences.db"
      provides: "Pre-built SQLite database for local benchmark"
  key_links:
    - from: "npm run dev"
      to: "public/data/occurrences.db"
      via: "manifest.json occurrences_db key → sqlite-worker.ts fetch"
      pattern: "occurrences_db"
---

<objective>
Regenerate the local development manifest and occurrences.db, then run a browser benchmark in Firefox to confirm the Phase 122 optimizations deliver the expected performance improvement.

Purpose: Measure actual SQL geo aggregation time and buffer transfer time against Phase 121 baseline (SQL geo: ~560–570 ms, transfer: ~100–113 ms). Target: SQL geo agg < 150 ms, buffer transfer < 10 ms.
Output: Human-verified benchmark numbers recorded in SUMMARY.md.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/122-worker-geojson-aggregation/RESEARCH.md
@.planning/phases/121-prebuilt-sqlite-load/121-03-SUMMARY.md
@.planning/phases/122-worker-geojson-aggregation/122-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Regenerate local manifest and verify occurrences.db exists</name>
  <files></files>
  <action>
    Run the local manifest generator to ensure public/data/manifest.json is current:

    ```bash
    node scripts/make-local-manifest.js
    ```

    Then verify that public/data/occurrences.db exists and is non-empty:

    ```bash
    ls -lh public/data/occurrences.db
    ```

    If occurrences.db is missing, it must be generated from the pipeline. The database is produced by the data pipeline step added in Phase 121. Run:

    ```bash
    cd data && uv run python run.py
    ```

    This will regenerate occurrences.db in the export directory (public/data/ after the Eleventy hoist). If uv is unavailable or the pipeline fails, report the error — do not proceed to the benchmark checkpoint without a valid occurrences.db.

    After confirming both files exist, run npm test one final time to confirm no regressions from the code changes in Plan 01:

    ```bash
    npm test
    ```
  </action>
  <verify>
    <automated>cd /Users/rainhead/dev/beeatlas && node scripts/make-local-manifest.js && ls -lh public/data/occurrences.db && npm test 2>&1 | tail -5</automated>
  </verify>
  <done>public/data/manifest.json contains "occurrences_db" key; public/data/occurrences.db exists with size > 1 MB; npm test exits 0</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Plan 01 replaced the 92K-row SQL query with a single json_group_array aggregate, and the worker now transfers an ArrayBuffer (zero-copy) instead of structured-cloning a GeoJSON object graph. The main thread decodes and builds GeoJSON.

    Task 1 of this plan regenerated the local manifest and confirmed occurrences.db exists.

    The dev server is NOT started automatically — start it yourself before benchmarking.
  </what-built>
  <how-to-verify>
    1. In a terminal: `npm run dev`
    2. Open Firefox DevTools → Console tab
    3. Navigate to http://localhost:8080 (or the port shown by the dev server)
    4. Wait for the loading screen to disappear
    5. In the Console, look for these [BENCHMARK] log lines (they appear in this order):
       - `[BENCHMARK] WASM instantiate: X ms`
       - `[BENCHMARK] fetch occurrences.db: X ms | X.X MB raw`
       - `[BENCHMARK] open_v2 (preloaded VFS): X ms`
       - `[BENCHMARK] SQL geo agg query: X ms` ← was ~560–570 ms; target: < 150 ms
       - `[BENCHMARK] TextEncoder.encode: X ms | X.X MB`
       - `[BENCHMARK] worker tablesReady: X ms total`
       - `[BENCHMARK] worker boot (main-thread wall time): X ms | main-thread heap: X MB`
       - `[BENCHMARK] loadOccurrenceGeoJSON buffer transfer: X ms | X.X MB` ← was ~100–113 ms; target: < 10 ms
       - `[BENCHMARK] decode+build GeoJSON: X ms | features: XXXXX`
    6. Reload the page once (warm cache) and record the second set of numbers
    7. Verify that occurrence dots appear on the map and the loading screen has lifted
    8. Click one occurrence dot on the map — confirm the sidebar opens with specimen details
    9. Apply a taxon filter — confirm the map updates
    10. Record all [BENCHMARK] line values from the second (warm) run

    Phase 121 baseline (for comparison):
    - SQL geo query: ~560–570 ms
    - loadOccurrenceGeoJSON transfer: ~100–113 ms
    - worker tablesReady: ~920–930 ms

    Success criteria: SQL geo agg query < 150 ms AND buffer transfer < 10 ms AND map/click/filter all work correctly.
  </how-to-verify>
  <resume-signal>
    Type "approved" with the benchmark numbers you observed (copy/paste the console output if possible), or describe any issues encountered.

    Example: "approved — SQL geo agg: 48 ms, encode: 12 ms, tablesReady: 185 ms, transfer: 2 ms, decode+build: 38 ms, features: 92802"
  </resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local dev only | This plan runs only in local dev; no production change |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-122-02-SC | Tampering | npm/pip/cargo installs | accept | No packages installed in this plan |
</threat_model>

<verification>
```bash
# Manifest contains occurrences_db key
grep -c "occurrences_db" public/data/manifest.json
# expect: 1

# occurrences.db is present and non-empty
ls -lh public/data/occurrences.db

# Tests still green
npm test
```
</verification>

<success_criteria>
- public/data/manifest.json contains occurrences_db key
- public/data/occurrences.db exists (size > 1 MB)
- npm test exits 0
- Human-verified Firefox benchmark shows SQL geo agg query < 150 ms and buffer transfer < 10 ms
- Map renders occurrences; click interaction and taxon filter work correctly
</success_criteria>

<output>
Create `.planning/phases/122-worker-geojson-aggregation/122-02-SUMMARY.md` when done.
Record the benchmark numbers (all [BENCHMARK] console lines from warm run) in the SUMMARY.md Accomplishments section.
</output>
