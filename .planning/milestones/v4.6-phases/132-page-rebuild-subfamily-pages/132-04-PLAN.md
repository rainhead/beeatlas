---
phase: 132-page-rebuild-subfamily-pages
plan: 04
type: execute
wave: 3
depends_on: ["132-02", "132-03"]
files_modified:
  - _data/species.js
  - _pages/subfamily.njk
  - src/tests/data-species.test.ts
autonomous: false
requirements: [PAGE-01, PAGE-02, PAGE-03, PAGE-04]
must_haves:
  truths:
    - "_data/species.js reads public/data/higher_taxa.json and stops reading higher_rank_taxon_ids.json (D-03)"
    - "genusList / tribeList / subgenusList taxon_ids and totals come from the rollup, not string-grouping (PAGE-01)"
    - "species.subfamilyList has exactly 12 entries, none named Eumeninae, each with a non-null taxon_id (PAGE-02, D-08)"
    - "subfamilyList carries nested tribes->genera (D-04); tribe-less subfamilies degrade to a flat genus list with no heading (D-05)"
    - "Genus swatch colors on subfamily pages match the by-genus map colors from Plan 03 (Pitfall 2)"
    - "Checklist-only bee species keep their existing grey-swatch / N-checklist-records rendering (PAGE-04, D-09)"
    - "Subfamily pages render at /species/subfamily/{Name}/ with SVG map + specimen/observation counts + iNat link (PAGE-02)"
  artifacts:
    - path: "_data/species.js"
      provides: "rollup-backed genus/tribe/subgenus lists + new subfamilyList"
      contains: "higher_taxa"
    - path: "_pages/subfamily.njk"
      provides: "subfamily page template, nested tribes->genera"
      contains: "species.subfamilyList"
    - path: "src/tests/data-species.test.ts"
      provides: "subfamilyList length/Eumeninae/taxon_id + rollup-sourced taxon_id assertions"
  key_links:
    - from: "_data/species.js"
      to: "public/data/higher_taxa.json"
      via: "readFileSync"
      pattern: "higher_taxa\\.json"
    - from: "_pages/subfamily.njk"
      to: "/data/species-maps/subfamily/"
      via: "img src"
      pattern: "species-maps/subfamily"
---

<objective>
Rewire the Eleventy data layer onto the rollup and ship the new subfamily pages. `_data/species.js`
reads `higher_taxa.json` (retiring the `higher_rank_taxon_ids.json` read, D-03); genus/tribe/subgenus
taxon_ids + totals come from the rollup (PAGE-01); a new `subfamilyList` builds the nested
tribes→genera structure (D-04) with tribe-less flat fallback (D-05) and genus swatch colors matching
the Plan 03 maps; and `_pages/subfamily.njk` renders 12 subfamily pages preserving checklist treatment
(PAGE-04, D-09).

Purpose: Make the templates thin presenters of the dbt rollup and deliver the new subfamily pages —
the user-visible deliverable of this phase.
Output: Rebuilt genus/tribe/subgenus pages and 12 new `/species/subfamily/{Name}/` pages.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/132-page-rebuild-subfamily-pages/132-CONTEXT.md
@.planning/phases/132-page-rebuild-subfamily-pages/132-RESEARCH.md
@.planning/phases/132-page-rebuild-subfamily-pages/132-PATTERNS.md

<interfaces>
<!-- From PATTERNS.md §_data/species.js and §subfamily.njk -->
higher_taxa.json: list of rows {taxon_id, rank, name, family, subfamily, tribe, genus,
specimen_count, inat_obs_count, occurrence_count, species_count, member_taxon_ids}.
Index as higherTaxaByRankName[rank][name] -> row.

Current taxon_id lookups to rewire (species.js): higherRankTaxonIds.genus[g.genus],
.subgenus[g.subgenus], .tribe[t.tribe]  ->  higherTaxaByRankName['genus']?.[g.genus]?.taxon_id, etc.

subfamilyList entry shape (consumed by subfamily.njk):
  { subfamily, family, taxon_id, specimen_count, inat_obs_count, species_count,
    tribesCount, generaCount, totalOccurrences,
    tribes: [ { tribe, taxon_id, specimen_count, inat_obs_count,
                genera: [ { genus, taxon_id, specimen_count, inat_obs_count, hexColor } ] } ],
    genera: [ ... ]   // populated (flat) only when tribes is empty (D-05) }

hslToHex(i*360/N, 70, 50): genus color index i = position in sorted(unique genera for subfamily) —
MUST match the sorted-genus ordering Plan 03's _group_colors used (Pitfall 2).

subfamily.njk pagination: data=species.subfamilyList, size=1, alias=subfamily,
permalink "/species/subfamily/{{ subfamily.subfamily }}/". Map src
"/data/species-maps/subfamily/{{ subfamily.subfamily }}.svg". iNat link guarded by
{%- if subfamily.taxon_id -%}. Checklist rendering: reuse genus.njk's occurrence_count>0 /
on_checklist / 0-records branch (D-09).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Rewire species.js onto higher_taxa.json + build subfamilyList (Wave 0 vitest)</name>
  <files>_data/species.js, src/tests/data-species.test.ts</files>
  <read_first>
    - _data/species.js (file header readFileSync block ~13-24; genusList/subgenusList/tribeList build blocks; taxon_id lookups at ~154/223/262; hslToHex ~82-96; export line ~267)
    - src/tests/data-species.test.ts (existing assertions to extend)
    - .planning/phases/132-page-rebuild-subfamily-pages/132-PATTERNS.md (§_data/species.js — full rewire: higherTaxaByRankName index, tribeList template for subfamilyList, hslToHex genus-granularity, export append)
    - .planning/phases/132-page-rebuild-subfamily-pages/132-RESEARCH.md (§Pattern 3 subfamilyList shape; Pitfall 2 color match; Pitfall 3 ENOENT retirement; Pitfall 4 no Eumeninae)
    - 132-03-SUMMARY.md (the sorted-genus ordering Plan 03 used — match it)
  </read_first>
  <behavior>
    - Test: species.subfamilyList has length 12
    - Test: no subfamilyList entry has subfamily === 'Eumeninae'
    - Test: every subfamilyList entry has a non-null integer taxon_id
    - Test: a multi-tribe subfamily (e.g. Apinae) has non-empty tribes[] each with genera[]; a tribe-less subfamily (e.g. Colletinae) has empty tribes[] and a populated flat genera[] (D-05)
    - Test: genusList[i].taxon_id is populated from the rollup (sourced from higher_taxa.json, not higher_rank_taxon_ids.json)
    - Test: each genus in a subfamily carries a hexColor; for one subfamily the hexColor sequence matches hslToHex over the sorted unique genera
  </behavior>
  <action>
    In `_data/species.js`, replace the `higherRankTaxonIdsPath`/`higherRankTaxonIds` readFileSync with
    `higherTaxaPath = join(repoRoot, 'public/data/higher_taxa.json')` and build
    `higherTaxaByRankName[rank][name] = row`. Rewire the three `taxon_id:` lookups in
    genusList/subgenusList/tribeList to `higherTaxaByRankName[rank]?.[name]?.taxon_id ?? null`. Add a
    `subfamilyList` built on the tribeList template but with the extra tribe→genera nesting: group bee
    species by subfamily, then by tribe, then by genus; produce the nested shape in <interfaces>. For
    tribe-less subfamilies (genus has no tribe), leave `tribes: []` and populate flat `genera: []`
    (D-05). Assign each genus a `hexColor` via `hslToHex(i*360/N, 70, 50)` where i is the genus's
    position in the alphabetically-sorted unique-genera list for that subfamily and N is that count —
    matching the ordering recorded in 132-03-SUMMARY (Pitfall 2). Exclude Eumeninae naturally (no bee
    species). Append `subfamilyList` to the default export. Extend `src/tests/data-species.test.ts`
    with the <behavior> assertions.
  </action>
  <verify>
    <automated>npm test -- data-species</automated>
  </verify>
  <acceptance_criteria>
    - `_data/species.js` reads `higher_taxa.json` and no longer references `higher_rank_taxon_ids` (`grep -c higher_rank_taxon_ids _data/species.js` returns 0)
    - `species.subfamilyList.length === 12`; no entry `subfamily === 'Eumeninae'`; every entry has integer `taxon_id`
    - Apinae has nested tribes→genera; Colletinae has `tribes: []` + flat `genera` (D-05)
    - `genusList` taxon_ids come from the rollup; each subfamily genus has `hexColor` matching `hslToHex` over the sorted genus list
    - `npm test -- data-species` passes
  </acceptance_criteria>
  <done>Data layer reads the rollup; genus/tribe/subgenus rebuilt onto it; subfamilyList (nested + flat fallback + matched swatch colors) exported; old artifact read retired.</done>
</task>

<task type="auto">
  <name>Task 2: subfamily.njk template (nested tribes->genera, flat fallback, checklist preserved)</name>
  <files>_pages/subfamily.njk</files>
  <read_first>
    - _pages/tribe.njk (pagination frontmatter, breadcrumb, metadata, media-grid, iNat link guard — primary analog)
    - _pages/genus.njk (swatch span + checklist branch: occurrence_count>0 / on_checklist / 0-records — D-09)
    - .planning/phases/132-page-rebuild-subfamily-pages/132-PATTERNS.md (§subfamily.njk — full adaptation, D-04 nesting, D-05 fallback)
    - .planning/phases/132-page-rebuild-subfamily-pages/132-RESEARCH.md (Pitfall 6 no county fills)
  </read_first>
  <action>
    Create `_pages/subfamily.njk` cloning `tribe.njk` pagination frontmatter onto
    `species.subfamilyList` (size 1, alias `subfamily`, permalink `/species/subfamily/{{ subfamily.subfamily }}/`).
    Body: breadcrumb `{{ subfamily.family }} / {{ subfamily.subfamily }}`, h1, metadata line
    "{{ subfamily.tribesCount }} tribes · {{ subfamily.generaCount }} genera · {{ subfamily.totalOccurrences }} records"
    (elide the tribes count when tribesCount is 0 per D-05), map img
    `src="/data/species-maps/subfamily/{{ subfamily.subfamily }}.svg"`. Nested list (D-04): iterate
    `subfamily.tribes` rendering each tribe as an `<h2>` linking to `/species/tribe/{{ t.tribe }}/`,
    nesting `for g in t.genera` beneath, each genus a link to `/species/{{ g.genus }}/` with a
    `<span class="swatch" style="background: {{ g.hexColor }};">` and its
    "{{ g.specimen_count }} specimens · {{ g.inat_obs_count }} community observations" count.
    D-05 fallback: if `subfamily.tribes` is empty, render `subfamily.genera` as a flat list with no
    tribe heading. iNat link guarded by `{%- if subfamily.taxon_id -%}`. Reuse the genus.njk checklist
    branch wherever a species count is shown (D-09). Append the
    `<script type="module" src="/src/entries/taxon-page.ts"></script>` line like the analogs.
  </action>
  <verify>
    <automated>npm run build 2>&1 | tail -5 && ls _site/species/subfamily/ | wc -l</automated>
  </verify>
  <acceptance_criteria>
    - `npm run build` succeeds (no ENOENT for higher_rank_taxon_ids — Pitfall 3)
    - `_site/species/subfamily/` contains 12 generated subfamily directories; no `Eumeninae/`
    - A built Apinae page shows nested tribe headings (h2 → /species/tribe/...) with genera nested beneath, genus swatches, and the SVG map src `/data/species-maps/subfamily/Apinae.svg`
    - A built Colletinae page shows a flat genus list with no tribe heading (D-05)
    - `_pages/subfamily.njk` reuses the genus.njk checklist branch (grep shows `on_checklist` and `checklist records`)
  </acceptance_criteria>
  <done>12 subfamily pages build with nested layout, flat fallback, by-genus swatches, SVG maps, iNat links, and preserved checklist treatment.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human-verify subfamily pages + rebuilt totals</name>
  <what-built>
    Subfamily pages at /species/subfamily/{Name}/ (nested tribes→genera, flat fallback for tribe-less
    subfamilies, by-genus map + swatches, checklist treatment) and rebuilt genus/tribe/subgenus totals
    now sourced from the dbt rollup.
  </what-built>
  <how-to-verify>
    1. Run `npm run dev` and open http://localhost:8080/species/subfamily/Apinae/ — confirm nested
       tribe headings with genera beneath, the SVG map renders, and each genus swatch color visibly
       matches its dot cluster on the map (Pitfall 2).
    2. Open http://localhost:8080/species/subfamily/Colletinae/ — confirm a flat genus list, no empty
       tribe chrome (D-05), metadata line omits the tribes count.
    3. Spot-check totals against the RESEARCH baselines on at least 5 genus/tribe pages spanning
       families: /species/Andrena/ (3,589 specimens · 2,735 community observations),
       /species/Bombus/ (1,768 · 7,763), /species/Megachile/ (1,186 · 480),
       /species/Lasioglossum/ (1,718 · 115), /species/Osmia/ (1,110 · 450),
       /species/tribe/Bombini/ (1,768 · 7,763) — confirm displayed totals match (success criterion 1).
    4. Open any genus page with a checklist-only species and confirm the grey swatch + "N checklist
       records" rendering is unchanged (PAGE-04, D-09).
    5. Confirm no /species/subfamily/Eumeninae/ page exists.
  </how-to-verify>
  <resume-signal>Type "approved" or describe any total mismatch / layout / color-correspondence issue.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| higher_taxa.json → Eleventy build | A stale/missing artifact or a wrong subfamilyList ships wrong public pages/links (build-integrity) |
| JS swatch color ↔ Python map color | Independent HSL implementations must agree per genus or page mis-correlates |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-132-11 | Denial of service | species.js still reads retired higher_rank_taxon_ids.json -> build ENOENT (Pitfall 3) | mitigate | rewire species.js to higher_taxa.json in the same plan; grep gate 0 occurrences; `npm run build` succeeds |
| T-132-12 | Information disclosure | Eumeninae subfamily page published (HIER-05) | mitigate | subfamilyList excludes Eumeninae (no bee species); vitest length-12 + no-Eumeninae; build-output dir check |
| T-132-13 | Tampering | rebuilt page totals drift from baseline (PAGE-01) | mitigate | totals sourced from rollup (Plan 01 baseline-tested); human-verify spot-check of 5 taxa against baselines |
| T-132-14 | Tampering | swatch color ≠ map dot color (Pitfall 2) | mitigate | hslToHex uses same sorted-genus order as Plan 03 _group_colors; vitest hexColor-sequence check + human-verify correspondence |
| T-132-15 | Tampering | checklist-only species rendering regresses (PAGE-04) | mitigate | reuse genus.njk checklist branch verbatim (D-09); human-verify grey-swatch/N-records |
</threat_model>

<verification>
- `npm test -- data-species` passes (subfamilyList length 12, no Eumeninae, taxon_id populated, nested/flat shapes, hexColor)
- `npm run build` succeeds; `_site/species/subfamily/` has 12 dirs, no Eumeninae
- Human-verify: 5-taxa baseline spot-check, nested/flat layouts, swatch↔dot color match, checklist treatment
</verification>

<success_criteria>
- genus/tribe/subgenus pages display rollup-sourced totals matching baselines (success criterion 1)
- 12 subfamily pages live at /species/subfamily/{Name}/ with maps + counts + nested/flat layout (success criterion 2, PAGE-02)
- public slugs name-based, taxon_id-keyed internally, no Eumeninae (PAGE-02/03, D-08)
- checklist-only species treatment preserved (PAGE-04, D-09)
</success_criteria>

<output>
Create `.planning/phases/132-page-rebuild-subfamily-pages/132-04-SUMMARY.md` when done.
Record: human-verify outcome, the 5-taxa baseline spot-check results, and confirmation the
higher_rank_taxon_ids.json read is gone.
</output>
