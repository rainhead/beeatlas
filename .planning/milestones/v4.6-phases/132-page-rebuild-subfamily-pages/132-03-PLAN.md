---
phase: 132-page-rebuild-subfamily-pages
plan: 03
type: execute
wave: 2
depends_on: ["132-01"]
files_modified:
  - data/species_maps.py
  - data/tests/test_species_maps.py
autonomous: true
requirements: [PAGE-02]
must_haves:
  truths:
    - "_generate_group_maps emits public/data/species-maps/subfamily/{Name}.svg for each of the 12 bee subfamilies"
    - "Subfamily SVG dots are colored by GENUS (one color per genus), not by species (D-06)"
    - "Genus color assignment uses _group_colors over the alphabetically-sorted genus list for the subfamily, so it matches the species.js swatch coloring (Pitfall 2)"
    - "No Eumeninae subfamily map is generated (data gate via species.parquet bee-only rows, D-08)"
  artifacts:
    - path: "data/species_maps.py"
      provides: "subfamily group-map pass (color-by-genus)"
      contains: "subfamily"
    - path: "data/tests/test_species_maps.py"
      provides: "subfamily SVG existence + per-genus single-color assertions"
  key_links:
    - from: "data/species_maps.py"
      to: "public/data/species-maps/subfamily/"
      via: "_write_group_svg(subfamily_name, ..., subfamily_dir)"
      pattern: "subfamily_dir"
---

<objective>
Add a subfamily pass to `_generate_group_maps` in `data/species_maps.py` that emits one SVG per bee
subfamily at `public/data/species-maps/subfamily/{Name}.svg`, colored by GENUS granularity (D-06) —
reusing the existing `_group_colors` / `_write_group_svg` machinery used by the genus/tribe/subgenus
passes.

Purpose: Supply the SVG occurrence maps that the new subfamily pages render, with genus-level coloring
that correlates to the genus swatches on the page.
Output: 12 subfamily SVGs under `public/data/species-maps/subfamily/`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/132-page-rebuild-subfamily-pages/132-CONTEXT.md
@.planning/phases/132-page-rebuild-subfamily-pages/132-RESEARCH.md
@.planning/phases/132-page-rebuild-subfamily-pages/132-PATTERNS.md

<interfaces>
<!-- From PATTERNS.md §species_maps.py -->
_group_colors(sorted_names) -> dict[name, hex] using deterministic HSL (hue = i*360/N).
_write_group_svg(slug_path, species_points, colors, backdrop, out_dir) -> int clipped count.
  species_points: dict[canonical_name, list[(lon,lat)]]; colors: dict[canonical_name, hex].
The species.parquet query selects rows WHERE occurrence_count > 0 (bee-only; no Eumeninae present).
Extend the query to also SELECT subfamily. Build subfamily_members[subfamily] -> [canonical_name] and
genus_of[canonical_name] -> genus. Color each species by its genus's color from
_group_colors(sorted(unique genera in subfamily)).
_UNRESOLVED_COLOR is the fallback for unresolved members.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wave 0 subfamily-map tests + subfamily pass in _generate_group_maps</name>
  <files>data/tests/test_species_maps.py, data/species_maps.py</files>
  <read_first>
    - data/species_maps.py (_generate_group_maps tribe pass ~lines 379-395; the species.parquet SELECT ~line 315; the membership build loop ~lines 330-342; _group_colors ~138-164; _write_group_svg ~261-291; _UNRESOLVED_COLOR; final print)
    - data/tests/test_species_maps.py (existing sandbox-gated test patterns for group-map output)
    - .planning/phases/132-page-rebuild-subfamily-pages/132-PATTERNS.md (§species_maps.py — full subfamily-pass adaptation, color-by-GENUS, _write_group_svg call convention)
    - .planning/phases/132-page-rebuild-subfamily-pages/132-RESEARCH.md (§Pattern 4, Pitfall 2 color-match, Pitfall 6 no county fills on group SVGs)
  </read_first>
  <behavior>
    - Test (sandbox-gated): after _generate_group_maps, species-maps/subfamily/{Name}.svg exists for each of the 12 bee subfamilies
    - Test (sandbox-gated): no species-maps/subfamily/Eumeninae.svg exists (D-08)
    - Test (sandbox-gated): in an Apinae subfamily SVG, all circles belonging to genus Bombus share one hex fill (genus-level coloring, D-06)
  </behavior>
  <action>
    Add the subfamily-map assertions to `data/tests/test_species_maps.py` (sandbox-gated like the
    existing group-map tests): assert 12 subfamily SVGs by name, assert Eumeninae.svg absent, and parse
    one Apinae SVG to confirm all dots for genus Bombus share a single fill color. Then modify
    `_generate_group_maps`: extend the species.parquet SELECT to include `subfamily`; in the membership
    loop build `subfamily_members[subfamily].append(canonical_name)` and `genus_of[canonical_name]=genus`.
    After the tribe pass add a subfamily pass that, per subfamily, computes
    `genera_in_sf = sorted(set(genus_of[c] for c in members))`, `genus_colors = _group_colors(genera_in_sf)`,
    maps each member species to its genus color (unresolved -> _UNRESOLVED_COLOR), and calls
    `_write_group_svg(subfamily_name, species_points, colors, backdrop, subfamily_dir)` where
    `subfamily_dir = maps_dir / "subfamily"`. Do NOT add county-fill logic (group SVGs have none — Pitfall 6).
    Update the final print() to include the subfamily count. Eumeninae is naturally absent because
    species.parquet carries no Eumeninae bee species (D-08).
  </action>
  <verify>
    <automated>bash data/dbt/run.sh build --select higher_taxa species && cd data && uv run pytest tests/test_species_maps.py -x</automated>
  </verify>
  <acceptance_criteria>
    - After running species-maps generation, `public/data/species-maps/subfamily/` contains 12 SVGs named for the 12 bee subfamilies; `Eumeninae.svg` is absent
    - In an Apinae SVG, every circle for genus Bombus uses the same hex fill (genus-granularity coloring, D-06)
    - `cd data && uv run pytest tests/test_species_maps.py -x` passes
    - `grep -c county_fill data/species_maps.py` shows the subfamily pass added no new county-fill logic to group SVGs
  </acceptance_criteria>
  <done>12 subfamily SVGs generated with by-genus coloring; tests green; no wasp map; no county fills on group SVGs.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| species.parquet → subfamily SVGs | Bee-only species rows drive the maps; a gating error could render a bycatch (wasp) map |
| Python genus coloring ↔ JS swatch coloring | Two independent HSL implementations must agree per genus or the page mis-correlates dots to swatches |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-132-08 | Information disclosure | Eumeninae (wasp) subfamily map rendered onto bee surface (HIER-05) | mitigate | data gate (species.parquet has no Eumeninae rows) + pytest assertion Eumeninae.svg absent |
| T-132-09 | Tampering | genus dot color ≠ page swatch color (Pitfall 2) | mitigate | both sides use _group_colors / hslToHex over the same alphabetically-sorted genus list; per-genus single-color SVG test; cross-checked against Plan 04 swatch coloring |
| T-132-10 | Tampering | per-species coloring leaks in (illegible, wrong granularity) | mitigate | pytest asserts all Bombus circles share one fill (genus granularity, D-06) |
</threat_model>

<verification>
- `cd data && uv run pytest tests/test_species_maps.py -x` passes (12 SVGs, no Eumeninae, by-genus coloring)
- After species-maps generation, `ls public/data/species-maps/subfamily/ | wc -l` equals 12
</verification>

<success_criteria>
- 12 subfamily SVG occurrence maps exist, colored by genus (D-06), consistent with existing group maps
- No wasp (Eumeninae) map (D-08, HIER-05) — supports success criterion 2
</success_criteria>

<output>
Create `.planning/phases/132-page-rebuild-subfamily-pages/132-03-SUMMARY.md` when done.
Record: the 12 subfamily SVG names, confirmation of by-genus coloring, and the sorted-genus-list
ordering used (so Plan 04 swatch coloring matches).
</output>
