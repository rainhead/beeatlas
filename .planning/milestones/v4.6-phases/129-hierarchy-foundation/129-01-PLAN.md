---
phase: 129-hierarchy-foundation
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - data/tests/test_sqlite_export.py
autonomous: true
requirements: [HIER-01, HIER-02, HIER-03, HIER-04, HIER-05, HIER-06]

must_haves:
  truths:
    - "All 9 hierarchy test functions exist in test_sqlite_export.py and fail (RED) because _build_taxon_hierarchy is not yet implemented"
    - "A deterministic mini taxa.csv.gz fixture exists with known Anthophila + bycatch rows"
    - "A parquet fixture with a taxon_id column (bee, bycatch, NULL) exists"
    - "The existing 5 occurrences-table tests still pass"
  artifacts:
    - path: "data/tests/test_sqlite_export.py"
      provides: "9 hierarchy test functions + taxa_csv_gz and src_parquet_with_taxon fixtures"
      contains: "def test_taxa_table_exists"
  key_links:
    - from: "data/tests/test_sqlite_export.py"
      to: "sqlite_export.generate_sqlite (taxa_path kwarg)"
      via: "import + call with taxa_path injected"
      pattern: "generate_sqlite\\(.*taxa_path"
---

<objective>
Create the Wave 0 Nyquist test scaffold for the taxon hierarchy build. Add 9 hierarchy
test functions plus two new fixtures to the existing `data/tests/test_sqlite_export.py`,
following the self-extension pattern in 129-PATTERNS.md. These tests define the contract
that the implementation in Plan 02 must satisfy — they MUST be RED (failing) at the end
of this plan because `_build_taxon_hierarchy` does not exist yet and `generate_sqlite` does
not yet accept a `taxa_path` keyword.

Purpose: Establish the executable acceptance contract before any implementation, satisfying
the Nyquist validation strategy in 129-VALIDATION.md (no 3 consecutive tasks without an
automated verify; Wave 0 creates all MISSING test references).
Output: Extended `test_sqlite_export.py` with fixtures `taxa_csv_gz` + `src_parquet_with_taxon`
and the 9 named test functions, all RED.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/129-hierarchy-foundation/129-CONTEXT.md
@.planning/phases/129-hierarchy-foundation/129-RESEARCH.md
@.planning/phases/129-hierarchy-foundation/129-PATTERNS.md
@.planning/phases/129-hierarchy-foundation/129-VALIDATION.md
@data/tests/test_sqlite_export.py

<interfaces>
<!-- Contract the test scaffold defines for Plan 02 to implement. -->
<!-- Plan 02 MUST make generate_sqlite accept an optional taxa_path kwarg and add -->
<!-- _build_taxon_hierarchy + _assert_no_orphan_taxon_ids. These signatures are fixed here. -->

From data/sqlite_export.py (EXISTING, to be extended in Plan 02):
- generate_sqlite(src_parquet: Path, dst_db: Path) -> None
  Plan 02 adds: taxa_path: Path | None = None, db_path: str | None = None
- (NEW in Plan 02) _build_taxon_hierarchy(con: duckdb.DuckDBPyConnection, dst_db: Path) -> None
- (NEW in Plan 02) _assert_no_orphan_taxon_ids(db_path: Path) -> None  # raises ValueError

taxa table contract (target schema, materialized-path default per D-02):
- taxon_id INTEGER PRIMARY KEY, rank TEXT NOT NULL, name TEXT NOT NULL,
  lineage_path TEXT (NULL for bycatch), is_anthophila INTEGER NOT NULL (1 bee / 0 bycatch)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add hierarchy fixtures (mini taxa.csv.gz + parquet with taxon_id)</name>
  <files>data/tests/test_sqlite_export.py</files>
  <read_first>
    - data/tests/test_sqlite_export.py (existing file — extend, do NOT replace the 5 existing tests or the src_parquet fixture)
    - .planning/phases/129-hierarchy-foundation/129-PATTERNS.md §"Import/fixture pattern" (the TAXA_ROWS, taxa_csv_gz, PARQUET_WITH_TAXON_ROWS, src_parquet_with_taxon blocks are the prescriptive templates)
    - .planning/phases/129-hierarchy-foundation/129-RESEARCH.md §"Validation Architecture" (Wave 0 Gaps)
  </read_first>
  <action>
    Append (do not replace existing content) two new pytest fixtures to test_sqlite_export.py,
    using the exact templates in 129-PATTERNS.md §"Import/fixture pattern":
    (1) `taxa_csv_gz(tmp_path)` — writes a mini gzip TSV with header
    `taxon_id, ancestry, rank_level, rank, name, active` and the TAXA_ROWS rows from PATTERNS.md
    (Anthophila superfamily 630955, Apidae family 47221, Apis genus 52775, Apis mellifera species
    47219 — all `active='true'` with Anthophila ancestry; plus bycatch Vespidae family 52747 whose
    ancestry does NOT contain 630955). Write via stdlib gzip + csv with `delimiter='\t'`.
    (2) `src_parquet_with_taxon(tmp_path)` — a parquet including a `taxon_id` column (int64) with one
    bee row (47219), one bycatch row (52747), and one NULL taxon_id row — per PARQUET_WITH_TAXON_ROWS.
    Reuse the existing pyarrow import pattern (pa.table / pq.write_table). Keep the existing
    `src_parquet` fixture and PARQUET_ROWS unchanged.
  </action>
  <verify>
    <automated>cd data && uv run pytest tests/test_sqlite_export.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: `grep -n "def taxa_csv_gz" data/tests/test_sqlite_export.py` returns a match.
    - Source assertion: `grep -n "def src_parquet_with_taxon" data/tests/test_sqlite_export.py` returns a match.
    - Behavior: the existing 5 tests (test_creates_occurrences_table, test_row_count_matches,
      test_column_names_match, test_overwrites_existing_db, test_main_uses_sandbox_and_export_dir)
      still pass — fixtures alone introduce no collection errors.
    - Source assertion: the bycatch fixture row 52747 has an ancestry string that does NOT contain
      `/630955/` (verifies it is genuinely non-Anthophila for the is_anthophila test).
  </acceptance_criteria>
  <done>Both fixtures present; pytest collects the file with no import/syntax errors; existing 5 tests green.</done>
</task>

<task type="auto">
  <name>Task 2: Add the 9 RED hierarchy test functions</name>
  <files>data/tests/test_sqlite_export.py</files>
  <read_first>
    - data/tests/test_sqlite_export.py (now contains the two new fixtures from Task 1)
    - .planning/phases/129-hierarchy-foundation/129-PATTERNS.md §"Test function pattern" (test_taxa_table_exists, test_zero_orphan_taxon_ids, test_orphan_assertion_raises, test_is_anthophila_flag are prescriptive templates; follow the same 4-line body shape for the rest)
    - .planning/phases/129-hierarchy-foundation/129-VALIDATION.md §"Per-Task Verification Map" (the 9 exact test function names)
  </read_first>
  <action>
    Add exactly these 9 test functions, named verbatim per 129-VALIDATION.md, each calling
    `generate_sqlite(src_parquet_with_taxon, dst, taxa_path=taxa_csv_gz)` (and `db_path` where PASS 3
    is exercised — pass the test's own duckdb or None; PASS 3 checklist coverage is NOT asserted here
    since the mini fixture has no checklist join, so test_active_taxa_only and counts use the mini
    fixture's own rows):
    1. `test_taxa_table_exists` — `taxa` in sqlite_master tables.
    2. `test_zero_orphan_taxon_ids` — `SELECT COUNT(*) FROM occurrences WHERE taxon_id IS NOT NULL AND
       taxon_id NOT IN (SELECT taxon_id FROM taxa)` == 0.
    3. `test_taxa_name_rank_non_null` — no NULL `name` or `rank` for any taxon_id referenced by occurrences.
    4. `test_apidae_descendant_query` — `SELECT taxon_id FROM taxa WHERE taxon_id = 47221 OR
       instr(lineage_path, '/47221/') > 0` returns > 0 rows and every returned row has is_anthophila = 1.
    5. `test_active_taxa_only` — bee taxa in `taxa` came from the `active = 'true'` PASS 1 set (assert the
       known active bee 47219 is present; assert no taxon outside the fixture's active+bycatch set appears).
    6. `test_orphan_assertion_raises` — build a bare db with an occurrence taxon_id absent from an empty
       `taxa` table; `_assert_no_orphan_taxon_ids(dst)` raises `ValueError` matching "orphan" (template in PATTERNS.md).
    7. `test_is_anthophila_flag` — taxon 47219 has is_anthophila = 1; bycatch 52747 has is_anthophila = 0
       (template in PATTERNS.md).
    8. `test_bycatch_present_in_taxa` — bycatch family 52747 is present in `taxa` with is_anthophila = 0 and
       a non-null `name` and `rank` (finest-rank resolution per D-05; lineage_path may be NULL).
    9. `test_complex_and_bycatch_counts` — the bycatch count `SELECT COUNT(*) FROM taxa WHERE is_anthophila = 0`
       is queryable and returns the expected fixture bycatch count (1 for the mini fixture); the complex-rank
       count query `SELECT COUNT(*) FROM taxa WHERE rank = 'complex'` runs without error.
    Do NOT implement _build_taxon_hierarchy — these tests MUST be RED now.
  </action>
  <verify>
    <automated>cd data && uv run pytest tests/test_sqlite_export.py -q 2>&1 | grep -E "9 (failed|error)" || cd data && uv run pytest tests/test_sqlite_export.py -k "taxa or orphan or anthophila or bycatch or apidae or complex or active" -q</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: all 9 names present —
      `grep -c -E "def (test_taxa_table_exists|test_zero_orphan_taxon_ids|test_taxa_name_rank_non_null|test_apidae_descendant_query|test_active_taxa_only|test_orphan_assertion_raises|test_is_anthophila_flag|test_bycatch_present_in_taxa|test_complex_and_bycatch_counts)" data/tests/test_sqlite_export.py` returns 9.
    - Behavior (RED gate): running the 9 hierarchy tests fails/errors (because generate_sqlite does not
      accept taxa_path and _build_taxon_hierarchy/_assert_no_orphan_taxon_ids do not exist). This RED state
      is the correct outcome of this plan.
    - Behavior: the original 5 occurrences tests remain green.
  </acceptance_criteria>
  <done>9 hierarchy tests exist and are RED; 5 pre-existing tests stay green; no collection errors.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| taxa.csv.gz (iNat Open Data) → pipeline | Untrusted upstream taxonomy file; schema/content can drift between nightly downloads |
| occurrences.parquet (dbt) → occurrences.db | Internal build artifact; `taxon_id` values must all resolve in the hierarchy |

Note: This is a build-time data pipeline with NO server runtime and NO user input surface
(static hosting). The realistic threat surface is data-integrity corruption shipping to
production, not web auth/injection.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-129-01 | Tampering | Wave 0 test fixtures (mini taxa.csv.gz) | mitigate | Fixtures are deterministic in-repo Python literals; no network fetch; reviewed in PR |
| T-129-02 | Information Disclosure | test data | accept | Fixture rows are public iNat taxon IDs; no PII or secrets |
| T-129-SC | Tampering | npm/pip/cargo installs | mitigate | No new packages this phase (RESEARCH.md Package Legitimacy Audit: none added); slopcheck N/A |
</threat_model>

<verification>
- `cd data && uv run pytest tests/test_sqlite_export.py` — 5 existing tests green, 9 new tests RED.
- All 9 test function names match 129-VALIDATION.md verbatim.
- Two new fixtures present; no replacement of existing fixtures/tests.
</verification>

<success_criteria>
- 9 hierarchy test functions added with the exact names from 129-VALIDATION.md.
- `taxa_csv_gz` and `src_parquet_with_taxon` fixtures present and deterministic.
- New tests are RED (implementation absent); existing 5 tests green.
</success_criteria>

<output>
Create `.planning/phases/129-hierarchy-foundation/129-01-SUMMARY.md` when done.
</output>
