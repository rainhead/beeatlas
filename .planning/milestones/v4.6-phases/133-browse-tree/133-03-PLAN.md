---
phase: 133-browse-tree
plan: 03
type: execute
wave: 2
depends_on: ["133-01"]
files_modified:
  - src/styles/taxon-pages.css
autonomous: true
requirements: [TREE-01, TREE-02]
must_haves:
  truths:
    - "The control bar lays out the filter input and 'Show all ranks' toggle on one row, wrapping below 480px"
    - "Tree node summaries are flex rows: name (flex 1), muted counts, then a 44×44px 🗺 tap target"
    - "Nested ranks are indented 1.5rem per depth (1rem under 480px); top-level family nodes are not indented"
    - "All interactive elements (summary, node-map, filter input, toggle) show a visible focus ring using --accent"
  artifacts:
    - path: "src/styles/taxon-pages.css"
      provides: "Control-bar + tree-node styling under the .species-index modifier scope"
      contains: "species-index-controls"
  key_links:
    - from: "src/styles/taxon-pages.css"
      to: "_pages/species.njk class names"
      via: ".species-index modifier selectors matching UI-SPEC classes"
      pattern: "tree-node"
---

<objective>
Add the control-bar and tree-node styling to `src/styles/taxon-pages.css` under the
existing `.species-index` modifier scope, implementing the UI-SPEC visual contract:
flex control bar, flex `<summary>` rows, muted counts, an accessible 44×44px 🗺 tap
target, per-depth indentation, the toggle active indicator, and focus-visible rings.
No new token system — only existing `src/index.css` custom properties (D-11).

Purpose: D-11 (visual chrome to UI-SPEC), supporting TREE-01 (legible nesting) and
TREE-02 (count typography). Styling keys on stable UI-SPEC class names, so this can
land in parallel with the template (Plan 02) — no file overlap.
Output: extended `.species-index` CSS block.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/133-browse-tree/133-CONTEXT.md
@.planning/phases/133-browse-tree/133-PATTERNS.md
@.planning/phases/133-browse-tree/133-UI-SPEC.md
@CLAUDE.md

<interfaces>
<!-- Class contract (from UI-SPEC §Component Inventory + 133-PATTERNS.md §taxon-pages.css):
  .species-index-controls      flex row, gap .5rem, mb .5rem, flex-wrap (mobile)
  #species-filter              flex 1 1 auto; min-width 0; focus → border-color var(--accent), outline none
  .rank-toggle-label           flex 0 0 auto; white-space nowrap; :has(#show-all-ranks:checked) → accent border (active indicator)
  details.tree-node > summary  flex; align-items baseline; gap .5rem; cursor pointer; padding .25rem 0
  .node-name                   flex 1 1 auto
  .node-counts                 flex 0 0 auto; .85rem; color var(--text-muted)
  .node-map                    flex 0 0 auto; .85rem; color var(--text-muted); padding .5rem; margin -.5rem (44×44 tap target); hover/focus → var(--text-body)
  details.tree-node            padding-left 1.5rem (per depth); > .tree-node--family → padding-left 0
  focus-visible (summary, .node-map, #show-all-ranks): outline 2px solid var(--accent); outline-offset 2px
  @media (max-width:480px): details.tree-node padding-left 1rem
  Existing block to extend: .species-index #species-filter / .family-section / .genus-row (lines ~121–140);
  existing .species-list / .count (lines ~72–91) — node-counts mirrors .count (.85rem / --text-muted). -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend .species-index CSS with control-bar + tree-node rules</name>
  <files>src/styles/taxon-pages.css</files>
  <read_first>
    - src/styles/taxon-pages.css (extend the existing `.species-index` block ~lines 121–140; reuse `.species-list`/`.count` conventions ~lines 72–91)
    - .planning/phases/133-browse-tree/133-PATTERNS.md §src/styles/taxon-pages.css (the full "New rules to add" block — control bar, toggle active indicator, summary flex, node-name/counts/map, indentation, focus-visible, 480px media query)
    - .planning/phases/133-browse-tree/133-UI-SPEC.md (§Spacing Scale, §Typography, §Color, §Visual Specifications, §Focus-visible styling)
    - src/index.css (confirm the custom-property names: --surface-subtle, --border, --text-muted, --text-body, --link, --accent)
  </read_first>
  <action>
    Append new rules under the `.species-index` modifier scope (keep existing rules;
    if you fold the standalone `.species-index #species-filter` into the control-bar
    selector, do not drop its padding/background/border declarations). Add, using ONLY
    existing custom properties from src/index.css with literal fallbacks as in the
    current file:
    - `.species-index .species-index-controls`: display flex; align-items center;
      gap 0.5rem; margin-bottom 0.5rem; flex-wrap wrap.
    - filter input inside the bar: flex 1 1 auto; min-width 0 (retain existing visual styling).
    - `.species-index #species-filter:focus`: outline none; border-color var(--accent)
      (mirrors the bee-pane.ts focus pattern in light DOM).
    - `.species-index .rank-toggle-label`: flex 0 0 auto; white-space nowrap; font-size 0.875rem.
    - Toggle active indicator: `.species-index .rank-toggle-label:has(#show-all-ranks:checked)`
      → border 1px solid var(--accent); border-radius 4px; padding 0.25rem 0.5rem
      (non-color signal also present via the checkbox itself per UI-SPEC).
    - `.species-index .tree-node > summary`: display flex; align-items baseline;
      gap 0.5rem; cursor pointer; padding 0.25rem 0.
    - `.species-index .node-name`: flex 1 1 auto.
    - `.species-index .node-counts`: flex 0 0 auto; font-size 0.85rem; color var(--text-muted).
    - `.species-index .node-map`: flex 0 0 auto; font-size 0.85rem; color var(--text-muted);
      padding 0.5rem; margin -0.5rem (expands tap target to ≥44×44px without shifting layout);
      hover/focus-visible → color var(--text-body).
    - Indentation: `.species-index details.tree-node { padding-left: 1.5rem }` and
      `.species-index > details.tree-node--family { padding-left: 0 }`.
    - Focus-visible: `.species-index summary:focus-visible, .species-index .node-map:focus-visible,
      .species-index #show-all-ranks:focus-visible { outline: 2px solid var(--accent);
      outline-offset: 2px; border-radius: 2px }`.
    - `@media (max-width: 480px) { .species-index details.tree-node { padding-left: 1rem } }`.
    Do NOT introduce new CSS custom properties or a parallel token system (D-11).
  </action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'species-index-controls' src/styles/taxon-pages.css` is ≥ 1.
    - `grep -c 'node-map' src/styles/taxon-pages.css` is ≥ 1 and a rule sets `padding: 0.5rem` with `margin: -0.5rem` for the 44×44 target.
    - `:has(#show-all-ranks:checked)` rule present (toggle active indicator).
    - `:focus-visible` rules reference `var(--accent)`; 480px media query reduces indent to 1rem.
    - No new `--` custom property is *declared* (only referenced); `npm run build` succeeds.
  </acceptance_criteria>
  <done>The .species-index block carries the full UI-SPEC styling for control bar, summary rows, counts, 🗺 tap target, indentation, toggle indicator, and focus rings — using only existing tokens.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | CSS-only change; no data, input, or URL handling. Inherits the page's existing trust posture. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-133-06 | Elevation/Tampering | CSS scope leakage | accept | All new rules are scoped under `.species-index`; no global selectors; no `!important`; styling cannot exfiltrate or alter data. Low risk, no PII. |
| T-133-SC | Tampering | npm/pip/cargo installs | accept | No package installs (uses existing tokens only); nothing to gate. |
</threat_model>

<verification>
- `npm run build` green.
- Manual visual check deferred to Plan 04's human-verify checkpoint (where JS behavior is also present).
</verification>

<success_criteria>
- Control bar, tree-node summary rows, muted counts, accessible 🗺 tap target,
  per-depth indentation, toggle active indicator, and focus-visible rings are all
  styled to the UI-SPEC contract using only existing tokens (D-11), supporting
  TREE-01/TREE-02 legibility.
</success_criteria>

<output>
Create `.planning/phases/133-browse-tree/133-03-SUMMARY.md` when done
</output>
