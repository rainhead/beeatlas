---
phase: 131-occurrence-normalization
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - src/tests/build-geojson.test.ts
  - src/tests/filter.test.ts
  - src/tests/bee-table.test.ts
autonomous: true
requirements: [NORM-01, NORM-02, NORM-03]
must_haves:
  truths:
    - "build-geojson.test.ts exercises the 7-field geo_blob layout and the { geojson }-only return shape"
    - "filter.test.ts asserts queryTablePage SQL contains LEFT JOIN taxa and display_name, and that OCCURRENCE_COLUMNS excludes the 4 dropped columns"
    - "bee-table.test.ts fixtures use display_name and no longer construct dropped DataSummary count fields"
    - "The rewritten/updated test files FAIL against current source (RED) — they encode the target behavior before implementation"
  artifacts:
    - path: "src/tests/build-geojson.test.ts"
      provides: "RED test for 7-field geo_blob decode + { geojson } return"
      contains: "geojson"
    - path: "src/tests/filter.test.ts"
      provides: "RED test for LEFT JOIN taxa + display_name + slimmer OCCURRENCE_COLUMNS"
      contains: "display_name"
    - path: "src/tests/bee-table.test.ts"
      provides: "RED fixtures using display_name"
      contains: "display_name"
  key_links:
    - from: "src/tests/build-geojson.test.ts"
      to: "src/features.ts _buildGeoJSONFromRaw"
      via: "import + call"
      pattern: "_buildGeoJSONFromRaw"
    - from: "src/tests/filter.test.ts"
      to: "src/filter.ts queryTablePage / OCCURRENCE_COLUMNS"
      via: "import + SQL-string assertion"
      pattern: "LEFT JOIN taxa"
---

<objective>
Rewrite/extend the three test files to encode Phase 131's target behavior BEFORE any
source change. This is the Wave 0 RED step: after this plan the suite MUST fail
(`build-geojson.test.ts` against the old 10-field layout/return shape; `filter.test.ts`
against the un-migrated queries; `bee-table.test.ts` against the old fixtures). Plans 02
and 03 turn these GREEN.

Purpose: The geo_blob positional coupling (sqlite_export.py ↔ features.ts) is the
highest-risk surface — a silent wrong-data failure, not a thrown error. The test must
pin the new 7-field positional layout explicitly so the implementation cannot drift.
Output: Three updated test files that are RED against current source.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/131-occurrence-normalization/131-CONTEXT.md
@.planning/phases/131-occurrence-normalization/131-RESEARCH.md
@.planning/phases/131-occurrence-normalization/131-PATTERNS.md
@.planning/phases/131-occurrence-normalization/131-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite build-geojson.test.ts for the 7-field geo_blob layout and { geojson }-only return</name>
  <files>src/tests/build-geojson.test.ts</files>
  <read_first>
    - src/tests/build-geojson.test.ts (the file being rewritten — current `toRow()` helper, `RowOverride` interface, all `summary`/`taxaOptions` assertions)
    - src/features.ts (current `_buildGeoJSONFromRaw` decode + return shape — the API the test targets)
    - .planning/phases/131-occurrence-normalization/131-PATTERNS.md §9 (build-geojson.test.ts full rewrite guide: new 7-field `RowOverride`, new `toRow`, tests to keep/delete/add)
    - .planning/phases/131-occurrence-normalization/131-RESEARCH.md (D-04 layout table; Pitfall 1 — positional index mismatch)
  </read_first>
  <action>
    Rewrite the fixture helpers and assertions to the new 7-field positional layout
    `[lat, lon, ecdysis_id, observation_id, specimen_observation_id, year, source]` (D-04).
    The `RowOverride` interface drops `scientificName`, `genus`, `family`; `toRow` returns a
    7-element array with `source` at index 6 (not index 9). Keep the behavioral tests:
    occId formation (`ecdysis:<id>` / `inat:<id>` / `inat_obs:<id>` precedence), lat/lon
    null-skip, and `recencyTier` (`thisYear`/`lastYear`/`earlier` driven by `year` at index 5).
    Delete every assertion referencing `result.summary`, `result.taxaOptions`, `speciesCount`,
    `genusCount`, `familyCount` — `_buildGeoJSONFromRaw` will return `{ geojson }` only.
    Add a positive assertion that the return value has property `geojson` and does NOT have
    `summary` or `taxaOptions`. Add at least one test that pins `source` decoding at index 6
    (a row with a known `source` string must produce that source on the feature) to lock the
    positional layout against silent drift (Pitfall 1). Do NOT change source files in this plan.
  </action>
  <verify>
    <automated>npx vitest run src/tests/build-geojson.test.ts 2>&1 | tail -20 — expect FAIL (RED) against current 10-field source</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "scientificName\|taxaOptions\|speciesCount\|genusCount\|familyCount" src/tests/build-geojson.test.ts` returns 0
    - `grep -q "row\[6\]\|index 6\|source.*6\|6.*source" src/tests/build-geojson.test.ts` — the file references the source-at-index-6 expectation (via fixture/assertion)
    - The file contains an assertion that the result has `geojson` and lacks `summary`/`taxaOptions` (e.g. `not.toHaveProperty('summary')`)
    - `npx vitest run src/tests/build-geojson.test.ts` exits non-zero (RED) — the new test fails against the still-old `features.ts`
  </acceptance_criteria>
  <done>build-geojson.test.ts targets the 7-field layout + { geojson } return and is RED against current source.</done>
</task>

<task type="auto">
  <name>Task 2: Extend filter.test.ts for the LEFT JOIN + display_name + slimmer OCCURRENCE_COLUMNS</name>
  <files>src/tests/filter.test.ts</files>
  <read_first>
    - src/tests/filter.test.ts (current OCCURRENCE_COLUMNS assertion ~L256; queryTablePage describe block ~L317-398; SQL-contains assertion idiom ~L318-351; mock result row ~L449)
    - src/filter.ts (current OCCURRENCE_COLUMNS, queryTablePage SELECT, the sqlite3.exec callback pattern)
    - .planning/phases/131-occurrence-normalization/131-PATTERNS.md §10 (filter.test.ts partial-update guide + the new SQL-contains test to add) and §"Shared Patterns / sqlite3.exec callback"
    - .planning/phases/131-occurrence-normalization/131-RESEARCH.md (Pitfall 4 — `o.` prefix needed because taxa has its own taxon_id)
  </read_first>
  <action>
    Update the `OCCURRENCE_COLUMNS` assertion so it asserts the constant does NOT contain
    `'scientificName'`, `'genus'`, `'family'`, or `'specimen_inat_taxon_name'` (invert the
    current `toContain('scientificName')` at ~L256). Add a new test in the `queryTablePage`
    describe block (copy the existing SQL-contains assertion idiom) asserting the data-query
    SQL contains `LEFT JOIN taxa` and `display_name` (alias `t.name AS display_name`). Remove
    the `scientificName: 'Bombus'` field from the mock result row (~L449). Do NOT add a JOIN
    assertion that depends on `queryListPage`/`queryAllFiltered` internals beyond what the
    existing describe blocks already exercise — keep the new assertions scoped to queryTablePage
    plus the OCCURRENCE_COLUMNS shape. Do NOT change source files in this plan.
  </action>
  <verify>
    <automated>npx vitest run src/tests/filter.test.ts 2>&1 | tail -20 — expect FAIL (RED) against current un-migrated filter.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "LEFT JOIN taxa" src/tests/filter.test.ts` succeeds
    - `grep -q "display_name" src/tests/filter.test.ts` succeeds
    - The OCCURRENCE_COLUMNS assertion asserts ABSENCE of the 4 dropped columns (e.g. `not.toContain('scientificName')`), not presence
    - `npx vitest run src/tests/filter.test.ts` exits non-zero (RED) against current source
  </acceptance_criteria>
  <done>filter.test.ts pins the JOIN + display_name + slimmer OCCURRENCE_COLUMNS and is RED.</done>
</task>

<task type="auto">
  <name>Task 3: Update bee-table.test.ts fixtures for display_name and slimmed DataSummary</name>
  <files>src/tests/bee-table.test.ts</files>
  <read_first>
    - src/tests/bee-table.test.ts (mock OCCURRENCE_COLUMNS ~L12; DataSummary mock in summary prop ~L20-21; row fixtures ~L150-163 and ~L220-233 using `scientificName: 'Bombus vosnesenskii'`)
    - src/bee-table.ts (Species column ColumnDef at L43; render loop at L382 — how dataField is consumed)
    - .planning/phases/131-occurrence-normalization/131-PATTERNS.md §11 (bee-table.test.ts fixture-update guide) and §3 (Species column migration)
    - .planning/phases/131-occurrence-normalization/131-RESEARCH.md (Pitfall 3 — DataSummary mock breakage)
  </read_first>
  <action>
    In the mock `OCCURRENCE_COLUMNS` array (~L12) remove `'scientificName'`, `'genus'`,
    `'family'`, `'specimen_inat_taxon_name'` and add `'display_name'`. In every `DataSummary`
    mock object (~L20-21) remove `speciesCount`, `genusCount`, `familyCount` (keep
    `totalSpecimens`, `earliestYear`, `latestYear`). In the row fixtures (~L151 TABLE-07,
    ~L221 TABLE-09 baseRow) change `scientificName: 'Bombus vosnesenskii'` to
    `display_name: 'Bombus vosnesenskii'` and remove any `genus`/`family` fixture fields. The
    Species-column test must now assert the cell renders from `display_name`. Do NOT change
    source files in this plan.
  </action>
  <verify>
    <automated>npx vitest run src/tests/bee-table.test.ts 2>&1 | tail -20 — expect FAIL (RED) against current bee-table.ts/filter.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "scientificName\|speciesCount\|genusCount\|familyCount" src/tests/bee-table.test.ts` returns 0
    - `grep -q "display_name" src/tests/bee-table.test.ts` succeeds
    - `npx vitest run src/tests/bee-table.test.ts` exits non-zero (RED) against current source
  </acceptance_criteria>
  <done>bee-table.test.ts fixtures use display_name + slimmed DataSummary and are RED.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | This plan edits test files only — no runtime input crosses any boundary |

## STRIDE Threat Register

Security enforcement is enabled (ASVS L1). Per RESEARCH.md Security Domain, this phase
introduces no new authentication, sessions, or user-controlled inputs. This plan in
particular touches only Vitest test files.

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-131-01 | Tampering | test files only | accept | No runtime surface; no user-controlled input; tests assert behavior only |
| T-131-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this phase (RESEARCH.md: "No new packages") — nothing to audit |
</threat_model>

<verification>
- `npx vitest run src/tests/build-geojson.test.ts src/tests/filter.test.ts src/tests/bee-table.test.ts` is RED (expected — implementation lands in Plans 02/03).
- The three files contain no remaining references to the 4 dropped column names except in
  inverted/absence assertions.
</verification>

<success_criteria>
All three test files encode the target Phase 131 behavior and FAIL against current source
(true RED). No source files modified. Plans 02 and 03 will drive them GREEN.
</success_criteria>

<output>
Create `.planning/phases/131-occurrence-normalization/131-01-SUMMARY.md` when done.
</output>
