---
phase: 130-map-filter-cutover
plan: 03
type: execute
wave: 3
depends_on: ["130-02"]
files_modified:
  - src/bee-occurrence-detail.ts
  - src/bee-pane.ts
  - src/bee-atlas.ts
  - src/tests/bee-sidebar.test.ts
autonomous: true
requirements: [MFILT-03]
must_haves:
  truths:
    - "The occurrence detail card resolves the displayed taxon name from the taxon cache by row.taxon_id, not from the still-present scientificName string column (D-07)"
    - "When row.taxon_id IS NULL (≈21k unidentified Ecdysis specimens), the card shows 'No determination' — never blank or undefined"
    - "The taxon cache is threaded as a property from bee-atlas → bee-pane → bee-occurrence-detail (presenter invariant; no module-level shared mutable state)"
  artifacts:
    - path: "src/bee-occurrence-detail.ts"
      provides: "taxonCache prop + taxon_id name resolution with No-determination fallback"
      contains: "taxonCache"
    - path: "src/tests/bee-sidebar.test.ts"
      provides: "render tests for cache-resolved name and null-taxon_id fallback"
      contains: "No determination"
  key_links:
    - from: "src/bee-atlas.ts _taxonCache"
      to: "bee-occurrence-detail.taxonCache"
      via: "bee-pane prop threading"
      pattern: "\\.taxonCache="
    - from: "src/bee-occurrence-detail.ts"
      to: "row.taxon_id"
      via: "taxonCache.get lookup"
      pattern: "taxonCache.*get"
---

<objective>
Switch the occurrence detail card to resolve taxon names from the taxon cache by
`row.taxon_id` (D-07), threading `bee-atlas._taxonCache` down through `bee-pane` to
`bee-occurrence-detail` as a property (presenter architecture invariant). The
`scientificName` string column still exists this phase but is treated as already-gone
to de-risk Phase 131. `taxon_id IS NULL` keeps the existing "No determination"
treatment — never blank/undefined.

Purpose: Close the detail-card half of MFILT-03 (criterion #3: hierarchy-cache name
resolution in detail cards).
Output: detail card renders cache-resolved names; render tests green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/130-map-filter-cutover/130-CONTEXT.md
@.planning/phases/130-map-filter-cutover/130-RESEARCH.md
@.planning/phases/130-map-filter-cutover/130-PATTERNS.md
@.planning/phases/130-map-filter-cutover/130-VALIDATION.md
@.planning/phases/130-map-filter-cutover/130-02-SUMMARY.md

<interfaces>
<!-- From Plans 01-02 (already shipped): -->
<!--   OccurrenceRow now has taxon_id: number | null (OCCURRENCE_COLUMNS includes 'taxon_id', -->
<!--     so every occurrence SELECT returns it). -->
<!--   bee-atlas._taxonCache: Map<number, { rank: string; name: string; lineagePath: string | null }> -->
<!--     is built lazily post-tablesReady. -->

<!-- Prop-threading chain (direct inspection): -->
<!--   bee-atlas.ts renders <bee-pane .taxaOptions=${this._taxaOptions} ...> (L179-216) -->
<!--   bee-pane.ts has @property occurrences/listRows (L47/L65) and renders -->
<!--     <bee-occurrence-detail .occurrences=${this.listRows}> (L1181) -->
<!--   bee-occurrence-detail.ts @property occurrences L47; name render at L188 -->
<!--     uses row.scientificName ? row.scientificName : <span class="no-determination">No determination</span> -->
<!--   (note L253-254 also renders row.scientificName as an <em> elsewhere — switch to the same cache lookup) -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Thread taxonCache prop bee-atlas → bee-pane → bee-occurrence-detail; resolve names by taxon_id with No-determination fallback</name>
  <files>src/bee-occurrence-detail.ts, src/bee-pane.ts, src/bee-atlas.ts, src/tests/bee-sidebar.test.ts</files>
  <read_first>
    - src/bee-occurrence-detail.ts (@property occurrences L47, name render L188, secondary name render L253-254/L267, no-determination style L82)
    - src/bee-pane.ts (@property block L53-76, bee-occurrence-detail render L1181)
    - src/bee-atlas.ts (_taxonCache field from Plan 02, bee-pane render L179-216)
    - src/tests/bee-sidebar.test.ts (existing render-test harness + fixtures; provisional/sample-only render tests for the mount pattern)
    - .planning/phases/130-map-filter-cutover/130-RESEARCH.md §"D-07 Detail Card Name Resolution"
    - .planning/phases/130-map-filter-cutover/130-PATTERNS.md §"src/bee-occurrence-detail.ts" Patterns 1-3
    - ./CLAUDE.md (architecture invariant: pure presenters receive state as properties; no module-level shared mutable state)
  </read_first>
  <behavior>
    - Test: mounting bee-occurrence-detail with an occurrence whose `taxon_id = 52775` and a `taxonCache` containing `52775 → { name: 'Bombus fervidus', rank: 'species' }` renders `Bombus fervidus` (from the cache, not from row.scientificName).
    - Test: mounting with an occurrence whose `taxon_id` is null renders the `No determination` span and never renders an empty string or `undefined`.
    - Test: mounting with a `taxon_id` that is absent from the cache (cache miss) falls back to `No determination` (no blank/undefined).
  </behavior>
  <action>
    In bee-occurrence-detail.ts: add `@property({ attribute: false }) taxonCache: Map<number, { rank: string; name: string }> | null = null;` (model: the existing `occurrences` @property at L47). Replace the name resolution at L188 (and the secondary `row.scientificName` renders at L253-254 and the alt-text at L267 where it names the taxon) with a cache lookup: compute `const info = row.taxon_id != null ? this.taxonCache?.get(row.taxon_id) : null; const displayName = info?.name ?? null;` and render `displayName` where present, falling back to the existing `<span class="no-determination">No determination</span>` when `displayName` is null. Do not read `row.scientificName` for the determination label any more (treat as already-gone, D-07). Keep the iNat alt-text reasonable — use `displayName ?? 'bee'`.

    In bee-pane.ts: add `@property({ attribute: false }) taxonCache: Map<number, { rank: string; name: string }> | null = null;` (alongside the existing props L53-76) and pass it down where bee-occurrence-detail is rendered (L1181): `<bee-occurrence-detail .occurrences=${this.listRows} .taxonCache=${this.taxonCache}>`.

    In bee-atlas.ts: pass `_taxonCache` into the bee-pane render block (L179-216): add `.taxonCache=${this._taxonCache}`. `_taxonCache` is a plain field (built in Plan 02) — to make the detail card re-render when the cache finishes loading, either promote the assignment to trigger an update (assign a new Map reference and call `this.requestUpdate()` after the cache build) or expose a `@state` mirror; planner-executor's discretion, but the card MUST update once the cache is populated.

    Add the three `<behavior>` render tests to src/tests/bee-sidebar.test.ts following the existing mount-and-assert pattern used by the provisional/sample-only tests.
  </action>
  <verify>
    <automated>npx tsc --noEmit && npm test -- --run && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `bee-occurrence-detail.ts` declares a `taxonCache` `@property` and resolves the determination label via `this.taxonCache?.get(row.taxon_id)` — `grep -n "taxonCache" src/bee-occurrence-detail.ts` shows both the property and the lookup.
    - The determination label no longer reads `row.scientificName` for the primary name (`grep -n "row.scientificName" src/bee-occurrence-detail.ts` shows it is gone from the determination render path; any remaining use is non-determination, e.g. unrelated).
    - `bee-pane.ts` declares a `taxonCache` `@property` and forwards `.taxonCache=` to `bee-occurrence-detail`.
    - `bee-atlas.ts` passes `.taxonCache=${this._taxonCache}` to `bee-pane`.
    - Render test: `taxon_id`-present occurrence shows the cache name; `taxon_id`-null and cache-miss occurrences both show `No determination` and never blank/undefined.
    - `npx tsc --noEmit` exits 0; `npm test -- --run` exits 0; `npm run build` exits 0.
  </acceptance_criteria>
  <done>Detail card resolves names from the taxon cache by taxon_id with a No-determination fallback; cache threads through the presenter chain; render tests + build green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| taxonCache → DOM | Cache-resolved taxon name rendered into the detail card |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-130-DC | Information disclosure / XSS | detail card name render | accept | Names come from the bundled read-only `taxa` table via the in-memory cache (trusted reference data), rendered through Lit's `html` template auto-escaping. No user-supplied string enters the determination label. `row.taxon_id` is an integer used only as a Map key. |
| T-130-NULL | Reliability | null/missing taxon_id | mitigate | Explicit `taxon_id != null` guard plus `info?.name ?? null` cache-miss fallback render "No determination"; no path renders blank/undefined (asserted by render tests). |
</threat_model>

<verification>
- `npx tsc --noEmit` clean; `npm test -- --run` green; `npm run build` green.
- Render tests assert cache-resolved name, null-taxon_id fallback, and cache-miss fallback.
</verification>

<success_criteria>
- Detail card resolves taxon names from the cache by taxon_id (MFILT-03 / D-07).
- taxon_id IS NULL → "No determination", never blank/undefined.
- Cache threads through the presenter chain per the architecture invariant.
</success_criteria>

<output>
Create `.planning/phases/130-map-filter-cutover/130-03-SUMMARY.md` when done.
</output>
