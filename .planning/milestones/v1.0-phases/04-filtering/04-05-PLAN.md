---
phase: 04-filtering
plan: 05
type: execute
wave: 2
depends_on:
  - 04-04
files_modified: []
autonomous: false
requirements:
  - FILTER-01
  - FILTER-02
gap_closure: true

must_haves:
  truths:
    - "Year inputs clearly indicate the user should enter a year (placeholder text visible)"
    - "Clicking a taxon in the datalist dropdown immediately applies the filter — map clusters update"
    - "An X/clear button on the taxon input allows clearing the taxon independently of other filters"
    - "Clicking a cluster shows the specimen listing with no back button; a 'Clear selection' button near filter controls dismisses the listing"
    - "All existing filter behaviors (ghosting, year range, month checkboxes, AND logic) continue working correctly"
  artifacts:
    - path: "frontend/src/bee-sidebar.ts"
      provides: "All 4 gap fixes from plan 04-04"
      contains: "From year"
  key_links:
    - from: "bee-sidebar taxon input"
      to: "filter-changed event"
      via: "datalist dropdown selection resolved in _onTaxonInput via exact label match"
      pattern: "taxaOptions.find"
---

<objective>
Human re-verification of Phase 4 filtering after gap-closure fixes from plan 04-04.

Purpose: Confirm the 4 issues found in the original verification (plan 04-03) are now resolved, and that existing filter behaviors remain intact.

Output: User approval marking Phase 4 complete, or new issues filed for further gap closure.
</objective>

<execution_context>
@/Users/rainhead/.claude/get-shit-done/workflows/execute-plan.md
@/Users/rainhead/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-filtering/04-CONTEXT.md
@.planning/phases/04-filtering/04-04-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Start dev server for re-verification</name>
  <files></files>
  <action>
Start the frontend dev server:

```bash
cd /Users/rainhead/dev/beeatlas && npm run dev --workspace=frontend
```

Leave the server running. Report the local URL (typically http://localhost:5173).
  </action>
  <verify>Dev server starts without error and prints a local URL.</verify>
  <done>Dev server is running and URL is reported to the user.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human re-verify all 4 fixed issues and confirm existing behaviors</name>
  <files></files>
  <action>
Present verification instructions to the user. All automated work is complete (plan 04-04 executed). No automated action needed — this task exists to gate the phase on human approval.
  </action>
  <verify>User types "approved" in response to the verification prompts.</verify>
  <done>User has confirmed all 4 fixes work correctly and existing behaviors are unbroken.</done>
  <what-built>
Gap-closure fixes to `bee-sidebar.ts`:
1. Year inputs now show "From year" / "To year" placeholder text
2. Selecting from the datalist dropdown now applies the taxon filter immediately
3. An X/clear button appears on the taxon input when a taxon is active
4. Cluster selection shows specimens inline with a "Clear selection" button (no back button)
  </what-built>
  <how-to-verify>
Open http://localhost:5173 in a browser. Verify each of the 4 fixed issues, then confirm existing behaviors still work.

**Issue 1: Year placeholder text**
- Look at the year range inputs in the filter controls
- Expected: inputs display "From year" and "To year" as placeholder text (not just "From" / "To")

**Issue 2: Taxon datalist dropdown selection**
- Click the taxon text input
- Type "Ap" — the datalist dropdown should show options like "Apidae (family)", "Apis mellifera", etc.
- Click/select "Apidae (family)" from the dropdown using the mouse (do NOT press Enter/Tab — use the native dropdown)
- Expected: the map immediately dims non-Apidae clusters; summary stats switch to "X of Y" format
- Previously this bug meant the filter did NOT apply when selecting from the dropdown

**Issue 3: Taxon clear button**
- With a taxon filter active (e.g., "Apidae (family)" selected), look at the taxon input
- Expected: an X or ✕ button appears to the right of the taxon input
- Click the X button
- Expected: taxon filter clears (all clusters return to full opacity); year and month filters are NOT affected

**Issue 4: Cluster selection paradigm**
- Click any full-opacity specimen cluster on the map
- Expected: the specimen listing appears in the sidebar below the filter controls — NO back button visible
- Expected: a "Clear selection" button appears near the filter controls (just before or after "Clear filters")
- Click "Clear selection"
- Expected: the specimen listing disappears; filter controls remain active

**Regression check: existing behaviors**
- Taxon filter (typing + keyboard Enter): still works
- Year range filter: still works
- Month checkboxes: still works
- Clear filters button: still clears all filters
- Ghosted cluster click: still produces no sidebar change
- AND combination of filters: still works
  </how-to-verify>
  <resume-signal>Type "approved" if all 4 fixes work and existing behaviors are intact, or describe any remaining issues.</resume-signal>
</task>

</tasks>

<verification>
All 4 gaps from 04-03-SUMMARY.md are verified fixed:
- [ ] Year inputs show "From year" / "To year" placeholder text
- [ ] Datalist dropdown selection (mouse click) applies the taxon filter
- [ ] X/clear button on taxon input clears taxon independently
- [ ] Specimen listing has no back button; "Clear selection" button dismisses listing
- [ ] All original filter behaviors remain intact (regression check)
</verification>

<success_criteria>
User types "approved" after verifying all 4 fixes and confirming no regressions. Phase 4 filtering is complete.
</success_criteria>

<output>
After completion, create `.planning/phases/04-filtering/04-05-SUMMARY.md`
</output>
