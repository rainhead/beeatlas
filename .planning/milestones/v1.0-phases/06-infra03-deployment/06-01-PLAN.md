---
phase: 06-infra03-deployment
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/02-infrastructure/02-02-SUMMARY.md
autonomous: false
requirements: [INFRA-03]

must_haves:
  truths:
    - "cdk deploy --all completes without error; CloudFront URL is accessible"
    - "Three GitHub Actions Variables (AWS_DEPLOYER_ROLE_ARN, S3_BUCKET_NAME, CF_DISTRIBUTION_ID) are set under the Variables tab (not Secrets tab)"
    - "Push to main triggers the GitHub Actions workflow; build and deploy jobs both succeed"
    - "Visiting the CloudFront *.cloudfront.net URL serves the bee atlas frontend (HTTP 200)"
    - "02-02-SUMMARY.md documents Variables (not Secrets) and correct bootstrap commands with --qualifier beeatlas for both regions"
  artifacts:
    - path: ".planning/phases/02-infrastructure/02-02-SUMMARY.md"
      provides: "Corrected setup documentation"
      contains: "Variables tab"
  key_links:
    - from: ".github/workflows/deploy.yml"
      to: "vars.AWS_DEPLOYER_ROLE_ARN"
      via: "GitHub Actions Variables context"
      pattern: "vars\\.AWS_DEPLOYER_ROLE_ARN"
    - from: "infra/lib/beeatlas-stack.ts"
      to: "AWS CloudFormation"
      via: "cdk deploy --all"
      pattern: "cdk deploy"
---

<objective>
Deploy the CDK infrastructure and verify the live site. The CDK code (S3, CloudFront OAC, OIDC provider, deployer role) and GitHub Actions workflow were fully written in Phase 2 but never executed. This plan closes INFRA-03 by running the actual deployment: CDK bootstrap for both regions, deploy, set GitHub Actions Variables (not Secrets), push to main, and verify the CloudFront URL serves the site.

Purpose: INFRA-03 is the last infrastructure gap standing between a locally-correct app and a publicly accessible, auto-deploying production site.
Output: Live CloudFront URL serving the bee atlas frontend; GitHub Actions deploy workflow verified passing on push to main.
</objective>

<execution_context>
@/Users/rainhead/.claude/get-shit-done/workflows/execute-plan.md
@/Users/rainhead/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-infra03-deployment/06-RESEARCH.md
@.planning/phases/02-infrastructure/02-02-SUMMARY.md
@infra/bin/infra.ts
@infra/cdk.json
@infra/lib/beeatlas-stack.ts
@infra/lib/global-stack.ts
@.github/workflows/deploy.yml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Correct 02-02-SUMMARY.md documentation errors</name>
  <files>.planning/phases/02-infrastructure/02-02-SUMMARY.md</files>
  <action>
The SUMMARY contains two documentation errors identified in the research; fix both:

**Error 1 — "Secrets" should be "Variables":**
In the "User Setup Required" section, Step 3 says "Set GitHub repository secrets" with URL https://github.com/rainhead/beeatlas/settings/secrets/actions. The workflow uses `vars.*` (GitHub Actions Variables), not `secrets.*`. Fix the heading, URL, and description:
- Change heading to: "### Step 3 — Set GitHub repository variables"
- Change URL to: https://github.com/rainhead/beeatlas/settings/variables/actions
- Change body to: "Set these three **variables** (not secrets) at: Settings → Secrets and variables → Actions → **Variables** tab → New repository variable"

**Error 2 — Bootstrap commands missing --qualifier and second region:**
Step 1 shows only `npx cdk bootstrap aws://YOUR_ACCOUNT_ID/us-east-1` with no `--qualifier beeatlas` flag. The `cdk.json` sets `bootstrapQualifier: "beeatlas"`, so bootstrap without `--qualifier beeatlas` will create wrong-named resources and `cdk deploy` will fail. Fix Step 1 to show bootstrap for BOTH regions with the custom qualifier:

```bash
cd infra
npx cdk bootstrap aws://YOUR_ACCOUNT_ID/us-east-1 --qualifier beeatlas --profile your-aws-profile
npx cdk bootstrap aws://YOUR_ACCOUNT_ID/us-west-2 --qualifier beeatlas --profile your-aws-profile
```

Also update Step 2 to show `npx cdk deploy --all` (not bare `npx cdk deploy`), since there are two stacks (GlobalStack + BeeAtlasStack).

Do NOT change any other content — only the three corrections above.
  </action>
  <verify>
Read the updated file and confirm:
1. Step 3 heading says "variables" not "secrets"
2. Step 3 URL contains "variables" not "secrets"
3. Step 1 bootstrap commands include `--qualifier beeatlas` for both us-east-1 and us-west-2
4. Step 2 deploy command is `npx cdk deploy --all`
  </verify>
  <done>02-02-SUMMARY.md accurately documents Variables (not Secrets) and both-region bootstrap with --qualifier beeatlas</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: CDK bootstrap and deploy (requires AWS credentials)</name>
  <files>infra/lib/beeatlas-stack.ts</files>
  <action>
Human executes CDK bootstrap for both regions with custom qualifier, then deploys all stacks. If the OIDC provider already exists in the account, apply the fromOpenIdConnectProviderArn fallback already documented as a comment in beeatlas-stack.ts lines 98-100. No code changes needed unless the OIDC provider fallback is required.
  </action>
  <what-built>Task 1 corrected the documentation. All CDK code is already written and synth output exists in infra/cdk.out/. No code changes needed.</what-built>
  <how-to-verify>
Run these steps from your terminal (requires AWS credentials with AdministratorAccess or CDK bootstrap permissions):

**Step 1 — Get your AWS account ID:**
```bash
aws sts get-caller-identity --query Account --output text --profile your-aws-profile
```

**Step 2 — Bootstrap both regions with custom qualifier:**
```bash
cd /Users/rainhead/dev/beeatlas/infra
npx cdk bootstrap aws://YOUR_ACCOUNT_ID/us-east-1 --qualifier beeatlas --profile your-aws-profile
npx cdk bootstrap aws://YOUR_ACCOUNT_ID/us-west-2 --qualifier beeatlas --profile your-aws-profile
```
Expected: Both commands print "Environment aws://ACCOUNT/REGION bootstrapped" (or similar).

**Step 3 — Deploy all stacks:**
```bash
cd /Users/rainhead/dev/beeatlas/infra
npx cdk deploy --all --profile your-aws-profile
```

Expected output ends with:
```
Outputs:
GlobalStack.NetNameServers = ns-XXXX.awsdns-XX.org, ...
GlobalStack.ComNameServers = ns-XXXX.awsdns-XX.net, ...
BeeAtlasStack.BucketName = beeatlasstack-sitebucket-XXXX
BeeAtlasStack.DistributionId = EXXXXXXXXXXXX
BeeAtlasStack.DistributionDomain = dXXXXXXXXXXXXX.cloudfront.net
BeeAtlasStack.DeployerRoleArn = arn:aws:iam::ACCOUNT:role/beeatlas-github-deployer
```

**Known failure modes and fixes:**

- If deploy fails with `GitHubOidcProvider already exists in this account`: Edit `infra/lib/beeatlas-stack.ts` lines 98-102. Replace the `new iam.OpenIdConnectProvider(...)` block with the commented-out `fromOpenIdConnectProviderArn` alternative (already in the file as a comment at line 98-100). Re-run `npx cdk deploy --all`.

- If deploy hangs for >5 min on `AWS::CertificateManager::Certificate` (ACM DNS validation): This means the domain registrar has not yet been updated to use Route 53 nameservers. The Phase 6 success criteria only require the CloudFront `*.cloudfront.net` URL to work — the custom domain (beeatlas.net) can come later. You can press Ctrl+C to abort the deploy and re-run; the S3 bucket, CloudFront distribution, OIDC provider, and deployer role are created before the certificate. Note the outputs from what deployed, then continue to Step 4 using the CloudFront URL.

**Step 4 — Record outputs for next task:**
Write down (or copy to clipboard) the four values from the deploy output:
- `BeeAtlasStack.DeployerRoleArn`
- `BeeAtlasStack.BucketName`
- `BeeAtlasStack.DistributionId`
- `BeeAtlasStack.DistributionDomain`
  </how-to-verify>
  <verify>CDK outputs printed to terminal: BeeAtlasStack.BucketName, BeeAtlasStack.DistributionId, BeeAtlasStack.DistributionDomain, BeeAtlasStack.DeployerRoleArn all present</verify>
  <done>Both CDK stacks deployed; four output values recorded for next task</done>
  <resume-signal>Paste the four CDK output values (BucketName, DistributionId, DistributionDomain, DeployerRoleArn), or type "done" with a note about any failures handled.</resume-signal>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 3: Set GitHub Variables, push to main, verify live site</name>
  <files>.github/workflows/deploy.yml</files>
  <action>
Human sets three GitHub Actions Variables (not Secrets) using CDK output values from Task 2, pushes to main to trigger the workflow, and verifies both the deploy job and the live CloudFront URL. No file changes needed unless the workflow needs debugging.
  </action>
  <what-built>CDK stack is deployed. Now wire GitHub Actions to the deployed resources and verify the full deploy pipeline.</what-built>
  <how-to-verify>
**Step 1 — Set three GitHub Actions Variables (NOT Secrets):**

Go to: https://github.com/rainhead/beeatlas/settings/variables/actions

Click "New repository variable" three times:
- Name: `AWS_DEPLOYER_ROLE_ARN` / Value: `BeeAtlasStack.DeployerRoleArn` from previous task
- Name: `S3_BUCKET_NAME` / Value: `BeeAtlasStack.BucketName` from previous task
- Name: `CF_DISTRIBUTION_ID` / Value: `BeeAtlasStack.DistributionId` from previous task

**CRITICAL:** Use the **Variables** tab (not the Secrets tab). The workflow uses `vars.*` context which reads Variables, not Secrets. If you accidentally set Secrets, the `vars.*` references will expand to empty string and the deploy job will fail with no clear error.

**Step 2 — Push to main to trigger the workflow:**
```bash
cd /Users/rainhead/dev/beeatlas
git commit --allow-empty -m "ci: trigger deploy verification" && git push origin main
```
(If you have uncommitted changes, commit those instead.)

**Step 3 — Monitor GitHub Actions:**
Visit: https://github.com/rainhead/beeatlas/actions

Watch the "Build and Deploy" workflow run triggered by your push. Expected:
- `build` job: passes (downloads ecdysis.org data, builds frontend with Vite)
- `deploy` job: passes (configures AWS via OIDC, syncs to S3, invalidates CloudFront)

If `build` job fails with a network error fetching ecdysis.org: Re-run the job from the Actions UI (the data is an external dependency). This is known tech debt.

If `deploy` job fails with `role-to-assume` getting empty string: The Variables were set as Secrets by mistake. Go back to settings, delete the secrets, and set them as Variables at https://github.com/rainhead/beeatlas/settings/variables/actions.

**Step 4 — Verify live site:**
Visit: `https://DISTRIBUTION_DOMAIN.cloudfront.net` (use the `DistributionDomain` value from the CDK deploy output).

Expected: Bee atlas frontend loads — map of Washington State with specimen clusters visible.

**Step 5 — Note nameservers (for future custom domain setup):**
If GlobalStack deployed successfully, the outputs include `GlobalStack.NetNameServers` and `GlobalStack.ComNameServers`. Note these for future registrar configuration (out of scope for Phase 6, but good to have).
  </how-to-verify>
  <verify>GitHub Actions "Build and Deploy" workflow shows green on both build and deploy jobs at https://github.com/rainhead/beeatlas/actions; visiting CloudFront DistributionDomain URL returns HTTP 200 with the bee atlas map</verify>
  <done>INFRA-03 satisfied: auto-deploy on push to main works; CloudFront URL serves live site</done>
  <resume-signal>Type "verified" if the site loads at the CloudFront URL and the GitHub Actions deploy job passed. Or describe any failures with the error message from the Actions log.</resume-signal>
</task>

</tasks>

<verification>
Phase 6 complete when ALL of the following are true:

1. `02-02-SUMMARY.md` documents Variables (not Secrets) and `--qualifier beeatlas` bootstrap for both regions
2. CDK outputs exist: `BeeAtlasStack.BucketName`, `BeeAtlasStack.DistributionId`, `BeeAtlasStack.DistributionDomain`, `BeeAtlasStack.DeployerRoleArn`
3. Three GitHub Actions Variables set (AWS_DEPLOYER_ROLE_ARN, S3_BUCKET_NAME, CF_DISTRIBUTION_ID) in the Variables tab
4. GitHub Actions "Build and Deploy" workflow passed on push to main (both build and deploy jobs green)
5. CloudFront `*.cloudfront.net` URL serves the bee atlas frontend (HTTP 200, map visible)

INFRA-03 is satisfied when conditions 3-5 are all true.
</verification>

<success_criteria>
- 02-02-SUMMARY.md corrected: "Variables" not "Secrets", both-region bootstrap with `--qualifier beeatlas`, `cdk deploy --all`
- CDK stack deployed: S3 bucket, CloudFront distribution, OIDC provider, deployer role all exist in AWS
- GitHub Actions Variables set (3 of them) in Variables tab at github.com/rainhead/beeatlas/settings/variables/actions
- GitHub Actions deploy job completed successfully on push to main (not just build job)
- Visiting CloudFront DistributionDomain URL serves the bee atlas map (site is live)
</success_criteria>

<output>
After completion, create `.planning/phases/06-infra03-deployment/06-01-SUMMARY.md` with:
- CDK outputs (BucketName, DistributionId, DistributionDomain, DeployerRoleArn — actual values)
- GitHub Actions Variables confirmed set
- CloudFront URL where site is live
- Any OIDC fallback applied (fromOpenIdConnectProviderArn) — yes/no
- ACM cert status (validated or still pending DNS propagation)
- Notes on nameservers (for future custom domain work)
</output>
