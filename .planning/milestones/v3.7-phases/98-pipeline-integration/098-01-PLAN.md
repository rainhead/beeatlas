---
phase: 98-pipeline-integration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - data/places_load.py
  - data/run.py
  - data/dbt/models/sources.yml
  - data/dbt/models/marts/occurrences.sql
  - data/dbt/models/marts/schema.yml
  - data/tests/conftest.py
  - data/tests/test_places_load.py
autonomous: true
requirements:
  - PPIPE-01
  - PPIPE-02
  - PPIPE-03

must_haves:
  truths:
    - "Running `cd data && uv run python run.py` calls places-load between places-validation and dbt-build"
    - "places-load creates geographies.places DuckDB table from content/places.toml with one row per [[places]] entry"
    - "geographies.places.geom column is a GEOMETRY usable by ST_Within (round-trip via ST_GeomFromText)"
    - "dbt build exits 0 with the new 31-column contract on marts/occurrences"
    - "occurrences.parquet contains a place_slug VARCHAR column"
    - "Occurrence points inside a known place polygon carry that place's slug; points outside all polygons carry NULL (no fallback)"
    - "pytest passes for places_load table creation and ST_Within usability"
  artifacts:
    - path: "data/places_load.py"
      provides: "load_places() main + load_places_step() zero-arg wrapper that loads content/places.toml into geographies.places"
      exports: ["load_places", "load_places_step"]
    - path: "data/run.py"
      provides: "places-load step wired between places-validation and dbt-build"
      contains: "(\"places-load\", load_places_step)"
    - path: "data/dbt/models/sources.yml"
      provides: "places table declared as part of the geographies source so {{ source('geographies', 'places') }} resolves"
      contains: "- name: places"
    - path: "data/dbt/models/marts/occurrences.sql"
      provides: "place_slug column added via with_place + place_dedup CTEs and a LEFT JOIN; no fallback"
      contains: "place_slug"
    - path: "data/dbt/models/marts/schema.yml"
      provides: "31st column entry place_slug VARCHAR appended to the occurrences model column list"
      contains: "place_slug"
    - path: "data/tests/conftest.py"
      provides: "fixture_db extended with geographies.places table + seed polygon covering the canonical test specimen coordinate (lat=47.608, lon=-120.912)"
      contains: "geographies.places"
    - path: "data/tests/test_places_load.py"
      provides: "pytest unit tests for places_load (table creation + ST_Within usability)"
      exports: ["test_load_creates_table", "test_places_geometry_usable"]
  key_links:
    - from: "data/run.py"
      to: "data/places_load.py"
      via: "from places_load import load_places_step"
      pattern: "places_load"
    - from: "data/dbt/models/marts/occurrences.sql"
      to: "geographies.places (DuckDB table)"
      via: "{{ source('geographies', 'places') }}"
      pattern: "source\\('geographies', 'places'\\)"
    - from: "data/dbt/models/marts/schema.yml"
      to: "data/dbt/models/marts/occurrences.sql"
      via: "dbt contract enforcement (column count + types)"
      pattern: "place_slug"
---

<objective>
Land the spatial substrate for v3.7 Places: load content/places.toml into a `geographies.places`
DuckDB table before dbt runs, then teach the `marts/occurrences` model to attach a `place_slug`
column to every occurrence via `ST_Within` LEFT JOIN against that table. Atomically extend the dbt
contract from 30 to 31 columns. Scaffold the pytest stubs that downstream plans will satisfy.

Purpose: PPIPE-01 + PPIPE-02 + PPIPE-03 — the data foundation. Subsequent plans (places.geojson /
places.json export, per-place SVG maps) all read `place_slug` out of `occurrences.parquet`.

Output: a new `data/places_load.py` pipeline step + run.py wiring; modified dbt sources/marts;
extended conftest fixture; new test_places_load.py unit tests; `geographies.places` populated and
`occurrences.parquet` shipping with a `place_slug VARCHAR` column.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/98-pipeline-integration/98-RESEARCH.md
@.planning/phases/98-pipeline-integration/98-PATTERNS.md
@.planning/phases/98-pipeline-integration/98-VALIDATION.md
@content/places.toml
@data/places_validation.py
@data/geographies_pipeline.py
@data/run.py
@data/dbt/models/sources.yml
@data/dbt/models/marts/occurrences.sql
@data/dbt/models/marts/schema.yml
@data/tests/conftest.py
@data/tests/test_places_validation.py

<interfaces>
<!-- Key code surfaces the executor must respect. Extracted from codebase. -->

From data/places_validation.py:
  validate_places_step() -> None        # zero-arg STEPS wrapper; mirror this shape for load_places_step
  validate_places(toml_path: Path|str)  # canonical TOML reader pattern: tomllib.load + data.get("places", [])
  Uses `con.execute("LOAD spatial")` (NOT INSTALL — decision 97-01)

From data/geographies_pipeline.py:
  CREATE OR REPLACE TABLE geographies.<name> (...)  pattern with INSERT loop
  Mirror the schema-creation idiom but use LOAD spatial only (do NOT copy `INSTALL spatial; LOAD spatial;`)

From data/run.py (current STEPS order, lines 76-92):
  ("places-validation", validate_places_step),
  ("dbt-build", _run_dbt_build),
  ("topology-postprocess", clean_region_topology),
  ("species-export", export_species_parquet),
  ("species-maps", generate_species_maps),
  ("feeds", generate_feeds),
  # places-load goes between places-validation and dbt-build

From data/dbt/models/sources.yml (lines 31-37):
  - name: geographies
    schema: geographies
    tables:
      - name: us_counties
      - name: us_states
      - name: ecoregions
  # add `- name: places`

From data/dbt/models/marts/occurrences.sql (final SELECT lines 68-82):
  Current CTEs: joined, occ_pt, wa_counties, wa_eco, with_county, county_fallback, final_county,
                with_eco, eco_dedup, eco_fallback, final_eco
  Current column tail: fc.county, fe.ecoregion_l3
  Current JOIN tail:  JOIN final_county fc ON fc._row_id = j._row_id
                       JOIN final_eco    fe ON fe._row_id = j._row_id
  # Add wa_places, with_place, place_dedup CTEs after final_eco.
  # Add LEFT JOIN place_dedup fp ON fp._row_id = j._row_id (LEFT, not JOIN — no fallback).
  # Append fp.place_slug to the SELECT column list.

From data/dbt/models/marts/schema.yml (lines 60-68):
  Currently 30 columns; last is:
      - name: ecoregion_l3
        data_type: varchar
  # Append:
      - name: place_slug
        data_type: varchar

From data/tests/conftest.py:
  _create_schemas creates "geographies" schema (line 12)
  _create_tables already defines geographies.us_counties / us_states / ecoregions
  _seed_data inserts the canonical Chelan specimen at lat=47.608, lon=-120.912 (line 188-201)
  fixture_db builds an on-disk DuckDB and runs INSTALL spatial; LOAD spatial; (line 535)
  # Extend _create_tables with `geographies.places` (GEOMETRY column).
  # Extend _seed_data with a polygon covering BOTH test specimens (-120.912, 47.608) and the iNat
  # observation (-120.8, 47.5) — e.g. POLYGON((-121.1 47.4, -120.7 47.4, -120.7 47.8, -121.1 47.8, -121.1 47.4)).

From data/tests/test_places_validation.py:
  write_toml(tmp_path, places) helper inlines [[places]] entries (lines 22-43)
  _VALID_PLACE dict shape (line 13-19) — slug, name, land_owner, geometry_wkt, permits
  # Reuse this exact helper shape in test_places_load.py for consistency.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend conftest + write failing pytest stubs for places_load</name>
  <files>data/tests/conftest.py, data/tests/test_places_load.py</files>
  <read_first>
    - data/tests/conftest.py (extend _create_tables + _seed_data, do not break existing seeds)
    - data/tests/test_places_validation.py (copy write_toml helper shape + _VALID_PLACE fixture pattern)
    - data/places_validation.py (LOAD spatial pattern; TOML loader shape)
    - content/places.toml (real seed entries — keep test polygons disjoint from these to avoid coupling)
  </read_first>
  <behavior>
    - test_load_creates_table: write_toml(tmp_path, [_VALID_PLACE]); call load_places(toml_path, db_path); assert SELECT COUNT(*) FROM geographies.places returns 1
    - test_places_geometry_usable: after load_places, run `SELECT slug FROM geographies.places WHERE ST_Within(ST_Point(-120.95, 47.05), geom)` against the test polygon (POLYGON((-121.0 47.0, -120.9 47.0, -120.9 47.1, -121.0 47.1, -121.0 47.0))) — must return ('test-place',) — proves the GEOMETRY column survives the round-trip
    - Both tests MUST fail initially with ModuleNotFoundError on `from places_load import load_places` (places_load.py does not yet exist)
  </behavior>
  <action>
Extend `data/tests/conftest.py`:
1. In `_create_tables`, append a `CREATE TABLE geographies.places (slug VARCHAR, name VARCHAR, land_owner VARCHAR, geom GEOMETRY)` block — same indentation as the other geographies tables.
2. In `_seed_data`, append an INSERT into `geographies.places` with slug='test-place', name='Test Place', land_owner='DNR', geom = `ST_GeomFromText('POLYGON((-121.1 47.4, -120.7 47.4, -120.7 47.8, -121.1 47.8, -121.1 47.4))')`. This polygon covers BOTH the canonical Ecdysis specimen (-120.912, 47.608) and the iNat observation (-120.8, 47.5), enabling integration tests in later plans.

Create `data/tests/test_places_load.py`:
1. Module docstring: "Tests for places_load.py — geographies.places table creation and ST_Within usability (PPIPE-01)."
2. Copy `write_toml(tmp_path, places)` helper verbatim from `data/tests/test_places_validation.py` (lines 22-43) — same TOML serialization shape; permits inlined.
3. Define `_VALID_PLACE = {"slug": "test-place", "name": "Test Place", "land_owner": "DNR", "geometry_wkt": "POLYGON((-121.0 47.0, -120.9 47.0, -120.9 47.1, -121.0 47.1, -121.0 47.0))", "permits": [{"issuing_authority": "DNR", "type": "project-level"}]}`.
4. Implement `test_load_creates_table(tmp_path)`: write TOML, set `db_path = str(tmp_path / "test.duckdb")`, call `load_places(toml_path, db_path)`, assert count == 1.
5. Implement `test_places_geometry_usable(tmp_path)`: after load, open the DB, `LOAD spatial`, run the ST_Within point-inside query against `ST_Point(-120.95, 47.05)`, assert returned slug == 'test-place'.
6. Implement `test_occurrence_inside_place_gets_slug(tmp_path)`: load a place with polygon `POLYGON((-121.0 47.0, -120.9 47.0, -120.9 47.1, -121.0 47.1, -121.0 47.0))`; open DB with spatial; assert `SELECT slug FROM geographies.places WHERE ST_Within(ST_Point(-120.95, 47.05), geom)` returns `'test-place'` (slug is non-NULL for inside point). This validates the ST_Within LEFT JOIN semantic that will be used in occurrences.sql.
7. Implement `test_occurrence_outside_places_is_null(tmp_path)`: reuse the same loaded DB; assert `SELECT slug FROM geographies.places WHERE ST_Within(ST_Point(-110.0, 35.0), geom)` returns an empty result (no match → place_slug IS NULL for outside points). This validates the no-fallback requirement from STATE.md.
8. Imports: `import pytest`, `from pathlib import Path`, `import duckdb`, `from places_load import load_places`.

Verify failure surface: `cd data && uv run pytest tests/test_places_load.py -x` must exit non-zero with ModuleNotFoundError before Task 2 ships. This is Wave 0 RED.
  </action>
  <verify>
    <automated>cd data && uv run pytest tests/test_places_load.py -x 2>&1 | grep -E "ModuleNotFoundError|No module named 'places_load'"</automated>
  </verify>
  <acceptance_criteria>
    - data/tests/test_places_load.py exists and defines test_load_creates_table, test_places_geometry_usable, test_occurrence_inside_place_gets_slug, and test_occurrence_outside_places_is_null
    - `grep -c "from places_load import" data/tests/test_places_load.py` returns 1
    - `grep -c "ST_Within(ST_Point(-120.95, 47.05)" data/tests/test_places_load.py` returns 1
    - `grep -c "CREATE TABLE geographies.places" data/tests/conftest.py` returns 1
    - `grep -c "INSERT INTO geographies.places" data/tests/conftest.py` returns 1
    - The polygon WKT in conftest covers BOTH (-120.912, 47.608) AND (-120.8, 47.5) — verify by inspection that the bounding box `(-121.1, 47.4)..(-120.7, 47.8)` contains both points
    - `cd data && uv run pytest tests/test_places_load.py -x` fails with ModuleNotFoundError (RED proven)
    - Existing fixture tests still collect: `cd data && uv run pytest tests/test_places_validation.py -q` exits 0
    - No INSTALL spatial added anywhere in places_load.py (Task 2) or conftest extensions
  </acceptance_criteria>
  <done>Failing tests exist with the exact behavior contract; conftest can build a fixture_db whose `geographies.places` row covers both canonical test occurrence coordinates.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create places_load.py + wire into run.py STEPS</name>
  <files>data/places_load.py, data/run.py</files>
  <read_first>
    - data/places_validation.py (LOAD spatial; TOML loader; zero-arg step wrapper pattern)
    - data/geographies_pipeline.py (CREATE OR REPLACE TABLE + INSERT loop pattern; ignore its `INSTALL spatial; LOAD spatial;` line — decision 97-01 says LOAD only)
    - data/run.py (STEPS list, import block, where to slot places-load)
    - data/tests/test_places_load.py (target test contract from Task 1)
    - content/places.toml (real input shape; multi-line geometry_wkt requires .strip())
  </read_first>
  <behavior>
    - load_places(toml_path: Path|str, db_path: str|None = None) reads the TOML, opens (or creates) the DuckDB at db_path (default DB_PATH env or data/beeatlas.duckdb), LOAD spatial, CREATE SCHEMA IF NOT EXISTS geographies, CREATE OR REPLACE TABLE geographies.places, INSERTs one row per [[places]] entry using parameterized `ST_GeomFromText(?)`, prints `  geographies.places: {N} row(s) loaded` (# noqa: T201), closes the connection
    - load_places_step() resolves content/places.toml relative to this module's parent.parent, then calls load_places(toml_path)
    - run.py imports load_places_step and inserts `("places-load", load_places_step)` between the existing places-validation entry and the dbt-build entry
  </behavior>
  <action>
Create `data/places_load.py`:
1. Module docstring describing the role: "Load content/places.toml into geographies.places DuckDB table (PPIPE-01). Runs after places-validation and before dbt-build."
2. Imports: `os`, `tomllib`, `from pathlib import Path`, `import duckdb`.
3. Module-level `DB_PATH = os.environ.get('DB_PATH', str(Path(__file__).parent / 'beeatlas.duckdb'))` — match the env var convention used by data/geographies_pipeline.py and data/species_export.py.
4. `def load_places(toml_path: "Path | str", db_path: str | None = None) -> None`:
   - `toml_path = Path(toml_path)`
   - `with open(toml_path, "rb") as f: data = tomllib.load(f)`; `places = data.get("places", [])`
   - `db = db_path or DB_PATH`
   - `con = duckdb.connect(db)`
   - `con.execute("LOAD spatial")` — explicitly NOT `INSTALL spatial; LOAD spatial;` (decision 97-01)
   - `con.execute("CREATE SCHEMA IF NOT EXISTS geographies")`
   - `con.execute("CREATE OR REPLACE TABLE geographies.places (slug VARCHAR, name VARCHAR, land_owner VARCHAR, geom GEOMETRY)")`
   - Loop: `con.execute("INSERT INTO geographies.places VALUES (?, ?, ?, ST_GeomFromText(?))", [p["slug"], p["name"], p["land_owner"], p["geometry_wkt"].strip()])` for each `p in places`
   - `print(f"  geographies.places: {len(places)} row(s) loaded")  # noqa: T201`
   - `con.close()`
5. `def load_places_step() -> None`: zero-arg wrapper that resolves `Path(__file__).parent.parent / "content" / "places.toml"` and calls `load_places(toml_path)`. Mirrors `validate_places_step` exactly.

Modify `data/run.py`:
1. Add `from places_load import load_places_step` next to the existing `from places_validation import validate_places_step` import.
2. In the STEPS list, insert `("places-load", load_places_step),` on the line AFTER `("places-validation", validate_places_step),` and BEFORE `("dbt-build", _run_dbt_build),`. Result must read in order: places-validation → places-load → dbt-build.

Do NOT touch sources.yml / occurrences.sql / schema.yml in this task — that is Task 3.

The two pytest tests created in Task 1 must now pass:
- test_load_creates_table proves the table is created and counted
- test_places_geometry_usable proves the GEOMETRY column survives round-trip and ST_Within works against the polygon
  </action>
  <verify>
    <automated>cd data && uv run pytest tests/test_places_load.py -v</automated>
  </verify>
  <acceptance_criteria>
    - data/places_load.py exports `load_places` and `load_places_step`
    - `grep -c '"LOAD spatial"' data/places_load.py` returns at least 1
    - `grep -c 'INSTALL spatial' data/places_load.py` returns 0 (decision 97-01)
    - `grep -c 'CREATE OR REPLACE TABLE geographies.places' data/places_load.py` returns 1
    - `grep -c 'ST_GeomFromText(?)' data/places_load.py` returns 1 (parameterized — WKT-injection mitigation)
    - `grep -c 'from places_load import load_places_step' data/run.py` returns 1
    - In data/run.py STEPS, `places-load` appears AFTER `places-validation` and BEFORE `dbt-build`: `grep -n '"places-validation"\|"places-load"\|"dbt-build"' data/run.py` shows them in that order
    - `cd data && uv run pytest tests/test_places_load.py -v` exits 0 (both tests pass — GREEN)
    - `cd data && uv run python -c "from places_load import load_places_step; load_places_step()"` runs without error against the on-disk data/beeatlas.duckdb (loads the real places.toml)
    - After the smoke run above, `duckdb data/beeatlas.duckdb -c "SELECT COUNT(*) FROM geographies.places"` returns ≥ 2 (matching the content/places.toml entry count)
  </acceptance_criteria>
  <done>places_load.py + run.py wiring shipped; the Wave 0 pytest stubs are GREEN; `geographies.places` is populated from content/places.toml.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Add place_slug column to dbt occurrences mart (sources + SQL + schema atomically)</name>
  <files>data/dbt/models/sources.yml, data/dbt/models/marts/occurrences.sql, data/dbt/models/marts/schema.yml</files>
  <read_first>
    - data/dbt/models/sources.yml (append `- name: places` to the geographies tables block, lines 31-37)
    - data/dbt/models/marts/occurrences.sql (mirror the with_eco / eco_dedup pattern; final SELECT column list and JOIN tail)
    - data/dbt/models/marts/schema.yml (append place_slug as 31st column on the occurrences model)
    - .planning/phases/98-pipeline-integration/98-RESEARCH.md (Pattern 2 + Pitfall 1 — LEFT JOIN, no fallback)
  </read_first>
  <behavior>
    - sources.yml declares `geographies.places` so `{{ source('geographies', 'places') }}` resolves
    - occurrences.sql includes a `wa_places` source CTE, a `with_place` CTE that LEFT JOINs `ST_Within(occ_pt.pt, p.geom)`, a `place_dedup` CTE using `DISTINCT ON (_row_id)`, and a `LEFT JOIN place_dedup fp ON fp._row_id = j._row_id` in the final SELECT; the column list ends with `fp.place_slug`
    - schema.yml lists `place_slug` as `data_type: varchar` after the `ecoregion_l3` entry on the `occurrences` model — total column entries = 31
    - `bash data/dbt/run.sh build` exits 0 against data/beeatlas.duckdb (Task 2 already populated geographies.places); the resulting `data/dbt/target/sandbox/occurrences.parquet` contains `place_slug`
  </behavior>
  <action>
Modify `data/dbt/models/sources.yml`:
1. In the `geographies` source's `tables:` block (after `- name: ecoregions`), append `      - name: places` — preserve existing indentation. No other source blocks are touched.

Modify `data/dbt/models/marts/occurrences.sql`:
1. After the existing `final_eco AS (...)` CTE (line ~64-67) and BEFORE the final `SELECT` (line ~68), insert three new CTEs:
   - `wa_places AS (SELECT * FROM {{ source('geographies', 'places') }}),`
   - `with_place AS (SELECT occ_pt._row_id, p.slug AS place_slug FROM occ_pt LEFT JOIN wa_places p ON ST_Within(occ_pt.pt, p.geom)),`
   - `place_dedup AS (SELECT DISTINCT ON (_row_id) _row_id, place_slug FROM with_place)` — note the trailing comma if more CTEs follow, otherwise no comma if this is the last CTE before SELECT
   Keep CTE comma punctuation valid — last CTE before SELECT has no trailing comma.
2. In the final SELECT column list, change the last column line from `fc.county, fe.ecoregion_l3` to `fc.county, fe.ecoregion_l3,` and add a new line `    fp.place_slug` immediately after.
3. In the JOIN tail (currently `JOIN final_county fc ... JOIN final_eco fe ...`), append `LEFT JOIN place_dedup fp ON fp._row_id = j._row_id` — MUST be `LEFT JOIN` (not `JOIN`) because places have no fallback and `place_slug IS NULL` is the correct value for occurrences outside all named places. Pitfall 1.
4. Add a brief inline SQL comment above `wa_places AS (...)` reading `-- PPIPE-02: place_slug via ST_Within LEFT JOIN; no fallback (NULL is correct outside all named places).`

Modify `data/dbt/models/marts/schema.yml`:
1. In the `occurrences` model's `columns:` list, after the `- name: ecoregion_l3 / data_type: varchar` block (lines 67-68), append:
     `      - name: place_slug`
     `        data_type: varchar`
   The column count becomes 31. This MUST be in the same commit as occurrences.sql — splitting the change immediately fails the dbt contract.

Verification run: `cd data && bash dbt/run.sh build` (requires Task 2's `geographies.places` to exist in data/beeatlas.duckdb — that is satisfied). On success, inspect the produced parquet:
- `duckdb -c "DESCRIBE SELECT * FROM read_parquet('data/dbt/target/sandbox/occurrences.parquet')" | grep place_slug` returns a row with VARCHAR
- `duckdb -c "SELECT COUNT(*) FROM read_parquet('data/dbt/target/sandbox/occurrences.parquet') WHERE place_slug IS NOT NULL"` returns ≥ 1 if any occurrence falls inside a seeded place polygon; 0 is acceptable for the real dataset (it does NOT prove a bug — just that the seeded TOML polygons may not yet contain any occurrence). The semantic invariant (NULL for outside) is what matters.
- `duckdb -c "SELECT COUNT(*) FROM read_parquet('data/dbt/target/sandbox/occurrences.parquet')"` matches the row count before this change (LEFT JOIN must NOT drop rows — Pitfall 1).
  </action>
  <verify>
    <automated>cd data && bash dbt/run.sh build && duckdb -c "SELECT typeof(place_slug) FROM read_parquet('dbt/target/sandbox/occurrences.parquet') LIMIT 1" | grep -i VARCHAR</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "^      - name: places$" data/dbt/models/sources.yml` returns 1 (places added under geographies)
    - `grep -c "place_slug" data/dbt/models/marts/occurrences.sql` returns at least 3 (wa_places CTE alias `p.slug AS place_slug`, place_dedup column, fp.place_slug in final SELECT)
    - `grep -c "LEFT JOIN place_dedup" data/dbt/models/marts/occurrences.sql` returns 1
    - `grep -cE "^JOIN place_dedup" data/dbt/models/marts/occurrences.sql` returns 0 (no inner JOIN — Pitfall 1)
    - `grep -c "DISTINCT ON (_row_id)" data/dbt/models/marts/occurrences.sql` returns 2 (eco_dedup and place_dedup)
    - `grep -c "wa_places AS" data/dbt/models/marts/occurrences.sql` returns 1
    - `grep -c "source('geographies', 'places')" data/dbt/models/marts/occurrences.sql` returns 1
    - `grep -c "name: place_slug" data/dbt/models/marts/schema.yml` returns 1
    - In data/dbt/models/marts/schema.yml under the `occurrences` model, the column entry count (lines matching `^      - name:`) for that model equals 31 (was 30)
    - `cd data && bash dbt/run.sh build` exits 0 (contract enforced; no schema mismatch)
    - `duckdb -c "DESCRIBE SELECT * FROM read_parquet('data/dbt/target/sandbox/occurrences.parquet')"` lists a `place_slug` column of VARCHAR type
    - Row count of `occurrences.parquet` after the change equals (or exceeds — no row drops) the count before (LEFT JOIN invariant)
    - For at least one occurrence point known to fall inside `content/places.toml`'s polygons (e.g. inside the rattlesnake-ledge bbox `-121.78..-121.76, 47.43..47.44` if any real occurrence is there), `place_slug` is the matching slug; for points clearly outside all polygons, `place_slug IS NULL`. Document the result in the task SUMMARY even if zero occurrences fall inside the seeded polygons (acceptable — the contract still holds).
  </acceptance_criteria>
  <done>The dbt contract is 31 columns; occurrences.parquet carries `place_slug VARCHAR`; LEFT JOIN preserves all occurrences; the change is a single atomic commit covering sources.yml + occurrences.sql + schema.yml.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| content/places.toml -> places_load.py | TOML maintained by coordinator; geometry_wkt is free text |
| places_load.py -> DuckDB | parameterized INSERTs only |
| {{ source('geographies', 'places') }} -> dbt mart | DuckDB query plan; no external input crosses this boundary |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-98-01 | Tampering | geometry_wkt in places.toml -> ST_GeomFromText | mitigate | Phase 97 places_validation.py rejects invalid WKT and non-WGS84 ranges BEFORE places-load runs; places_load.py uses parameterized `ST_GeomFromText(?)` so even a malformed string cannot escape the geometry-parser boundary |
| T-98-02 | Tampering | Stale geographies.places from aborted run | mitigate | `CREATE OR REPLACE TABLE geographies.places` makes every run idempotent — no stale rows can survive |
| T-98-03 | Information Disclosure | place_slug leaks polygon membership for sensitive sites | accept | All places.toml entries are intentionally publicly published collecting locations; place_slug surfacing is the entire point of the milestone |
| T-98-SC | Tampering | npm/pip/cargo installs | mitigate | No new package installs in this plan (RESEARCH.md confirms stdlib + duckdb already pinned) — slopcheck N/A; if a future task adds a package the legitimacy gate fires before install |
</threat_model>

<verification>
After all three tasks land:
1. `cd data && uv run pytest tests/test_places_load.py tests/test_places_validation.py -v` — both files exit 0
2. `cd data && uv run python run.py` runs cleanly through places-validation → places-load → dbt-build (and the rest of the pipeline) against data/beeatlas.duckdb
3. `duckdb data/beeatlas.duckdb -c "SELECT slug, name, ST_AsText(geom) FROM geographies.places"` shows all rows from content/places.toml
4. `duckdb -c "SELECT typeof(place_slug), COUNT(*) FROM read_parquet('public/data/occurrences.parquet') GROUP BY 1"` shows VARCHAR (the column exists and has the right type)
5. `grep -c 'place_slug' data/dbt/models/marts/schema.yml data/dbt/models/marts/occurrences.sql` returns ≥ 2 (contract present in both)
</verification>

<success_criteria>
- PPIPE-01: `places-load` step runs between places-validation and dbt-build; geographies.places table populated from content/places.toml with one GEOMETRY row per entry
- PPIPE-02: occurrences.parquet has `place_slug VARCHAR` column; occurrences inside a place polygon carry its slug; occurrences outside all polygons carry NULL (no fallback); LEFT JOIN preserves row count
- PPIPE-03: dbt contract is 31 columns; schema.yml and occurrences.sql shipped atomically; `bash data/dbt/run.sh build` exits 0
- Test stubs for downstream plans exist (test_places_load.py); conftest.py fixture_db now includes a test polygon covering the canonical test coordinates so future integration tests can assert spatial-join behavior
</success_criteria>

<output>
Create `.planning/phases/98-pipeline-integration/098-01-SUMMARY.md` when done.
</output>
