---
phase: 99-place-static-pages
plan: 1
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/REQUIREMENTS.md
  - .planning/ROADMAP.md
  - src/tests/data-places.test.ts
  - src/tests/build-output.test.ts
autonomous: true
requirements: [PPAGE-01, PPAGE-02]
tags: [eleventy, nunjucks, static-pages, tdd, places, vitest]

must_haves:
  truths:
    - "REQUIREMENTS.md PPAGE-01 and PPAGE-02 no longer reference permits in any form (no permit table, no permit status summary)"
    - "ROADMAP.md Phase 99 success criteria 1 and 3 no longer reference permits"
    - "REQUIREMENTS.md §Out of Scope contains a Permit table display row"
    - "src/tests/data-places.test.ts exists and contains assertions for _data/places.js shape"
    - "src/tests/build-output.test.ts contains place-page assertions (PPAGE-01 and PPAGE-02)"
    - "The new tests in build-output.test.ts and data-places.test.ts are RED (fail) at end of this plan — implementation is in Plan 02"
  artifacts:
    - path: ".planning/REQUIREMENTS.md"
      provides: "Permits scrubbed from PPAGE-01 and PPAGE-02; Out of Scope row added"
      contains: "PPAGE-02"
    - path: ".planning/ROADMAP.md"
      provides: "Phase 99 success criteria scrubbed of permits"
      contains: "Phase 99: Place Static Pages"
    - path: "src/tests/data-places.test.ts"
      provides: "Unit contract for _data/places.js (placesArray shape, no parquet read)"
      contains: "describe('_data/places.js"
    - path: "src/tests/build-output.test.ts"
      provides: "Augmented with place-page build-output tests"
      contains: "_site/places.html"
  key_links:
    - from: "src/tests/data-places.test.ts"
      to: "_data/places.js"
      via: "import places from '../../_data/places.js'"
      pattern: "import .* from '\\.\\./\\.\\./_data/places\\.js'"
    - from: "src/tests/build-output.test.ts"
      to: "_site/places.html"
      via: "readFileSync(resolve(ROOT, '_site/places.html'))"
      pattern: "_site/places\\.html"
---

<objective>
Open Phase 99 by (1) scrubbing every permit reference out of REQUIREMENTS.md §PPAGE-01 / §PPAGE-02 and ROADMAP.md §Phase 99 per locked decision D-01 (permits wholly out of v3.7 scope), and (2) writing the RED test scaffolding that Plan 02 will turn GREEN.

Purpose: Lock the phase scope (no permit display anywhere) into the canonical planning docs so downstream agents do not re-introduce it, and pin the expected behavior of the place index page, the per-place detail page, and the `_data/places.js` data module via Vitest assertions before any implementation is written.

Output: Updated REQUIREMENTS.md, updated ROADMAP.md, new `src/tests/data-places.test.ts` file, and augmented `src/tests/build-output.test.ts` with place-page assertions.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/99-place-static-pages/99-CONTEXT.md
@.planning/phases/99-place-static-pages/99-RESEARCH.md
@.planning/phases/99-place-static-pages/99-PATTERNS.md
@.planning/phases/99-place-static-pages/99-UI-SPEC.md
@src/tests/data-species.test.ts
@src/tests/build-output.test.ts
@public/data/places.json

<interfaces>
<!-- Key signatures and field names the tests will assert against. -->
<!-- These come from existing analog files and the committed places.json. -->

From public/data/places.json (committed, 2 entries — rattlesnake-ledge, tiger-mountain):
- Each place object has fields: slug (string), name (string), land_owner (string), specimen_count (number), sample_count (number)
- Current seed data has specimen_count: 0 for both entries — the SVG guard `specimen_count > 0` means no <img> tag is rendered yet

From _data/species.js (analog for _data/places.js — Plan 02 will create the mirror):
- Default export shape: { placesArray } per Pattern 1 of 99-RESEARCH.md
- Plan 02 will read public/data/places.json (NOT parquet); HMR pitfall in species.js comment

From src/tests/data-species.test.ts (analog test file):
- Imports module as: `import species from '../../_data/species.js'` with `@ts-expect-error`
- Asserts source does not contain /parquet/i via readFileSync of the module source

From src/tests/build-output.test.ts (analog and target for augmentation):
- Existing describe is `describe.skipIf(SKIP_BUILD)('build output (PAGE-07, PAGE-09)', ...)`
- New tests slot inside that same describe block — beforeAll already runs `npm run build` for the whole block
- ROOT constant is `resolve(dirname(fileURLToPath(import.meta.url)), '../..')`

SVG map directory canonical path:
- Phase 98 (pipeline) writes to `public/data/place-maps/{slug}.svg` (SINGULAR "map") per data/places_maps.py
- CONTEXT.md D-07 says "places-maps/" (plural) — that is a typo; the canonical path is `place-maps/`
- Tests in this plan assert that the rendered HTML, when an SVG is present, references `/data/place-maps/` (singular). For Plan 01 (current plan) the absent-img test asserts the HTML contains no `place-maps` substring at all (because seed data has specimen_count=0)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Scrub permit references from REQUIREMENTS.md and ROADMAP.md (D-01)</name>
  <files>.planning/REQUIREMENTS.md, .planning/ROADMAP.md</files>
  <read_first>
    - .planning/REQUIREMENTS.md (see §PPAGE-01, §PPAGE-02, and §Out of Scope — the rows to update)
    - .planning/ROADMAP.md (see ### Phase 99 success-criteria block — criteria 1 and 3 mention permits)
    - .planning/phases/99-place-static-pages/99-CONTEXT.md (see decision D-01 for exact scope-change wording: "Permit table is **out of scope** for Phase 99 (and for the v3.7 milestone)")
  </read_first>
  <action>
    Apply locked decision D-01 (permits wholly out of scope for v3.7) to two planning docs. The CONTEXT D-01 wording is unambiguous: "Do not implement any permit display." That includes the per-place "permit table" AND the index-page "permit status summary" — both are permit displays.

    1. In `.planning/REQUIREMENTS.md` §PPAGE-01: rewrite the requirement text to read exactly:
       `**PPAGE-01**: /places.html index page (or equivalent direct-path URL per Eleventy permalink config) lists all places with name, land owner, and specimen count`
       Remove the substring "permit status summary, ". Keep the checkbox state ([ ]) unchanged.

    2. In `.planning/REQUIREMENTS.md` §PPAGE-02: rewrite the requirement text to read exactly:
       `**PPAGE-02**: Per-place page at a direct-path URL (e.g. /places/{slug}.html) shows name, owner, specimen count, SVG occurrence map, and a link that opens the main map with that place's filter applied`
       Remove the substring "permit table with active/inactive/no-expiry status, ". Keep the checkbox state ([ ]) unchanged.

    3. In `.planning/REQUIREMENTS.md` §Out of Scope table: add a new row at the bottom of the table with columns `| Permit display (table or summary) | Removed from v3.7 milestone per Phase 99 decision D-01; revisit in v3.8+ when permit tracking resurfaces |`. Do not change any other rows.

    4. In `.planning/ROADMAP.md` ### Phase 99 success-criteria list, success-criterion 1: rewrite the bullet text to read exactly:
       `  1. /places.html (or equivalent direct-path URL) lists all places with name, land owner, and specimen count`
       Remove the substring "permit status summary, ".

    5. In `.planning/ROADMAP.md` ### Phase 99 success-criteria list, success-criterion 3: rewrite the bullet text to read exactly:
       `  3. The per-place page shows name, land owner, specimen count, the SVG occurrence map, and a link that opens the main map with that place's filter applied`
       Remove the substring "a permit table with active/inactive/no-expiry status, ". Leave criteria 2 and 4 unchanged.

    Do not edit any other phase entries, the traceability table, the milestone summary line, the Future Requirements section, or the v3.7 Requirements headers. Do not renumber requirement IDs. Do not touch the Deferred Ideas section of 99-CONTEXT.md (which already lists permits as deferred).
  </action>
  <verify>
    <automated>set -e; PR=$(grep -cE "permit (table|status)" .planning/REQUIREMENTS.md | tr -d '\n'); PM=$(grep -cE "permit (table|status)" .planning/ROADMAP.md | tr -d '\n'); OOS=$(grep -cF "Permit display (table or summary)" .planning/REQUIREMENTS.md | tr -d '\n'); echo "REQ_permits=$PR ROADMAP_permits=$PM OOS_row=$OOS"; [ "$PM" = "0" ] || { echo "FAIL: ROADMAP.md still references permits"; exit 1; }; [ "$OOS" = "1" ] || { echo "FAIL: Out of Scope row missing"; exit 1; }; [ "$PR" = "0" ] || { echo "NOTE: REQUIREMENTS.md still contains 'permit table' or 'permit status' references — verify only the v3.7 PPAGE-01/PPAGE-02 lines were targeted"; exit 1; }; echo "OK"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -E "permit (table|status)" .planning/REQUIREMENTS.md` returns 0 matches (PPAGE-01 and PPAGE-02 both cleaned; no other v3.7 requirement mentions permits)
    - `grep -E "permit (table|status)" .planning/ROADMAP.md` returns 0 matches
    - `grep -F "PPAGE-01" .planning/REQUIREMENTS.md` still matches (requirement not deleted, only edited)
    - `grep -F "PPAGE-02" .planning/REQUIREMENTS.md` still matches (requirement not deleted, only edited)
    - `grep -F "Phase 99: Place Static Pages" .planning/ROADMAP.md` still matches the phase heading
    - `grep -F "Permit display (table or summary)" .planning/REQUIREMENTS.md` returns exactly 1 match (the new Out-of-Scope row)
    - `grep -F "PCOLL-01" .planning/REQUIREMENTS.md` still matches (Future Requirements section preserved — those are v3.8+ items unrelated to this scope change)
  </acceptance_criteria>
  <done>
    REQUIREMENTS.md PPAGE-01 and PPAGE-02 both reflect D-01 (no permit display of any kind). ROADMAP.md Phase 99 success criteria 1 and 3 are similarly scrubbed. The Out-of-Scope table carries a Permit-display row. Future Requirements (PCOLL-*, PRICH-*) are untouched.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create src/tests/data-places.test.ts with RED unit contract for _data/places.js</name>
  <files>src/tests/data-places.test.ts</files>
  <read_first>
    - src/tests/data-species.test.ts (analog test file — copy the import shape, the `@ts-expect-error`, the ROOT constant, and the "does NOT read parquet" pattern)
    - public/data/places.json (confirms the field names: slug, name, land_owner, specimen_count, sample_count)
    - .planning/phases/99-place-static-pages/99-PATTERNS.md (see the "src/tests/data-places.test.ts" section for the exact test bodies to replicate)
    - .planning/phases/99-place-static-pages/99-RESEARCH.md (see §Validation Architecture for the test contract intent)
  </read_first>
  <behavior>
    - Test 1 (PPAGE-01): module default export has a `placesArray` property that is an Array
    - Test 2 (PPAGE-01): every entry in `placesArray` has string `slug`, string `name`, string `land_owner`, numeric `specimen_count`, numeric `sample_count`
    - Test 3 (PPAGE-01): `placesArray.length` is greater than 0 (seed data has 2 entries)
    - Test 4 (Pitfall #8 — HMR): the raw source of `_data/places.js` does not contain the case-insensitive substring `parquet`
    - All four tests RED initially because `_data/places.js` does not exist yet — Plan 02 creates it
  </behavior>
  <action>
    Create a new Vitest file at `src/tests/data-places.test.ts` that mirrors `src/tests/data-species.test.ts` structure. The file MUST:

    - Use ESM imports: `describe, test, expect` from `vitest`; `readFileSync` from `node:fs`; `resolve, dirname` from `node:path`; `fileURLToPath` from `node:url`
    - Import the data module via `import places from '../../_data/places.js'` preceded by a `// @ts-expect-error -- _data/*.js is plain ESM consumed by Eleventy; no .d.ts` comment line
    - Define `const ROOT = resolve(dirname(fileURLToPath(import.meta.url)), '../..')`
    - Wrap all tests in `describe('_data/places.js (PPAGE-01, PPAGE-02)', () => { ... })`
    - Implement the four tests listed in `<behavior>`. The field-shape test must iterate `placesArray` and assert `typeof p.slug === 'string'`, `typeof p.name === 'string'`, `typeof p.land_owner === 'string'`, `typeof p.specimen_count === 'number'`, `typeof p.sample_count === 'number'`
    - The "does NOT read parquet" test must `readFileSync(resolve(ROOT, '_data/places.js'), 'utf-8')` and assert the result does not match `/parquet/i`
    - Add a one-line leading comment: `// Phase 99 Wave 0 — RED contract for _data/places.js (PPAGE-01, PPAGE-02). Mirrors data-species.test.ts.`

    Do NOT add color-algorithm tests, tree tests, or anything beyond the four behaviors listed. `_data/places.js` is intentionally minimal per 99-RESEARCH.md Pattern 1.
  </action>
  <verify>
    <automated>VITEST_SKIP_BUILD=1 npx vitest run src/tests/data-places.test.ts 2>&1 | tee /tmp/p99-01-vt.log; grep -E "Test Files.*failed|Cannot find module|Failed to load" /tmp/p99-01-vt.log && echo "OK: RED as expected" || (echo "UNEXPECTED: tests passed without _data/places.js present" && exit 1)</automated>
  </verify>
  <acceptance_criteria>
    - File `src/tests/data-places.test.ts` exists
    - File contains the literal string `describe('_data/places.js (PPAGE-01, PPAGE-02)'`
    - File contains the literal string `import places from '../../_data/places.js'`
    - File contains the literal string `@ts-expect-error -- _data/*.js is plain ESM consumed by Eleventy; no .d.ts`
    - File contains exactly four `test(` invocations (grep `-c "  test("` returns 4)
    - File contains the assertion `expect(src).not.toMatch(/parquet/i)` (the HMR pitfall guard)
    - `VITEST_SKIP_BUILD=1 npx vitest run src/tests/data-places.test.ts` exits non-zero because `_data/places.js` does not exist (RED state expected at end of Plan 01)
  </acceptance_criteria>
  <done>
    The new test file exists, is syntactically valid TypeScript, and fails when run because the module it imports has not been written yet. Plan 02 will create `_data/places.js` and make these tests green.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Augment src/tests/build-output.test.ts with place-page assertions</name>
  <files>src/tests/build-output.test.ts</files>
  <read_first>
    - src/tests/build-output.test.ts (target file — see the existing describe.skipIf(SKIP_BUILD) block and the existing species/genus/subgenus/tribe test groupings; new tests slot at the end of that same describe block before the closing `})`)
    - public/data/places.json (the two seed slugs — rattlesnake-ledge and tiger-mountain — are used as concrete fixtures in the assertions)
    - _pages/species-detail.njk (analog for the per-item page output the new tests assert on)
    - _pages/species.njk (analog for the index-page output)
    - .planning/phases/99-place-static-pages/99-PATTERNS.md (see the "src/tests/build-output.test.ts" section for the exact assertion bodies)
  </read_first>
  <behavior>
    - Test 1 (PPAGE-01): `_site/places.html` exists and contains the literal `class="places-list"` AND a per-place link of the form `href="/places/{slug}.html"` for at least one slug (regex: `href="/places/[a-z0-9-]+\.html"`)
    - Test 2 (PPAGE-01): `_site/places.html` contains the name `Rattlesnake Ledge Recreation Area` and the owner `Washington Department of Natural Resources` (both are in seed places.json)
    - Test 3 (PPAGE-02): `_site/places/rattlesnake-ledge.html` exists and contains `Rattlesnake Ledge Recreation Area`, the owner text, a `\d+ specimens` string, and the deep-link `href="/?place=rattlesnake-ledge"`
    - Test 4 (PPAGE-02): `_site/places/rattlesnake-ledge.html` contains no occurrence of `place-maps` AND no occurrence of `places-maps` (because seed specimen_count is 0 and the `<img>` is suppressed by the guard)
    - Test 5 (PPAGE-02): every `<img>` on `_site/places/rattlesnake-ledge.html` carries `loading="lazy"` (mirrors the species page lazy-loading carry-forward)
    - Test 6 (D-09 — no JS entry): `_site/places.html` and `_site/places/rattlesnake-ledge.html` each contain no `<script type="module"` tag (places pages are fully static)
    - Test 7 (D-02 — direct-path URL): `_site/places/rattlesnake-ledge.html` exists as a flat file (no `_site/places/rattlesnake-ledge/index.html` is produced)
    - All seven tests RED initially because the templates do not exist yet — Plan 02 creates them.
  </behavior>
  <action>
    Insert seven new `test(...)` blocks inside the existing `describe.skipIf(SKIP_BUILD)('build output (PAGE-07, PAGE-09)', ...)` block in `src/tests/build-output.test.ts`. Place the new block at the end of the describe, after the last existing tribe test and before the closing `});`. Use the existing `ROOT`, `readFileSync`, `existsSync` (already imported) — do not add new imports.

    Each new test must be tagged with `(PPAGE-01)` or `(PPAGE-02)` in its description so the requirement→test mapping is greppable. Prefix the group with a comment line `// Phase 99 — place page tests (PPAGE-01, PPAGE-02)`.

    Concrete assertions (slug values come from public/data/places.json):
    - Test 1: `expect(html).toMatch(/class="places-list"/)` AND `expect(html).toMatch(/href="\/places\/[a-z0-9-]+\.html"/)` reading `_site/places.html`
    - Test 2: assert `_site/places.html` `toContain('Rattlesnake Ledge Recreation Area')` and `toContain('Washington Department of Natural Resources')`
    - Test 3: read `_site/places/rattlesnake-ledge.html`; assert toContain('Rattlesnake Ledge Recreation Area'), toContain('Washington Department of Natural Resources'), toMatch(/\d+ specimens/), toMatch(/href="\/\?place=rattlesnake-ledge"/)
    - Test 4: read `_site/places/rattlesnake-ledge.html`; assert `expect(html).not.toMatch(/place-maps/)` AND `expect(html).not.toMatch(/places-maps/)`
    - Test 5: read `_site/places/rattlesnake-ledge.html`; for every match of `/<img\b[^>]*>/g` assert `loading="lazy"` is present (mirror the existing species img-lazy test pattern at build-output.test.ts lines 94-102)
    - Test 6: read both `_site/places.html` and `_site/places/rattlesnake-ledge.html`; assert `expect(html).not.toMatch(/<script\s+type="module"/)` on each
    - Test 7: `expect(existsSync(resolve(ROOT, '_site/places/rattlesnake-ledge.html'))).toBe(true)` AND `expect(existsSync(resolve(ROOT, '_site/places/rattlesnake-ledge/index.html'))).toBe(false)`

    Do not modify the existing tests. Do not change `SKIP_BUILD` semantics. Do not add `beforeAll` — the existing one runs `npm run build` for the entire describe.
  </action>
  <verify>
    <automated>npx vitest run src/tests/build-output.test.ts 2>&1 | tee /tmp/p99-01-bo.log; grep -E "_site/places.html|places/rattlesnake-ledge.html" /tmp/p99-01-bo.log >/dev/null && grep -E "(Test Files.*failed|FAIL)" /tmp/p99-01-bo.log >/dev/null && echo "OK: RED as expected" || (echo "Inspect /tmp/p99-01-bo.log — expected RED state, place tests should fail because templates do not exist yet" && exit 1)</automated>
  </verify>
  <acceptance_criteria>
    - `src/tests/build-output.test.ts` contains the comment line `// Phase 99 — place page tests (PPAGE-01, PPAGE-02)`
    - `grep -c "(PPAGE-0[12])" src/tests/build-output.test.ts` returns at least 7
    - The file contains the literal string `_site/places.html`
    - The file contains the literal string `_site/places/rattlesnake-ledge.html`
    - The file contains the literal string `href="\/?place=rattlesnake-ledge"` (escape syntax used in the test pattern)
    - The file contains the assertion that `_site/places/rattlesnake-ledge/index.html` does NOT exist (D-02 direct-path verification)
    - All existing tests in the file are still present (test-count grep of `^  test(` does not decrease relative to before)
    - `npx vitest run src/tests/build-output.test.ts` runs `npm run build` and the new place tests fail (RED) — existing species/genus/subgenus/tribe tests still pass
  </acceptance_criteria>
  <done>
    `src/tests/build-output.test.ts` now contains seven new tests for the places index and detail pages. They fail at end of Plan 01 because Plan 02 has not yet produced the templates that emit the asserted HTML. Existing tests still pass.
  </done>
</task>

</tasks>

<verification>
After all three tasks:
- `grep -E "permit (table|status)" .planning/REQUIREMENTS.md .planning/ROADMAP.md` returns 0 matches
- `grep -F "Permit display (table or summary)" .planning/REQUIREMENTS.md` returns exactly 1 match (the new Out-of-Scope row)
- `ls src/tests/data-places.test.ts` succeeds
- `grep -c "(PPAGE-0[12])" src/tests/build-output.test.ts` returns >= 7
- Running `npx vitest run src/tests/data-places.test.ts src/tests/build-output.test.ts` produces a RED test suite (the new place tests fail; the existing species/genus/subgenus/tribe tests still pass). This is the expected end-of-plan state — Plan 02 turns these tests GREEN.
</verification>

<success_criteria>
- REQUIREMENTS.md §PPAGE-01 and §PPAGE-02 contain no permit references
- REQUIREMENTS.md §Out of Scope contains a "Permit display (table or summary)" row
- ROADMAP.md §Phase 99 success criteria 1 and 3 contain no permit references
- Future Requirements section (PCOLL-*, PRICH-*) is preserved unchanged
- `src/tests/data-places.test.ts` exists and follows the data-species.test.ts skeleton
- `src/tests/build-output.test.ts` contains seven new place-page assertions inside the existing skipIf describe block
- The new place-related tests are RED (fail because Plan 02 hasn't run); pre-existing tests still pass
- No production source files (`_data/`, `_pages/`, `src/styles/`) are touched in this plan
</success_criteria>

<output>
Create `.planning/phases/99-place-static-pages/99-01-SUMMARY.md` when done. Include in summary: links to the four modified files, the test counts (4 in data-places.test.ts, 7 added in build-output.test.ts), the documentation scope-change applied (PPAGE-01 + PPAGE-02 + ROADMAP success criteria 1 + 3 + Out-of-Scope row), and the confirmation that the new place tests are RED (and that this is expected — Plan 02 turns them GREEN).
</output>
