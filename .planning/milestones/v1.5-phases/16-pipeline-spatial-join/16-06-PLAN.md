---
phase: 16-pipeline-spatial-join
plan: "06"
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/assets/wa_counties.geojson
  - frontend/src/assets/epa_l3_ecoregions_wa.geojson
autonomous: true
gap_closure: true
requirements:
  - PIPE-07

must_haves:
  truths:
    - "wa_counties.geojson exists at frontend/src/assets/ and is under 400 KB"
    - "epa_l3_ecoregions_wa.geojson exists at frontend/src/assets/ and is under 400 KB"
    - "Both GeoJSON files are committed to git and will be present during CI frontend builds without any S3 or workflow dependency"
  artifacts:
    - path: "frontend/src/assets/wa_counties.geojson"
      provides: "Simplified WA county boundaries for frontend bundling"
      contains: "NAME property on each feature"
    - path: "frontend/src/assets/epa_l3_ecoregions_wa.geojson"
      provides: "Simplified WA EPA Level III ecoregion boundaries for frontend bundling"
      contains: "NA_L3NAME property on each feature"
  key_links:
    - from: "data/scripts/build_geojson.py"
      to: "frontend/src/assets/wa_counties.geojson"
      via: "gdf.to_file(ASSETS_DIR / 'wa_counties.geojson', driver='GeoJSON')"
      pattern: "wa_counties\\.geojson"
    - from: "data/scripts/build_geojson.py"
      to: "frontend/src/assets/epa_l3_ecoregions_wa.geojson"
      via: "gdf.to_file(ASSETS_DIR / 'epa_l3_ecoregions_wa.geojson', driver='GeoJSON')"
      pattern: "epa_l3_ecoregions_wa\\.geojson"
---

<objective>
Generate the simplified WA county and ecoregion GeoJSON boundary files and commit them to git so they are present during every CI frontend build without requiring any S3 cache or workflow changes.

Purpose: The deploy workflow (`deploy.yml`) runs `npm run build` which bundles `frontend/src/assets/`. These GeoJSON files must be committed to git — they are static WA boundary data that changes infrequently (annual Census TIGER updates; ecoregion boundaries are stable). Committing is the simplest path: no S3 upload changes, no workflow changes, and the files travel with the repo.

Output: Two committed GeoJSON files under 400 KB each in `frontend/src/assets/`.
</objective>

<execution_context>
@/Users/rainhead/.claude/get-shit-done/workflows/execute-plan.md
@/Users/rainhead/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-pipeline-spatial-join/16-CONTEXT.md

<!-- Gap being closed: GeoJSON files do not exist and have no path to exist in CI.
     build_geojson.py works correctly (all 9 tests pass) but is only wired into
     the local build-data.sh, not any CI workflow. Committing the generated files
     to git is the simplest resolution (option 1 from VERIFICATION.md). -->

<interfaces>
<!-- From data/scripts/build_geojson.py — the script to execute -->
Key functions:
  build_county_geojson(out_path=None) → writes wa_counties.geojson to ASSETS_DIR (or out_path)
  build_ecoregion_geojson(out_path=None) → writes epa_l3_ecoregions_wa.geojson to ASSETS_DIR (or out_path)
  load_county_gdf() → loads TIGER zip, filters to STATEFP='53', reprojects to EPSG:4326
  load_ecoregion_gdf() → loads NA_CEC_Eco_Level3.zip, reprojects to EPSG:4326
  SIMPLIFY_TOLERANCE = 0.006  # verified: produces ~357 KB for ecoregions (under 400 KB)
  ASSETS_DIR = data/../frontend/src/assets/

Script invocation:
  cd data && uv run python scripts/build_geojson.py
  (The __main__ block calls both build_county_geojson() and build_ecoregion_geojson())

Source files (download-if-missing):
  data/NA_CEC_Eco_Level3.zip  — already present (confirmed in research)
  data/tl_2024_us_county.zip  — will be downloaded (~84 MB) if not present
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run build_geojson.py to generate GeoJSON files</name>
  <files>frontend/src/assets/wa_counties.geojson, frontend/src/assets/epa_l3_ecoregions_wa.geojson</files>
  <action>
    Run the build_geojson.py script from the data/ directory. It will:
    - Download tl_2024_us_county.zip (~84 MB) if not already present at data/tl_2024_us_county.zip
    - Generate wa_counties.geojson (39 WA county polygons, NAME property, simplified at 0.006 degrees)
    - Generate epa_l3_ecoregions_wa.geojson (WA-clipped ecoregion polygons, NA_L3NAME property, simplified at 0.006 degrees)
    - Write both files directly to frontend/src/assets/

    Command: cd /Users/rainhead/dev/beeatlas/data && uv run python scripts/build_geojson.py

    Note: The ecoregion zip (NA_CEC_Eco_Level3.zip) is already present; only the TIGER county zip may need downloading. Allow up to 2 minutes for the county zip download on first run.

    After generation, verify file sizes with: ls -lh /Users/rainhead/dev/beeatlas/frontend/src/assets/*.geojson
    Both files must be under 400 KB. At SIMPLIFY_TOLERANCE=0.006, expected sizes are approximately:
    - wa_counties.geojson: well under 100 KB (39 simple county polygons)
    - epa_l3_ecoregions_wa.geojson: ~357 KB (measured in research)

    If either file exceeds 400 KB, the simplification tolerance is wrong — check SIMPLIFY_TOLERANCE in build_geojson.py and confirm it is 0.006 (not 0.005).
  </action>
  <verify>
    <automated>ls -lh /Users/rainhead/dev/beeatlas/frontend/src/assets/wa_counties.geojson /Users/rainhead/dev/beeatlas/frontend/src/assets/epa_l3_ecoregions_wa.geojson && python3 -c "import json; d=json.load(open('/Users/rainhead/dev/beeatlas/frontend/src/assets/wa_counties.geojson')); assert d['features'][0]['properties'].get('NAME'), 'NAME missing'; d2=json.load(open('/Users/rainhead/dev/beeatlas/frontend/src/assets/epa_l3_ecoregions_wa.geojson')); assert d2['features'][0]['properties'].get('NA_L3NAME'), 'NA_L3NAME missing'; print('Both files valid')"</automated>
  </verify>
  <done>Both GeoJSON files exist in frontend/src/assets/, are under 400 KB, and contain NAME (counties) and NA_L3NAME (ecoregions) properties on their features.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Commit GeoJSON files to git</name>
  <files>frontend/src/assets/wa_counties.geojson, frontend/src/assets/epa_l3_ecoregions_wa.geojson</files>
  <action>
    Add both GeoJSON files to git and commit them. These are static WA boundary data files — not build artifacts, not secrets. They are NOT covered by .gitignore (only *.parquet is ignored).

    Confirm they are not gitignored first:
      git check-ignore -v /Users/rainhead/dev/beeatlas/frontend/src/assets/wa_counties.geojson
      (should print nothing — unignored)

    Then stage and commit:
      git add frontend/src/assets/wa_counties.geojson frontend/src/assets/epa_l3_ecoregions_wa.geojson
      git commit -m "feat(16): commit WA county and ecoregion GeoJSON boundary files

    Static boundary files generated by data/scripts/build_geojson.py.
    Committed to git so frontend builds have these files without requiring
    S3 cache changes. SIMPLIFY_TOLERANCE=0.006 degrees; ecoregion file is
    ~357 KB (under 400 KB success criterion).

    Closes Gap 2 from 16-VERIFICATION.md (PIPE-07)."

    Do NOT use git add -f or git add . — add only the two specific files.
  </action>
  <verify>
    <automated>git log --oneline -1 /Users/rainhead/dev/beeatlas/frontend/src/assets/wa_counties.geojson && git log --oneline -1 /Users/rainhead/dev/beeatlas/frontend/src/assets/epa_l3_ecoregions_wa.geojson</automated>
  </verify>
  <done>Both GeoJSON files appear in git log — they are tracked and committed. A CI deploy build starting from a fresh checkout will have these files available for frontend bundling.</done>
</task>

</tasks>

<verification>
After both tasks complete:
1. git show HEAD --stat | grep geojson — both files appear in the commit
2. ls -lh /Users/rainhead/dev/beeatlas/frontend/src/assets/*.geojson — confirm sizes under 400 KB
3. cd /Users/rainhead/dev/beeatlas/data && uv run pytest tests/test_spatial.py::TestGeoJSONGeneration -x — confirm generation tests still pass
</verification>

<success_criteria>
- frontend/src/assets/wa_counties.geojson committed to git, under 400 KB, contains NAME property
- frontend/src/assets/epa_l3_ecoregions_wa.geojson committed to git, under 400 KB, contains NA_L3NAME property
- git status shows both files as tracked (not untracked, not modified)
- A fresh git checkout will have both files present for the Vite build to bundle
</success_criteria>

<output>
After completion, create `.planning/phases/16-pipeline-spatial-join/16-06-SUMMARY.md`
</output>
