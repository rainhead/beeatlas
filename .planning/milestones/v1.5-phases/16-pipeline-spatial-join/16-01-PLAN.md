---
phase: 16-pipeline-spatial-join
plan: 01
type: tdd
wave: 0
depends_on: []
files_modified:
  - data/tests/test_spatial.py
autonomous: true
requirements:
  - PIPE-05
  - PIPE-06
  - PIPE-07

must_haves:
  truths:
    - "Test file exists and pytest discovers all four test classes"
    - "All tests fail RED (implementations do not exist yet)"
    - "Test for add_region_columns verifies county and ecoregion_l3 columns added with correct string values"
    - "Test for nearest fallback verifies a point outside polygon boundary gets a non-null result"
    - "Test for iNat integration verifies main() output DataFrame has county and ecoregion_l3 columns"
    - "Test for GeoJSON generation verifies output file has expected properties and is under 400 KB"
  artifacts:
    - path: data/tests/test_spatial.py
      provides: "pytest test scaffold covering PIPE-05, PIPE-06, PIPE-07"
      exports: ["TestAddRegionColumns", "TestNearestFallback", "TestInatIntegration", "TestGeoJSONGeneration"]
  key_links:
    - from: data/tests/test_spatial.py
      to: data/spatial.py
      via: "import spatial"
      pattern: "from spatial import|import spatial"
    - from: data/tests/test_spatial.py
      to: data/scripts/build-geojson.py
      via: "import build_geojson"
      pattern: "from scripts.build_geojson|import build_geojson"
---

<objective>
Create the failing test scaffold for Phase 16. All four test classes must exist and fail RED before any implementation is written.

Purpose: Nyquist compliance — every implementation task in this phase has automated verification. Writing tests first defines the contracts that spatial.py and build-geojson.py must satisfy.
Output: data/tests/test_spatial.py with four test classes, all failing.
</objective>

<execution_context>
@/Users/rainhead/.claude/get-shit-done/workflows/execute-plan.md
@/Users/rainhead/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-pipeline-spatial-join/16-CONTEXT.md
@.planning/phases/16-pipeline-spatial-join/16-RESEARCH.md
@.planning/phases/16-pipeline-spatial-join/16-VALIDATION.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write failing test scaffold for spatial join and GeoJSON generation</name>
  <files>data/tests/test_spatial.py</files>
  <behavior>
    - TestAddRegionColumns.test_adds_county_column: Given a DataFrame with lon/lat cols and mock county GDF, add_region_columns returns DataFrame with 'county' column containing the correct county name string
    - TestAddRegionColumns.test_adds_ecoregion_column: Given mock ecoregion GDF, add_region_columns returns DataFrame with 'ecoregion_l3' column containing the correct region name string
    - TestAddRegionColumns.test_handles_inat_lat_lon_columns: DataFrame with 'lat'/'lon' column names (not 'latitude'/'longitude') is handled correctly
    - TestNearestFallback.test_point_outside_polygon_gets_nearest: A point clearly outside all mock polygons still gets a non-null county value (nearest fallback fires)
    - TestNearestFallback.test_no_nulls_in_output: After add_region_columns, county column has zero null values even when some points fall outside polygons
    - TestInatIntegration.test_main_output_has_county_ecoregion: Calling main() in a test context (with mocked fetch and boundary GDFs) produces samples.parquet with 'county' and 'ecoregion_l3' columns present and non-null
    - TestGeoJSONGeneration.test_ecoregion_geojson_has_na_l3name_property: Generated ecoregion GeoJSON features have 'NA_L3NAME' property
    - TestGeoJSONGeneration.test_county_geojson_has_name_property: Generated county GeoJSON features have 'NAME' property
    - TestGeoJSONGeneration.test_geojson_files_under_400kb: Generated files are under 400,000 bytes
  </behavior>
  <action>
    Create data/tests/test_spatial.py. All tests must import from modules that do not yet exist (data/spatial.py, data/scripts/build_geojson.py), so they fail with ImportError or AttributeError at collection time — that counts as RED.

    Structure the file with these four classes:

    **TestAddRegionColumns** — Unit tests using synthetic GeoDataFrames. Create minimal mock GDFs using shapely Polygon geometries (no real shapefiles). Use geopandas.GeoDataFrame with a single polygon covering a known lat/lon point. Verify county column value matches the mock GDF NAME field.

    **TestNearestFallback** — Use a point that is clearly outside all mock polygons (e.g., 0.5 degrees outside the polygon edge). Verify add_region_columns still returns a non-null county for that point.

    **TestInatIntegration** — Patch inat.download.main() to avoid real network calls. After calling main(), read the written samples.parquet and assert 'county' and 'ecoregion_l3' columns exist with no null values. Use tmp_path fixture for the parquet output. Mock the boundary loading to return synthetic GDFs.

    **TestGeoJSONGeneration** — Call build_county_geojson() and build_ecoregion_geojson() with tmp_path output dirs (do not run downloads in tests — mock the load step). Assert the output file has the expected top-level property names and is under 400,000 bytes.

    Import path for spatial module: `from spatial import add_region_columns`
    Import path for build-geojson: `from scripts.build_geojson import build_county_geojson, build_ecoregion_geojson` (note: file is build-geojson.py so import as build_geojson)

    Include `# type: ignore` comments where needed to silence linters on missing modules.
  </action>
  <verify>
    <automated>cd /Users/rainhead/dev/beeatlas/data && uv run pytest tests/test_spatial.py --co -q 2>&1 | tail -20</automated>
  </verify>
  <done>
    pytest collects all four test classes (TestAddRegionColumns, TestNearestFallback, TestInatIntegration, TestGeoJSONGeneration) and reports failures or collection errors due to missing modules — not zero tests collected. Running the tests produces failures, not passes.
  </done>
</task>

</tasks>

<verification>
Run: cd /Users/rainhead/dev/beeatlas/data && uv run pytest tests/test_spatial.py --co -q
Expected: Four test classes collected, all tests FAIL (RED state confirmed).
</verification>

<success_criteria>
data/tests/test_spatial.py exists with four test classes. pytest collects at least 8 tests total. All tests fail — either ImportError on missing modules or assertion failures. No tests pass (that would indicate stale implementation already exists).
</success_criteria>

<output>
After completion, create .planning/phases/16-pipeline-spatial-join/16-01-SUMMARY.md
</output>
