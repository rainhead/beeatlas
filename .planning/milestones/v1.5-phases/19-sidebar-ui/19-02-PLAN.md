---
phase: 19-sidebar-ui
plan: 02
type: execute
wave: 2
depends_on:
  - "19-01"
files_modified: []
autonomous: false
requirements:
  - FILTER-03
  - FILTER-04
  - FILTER-06

must_haves:
  truths:
    - "Boundary toggle in sidebar controls region layer visibility on map"
    - "Selecting a county from autocomplete adds a chip and filters map points"
    - "Selecting an ecoregion from autocomplete adds a chip with type label"
    - "Removing a chip updates the map immediately"
    - "Clear filters removes all chips, resets boundary toggle to Off, and resets taxon/date/month filters"
    - "Floating boundary toggle is gone from the map"
  artifacts: []
  key_links: []
---

<objective>
Verify the complete sidebar region UI works end-to-end in the browser.

Purpose: Manual verification that all three requirements (FILTER-03, FILTER-04, FILTER-06) work correctly before marking the phase complete.
Output: Verified working sidebar with all region filter controls.
</objective>

<execution_context>
@/Users/rainhead/.claude/get-shit-done/workflows/execute-plan.md
@/Users/rainhead/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/19-sidebar-ui/19-01-SUMMARY.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Complete sidebar region filter UI: boundary mode toggle (Off/Counties/Ecoregions), county and ecoregion multi-select autocomplete with removable chips, type badges, extended Clear filters, and floating toggle removal from map.</what-built>
  <how-to-verify>
    1. Start dev server: `cd frontend && npm run dev`
    2. Open http://localhost:5173 in browser

    **Floating toggle removed:**
    3. Confirm there is NO Off/Counties/Ecoregions button group floating in the top-right corner of the map

    **Boundary toggle in sidebar:**
    4. At the top of the sidebar, confirm a three-button toggle: Off | Counties | Ecoregions
    5. Click "Counties" — county boundary polygons appear on the map; "Counties" button shows green active state
    6. Click "Ecoregions" — ecoregion boundaries appear instead; "Ecoregions" button is active
    7. Click "Off" — boundaries disappear

    **County autocomplete (FILTER-03):**
    8. Below the layer toggle and filter controls, find "Filter by county..." input
    9. Type "Ki" — native datalist dropdown shows "King" (and possibly "Kitsap", "Kittitas")
    10. Select "King" — a chip labeled "King" appears below the input; input clears
    11. Confirm map points are filtered to King County only
    12. Add "Pierce" — second chip appears; map shows King OR Pierce specimens

    **Ecoregion autocomplete (FILTER-04):**
    13. Find "Filter by ecoregion..." input
    14. Type "Cas" — datalist shows "Cascades"
    15. Select "Cascades" — a chip appears; since both county and ecoregion chips exist, each chip shows a type badge: [county] King, [county] Pierce, [ecoregion] Cascades
    16. Confirm map shows only specimens in (King OR Pierce) AND Cascades

    **Chip removal:**
    17. Click the X on the "King" chip — it disappears; map updates to Pierce AND Cascades
    18. Click the X on the "Pierce" chip — it disappears; only ecoregion badge remains, so type badges may disappear (only one type active)

    **Clear filters (FILTER-06):**
    19. Add a county chip and an ecoregion chip
    20. Set boundary toggle to "Counties"
    21. Click "Clear filters"
    22. Confirm: all chips gone, boundary toggle shows "Off", map shows all specimens, map position unchanged

    **URL round-trip:**
    23. Add a county filter, set boundary mode to Counties
    24. Copy the URL (should contain counties= and bm= params)
    25. Open in new tab — same boundary mode and county filter restored

    **Sample mode:**
    26. Switch to Samples layer — confirm region controls (county/ecoregion inputs) still visible
    27. Add a county filter in sample mode — confirm sample dots are filtered
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues to fix</resume-signal>
</task>

</tasks>

<verification>
Human verification of all FILTER-03, FILTER-04, FILTER-06 success criteria from ROADMAP.md.
</verification>

<success_criteria>
- All 4 phase success criteria from ROADMAP pass manual verification
- No visual regressions in existing sidebar functionality
</success_criteria>

<output>
After completion, create `.planning/phases/19-sidebar-ui/19-02-SUMMARY.md`
</output>
