---
phase: 123-dbt-layer-occurrence-synonymy
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - data/dbt/seeds/occurrence_synonyms.csv
  - data/dbt/seeds/schema.yml
  - data/dbt/dbt_project.yml
  - data/canonical_name.py
  - data/inat_obs_pipeline.py
  - data/checklist_pipeline.py
  - data/occurrence_synonyms.csv
autonomous: true
requirements:
  - SYN-01

must_haves:
  truths:
    - "occurrence_synonyms.csv lives in data/dbt/seeds/ and is the single source of truth"
    - "Running checklist_pipeline.load_checklist() stores normalize_scientific_name() output (NOT synonymized) in ecdysis_data.occurrences.canonical_name"
    - "Running inat_obs_pipeline.load_inat_obs() stores normalize_scientific_name() output (NOT synonymized) in inat_obs_data.observations.canonical_name"
    - "canonical_name.apply_synonym() still exists as a Python utility and its unit tests pass"
    - "test_canonical_name.py + test_checklist_pipeline.py + test_checklist_reconcile.py all pass"
  artifacts:
    - path: "data/dbt/seeds/occurrence_synonyms.csv"
      provides: "dbt seed source — synonym → accepted_name mapping"
      contains: "agapostemon texanus,agapostemon subtilior"
    - path: "data/dbt/seeds/schema.yml"
      provides: "dbt seed schema tests (not_null, unique on synonym)"
    - path: "data/dbt/dbt_project.yml"
      provides: "seeds: section with +column_types for occurrence_synonyms"
      contains: "seeds:"
    - path: "data/canonical_name.py"
      provides: "OCCURRENCE_SYNONYMS_PATH pointing at data/dbt/seeds/occurrence_synonyms.csv"
      contains: "dbt/seeds/occurrence_synonyms.csv"
    - path: "data/inat_obs_pipeline.py"
      provides: "load_inat_obs writing normalize_scientific_name() (no apply_synonym wrap)"
    - path: "data/checklist_pipeline.py"
      provides: "_update_occurrences_canonical_name writing normalize_scientific_name() (no apply_synonym wrap)"
  key_links:
    - from: "data/canonical_name.py"
      to: "data/dbt/seeds/occurrence_synonyms.csv"
      via: "OCCURRENCE_SYNONYMS_PATH constant"
      pattern: "dbt/seeds/occurrence_synonyms.csv"
    - from: "data/inat_obs_pipeline.py:load_inat_obs"
      to: "canonical_name.normalize_scientific_name"
      via: "direct call (no apply_synonym wrapper)"
      pattern: "normalize_scientific_name\\(sci_name\\)"
    - from: "data/checklist_pipeline.py:_update_occurrences_canonical_name"
      to: "canonical_name.normalize_scientific_name"
      via: "direct call in mapping list comprehension"
      pattern: "normalize_scientific_name\\(r\\[0\\]\\)"
---

<objective>
Move occurrence_synonyms.csv to the dbt seeds directory and stop applying synonyms at Python ingest time. After this plan: raw `canonical_name` columns in ecdysis_data.occurrences and inat_obs_data.observations store only the output of `normalize_scientific_name()`. The `apply_synonym()` Python function itself remains in `canonical_name.py` (still used by unit tests and available as a utility); only its callsites inside the two ingest pipelines are removed.

Purpose: Eliminates "synonym applied at ingest" so adding a new entry to occurrence_synonyms.csv can take effect via a dbt rebuild alone (no re-ingestion). Also relocates the canonical CSV into dbt's seeds tree so Plan 02 can reference it via `{{ ref('occurrence_synonyms') }}`.

Output: Seed CSV in dbt tree, schema.yml with not_null/unique tests, dbt_project.yml seeds config, updated path constant, removed callsites, all existing pytests green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/123-dbt-layer-occurrence-synonymy/123-RESEARCH.md
@.planning/phases/123-dbt-layer-occurrence-synonymy/123-VALIDATION.md
@CLAUDE.md

<interfaces>
<!-- Existing Python utilities — Plan 01 must preserve their signatures -->

From data/canonical_name.py:
- `normalize_scientific_name(name: str | None) -> str | None` — D-04 5-step canonicalization; idempotent
- `apply_synonym(name: str | None) -> str | None` — maps canonical synonym to accepted; returns input if unknown; reads from OCCURRENCE_SYNONYMS_PATH on first call
- `OCCURRENCE_SYNONYMS_PATH: Path` — module-level constant; currently points at data/occurrence_synonyms.csv
- `_SYNONYMS: dict[str, str] | None` — module-level cache; tests monkeypatch this

Test that exercises path resolution (must keep passing after path constant change):
- `data/tests/test_canonical_name.py::test_apply_synonym_loads_agapostemon_from_csv`
  — sets `_SYNONYMS = None` to force re-read from disk; asserts `apply_synonym("agapostemon texanus") == "agapostemon subtilior"`
  — file at the new path MUST contain the texanus → subtilior row for this test to keep passing
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create dbt seed (CSV + schema.yml + dbt_project.yml config); update OCCURRENCE_SYNONYMS_PATH</name>
  <files>data/dbt/seeds/occurrence_synonyms.csv, data/dbt/seeds/schema.yml, data/dbt/dbt_project.yml, data/canonical_name.py, data/occurrence_synonyms.csv</files>
  <read_first>
    - data/occurrence_synonyms.csv
    - data/canonical_name.py
    - data/dbt/dbt_project.yml
    - data/tests/test_canonical_name.py
    - .planning/phases/123-dbt-layer-occurrence-synonymy/123-RESEARCH.md (sections: Pattern 1 "dbt Seed Definition", Code Examples "Seed Schema File")
  </read_first>
  <action>
    Move the synonyms CSV into the dbt seeds tree as a first-class dbt seed.

    1. Create directory `data/dbt/seeds/`.
    2. Create `data/dbt/seeds/occurrence_synonyms.csv` with header `synonym,accepted_name,source` and one data row:
       `agapostemon texanus,agapostemon subtilior,Portman et al. 2024`
       (exact byte-for-byte copy of current `data/occurrence_synonyms.csv` content).
    3. Delete the old file `data/occurrence_synonyms.csv` (`git rm`). The seed CSV is the single source of truth after this plan.
    4. Create `data/dbt/seeds/schema.yml` with dbt schema-version 2 declaring a `seeds:` entry named `occurrence_synonyms` with columns `synonym` (data_tests: not_null + unique), `accepted_name` (data_tests: not_null), and `source` (description: "Citation for the synonymy decision"). Match the template in 123-RESEARCH.md "Seed Schema File".
    5. Edit `data/dbt/dbt_project.yml`: add a top-level `seeds:` block at the end (sibling of the existing `models:` block) with:
       ```
       seeds:
         beeatlas:
           occurrence_synonyms:
             +column_types:
               synonym: varchar
               accepted_name: varchar
               source: varchar
       ```
       This guards against dbt-duckdb 1.10.1 type-inference quirks (research open question 2).
    6. Edit `data/canonical_name.py`: change the `OCCURRENCE_SYNONYMS_PATH` constant from
       `Path(__file__).parent / "occurrence_synonyms.csv"` to
       `Path(__file__).parent / "dbt" / "seeds" / "occurrence_synonyms.csv"`.
       Do not change the rest of the module — `apply_synonym()` and `_ensure_synonyms()` keep their current shape.

    Critical: the test `test_apply_synonym_loads_agapostemon_from_csv` in `data/tests/test_canonical_name.py` reads from `OCCURRENCE_SYNONYMS_PATH` after setting `_SYNONYMS = None`. After this task the file at the NEW path must contain the texanus → subtilior row, or that test will fail.

    Do NOT remove `apply_synonym()` from `canonical_name.py` — it is still called by the Python unit tests and remains as a public utility (per 123-RESEARCH Pitfall 3 and "State of the Art" deprecation note).
  </action>
  <verify>
    <automated>cd data && uv run pytest tests/test_canonical_name.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `data/dbt/seeds/occurrence_synonyms.csv` exists and contains the exact text `agapostemon texanus,agapostemon subtilior,Portman et al. 2024` (verify with `grep -c 'agapostemon texanus' data/dbt/seeds/occurrence_synonyms.csv` returning `1`)
    - `data/occurrence_synonyms.csv` no longer exists (verify with `test ! -f data/occurrence_synonyms.csv`)
    - `data/dbt/seeds/schema.yml` exists and contains a `seeds:` entry with `name: occurrence_synonyms` (verify with `grep -E '^\s+- name: occurrence_synonyms' data/dbt/seeds/schema.yml` returning a match)
    - `data/dbt/dbt_project.yml` contains a top-level `seeds:` key (verify with `grep -E '^seeds:' data/dbt/dbt_project.yml` returning exactly 1 match)
    - `data/canonical_name.py` line containing `OCCURRENCE_SYNONYMS_PATH` references `dbt` and `seeds` (verify with `grep -E 'OCCURRENCE_SYNONYMS_PATH.*dbt.*seeds' data/canonical_name.py` returning a match)
    - `apply_synonym` function definition still present in `data/canonical_name.py` (verify with `grep -c '^def apply_synonym' data/canonical_name.py` returning `1`)
    - `cd data && uv run pytest tests/test_canonical_name.py -q` passes all tests (including `test_apply_synonym_loads_agapostemon_from_csv` which re-reads from disk)
  </acceptance_criteria>
  <done>
    Seed CSV lives at data/dbt/seeds/occurrence_synonyms.csv with proper schema.yml; dbt_project.yml has seeds config; canonical_name.py points at new path; all canonical_name unit tests green including the disk-reading integration test.
  </done>
</task>

<task type="auto">
  <name>Task 2: Remove apply_synonym call from inat_obs_pipeline.load_inat_obs</name>
  <files>data/inat_obs_pipeline.py</files>
  <read_first>
    - data/inat_obs_pipeline.py
    - .planning/phases/123-dbt-layer-occurrence-synonymy/123-RESEARCH.md (section: Pattern 3 "Update inat_obs_pipeline.py")
  </read_first>
  <action>
    Edit `data/inat_obs_pipeline.py`:

    1. In the `rows.append((...))` block inside `load_inat_obs()` (currently around line 68), change the canonical_name field from
       `apply_synonym(normalize_scientific_name(sci_name)),`
       to
       `normalize_scientific_name(sci_name),`
       (drop the `apply_synonym(...)` wrapper only; preserve the trailing comma and surrounding tuple structure).
    2. Update the import on line 16 from
       `from canonical_name import apply_synonym, normalize_scientific_name`
       to
       `from canonical_name import normalize_scientific_name`
       (remove `apply_synonym` from the import — it is no longer used in this file).
    3. Do NOT modify any other behavior. The `scientific_name` raw column, dedup, schema, and print statements remain unchanged. The non-null invariant tested by PIPE-02 (`test_canonical_name_non_null`) continues to hold because `normalize_scientific_name` returns non-null for non-null non-empty input (per 123-RESEARCH Pitfall 4).
  </action>
  <verify>
    <automated>cd data && uv run pytest tests/test_canonical_name.py tests/test_inat_obs_pipeline.py -q && ! grep -nE 'apply_synonym' inat_obs_pipeline.py</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'apply_synonym' data/inat_obs_pipeline.py` returns `0` (no callsites OR imports left)
    - `grep -c 'normalize_scientific_name(sci_name)' data/inat_obs_pipeline.py` returns at least `1`
    - `grep -E '^from canonical_name import' data/inat_obs_pipeline.py` shows `normalize_scientific_name` only (no `apply_synonym` in the import list)
    - `cd data && uv run pytest tests/test_inat_obs_pipeline.py -q` passes (PIPE-01..04 including non-null invariant remain green)
    - `cd data && uv run pytest tests/test_canonical_name.py -q` still passes (apply_synonym Python tests untouched)
  </acceptance_criteria>
  <done>
    inat_obs_pipeline.load_inat_obs writes raw normalize_scientific_name() output only; apply_synonym import removed; test_inat_obs_pipeline.py green.
  </done>
</task>

<task type="auto">
  <name>Task 3: Remove apply_synonym call from checklist_pipeline._update_occurrences_canonical_name</name>
  <files>data/checklist_pipeline.py</files>
  <read_first>
    - data/checklist_pipeline.py
    - data/tests/test_checklist_reconcile.py
    - data/tests/test_checklist_pipeline.py
    - .planning/phases/123-dbt-layer-occurrence-synonymy/123-RESEARCH.md (section: Pattern 4 "Update _update_occurrences_canonical_name in checklist_pipeline.py")
  </read_first>
  <action>
    Edit `data/checklist_pipeline.py`:

    1. In `_update_occurrences_canonical_name()` (currently around line 49), change the mapping list comprehension from
       `mapping: list[tuple[str | None, str]] = [`
       `    (apply_synonym(normalize_scientific_name(r[0])), r[0]) for r in rows`
       `]`
       to
       `mapping: list[tuple[str | None, str]] = [`
       `    (normalize_scientific_name(r[0]), r[0]) for r in rows`
       `]`
       (drop the `apply_synonym(...)` wrapper only).
    2. Update the import on line 20 from
       `from canonical_name import apply_synonym, normalize_scientific_name`
       to
       `from canonical_name import normalize_scientific_name`
       (remove `apply_synonym` — no other callsite remains in this file; `reconcile()` uses the separate `SYNONYMS_PATH`/`checklist_synonyms.csv` mechanism, not `apply_synonym`).
    3. Do NOT modify `reconcile()`, `load_checklist()`, `_load_checklist_records()`, or any DDL. The checklist-side synonym mechanism (checklist_synonyms.csv) is independent of occurrence_synonyms.csv per 123-RESEARCH Pitfall 5 / Pitfall 7.

    Note: the pre-existing pytest `test_update_occurrences_adds_canonical_name_column` (in test_checklist_reconcile.py) asserts that
    `Lasioglossum (Dialictus) zonulum` maps to `lasioglossum zonulum` and `Andrena fulva (Müller, 1766)` maps to `andrena fulva` — both are `normalize_scientific_name()` outputs untouched by `apply_synonym()`, so the assertion continues to hold without modification.
  </action>
  <verify>
    <automated>cd data && uv run pytest tests/test_checklist_pipeline.py tests/test_checklist_reconcile.py -q && ! grep -nE 'apply_synonym' checklist_pipeline.py</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'apply_synonym' data/checklist_pipeline.py` returns `0` (no callsites OR imports remain)
    - `grep -c 'normalize_scientific_name(r\[0\])' data/checklist_pipeline.py` returns at least `1` (the mapping comprehension uses bare normalize)
    - `grep -E '^from canonical_name import' data/checklist_pipeline.py` shows `normalize_scientific_name` only
    - `cd data && uv run pytest tests/test_checklist_pipeline.py tests/test_checklist_reconcile.py -q` passes — including `test_update_occurrences_adds_canonical_name_column` and the reconcile fixtures
    - `cd data && uv run pytest tests/test_canonical_name.py -q` still passes
  </acceptance_criteria>
  <done>
    checklist_pipeline._update_occurrences_canonical_name writes raw normalize_scientific_name() output only; apply_synonym import removed; test_checklist_pipeline.py and test_checklist_reconcile.py green.
  </done>
</task>

</tasks>

<verification>
After all three tasks complete, run the quick suite to confirm no regression in Python ingest:

```bash
cd data && uv run pytest tests/test_canonical_name.py tests/test_checklist_pipeline.py tests/test_checklist_reconcile.py tests/test_inat_obs_pipeline.py -q
```

All four test modules must be green. Additionally:

- `grep -rn 'apply_synonym' data/inat_obs_pipeline.py data/checklist_pipeline.py` returns no matches (callsites and imports both removed in the two ingest pipelines)
- `apply_synonym` function definition is still present in `data/canonical_name.py` (preserved for unit tests and as a public utility)
- The seed CSV at `data/dbt/seeds/occurrence_synonyms.csv` is in place and Plan 02 can now reference it via `{{ ref('occurrence_synonyms') }}`
</verification>

<success_criteria>
- `data/dbt/seeds/occurrence_synonyms.csv` exists with the texanus → subtilior mapping
- `data/dbt/seeds/schema.yml` declares the seed with not_null + unique tests on `synonym` and not_null on `accepted_name`
- `data/dbt/dbt_project.yml` has a `seeds:` section configuring `occurrence_synonyms` column types
- `data/occurrence_synonyms.csv` no longer exists (replaced by the seed)
- `data/canonical_name.py` `OCCURRENCE_SYNONYMS_PATH` points at the new seed location
- `apply_synonym()` function definition remains in `canonical_name.py`
- `apply_synonym` import and callsite removed from `data/inat_obs_pipeline.py`
- `apply_synonym` import and callsite removed from `data/checklist_pipeline.py`
- All Python tests pass: `cd data && uv run pytest tests/test_canonical_name.py tests/test_checklist_pipeline.py tests/test_checklist_reconcile.py tests/test_inat_obs_pipeline.py -q`
</success_criteria>

<output>
Create `.planning/phases/123-dbt-layer-occurrence-synonymy/123-01-SUMMARY.md` when done.
</output>
