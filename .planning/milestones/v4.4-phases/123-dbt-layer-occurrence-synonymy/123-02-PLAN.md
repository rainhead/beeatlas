---
phase: 123-dbt-layer-occurrence-synonymy
plan: 02
type: execute
wave: 2
depends_on:
  - 123-01
files_modified:
  - data/dbt/models/intermediate/int_combined.sql
  - data/dbt/models/intermediate/int_species_universe.sql
  - data/tests/test_dbt_synonymy.py
autonomous: true
requirements:
  - SYN-02
  - SYN-03

must_haves:
  truths:
    - "After `bash data/dbt/run.sh build`, occurrences.parquet contains the canonical_name `agapostemon subtilior` for rows whose raw scientific name is Agapostemon texanus"
    - "After `bash data/dbt/run.sh build`, occurrences.parquet contains NO rows with canonical_name `agapostemon texanus` (every texanus is rewritten to subtilior via the LEFT JOIN)"
    - "ARM 2 (provisional WABA) rows in int_combined retain canonical_name = NULL (synonym JOIN on NULL produces NULL, which is correct)"
    - "int_species_universe.inat_obs_count is keyed by the SYNONYMIZED canonical_name (texanus inat_obs rows are counted under subtilior)"
    - "Adding a new line to data/dbt/seeds/occurrence_synonyms.csv and re-running `bash data/dbt/run.sh build` propagates the new mapping into occurrences.parquet without re-running any Python ingest"
    - "All existing tests remain green: `cd data && uv run pytest -q` and `npm test`"
  artifacts:
    - path: "data/dbt/models/intermediate/int_combined.sql"
      provides: "Synonyms CTE + LEFT JOIN on ARM 1 (ecdysis) and ARM 3 (inat_obs); COALESCE(syn.accepted_name, raw.canonical_name) replaces direct canonical_name reference"
      contains: "ref('occurrence_synonyms')"
    - path: "data/dbt/models/intermediate/int_species_universe.sql"
      provides: "inat_obs_count_agg CTE applies synonym JOIN so counts roll up under synonymized canonical_name"
      contains: "ref('occurrence_synonyms')"
    - path: "data/tests/test_dbt_synonymy.py"
      provides: "Pytest integration test asserting Agapostemon texanus → subtilior mapping in occurrences.parquet"
      contains: "agapostemon subtilior"
  key_links:
    - from: "data/dbt/models/intermediate/int_combined.sql"
      to: "data/dbt/seeds/occurrence_synonyms.csv"
      via: "{{ ref('occurrence_synonyms') }}"
      pattern: "ref\\(['\\\"]occurrence_synonyms['\\\"]\\)"
    - from: "data/dbt/models/intermediate/int_species_universe.sql:inat_obs_count_agg"
      to: "data/dbt/seeds/occurrence_synonyms.csv"
      via: "LEFT JOIN ref('occurrence_synonyms') ON synonym = io.canonical_name"
      pattern: "ref\\(['\\\"]occurrence_synonyms['\\\"]\\)"
    - from: "data/tests/test_dbt_synonymy.py"
      to: "public/data/occurrences.parquet OR data/dbt/target/sandbox/occurrences.parquet"
      via: "duckdb.read_parquet + canonical_name filter"
      pattern: "agapostemon (texanus|subtilior)"
---

<objective>
Apply occurrence synonymy uniformly across all data sources at dbt build time by joining the `occurrence_synonyms` seed (created in Plan 01) into `int_combined.sql` for ARM 1 (ecdysis) and ARM 3 (iNat expert observations), and fix the latent bug in `int_species_universe.inat_obs_count_agg` where iNat counts bypass the synonym JOIN. Add a pytest integration test that runs after `dbt build` and asserts the texanus → subtilior mapping is materialized in `occurrences.parquet`.

Purpose: This closes the end-to-end synonymy mechanism — Plan 01 made the Python ingest layer "raw", and this plan makes the dbt layer "synonym-aware" via a single seed reference. After this plan ships, adding a row to `data/dbt/seeds/occurrence_synonyms.csv` and running `bash data/dbt/run.sh build` is the entire workflow for a new synonym.

Output: Updated `int_combined.sql` with synonym CTE + JOINs on ARM 1 and ARM 3; updated `int_species_universe.sql` with synonym JOIN in `inat_obs_count_agg`; new pytest `test_dbt_synonymy.py`; `dbt build` succeeds; full pytest suite green; `npm test` green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/123-dbt-layer-occurrence-synonymy/123-RESEARCH.md
@.planning/phases/123-dbt-layer-occurrence-synonymy/123-VALIDATION.md
@.planning/phases/123-dbt-layer-occurrence-synonymy/123-01-SUMMARY.md
@CLAUDE.md

<interfaces>
<!-- Existing dbt model contracts Plan 02 must preserve -->

From data/dbt/models/intermediate/int_combined.sql (UNION ALL of 3 arms):
- 30-column row contract (occurrences mart enforces via dbt contract — DO NOT change column order, names, or types)
- ARM 1: reads `int_ecdysis_base` aliased `e` — exposes `e.canonical_name` (currently un-synonymized after Plan 01)
- ARM 2: WABA provisional — hardcodes `NULL AS canonical_name` (no scientific name available; leave NULL — per 123-RESEARCH Pitfall 1)
- ARM 3: reads `source('inat_obs_data', 'observations')` aliased `io` — exposes `io.canonical_name` (currently un-synonymized after Plan 01)
- Materialized as TABLE (config at top): `{{ config(materialized='table') }}`

From data/dbt/models/intermediate/int_species_universe.sql:
- `inat_obs_count_agg` CTE currently reads `{{ source('inat_obs_data', 'observations') }}` directly and groups by raw `canonical_name`
- After Plan 01 this would undercount synonymized species (any inat_obs rows with `agapostemon texanus` would be counted under texanus instead of subtilior)
- Fix: apply the same synonym JOIN in this CTE so the grouped key is the synonymized canonical_name

From data/dbt/seeds/occurrence_synonyms.csv (created in Plan 01):
- columns: `synonym, accepted_name, source`
- accessible as `{{ ref('occurrence_synonyms') }}` in any dbt model after the seed loads

From data/dbt/run.sh:
- `bash data/dbt/run.sh build` runs `dbt build` which executes `dbt seed` (loads CSVs) → `dbt run` (models) → `dbt test` (schema tests)
- Outputs land in `data/dbt/target/sandbox/` for the occurrences mart parquet
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add pytest integration test for texanus → subtilior in occurrences.parquet (Wave 0 RED)</name>
  <files>data/tests/test_dbt_synonymy.py</files>
  <read_first>
    - data/tests/test_dbt_diff.py
    - .planning/phases/123-dbt-layer-occurrence-synonymy/123-VALIDATION.md
    - .planning/phases/123-dbt-layer-occurrence-synonymy/123-RESEARCH.md (section: Wave 0 Gaps)
  </read_first>
  <behavior>
    - Test 1: `test_occurrences_has_agapostemon_subtilior` — opens `data/dbt/target/sandbox/occurrences.parquet`, asserts at least 1 row exists with `canonical_name = 'agapostemon subtilior'` (proves the synonym JOIN fired and rewrote at least one record).
    - Test 2: `test_occurrences_has_no_agapostemon_texanus` — opens the same parquet, asserts ZERO rows have `canonical_name = 'agapostemon texanus'` (proves every texanus was rewritten — no synonym-application gap remains).
    - Test 3: `test_inat_obs_count_uses_synonymized_canonical_name` — opens `data/dbt/target/sandbox/species.parquet` (the species mart, which carries `inat_obs_count`), asserts that no row has `canonical_name = 'agapostemon texanus'` and that the row for `canonical_name = 'agapostemon subtilior'` has a non-negative `inat_obs_count` value (sanity: the column exists and is queryable on the synonymized key).
    - All tests skip via `pytest.mark.skipif` when `data/dbt/target/sandbox/occurrences.parquet` does NOT exist (so the suite stays green for contributors who have not run `dbt build`). Mirror the `_SANDBOX_GUARD` pattern from `data/tests/test_dbt_diff.py`.
  </behavior>
  <action>
    Create `data/tests/test_dbt_synonymy.py` following the pattern of `data/tests/test_dbt_diff.py`:

    1. Module docstring stating this verifies SYN-02 / SYN-03 acceptance — Agapostemon texanus → subtilior mapping appears correctly in `occurrences.parquet` after `bash data/dbt/run.sh build`.
    2. Imports: `from pathlib import Path`, `import duckdb`, `import pytest`.
    3. Constants: `SANDBOX = Path(__file__).resolve().parent.parent / "dbt" / "target" / "sandbox"`.
    4. `_SANDBOX_GUARD = pytest.mark.skipif(not (SANDBOX / "occurrences.parquet").exists(), reason="run `bash data/dbt/run.sh build` first to produce sandbox outputs")`.
    5. Three test functions described in `<behavior>` above, each decorated with `@_SANDBOX_GUARD`. Use `duckdb.connect(":memory:")` and query the parquet directly via `SELECT COUNT(*) FROM read_parquet('{SANDBOX}/occurrences.parquet') WHERE canonical_name = 'agapostemon subtilior'` etc.
    6. For Test 3, guard separately: `@pytest.mark.skipif(not (SANDBOX / "species.parquet").exists(), ...)`.

    Write the tests so they RED before Task 2/3 (no JOIN yet → Test 2 will fail because raw `agapostemon texanus` rows still exist in occurrences.parquet after Plan 01's removal of Python-side apply_synonym). The next two tasks turn them GREEN.

    Do not put a behavior assertion that requires running dbt build inside the test — running dbt is the executor's responsibility before invoking pytest (see verify command).
  </action>
  <verify>
    <automated>cd data && uv run pytest tests/test_dbt_synonymy.py --collect-only -q</automated>
  </verify>
  <acceptance_criteria>
    - File `data/tests/test_dbt_synonymy.py` exists
    - `grep -c '^def test_' data/tests/test_dbt_synonymy.py` returns at least `3`
    - `grep -c '_SANDBOX_GUARD' data/tests/test_dbt_synonymy.py` returns at least `4` (constant + 3 decorations, or more)
    - `cd data && uv run pytest tests/test_dbt_synonymy.py --collect-only -q` reports at least 3 collected test items with no collection errors
    - `grep -c 'agapostemon subtilior' data/tests/test_dbt_synonymy.py` returns at least `1`
    - `grep -c 'agapostemon texanus' data/tests/test_dbt_synonymy.py` returns at least `1`
  </acceptance_criteria>
  <done>
    Pytest module exists with three skipif-guarded behavior tests asserting the texanus → subtilior mapping in occurrences.parquet and species.parquet. Tests collect cleanly. They will RED until Tasks 2 and 3 add the dbt JOIN and the executor runs `bash data/dbt/run.sh build`.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add synonyms CTE + LEFT JOIN to int_combined.sql (ARM 1 + ARM 3)</name>
  <files>data/dbt/models/intermediate/int_combined.sql</files>
  <read_first>
    - data/dbt/models/intermediate/int_combined.sql
    - data/dbt/seeds/occurrence_synonyms.csv
    - .planning/phases/123-dbt-layer-occurrence-synonymy/123-RESEARCH.md (sections: Pattern 2 "LEFT JOIN Synonym in int_combined", Code Examples "int_combined.sql Synonym Join", Pitfall 1 "WABA ARM 2 Already Has NULL canonical_name")
  </read_first>
  <action>
    Edit `data/dbt/models/intermediate/int_combined.sql` to apply the synonym JOIN on ARM 1 (ecdysis) and ARM 3 (inat_obs). ARM 2 (provisional WABA) keeps `NULL AS canonical_name` — do not touch it.

    1. Before the first `SELECT` (after the `{{ config(materialized='table') }}` line and the existing leading comments), introduce a leading CTE that materializes the synonyms reference once for reuse across all arms:
       ```sql
       WITH synonyms AS (
           SELECT synonym, accepted_name FROM {{ ref('occurrence_synonyms') }}
       )
       ```
       Restructure the file so each arm becomes a CTE/SELECT that can reference `synonyms`. Concretely: convert the existing top-level `SELECT ... FROM int_ecdysis_base e ...` (ARM 1), the existing `SELECT ... FROM int_provisional_waba_ids p ...` (ARM 2), and the existing `SELECT ... FROM source('inat_obs_data', 'observations') io ...` (ARM 3) into the body of a final `SELECT * FROM (arm1 UNION ALL arm2 UNION ALL arm3)` — OR simpler: keep the UNION ALL structure and inline a per-arm subquery against `{{ ref('occurrence_synonyms') }}` on each of ARM 1 and ARM 3. Choose whichever produces the smallest diff while keeping the 30-column contract intact.

    2. In ARM 1: change the `e.canonical_name,` line (currently `e.canonical_name,` around line 40) to
       `COALESCE(syn_e.accepted_name, e.canonical_name) AS canonical_name,`
       and add to the bottom of ARM 1's FROM clause (after the existing LEFT JOIN on `int_specimen_obs_base`):
       `LEFT JOIN {{ ref('occurrence_synonyms') }} syn_e ON syn_e.synonym = e.canonical_name`

    3. In ARM 2: leave `NULL AS canonical_name` exactly as-is. Do NOT add a synonym JOIN here — ARM 2 has no determined scientific name (per 123-RESEARCH Pitfall 1).

    4. In ARM 3: change the `io.canonical_name,` line (currently `io.canonical_name,` around line 132) to
       `COALESCE(syn_io.accepted_name, io.canonical_name) AS canonical_name,`
       and add to ARM 3's FROM clause (after `FROM {{ source('inat_obs_data', 'observations') }} io`):
       `LEFT JOIN {{ ref('occurrence_synonyms') }} syn_io ON syn_io.synonym = io.canonical_name`

    5. Preserve the existing 30-column row contract — `canonical_name` must remain the same column position, name, and VARCHAR type (COALESCE of two VARCHAR columns returns VARCHAR). All other columns unchanged.

    6. Update the file's leading comment block to note the synonym JOIN: change the existing "Mirrors export.py:135-197 (combined CTE)" line into a two-line block referencing Phase 123 and the seed:
       `-- Phase 123 (SYN-02): synonymy applied via LEFT JOIN on ref('occurrence_synonyms')`
       `-- for ARM 1 (ecdysis) and ARM 3 (inat_obs); ARM 2 (provisional WABA) has`
       `-- NULL canonical_name (no scientific name) and is not joined.`

    Note on Assumption A1 from 123-RESEARCH: if dbt-duckdb rejects the leading `WITH synonyms AS (...)` referenced across UNION ALL arms (DuckDB CTE scoping limitation), fall back to per-arm subquery references via `{{ ref('occurrence_synonyms') }}` directly in each LEFT JOIN — same semantics, slightly more verbose. The per-arm approach is what step 2 and step 4 above describe and is the safe default; the leading `synonyms AS` CTE in step 1 is optional sugar.

    Use Edit tool, not Write — preserve the rest of the file byte-for-byte.
  </action>
  <verify>
    <automated>cd data && bash dbt/run.sh build 2>&1 | tail -30 && uv run pytest tests/test_dbt_synonymy.py::test_occurrences_has_agapostemon_subtilior tests/test_dbt_synonymy.py::test_occurrences_has_no_agapostemon_texanus -q</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "ref('occurrence_synonyms')" data/dbt/models/intermediate/int_combined.sql` returns at least `2` (one for ARM 1, one for ARM 3; possibly 3 if the leading CTE is also used)
    - `grep -c 'COALESCE(syn_e.accepted_name' data/dbt/models/intermediate/int_combined.sql` returns exactly `1` (ARM 1)
    - `grep -c 'COALESCE(syn_io.accepted_name' data/dbt/models/intermediate/int_combined.sql` returns exactly `1` (ARM 3)
    - ARM 2's `NULL AS canonical_name` line still present: `grep -c 'NULL.*AS canonical_name' data/dbt/models/intermediate/int_combined.sql` returns at least `1` (the ARM 2 line)
    - `bash data/dbt/run.sh build` completes with exit code 0 (no SQL errors, no dbt contract violations on the 30-column occurrences mart)
    - After build, `cd data && uv run pytest tests/test_dbt_synonymy.py::test_occurrences_has_agapostemon_subtilior tests/test_dbt_synonymy.py::test_occurrences_has_no_agapostemon_texanus -q` passes both tests
  </acceptance_criteria>
  <done>
    int_combined.sql joins the occurrence_synonyms seed on ARM 1 and ARM 3 via COALESCE; ARM 2 untouched. `dbt build` succeeds. occurrences.parquet has agapostemon subtilior rows and zero agapostemon texanus rows.
  </done>
</task>

<task type="auto">
  <name>Task 3: Fix int_species_universe.inat_obs_count_agg to apply synonym JOIN, then run full suite</name>
  <files>data/dbt/models/intermediate/int_species_universe.sql</files>
  <read_first>
    - data/dbt/models/intermediate/int_species_universe.sql
    - .planning/phases/123-dbt-layer-occurrence-synonymy/123-RESEARCH.md (sections: Pitfall 2 "inat_obs_count_agg in int_species_universe.sql Reads Source Directly", Code Examples "int_species_universe.sql Fix for inat_obs_count_agg", Open Questions #1)
  </read_first>
  <action>
    Edit `data/dbt/models/intermediate/int_species_universe.sql` to apply synonymy in `inat_obs_count_agg`, then run the full pytest + npm test suite.

    1. Find the `inat_obs_count_agg` CTE (currently around lines 57–63):
       ```sql
       inat_obs_count_agg AS (
           SELECT canonical_name, COUNT(*) AS inat_obs_count
           FROM {{ source('inat_obs_data', 'observations') }}
           WHERE canonical_name IS NOT NULL
           GROUP BY canonical_name
       ),
       ```
       Replace it with:
       ```sql
       -- Phase 123 (SYN-02): apply occurrence synonymy here too. Reads source
       -- directly (avoids circular DAG with occurrences mart) so it must
       -- redo the same LEFT JOIN that int_combined applies to ARM 3.
       inat_obs_count_agg AS (
           SELECT
               COALESCE(syn.accepted_name, io.canonical_name) AS canonical_name,
               COUNT(*) AS inat_obs_count
           FROM {{ source('inat_obs_data', 'observations') }} io
           LEFT JOIN {{ ref('occurrence_synonyms') }} syn ON syn.synonym = io.canonical_name
           WHERE io.canonical_name IS NOT NULL
           GROUP BY 1
       ),
       ```
       This ensures the species mart's `inat_obs_count` rolls up under the synonymized canonical_name (texanus inat_obs rows counted under subtilior, matching the occurrences mart).

    2. Do NOT modify the other CTEs (`occ_agg`, `checklist_month_agg`, `checklist_count_agg`, `provisional_agg`, `geo_agg`, `species_universe`) or the final SELECT — `occ_agg` reads `int_species_occurrences_agg` which transitively reads `int_combined`, so it already sees the synonymized values from Task 2.

    3. Re-run `bash data/dbt/run.sh build` to verify the species mart rebuilds without contract violation.

    4. Run the full Python suite + the frontend suite to confirm no regression:
       - `cd data && uv run pytest -q`
       - `npm test`

    These must both be green before the plan is complete.
  </action>
  <verify>
    <automated>cd data && bash dbt/run.sh build 2>&1 | tail -10 && uv run pytest -q && (cd "$(git rev-parse --show-toplevel)" && npm test)</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "ref('occurrence_synonyms')" data/dbt/models/intermediate/int_species_universe.sql` returns exactly `1`
    - `grep -c 'inat_obs_count_agg AS' data/dbt/models/intermediate/int_species_universe.sql` returns exactly `1` (CTE renamed/preserved, not duplicated)
    - The `inat_obs_count_agg` CTE contains both `COALESCE(syn.accepted_name` and `LEFT JOIN {{ ref('occurrence_synonyms') }}` (verify with `grep -A 8 'inat_obs_count_agg AS' data/dbt/models/intermediate/int_species_universe.sql | grep -c 'COALESCE\|LEFT JOIN'` returning at least `2`)
    - `bash data/dbt/run.sh build` completes with exit code 0
    - `cd data && uv run pytest -q` exits 0 (every Python test green, including the three new test_dbt_synonymy.py tests)
    - `npm test` exits 0 (vitest suite green; per CLAUDE.md feedback "always run npm test before git push")
    - After build, opening `data/dbt/target/sandbox/species.parquet` and filtering for `canonical_name = 'agapostemon texanus'` returns 0 rows (verify via `duckdb :memory: -c "SELECT COUNT(*) FROM read_parquet('data/dbt/target/sandbox/species.parquet') WHERE canonical_name='agapostemon texanus'"` returning 0)
  </acceptance_criteria>
  <done>
    int_species_universe.inat_obs_count_agg applies the synonym JOIN; dbt build succeeds; full pytest suite green; npm test green; species.parquet contains no `agapostemon texanus` row.
  </done>
</task>

</tasks>

<verification>
End-to-end propagation check (proves SYN-03):

1. Add a throwaway test synonym to `data/dbt/seeds/occurrence_synonyms.csv` (e.g., append `bombus zzz_synthetic,bombus vosnesenskii,phase 123 test`).
2. Run `bash data/dbt/run.sh build`.
3. Query `data/dbt/target/sandbox/occurrences.parquet` — any pre-existing record whose raw canonical name was `bombus zzz_synthetic` would now appear as `bombus vosnesenskii`. (For the actual real-data test, there is no such record; the proof point is that no Python re-ingest was needed.)
4. Revert the throwaway CSV change (`git checkout data/dbt/seeds/occurrence_synonyms.csv`).

Quick suite (mandatory):
```bash
cd data && uv run pytest -q
npm test
```

Both must exit 0.
</verification>

<success_criteria>
- `int_combined.sql` joins `ref('occurrence_synonyms')` on ARM 1 (ecdysis) and ARM 3 (inat_obs) via `COALESCE(syn.accepted_name, e.canonical_name) AS canonical_name`
- ARM 2 (provisional WABA) `canonical_name` remains `NULL` — no synonym JOIN added (per 123-RESEARCH Pitfall 1)
- `int_species_universe.inat_obs_count_agg` applies the same synonym JOIN — counts roll up under synonymized canonical_name
- `data/tests/test_dbt_synonymy.py` exists with 3 skipif-guarded tests; all 3 pass after `bash data/dbt/run.sh build`
- `bash data/dbt/run.sh build` completes without contract or test failures
- `cd data && uv run pytest -q` exits 0
- `npm test` exits 0
- `occurrences.parquet` contains rows with `canonical_name = 'agapostemon subtilior'` and ZERO rows with `canonical_name = 'agapostemon texanus'`
- Phase goal achieved: adding a new entry to `data/dbt/seeds/occurrence_synonyms.csv` + `bash data/dbt/run.sh build` propagates to all artifacts with no Python re-ingest
</success_criteria>

<output>
Create `.planning/phases/123-dbt-layer-occurrence-synonymy/123-02-SUMMARY.md` when done.
</output>
