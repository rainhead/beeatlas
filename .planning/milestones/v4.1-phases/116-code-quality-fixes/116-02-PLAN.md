---
phase: 116-code-quality-fixes
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - data/run.py
autonomous: true
requirements: [CODE-02]

must_haves:
  truths:
    - "run.py module docstring lists every step name that appears in the STEPS list"
    - "Docstring step order matches STEPS execution order exactly"
    - "Newly added steps places-load, topology-postprocess, places-export, places-maps appear in the docstring"
  artifacts:
    - path: "data/run.py"
      provides: "accurate pipeline orchestration docstring"
      contains: "places-load"
  key_links:
    - from: "data/run.py module docstring"
      to: "data/run.py STEPS list"
      via: "step name parity"
      pattern: "places-load|topology-postprocess|places-export|places-maps"
---

<objective>
Update `data/run.py`'s module docstring so the "Pipelines are executed in this order" list names every step currently in `STEPS` and in the actual execution order. Closes CODE-02.

Purpose: Removes drift between documented and actual pipeline so future maintainers (and the AI assistant) see the truth at the top of the file.
Output: Updated module docstring listing all 19 pipeline steps in execution order.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Sync run.py module docstring with STEPS list</name>
  <files>data/run.py</files>
  <read_first>
    - data/run.py (current docstring lines 1-14 and STEPS list lines 81-101)
  </read_first>
  <action>
    Edit the module docstring at the top of `data/run.py` so the "Pipelines are executed in this order:" block enumerates every step name from the `STEPS` list in execution order. The current docstring lists 15 steps but omits 4 that exist in STEPS: `places-load` (between `places-validation` and `dbt-build`), `topology-postprocess` (between `dbt-build` and `species-export`), `places-export` (between `species-maps` and `feeds`), and `places-maps` (between `places-export` and `feeds`). The final order must read: ecdysis, ecdysis-links, inaturalist, waba, projects, anti-entropy, checklist, resolve-taxon-ids, taxa-download, taxon-lineage-extended, places-validation, places-load, dbt-build, topology-postprocess, species-export, species-maps, places-export, places-maps, feeds. Use the existing arrow-separated wrapping style (`->`); break across lines as needed to keep line length reasonable. Do NOT alter the Usage block, the Geographies note, the imports, or the STEPS list itself.
  </action>
  <verify>
    <automated>cd data && uv run python -c "import ast, sys; m=ast.parse(open('run.py').read()); doc=ast.get_docstring(m); steps=['ecdysis','ecdysis-links','inaturalist','waba','projects','anti-entropy','checklist','resolve-taxon-ids','taxa-download','taxon-lineage-extended','places-validation','places-load','dbt-build','topology-postprocess','species-export','species-maps','places-export','places-maps','feeds']; missing=[s for s in steps if s not in doc]; sys.exit(1 if missing else 0); print('all present')"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'places-load' data/run.py` returns ≥ 2 (one in docstring, one in STEPS).
    - `grep -c 'topology-postprocess' data/run.py` returns ≥ 2.
    - `grep -c 'places-export' data/run.py` returns ≥ 2.
    - `grep -c 'places-maps' data/run.py` returns ≥ 2.
    - The docstring lists step names in the same order as the STEPS list (verifiable by inspection — every step name in docstring appears before the next one in STEPS order).
    - `cd data && uv run python -c "import run; print(run.__doc__)"` prints a docstring containing all 19 step names.
    - `cd data && uv run python -c "import run; assert len(run.STEPS) == 19"` exits 0 (sanity check that STEPS list is unchanged).
  </acceptance_criteria>
  <done>Module docstring lists all 19 pipeline steps in execution order; STEPS list and imports unchanged.</done>
</task>

</tasks>

<verification>
- `cd data && uv run python -c "import run"` succeeds (no syntax error).
- The automated verify command in Task 1 exits 0.
</verification>

<success_criteria>
- `run.py` docstring lists every step name from STEPS in execution order, including the four previously missing steps.
</success_criteria>

<output>
Create `.planning/phases/116-code-quality-fixes/116-02-SUMMARY.md` when done.
</output>
