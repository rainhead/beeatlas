---
phase: 116-code-quality-fixes
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - data/places_validation.py
  - data/tests/test_places_validation.py
autonomous: true
requirements: [CODE-01]

must_haves:
  truths:
    - "validate_places() raises ValueError when any permit entry is missing 'issuing_authority'"
    - "validate_places() raises ValueError when any permit entry is missing 'type'"
    - "Valid places.toml (all permits well-formed or empty permits list) still passes validation"
    - "Permit field validation runs before geometry checks so a malformed permit fails fast"
    - "Error message identifies the offending place slug and the missing field name"
  artifacts:
    - path: "data/places_validation.py"
      provides: "permit field validation in validate_places()"
      contains: "permits"
    - path: "data/tests/test_places_validation.py"
      provides: "two new tests covering missing issuing_authority and missing type"
      contains: "permit"
  key_links:
    - from: "data/places_validation.py:validate_places"
      to: "place['permits']"
      via: "iteration with required-field check"
      pattern: "permits"
---

<objective>
Add permit-field validation to validate_places() so a places.toml entry whose permits list contains an item missing 'issuing_authority' or 'type' fails fast with a descriptive ValueError. Closes CODE-01.

Purpose: Catches malformed permit metadata at pipeline start rather than silently loading invalid records.
Output: Updated validator + two new pytest cases.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@CLAUDE.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add permit field validation between slug and geometry checks</name>
  <files>data/places_validation.py, data/tests/test_places_validation.py</files>
  <read_first>
    - data/places_validation.py (current validator body)
    - data/tests/test_places_validation.py (existing pytest patterns — `tmp_path`, `pytest.raises(ValueError, match=...)`)
    - content/places.toml (lines 1-40 — see real permits list shape: `permits = [ { issuing_authority = "...", type = "..." } ]`; empty `permits = []` is valid)
  </read_first>
  <behavior>
    - Test 1 (test_permit_missing_issuing_authority): A place whose permits list contains a dict missing the `issuing_authority` key raises ValueError matching pattern `permit missing required field 'issuing_authority'` and the offending slug appears in the message.
    - Test 2 (test_permit_missing_type): A place whose permits list contains a dict missing the `type` key raises ValueError matching pattern `permit missing required field 'type'` and the offending slug appears in the message.
    - Test 3 (test_empty_permits_list_passes): A place with `permits = []` does NOT raise (covered by adapting existing valid fixture or adding an explicit case).
    - Test 4 (test_place_without_permits_key_passes): A place that omits the `permits` key entirely does NOT raise (treat missing as empty list).
    - The new validation must execute BEFORE the geometry validity check so an invalid permit short-circuits before DuckDB spatial work begins.
  </behavior>
  <action>
    In `validate_places()` insert a new "Permit field validation" block between section "1 + 2 — Slug validation" and section "3 + 4 — Geometry validity". Iterate places, then iterate `place.get("permits", [])`, and for each permit dict check membership of the two required keys `issuing_authority` and `type`. Raise `ValueError(f"places.toml: place '{slug}': permit missing required field '{field}'")` matching the message format already used elsewhere in the function (single offending field per error — fail on first missing field). Treat a missing `permits` key as an empty list (no error). Add a comment header matching the existing `# ---- ... ----` style so the new section is documented as "0.5 — Permit field validation" or renumber to keep the docstring ordering consistent (update the module docstring "Checks performed in order" list to include the new permits check at position 3, renumbering subsequent items). Add the four pytest cases listed in `<behavior>` to `data/tests/test_places_validation.py` following the existing `tmp_path` + inline TOML fixture pattern (see `test_invalid_slug_chars` for the shape). Reuse a minimal valid `geometry_wkt` (e.g. the WKT from the existing valid fixture) so geometry checks don't fire.
  </action>
  <verify>
    <automated>cd data && uv run pytest tests/test_places_validation.py -x -v</automated>
  </verify>
  <acceptance_criteria>
    - `cd data && uv run pytest tests/test_places_validation.py -v` exits 0 and shows at least 4 new passing test names matching `test_permit_missing_issuing_authority`, `test_permit_missing_type`, `test_empty_permits_list_passes`, `test_place_without_permits_key_passes`.
    - `grep -n "permit missing required field" data/places_validation.py` returns at least one line inside `validate_places()`.
    - `grep -n 'permits' data/places_validation.py` shows the validator iterates `place.get("permits", [])` or equivalent (no KeyError on missing permits key).
    - The module docstring "Checks performed in order" list in `data/places_validation.py` mentions permit field validation as one of the numbered steps.
    - `cd data && uv run python -c "from places_validation import validate_places_step; validate_places_step()"` exits 0 against the real `content/places.toml` (real permits are well-formed).
  </acceptance_criteria>
  <done>Permit field validation enforces presence of `issuing_authority` and `type`, runs before geometry checks, has four passing tests, and real places.toml still validates.</done>
</task>

</tasks>

<verification>
- `cd data && uv run pytest tests/test_places_validation.py -v` shows ≥4 new passing permit tests, 0 failures.
- `cd data && uv run python run.py` would still pass the `places-validation` step against real `content/places.toml` (no need to run full pipeline; the targeted validator import call above suffices).
</verification>

<success_criteria>
- validate_places() raises a descriptive ValueError when any permit lacks `issuing_authority` or `type`.
- All new permit-field tests pass; existing tests in `test_places_validation.py` still pass.
- Real `content/places.toml` continues to validate.
</success_criteria>

<output>
Create `.planning/phases/116-code-quality-fixes/116-01-SUMMARY.md` when done.
</output>
