{
  "name": "beeatlas",
  "private": true,
  "version": "0.0.0",
  "description": "",
  "homepage": "https://github.com/rainhead/beeatlas#readme",
  "bugs": {
    "url": "https://github.com/rainhead/beeatlas/issues"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/rainhead/beeatlas.git"
  },
  "license": "ISC",
  "author": "",
  "type": "module",
  "scripts": {
    "predev": "node scripts/make-local-manifest.js",
    "dev": "eleventy --serve",
    "validate-species": "node scripts/validate-species.mjs",
    "validate-db": "node scripts/validate-db.mjs",
    "validate-bundle-size": "node scripts/validate-bundle-size.mjs",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "test:data": "BEEATLAS_DATA_TESTS=1 vitest run",
    "build:validate-links": "lychee --config lychee.toml --root-dir _site '_site/**/*.html'",
    "validate": "npm run validate-species && npm run validate-db && npm run typecheck",
    "build": "npm run validate && npm run build:app && eleventy && npm run build:sw && npm run validate-bundle-size",
    "build:app": "node scripts/build-app.mjs",
    "build:bundle": "vite build",
    "build:sw": "vite build -c vite.sw.config.ts",
    "build:content": "npm run validate && eleventy && node scripts/postbuild-data.mjs",
    "postbuild": "node scripts/postbuild-data.mjs && node scripts/build-receipt.mjs --write",
    "preview": "vite preview --outDir _site",
    "smoke": "node scripts/smoke-boot.mjs",
    "fetch-data": "bash scripts/fetch-data.sh",
    "pull-published": "bash scripts/pull-published.sh",
    "measure-lcp": "bash scripts/measure-lcp.sh"
  },
  "devDependencies": {
    "@11ty/eleventy": "^3.1.5",
    "@types/geojson": "^7946.0.16",
    "@types/node": "^24.13.3",
    "acorn": "^8.18.0",
    "happy-dom": "^20.11.1",
    "playwright": "^1.62.0",
    "typescript": "^7.0.2",
    "vite": "^8.1.5",
    "vitest": "^4.1.10",
    "workbox-cacheable-response": "^7.4.1",
    "workbox-expiration": "^7.4.1",
    "workbox-strategies": "^7.4.1"
  },
  "dependencies": {
    "@iarna/toml": "^2.2.5",
    "lit": "^3.3.3",
    "mapbox-gl": "^3.27.0",
    "vite-plugin-pwa": "^1.3.0",
    "wa-sqlite": "^1.0.0",
    "workbox-precaching": "^7.4.1",
    "workbox-routing": "^7.4.1",
    "workbox-window": "^7.4.1"
  }
}
