{
  "mcpServers": {
    "playwright": {
      "command": "npx",
      "args": [
        "-y",
        "@playwright/mcp@latest",
        "--browser=webkit",
        "--device=iPhone 15",
        "--isolated",
        "--headless"
      ]
    }
  }
}
